# ChatGPT/Codex local build-evidence reconciliation — beethoven (dirty worktree)

- Audit fingerprint: `48ada80335900f35bc5c5838c146e61ae362922b68aca6ff27076a5d37092110`
- Task: `chatgpt-local-reconcile-beethoven-48ada8033590`
- Evidence source: `/Users/kpasch/Documents/beethoven/claude-orchestrator` working tree (read-only; nothing stashed, cleaned, reset or moved)
- Entries enumerated from the live source: **590** (5 tracked modifications, 1 added-modified, 584 untracked)
- UNKNOWN items: **0**
- Machine-readable ledger: `.orch/recovery-ledger-48ada8033590.json`

## Classification summary

| Classification | Count |
|---|---|
| ALREADY_PRESENT | 0 |
| SUPERSEDED_BY_NEWER | 582 |
| ACTIVE_IN_ANOTHER_TASK | 1 |
| RECOVERABLE_VALUE | 3 |
| CONFLICTED_NEEDS_FOCUSED_TASK | 4 |

## Recovered value — one coherent slice, not three loose files

The uncommitted work is a single feature that had been left half-applied across a source file, an
entry point and a test:

- `runner/fleet_control.py` — adds `get_config(key, default)`: reads an `ORCH_`-prefixed key that
  `load_config()` has already promoted into the environment, and fails soft to the default on any
  error rather than raising into the runner.
- `runner/runner.py` — calls `fleet_control.load_config()` at startup, after the singleton lock so
  initialisation stays single-threaded, wrapped so a config-load failure warns instead of
  preventing boot. Without it, `ORCH_*` keys in `fleet_config` only took effect after a manual
  restart.
- `runner/test_config_consumer_comprehensive.py` — the 44-case suite for that behaviour.

They were taken together because taking any one alone leaves the tree incoherent: the test without
the source fails, and the source without the startup call is unreachable.

Verification: **44/44 passed**. `fleet_control.get_config('NOPE','dflt')` returns `dflt` on a
missing key, so the fail-soft path is exercised outside the mocks too.

## Deferred rather than shipped red

Two other untracked suites were recovered and then held back:

| Suite | Why |
|---|---|
| `runner/test_interlock_billing_killswitch.py` | 3 cases fail — `mock.patch` targets a module that is not present in this tree |
| `runner/test_orch_config_consumption.py` | 2 cases fail — asserts `load_config()` returns 3 where it returns 0 against the current schema |

Shipping them would put the merge train red for a reason unrelated to the slice above. Queued as
`beethoven-followup-dirty-worktree-red-config-suites`.

## The other 584 entries

- **552 `intake/processed/*`** — records of prompts the drop-box has already consumed. Operational
  history, not source.
- **15 `docs/decisions/ADR-*`** — auto-generated stubs from business-model checks, carrying no
  decision text.
- **3 `package-lock.json`** — generated; regenerated by install, never recovered by hand.
- **planner artifacts** at the repo root (`REFINED_SPEC-*`, `TASK-*`, `MEMO-*`): the work they
  describe is tracked as orchestrator tasks, not repo source.
- **`Updated show_greeting.py`** — a diff-header line captured as a filename by a malformed patch.
  The same artifact class turned up in the rescue-ref pass; it is not a file.
- **2 tracked modifications** (`runner/canary_validation.py`, `runner/test_new_module_config.py`)
  touch files `origin/master` already carries and need per-hunk adjudication, not a replay.
- **`docs/recovery/APPARENTLY_MANUAL_RESTART_CONTINUATION.md`** was already recovered on a sibling
  branch this session and is not duplicated.

## Verification

| | Failed | Passed |
|---|---|---|
| dirty checkout baseline (`-k "config or fleet"`) | 20 | 1092 |
| this branch | 19 | 1089 |

No new failure; the recovered suite is green. Counts differ slightly because the dirty checkout
also collects the two suites this branch deliberately defers.

## Provenance

- RECOVERABLE_VALUE -> branch `agent/chatgpt-local-reconcile-beethoven-48ada8033590` (this commit).
- CONFLICTED_NEEDS_FOCUSED_TASK -> `beethoven-followup-dirty-worktree-red-config-suites`.
- The working tree was not stashed, cleaned, reset or moved.

