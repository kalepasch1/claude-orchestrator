# canary-gpt-mini-39

Canary verification: documented bootstrap test fixture conventions.

## Change
- Noted the `env_minimal`, `mock_supabase_valid`, and `mock_supabase_invalid`
  fixture naming pattern in `test_bootstrap_runner.py` so future contributors
  follow the same convention when adding new test scenarios.
