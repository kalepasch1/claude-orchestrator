# v4 Global Pass — remediations, cross-app coordination, kill-shots, immediate readiness

Operator-authorized queue drop, 2026-07-09. Premise: all v1–v3 queued work deployed. This pass fixes the gaps that deployment will expose, wires cross-app coordination not yet queued, and adds conversion/readiness features. Planner: fan out per target repo; dedup against existing tasks (G7).

## A. Remediations (gaps the deployed system will hit)

- **A-1 CADE cost & latency governance (kernel + Tomorrow + Apparently)** — per-room/per-org/per-app CADE budgets under G13 allocation; determination memoization across similar clauses and rooms; standing-roster cache reuse; scheduled rooms pre-compute determinations overnight. Degrade-don't-stall invariant everywhere. Proof: budget + degradation tests.
- **A-2 Off-network counterparty fallback (Tomorrow/Smarter)** — W1 pre-resolution assumes both sides on-network. Add: shadow counterparty persona (CADE, from scouting corpus) so one-sided pre-staging still works; one-button email acceptance flow for off-network parties (generalize B12's path); every off-network close triggers the T8 onboarding funnel ("next time: zero paperwork"). Proof: fixture off-network negotiation pre-stages + email-accept round-trip.
- **A-3 Time-to-first-value (TTFV) golden journeys (all apps)** — extend O2 suite with latency budgets as CI gates: Tomorrow upload→priced quote <10 min; Apparently connect-repo→first filing draft <1 hr; Pareto link-accounts→first savings receipt <24 h; Galop install→first live race on screen <60 s; Triage install→first staked report <5 min; RAISE app-registered→first qualified target list <24 h. A TTFV regression blocks merge like any eval gate (G8). Proof: journeys wired into CI.
- **A-4 Cold-start honesty gates (all corpus-dependent engines)** — clause pricing, exam precedents, crowd benchmarks, calibration scores all assume corpus depth. Every determination/price/benchmark carries a corpus-depth badge; below threshold → CADE `unsettled` semantics (present as "insufficient precedent — expert-panel estimate" not false precision) + public-data bootstrap seeding per engine. Proof: thin-corpus fixture renders badge + degraded claim language.
- **A-5 Consent & information-barrier spine (kernel)** — one consent ledger for every cross-app data hop (passport claims, behavioral learning, credibility currency, Smarter→Tomorrow signals); each hop emits a barrier receipt (what crossed, under which consent/barrier rule — D4 pattern generalized); privilege-sensitive hops (Smarter matter data) get a dedicated barrier class with deny-by-default. Proof: unauthorized-hop deny test; receipt-per-hop test.

## B. Cross-app coordination (not yet queued)

- **B-1 One Onboarding Graph** — extend P0-PASSPORT from claims to full profile prefill: sign up in any app → entity/household graph pre-fills every other app's onboarding; invariant: the network never asks the same question twice. Proof: cross-app prefill fixture test.
- **B-2 Unified wallet & billing** — one subscription wallet across all apps (tiers, rewards fungibility I2, T8 rebates, RAISE-agnostic); cross-sell = one-click tier add, one invoice. Proof: cross-app entitlement test.
- **B-3 Kernel persona registry** — (mirrors PROMPT-w2a-expert-roster) persona definitions + reliability live once; all apps consume; calibration compounds portfolio-wide. 
- **B-4 Typed event fabric** — unify X2 oracle events, C5/D5 signals, coordination bus topics into one typed, replayable event fabric (H3 projections as the replay engine); any new app subscribes to the whole portfolio's nervous system on day one. Proof: replay determinism test.

## C. Kill-shots (competition-ending conversion features)

- **C-1 Switch Day migration engine (all apps; flagship on Tomorrow + Apparently)** — upload incumbent artifacts (insurance decs, vendor contracts, compliance calendars, ADW account exports, brokerage statements) → parsed → full migration plan: side-by-side cost/coverage/feature comparison, auto-drafted termination/non-renewal letters, overlap-free cutover calendar, projected savings receipt. One click to leave the incumbent. This is the highest-conversion feature in the portfolio; K6's dec-page parser generalizes into it. Proof: fixture incumbent-artifact set produces plan + letters + calendar.
- **C-2 Guarantee Everything doctrine** — generalize W7's calibration-gated guarantee pattern: each app publishes one measurable promise with auto-credit (Apparently: zero missed deadlines or the month is free; Galop: paid-play uptime SLA; Pareto: annual savings floor per tier; Triage: TM3 placement bands; Tomorrow: W7). Same machinery: shadow-first, calibration-gated activation, fee-credit-capped, circuit-breakered. Competitors cannot follow without our instrumentation. Proof: shared guarantee engine + per-app config tests.
- **C-3 Public proof pages (all apps)** — generalize T10: each app ships a live public metrics page from signed receipts (deadlines never missed, savings delivered, uptime, avoided losses, placements). Marketing that updates itself and cannot be faked — or matched. Proof: page renders from receipts only (no manual numbers — lint).
- **C-4 Receipts-grounded concierge (all apps)** — R2's natural-language query engine pointed at customers: "ask anything about your account/filing/position/program" answered from the user's own evidence graph with receipts, escalation to human path. Kills the "startups have no support" objection at zero marginal headcount. Proof: answer-grounding test (no unreceipted claims).
- **C-5 Competitor watchtower (orchestrator)** — the crawl infrastructure (A1, RAISE F1) pointed at competitors: filings, releases, pricing pages, job posts → per-app counter-positioning briefs and same-day marketing-surface update PRs (through brand/eval gates). Truth gate applies (receipted claims only in public counters). Proof: fixture competitor event produces brief + gated PR.

## D. Readiness polish (immediately user-friendly)

- **D-1 Mobile command surface** — X1 Autonomy Console as a mobile app with push approvals: war-room decisions, Pareto authority approvals, Apparently officer sign-offs, fleet brief, post-hoc review queue — the operator's and every user's whole day in 3 taps. Proof: approval round-trip from push notification fixture.
- **D-2 Zero-empty-states** — every app's first-run screens show live value before any user data: market-wide stats, public benchmarks, demo rooms/races/filings pre-loaded; no blank dashboards anywhere (lint: every route renders non-empty on a fresh account). 
- **D-3 Plain-language everything** — the W2d implication-card standard (audience-grade reading level, jargon expansion on hover, "what this means for you" section) applied as a portfolio lint on all user-facing generated text.

Sequencing: A-3 TTFV gates first (they measure everything else), then C-1 Switch Day (conversion), B-4 fabric + B-3 registry (multipliers), then the rest by G13 allocation.
