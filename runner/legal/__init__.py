"""Legal-document quality gates.

Pure-logic modules only: every corpus (competitor snapshots, required
jurisdictions) is INJECTED by the caller, so nothing here reaches the network,
the database, or the filesystem, and nothing here gives legal advice — these are
consistency checks on text a drafting agent produced.
"""
