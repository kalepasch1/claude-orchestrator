PROJECT: tomorrow

- id: perpetual-contracts-spec
  title: Perpetual Contracts product + system spec (Tomorrow side)
  material: yes
  model: opus
  depends: []
  proof: `npx -y markdownlint-cli2 docs/perpetual-contracts-spec.md` exits 0
  prompt: |
    Write docs/perpetual-contracts-spec.md — the Tomorrow-side technical/product
    spec for the parametric legal-event hedge that attaches to a legal work
    product. Cross-reference the smarter repo's docs/perpetual-contracts-
    structuring.md (three paths: licensed-carrier parametric insurance; CFTC event
    contract on a DCM; Regulatory Change Adjustment Rider/warranty). Cover:
    (1) trigger taxonomy + machine-readable trigger schema (statute/reg/precedent/
    agency-guidance events); (2) oracle interface that consumes Smarter
    Perpetual-Memo dependency-change events and verifies a trigger fired;
    (3) issuance flow (one-click, alongside the contract) and settlement flow
    (auto-payout on verified trigger), including the "perpetual" funding-rate
    persistence; (4) capacity/counterparty model per path; (5) MNPI information-
    wall requirements (firms hold deal MNPI; CFTC event-contract insider-trading
    regime); (6) audit + dispute handling. Honest framing: reduces (not
    eliminates) the regulatory-change peril; firm is not the issuer/seller. Not a
    filing and not advice.

- id: perpetual-contracts-pricing-design
  title: Pricing & funding-rate methodology for Perpetual Contracts
  material: yes
  model: sonnet
  depends: [perpetual-contracts-spec]
  proof: `npx -y markdownlint-cli2 docs/perpetual-contracts-pricing.md` exits 0
  prompt: |
    Write docs/perpetual-contracts-pricing.md defining how each contract is priced
    and how the perpetual funding rate re-prices over time. Cover: probability
    estimation of each trigger (Tomorrow markets + Consensus Engine ensemble),
    premium derivation, funding-rate mechanics (perp-swap analogy), payout basis
    options (fixed parametric vs. indemnity-style cost-to-re-paper), loss-ratio
    targets and capital/reinsurance buffer, adverse-selection and moral-hazard
    controls, and how the decorrelation score feeds carrier/capacity pricing. Give
    worked numeric examples. Methodology only — no live market data, no secrets.

- id: tomorrow-smarter-integration-design
  title: Smarter <-> Tomorrow oracle/event integration design
  material: yes
  model: sonnet
  depends: [perpetual-contracts-spec]
  proof: `npx -y markdownlint-cli2 docs/smarter-tomorrow-integration.md` exits 0
  prompt: |
    Write docs/smarter-tomorrow-integration.md describing the event contract
    between Smarter (Perpetual-Memo dependency-monitoring) and Tomorrow
    (issuance/pricing/settlement). Define: the event payload schema for a
    dependency/trigger change; the verification handshake; idempotency + replay
    safety; the information-wall boundary so deal-side MNPI in Smarter never
    reaches the tradable market side of Tomorrow; per-tenant isolation; and the
    coordination/implementation agents needed between the two apps. Design only.

- id: perpetual-contracts-prototype
  title: Standalone reference prototype of the contract lifecycle (mocked)
  material: yes
  model: opus
  depends: [perpetual-contracts-spec, perpetual-contracts-pricing-design]
  proof: `python3 -m pytest -q prototype/perpetual_contracts/tests` exits 0
  prompt: |
    Build a standalone reference prototype under prototype/perpetual_contracts/
    (independent of the main app stack). Implement, per docs/perpetual-contracts-
    spec.md and docs/perpetual-contracts-pricing.md: (1) the trigger schema +
    parser; (2) a MOCK oracle that accepts simulated dependency-change events and
    verifies triggers (no live data, no secrets); (3) a pricing stub implementing
    the funding-rate methodology with deterministic inputs; (4) an issuance ->
    active -> triggered -> settled state machine, with a MOCK carrier/market
    counterparty; (5) an audit log. Add prototype/perpetual_contracts/tests with
    pytest covering trigger verification, the pricing math, full lifecycle
    settlement, idempotent re-delivery, and the information-wall boundary (assert
    MNPI fields never cross to the market-side interface). README must state this
    is a prototype; carrier/CFTC/secrets are operator prerequisites for any real
    issuance. No secrets in code.

OPERATOR:
  - Licensed carrier/reinsurer partnership + bound capacity (Path 1) — external counterparty, not a code task.
  - CFTC/DCM registration and any "public interest" (Rule 40.11) submission, plus state insurance/gambling counsel sign-off per state (Path 2) — actual regulatory filings.
  - Production market/carrier API credentials + spend/exposure caps — secrets, do not commit.
  - Production deployment of any live issuance/settlement path — deploy step, gated on the above.
