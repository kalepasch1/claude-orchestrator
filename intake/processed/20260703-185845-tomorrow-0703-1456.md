PROJECT: tomorrow

# The 5/95 surface: embed the engine, show one line + one action. Ties together the
# already-queued+processed tasks (install-bundle, regenerate-belief-canvas-system,
# persist-target-exposure, wire-canvas-ui, bench-kpi-dashboard-tile, plus the
# conviction/hedge-aware engines). Those slugs EXIST — depends reference them by id.
# Repo: Nuxt 4 / TS / Prisma / Supabase. Prod build gate: `npm run build`.
# CONFLICT SAFETY: backend tasks are NEW files (parallel-safe). UI tasks are serialized on the
#   file they edit — command-bar-ui is the ONLY editor of pages/app/conviction.vue after
#   wire-canvas-ui; risk-line-ui is the ONLY editor of the dashboard page after bench-kpi-dashboard-tile.
# RULE FOR EVERY TASK: search the repo first; smallest additive diff that REUSES existing endpoints
#   (parse.post, solve-intent.post, approval-card.post, book-exposure.get, hedge-drift.get,
#   bench-win-rate.get, edge-radar.get, leaderboard.get, hivemind/status.get); keep the build green.

- id: risk-state-precompute
  title: Single cached 'risk state' assembler for instant first paint (the one line)
  material: no
  model: sonnet
  depends: [persist-target-exposure]
  proof: "npx vitest run server/utils/hive/__tests__/riskState.test.ts exits 0 AND npm run build exits 0"
  prompt: |
    Add server/utils/hive/riskState.ts (PURE): reduce the whole engine to ONE decision line. Input the
    already-computed pieces (book exposure from bookFactorExposure, drift from hedgeDrift vs persisted target,
    worst tail from tailFactors.stressAll, bench-win-rate, regret) and output
    { headline: string, severity: 'ok'|'watch'|'act', primaryAction?: {label, kind, payload}, deltas[] } —
    the "$6M over credit target; a rate shock amplifies 2.4x; Fix it?" sentence + at most one action. Add GET
    /api/hivemind/conviction/risk-state (requireUserId) that assembles it and caches the result per-user in
    Nitro useStorage with a short TTL (single cheap read on first paint; recompute lazily). NEW FILES ONLY.
    Unit-test the PURE assembler with fixtures (ok vs act severity, correct primaryAction) in riskState.test.ts.
    Keep the build green. CANDIDATE-SHARED: a one-line risk-state assembler suits any factor-modeled book.

- id: swarm-suggested-adjustments
  title: 1-click bot/swarm-predicted adjustments the CFO should make
  material: no
  model: sonnet
  depends: [install-bundle]
  proof: "npx vitest run server/utils/hive/__tests__/suggestedAdjustments.test.ts exits 0 AND npm run build exits 0"
  prompt: |
    The swarm should PROPOSE the moves, not wait to be asked. Add server/utils/hive/suggestedAdjustments.ts
    (PURE ranker): given signals already available — hive/swarm view (existing /api/hivemind/status +
    edge-radar unexpressed edge + leaderboard capital-routing), the user's drift-from-target (hedgeDrift), and
    tail amplification (tailFactors) — produce a ranked list of AdjustmentCard { id, title, rationale,
    severity, oneClick: {kind:'solve-intent'|'approval-card'|'conviction', payload} } where payload is a
    ready-to-POST body for the existing endpoints (so the UI button is literally one POST). Each card = one
    move the CFO likely should make (restore a drifted hedge, express edge the swarm sees, trim a tail
    concentration). Add GET /api/hivemind/conviction/suggested-adjustments (requireUserId) that gathers the
    signals fail-soft and returns the ranked cards. NEW FILES ONLY — reuse existing endpoints for data; do not
    duplicate their logic. Unit-test the PURE ranker with fixtures (a drifted hedge outranks a minor edge; each
    card carries a valid oneClick payload) in suggestedAdjustments.test.ts. Keep the build green.
    CANDIDATE-SHARED: swarm-predicted 1-click adjustments generalize to any book.

- id: command-endpoint
  title: One conversational input -> one card + the swarm's 1-click adjustments
  material: no
  model: sonnet
  depends: [swarm-suggested-adjustments]
  proof: "npx vitest run server/utils/hive/__tests__/commandRouter.test.ts exits 0 AND npm run build exits 0"
  prompt: |
    A sentence in, a sentence + one card out. Add server/utils/hive/commandRouter.ts with a PURE
    classifyIntent(text) -> {intent:'set-target'|'express-view'|'ask-status'|'unknown', slots} (deterministic
    keyword floor: "flat/neutral on X"->set-target flat X; "lean into/short/long X"->express-view; else
    ask-status). Add POST /api/hivemind/conviction/command (requireUserId): run classifyIntent (optionally
    refine with aiCall + AI_MODELS.fast, fail-soft to the deterministic floor), then route — set-target ->
    solve-intent.post; express-view -> parse.post then approval-card.post; ask-status -> risk-state — and
    ALWAYS also attach suggested-adjustments cards. Return { reply: string, card?, adjustments[] }. NEW FILES
    ONLY — reuse the existing endpoints/util functions; do not reimplement sizing/solve. Unit-test the PURE
    classifyIntent with fixtures (each phrase -> right intent+slots) in commandRouter.test.ts. Keep build green.
    CANDIDATE-SHARED: NL-command router over a factor engine.

- id: command-bar-ui
  title: The single NL command bar in the conviction page (sentence in, card + 1-click buttons out)
  material: no
  model: sonnet
  depends: [wire-canvas-ui, command-endpoint]
  proof: "npm run build exits 0"
  prompt: |
    Add ONE natural-language command bar to pages/app/conviction.vue that POSTs to /command and renders the
    returned {reply, card, adjustments}: the reply line, the single approval Card (its commitPayload posts to
    /api/hivemind/conviction), and the swarm's adjustments as 1-CLICK BUTTONS (each button POSTs its oneClick
    payload to the named endpoint, then shows the resulting card). Collapse all engine detail behind a single
    "why" toggle (5/95 — the 95% stays hidden until asked). ADDITIVE only; this is the ONLY task editing
    conviction.vue after wire-canvas-ui (chained to avoid conflict). New components go in components/conviction/.
    Prove with `npm run build` (SFCs must compile in prod). Do not change any engine/business logic.

- id: risk-line-ui
  title: The one-line Risk Line on the dashboard (precomputed, collapsed 95%)
  material: no
  model: haiku
  depends: [bench-kpi-dashboard-tile, risk-state-precompute]
  proof: "npm run build exits 0"
  prompt: |
    Render the single Risk Line from GET /risk-state at the top of the CFO dashboard: the headline sentence, a
    severity dot, and — only when severity is 'act' — one primary action button (its payload posts to the
    named endpoint). Everything else (factor bars, tail panel, bench detail) sits collapsed behind one "details"
    affordance and is lazy-loaded only on expand (performance: first paint is the single cached risk-state read,
    not a solver run). ADDITIVE; this is the ONLY task editing the dashboard page after bench-kpi-dashboard-tile
    (chained). New component in components/conviction/. Prove with `npm run build`.

OPERATOR:
  - suggested-adjustments reads /api/hivemind/status (live swarm view) — confirm it returns real data in prod, else the ranker falls back to the user's own drift/edge signals only.
  - The command endpoint uses aiCall (AI_MODELS.fast) for optional intent refinement; it fails soft to a deterministic classifier, so no key is strictly required, but set the Anthropic key for best NL understanding.
