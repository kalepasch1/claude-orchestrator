PROJECT: galop

# Second batch — social virality, AI moat, live tension, trust. Independent of
# galop-0629.md (the betting-mechanics batch); these reference already-committed
# primitives (rf_runner_intel, rf_live_field_feed, store/slip.ts, near-live
# resolver, pools, clans, push infra). Cross-file ordering is not assumed —
# dependencies below are within THIS file only; external prereqs noted in prose.

- id: share-result-card
  title: Auto-generated shareable result card on big wins (image + share sheet)
  material: no
  model: sonnet
  depends: []
  proof: `cd racefeed && npx tsc --noEmit` exits 0 AND `npm test` passes
  prompt: |
    Build a viral share-card. On any settled win above a threshold (single win
    dec_odds>=5, or any group/acca win), render a styled result card (silks via
    components/feed/silks.ts, horse + AI score, stake, payout, parlay legs if any,
    Galop wordmark) and let the user one-tap share it. Add dependency
    react-native-view-shot (Expo-compatible) and capture a hidden styled <View> to a
    PNG, then expo Sharing.shareAsync. New components/feed/ShareCard.tsx +
    hooks/useShareCard.ts. Trigger from the settle flash in
    components/feed/BetSlip.tsx and components/feed/ParlaySheet.tsx (a "Share win"
    action on the toast/result). No server/schema changes — purely client render of
    data the client already has. Acceptance: a winning bet produces a shareable PNG;
    the card includes legs + payout; no winner leak (only own settled data).

- id: nl-bet-entry
  title: Natural-language bet entry ("200 across the three steamers to place")
  material: no
  model: opus
  depends: []
  proof: `cd racefeed && npx tsc --noEmit` exits 0 AND `node --test lib/nlBet.test.ts` passes
  prompt: |
    Add a natural-language bet bar to the bet slip. User types intent; an LLM/intent
    layer parses it into a structured bet that maps onto the EXISTING audited RPCs
    (rf_submit_win / rf_submit_exotic / rf_submit_group / rf_submit_acca). Put the
    deterministic mapping + validation in lib/nlBet.ts (pure) with lib/nlBet.test.ts
    (node --test): given a parsed intent {action, selectors, stake, leg} resolve it
    against a race's runners + rf_runner_intel (selectors like "favourite",
    "top 3 steamers", "longshots", runner names/numbers) into concrete runner_no(s) +
    the right RPC call + args. The LLM call (use the app's existing AI config if present,
    else a thin client) only produces the intent JSON; ALL economy/submission stays
    server-authoritative via the existing RPCs (no new bet RPC, no schema change). Wire a
    text input + "interpret" preview into components/feed/BetSlip.tsx that shows the
    resolved bet before placing. Acceptance: lib/nlBet.test.ts covers favourite/steamers/
    longshots/named-runner/place-vs-win/parlay phrasings; preview matches what gets
    submitted; garbage input fails safe (no bet).

- id: in-running-cashout
  title: Live in-running odds drift on the running clock + mid-race cash-out
  material: yes
  model: opus
  depends: []
  proof: `cd racefeed && npx tsc --noEmit` exits 0 AND `npm test` passes AND live SQL check on Supabase project qlzsnuspiypyejaqcdad shows rf_cash_out_live exists, execute granted to authenticated only, winner-free
  prompt: |
    Add the most addictive surface in betting: a moving price during the race. As the
    LiveRaceCard running clock advances (components/feed/LiveRaceCard.tsx,
    feed_duration_seconds), show a drifting indicative cash-out value for the user's
    open single/win bet on that race, and let them cash out mid-race. New migration:
    rf_cash_out_live(p_bet_id uuid) — security definer, authenticated, own OPEN bet
    only; prices the cash-out from elapsed fraction + the runner's implied prob (mirror
    the existing rf_cash_out time-decay pattern), refunds the priced amount, marks the
    bet settled/void appropriately. MUST stay winner-free: the drift uses elapsed time +
    static odds only, never finish_pos. Apply migration to qlzsnuspiypyejaqcdad (grant
    authenticated). Surface a Cash-Out button with the live value on the card for the
    user's pending bet. Acceptance: value moves with the clock; cash-out refunds a priced
    amount and closes the ticket; no finish_pos read anywhere in the path.

- id: ai-calibration-rpc
  title: AI score calibration RPC (historical hit-rate by ai_score bucket)
  material: yes
  model: sonnet
  depends: []
  proof: `cd racefeed && npx tsc --noEmit` exits 0 AND live SQL check on qlzsnuspiypyejaqcdad shows rf_ai_calibration() returns buckets with a hit_rate field and execute granted to anon+authenticated
  prompt: |
    Make the AI score trustworthy by showing its track record. New migration:
    rf_ai_calibration() — security definer, returns aggregate hit-rate of the model by
    ai_score bucket over RESOLVED races (e.g. 0-20,20-40,40-60,60-80,80-100): for each
    resolved race recompute each runner's ai_score with the SAME formula as
    rf_runner_intel (factor it into a shared SQL helper so both stay in lock-step) and
    tally finish_pos=1 rate per bucket. Output is an AGGREGATE only (no per-live-race
    result), so it is winner-free for live races. Grant execute to anon+authenticated.
    Apply to qlzsnuspiypyejaqcdad. Then surface it in components/feed/HorseIntelPanel.tsx
    as "horses we rate 80+ win X% of the time" near the AI score. Acceptance: RPC returns
    monotonic-ish buckets; intel panel shows the calibration line; refactor keeps
    rf_runner_intel output identical (verify a sample race intel is unchanged).

- id: ai-race-narrator
  title: AI pre-race read + post-race recap on each card
  material: no
  model: opus
  depends: []
  proof: `cd racefeed && npx tsc --noEmit` exits 0 AND `npm test` passes
  prompt: |
    Turn silent clips into an opinionated experience. Add a short generated pre-race
    read (2-3 lines: draw, market move, the model's value angle) built from
    rf_runner_intel data (winner-free), and a post-race recap after a race the user bet
    on resolves (uses the settled result the user is already allowed to see). New
    hooks/useRaceNarration.ts + a collapsible narration strip in
    components/feed/LiveRaceCard.tsx. Use the app's existing AI config if present; cache
    per-race so it generates once. Keep pre-race strictly winner-free (no finish_pos).
    Acceptance: pre-race read appears from intel only; recap appears only post-settle for
    the user's own bet; cached so it doesn't regenerate on every scroll.

- id: beat-the-ai-mode
  title: "Beat the AI" head-to-head with a season-long human-vs-model scoreline
  material: yes
  model: sonnet
  depends: []
  proof: `cd racefeed && npx tsc --noEmit` exits 0 AND `npm test` passes AND live SQL check on qlzsnuspiypyejaqcdad shows table beat_ai_scores + rf_beat_ai_record exist, RLS read-own, RPC execute granted to authenticated only
  prompt: |
    Gamify the intelligence layer as a personal rivalry. On each race the model has a
    "pick" (top ai_score runner from rf_runner_intel). When the user places a win bet,
    record the head-to-head; on resolution, award a point to whoever (user pick vs model
    pick) finished better. New migration: table beat_ai_scores(user_id, wins, losses,
    ties, updated_at) RLS read-own + rf_beat_ai_record(p_race_id, p_user_runner) settle
    hook that updates the tally (winner-free input; graded server-side). Apply to
    qlzsnuspiypyejaqcdad. Surface the season scoreline in the RewardsHud drawer and a
    "You vs Galop AI" chip. Acceptance: a resolved race updates the tally correctly;
    RLS prevents reading others' scores.

- id: loss-recovery-rebuild
  title: One-tap rebuild of a parlay that missed by a single leg
  material: no
  model: sonnet
  depends: []
  proof: `cd racefeed && npx tsc --noEmit` exits 0 AND `npm test` passes
  prompt: |
    Capture the highest-converting re-engagement moment. When an acca settles as a loss
    with legs_correct == legs_total-1 (read own acca_bets row), surface a "Rebuild
    without the loser" prompt that pre-loads store/slip.ts with the winning legs (drop
    the missed leg) so the user can re-stake in one tap. Detect from the open-bets/settle
    path (or a small hooks/useNearMiss.ts reading own acca_bets). No economic change, no
    schema — it only pre-fills the slip. Acceptance: a near-miss acca produces a rebuild
    prompt with the correct legs; placing it goes through the existing rf_submit_acca.

- id: feed-chapters
  title: Interleave non-bet cards (highlight / quest / clan / tipster) into the feed
  material: no
  model: sonnet
  depends: []
  proof: `cd racefeed && npx tsc --noEmit` exits 0 AND `npm test` passes
  prompt: |
    Give the scroll TikTok-style variety. In app/(tabs)/index.tsx + the feed data
    (hooks/useLiveFeed.ts), interleave occasional non-race cards every N races: a
    "quest progress" card (useQuests), a "clan result/standings" card, and a "race of
    the day highlight" card. Add a discriminated-union feed item type and a renderer
    switch in the pager so race cards and chapter cards coexist in one FlatList. Keep
    autoplay/active logic working only for race cards. Acceptance: chapter cards appear
    at a configurable cadence; scrolling/auto-advance still works; tsc 0.

- id: bet-telemetry-ab
  title: Instrument new bet surfaces with analytics + server-gated UI variants
  material: no
  model: sonnet
  depends: []
  proof: `cd racefeed && npx tsc --noEmit` exits 0 AND `npm test` passes
  prompt: |
    Make "10-200X" measurable. Add PostHog events (lib/analytics.ts already exists) for
    every new bet surface: spread preset chosen, dutch vs even, parlay leg added/removed,
    parlay placed, group placed, win one-tap, intel opened, share tapped. Add a tiny
    server-gated variant flag reader (a config row or existing remote-config path) so the
    bet-slip default segment and the one-tap-vs-panel default can be A/B'd without a
    release. Acceptance: events fire with useful props (raceId, betType, stake, mode,
    legCount); a variant flag flips the slip default; no PII; tsc 0.

- id: live-stakes-push
  title: Push notifications tied to live stakes + clan urgency
  material: yes
  model: sonnet
  depends: []
  proof: `cd racefeed && npx tsc --noEmit` exits 0 AND live SQL/edge check on qlzsnuspiypyejaqcdad shows the scheduling/send path exists and is CRON_SECRET/service-role gated
  prompt: |
    Replace generic re-engagement pings with moment-driven push. Using the existing push
    infra (hooks/usePushNotifications, supabase/functions/notify-new-race pattern,
    push_tokens table), send: (a) "Your bet on the 3:40 is running now" when a user has an
    open bet on a race going live; (b) "Your clan needs one more win tonight" for clan
    urgency. Add a service-role/CRON_SECRET-gated edge function or cron RPC that finds
    open bets on now-live races + clan states and enqueues sends; respect notification
    prefs + quiet hours. Apply any schema to qlzsnuspiypyejaqcdad. Acceptance: the send
    path is gated (not anon-callable); a user with an open bet on a live race gets exactly
    one push; opted-out users get none.

OPERATOR:
  - DB migrations + edge functions apply to Supabase project qlzsnuspiypyejaqcdad via MCP/CLI. If the orchestrator lacks write access, an operator applies the migration files under racefeed/supabase/migrations/ (and any function under racefeed/supabase/functions/) for the material tasks: in-running-cashout, ai-calibration-rpc, beat-the-ai-mode, live-stakes-push.
  - AI features (nl-bet-entry, ai-race-narrator) need an LLM key/path. If the app has no existing AI config, an operator must provision one (e.g. an Anthropic key in the edge environment); until then ship behind a feature flag with a deterministic fallback.
  - share-result-card adds the react-native-view-shot dependency — operator/CI must `npm install` and rebuild the native app (Expo prebuild) before it works on device.
  - live-stakes-push requires APNs/push credentials already configured for the Expo project; confirm before enabling sends.
  - Device/visual QA after merge: share card render, NL bet preview accuracy, in-running cash-out feel, narration tone.
