PROJECT: tomorrow

# Frontier-2 (TOMORROW) — legal-book portfolio compression, compliance-risk term structure, adverse-selection defense,
# insurance displacement at book scale. Builds on the CEPL market (tomorrow-0629-creg.md) + PLOEH + Risk Studio.
# REUSE: riskFabric (PTRRS compression), otc/lending/marginalRisk + networkNetting, Risk Studio displacement/ILS,
# uncertaintyRates (term structure), the credit/oracle infra. Pure cores unit-testable (vitest.pure.config.ts).
# Bright line: parametric-only, bilateral ECP, swap-only, non-custodial.

- id: legal-book-compression
  title: Portfolio compression for the CEPL/legal book (PTRR for contracts)
  material: yes
  model: opus
  depends: []
  proof: "`npx vitest run server/utils/otc/cepl/__tests__/compression.test.ts` exits 0 (offsetting CEPL legs across a client's contract book net/cancel; the compressed book's gross notional < pre-compression while per-primitive net exposure is preserved fp-exact; named bilateral only, no mutualization)"
  prompt: |
    Hedge the NET legal risk, not 200 contracts. Reuse riskFabric PTRRS compression over a client's CEPL book:
    server/utils/otc/cepl/compression.ts: compressBook(legs) nets offsetting standardized legs across contracts,
    returning the compressed leg set (gross notional down, per-primitive net exposure preserved exactly). Enforce N6
    (named bilateral, no pool) + market-risk-neutral within tolerance. Pure, fp-exact, unit-tested (net preservation,
    gross reduction). MATERIAL. This is only possible because everything is in standardized units.

- id: compliance-term-structure
  title: Compliance-risk term structure (implied probability + timing curve per jurisdiction/activity)
  material: no
  model: opus
  depends: []
  proof: "`npx vitest run server/utils/otc/cepl/__tests__/term-structure.test.ts` exits 0 (buildTermStructure yields an arb-free curve of implied adverse-regime probability by horizon per (jurisdiction×activity); discount/survival factors monotone; no-arb enforced)"
  prompt: |
    A yield curve for regulatory risk. Reuse the uncertaintyRates arb-free curve machinery:
    server/utils/otc/cepl/termStructure.ts: buildTermStructure(primitiveMarks, calibratedProbs) → per
    (jurisdiction × activity_class) an arb-free curve of implied adverse-regime probability + expected timing by
    horizon. Pure, unit-tested (monotone survival, no-arb). This is a macro signal for Tomorrow's whole book AND an
    independently saleable data product; it also front-runs pricing.

- id: adverse-selection-defense
  title: Oracle-edge market-making — price adverse selection using the calibrated oracle + diffusion
  material: yes
  model: opus
  depends: []
  proof: "`npx vitest run server/utils/otc/cepl/__tests__/adverse-selection.test.ts` exits 0 (a taker hedging a primitive whose calibrated probability/diffusion signal is elevated gets a wider quote; the skew reflects the oracle's information edge; symmetric when no signal)"
  prompt: |
    Turn adverse selection into the moat. Because the closed-loop-calibrated oracle + diffusion forecaster front-run
    the information, you price takers better than they price you. Engine server/utils/otc/cepl/adverseSelection.ts:
    quoteWithEdge(primitive, side, oracleSignal) → a skew/width that widens against informed flow (elevated
    calibrated probability or a rising diffusion signal) and tightens when neutral. Pure quoting core unit-tested
    (skew direction, magnitude, symmetry). MATERIAL: it sets prices. This is the market-maker's edge that keeps the
    standardized market solvent.

- id: legal-insurance-displacement
  title: Displace D&O/E&O/regulatory-liability insurance with parametric CEPL baskets (book scale)
  material: yes
  model: opus
  depends: [legal-book-compression]
  proof: "`npx vitest run server/utils/otc/cepl/__tests__/insurance-displacement.test.ts` exits 0 (for a client's legal-risk profile, displacementAnalysis returns REPLACEABLE/COMPLEMENT/STATUTORY_KEEP per coverage line with the CEPL-basket premium vs the traditional insurance quote; parametric bright line enforced)"
  prompt: |
    The netted CEPL book IS a synthetic insurance book. Reuse Risk Studio's displacement engine:
    server/utils/otc/cepl/insuranceDisplacement.ts: displacementAnalysis(legalRiskProfile, insuranceQuotes) →
    per line (D&O, E&O, regulatory-liability, some GL) a verdict REPLACEABLE / COMPLEMENT / STATUTORY_KEEP with the
    parametric CEPL-basket premium (compressed/netted) vs the carrier quote. Enforce parametric-only /
    not-statutory-insurance. Pure analysis core unit-tested. MATERIAL + counsel review (insurance characterization).
    A new, cheaper insurance category originated from legal risk.

OPERATOR:
  - Counsel: insurance-displacement characterization (parametric swap vs regulated insurance) per line; the term structure + adverse-selection skew are pricing/market-conduct surfaces — confirm §2(h)(7) bilateral non-price-forming posture holds.
  - Deploy any new Prisma models/migrations after name-check + shadow dry-run; engines run fail-open on KV until then.
  - Capacity/underwriting sign-off before displacing any regulated coverage line for a client.
