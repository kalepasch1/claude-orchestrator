PROJECT: smarter

# Hosted-negotiation corpus for contract types (design/architecture, engineering, construction, consulting, engagement letters).
# Handoff: corpus/README.md, corpus/reference/contract-playbooks.json, corpus/config/sources.json.
# Already DONE + deployed to Supabase olaxnyrzoptjcntrrjgn (do NOT redo):
#   base tables corpus_documents/corpus_clauses/corpus_clause_embeddings (migration corpus_shared_reference);
#   corpus/scripts skeleton (shared lib + harvest-edgar + segment + export + ingest + emit + run-all);
#   shared Tomorrow exports in corpus/exports/; contract-playbooks.json (per-type clauses + checklist + tasks + deadlines).
# NOTE: smarter's DB has the base corpus tables but NOT the search/coverage fns or the v2 graph/log tables, and NO /api/research endpoint.

- id: deploy-search-fns
  title: Add search fns + v2 graph/log/gap tables + source_state to smarter DB (migration)
  material: yes
  model: sonnet
  depends: []
  proof: `test -f supabase/migrations/*corpus_search*/*.sql || ls supabase/migrations | grep -c corpus_search`
  prompt: |
    Author supabase/migrations/<ts>_corpus_search_and_v2.sql bringing smarter to parity with Apparently:
    fns search_corpus_authority + corpus_coverage + log_corpus_query + corpus_completeness +
    increment_corpus_fetch_count; tables corpus_citations + corpus_query_log + corpus_gaps + corpus_source_state;
    cols fetch_count/body_fetched/superseded/supersedes on corpus_documents. Copy the verbatim SQL from the
    Apparently deployment (see Apparently corpus/IMPROVEMENTS.md "Built this pass"). Material (migration).
    Proof = the migration file exists; human applies to prod.

- id: research-endpoint
  title: Build /api/research/query for the negotiation copilot
  material: no
  model: sonnet
  depends: [deploy-search-fns]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Port Apparently's server/api/research/query.post.ts into smarter (adapt to smarter's Nuxt server + Supabase
    client conventions). Filter by contract-type product/clause_type. Return cited clause/precedent results +
    coverage; log via log_corpus_query. Read-only, fail-soft.

- id: playbooks-from-corpus
  title: Regenerate contract-playbooks.json from the observed clause corpus
  material: no
  model: sonnet
  depends: [deploy-search-fns]
  proof: `node --check corpus/scripts/build-playbooks.mjs && node -e "JSON.parse(require('fs').readFileSync('corpus/reference/contract-playbooks.json','utf8'))"`
  prompt: |
    Today corpus/reference/contract-playbooks.json is hand-written/static. Write corpus/scripts/build-playbooks.mjs
    that reads corpus_clauses (per contract type) and derives, per type: the most-common clause types ->
    checklist items, and observed timing language -> typical deadlines. Merge with the existing curated
    skeleton (don't drop curated entries; augment with observed frequency + counts). Output valid JSON back to
    corpus/reference/contract-playbooks.json. Pure transform + a fallback to the existing file when the corpus
    is sparse. Proof = node --check + the output parses as JSON.

- id: copilot-wire-playbooks
  title: Wire the negotiation copilot to corpus_clauses + active-type playbook (checklist/tasks/deadlines)
  material: no
  model: sonnet
  depends: [research-endpoint]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    In the War Room / negotiation copilot, for the active contract type load (a) precedent clause language via
    /api/research/query (FTS over corpus_clauses) and (b) the matching entry from
    corpus/reference/contract-playbooks.json to render the negotiation checklist, standard tasks, and typical
    deadlines/timing. Additive to the existing copilot UI; fail-soft if the corpus is empty. vue-tsc 0 errors.

OPERATOR:
  - Apply the deploy-search-fns migration to smarter prod (olaxnyrzoptjcntrrjgn).
  - Run corpus/scripts/run-all.mjs (EDGAR commercial slice) in CI with a CORPUS_USER_AGENT + ingest-to-db.mjs to populate corpus_clauses before playbooks-from-corpus produces meaningful output.
  - Set VOYAGE_API_KEY if/when smarter wants the semantic layer.
