PROJECT: smarter

# Frontier-2 (SMARTER) — generative contract synthesis toward minimum hedge cost.
# Builds on the CREG intake (smarter-0629-creg.md): extractCreg, opinion-as-code, clause-hedge-pricing-feedback,
# and the live CEPL basket prices from Tomorrow over S2S. Conventions: TS strict; provider seam (llm.ts) for AI;
# pure cores unit-testable; Novelty Sentinel escalation for novel judgment (UPL-safe).

- id: generative-contract-synthesis
  title: Draft the contract whose CEPL hedge cost is minimized for a target risk posture
  material: no
  model: opus
  depends: []
  proof: "`npx vitest run tests/creg/generative-synthesis.test.ts` exits 0 (synthesizeContract iterates clause variants, prices each variant's CEPL basket, and returns the variant minimizing hedgeCost subject to the client's must-have constraints; a constraint violation is never returned)"
  prompt: |
    Flip analysis into generation. Engine server/utils/creg/generative-synthesis.ts: synthesizeContract({intent,
    mustHaves, riskPosture}) proposes clause variants (via the provider seam), extracts each variant's CREG, prices its
    CEPL basket (from Tomorrow over S2S), and returns the variant that minimizes hedge cost SUBJECT TO the client's
    must-have constraints (never violate a hard requirement). Contract drafting becomes cost-optimization against a
    live risk market — the provably-cheapest-to-bear contract, which no firm can produce because they can't price the
    residual. Pure optimizer core unit-tested (constraint respect, monotone toward lower hedge cost, deterministic on
    a fixed variant set + price map). Novel/edge clauses escalate to counsel.

- id: generative-synthesis-ui
  title: "Draft to minimize risk cost" UI in the contract composer
  material: no
  model: sonnet
  depends: [generative-contract-synthesis]
  proof: "`npx vue-tsc --noEmit` exits 0 AND the composer surface offers a 'minimize hedge cost' action that shows the synthesized variant, its CEPL basket price, and the deltas vs the original"
  prompt: |
    Surface it in Smarter's contract/opinion composer: a 'minimize risk cost' action that runs synthesizeContract and
    shows the recommended variant, its live CEPL hedge price, and a clause-level diff vs the original with the per-clause
    price deltas. Read-mostly over the engine; reuse existing composer scaffolding. Prove vue-tsc clean.

OPERATOR:
  - Counsel/UPL review: a system that generates contracts optimized against a risk market must keep the Novelty Sentinel escalation + human sign-off; confirm the "cheapest-to-hedge" objective never overrides a legal-necessity clause.
  - Provision PLOEH_S2S_SECRET so the synthesizer can price variants against Tomorrow's live CEPL market.
