PROJECT: apparently

# Self-driving legal opinion — APPARENTLY side (CEPL primitive library + CREG↔reg_facts grounding + perpetual memo).
# Coordinated 3-repo build with smarter-0629-creg.md + tomorrow-0629-creg.md; builds on the PLOEH intakes
# (apparently-0629-ploeh.md, tomorrow-0629-ploeh.md) already queued. Apparently owns the CANONICAL EXPOSURE PRIMITIVE
# LIBRARY (CEPL): the finite, curated set of STANDARDIZED parametric primitives that make hedges fungible + liquid.
# A bespoke contract's CREG (from Smarter) is expressed as a weighted CEPL basket + per-leg basis score + a thin
# bespoke residual. Shared schema in shared/schemas/{creg,cepl}.ts (must match all three repos). BRIGHT LINE
# (test-enforced): parametric/index triggers over reg_facts — never indemnity of actual loss. Builds on the PLOEH
# compliance-event-taxonomy + risk-vector oracle + memo binding. Conventions: typed SupabaseClient<Database>;
# callClaudeStructured + selectModel; idempotent name-checked migrations; pure cores unit-testable; legal steps human-gated.

- id: cepl-library
  title: Canonical Exposure Primitive Library — standardized parametric primitives (fungible, liquid)
  material: yes
  model: opus
  depends: []
  proof: "migration name-checks clean AND `npx vitest run tests/engines/cepl/library.test.ts` exits 0 (each primitive has a standardized deterministic trigger over reg_facts, a $1 payoff unit, a jurisdiction/activity-class taxonomy key, and a basis-grade; evaluatePrimitive fires correctly; two clients with the same exposure resolve to the SAME primitive)"
  prompt: |
    The core of the whole design. Migration supabase/migrations/<next>_cepl_library.sql: table cepl_primitives
    (id, primitive_key unique, event_type, activity_class, jurisdiction, trigger_spec jsonb (deterministic
    predicate over reg_facts, reusing ploeh event-taxonomy), payoff_unit, basis_grade, status, created_at) + RLS.
    Engine server/engines/cepl/library.ts: registerPrimitive, listPrimitives, evaluatePrimitive(primitive,
    regFacts, asOf) → {fired, quality} (stale/divergent → manual_review). Seed the initial library from the
    compliance-event taxonomy across the covered verticals/jurisdictions. The point: your "CA dual-currency ban"
    leg and my "CA dual-currency ban" leg are the SAME fungible instrument. Unit-test standardized firing +
    fungibility (same exposure → same primitive key). MATERIAL: schema + it defines the tradeable universe.

- id: creg-to-cepl-mapper
  title: Map a CREG node → weighted CEPL basket + basis score + residual flag
  material: no
  model: opus
  depends: [cepl-library]
  proof: "`npx vitest run tests/engines/cepl/mapper.test.ts` exits 0 (mapCregToCepl decomposes an exposure node into standardized primitive legs with weights + a basis score; the unhedgeable remainder is flagged as bespoke residual, kept thin)"
  prompt: |
    Decompose the bespoke into standardized legs (like replicating an exotic in vanillas). Engine
    server/engines/cepl/mapper.ts: mapCregToCepl(cregNode) → CeplBasket = weighted primitive legs + per-leg basis
    score (how well the standardized trigger matches the exposure — reuse the Risk Studio basis-model/backtester/
    trigger-ratings engines conceptually) + a bespoke-residual descriptor for what no primitive covers. Pure,
    unit-tested (decomposition, basis scoring, residual). The residual sleeve must stay small + measured.

- id: perpetual-memo-basket-binding
  title: Bind the living memo to a CEPL basket; re-weight (open/close signal) on every fact change
  material: yes
  model: sonnet
  depends: [creg-to-cepl-mapper]
  proof: "migration name-checks clean AND `npx vitest run tests/engines/cepl/memo-basket.test.ts` exits 0 (a reg_fact change touching the memo's subjects re-computes basket weights and version-appends a re-weight delta the hedge engine consumes as open/close signals — NOT a novation)"
  prompt: |
    The perpetual memo now speaks in CEPL basket weights. Migration <next>_cepl_memo_baskets.sql: table
    cepl_memo_baskets (binding_id, opinion_id, org_id, basket jsonb, version, emitted_at) + RLS. Engine
    server/engines/cepl/memo-basket.ts: onRegFactChange recomputes the CEPL basket for each bound memo and
    version-appends the DELTA (which legs to add-weight / reduce-weight) — expressed as open/close instructions,
    never novation. Emit the delta to Tomorrow over S2S. Unit-test re-weight + delta emission (mocked). MATERIAL: schema.

- id: closed-loop-oracle-calibration
  title: Calibrate the risk-vector oracle from realized trigger outcomes (Brier-scored)
  material: yes
  model: opus
  depends: [cepl-library]
  proof: "`npx vitest run tests/engines/cepl/calibration.test.ts` exits 0 (feeding realized fired/not-fired outcomes improves the Brier score of the per-primitive probability estimates; calibration adjustments persist and shift the next estimate toward the realized frequency)"
  prompt: |
    Because triggers settle continuously, the oracle can be better-calibrated than any actuary. Engine
    server/engines/cepl/calibration.ts: every fired/not-fired primitive outcome is a labeled probability event;
    computeCalibration(outcomes, priorProbabilities) → per-primitive adjustment (Platt/isotonic-style) that reduces
    Brier score; persist via the applied_config KV pattern and read it in the risk-vector oracle. Pure calibration
    core unit-tested (Brier improves; monotone toward realized frequency; min-sample guard). MATERIAL: it tunes
    pricing inputs — gate material shifts for approval.

- id: cepl-s2s-bridge-apparently
  title: Consume Smarter CREGs; emit CEPL baskets + basis + calibrated probabilities to Tomorrow
  material: yes
  model: sonnet
  depends: [creg-to-cepl-mapper, perpetual-memo-basket-binding]
  proof: "`npx vitest run tests/api/cepl-s2s.test.ts` exits 0 (verifies a Smarter-signed CREG; emits a signed CEPL basket + basis + calibrated per-primitive probabilities matching the shared schema; tamper/expiry rejected)"
  prompt: |
    The apparently hub of the seam. Ingest CREGs from Smarter (verify signature), map to CEPL baskets + basis, attach
    calibrated per-primitive probabilities (from the oracle + calibration), and emit to Tomorrow for pricing/hedging.
    Reuse the PLOEH S2S bridge (PLOEH_S2S_SECRET). Unit-test verify/map/emit/tamper against shared/schemas.
    MATERIAL: trust boundary; risk data only.

- id: cepl-ops-surface
  title: CEPL library + basket + basis + calibration dashboard
  material: no
  model: sonnet
  depends: [cepl-library, perpetual-memo-basket-binding]
  proof: "`npx vue-tsc --noEmit` exits 0 AND the surface renders the primitive library, a memo's CEPL basket + per-leg basis + residual, the basket re-weight history, and oracle calibration quality against real columns"
  prompt: |
    Surface the standardized layer: the CEPL primitive library (with basis grades), a bound memo's current CEPL
    basket + per-leg basis + bespoke residual, the basket re-weight timeseries, and per-primitive calibration
    (Brier) quality. Read-only over the new tables; reuse dashboard scaffolding. Prove vue-tsc clean.

OPERATOR:
  - Counsel sign-off on the CEPL primitive definitions as parametric (index) triggers — not indemnity of loss — and that each primitive sits inside the permitted swap allowlist / ECP-gating. This is the swap-vs-insurance crux at the primitive level.
  - Deploy the new migrations (cepl_primitives, cepl_memo_baskets) to prod after name-checking against the live Supabase project.
  - Provision PLOEH_S2S_SECRET + attestation signing key (shared across the three repos).
  - Approve the shared shared/schemas/{creg,cepl}.ts version jointly with smarter + tomorrow before dependent tasks build.
