PROJECT: tomorrow

# Remaining work for the conviction / hedge-aware engine.
#
# CONTEXT: A verified, tested bundle already exists at
#   outputs/tomorrow-conviction-hedge-engine/  (28 unit tests green)
# but the interactive session kept losing repo-tree writes to a workspace reset,
# so the code lives in that bundle, not yet in tomorrow/tomorrow/. Task `install-bundle`
# lands it; everything else depends on it. Repo: Nuxt 4 / TS / Prisma / Supabase.
# Conventions: requireUserId (~/server/utils/userAuth), loadPolicy (hive/mandatePolicy),
# usePrisma (~/server/utils/prisma), aiCall + AI_MODELS.fast (~/server/utils/aiConfig),
# requireCronAuth (~/server/utils/cronAuth), sendEmail (~/server/utils/emailService),
# vitest (`npx vitest run`), timestamped Prisma migrations (idempotent IF NOT EXISTS).

- id: install-bundle
  title: Land the delivered conviction/hedge-aware bundle into the repo tree
  material: no
  model: haiku
  depends: []
  proof: "npx vitest run server/utils/hive/__tests__/hedgeDecomposition.test.ts server/utils/hive/__tests__/benchAndResolution.test.ts server/utils/hive/__tests__/cfoSuite.test.ts exits 0 (28 tests pass)"
  prompt: |
    Copy every file from outputs/tomorrow-conviction-hedge-engine/ into
    tomorrow/tomorrow/ at the identical relative paths (server/utils/hive/*.ts,
    server/utils/hive/__tests__/*.test.ts, server/api/hivemind/conviction/*.ts,
    server/api/cron/*.ts). Do NOT rewrite logic — these are verified. After copying,
    run the three test files named in `proof` and confirm 28 pass. If any import path
    is stale relative to the current repo, fix only the import, not the logic.

- id: regenerate-belief-canvas-system
  title: Regenerate the earlier belief-canvas conviction system (was wiped by the workspace reset)
  material: no
  model: opus
  depends: [install-bundle]
  proof: "npx vitest run server/utils/hive/__tests__/convictionBelief.test.ts server/utils/hive/__tests__/convictionCalibration.test.ts server/utils/hive/__tests__/edgeRadar.test.ts server/utils/hive/__tests__/convictionRegret.test.ts server/utils/hive/__tests__/convictionBasketDecayPremortem.test.ts server/utils/hive/__tests__/convictionPhase2.test.ts server/utils/hive/__tests__/convictionService.test.ts exits 0 (74 tests pass)"
  prompt: |
    Rebuild the belief-canvas system that was built earlier and lost to the workspace
    reset. Pure engines (server/utils/hive/): convictionBelief (belief tuple + fractional-Kelly
    sizing reconciled to mandate: boost ceiling, single-event cap, override flag, calibration
    extension), convictionCalibration (Brier, reliability bins, earned sizing multiplier),
    edgeRadar (EV-ranked unexpressed edge), convictionRegret (undersized/mandate-blocked/oversized
    ledger), convictionBasket (correlation-weighted mandate-clamped basket), convictionDecay
    (edge half-life + broadcast tier), convictionPremortem (MNPI source gate + already-priced-in),
    convictionOutcomesData (Prisma join + snapshot join), marketProbability (venue quotes -> implied
    prob), convictionLeaderboard (suggested Kelly, specialist ranking, softmax capital routing),
    contractCorrelation (structural correlation heuristic — reuse riskFactors.cosine where useful),
    catalystPublicity (already-priced-in from market signals), convictionSnapshot (sizing snapshot
    persistence, fail-soft), convictionRegretDigest (weekly sweep), convictionService (pure façade
    re-exporting all engines + version + surface manifest). Endpoints under
    server/api/hivemind/conviction/: belief-size.post, calibration.get, regret.get, parse.post
    (aiCall NL->belief), premortem.post (folds in catalystPublicity), basket.post, edge-radar.get,
    market-price.get (scanRetailPricing + catalog fallback), suggested-kelly.get, leaderboard.get,
    capture.post, captures.get, contract-correlations.get. Cron: conviction-regret-digest.get.
    Write matching vitest files so the `proof` command shows 74 passing. Follow repo conventions
    listed in the header. Do NOT modify pages/*.vue in this task (see wire-canvas-ui).

- id: prisma-models-snapshot-capture
  title: Prisma migration + models for ConvictionSizingSnapshot and ConvictionCapture
  material: yes
  model: sonnet
  depends: [install-bundle]
  proof: "npx prisma validate exits 0 AND npx prisma migrate diff --from-schema-datamodel prisma/schema.prisma --to-schema-datasource prisma/schema.prisma --exit-code shows no drift after generate"
  prompt: |
    Add two Prisma models to prisma/schema.prisma matching repo style (@db.Uuid gen_random_uuid,
    @db.Timestamptz(6), Json?, @@index, @@map to snake_case): ConvictionSizingSnapshot
    (convictionId unique, orgId, recommendedSize/justifiedSize/mandateBlocked Decimal(20,2),
    boundBy String, snapshotJson Json?, createdAt) -> table conviction_sizing_snapshots; and
    ConvictionCapture (orgId, rawText, channel default 'app', parsedJson Json?, contractType?,
    status default 'draft', createdAt, updatedAt) -> table conviction_captures. Add a timestamped
    idempotent migration (CREATE TABLE IF NOT EXISTS + indexes) under prisma/migrations/. Run
    `npx prisma generate`. MATERIAL: schema/migration change — route to human approval before merge.

- id: persist-target-exposure
  title: Persist each CFO's target net factor exposure (the drift/letter keystone)
  material: yes
  model: sonnet
  depends: [install-bundle, prisma-models-snapshot-capture]
  proof: "npx vitest run server/utils/hive/__tests__/targetExposure.test.ts exits 0 AND npx prisma validate exits 0"
  prompt: |
    Add a Prisma model TargetExposure (orgId unique, targetJson Json for the FactorVector,
    tolerancesJson Json?, updatedAt) + idempotent timestamped migration + `prisma generate`.
    Add server/utils/hive/targetExposure.ts (pure helpers: normalize/validate a FactorVector
    against ALL_FACTORS from riskFactors.ts; diff current-vs-target) with a vitest file
    targetExposure.test.ts (proof). Add endpoints GET/PUT /api/hivemind/conviction/target-exposure
    (requireUserId, usePrisma, fail-soft). Then rewire hedge-drift.get and the risk-letter cron
    to compare against the persisted target instead of the current book. MATERIAL: schema change.

- id: factor-betas-feeder
  title: Weekly job to refresh factor loadings from realized settlement returns
  material: no
  model: sonnet
  depends: [install-bundle]
  proof: "npx vitest run server/utils/hive/__tests__/factorBetaFeeder.test.ts exits 0"
  prompt: |
    Add server/utils/hive/factorBetaFeeder.ts that assembles ReturnObservation[] (contract return
    vs factor returns) from settlement/mesh price history via usePrisma (fail-soft to []), calls
    estimateLoadingsFromReturns + blendWithPrior + sampleWeight from factorBetas.ts, and writes the
    blended loadings to a new Prisma model ContractFactorLoading (contractType unique, loadingsJson,
    sampleSize, updatedAt) — include idempotent migration. Add loadingsForLearned(contractType) that
    reads the learned row and falls back to riskFactors.loadingsFor. Add cron
    /api/cron/conviction-factor-betas (requireCronAuth). Unit-test the pure assembly/blend path with
    synthetic data (factorBetaFeeder.test.ts). NOTE: adds a model — if you add the migration here,
    set material: yes at merge; keeping betas in-code only would be material: no.

- id: full-factor-covariance
  title: Estimate the full factor covariance matrix and add variance-minimizing mode to the optimizer
  material: no
  model: opus
  depends: [install-bundle, factor-betas-feeder]
  proof: "npx vitest run server/utils/hive/__tests__/factorCovariance.test.ts exits 0"
  prompt: |
    Add server/utils/hive/factorCovariance.ts: estimate the factor covariance matrix from the same
    ReturnObservation feed (sample covariance + shrinkage toward a diagonal target for stability;
    pure). Extend bookOptimizer.solveToTarget with an optional objective:'variance' mode that, given
    the covariance matrix, minimizes portfolio factor variance subject to hitting the named targets
    (ridge-regularized quadratic solve reusing the existing Gaussian-elimination helper). Keep the
    default least-squares behavior unchanged. Unit tests: covariance is symmetric PSD-ish after
    shrinkage; variance mode produces lower estimated variance than the plain solve on a correlated
    synthetic book (factorCovariance.test.ts).

- id: scenario-conditioned-targets
  title: Scenario-conditioned target exposure (pre-position as regime probabilities shift)
  material: no
  model: sonnet
  depends: [persist-target-exposure]
  proof: "npx vitest run server/utils/hive/__tests__/scenarioTargets.test.ts exits 0"
  prompt: |
    Add server/utils/hive/scenarioTargets.ts (pure): a rule set mapping STRESS_SCENARIOS (from
    tailFactors.ts) + a regime-probability input to a blended target FactorVector (e.g. "net-flat
    credit unless recession prob > 0.4, then short credit"). Compose with predictionSignals if present
    (fail-soft). Extend the TargetExposure model/endpoint to store optional scenario rules and resolve
    the effective target from current regime probabilities. Unit-test the resolver: base target when
    calm, shifted target when a scenario fires (scenarioTargets.test.ts).

- id: wire-canvas-ui
  title: Wire the belief canvas + hedge-aware/book/approval-card panels into the conviction page
  material: no
  model: sonnet
  depends: [install-bundle, regenerate-belief-canvas-system]
  proof: "npx nuxi typecheck exits 0 AND npx vue-tsc --noEmit exits 0"
  prompt: |
    In pages/app/conviction.vue add (behind a toggle, Nuxt auto-imports components/composables):
    ConvictionCanvas (belief tuple + Kelly), a Book Factor Exposure panel (from /book-exposure),
    a Solve-for-intent panel (/solve-intent), and the one-tap Approval Card (/approval-card) whose
    commitPayload posts to /api/hivemind/conviction. Add composables/useConvictionBelief.ts and
    components/conviction/*.vue. Keep changes additive; do not remove existing composer behavior.
    The delivered ConvictionCanvas.vue + useConvictionBelief.ts in the outputs bundle history are a
    reference. Prove with typecheck (proof). No business-logic changes in this task.

- id: bench-kpi-dashboard-tile
  title: Surface bench-win-rate + regret as a CFO dashboard tile
  material: no
  model: haiku
  depends: [install-bundle, wire-canvas-ui]
  proof: "npx nuxi typecheck exits 0"
  prompt: |
    Add a small dashboard tile/component that calls GET /api/hivemind/conviction/bench-win-rate and
    /regret and renders the north-star (did convictions beat the market), win-rate, and net edge left
    on the table. Read-only, additive, theme-agnostic. Prove with typecheck.

OPERATOR:
  - Enable the new cron endpoints in vercel.json + set CRON_SECRET: /api/cron/conviction-resolution, /api/cron/conviction-regret-digest, /api/cron/conviction-risk-letter, /api/cron/conviction-factor-betas.
  - Apply the Prisma migrations to Supabase (prisma migrate deploy) after the material schema tasks are approved.
  - Confirm scanRetailPricing / getMispricingPrediction in hive/convictionRouter.ts return live venue data in prod (market-price + premortem publicity depend on it); provide Kalshi/Polymarket keys if a live feed is required.
  - Legal/compliance sign-off on the MNPI source-gate + attestation copy before it goes to real users (premortem.post over-mandate gate).
