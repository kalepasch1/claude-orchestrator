PROJECT: beethoven

# Integration-seam batch (2026-07-09, owner-directed): optimize every junction — Supabase,
# GitHub, Vercel, scheduler, coder workspace protocol, model gateway. Extend existing modules.

- id: relfix-claim-next-rpc
  title: claim_next() Postgres function — race-free sub-10ms claiming replaces the 1000-row scan
  material: yes
  model: opus
  depends: []
  proof: `python3 -m pytest runner/tests -q -k claim_next` exits 0
  prompt: |
    db.claim_task scans up to 1000 rows, sorts across ~15 criteria client-side, and races the
    other Mac. Write an idempotent migration creating claim_next(runner_id text, runnable_projects
    uuid[]) — a Postgres function that encodes the SAME ordering semantics (port the sort tuple
    from db.py claim_task: release-fix/recovery/improvement lanes, portfolio project rank,
    confidence rank, created_at; document each tier in SQL comments) selecting ONE task
    FOR UPDATE SKIP LOCKED, setting state=RUNNING+runner atomically, returning the row. Wire
    db.claim_task to try the RPC first (feature flag ORCH_CLAIM_RPC, default false) and fall back
    to the scan path on any error. Enable via fleet_config after one observed clean day (note in
    PR). Host-affinity: pass runnable project ids from repo_runnable_here. Tests: mocked RPC
    path, fallback, flag off = unchanged behavior. Include a pgTAP-style SQL test file or a
    python integration test marked for live-DB runs.

- id: relfix-github-app-migration
  title: GitHub App auth — installation tokens replace PATs (rate limits, scoping, one revocation)
  material: yes
  model: sonnet
  depends: []
  proof: `python3 -m pytest runner/tests -q -k gh_app` exits 0
  prompt: |
    runner/gh_auth.py: mint GitHub App installation tokens (JWT from app private key -> 
    installation token, cached until expiry) and expose gh_token() consumed by pr_integrate,
    ci_dispatch, and any git-over-https push path (credential helper or x-access-token remote
    rewrite — pick the least invasive; SSH remotes stay untouched). Fallback chain: App token ->
    GITHUB_TOKEN PAT -> gh CLI auth, so nothing breaks before the App exists. Also: native merge
    queue adoption plan — for PR-mode repos, detect if the repo has GitHub merge queue enabled
    and if so hand the merge to it (train marks card handed-off and polls the queue result)
    instead of custom serialization. Operator card on merge listing the exact App-creation steps
    (permissions: contents rw, pull_requests rw, checks r, actions r). Tests: token mint/cache
    with mocked API, fallback chain, merge-queue handoff detection.

- id: relfix-vercel-checks-cache
  title: Twin-QA as native Vercel checks + build-cache audit for all Nuxt repos
  material: no
  model: sonnet
  depends: []
  proof: `python3 -m pytest runner/tests -q -k vercel_checks` exits 0
  prompt: |
    (1) Vercel Checks API: register twin_qa (relfix-digital-twin-qa) as a check on deployments
    for each linked project — runner/vercel_checks.py: on deployment webhook/poll, create a
    check, run the app's personas against the deployment URL, report pass/fail; failed check
    blocks promotion natively. Needs VERCEL_TOKEN (exists for CLI — reuse). (2) Build cache
    audit: for each Nuxt repo verify build cache is effective (inspect recent build logs for
    cache restore lines via vercel CLI); where cold, fix the cause (lockfile churn, custom
    install commands bypassing cache) with per-repo intake tasks. Tests: check lifecycle mocked,
    log-parse cache detection.

- id: relfix-event-stream-scheduler
  title: Single asyncio scheduler + one structured JSONL event stream
  material: yes
  model: opus
  depends: []
  proof: `python3 -m pytest runner/tests -q -k event_stream` exits 0
  prompt: |
    Two-phase, careful — this touches the runner's spine. PHASE 1 (this task): the event stream.
    runner/events.py: emit(kind, **fields) appends structured JSONL to .runtime/events/<date>.jsonl
    (size-capped, rotated); migrate the highest-value emitters (sentinel, merge train outcomes,
    claim/finish transitions, governor decisions, deploy_watch) to ALSO emit events alongside
    existing prints; readers: the daily brief, dashboard API, fleet-RAG indexer consume the
    stream. PHASE 2 (emit a follow-up intake task, do NOT build here): consolidate the ~40
    subprocess periodic jobs into an asyncio scheduler with hot_reload, keeping subprocess
    isolation ONLY for jobs that need it (agent execution). Never regress the property that
    child jobs pick up new code without runner restart — document how the asyncio design
    preserves it (module reload on mtime change). Tests: emit/rotate/read-back, reader helpers,
    migrated emitters fire.

- id: relfix-workspace-protocol
  title: One workspace contract for every coder + fix aider model-name misconfigurations
  material: no
  model: sonnet
  depends: []
  proof: `python3 -m pytest runner/tests -q -k workspace_protocol` exits 0
  prompt: |
    Formalize what every coder receives, identically, via the adapter layer (agent-adapter-layer
    task, likely merged): runner/workspace_protocol.py builds a ContextPack per task — worktree
    path, proof command, applicable patterns, RAG chunks (when fleet-rag lands; fail-soft until),
    code-intel symbols (same), repo conventions (CLAUDE.md path), and budget/timeout — serialized
    to .orch/context.json inside the worktree so ANY coder (claude/codex/gemini/aider/ollama)
    reads one canonical file instead of bespoke launch flags. Migrate the existing launch paths
    to consume it. ALSO: audit agentic_coders'/aider's model name configs against each vendor's
    current model list (the operator's browser shows recurring aider model-warning pages =
    misconfigured names silently degrading to fallbacks) — fix the registry entries, add a
    startup validation that logs unknown model names as events. Tests: pack build/serialize,
    launch-path consumption, model-name validation.

- id: relfix-gateway-sole-egress-speed-tier
  title: model_gateway as the ONLY AI egress + Groq/Cerebras/xAI speed tier for judge/classify
  material: yes
  model: opus
  depends: []
  proof: `python3 -m pytest runner/tests -q -k gateway_egress` exits 0
  prompt: |
    (1) Sole egress: inventory every direct AI SDK/API callsite outside model_gateway (grep
    anthropic/openai/google clients across runner/); route them through the gateway (which owns
    keys, budget guards via billing_guard, retries, semantic cache when it lands, and per-vendor
    metrics). Add a CI-style check (scripts/check-gateway-egress.mjs or python) failing on new
    direct callsites, wired into the repo check chain. (2) Speed tier: add groq + cerebras + xai
    provider adapters to the gateway (OpenAI-compatible endpoints; keys from .env if present,
    provider skipped when absent) and route the operation tags 'classification','judge','triage'
    to the speed tier by default with fallback to the current provider. Budget caps per provider.
    Tests: egress check catches a synthetic violation, speed-tier routing + absent-key skip,
    fallback.

OPERATOR:
  - PITR backups: Supabase dashboard -> project -> Database -> Backups -> enable PITR (paid add-on, ~$100/mo on Pro for 7-day; a daily-backup alternative is included free — at minimum confirm daily backups are on). 2 minutes, real insurance.
  - GitHub App: when relfix-github-app-migration lands, its ops card walks you through creating the App (5 min, github.com/settings/apps) and installing it on your repos; paste the App ID + private key path into runner/.env.
  - Speed tier keys (optional, enables 10-50x faster judging): add GROQ_API_KEY and/or CEREBRAS_API_KEY and/or XAI_API_KEY to runner/.env — you already have an xAI console account per your open tabs.
