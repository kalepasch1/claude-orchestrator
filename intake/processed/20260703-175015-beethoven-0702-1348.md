PROJECT: beethoven

# Second batch of orchestrator-internal CADE improvements (the "additional 20-500X" levers). Built to mesh
# with what's already queued: the serialized committees.py chain (cade-contracts → … →
# cade-counter-bandit-causal → cade-determination-rlhf), my live modules (determinations table, certify(),
# deliberation_onepager(), outcome_instrument.py, constitution.py, causal_attribution.py, the
# determination_actions instant trigger), and the FIRST batch (cade-extras-harness + runner/cx_*.py, dropped
# earlier today). Conflict-free by construction: everything here is a NEW runner/cx_*.py file discovered by
# cade-extras-harness — EXCEPT the one certificate change, which edits committees.py and is therefore chained
# onto the committees chain tail so it can't run in parallel with it. All read tables that already exist
# (determinations, determination_outcomes, committee_scoreboard, committee_experiments, constitution_checks,
# owner_model, owner_overrides). Reuse committees.py helpers (db.*, review(), _consensus, _premortem,
# _cal_weight) — do not reimplement or edit committees.py except in the chained certificate task.

- id: cx-determination-memoize
  title: Semantic determination cache — reuse prior rulings on near-identical issues (with provenance)
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_determination_memoize.py` exits 0
  prompt: |
    Add runner/cx_determination_memoize.py with run(): before a fresh determination is needed, check whether
    a MATERIALLY-similar prior determination already exists (token/embedding overlap on title+body over the
    determinations table). When a strong match is found on a low-materiality subject, record a reuse
    suggestion (inbox kind='determination_reuse' citing the prior determination id + its proof_hash as
    provenance) so the engine can skip re-litigating — never silently; always cite provenance and re-check
    materiality. Cuts cost/latency at scale. Read-only except the reuse digest; no schema change; do not edit
    committees.py. CANDIDATE-SHARED: semantic determination cache.

- id: cade-counterfactual-certificate
  title: Counterfactual-bounded Optimality Certificate
  material: no
  model: sonnet
  depends: [cade-determination-rlhf]
  proof: `python3 -m py_compile runner/committees.py` exits 0 AND `python3 -c "import sys;sys.path.insert(0,'runner');import committees"` exits 0
  prompt: |
    Extend certify() in runner/committees.py so the certificate asserts a COUNTERFACTUAL bound: "no reachable
    alternative scored materially higher." Compute it from the panel's factions — the margin between the
    winning stance's weighted EV and the best-scoring losing stance — and store it on the certificate
    (counterfactual_margin + a one-line claim). Do NOT rewrite the file or existing functions; keep py_compile
    clean and add a focused test. This edits committees.py, so it is chained after the committees serial chain
    (cade-determination-rlhf) to avoid merge conflicts.

- id: cx-tribunal-model
  title: Tribunal / audience model — score determinations against the real decision-maker
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_tribunal_model.py` exits 0
  prompt: |
    Add runner/cx_tribunal_model.py with run(): for recent escalated determinations, forecast how the ACTUAL
    decision-maker (owner / regulator / investor depending on subject_type + domain) would likely react, and
    attach a short "audience read" (inbox kind='tribunal' or onto the determination's rationale note) so
    drafted outputs are tuned to who reads them. Reuse model_gateway.complete + the determination's
    contributors/factions as context; bounded to a few per run. No schema change; do not edit committees.py.

- id: cx-adversarial-sufficiency
  title: Adversarial sufficiency — gate high-materiality determinations on "attempts survived"
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_adversarial_sufficiency.py` exits 0
  prompt: |
    Add runner/cx_adversarial_sufficiency.py with run(): for the most recent high-materiality determinations
    (bounded, cost-safe on subscription models), run a short red-team that tries to move the verdict; record
    how many attempts it survived (inbox kind='adv_sufficiency' + a determination_outcomes row
    source='adversarial'). Surfaces determinations that look confident but are fragile, so robustness — not
    just consensus — informs autonomy. Reuse committees._premortem-style prompting + model_gateway; no schema
    change; do not edit committees.py.

- id: cx-calibration-budget
  title: Calibration-driven autonomy budget advisory (Brier-based, advisory only)
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_calibration_budget.py` exits 0
  prompt: |
    Add runner/cx_calibration_budget.py with run(): compute a recommended daily autonomy-budget adjustment
    from the Brier scores in committee_scoreboard (now that ground-truth is wired) and write it as an ADVISORY
    (owner_model key 'autonomy_budget_brier_adj' + an inbox note) — do NOT overwrite the live
    'autonomy_budget' that committees.tune_budget owns, to avoid two writers. This lets trust be earned on
    proper scoring, not raw accuracy. No schema change; do not edit committees.py.

- id: cx-contention-slo-alerts
  title: Portfolio contention SLO alerts — flag systemic uncertainty before it compounds
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_contention_slo_alerts.py` exits 0
  prompt: |
    Add runner/cx_contention_slo_alerts.py with run(): aggregate recent determinations by expert domain and
    open an inbox alert (kind='contention_slo') when any domain's average consensus stays below a floor
    (default 0.6, reuse owner_model consensus_floor as the reference) across N>=3 determinations — so systemic
    uncertainty in a domain surfaces early rather than compounding silently. Read-only except the alert digest;
    no schema change; do not edit committees.py.

OPERATOR:
  - None required. All tasks are runner-only, additive, and read tables that already exist — no secrets, deploys, schema migrations, or legal sign-off.
