PROJECT: tomorrow

# Curation -> War Room bridge + shared-package extraction + war-room capability
# builds. All ADDITIVE (new files) so they cannot merge-conflict with the in-flight
# curation tasks that edit dashboard.get.ts / index.vue / cron / schema. Rooms are
# the NegotiationRoom model created via server/utils/warRoom/templateWizard.ts
# (runTemplateWizard, called by server/api/firm/rooms/war-room-wizard.post.ts).
# Depends reference already-queued base tasks (curation-layer-land,
# curation-frontier-product-deltas). Prod build (nuxt build) is the merge gate.

- id: tomorrow-shared-curation-extract
  title: Extract the pure curation modules into a standalone packages/curation-core
  material: no
  model: sonnet
  depends: [curation-layer-land]
  proof: `npx vitest run packages/curation-core` exits 0 AND `npm run build` exits 0
  prompt: |
    Copy the PURE curation modules (types, institutionTypes, metricRegistry,
    regulatoryMetrics, ragStatus, remediationRouter, peerBenchmark, selfTuning,
    provenance, regulatoryRelevance, dashboardCurator, navBlueprint, valueAssembler,
    thresholdPolicy, remediationSimulator, constraintFrontier, filingGenerator,
    salienceBandit, curationDiff — plus the additive attribution/uncertainty/
    narrative/groupSupervision/conformance modules if present) with their tests into
    a NEW workspace packages/curation-core/ (own package.json + vitest config).
    Replace the single ~ import (CapitalSegment) with a locally-inlined InstitutionType
    union so the package has zero app dependencies. Do NOT rewire the tomorrow app
    yet — cross-app adoption is gated by the orchestrator orch-cross-project-depends
    task. Additive; the app build stays green. CANDIDATE-SHARED: packages/curation-core
    for apparently / smarter / pareto-2080.

- id: tomorrow-curation-warroom-bridge
  title: Auto-open a remediation War Room from a binding-constraint breach
  material: yes
  model: sonnet
  depends: [curation-frontier-product-deltas]
  proof: `npm run build` exits 0 AND `npx vitest run server/utils/warRoom/__tests__/curationBridge.test.ts` exits 0
  prompt: |
    Add a PURE server/utils/warRoom/curationBridge.ts exporting
    buildRoomSpecFromBreach(breach, frontier) that returns a NegotiationRoom spec
    (title, participants, agenda, pre-loaded remediation frontier + provenance)
    shaped for server/utils/warRoom/templateWizard.ts runTemplateWizard. Add a NEW
    endpoint server/api/curation/open-remediation-room.post.ts that, given a binding
    breach + the frontier (server/utils/curation/constraintFrontier.ts +
    productDeltas), creates a NegotiationRoom via the existing wizard path pre-loaded
    with the best remediation action. Do NOT edit curation-scan.post.ts (the alert
    path calls this endpoint) — avoids conflict with curation-snapshot-diff-alerts.
    Unit-test the pure builder. Material: creates rooms.

- id: tomorrow-warroom-remediation-playbook
  title: Generate a negotiation playbook from the remediation frontier
  material: no
  model: sonnet
  depends: [curation-frontier-product-deltas]
  proof: `npm run build` exits 0 AND `npx vitest run server/utils/warRoom/__tests__/remediationPlaybook.test.ts` exits 0
  prompt: |
    Add a PURE server/utils/warRoom/remediationPlaybook.ts turning the frontier
    best-action into a negotiation playbook (opening ask, ranked fallbacks, walk-away
    threshold) by REUSING the existing playbook type/structure in server/utils/warRoom
    (search for a playbook/leverage type first). Additive + unit test. This lets the
    remediation room open with a ready-to-run negotiation strategy, not just data.

- id: tomorrow-warroom-outcome-feedback
  title: Feed resolved remediation-room outcomes back into curation salience/KPI
  material: no
  model: sonnet
  depends: [tomorrow-curation-warroom-bridge]
  proof: `npm run build` exits 0 AND `npx vitest run server/utils/warRoom/__tests__/curationOutcomeFeedback.test.ts` exits 0
  prompt: |
    Add a PURE server/utils/warRoom/curationOutcomeFeedback.ts mapping a resolved
    remediation-room outcome (accepted action, realized metric move, time-to-resolve)
    into (a) curation salience feedback (server/utils/curation/salienceBandit.ts input)
    and (b) a curation KPI event. Pure core (no writes); returns the feedback objects
    the endpoints persist. Unit test the mapping. Closes the detection -> deal ->
    learning loop.

OPERATOR:
  - Cross-app adoption of packages/curation-core (rewiring apparently/smarter/pareto imports) is a follow-on wave gated by the orchestrator orch-cross-project-depends task — do not rewire apps until that lands.
