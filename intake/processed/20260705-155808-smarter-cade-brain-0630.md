PROJECT: smarter

# Extends CADE_IMPLEMENTATION_HANDOFF.md toward "CADE as the central brain of smarter."
# Obey smarter/CLAUDE.md + DARWIN_KERNEL_ADOPTION.md. Guardrails: additive only; fail
# closed (never loosen a locked gate); one repo per task; CADE DETERMINES, existing
# approval flows ACT — no autonomous sending/filing/money movement. Consume
# @darwin/kernel/cade (runDetermination, packageForReviewer); do not modify the kernel here.

- id: cade-advocacy-firewall
  title: Enforce the drafting/determination firewall (MNPI + advocacy vs. settlement)
  material: yes
  model: opus
  depends: []
  proof: `npx vitest run server/utils/cade/__tests__/firewall.test.ts` exits 0
  prompt: |
    Implement the information wall that keeps CADE defensible (handoff §5.5): the
    agents/personas that DRAFT or ADVOCATE a position must be isolated from the
    ones that DETERMINE/settle an outcome, and MNPI / client-confidential fields
    must never cross from the drafting side to any market/determination-facing
    interface. Build server/utils/cade/firewall.ts wrapping the invoker/package
    paths with a checkable boundary + provenance tags. Add
    server/utils/cade/__tests__/firewall.test.ts asserting: (a) a determination
    call cannot read drafting-only/MNPI fields; (b) packageForReviewer restyle
    cannot alter substance (semantic-diff guard already in kernel — assert it
    triggers); (c) any attempted cross-flow throws and is logged. Additive; do not
    change existing route behavior.

- id: cade-inbound-triage
  title: CADE auto-reviews every inbound document on receipt (flag / request / fix)
  material: yes
  model: opus
  depends: [cade-advocacy-firewall]
  proof: `npx vitest run server/engines/cade-inbound/__tests__/triage.test.ts` exits 0
  prompt: |
    When a document arrives (upload/email bridge/portal), decompose it into
    contestable units (toIssues) and run CADE review. Produce a triage object per
    finding with one of three dispositions: (1) FLAG-FOR-HUMAN — substantive/
    material issues (e.g., a case misinterprets a holding, a key contract clause is
    missing, a novel-judgment call) surfaced to the partner queue with the CADE
    dissent + certificate; (2) DRAFT-FACT-REQUEST — missing facts → an autonomous
    DRAFT request for the missing info, queued for human approval, never auto-sent;
    (3) AUTO-FIX — only mechanical, non-substantive errors (wrong citation format/
    dead cite replaced with the correct verified cite, misspelled party/company
    name, obvious typo) applied as a logged, reversible diff with the proof pack;
    anything above a materiality threshold routes to FLAG, not AUTO-FIX. Build
    server/engines/cade-inbound/ + triage.test.ts covering all three paths and the
    materiality gate (assert a misread-holding routes to FLAG and never AUTO-FIX;
    assert no outbound send occurs). Log every AI call; ship a signed proof per
    finding. Additive.

- id: cade-todo-autonomy
  title: Embed CADE into the smarter to-do list + autonomous draft/review/critique
  material: yes
  model: sonnet
  depends: [cade-inbound-triage]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Wire CADE into the smarter task/to-do surface so each actionable item can be
    (a) auto-drafted, (b) auto-reviewed/critiqued, and (c) error-checked by CADE,
    with the determination + dissent + certificate shown inline and a one-click
    "send to partner judgment queue" for anything flagged. Autonomy is bounded:
    CADE produces drafts and reviews; a human approves any outward action. Add the
    determination-rendering tile (position + confidence on the surface, "see the
    debate" reveals dissent/proof — the 5%/95% pattern). Preserve all existing RLS
    and approval gates. Additive; typecheck must stay clean.

- id: partner-tier-consortium-brain
  title: Partner-tier "Consortium Brain" — a CADE cowork console tuned to the portfolio
  material: yes
  model: opus
  depends: [cade-advocacy-firewall]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Build a Partner-tier-gated console: a cowork-style chat/agentic workspace whose
    brain is CADE, tuned to the consortium (smarter legal knowledge + Apparently
    process flows + Tomorrow risk), retrieving over the firm's isolated tenant plus
    the shared darwin_* knowledge with per-tenant isolation preserved. It can
    draft, review, source, triage, and kick programs (calls the mass-drafting
    orchestration). Gate strictly to the Partner subscription tier (entitlement
    check + RLS); associates/free tiers cannot access. No autonomous send/file —
    outward actions require approval. Ship the entitlement check, the console UI,
    the CADE-backed chat endpoint (POST /api/cade/console), and an entitlement
    test. Keep model strings from the repo constants; log all AI calls. Additive.

- id: mass-drafting-orchestration
  title: Partner-initiated mass document-program drafting (FCM exemplar)
  material: yes
  model: opus
  depends: [partner-tier-consortium-brain]
  proof: `npx vitest run server/engines/mass-drafting/__tests__/fcm.test.ts` exits 0
  prompt: |
    Let a partner state an objective in plain language ("register an FCM as fast as
    possible") and autonomously orchestrate the full program: pull the reusable
    process flow from Apparently's process-flow registry (cade-process-flow-
    registry), decompose into every required deliverable, run CADE per unit, and
    assemble the package — required registration/compliance filings, ongoing-
    compliance + exam-readiness documents, best-in-class INTERNAL policies (not
    strictly required but review-grade), and a compliance SPEC handoff for the dev
    team (the machine-checkable rules the product code must satisfy). Surface the
    FEWEST possible human-judgment questions (only genuinely novel/contested units,
    ranked, decision-ready). NOTHING is filed or sent autonomously — the partner
    approves; actual submission is an operator/human step. Build
    server/engines/mass-drafting/ + fcm.test.ts using an FCM golden fixture that
    asserts: full deliverable set produced, each unit carries a CADE proof, the
    judgment-question set is minimal + ranked, and no auto-file path exists.
    Additive.

OPERATOR:
  - Actual regulatory submission/filing (FCM registration, etc.) and any outbound client sending — human/operator action; CADE never files or sends autonomously.
  - Model API budgets / per-determination cost ceilings for the Partner console + mass drafting; DARWIN_SIGNING_PRIVATE_KEY_PEM in smarter prod env.
  - Legal/counsel sign-off before the mass-drafting program is offered for a given regulated regime (FCM/NFA, etc.).
  - Partner-tier entitlement/billing configuration (subscription product) in prod.
