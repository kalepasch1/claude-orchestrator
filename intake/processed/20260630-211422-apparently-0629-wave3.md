PROJECT: apparently

# Wave 3 — trust/liability, compounding-knowledge, cost, and workflow layers on the corpus.
# Handoffs: corpus/FLYWHEEL_STRATEGY.md, corpus/IMPROVEMENTS.md. Deployed objects + Wave-1/Wave-2 tasks assumed.
# Supabase oosolxvlfyifkhjohdzq. Reuse: search_corpus_authority, corpus_query_log, corpus_documents(superseded),
#   citation_freshness_scores, corpus-precedent.ts, research-agent (Wave-2), aiCall.

- id: regression-replay
  title: Nightly regression replay — "your past advice may now be wrong" alerts
  material: yes
  model: opus
  depends: []
  proof: `npx vitest run server/utils/__tests__/replay.test.ts` exits 0
  prompt: |
    Re-run every past answered query (from corpus_query_log) against the current corpus; when the top
    authority CHANGED vs the stored result_doc_ids (e.g. a new doc superseded the prior basis), write an
    answer_drift_alerts row (migration) keyed to the matter/client. Pure diff helper server/utils/replay.ts
    `detectDrift(prevDocIds, nowDocIds)` unit-tested. Add a CRON_SECRET-gated cron + nuxt/vercel registration.
    Material (writes alerts). Liability shield + recurring-revenue signal.

- id: llm-judge-selfaudit
  title: LLM-as-judge self-audit of cited propositions
  material: yes
  model: sonnet
  depends: []
  proof: `npx vitest run server/utils/__tests__/selfaudit.test.ts` exits 0
  prompt: |
    Sample recent answers; for each (proposition, cited span) call aiCall to judge supports|partial|contradicts.
    Persist to corpus_audit_findings (migration); contradictions feed the eval + raise a flag. Pure
    server/utils/selfaudit.ts `scoreSupport(...)` (parse + clamp verdicts, fail-soft) unit-tested. Cron-runnable.
    Material (new table).

- id: confidentiality-rings
  title: Per-tenant confidentiality rings on work-product precedent
  material: yes
  model: opus
  depends: []
  proof: `npx vitest run server/utils/__tests__/rings.test.ts` exits 0
  prompt: |
    Drafts-as-precedent must not leak across clients. Add owner_org/visibility cols to firm_work_product corpus
    rows (migration) + RLS so retrieval of work_product is scoped to the requesting tenant (public authority
    stays shared). Add a pure filter server/utils/rings.ts `scopeWorkProduct(rows, tenantId)` and unit-test that
    tenant A never sees tenant B's work_product while both see public docs. Touches RLS/auth -> material.

- id: living-restatements
  title: Canonical versioned "Restatements" (synthesized authority memos)
  material: yes
  model: opus
  depends: []
  proof: `npx vitest run server/utils/__tests__/restatement.test.ts` exits 0
  prompt: |
    For recurring questions, synthesize ALL authority into one canonical, versioned memo that becomes the first
    hit. Table corpus_restatements(topic, version, body, cited_doc_ids[], corpus_version, created_at) (migration).
    server/utils/restatement.ts `buildRestatement(topic, docs)` (aiCall synthesis grounded only in supplied docs;
    version bumps on corpus change). Index restatements into corpus_documents as doc_type='restatement' so they
    rank first. Unit-test versioning + that cited_doc_ids ⊆ supplied docs. Material.

- id: negative-space-answers
  title: "No authority on point" as a coverage-proven answer
  material: no
  model: sonnet
  depends: []
  proof: `npx vitest run server/utils/__tests__/negative-space.test.ts` exits 0
  prompt: |
    When retrieval + coverage indicate a question's universe is well-covered but no doc is on point, return a
    structured "no direct authority found (searched N of N <source> docs); nearest analogues: ..." answer rather
    than a weak guess. Pure helper server/utils/negative-space.ts `isNegativeSpace(results, coverage, threshold)`
    unit-tested. Wire into the research endpoint response. Read-only.

- id: matter-watch
  title: Matter-watch subscriptions (saved query -> new doc -> auto memo)
  material: yes
  model: sonnet
  depends: []
  proof: `npx vitest run server/utils/__tests__/matter-watch.test.ts` exits 0
  prompt: |
    Table matter_watches(matter_id, query, filters, last_checked, owner). A cron re-runs each watch; on a NEW
    matching corpus doc since last_checked, draft a short client memo (aiCall) + create a notification. Pure
    helper `newSince(results, lastChecked)` unit-tested; cron registered + CRON_SECRET-gated. Material (subs +
    notifications). Recurring-revenue monitoring product.

- id: entity-resolution-hub
  title: Shared entity-resolution hub (one entity graph, all apps query)
  material: yes
  model: opus
  depends: []
  proof: `npx vitest run server/utils/__tests__/entity-hub.test.ts` exits 0
  prompt: |
    Promote corpus_entities (Wave-2) into a queryable hub: canonical_entities(id, type, canonical_name, aliases[])
    + entity_mentions(entity_id, doc_id, span) (migration), with a resolver that dedupes aliases to a canonical
    id. Expose GET /api/research/entity?name= returning the entity + all corpus mentions. Pure
    server/utils/entity-hub.ts `resolveCanonical(name, candidates)` unit-tested. Material (new tables). Cross-app
    link target for Tomorrow/smarter.

- id: influence-graph
  title: Lobbying / comment-influence graph (who shapes final rules)
  material: no
  model: sonnet
  depends: []
  proof: `node --check corpus/scripts/build-influence-graph.mjs`
  prompt: |
    From regulations.gov comments + the resulting final rule, compute which commenters' language survived into
    the final text (n-gram / passage overlap). Write corpus/scripts/build-influence-graph.mjs producing an
    influence edge list (commenter -> rule, adopted_score). Data/analysis task; live run operator-gated. Proof =
    node --check.

- id: outcome-analytics
  title: Judge / regulator / ALJ outcome analytics
  material: no
  model: sonnet
  depends: []
  proof: `npx vitest run server/utils/__tests__/outcome-analytics.test.ts` exits 0
  prompt: |
    Aggregate litigation/enforcement outcomes by adjudicator/regulator from corpus_documents + corpus_entities
    to answer "before Judge X, argument Y wins Z%". Pure aggregator server/utils/outcome-analytics.ts unit-tested
    on synthetic rows; expose GET /api/research/outcomes?entity=. Read-only.

- id: answer-cache
  title: Synthesized-answer cache keyed by corpus version
  material: yes
  model: sonnet
  depends: []
  proof: `npx vitest run server/utils/__tests__/answer-cache.test.ts` exits 0
  prompt: |
    Cache full synthesized+cited answers keyed by (normalized_question, corpus_version); serve instantly until
    the corpus_version bumps (track a global corpus_version that increments on ingest). Table answer_cache
    (migration). Pure helpers `normalizeQuestion(q)` + `cacheKey(q, ver)` unit-tested. 100x latency/cost on
    repeats. Material (new table).

- id: difficulty-routing
  title: Difficulty-aware cost routing (haiku vs opus)
  material: no
  model: sonnet
  depends: []
  proof: `npx vitest run server/utils/__tests__/difficulty.test.ts` exits 0
  prompt: |
    Add server/utils/difficulty.ts `classifyDifficulty(question, retrievalStats)` (features: #legal elements,
    coverage, top_rank, novelty) -> easy|medium|hard, used to pick the cheapest capable model + retrieval depth
    for the research/synthesis path. Unit-test the feature thresholds. Read-only routing logic.

- id: embedding-versioning
  title: Embedding versioning + drift A/B
  material: yes
  model: sonnet
  depends: []
  proof: `npx vitest run server/utils/__tests__/embed-version.test.ts` exits 0
  prompt: |
    Add a `model` + `version` column to the embeddings tables (migration) so an embedding-model upgrade is an
    A/B (keep both, compare eval recall) not a destructive rebuild. Pure helper to select the active version +
    a fallback. Unit-test version selection. Material (schema).

- id: table-extraction
  title: Multi-modal table/figure extraction (the actual numbers)
  material: yes
  model: opus
  depends: []
  proof: `npx vitest run server/utils/__tests__/table-extract.test.ts` exits 0
  prompt: |
    Many rules/orders carry the operative numbers in tables (position limits, margin/fee schedules). Extract
    tables from doc bodies/PDFs into corpus_tables(doc_id, ordinal, caption, rows jsonb) (migration) so
    "what's the position limit for X" returns the number, not prose. Pure parser for HTML tables + a hook for
    PDF table extraction; unit-test the HTML-table parser. Material (new table).

- id: distill-dataset
  title: Build the distillation dataset for a small specialist model
  material: no
  model: sonnet
  depends: []
  proof: `node --check corpus/scripts/build-distill-dataset.mjs`
  prompt: |
    Export a training set from the corpus + corpus_query_log + human-edit labels: {question, retrieved_docs,
    accepted_answer, cited_doc_ids} as JSONL for distilling a small triage/classifier model. Write
    corpus/scripts/build-distill-dataset.mjs (repo-only output). The actual GPU training is OPERATOR. Proof =
    node --check.

- id: auto-bluebook
  title: Auto-Bluebook citation formatting + resolution validation
  material: no
  model: sonnet
  depends: []
  proof: `npx vitest run server/utils/__tests__/bluebook.test.ts` exits 0
  prompt: |
    server/utils/bluebook.ts `formatCite(parsed)` -> Bluebook string + `validateCite(cite)` that confirms it
    resolves to a real corpus doc (via corpus_citations normalized form). Unit-test formatting of usc/cfr/case/
    public_law + that an unresolvable cite is flagged. Read-only; removes a major associate time-sink.

- id: active-learning-queue
  title: Active-learning reviewer routing (max label value per minute)
  material: no
  model: sonnet
  depends: []
  proof: `npx vitest run server/utils/__tests__/active-learning.test.ts` exits 0
  prompt: |
    Prioritize human-review items by impact x uncertainty (high fetch_count x low support/confidence x
    contradiction flags from self-audit). Pure scorer server/utils/active-learning.ts `priorityScore(item)`
    unit-tested; expose GET /api/research/review-queue ordered by it. Read-only; compounds the learning-to-rank
    loop.

- id: dual-audience-explain
  title: Explain-to-a-regulator dual-audience generation
  material: no
  model: sonnet
  depends: []
  proof: `npx nuxi typecheck` exits 0
  prompt: |
    Add a generation helper that produces two grounded variants per answer/doc: a plain-language client version
    and a formal regulator-facing version, both cite-grounded. Expose as an option on the research/synthesis
    path. Read-only; fail-soft.

- id: coverage-scoreboard
  title: Public coverage / answered-from-corpus scoreboard (trust + sales)
  material: yes
  model: sonnet
  depends: []
  proof: `npx nuxi typecheck` exits 0
  prompt: |
    A public page + endpoint publishing anonymized per-universe coverage_pct + answered-from-corpus rate (from
    corpus_completeness + corpus_query_log). No PII; aggregate only. Publishing public content -> material
    (review before exposing). typecheck 0 errors.

OPERATOR:
  - Approve prod apply of new tables: answer_drift_alerts, corpus_audit_findings, work_product visibility/RLS,
    corpus_restatements, matter_watches, canonical_entities/entity_mentions, answer_cache, embedding version cols,
    corpus_tables.
  - Confirm public exposure is acceptable for coverage-scoreboard (aggregate, no PII).
  - Distillation (distill-dataset) training run needs GPU/offline pipeline + model-hosting decision.
  - Keys: aiCall (Anthropic) for judge/restatement/matter-watch synthesis; VOYAGE for any new embeddings.
