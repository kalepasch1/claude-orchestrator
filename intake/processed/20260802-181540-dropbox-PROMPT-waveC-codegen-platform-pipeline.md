# WAVE C (compounding): Codegen + platform spine + pipeline structure (operator-approved 2026-07-31)

project: beethoven

AUTHORITATIVE SPEC: /Users/kpasch/Documents/beethoven/IMPROVEMENTS_MASTER_UNQUEUED_2026-07-31.md
Read Parts 4, 6, 7. Cross-app pieces define contracts here; per-app surfaces are built by their
own shards.

## PART 4 — SELF-SERVICE CODE GENERATOR
TRANSPLANT-PROVEN-ORGANS: disposition ledger + merged-diff library at the raised 0.55 similarity
floor (never grow tumors — low-similarity "priors" hijacked real specs and are now banned).
CONTRACT-FIRST GENERATION: emit the failing test + type signatures FIRST, then fill — the verify
gate becomes the spec. PER-VERTICAL GOLDEN-PATH TEMPLATES distilled from top-decile merged
shards. STRATEGY-AWARE GENERATION: an approved tribunal strategy is context for every shard, so
product code is born compliant-by-design for the chosen structure (e.g. a sweepstakes-mechanic
entry generates AMOE flows + state-toggle gates natively) rather than remediated later.

## PART 6 — CROSS-APP PLATFORM
MATTER SPINE: intake, triage, licensing, filings, video education, and newsletters all keyed to
ONE matter record — the department inbox, the client portal, and Foulkon's exposure model become
three views of one truth. EXPOSURE-TO-HEDGE FLYWHEEL METRIC: % of quantified expected_loss_usd
across all clients that is hedgeable on Tomorrow today, trending — simultaneously a product-gap
tracker (unhedgeable exposure auto-feeds the instrument foundry), a Tomorrow demand signal, and
the investor stat. RENEWAL ANNUITY ENGINE: every completed filing schedules its own renewal and
reporting calendar wired to the ambient monitor (one-shot filings become recurring revenue).

## PART 7 — PIPELINE STRUCTURE (beyond the queued release-pipeline-completion shard)
INITIATIVE-LEVEL INTEGRATION: make the unit of merge the INITIATIVE, not the branch — shards
merge continuously into an initiative branch, ONE card judges the coherent changeset, so
thousands of merge decisions collapse to dozens and "is capability-OS live?" becomes a single
yes/no. DISPOSITION MEMORY: every branch closure (already-merged / superseded / stale) trains
the dedupe engine and the planner so duplicate work stops being GENERATED, not merely cleaned up.

BINDING: release windows govern prod promotion (merge continuously, promote in batches); build
suppression stays on; regression guard + static gate must stay green; tests per module; commits
kalepasch1 <kalepasch@gmail.com>.
