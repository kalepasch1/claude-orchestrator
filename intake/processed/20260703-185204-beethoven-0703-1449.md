PROJECT: beethoven

# Orchestrator self-improvement — the closing loop: turn the growth system's own detected gaps
# into PROPOSED intake tasks, human-gated. Depends on smarter's social-gap-detector (populates
# growth_intake_suggestion on Supabase eatfwdzfurujcuwlhdgj) so it runs only after that merges.
# The runner is Python (runner/*.py, pytest in runner/tests/); intake parsing lives in
# runner/intake_watcher.py. Build/merge gate = the runner pytest suite staying green.

- id: social-gap-to-intake
  title: Convert high-severity growth gaps into PROPOSED intake files (human-gated)
  material: yes
  model: sonnet
  depends: [social-gap-detector]
  proof: `cd claude-orchestrator/runner && python -m pytest tests/ -q` passes, including a new tests/test_social_gap_intake.py that feeds sample growth_intake_suggestion rows and asserts a well-formed intake markdown is written to intake/proposed/ (NOT intake/) with correct PROJECT + proof fields
  prompt: |
    Close the self-writing loop WITHOUT letting the growth system trigger autonomous work unreviewed.
    Add NEW module `runner/social_gap_intake.py` that reads high-severity rows from
    growth_intake_suggestion (Supabase eatfwdzfurujcuwlhdgj; reuse whatever Supabase/env access
    runner already uses — search runner/ first, do not add a new client pattern) and drafts intake
    markdown in the EXACT format runner/intake_watcher.py consumes (PROJECT, id, title, material,
    model, depends, proof, prompt). CRITICAL: write to a NEW `intake/proposed/` directory, never
    directly to `intake/` — a human moves approved files into intake/. Map each gap kind to a small,
    build-green deliverable (e.g. "account with 0 posts in 7d" -> a task to apply a starter scheme;
    "drafts stuck needs_generation" -> a fillDrafts trigger task) with a concrete `proof` that keeps
    `nuxt build` green. Idempotent: skip a gap that already has a proposed/queued/processed task
    (dedupe by a stable slug). Add tests/test_social_gap_intake.py. Do NOT modify intake_watcher.py's
    live-intake behavior. CANDIDATE-SHARED: gap->proposed-intake generator generalizes to every app.

OPERATOR:
  - This is intentionally human-gated: it writes to intake/proposed/, never intake/. A human reviews and moves approved proposals into intake/ to actually queue them. Do not automate the proposed/ -> intake/ move without explicit sign-off (it would let the system self-trigger autonomous merges).
