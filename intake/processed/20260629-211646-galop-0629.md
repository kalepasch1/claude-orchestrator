PROJECT: galop

- id: streak-mult-payouts
  title: Apply streak multiplier to win/group/acca bet payouts (server + client)
  material: yes
  model: opus
  depends: []
  proof: `cd racefeed && npx tsc --noEmit` exits 0 AND `npm test` passes AND a live read-only SQL check on Supabase project qlzsnuspiypyejaqcdad shows rf_streak_mult exists and submit RPCs return a `mult` field
  prompt: |
    Implement TASK 1 of galop/GALOP_FEED_BETTING_BUILD_PROMPT.md verbatim.
    The Rewards HUD shows a streak multiplier (racefeed/lib/odds.ts streakMultiplier:
    1.0 / >=5->1.2 / >=10->1.5 / >=15->2.0 / >=20->3.0) but the new bet payouts
    ignore it. Wire it in, PROFIT-ONLY, using the streak at settle time, clamped
    to existing coin caps.
    Backend — new migration racefeed/supabase/migrations/2026063000_streak_boost.sql:
      1) Add pure helper rf_streak_mult(int)->numeric mirroring streakMultiplier
         (single source of truth).
      2) In rf_submit_win resolved path: read caller streaks.current_streak,
         v_pay := stake + round((v_pay - stake) * mult), clamp to v_cap (stake*200);
         return `mult` and `boosted_payout`.
      3) Same in resolved paths of rf_submit_group (win leg only) and rf_submit_acca.
      4) Same in deferred settle sweeps rf_settle_exotics (bet_type='win' only),
         rf_settle_groups, rf_settle_accas — read streaks.current_streak per bet user_id.
      5) Leave place/each_way/exacta/forecast UNBOOSTED.
    Frontend: add mult?/boosted_payout? to racefeed/types/feed.ts WinBetResult +
    GroupBetResult (+ acca result); show "(xN streak)" in settle flashes in
    racefeed/components/feed/BetSlip.tsx + racefeed/components/feed/ParlaySheet.tsx;
    add a "xN streak boost active" badge in the bet slip header when current streak
    gives mult>1.
    Posture: server-authoritative, winner-free, coins only. Apply migration via Supabase
    MCP to project qlzsnuspiypyejaqcdad; grants submit->authenticated, settle->service_role.
    Acceptance: streak-0 user sees identical payouts (no regression); profit (not stake)
    scales; never exceeds cap.

- id: one-tap-rail-bet
  title: One-tap rail betting (tap=instant win + undo, long-press=intel) + rf_void_bet
  material: yes
  model: sonnet
  depends: [streak-mult-payouts]
  proof: `cd racefeed && npx tsc --noEmit` exits 0 AND `npm test` passes AND live SQL check shows rf_void_bet exists with execute granted to authenticated only
  prompt: |
    Implement TASK 2 of galop/GALOP_FEED_BETTING_BUILD_PROMPT.md. In
    racefeed/components/feed/FieldRail.tsx + racefeed/components/feed/LiveRaceCard.tsx:
    tap a runner chip = instant Win bet at the last-used stake (persist last stake in
    racefeed/store/slip.ts) with an undo flash toast (~4s); MOVE the open-intel action
    to long-press. Add an inline stake stepper chip (cycle 50/100/250/500) on the rail.
    New small migration: rf_void_bet(p_bet_id uuid) — security definer, authenticated,
    own-row only, only if outcome is null AND within 10s of created_at; refunds stake,
    sets outcome='void'. Must handle exotic_bets win rows (and reject others gracefully).
    Wire undo -> rf_void_bet. Increment the same RG/session counters as the slip path
    (racefeed/hooks/useSession). Apply migration to qlzsnuspiypyejaqcdad. Acceptance:
    tap places + undo refunds within window; long-press still opens intel; RG counters
    increment.

- id: ai-spread-presets
  title: AI-driven Spread presets in the bet slip (Value 3 / Steamers / AI Longshots / Banker+spread)
  material: no
  model: sonnet
  depends: []
  proof: `cd racefeed && npx tsc --noEmit` exits 0 AND `npm test` passes
  prompt: |
    Implement TASK 3 of galop/GALOP_FEED_BETTING_BUILD_PROMPT.md. In
    racefeed/components/feed/BetSlip.tsx Spread segment, add presets driven by
    rf_runner_intel (fetch via racefeed/hooks/useRunnerIntel for the open race):
    Value 3 (top 3 by ai_score - market_pct), Steamers (market_move='steamer'),
    AI Longshots (ai_score high but dec_odds>=8), Banker+spread (one favourite to Win
    via rf_submit_win + dutch the rest to Place via rf_submit_group, submitted as two
    calls in one Place action). Show each preset's modelled hit-rate (sum of win_prob)
    and expected return. No schema/RPC changes (reuse existing). Acceptance: presets
    select correct runners from live intel; hit-rate shown; submits succeed.

- id: auto-staking-solver
  title: Target-return / target-chance auto-staking + Kelly-lite (tested lib/staking.ts)
  material: no
  model: sonnet
  depends: [ai-spread-presets]
  proof: `cd racefeed && npx tsc --noEmit` exits 0 AND `node --test lib/staking.test.ts` passes
  prompt: |
    Implement TASK 4 of galop/GALOP_FEED_BETTING_BUILD_PROMPT.md. Add
    racefeed/lib/staking.ts (pure) + racefeed/lib/staking.test.ts (node --test).
    Functions: dutchStakeForTargetReturn(target, decOdds[]) = target * sum(1/odds);
    pickRunnersForTargetChance(intel[], target) greedily adds by win_prob until summed
    de-vigged prob >= target then dutches them; kellyLiteStake(winProb, decOdds,
    bankroll, mult). Wire two solver modes into the Spread sheet in
    racefeed/components/feed/BetSlip.tsx (preview only; submission stays via the server
    RPC). Acceptance: solver math has unit tests; preview matches server potential
    within rounding.

- id: open-bets-ribbon
  title: Live open-bets ribbon + in-place settle animation
  material: no
  model: sonnet
  depends: [streak-mult-payouts]
  proof: `cd racefeed && npx tsc --noEmit` exits 0 AND `npm test` passes
  prompt: |
    Implement TASK 5 of galop/GALOP_FEED_BETTING_BUILD_PROMPT.md. Add
    racefeed/hooks/useOpenBets.ts reading own open tickets (RLS) across exotic_bets,
    group_bets, acca_bets where outcome is null. Add
    racefeed/components/feed/OpenBetsRibbon.tsx: slim strip above the tab bar listing
    pending tickets + each race's running clock; on resolution (reuse
    useCoins.pollResolution pattern) fire a win/loss animation in place + coin tick.
    Mount as an overlay in racefeed/app/(tabs)/index.tsx like BetBuilderBar.
    Winner-free: read own settled rows only. No schema changes.

- id: parlay-intel-cashout
  title: Parlay suggested-next-leg + acca cash-out (rf_cash_out_acca) + shareable slip deep link
  material: yes
  model: opus
  depends: [open-bets-ribbon]
  proof: `cd racefeed && npx tsc --noEmit` exits 0 AND `npm test` passes AND live SQL check shows rf_cash_out_acca exists, execute granted to authenticated only
  prompt: |
    Implement TASK 6 of galop/GALOP_FEED_BETTING_BUILD_PROMPT.md. (a) In
    racefeed/components/feed/ParlaySheet.tsx, while the slip is non-empty surface the
    highest ai_score runner from the next feed race as a one-tap add. (b) New migration:
    rf_cash_out_acca(p_bet_id uuid) — security definer, authenticated, own open acca
    only; prices remaining legs at current implied probability and settles early
    (mirror existing rf_cash_out). Surface a Cash-Out button per pending acca in the
    OpenBetsRibbon. (c) Shareable slip: encode slip legs into an expo-linking deep link;
    opening it pre-loads racefeed/store/slip.ts. Apply migration to qlzsnuspiypyejaqcdad
    (grants: authenticated). Acceptance: suggested leg adds; cash-out refunds a priced
    amount + marks settled; deep link round-trips a slip.

- id: mechanics-bet-hooks
  title: New quests/loot/spin tied to the new bet types
  material: no
  model: sonnet
  depends: [streak-mult-payouts]
  proof: `cd racefeed && npx tsc --noEmit` exits 0 AND `npm test` passes
  prompt: |
    Implement TASK 7 of galop/GALOP_FEED_BETTING_BUILD_PROMPT.md. Add quest defs in
    racefeed/hooks/useQuests.ts: "Place a 4-leg parlay", "Win a dutch spread",
    "Hit a 1-2 finish"; emit progress from the new submit results (call quest recorders
    from racefeed/components/feed/BetSlip.tsx + ParlaySheet.tsx on success). Grant a loot
    chest on a big-odds win (settled win/acca with dec_odds/combined_odds >= 10) via a
    server hook in the settle paths or a client rf_sync_chests after a qualifying win.
    Ensure new quest completions + chest grants surface in
    racefeed/components/feed/RewardsHud.tsx (reads rf_loot_state). If a settle-path
    server change is needed, add an additive migration to qlzsnuspiypyejaqcdad.

- id: history-all-tickets
  title: Surface group_bets + acca_bets (with boost) in pick history / profile
  material: no
  model: sonnet
  depends: [streak-mult-payouts]
  proof: `cd racefeed && npx tsc --noEmit` exits 0 AND `npm test` passes
  prompt: |
    Implement TASK 8 of galop/GALOP_FEED_BETTING_BUILD_PROMPT.md. History/profile
    currently reads exotic_bets/multi_bets only. Add queries + UI rows for group_bets
    and acca_bets (own-row RLS), showing stake/return and the streak boost applied
    (from streak-mult-payouts). Identify the history screen under racefeed/app/(tabs)/
    and the relevant hook; keep winner-free. No schema changes expected.

- id: demo-clip-expansion
  title: Expand demo_clips to 50-100 + race-type/duration matching in feed RPC
  material: yes
  model: sonnet
  depends: []
  proof: `cd racefeed && npx tsc --noEmit` exits 0 AND live SQL check on qlzsnuspiypyejaqcdad shows demo_clips count >= 50 and rf_live_field_feed returns a clip whose affinity matches race_type for a sample sprint and a sample staying race
  prompt: |
    Implement TASK 9 of galop/GALOP_FEED_BETTING_BUILD_PROMPT.md. Additive migration:
    expand demo_clips toward 50-100 rows (keep the 11 proven archive.org racing films
    as guaranteed-good; add reliably-hosted public-domain MP4s), each with
    race_type_affinity + duration_min/max. Update rf_live_field_feed so the demo clip
    is chosen by matching race_type_affinity to the race type and duration to
    feed_duration_seconds (sprint->short, staying/jumps->long), keeping the race's own
    clip_url as first preference and never running dry. Apply to qlzsnuspiypyejaqcdad.

- id: feed-personalization-polish
  title: Personalized For-You ranking + settle haptics/sound + coachmark + a11y
  material: yes
  model: opus
  depends: [open-bets-ribbon]
  proof: `cd racefeed && npx tsc --noEmit` exits 0 AND `npm test` passes AND live SQL check shows rf_live_field_feed still returns the expected winner-free shape
  prompt: |
    Implement TASK 10 of galop/GALOP_FEED_BETTING_BUILD_PROMPT.md. Evolve
    rf_live_field_feed ordering to blend legacy rf_feed signals (difficulty band from
    user accuracy in streaks, country variety interleave, pool depth, clip quality) plus
    which race types the user bets on, keeping live-first + per-user jitter (additive
    migration to qlzsnuspiypyejaqcdad). Polish: settle haptics+sound for the new bet
    types (racefeed/lib/haptics, racefeed/lib/sounds — currently legacy-only); first-run
    3-card coachmark over the feed (browse / tap for intel / + to parlay);
    accessibilityLabels on intel meters, verdict tags, rail chips; larger rail-chip hit
    targets. Keep winner-free.

OPERATOR:
  - DB migrations apply via the Supabase MCP to project qlzsnuspiypyejaqcdad. If the orchestrator lacks Supabase write access, an operator must apply the generated migration files under racefeed/supabase/migrations/ for the material DB tasks (streak-mult-payouts, one-tap-rail-bet, parlay-intel-cashout, demo-clip-expansion, feed-personalization-polish) and confirm grants (submit->authenticated, settle->service_role).
  - Device/visual QA: none of this is testable on an iOS simulator in CI — an operator should smoke-test on a device after merge (one-tap betting, parlay flow, rewards drawer, settle animations).
  - Full per-task spec lives in galop/GALOP_FEED_BETTING_BUILD_PROMPT.md (committed this session); the roadmap rationale is in galop/GALOP_FEED_BETTING_10-200X_ROADMAP.md.
