PROJECT: smarter

# Optimization + remediation batch from 2026-07-08 full-fleet review (autonomous coordination,
# user-task learning + replication into Apparently, email ingestion/sorting, negotiation room +
# counterparty portal, coworker behavioral scoring, security, UI tokens). Every task: verify the
# cited finding in-repo FIRST; if already fixed, note it and do the nearest unfixed adjacent item.
# Follow CLAUDE.md: strict TS, Pinia for cross-component state, token-contract styling
# (bg-surface-1 / text-ink-primary etc.), tests in tests/*.test.mjs (node test runner),
# npm run build must pass before commit, never weaken AUTH_REQUIRED guards.

- id: portal-token-hardening
  title: Counterparty portal — remove dev-secret fallback, add revocation, persist responses
  material: yes
  model: opus
  depends: []
  proof: `npm test` exits 0 AND `npm run build` exits 0
  prompt: |
    The negotiation-room counterparty portal signs tokens with a hardcoded dev fallback when
    PORTAL_SECRET is unset, has no revocation, and holds counterparty responses in memory (lost on
    restart). Remediate fail-closed:
    1) In production (or when AUTH_REQUIRED=true), missing/short PORTAL_SECRET must throw at startup
       — never fall back to a baked-in dev secret. Dev mode may keep a fallback but must log loudly.
    2) Add token revocation: persist issued portal tokens (id/hash, roomId, counterparty, issuedAt,
       expiresAt, revokedAt) in Supabase with RLS; verification checks revocation + expiry; add a
       revoke endpoint gated by the room owner.
    3) Persist counterparty portal responses/clause positions to Supabase instead of process memory,
       keyed idempotently so a resubmit doesn't duplicate.
    Extend tests/security-regression.test.mjs: unsigned token rejected, revoked token rejected,
    expired token rejected, prod boot without PORTAL_SECRET fails.

- id: auth-required-and-regression-coverage
  title: Default-deny auth posture + expand security regression coverage
  material: yes
  model: sonnet
  depends: []
  proof: `npm test` exits 0 AND `npm run build` exits 0
  prompt: |
    AUTH_REQUIRED currently defaults to off (demo mode, no login) and the security regression suite
    covers only part of the server/ routes. 1) Make the effective default safe: when NODE_ENV is
    production and AUTH_REQUIRED is unset, treat auth as REQUIRED and log the assumption (explicit
    AUTH_REQUIRED=false remains possible for demos). 2) Inventory every route under server/ and
    classify: public / authed / portal-token; add a generated manifest (tests can import it) and a
    regression test asserting every non-public route rejects unauthenticated requests. 3) Verify the
    Tomorrow War Room integration (tomorrowWarroomApiUrl/ApiKey via useRuntimeConfig) never exposes
    the key client-side and times out/fails soft when the war-room API is down. List every route whose
    classification you had to guess in the PR description for review.

- id: oauth-token-encryption
  title: Encrypt stored Google/Microsoft OAuth tokens at rest
  material: yes
  model: sonnet
  depends: []
  proof: `npm test` exits 0 AND `npm run build` exits 0
  prompt: |
    Email-integration OAuth access/refresh tokens are stored unencrypted. Add an encryption util
    (AES-256-GCM, key from a new TOKEN_ENCRYPTION_KEY env var, >=32 bytes, required in prod just like
    PORTAL_SECRET) and encrypt tokens before persistence; decrypt only server-side at use time.
    Include a lazy migration path: on read, if a stored token is plaintext, use it once and rewrite
    encrypted (no big-bang backfill needed). Never log tokens; sweep existing logging for token
    leakage while you're in there. Tests: round-trip encryption, plaintext-upgrade path, prod boot
    fails without the key when AUTH_REQUIRED=true.

- id: coordination-workspace-guards
  title: Workspace access guards + durable persistence for autonomous coordination
  material: yes
  model: sonnet
  depends: []
  proof: `npm test` exits 0 AND `npm run build` exits 0
  prompt: |
    The autonomous project-coordination substrate (matter/project organization) is missing consistent
    workspace access enforcement and keeps coordination state in memory. 1) Apply the established
    pattern — await requireWorkspaceAccess(event, workspaceId) immediately after resolving workspace
    context — to EVERY coordination endpoint; workspaceId defaulting (e.g. 'ws-skadden') must be
    extracted once at the top, not scattered. 2) Move coordination state (task assignments, org
    structures, autonomous decisions) from in-memory maps to Supabase tables with RLS, upsert-based
    writes (idempotent), so coordination survives restarts. 3) Autonomous actions get an audit row
    (who/what/why/when). Extend the route manifest from auth-required-and-regression-coverage with a
    coordination section; regression test that a foreign workspaceId is rejected on every
    coordination route.

- id: apparently-export-integration
  title: Fix dead process-replication export to Apparently (contract + retry + test)
  material: no
  model: sonnet
  depends: []
  proof: `npm test` exits 0 AND `npm run build` exits 0
  prompt: |
    The user-task learning layer records learned workflows/playbooks but the export path that
    replicates learned processes into Apparently is dead code — the integration never actually
    fires. 1) Define an explicit export contract (TS type + JSON schema) for a learned process:
    steps, triggers, required inputs, PII fields flagged. 2) Implement the exporter as a server
    route/queue with retry + backoff against the Apparently endpoint configured via runtimeConfig
    (APPARENTLY_API_URL/KEY — add to .env.example with placeholders, never commit real values);
    scrub/flag PII fields before export. 3) If the Apparently side is unreachable, exports queue
    durably (Supabase table) instead of being dropped. 4) Contract tests with a mocked Apparently
    server: success, 500-retry, PII scrubbed, queue drain. Document the contract in a short md next
    to the code so the Apparently side can implement its receiver against it.

- id: email-ingestion-robustness
  title: Replace brittle regex event detection with tested normalizer pipeline
  material: no
  model: sonnet
  depends: []
  proof: `npm test` exits 0 AND `npm run build` exits 0
  prompt: |
    Email ingestion/sorting relies on brittle regexes for detecting events/intents in message
    bodies. Refactor to a normalizer pipeline: per-source normalizer functions (gmail, microsoft)
    map raw payloads into one canonical internal message/event type (align with the CommEvent
    schema work if src/comms exists — reuse it, don't duplicate), followed by pure, individually
    unit-tested classifiers. Keep every classifier deterministic (no Date.now inside logic; inject
    clock); where AI classification is used, fail-open to 'unclassified' rather than blocking
    ingestion. Build a fixtures corpus from representative (sanitized) real messages, including the
    known failure cases of the current regexes. Sorting results must be idempotent on re-ingest of
    the same message id.

- id: coworker-scoring-auditability
  title: Make temperament/sociability/behavior scoring deterministic, stored, and explainable
  material: yes
  model: sonnet
  depends: []
  proof: `npm test` exits 0 AND `npm run build` exits 0
  prompt: |
    The email coworker/counterparty scoring (temperament, sociability, problematic-behavior) is
    internal-only and non-deterministic: scores are produced ad hoc with no stored inputs or
    rationale, so they can't be audited or contested. Because these scores judge real people, treat
    this as a compliance surface: 1) Persist every score with: subject ref, score dimensions,
    model+prompt version, the message-id set it was derived from, timestamp, and a short generated
    rationale. 2) Make recomputation deterministic given the same inputs (pin model + temperature,
    or snapshot the derivation). 3) Scores are advisory only — grep for any code path that takes
    automated action from a score and gate it behind explicit human confirmation. 4) Add a
    retention/erasure path (delete scores for a subject). Never export these scores off-system
    (verify the Apparently exporter and war-room integration exclude them). Unit tests: determinism,
    persistence shape, erasure, advisory-only guard.

- id: ui-token-contract-sweep
  title: Replace hardcoded colors with @ht/ui token classes
  material: no
  model: haiku
  depends: []
  proof: `npm run build` exits 0 AND `npm test` exits 0
  prompt: |
    ~120 hardcoded color values (hex/rgb/tailwind palette classes like bg-gray-800) violate the
    @ht/ui token contract. Sweep components/ and pages/: replace with semantic token classes
    (bg-surface-0..4, text-ink-primary/ink-muted, border-surface-4, bg-accent-subtle, semantic
    red-500/green-400 only where semantic). Use vendor/ht-ui/tokens/contract.css as the source of
    truth; where no token fits, add a note and pick the nearest token rather than keeping the raw
    value. Add a simple lint/grep script (scripts/check-tokens.mjs) that fails on new raw hex colors
    in components/ and wire it into npm test or a check script. Purely mechanical — no visual
    redesign; verify npm run build renders (spot-check key pages in dev).

OPERATOR:
  - Set PORTAL_SECRET (32+ bytes) and TOKEN_ENCRYPTION_KEY in production env BEFORE portal-token-hardening / oauth-token-encryption deploy (they hard-fail without).
  - Provision APPARENTLY_API_URL / APPARENTLY_API_KEY pair (and the receiving endpoint on Apparently) for the export integration; until then exports queue durably.
  - Legal review recommended for coworker behavioral scoring retention policy (scores about identifiable people).
  - Confirm AUTH_REQUIRED=true in production env; demo deployments must set it false explicitly.
