# prediction-markets-institute: PMA credential rename + the Publius content engine

SUBMITTED-BY: kale@smrter.us (operator) 2026-07-28. Strategy: PORTFOLIO_STRATEGY_V2 Part 12.5c + 12.6. Extends the in-flight PMI launch wave.

WORKFLOW: fast_coherent

1. CREDENTIAL RENAME: the credential is the **Prediction Markets Advisor (PMA)** — replace the "Registered Outcomes Advisor" working title everywhere (site copy, application flow, exam pages, waiver language for CFA/CFP/FRM).
2. THE PUBLIUS ENGINE (12.6): a standing editorial swarm generating publication-grade thought leadership from Tomorrow/Apparently data with HARD GUARDS — no PII, no client data, and never publishing the chargeable data sources themselves (aggregates/insights only; index/curve feeds stay paid products; add an egress check against a protected-sources list). Two authorship channels:
   (a) **Pseudonymous fellows**: Publius-style pen names rendered as natural first/last names, each with a consistent voice, beat, bio, and archive page — the field's federalist papers. Maintain a private pseudonym registry (operator-visible only).
   (b) **Operator byline**: pieces drafted under Kale Pasch flow to DRAFTS ONLY — Medium draft via API where configured, otherwise an orchestrator/PMI review queue — never auto-published; operator reads/edits/publishes.
3. CADENCE + TRIGGERS: weekly flagship-adjacent pieces; event-reactive same-day commentary when the living-memo/reg-change engine fires; NPRM-aligned advocacy timed to comment windows (coalition-engine hook). Every piece ends with the right door: Apparently/Apparently Law (advisory), Illuminati (agents), Tomorrow (instruments), Pareto (personal).
4. CADE EXPERT REPORTS: a report pipeline producing PMI-branded expert analyses (CADE multi-pass, adversarially reviewed) from Tomorrow/Apparently aggregates — publishable tier free, deep tier as paid PMI products.
Proof: rename sweep complete; a fixture data drop generates a pseudonymous piece + an operator-byline draft into the review queue (never published); protected-sources egress check blocks a fixture leak; report pipeline emits a CADE-reviewed fixture report.
