PROJECT: tomorrow

# Follow-ups to the conviction/hedge-aware work already queued in
# processed/…-tomorrow-0702-2115.md. Those task slugs (install-bundle,
# regenerate-belief-canvas-system, prisma-models-snapshot-capture,
# persist-target-exposure, factor-betas-feeder, full-factor-covariance,
# scenario-conditioned-targets, wire-canvas-ui, bench-kpi-dashboard-tile,
# shadow-book-cost-to-close) ALREADY EXIST — the `depends` below reference them.
# Everything here is ADDITIVE (new files) EXCEPT target-exposure-history, which
# edits prisma/schema.prisma and is therefore chained AFTER the last schema task
# (factor-betas-feeder) so schema edits never run in parallel / merge-conflict.
# Repo: Nuxt 4 / TS / Prisma / Supabase. Prod build gate: `npm run build`.
# RULE FOR EVERY TASK: search the repo first; smallest diff that reuses existing code; keep the build green.

- id: regime-signals-feeder
  title: Produce per-scenario regime probabilities to drive scenario-conditioned targets
  material: no
  model: sonnet
  depends: [install-bundle]
  proof: "npx vitest run server/utils/hive/__tests__/regimeSignals.test.ts exits 0 AND npm run build exits 0"
  prompt: |
    scenario-conditioned-targets consumes a regime-probability input but nothing produces one yet, so it
    would ship dark. Add server/utils/hive/regimeSignals.ts (PURE): blend predictionSignals (import
    fail-soft if the module exists) with simple rates/vol proxies into a probability (0..1) per key in
    STRESS_SCENARIOS from tailFactors.ts (credit_crunch, rate_shock, risk_off). Export
    computeRegimeProbabilities(inputs) -> Record<scenarioKey, number> that sums-normalises and defaults to
    a calm baseline when inputs are absent. NEW FILE ONLY (plus its test) — do not edit scenarioTargets.ts
    or the target-exposure endpoint (scenario-conditioned-targets already reads this fail-soft). Unit-test
    with synthetic inputs: calm baseline vs a fired scenario (regimeSignals.test.ts). Keep the build green.
    CANDIDATE-SHARED: regime probabilities are useful to any factor-modeled app.

- id: conviction-core-shared-copy
  title: Publish the pure conviction/factor engines as an additive shared package (no repoint)
  material: no
  model: sonnet
  depends: [regenerate-belief-canvas-system, full-factor-covariance]
  proof: "npx vitest run packages/conviction-core/__tests__/smoke.test.ts exits 0 AND npm run build exits 0"
  prompt: |
    Dedupe the conviction engine that has now been rebuilt several times across sessions. Create
    packages/conviction-core/ containing VERBATIM copies of the PURE modules from server/utils/hive/
    (riskFactors, hedgeDecomposition, bookFactorExposure, bookOptimizer, factorBetas, factorCovariance,
    hedgeDrift, tailFactors, convictionCard, riskLetter, benchWinRate, convictionResolution, plus the
    belief-canvas pure engines: convictionBelief, convictionCalibration, edgeRadar, convictionRegret,
    convictionBasket, convictionDecay, convictionPremortem, marketProbability, convictionLeaderboard,
    contractCorrelation, catalystPublicity). Add an index.ts barrel re-export + a minimal package.json
    (name @tomorrow/conviction-core, type module) + a smoke.test.ts that imports the barrel and exercises
    sizeFromBelief + buildHedgeAwareConviction + solveToTarget. This is ADDITIVE — do NOT repoint existing
    server/utils/hive imports (that would touch many files and conflict). Depends on full-factor-covariance
    because it copies factorCovariance and the extended bookOptimizer. Keep `npm run build` green.
    CANDIDATE-SHARED: this package is the seam for cross-app reuse (apparently, smarter, pareto-2080).

- id: conviction-core-golden-test
  title: Golden-vector contract test to keep the shared core from drifting
  material: no
  model: haiku
  depends: [conviction-core-shared-copy]
  proof: "npx vitest run packages/conviction-core/__tests__/golden.test.ts exits 0 AND npm run build exits 0"
  prompt: |
    Add packages/conviction-core/__tests__/golden.test.ts + a committed fixture golden.json: fixed inputs
    (a belief, a book, a target vector) -> expected sizeFromBelief / buildHedgeAwareConviction / solveToTarget
    outputs, asserted to a stable rounded precision. This is the guardrail that makes the shared package safe
    to consume from other apps: any behavior change must update the golden deliberately. NEW FILES ONLY.
    Keep the build green. CANDIDATE-SHARED: other apps can run this same golden against their copy to detect
    divergence at their own build gate.

- id: target-exposure-history
  title: Append-only audit trail for changes to a CFO's target exposure
  material: yes
  model: sonnet
  depends: [factor-betas-feeder]
  proof: "npx vitest run server/utils/hive/__tests__/targetExposureHistory.test.ts exits 0 AND npm run build exits 0"
  prompt: |
    The persisted target exposure is what the drift alerts, weekly letter, and (later) the autopilot hang on,
    so changes to it must be attributable. Add Prisma model TargetExposureHistory (id @db.Uuid gen_random_uuid,
    orgId, targetJson Json, tolerancesJson Json?, changedBy String?, reason String?, createdAt @db.Timestamptz(6);
    @@index([orgId, createdAt]); @@map target_exposure_history) + ONE idempotent timestamped migration +
    `npx prisma generate`. Edits ONLY prisma/schema.prisma + the new migration dir + the PUT
    /api/hivemind/conviction/target-exposure handler (append a history row on every write, fail-soft) + a small
    pure helper server/utils/hive/targetExposureHistory.ts with a vitest (proof). Chained after
    factor-betas-feeder so schema.prisma edits serialize and never merge-conflict. MATERIAL: schema change —
    human approves merge.

OPERATOR:
  - After conviction-core-shared-copy lands and is proven stable, a SEPARATE human-reviewed task can repoint server/utils/hive imports to @tomorrow/conviction-core and promote the package to the shared/monorepo location for apparently / smarter / pareto-2080 (not queued here — it touches many files and belongs behind explicit approval).
  - Apply the target_exposure_history migration to Supabase (prisma migrate deploy) after approval.
