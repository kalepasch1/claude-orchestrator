PROJECT: apparently

- id: coverage-public-flag-migration
  title: Replace env allowlist with organizations.public_coverage_enabled opt-in column
  material: yes
  model: sonnet
  depends: []
  proof: `npx vitest run server/utils/__tests__/coverage-attestation.test.ts` exits 0
  prompt: |
    Context: a prior Cowork session bound the signed coverage-attestation system to real
    per-org data. server/utils/coverage-attestation.ts → resolveCoverageSummary(org, event)
    currently gates real data behind the env allowlist runtimeConfig.coverageBadgePublicOrgs
    (COVERAGE_BADGE_PUBLIC_ORGS), falling back to SAMPLE_COVERAGE. There is an explicit
    TODO(bind-org→coverage, db-flag) naming this follow-up.

    Deliverable (one): add a real per-org opt-in column and read it instead of the env list.
    1. Add an idempotent migration under supabase/migrations/ (NNN_org_public_coverage.sql):
       ALTER TABLE organizations ADD COLUMN IF NOT EXISTS public_coverage_enabled boolean
       NOT NULL DEFAULT false; keep RLS unchanged (default-deny preserved; this is a public
       aggregate read via the service client only).
    2. In resolveCoverageSummary, when resolving the org row, also SELECT public_coverage_enabled
       (use the typed service client — NO raw .from()). Gate real-data aggregation on
       (public_coverage_enabled === true). Keep the env allowlist as a secondary override so
       nothing regresses, but the column is now authoritative. Keep it fail-soft: any miss/error
       → summarizeCoverage(SAMPLE_COVERAGE), never throw. Use logger, not console.
    3. Regenerate types if the repo workflow requires (npm run gen:types) OR add the column to
       types/database.types.ts manually so `select('public_coverage_enabled')` is typed.
    4. Extend server/utils/__tests__/coverage-attestation.test.ts (create if absent) to prove:
       flag=false → sample; flag=true → real aggregate; resolver error → sample (no throw);
       signing still round-trips. Mock the typed client. Keep TypeScript strict-clean.
    INVARIANT: only aggregate counts {approved,inProgress,total} ever leave the function — never
    per-filing rows. MATERIAL: schema migration — do not merge autonomously; produce green branch.

- id: coverage-trust-page-live-data
  title: Wire public trust + embed pages to the real public coverage endpoint
  material: no
  model: sonnet
  depends: [coverage-public-flag-migration]
  proof: `npx vitest run app/components/ui/__tests__/CoverageVerification.test.ts` exits 0
  prompt: |
    Today app/pages/coverage/[org].vue and app/pages/embed/coverage/[org].vue still render a
    LOCAL SAMPLE_COVERAGE constant for the headline numbers, while the signed attestation
    endpoints (server/api/public/coverage/[org]/attest.get.ts + attestation.jsonld.get.ts)
    already return real opt-in data. Make the pages fetch from the real public endpoint so the
    human-visible numbers match the signed attestation.
    Steps: add/confirm a public read endpoint returning {summary, asOf, verified}; replace the
    in-component SAMPLE_COVERAGE usage in both pages with a useFetch of that endpoint; fail-soft
    to an empty/preview state on error (never blank, never crash). Keep the existing
    CoverageVerification.vue verified/preview/unverified UI and a11y (aria-live) intact.
    Add a component test (app/components/ui/__tests__/CoverageVerification.test.ts) asserting it
    renders verified vs preview states from fetched props. Token-driven; logo untouched.

- id: coverage-jsonld-context-and-caching
  title: Publish JSON-LD @context doc, add ETag/conditional-GET + describedby header
  material: no
  model: haiku
  depends: []
  proof: `npx vitest run server/api/public/coverage/__tests__/jsonld.test.ts` exits 0
  prompt: |
    Polish the machine-readable attestation layer added previously
    (server/api/public/coverage/[org]/attestation.jsonld.get.ts +
    server/routes/.well-known/coverage-attestation.get.ts).
    1. Serve a stable JSON-LD @context document at a server route (e.g.
       server/routes/.well-known/coverage-context.jsonld.get.ts) defining the apparently:
       vocabulary terms (summary, status, verified, token, reference, asOf, verifyEndpoint,
       verifyUrl) so the @context in the attestation doc resolves to a real vocabulary instead
       of a placeholder URL. Point the attestation doc's @context at it.
    2. Add an ETag derived from {org,asOf,token} on the JSON-LD response and honor
       If-None-Match → 304, so pollers do conditional GETs.
    3. Add a `Link: <…/attestation.jsonld>; rel="describedby"; type="application/ld+json"`
       header on app/pages/coverage/[org].vue's server response (via a route rule or server
       middleware) for auto-discovery from the human page.
    Add server/api/public/coverage/__tests__/jsonld.test.ts asserting: context doc returns the
    expected terms; ETag present; matching If-None-Match → 304. No secrets exposed.

- id: htpopover-focus-trap-unit-test
  title: Add a vitest jsdom unit test for the HtPopover Tab focus-trap (kit)
  material: no
  model: sonnet
  depends: []
  proof: `cd packages/ht-ui && npx vitest run` exits 0
  prompt: |
    The shared kit at packages/ht-ui has NO unit-test runner today (only a Playwright VR suite
    that needs macOS browsers). HtPopover.vue recently gained a Tab focus-trap (wraps at both
    ends, focus cannot escape behind the panel) but it is only covered by visual diff.
    Deliverable: add a minimal vitest + jsdom setup to packages/ht-ui (vitest, @vue/test-utils,
    jsdom as devDeps; a vitest.config + one "vr"-independent unit test dir) and write a test that
    mounts HtPopover open with 2+ focusable children and asserts: Tab past the last focusable
    wraps to the first; Shift+Tab past the first wraps to the last; Esc closes and returns focus
    to the trigger. Do NOT add any new required contract var. Keep the existing VR scripts
    untouched. proof is the new vitest run passing.

OPERATOR:
  - Set COVERAGE_BADGE_SECRET (signing) and COVERAGE_BADGE_PUBLIC_ORGS (and/or flip
    organizations.public_coverage_enabled per org) in prod env to enable real signed attestations.
  - Approve the prod merge of coverage-public-flag-migration (material — schema migration).
  - Capture the HtPopover visual-regression baseline on macOS: `cd packages/ht-ui && npm run vr:baseline`
    then commit visual/__screenshots__/primitives-htpopover.png.
  - Host the apparently: JSON-LD @context at a stable public URL (DNS/hosting decision) once
    coverage-jsonld-context-and-caching lands.
  - Decide @ht/ui productionization path per packages/ht-ui/EXTRACTION.md (pnpm workspace vs
    private registry); the registry path needs npm publish credentials.
  - One-time scratch cleanup (mount blocked rm in the Cowork sandbox): verify origin of
    `.jr-chunk.mjs`/`.jr-runner-active.mjs` then `git clean -f _sfccheck.mjs scripts/_tmp_count.mjs scripts/_tmp_inspect.mjs`.
