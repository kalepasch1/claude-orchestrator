PROJECT: pareto-2080

- id: retirement-days-util-test
  title: Lock the USD→retirement-days conversion with a node:test
  material: no
  model: haiku
  depends: []
  proof: `node --test tests/retirementDays.test.js` exits 0
  prompt: |
    A prior session added utils/retirementDays.js (pure conversion: usdToRetirementDays, annualize,
    isRecurring, retirementDaysLabel, describeRetirementDays, DEFAULT_DAYS_PER_DOLLAR=0.15) as the
    single source of truth behind components/RetirementDaysChip.vue and the v-retirement-days
    directive (plugins/retirement-days.client.js). It has no automated test yet.
    Deliverable: add tests/retirementDays.test.js using node:test (match the repo's existing
    node:test pattern — see other tests/*.test.js) asserting: 45000 @ 0.15 → 6750 days; recurring
    monthly/weekly/quarterly annualization; negative usd → gain tone; null/invalid rate handled;
    parity with server/utils/valueLedger.js summarize for the same inputs. ESM, no new deps.

- id: retirement-days-surface-more
  title: Surface v-retirement-days on Plan, Finance hub, and Deductions totals
  material: no
  model: haiku
  depends: [retirement-days-util-test]
  proof: `npx vue-tsc --noEmit` exits 0 AND `grep -RL "v-retirement-days" pages/Plan pages/Finance pages/Deductions` returns nothing for the edited total elements
  prompt: |
    The v-retirement-days directive (registered globally via plugins/retirement-days.client.js,
    reads the live Monte-Carlo rate published with setRetirementDayRate from useRetirementDays.js)
    is currently only on the Spend and Optimize pages. Surface it on the main dollar figures of:
    pages/Plan/[userId].vue, pages/Finance/[userId].vue, and pages/Deductions/User/[userId].vue —
    each page must first publish the live rate (setRetirementDayRate(mcDaysPerDollar) once balance/MC
    data loads, exactly as Spend does), then annotate 1-2 high-traffic totals with
    v-retirement-days="{ usd, period }". Add aria-describedby from the annotated dollar element to
    the injected chip for tighter screen-reader association. Reduced-motion safe (the directive
    already respects it). Do not change visual design beyond the additive chip.

OPERATOR:
  - (none — both tasks are self-contained and test-gated.)
