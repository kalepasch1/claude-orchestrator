PROJECT: beethoven

# Orchestrator self-improvement. These EXTEND existing runner modules (smallest
# diff, reuse-first) rather than rebuilding: intake_watcher.py (intake parse),
# budget.py + the EV scheduler (test_ev_scheduler.py), merge_train.py +
# approval_policy.py + test_safety.py (merge gate), planner.py + enqueue_task.py
# (task creation + dependency resolution). Merge gate = the runner test suite;
# every proof runs `python -m pytest runner/tests/ -q` (full suite green) and each
# task adds its own runner/tests/test_<slug>.py. All material: yes — they touch
# scheduling/merge/cost core, so a human approves each merge.

- id: orch-intake-compiler
  title: Compile Supabase coordination_tasks into orchestrator intake files
  material: yes
  model: sonnet
  depends: []
  proof: `python -m pytest runner/tests/ -q` exits 0 (incl. new runner/tests/test_intake_compiler.py)
  prompt: |
    Add runner/intake_compiler.py that reads QUEUED rows from the app databases'
    coordination_tasks queue (the code_improvement_request rows; see the tomorrow
    curation-layer-architect entries) and EMITS intake/*.md files in the exact
    format intake_watcher.py already consumes (PROJECT header + id/title/material/
    model/depends/proof/prompt blocks). Reuse the existing parser/field names from
    intake_watcher.py so output is round-trippable; dedupe by slug (skip ids already
    present in intake/ or intake/processed/). Keep it a pure transform with the
    Supabase read behind an injected client so the unit test uses a fixture (NO live
    DB in tests). Add runner/tests/test_intake_compiler.py asserting a sample
    coordination_task row produces a valid, watcher-parseable intake block. Live
    Supabase credentials are an OPERATOR item. This makes the two backlogs
    self-syncing instead of hand-transcribed.

- id: orch-tests-first-gate
  title: Planner splits a new-test proof into a depends-first test-authoring task
  material: yes
  model: opus
  depends: []
  proof: `python -m pytest runner/tests/ -q` exits 0 (incl. new runner/tests/test_tests_first_gate.py)
  prompt: |
    In planner.py / enqueue_task.py, when a task's `proof` references a test file
    that does not yet exist, split it into two tasks: a depends-first "author the
    failing test" task and the implementation task that depends on it — so the test
    is written before (and by a different unit of work than) the implementation that
    must pass it. Smallest diff; reuse the existing task/dependency structures. Add
    runner/tests/test_tests_first_gate.py covering: a proof naming a missing test
    splits; a proof naming an existing test or a build command does not. Do not
    change behavior for tasks whose proof is a build/existing-test command.

- id: orch-cross-project-depends
  title: Allow cross-project depends (project:id) in the dependency resolver
  material: yes
  model: opus
  depends: [orch-tests-first-gate]
  proof: `python -m pytest runner/tests/ -q` exits 0 (incl. new runner/tests/test_cross_project_depends.py)
  prompt: |
    Extend the dependency resolver (planner.py / enqueue_task.py — SAME files as
    orch-tests-first-gate, hence the depends to avoid a merge conflict) so a
    `depends` entry may be `project:id` (e.g. apparently:curation-layer-land) and
    resolves against the global task namespace, while bare ids stay project-local
    and backward compatible. This unblocks extracting CANDIDATE-SHARED modules into
    one shared package as a single coordinated wave. Add
    runner/tests/test_cross_project_depends.py covering local id, cross-project id,
    and an unknown reference (stays blocked, never silently runs).

- id: orch-ev-outcome-weighting
  title: Weight the EV scheduler by realized merge outcomes (flag-gated)
  material: yes
  model: opus
  depends: []
  proof: `python -m pytest runner/tests/ -q` exits 0 (incl. new runner/tests/test_ev_outcome_weighting.py)
  prompt: |
    Extend the existing EV scheduler + budget.py (see test_ev_scheduler.py,
    cost_ledger.py, learn_from_merges.py) to weight task-family priority by REALIZED
    outcomes (merged-and-stayed-green rate, retries, human-reject rate) rather than
    flat cost only. Reuse learn_from_merges.py signals. Gate the new weighting behind
    a default-OFF flag so current scheduling is byte-identical until enabled. Add
    runner/tests/test_ev_outcome_weighting.py: with the flag off, ordering is
    unchanged; with it on, a family with a higher realized success rate outranks an
    equal-cost family with a lower one. Do not change the default budget caps.

- id: orch-merge-invariant-firewall
  title: Global pre-merge invariant firewall (flag-gated, composes with build gate)
  material: yes
  model: opus
  depends: []
  proof: `python -m pytest runner/tests/ -q` exits 0 (incl. new runner/tests/test_merge_invariant_firewall.py)
  prompt: |
    Add a global pre-merge invariant check in merge_train.py / approval_policy.py
    (reuse the existing safety hooks in test_safety.py) that runs BEFORE merge and
    blocks (routes to human) any diff that: weakens an RLS policy, flips a
    money-movement/settlement default (e.g. SETTLEMENT_SWEEP_ENABLED), or removes a
    money-movement token gate. Implement as a list of pure predicate checks over the
    diff so each is unit-testable; compose WITH (do not replace) the existing build
    gate. Gate behind a default-OFF flag initially; when off, behavior is unchanged.
    Add runner/tests/test_merge_invariant_firewall.py with a passing diff and one
    diff per invalidating pattern (each blocked). This makes "risky -> human" a
    proven gate rather than a heuristic and lets the autonomy ceiling rise safely.

OPERATOR:
  - Provide the read-only Supabase connection/credentials for orch-intake-compiler to read each app's coordination_tasks in production (never committed; injected via env).
  - Decide when to flip the default-OFF flags to ON after review: EV outcome-weighting (orch-ev-outcome-weighting) and the merge invariant firewall (orch-merge-invariant-firewall).
