PROJECT: hisanta

# HiSanta — Phase 4: new surfaces, trust, and growth loops.
# Builds on Phase 1 (LIVE in Supabase `santas-secret-workshop`, ref whhfugddqehxxbmwutsw), Phase 2
# (intake/hisanta-0629.md), Phase 3 learning engine (intake/hisanta-learning-engine-0629.md).
# Cross-file deps by id: capture-live-migrations, le-item-responses, recommend_arm (live fn).
# Reuse live tables: ai_interactions (+flagged), little_sparks/family_lessons (media), community_acts +
#   community_act_completions, coop_quests/coop_participants, child_world + world_items, annual_spark_stories,
#   family_circle, consent_edges (+ app_has_consent), gift_deliveries + advent_pass, notification_queue, spark_ledger.
# RLS: guardian via children.guardian_id/co_guardian_id=auth.uid(); family read via family_circle. Helper fns
#   app_is_guardian_of/app_can_access_child stay anon+authenticated EXECUTE-able. Privileged DEFINER fns guard
#   `if auth.uid() is not null and <unauthorized> then raise`; anon EXECUTE revoked. Sparks are NEVER money-redeemable.
# Every DB task ships a pgTAP test (gate `supabase test db`); every job/algorithm ships a colocated test
#   (gate `deno test -A <path>`). Agents: `supabase db reset` first; inspect repo before editing. Honor wellbeing
#   caps + consent on anything child-facing.

- id: voice-ambient-mode
  title: Voice-first tutor loop (STT -> tutor -> TTS) for pre-literate learners
  material: yes
  model: sonnet
  depends: [capture-live-migrations]
  proof: `deno test -A supabase/functions/voice-tutor/index.test.ts` exits 0
  prompt: |
    Many target ages can't read fluently. Build supabase/functions/voice-tutor/index.ts: accept an audio clip,
    transcribe (STT), route the text to the existing tutor/persona path, run the SAME moderation as ai_interactions
    (set flagged + reasons on violation), synthesize a spoken reply (TTS), and log an ai_interactions row
    (kind='voice'). Return transcript + audio URL. Enforce wellbeing session caps. index.test.ts mocks STT/TTS/LLM/
    moderation: audio in yields transcript+reply+exactly one ai_interactions row; a moderation hit sets flagged=true
    and returns a safe fallback, not the raw model text. (STT/TTS/LLM keys + deploy = OPERATOR.)

- id: vision-show-me
  title: "Show me" multimodal assessment — camera turns the real world into items
  material: yes
  model: sonnet
  depends: [capture-live-migrations, le-item-responses]
  proof: `deno test -A supabase/functions/vision-assess/index.test.ts` exits 0
  prompt: |
    Build supabase/functions/vision-assess/index.ts: accept an image (homework, a build, a made bed) + a skill or
    quest context; a multimodal model returns a structured assessment {correct/partial/incorrect, skill, feedback,
    detected_misconception?}. For a learning item, write an item_responses row; for a chore/community act, write a
    community_act_completions confirmation (status='confirmed') so OFF-SCREEN activity earns sparks. Reject unsafe or
    off-topic images. index.test.ts mocks the vision model: a correct-homework image writes one item_responses row;
    a made-bed image confirms the act; an irrelevant/unsafe image writes nothing and returns a safe error.
    (Vision model key + deploy = OPERATOR.)

- id: sensor-verified-quests
  title: Sensor/photo/timer verification for off-screen quests (no bare self-report)
  material: yes
  model: sonnet
  depends: [capture-live-migrations]
  proof: `supabase test db` exits 0 (supabase/tests/quest_verification_test.sql)
  prompt: |
    Reduce screen dependence and tighten the audit trail. Migration supabase/migrations/0030_quest_verifications.sql:
    quest_verifications (id, quest_id fk coop_quests, child_id fk children, method text check in
    ('photo','sensor','timer','adult_confirm'), artifact_ref text, verified boolean default false, verified_by uuid,
    created_at). Add award_quest_completion(p_quest,p_child) DEFINER fn that refuses to credit sparks unless a
    verified quest_verifications row exists. RLS family read; insert auth.uid() not null. pgTAP
    supabase/tests/quest_verification_test.sql: crediting throws without a verified artifact; with one it inserts a
    spark_ledger row of the right type; RLS blocks non-family read.

- id: family-escrow-chores
  title: Parent-staked reward escrow that auto-releases on confirmed co-done chores
  material: yes
  model: sonnet
  depends: [capture-live-migrations]
  proof: `supabase test db` exits 0 (supabase/tests/chore_escrow_test.sql)
  prompt: |
    Automate the chore-reward nag. Migration supabase/migrations/0031_chore_escrow.sql: chore_escrow (id, parent_id,
    child_id, coop_quest_id null, stake_sparks int, status text check in ('held','released','cancelled'), created_at,
    released_at). stake_escrow() (guardian only) holds the reward; release_escrow() DEFINER fn releases ONLY when the
    linked quest/chore has parent confirmation (coop_participants.parent_confirmed_together or adult confirm), writing
    a spark_ledger row (source_type='chore'); cancel_escrow() returns to held->cancelled. Sparks only, never money.
    pgTAP supabase/tests/chore_escrow_test.sql: release throws while unconfirmed; after confirmation it releases exactly
    the staked amount once (idempotent); only a guardian can stake.

- id: annual-growth-story
  title: Auto-generated narrated year-in-review film record (annual_spark_stories)
  material: yes
  model: sonnet
  depends: [capture-live-migrations]
  proof: `deno test -A supabase/functions/growth-story/index.test.ts` exits 0
  prompt: |
    Turn a year of data into the best re-subscribe moment. Build supabase/functions/growth-story/index.ts: assemble a
    child's year (skills mastered, kindness sparks, peer-teaching wins, family clips) into a narrated story written to
    annual_spark_stories (sections jsonb + narration script + selected media refs). ONLY include media for which
    app_has_consent(child,'media_capture') is true. index.test.ts mocks LLM/render: produces a story record with the
    expected sections for a fixture child; a clip lacking media_capture consent is excluded; an empty year yields a
    graceful minimal story, not an error. (LLM + video/render service + deploy = OPERATOR.)

- id: narrative-saga-schema
  title: Persistent story-world state — lessons become quests in an evolving saga
  material: yes
  model: sonnet
  depends: [capture-live-migrations]
  proof: `supabase test db` exits 0 (supabase/tests/narrative_saga_test.sql)
  prompt: |
    Story continuity is the retention edge generic drill apps lack. Migration supabase/migrations/0032_narrative_saga.sql:
    saga_state (child_id pk fk children, chapter int default 1, arc text, unlocked_world_items uuid[], updated_at) tied
    to existing child_world/world_items. advance_saga(p_child, p_skill_completed) DEFINER fn progresses chapter/arc on a
    durable mastery event and may unlock a world_item; idempotent per completion. RLS family. pgTAP
    supabase/tests/narrative_saga_test.sql: a durable mastery advances the saga and unlocks an item exactly once;
    a non-durable event does not advance it.

- id: narrative-tutor-memory
  title: Tutor persona that remembers the child's interests (privacy-bounded, healthy)
  material: yes
  model: opus
  depends: [narrative-saga-schema]
  proof: `deno test -A supabase/functions/_shared/tutor_memory.test.ts` exits 0
  prompt: |
    Give the tutor continuity without making it compulsive. Add supabase/functions/_shared/tutor_memory.ts:
    buildTutorContext(child) assembles a bounded memory (interests, recent saga arc, recent struggles) from
    child_preference_profiles + saga_state + recent ai_interactions, redacting anything outside the family scope and
    excluding sensitive fields. The system prompt it produces must include the wellbeing/satiation guidance (encourage
    wrapping up at target) and must NOT include engagement-maximizing dark-pattern instructions. tutor_memory.test.ts:
    context includes the child's top interests + current arc; it excludes non-consented media and legal-name fields;
    it contains the wind-down guidance string. (No raw PII leaves the family scope.)

- id: parent-safety-copilot
  title: Parent safety dashboard — flagged AI interactions + anomaly alerts
  material: yes
  model: sonnet
  depends: [capture-live-migrations]
  proof: `supabase test db` exits 0 (supabase/tests/safety_copilot_test.sql)
  prompt: |
    Surface trust as a product. Migration supabase/migrations/0033_safety_copilot.sql: a guardian-only view
    safety_feed (per child: recent ai_interactions with flagged + reasons, counts, last_flag_at) and a function
    detect_interaction_anomaly(p_child) that enqueues a notification_queue alert when flagged-rate spikes beyond a
    threshold over a window. RLS: only the child's guardian/family may read safety_feed (use app_can_access_child).
    pgTAP supabase/tests/safety_copilot_test.sql: a non-family user cannot read safety_feed; seeding a flag spike
    produces exactly one queued alert; normal activity produces none.

- id: explainable-recommendations
  title: "Why this?" — explain the engine's lesson/reward choice to parents
  material: no
  model: sonnet
  depends: [capture-live-migrations]
  proof: `deno test -A supabase/functions/_shared/explain_reco.test.ts` exits 0
  prompt: |
    Transparency is a trust + district-sales wedge. Add supabase/functions/_shared/explain_reco.ts:
    explainRecommendation(child, lever) returns a plain-language rationale referencing the actual signals — the chosen
    arm from recommend_arm, the child's mastery frontier, recent struggles, and (when present) the learner-cluster
    strategy — with NO identifiers of other children and no raw model internals. explain_reco.test.ts: for a fixture
    child the explanation names the chosen arm and cites the real mastery/skill rationale; it never leaks another
    child's id or a raw embedding.

- id: early-screening-indicators
  title: Early learning-difference INDICATORS (not diagnosis) from response patterns
  material: yes
  model: opus
  depends: [le-item-responses]
  proof: `deno test -A supabase/functions/screening-indicators/index.test.ts` exits 0
  prompt: |
    Highest ceiling, highest sensitivity. Build supabase/functions/screening-indicators/index.ts: analyze item_responses
    + latency + misconception patterns for signatures associated with dyslexia/dyscalculia-type differences and produce a
    PARENT-ONLY, consent-gated indicator: {area, strength_of_signal, suggested_next_step:'consider talking to a specialist'}.
    HARD RULES: never output a diagnosis or condition label; never assert; gate behind app_has_consent(child,'screening')
    and guardian-only visibility; include a clear non-clinical disclaimer in the payload. index.test.ts on fixtures with a
    known dyscalculia-like signature: raises an indicator with a non-diagnostic message and a specialist suggestion;
    a typical-profile fixture raises nothing; output is suppressed without consent. (Clinical + legal review, consent copy,
    and any external referral list = OPERATOR; do not surface to parents until signed off.)

- id: optimal-timing-engine
  title: Per-child best-time-of-day + spacing model for retention and nudges
  material: yes
  model: sonnet
  depends: [le-item-responses]
  proof: `deno test -A supabase/functions/timing-engine/index.test.ts` exits 0
  prompt: |
    Same content, more retention. Build supabase/functions/timing-engine/index.ts: from item_responses timestamps +
    correctness, estimate each child's high-performance time window(s) and optimal review spacing, write a
    learning_timing (child_id pk, best_windows jsonb, spacing_profile jsonb, updated_at) row (migration
    supabase/migrations/0034_learning_timing.sql, RLS family), and schedule review nudges into notification_queue at
    those windows respecting wellbeing caps. index.test.ts on a fixture with a clear morning-performance pattern: the
    recommended window is morning and a review nudge is queued within it; flat-pattern fixture yields a sensible default.
    (Deploy/cron = OPERATOR.)

- id: grandparent-onboarding
  title: 2-minute "record a lesson" grandparent funnel (payer + content in one)
  material: yes
  model: sonnet
  depends: [capture-live-migrations]
  proof: `supabase test db` exits 0 (supabase/tests/grandparent_onboarding_test.sql)
  prompt: |
    Grandparents are high willingness-to-pay and rich content. Migration supabase/migrations/0035_grandparent_onboarding.sql:
    a guardian-issued invite flow that, on acceptance, creates a SCOPED consent_edge (edge_type='grandparent_child',
    feature='family_curriculum'+'media_capture') and a family_circle row (relationship='grandparent', status='active'),
    then allows a family_lessons recording. Add accept_grandparent_invite() DEFINER fn enforcing guardian issuance + the
    consent edges before any media row is allowed. pgTAP supabase/tests/grandparent_onboarding_test.sql: accepting an
    invite creates the consent edges + family_circle row; a family_lessons insert with media is refused until
    media_capture consent exists; only a guardian can issue the invite.

- id: gift-a-box-referral
  title: Child-gifts-a-friend advent box (double parent-approved) viral onboarding loop
  material: yes
  model: sonnet
  depends: [capture-live-migrations]
  proof: `supabase test db` exits 0 (supabase/tests/gift_referral_test.sql)
  prompt: |
    A generous (not spammy) growth loop. Migration supabase/migrations/0036_gift_referral.sql: gift_referrals (id,
    from_child_id, sender_guardian_approved boolean, recipient_contact text, recipient_guardian_accepted boolean,
    gift_delivery_id fk gift_deliveries null, onboarding_invite_code text, status, created_at). send_gift_referral()
    requires sender-guardian approval; accept_gift_referral() requires recipient-guardian acceptance and only THEN
    creates the gift_deliveries row + advent_pass for the new family. A referral never delivers without both approvals.
    pgTAP supabase/tests/gift_referral_test.sql: delivery is blocked until both approvals are true; once both hold, a
    gift_deliveries row is created exactly once; missing either approval delivers nothing.

OPERATOR:
  - Approve + apply each material migration to prod Supabase `santas-secret-workshop` (ref whhfugddqehxxbmwutsw) after merge.
  - Deploy edge functions (voice-tutor, vision-assess, growth-story, screening-indicators, timing-engine) and enable pg_cron/pg_net where scheduled.
  - Provision secrets: STT/TTS (voice), multimodal vision model, LLM, video/render service (growth story).
  - Clinical + legal sign-off on early-screening-indicators BEFORE it is surfaced to any parent; write the non-clinical disclaimer + consent copy; assemble any specialist-referral guidance. Add 'screening' as a consent feature with parent-facing copy.
  - Content-moderation review of voice + narrative tutor outputs; confirm the tutor system prompt contains no engagement-maximizing instructions (wellbeing/satiation only).
  - Confirm COPPA posture for new media/audio/image capture flows and retention windows.
