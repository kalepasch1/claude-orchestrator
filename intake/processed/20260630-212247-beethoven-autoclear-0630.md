PROJECT: beethoven

# Rule-based auto-clearing of operator sign-offs. Builds on the per-item operator cards
# (runner/intake_watcher.py emit_operator_cards) and the dashboard operator section
# (web/pages/index.vue + web/components/ApprovalCard.vue). Goal: humans only ever see the
# exceptions; low-risk operator items clear automatically under explicit, owner-defined rules.
# SAFETY: never auto-clear a 2-key card; never auto-clear prod deploys; everything is audited
# and reversible; a global kill-switch disables all auto-clearing.

- id: oc-autoclear-policy
  title: Operator auto-clear policy store + evaluator (bypasses the human gate — gated)
  material: yes
  model: opus
  depends: []
  proof: `cd runner && python3 -m pytest tests/test_autoclear.py -q` exits 0
  prompt: |
    Add rule-based auto-clearing for operator approval cards in THIS repo (the orchestrator,
    repo_path /Users/kpasch/Documents/beethoven/claude-orchestrator). Scope:
    - runner/autoclear.py (NEW): load rules (from an operator_autoclear_rules table via db.select, with a
      file fallback runner/autoclear_rules.yaml). A rule matches on: project, kind (operator|deploy|secret|legal),
      optional max_usd (parsed from the card detail), and an environment flag. Export
      autoclear_decision(card_row, rules) -> (decision, rule_id) where decision is 'approved' or None.
      HARD GUARDS (unit-tested): return None (never auto-approve) if card.approvals_required >= 2, if kind == 'legal',
      if the detail mentions prod/production deploy, or if a global kill-switch (env AUTOCLEAR_ENABLED != 'true').
    - runner/intake_watcher.py: in emit_operator_cards, after composing each card, call autoclear_decision; if it
      returns 'approved', insert the card with status='approved', decided_by=f'auto-rule:{rule_id}',
      decided_at=now() instead of pending. Otherwise insert pending as today. Keep idempotency by title.
    - supabase: add migration for operator_autoclear_rules (id, project, kind, max_usd, enabled) — generate, do
      not apply (operator deploys).
    Add tests/test_autoclear.py: a matching non-prod deploy under threshold auto-approves; a 2-key card never
    auto-approves; a legal card never auto-approves; a prod-deploy card never auto-approves; kill-switch off
    forces all to pending.

- id: oc-autoclear-audit
  title: Audit trail for every auto-cleared operator item
  material: no
  model: sonnet
  depends: [oc-autoclear-policy]
  proof: `cd runner && python3 -m pytest tests/test_autoclear_audit.py -q` exits 0
  prompt: |
    Ensure every auto-clear is traceable and reversible. In runner/autoclear.py add record_autoclear(card, rule_id)
    that writes an audit row (table autoclear_log: card_title, project, kind, rule_id, decided_at) via db.insert,
    and expose list_recent_autoclears(limit). Auto-cleared approvals must keep decided_by='auto-rule:<id>' so they
    are distinguishable from human decisions and can be re-opened. Add tests/test_autoclear_audit.py: an auto-clear
    writes exactly one audit row with the rule_id; a human decision writes none.

- id: oc-autoclear-ui
  title: Dashboard panel — manage auto-clear rules + show what was auto-cleared
  material: no
  model: sonnet
  depends: [oc-autoclear-policy]
  proof: `cd web && node -e "const s=require('@vue/compiler-sfc');const fs=require('fs');const d=s.parse(fs.readFileSync('components/AutoClearRules.vue','utf8'));if(d.errors.length)process.exit(1);const c=s.compileTemplate({source:d.descriptor.template.content,filename:'x',id:'x'});process.exit(c.errors.length?1:0)"` exits 0
  prompt: |
    web/components/AutoClearRules.vue (NEW), mounted in web/pages/index.vue near the Operator sign-offs section:
    list current auto-clear rules (project, kind, max_usd, enabled) with add/disable controls writing to
    operator_autoclear_rules, plus a read-only "recently auto-cleared" list from autoclear_log. Show the global
    AUTOCLEAR_ENABLED kill-switch state prominently. Read/compile cleanly (proof compiles the SFC).

OPERATOR:
  - Define the actual auto-clear rule set (which projects / kinds / max_usd / non-prod only) — business owner; nothing auto-clears until rules exist AND AUTOCLEAR_ENABLED=true.
  - Confirm the safety guards with the owner: 2-key cards, legal cards, and any prod deploy are NEVER auto-cleared.
  - Apply the operator_autoclear_rules + autoclear_log migrations to Supabase.
