PROJECT: smarter

# CADE wiring — smarter: the negotiation-room half of the merged adaptive vehicle.
# Engine: @darwin/kernel/cade (built+tested). Spec: claude-orchestrator/CADE_IMPLEMENTATION_HANDOFF.md.
# smarter already adopts @darwin/kernel (see DARWIN_KERNEL_ADOPTION.md). Reuse the kernel signing/proof.
# If the repo test runner differs, adapt the proof command to the repo's vitest/vue-tsc setup.

- id: cade-embedder-sm
  title: CADE Embedder adapter (kernel hashEmbedder + repo embeddings if present)
  material: no
  model: sonnet
  depends: []
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Build server/utils/cade/embedder.ts implementing kernel `Embedder`. Use the repo's embedding
    path if one exists, else kernel hashEmbedder(64). Cache by sha256(text). Add a unit test if the
    repo has a runner; otherwise vue-tsc must pass. See HANDOFF §2.1.

- id: cade-invoker-sm
  title: CADE Invoker adapter (negotiation grounding + tiering)
  material: no
  model: opus
  depends: [cade-embedder-sm]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Build server/utils/cade/invoker.ts implementing kernel Invoker.invoke. Persona system prompt +
    "cite or do not assert" + atemporal grounding; ground in smarter's clause/precedent corpus;
    tier→model cheap=Haiku/deep=Sonnet/adversary=Opus; return PersonaPosition with validated
    citations + subQuestions. Mock the model in a unit test where possible. See HANDOFF §2.2.

- id: cade-roster-negotiation
  title: Negotiation Standing Roster (master negotiators + deal counsel + mosaic)
  material: no
  model: opus
  depends: []
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Build server/utils/cade/roster/negotiation.json + roster.ts loader: role='authority' great
    negotiators + deal lawyers; role='discipline' mosaic (game theory, behavioral econ); role=
    'adversary' (counterparty lead + GC); role='reviewer' (counterparty, arbitrator). Diversity in
    priorsTag/values. Loader returns a typed Persona[]. See HANDOFF §3.1.

- id: cade-negotiation-determine-sm
  title: runDetermination per clause/move in the negotiation room
  material: yes
  model: opus
  depends: [cade-invoker-sm, cade-roster-negotiation]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Decompose the room's clauses/counters into IssueSpec(kind:'negotiation'); call
    runDetermination(issue, roster, {invoker,embedder}, {sign:true,maxRounds:4}). Surface the move +
    dissent ("what the counterparty will argue") + BATNA-robust certificate. Add the room endpoint.
    See HANDOFF §4.2.

- id: cade-merge-vehicle-adapter-sm
  title: Consume Tomorrow's shared merged-vehicle room contract
  material: no
  model: sonnet
  depends: [cade-negotiation-determine-sm]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Implement the smarter side of the merged adaptive negotiation vehicle: consume the shared room
    contract defined by tomorrow's cade-negotiation-vehicle (via @darwin/kernel/cade), so a War Room
    and a smarter room are one object. Type-safe adapter; vue-tsc clean. See HANDOFF §4.2.

- id: cade-advocacy-restyle-sm
  title: Advocacy restyle adapter (firewalled, semantic-diff guarded)
  material: no
  model: sonnet
  depends: [cade-negotiation-determine-sm]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Implement PackageDeps.restyle for negotiation delivery (tone for the modeled counterparty,
    honest hedging); call kernel packageForReviewer; reject on substancePreserved===false. See
    HANDOFF §5.5.

- id: cade-redteam-saas-sm
  title: Red-Team-as-a-Service endpoint for externally-drafted deals
  material: no
  model: sonnet
  depends: [cade-invoker-sm]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Add the red-team-only endpoint: take an externally-drafted clause set, run the adversarial stage,
    return findings + a signed proof. See HANDOFF §6.5.

OPERATOR:
  - Confirm smarter's test runner so proofs can move from vue-tsc to real unit tests where possible.
  - Cross-repo: the merged-vehicle room contract is OWNED by tomorrow (cade-negotiation-vehicle); sequence cade-merge-vehicle-adapter-sm after it lands.
  - Set DARWIN_SIGNING_PRIVATE_KEY_PEM in smarter prod env (shared proof anchor).
