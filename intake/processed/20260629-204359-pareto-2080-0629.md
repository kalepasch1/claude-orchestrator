PROJECT: pareto-2080

# Wiring the autonomy + rate/reward moat. The pure-logic core is ALREADY BUILT and unit-tested
# (27 engines in server/utils/, ~190 passing node:test tests, 0 failures in new code) plus 4
# endpoints and a CI scanner. Full spec: PARETO_RATEMOAT_HANDOFF_PROMPT.md (repo root).
# These tasks are the production wiring only — import the existing engines, do NOT rewrite them.
# Governing invariants (enforce, never break): (1) free — no fee/affiliate/data-sale revenue;
# (2) non-discretionary by default — prepare, human commits; (3) non-custodial; (4) agent-of-user;
# (5) every completed transaction writes back to the market graph (recordMarketOutcome.requireWriteback).
# Repo conventions: pages owning <AppNav/> use definePageMeta({ layout: false }) NOT "none";
# parseInt+isNaN guard before Prisma; cast `$fetch as any`; every handler awaits requireAuth(event)
# + requireOwnership(event, uid). Prod is Vercel serverless (crons dual-register in nuxt.config.ts
# scheduledTasks AND vercel.json). Test gate: `npm test` must not regress beyond the 2 known
# pre-existing failures (tests/agentLedger.test.js, tests/bookingSaga.test.js — local Prisma-binary
# mismatch only).

- id: prisma-models-migration
  title: Add Prisma models + migration for moat persistence (observations, staged actions, elasticity, credentials, cohorts, counterfactual)
  material: yes
  model: opus
  depends: []
  proof: `npx prisma validate` exits 0 AND `npx prisma migrate diff --from-schema-datamodel prisma/schema.prisma --to-schema-datamodel prisma/schema.prisma --script` shows no drift
  prompt: |
    Add models to prisma/schema.prisma to persist the (already-built) engines' state, then create ONE
    migration. NAME-CHECK every table/column against the live DB before landing — this repo has a
    documented history of casing-mismatch P3018 failures (see CLAUDE.md "Migration Authoring").
    Models to add:
      - MarketObservation { id, userId, productKey, segmentKey, segmentJson Json, value Float, kind,
        source, providerId?, observedAt } — indexes (productKey, segmentKey) and (observedAt). This is
        the moat datastore fed by server/utils/recordMarketOutcome.recordOutcome(...).
      - StagedAction { id, userId, type, domain, category?, amountUsd, status, commitGate, payloadJson Json,
        targetJson Json, preparedAt, expiresAt, committedBy?, committedAt? } — mirrors server/utils/sessionRail.js.
      - ProviderElasticityState { providerId, productKey, n, sumRivalDeltaBp, sumMovedBp, bestFinal,
        observationsJson Json } unique (providerId, productKey) — mirrors server/utils/providerElasticity.js.
      - StandingAuction { id, userId, productKey, incumbentJson Json, cadenceMs, round, lastRunAt, historyJson Json }.
      - VerifiedDemandCredential { id, userId, productKey, tier, scoreBand, qualified, probability, issuedAt }
        — store the prequalification credential; NEVER store raw PII.
      - Cohort + CohortMember — for rfqMesh.formCohorts and demand/forward auctions.
      - CounterfactualEntry { id, userId, outcome, label, domain?, annualUsd, oneTimeUsd, at } — feeds
        server/utils/counterfactualLedger.rollup.
    Keep AutonomyConfig where it is (preferenceProfile.categoryWeights.autonomy) — do not migrate it.
    Follow every rule in the CLAUDE.md "Migration Authoring — name-check before landing" section.
    Do NOT run migrate deploy against prod (operator does that). `prisma validate` must pass.

- id: writeback-endpoint
  title: POST /api/finance/market-outcome — persist a real outcome via requireWriteback (annotated for the CI gate)
  material: yes
  model: sonnet
  depends: [prisma-models-migration]
  proof: `npm run check:writeback` exits 0 AND `node --check server/api/finance/market-outcome.post.js` exits 0
  prompt: |
    Add server/api/finance/market-outcome.post.js. requireAuth + requireOwnership + parseInt guards.
    Body = a real outcome (closing/negotiation/rfq response). Call
    server/utils/recordMarketOutcome.requireWriteback(...) to validate+normalize, then persist the
    returned observation as a MarketObservation row. Annotate the handler file with the marker comment
    `// @writeback-required` (the scanner scripts/check-writeback-invariant.mjs enforces that any file
    with this marker actually calls requireWriteback/recordOutcome). This is the data-flywheel invariant
    in production.

- id: market-rate-live-endpoint
  title: GET /api/finance/market-rate-live — build the true-market surface from persisted observations
  material: no
  model: sonnet
  depends: [prisma-models-migration]
  proof: `node --check server/api/finance/market-rate-live.get.js` exits 0
  prompt: |
    Add server/api/finance/market-rate-live.get.js (DB-backed sibling of the existing compute-only
    market-rate.post.js). requireAuth + requireOwnership + parseInt guards. Query: productKey + the
    user's segment. Read MarketObservation rows for that product, pass to
    server/utils/marketGraph.buildSurface (use commoditizedProducts.lowerIsBetter(productKey) for the
    direction), then marketGraph.bestAchievable for the segment. Return achievable + edgeVsAdvertised +
    basis. k-anonymity floor is enforced inside buildSurface (minCohort 3).

- id: staged-actions-endpoints
  title: Staged-action list + one-tap commit endpoints (the "stop at Submit" surface)
  material: yes
  model: sonnet
  depends: [prisma-models-migration]
  proof: `node --check server/api/finance/staged-actions.get.js && node --check server/api/finance/staged-actions/[id]/commit.post.js` exits 0
  prompt: |
    Add GET /api/finance/staged-actions (list the user's StagedAction rows) and
    POST /api/finance/staged-actions/:id/commit. The commit handler runs
    server/utils/sessionRail.commitStagedAction(staged, { actor: 'human' }) and returns the
    non-custodial submit instruction (the runtime in the session-rail task presses the button). Enforce
    ownership; never auto-commit here (human actor only). parseInt guards, requireAuth + requireOwnership.

- id: bundle-commit-endpoint
  title: POST /api/finance/bundle-commit — one authorization commits a correlated batch
  material: yes
  model: sonnet
  depends: [staged-actions-endpoints]
  proof: `node --check server/api/finance/bundle-commit.post.js` exits 0
  prompt: |
    Add server/api/finance/bundle-commit.post.js. Load the user's staged actions, group with
    server/utils/bundleCommit.buildBundles, and commit one bundle with a single authorization via
    bundleCommit.commitBundle({ actor:'human' }). Return per-item results (partial-safe). requireAuth +
    requireOwnership + parseInt guards.

- id: counterfactual-endpoint
  title: GET /api/finance/counterfactual — captured vs forgone vs incumbent-cost rollup
  material: no
  model: haiku
  depends: [prisma-models-migration]
  proof: `node --check server/api/finance/counterfactual.get.js` exits 0
  prompt: |
    Add server/api/finance/counterfactual.get.js. requireAuth + requireOwnership + parseInt guards.
    Read CounterfactualEntry rows for the user and return server/utils/counterfactualLedger.rollup(entries,
    { dailyRetirementValueUsd }). dailyRetirementValueUsd may be read from the user's existing
    value-ledger/world-model context if available, else omit.

- id: savings-index-endpoint
  title: GET /api/finance/savings-index — PUBLIC anonymized "beat advertised by N bp" index
  material: no
  model: haiku
  depends: [market-rate-live-endpoint]
  proof: `node --check server/api/finance/savings-index.get.js` exits 0
  prompt: |
    Add server/api/finance/savings-index.get.js. NO auth (public growth surface). Build the market
    surface from all MarketObservation rows (reuse the buildSurface logic from market-rate-live), then
    server/utils/savingsIndex.buildIndex(surface, { minDistinctUsers: 5 }). Return products + overallBp +
    headline. Enforce the stronger k-anonymity floor (>=5 distinct users) for a public figure.

- id: rfq-run-endpoint
  title: POST /api/finance/rfq/run — cohort -> targeted -> throttled RFQ orchestration + writeback on responses
  material: yes
  model: opus
  depends: [writeback-endpoint, staged-actions-endpoints]
  proof: `node --check server/api/finance/rfq/run.post.js` exits 0 AND `npm run check:writeback` exits 0
  prompt: |
    Add server/api/finance/rfq/run.post.js. Pipeline using the existing engines:
    rfqMesh.formCohorts -> rfqTargeting.targetProviders (feed elasticity via providerElasticity.elasticity)
    -> antiDetection.throttlePlan (schedule) -> emit RFQs (actual send is the session-rail runtime task;
    here, persist the plan + cohort + StandingAuction state). On provider responses,
    rfqMesh.ingestRfqResponses -> recordMarketOutcome.recordOutcome (writeback; annotate file
    `// @writeback-required`). Persist Cohort/CohortMember + StandingAuction rows. requireAuth +
    requireOwnership + parseInt guards.

- id: demand-auction-endpoint
  title: POST /api/finance/demand-auction — provider-side blind commitment auction over verified demand
  material: yes
  model: sonnet
  depends: [prisma-models-migration]
  proof: `node --check server/api/finance/demand-auction.post.js` exits 0
  prompt: |
    Add server/api/finance/demand-auction.post.js. Build a verified-demand block with
    server/utils/prequalification.cohortVerifiedDemand over VerifiedDemandCredential + Cohort rows, then
    run server/utils/demandAuction.runDemandAuction(block, bids). Return allocations + clearingRate +
    fill. requireAuth + requireOwnership + parseInt guards. Compute-only over persisted data; moves no
    money.

- id: wallet-and-forward-endpoints
  title: POST /api/finance/wallet-rfq and POST /api/finance/forward-demand
  material: no
  model: sonnet
  depends: [prisma-models-migration]
  proof: `node --check server/api/finance/wallet-rfq.post.js && node --check server/api/finance/forward-demand.post.js` exits 0
  prompt: |
    Add two endpoints. wallet-rfq.post.js: server/utils/walletBundle.assembleWalletRfq from the user's
    products (read their holdings) + compareWalletVsIndividual when offers are supplied.
    forward-demand.post.js: server/utils/forwardDemand.forecastPipeline from upcoming events (rate-lock
    expiry, CD maturity, policy renewal — read from the user's accounts/world model) and
    buildForwardBlock per product. requireAuth + requireOwnership + parseInt guards.

- id: elasticity-loop-wiring
  title: Close the provider-elasticity loop into RFQ openings + targeting
  material: yes
  model: opus
  depends: [rfq-run-endpoint, demand-auction-endpoint]
  proof: `npm test` does not regress beyond the 2 known pre-existing failures (agentLedger, bookingSaga)
  prompt: |
    Persist ProviderElasticityState. After every standing-auction round / negotiation outcome, call
    server/utils/providerElasticity.recordResponse and save state. In the RFQ/negotiation opening logic
    (rfq/run + any negotiationFanout caller), use providerElasticity.recommendOpening(state, providerId,
    productKey, marketAnchor) so we open at each provider's LEARNED FLOOR rather than asking them to beat
    a number. Feed providerElasticity.elasticity(...) into rfqTargeting.targetProviders via the
    elasticityByProvider arg. Add a focused node:test (tests/elasticityLoop.test.js) covering the
    record->recommend path so `npm test` proves the wiring; keep the suite green.

- id: crons-dual-register
  title: Register the 5 moat crons in nuxt.config.ts scheduledTasks AND vercel.json
  material: yes
  model: sonnet
  depends: [market-rate-live-endpoint, rfq-run-endpoint]
  proof: `node -e "const v=require('./vercel.json'); if(!(v.crons||[]).some(c=>c.path.includes('continuous-reshop')))process.exit(1)"` exits 0
  prompt: |
    Add 5 cron handlers under server/api/cron/ (CRON_SECRET-gated, fail-open) and dual-register each in
    BOTH nuxt.config.ts scheduledTasks AND vercel.json crons (prod is Vercel serverless):
      - continuous-reshop (daily): server/utils/continuousReshop.findReshopDeltas per user vs the live
        surface; pre-stage one-tap actions into StagedAction; surface in approvals.
      - standing-auction-sweep (daily): for each StandingAuction, if standingAuction.dueToReRun, re-fire
        RFQs + evaluateRound; switch/retain; writeback (annotate `// @writeback-required`).
      - trust-autotune (daily): trustAutotune.autotuneCaps from lane outcomes -> update each user's
        per-category caps within their floor/ceiling.
      - forward-demand-scan (daily): forwardDemand.forecastPipeline -> open forward blocks.
      - savings-index-rebuild (daily): recompute the public index.
    Any cron that completes a transaction is `// @writeback-required`. `npm run check:writeback` must stay green.

- id: ci-writeback-gate
  title: Add `npm run check:writeback` to the CI/preflight workflow
  material: yes
  model: haiku
  depends: []
  proof: `grep -rq "check:writeback" .github/workflows/` exits 0
  prompt: |
    The script + npm script already exist (scripts/check-writeback-invariant.mjs, `npm run check:writeback`).
    Add a step that runs `npm run check:writeback` to the repo's preflight / CI GitHub Actions workflow
    (under .github/workflows/) so the build fails if any `// @writeback-required` module doesn't write
    back to the market graph. Place it alongside the existing test step.

- id: autonomy-cockpit-page
  title: /Autonomy/:userId cockpit with simulate-before-arm
  material: no
  model: sonnet
  depends: []
  proof: `npx nuxt typecheck` exits 0 (or `node --check` of the page's <script> if typecheck unavailable)
  prompt: |
    Add pages/Autonomy/[userId].vue (own <AppNav/>, so definePageMeta({ layout: false }) — NOT "none").
    UI over the EXISTING endpoints GET/PUT /api/finance/autonomy and POST /api/finance/autonomy-simulate:
    global mode toggle (assist/autonomous/off), per-domain overrides, and the configurable limits
    (per-action/daily/weekly/monthly/per-category caps, allowed actions, trust floor, vendor allowlist,
    quiet hours, kill switch). Include a Simulate-before-arm panel that posts a proposed config and shows
    would-auto / would-hold / would-block counts + dollar values. Use the design-system tokens in
    CLAUDE.md; cast `$fetch as any`. Add a sidebar + command-palette entry.

- id: true-market-page
  title: "True market vs Bankrate" page/tab
  material: no
  model: sonnet
  depends: [market-rate-live-endpoint]
  proof: `npx nuxt typecheck` exits 0 (or `node --check` of the page's <script>)
  prompt: |
    Add a "True market vs Bankrate" surface (page or Finance tab) calling GET /api/finance/market-rate-live
    (and POST /api/finance/true-market for reward products). Per product show what a comparable profile is
    actually getting (bestAchievable), the edgeVsAdvertised, and the pre-staged one-tap switch. For cards,
    show the net-$/yr rewards ranking from rewardsNormalizer. Design-system tokens; `$fetch as any`;
    sidebar + command-palette entry.

- id: savings-index-page
  title: Public /savings-index page (growth surface)
  material: no
  model: haiku
  depends: [savings-index-endpoint]
  proof: `npx nuxt typecheck` exits 0 (or `node --check` of the page's <script>)
  prompt: |
    Add pages/savings-index.vue — PUBLIC (no auth) — rendering GET /api/finance/savings-index
    ("Pareto users beat advertised by N bp across M products"). This is the anti-Bankrate growth surface.
    Design-system tokens; `$fetch as any`.

- id: approvals-onetap-bundle
  title: Extend the approvals inbox with one-tap commit + bundle "approve all"
  material: no
  model: sonnet
  depends: [staged-actions-endpoints, bundle-commit-endpoint]
  proof: `npx nuxt typecheck` exits 0 (or `node --check` of the page's <script>)
  prompt: |
    Extend pages/Approvals/User/[userId].vue to render StagedAction items (from GET /api/finance/staged-actions)
    with a one-tap commit (POST .../staged-actions/:id/commit) and a bundle "approve all" that calls
    POST /api/finance/bundle-commit. Add a counterfactual ribbon reading GET /api/finance/counterfactual
    (captured vs forgone vs incumbent-cost). Design-system tokens; `$fetch as any`.

OPERATOR:
  - Run `npx prisma migrate deploy` for the new migration against a shadow DB, then prod (after name-check) — the orchestrator must not deploy DB migrations to prod autonomously.
  - Session-rail browser runtime (replaces Plaid): build with Claude-in-Chrome / browser-agent tools, NOT pure utils — reads the user's own authenticated bank/brokerage/card/insurer sessions (or OAuth/FDX/email-statement/document-upload), prepares forms, stops at Submit, presses on the user's tap. Requires per-user consent + browser-agent infra; cannot run as a queued repo task.
  - Cross-platform S2S federation with Smarter + Apparently (contract terms, regulatory corpus, federated cohort pooling): needs secrets PARETO_SMARTER_S2S_SECRET and PARETO_APPARENTLY_S2S_SECRET set in Vercel prod env + each sibling's env, and endpoints on the sibling repos. HMAC pattern per the Tomorrow/Apparently _s2s helpers.
  - Confirm the legal posture (free / non-discretionary / non-custodial / agent-of-user) with counsel before any autonomous-execution path ships — this is the keystone that keeps the system outside the regulated-intermediary perimeter.
  - Do NOT introduce any fee, affiliate, lead-gen, or data-sale revenue path — it breaks invariant (1) and reopens the "for compensation" regulatory trigger.
