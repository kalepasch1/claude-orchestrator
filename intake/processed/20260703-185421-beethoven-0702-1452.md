PROJECT: beethoven

# Fourth batch of orchestrator-internal CADE improvements. Same conflict-free design as batches 1-3: every
# task is a NEW runner/cx_*.py file auto-discovered + run by cade-extras-harness, so nothing edits
# committees.py / periodic.py / runner.py and none can merge-conflict with the serialized committees chain
# (cade-contracts → … → cade-determination-rlhf), the earlier cx_* batches, or each other. All read tables
# that already exist (determinations, determination_outcomes, committee_scoreboard, seat_calibration,
# committee_reviews, approvals, cost_ledger if present) and reuse committees.py helpers (db.*, review(),
# replay_determination, model_gateway) — do not reimplement or edit committees.py.

- id: cx-confidence-gap-learning
  title: Confidence-gap active learning — re-deliberate the system's own surprises
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_confidence_gap_learning.py` exits 0
  prompt: |
    Add runner/cx_confidence_gap_learning.py with run(): find determinations where predicted confidence and
    the realized labeled_outcome most disagree (high-confidence GO that went bad, or low-confidence HOLD that
    would have paid off), and route those exact subjects to re-deliberation (queue an inbox item
    kind='surprise_review' + optionally an improvement_proposal to re-run). Learning fastest from surprises.
    Read-only except the queue/digest; reuse determination_outcomes; do not edit committees.py.

- id: cx-persona-lifecycle
  title: Persona retirement / promotion proposals from calibration
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_persona_lifecycle.py` exits 0
  prompt: |
    Add runner/cx_persona_lifecycle.py with run(): from committee_scoreboard + seat_calibration, identify
    chronically-wrong expert archetypes (low accuracy / high Brier over enough calls) and consistently-right
    ones, and PROPOSE retiring or promoting them into the triage default pool as an owner-ratifiable approval
    card (insert approvals kind='material', status='pending', clear why). Never auto-change the roster.
    Advisory only; no schema change; do not edit committees.py.

- id: cx-determination-bundling
  title: Determination bundling — one blanket ruling for clusters of near-identical low-materiality calls
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_determination_bundling.py` exits 0
  prompt: |
    Add runner/cx_determination_bundling.py with run(): cluster recent LOW-materiality determinations by
    title/body similarity, and when a tight cluster repeats, propose a single blanket ruling to cover the
    class (inbox kind='bundle_proposal' citing the members + the common recommendation) so the engine stops
    re-litigating the same call. Bounded; read-only except the proposal; do not edit committees.py.
    CANDIDATE-SHARED: determination bundling.

- id: cx-owner-language-tuning
  title: Owner-language tuning — rewrite escalated 1-pagers in the owner's preferred format
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_owner_language_tuning.py` exits 0
  prompt: |
    Add runner/cx_owner_language_tuning.py with run(): learn the owner's preferred brevity/format from which
    inbox digests get acted on (owner_overrides + inbox read/act signals if available), and produce a
    tightened restatement of each freshly-escalated determination's 1-pager (store as an inbox item
    kind='onepager_tuned' or alongside the determination) so the reviewer surface keeps getting faster to
    read. Do not overwrite the canonical onepager. Reuse model_gateway; no schema change; do not edit
    committees.py.

- id: cx-regret-accounting
  title: Regret accounting — money-denominated cost of wrong decisions per domain
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_regret_accounting.py` exits 0
  prompt: |
    Add runner/cx_regret_accounting.py with run(): for determinations with a realized negative outcome,
    estimate the regret (missed upside + incurred downside) from determination_outcomes/merge_revenue and
    aggregate by expert domain into an inbox report (kind='regret_report'), giving a money view of where
    deliberation quality matters most. Read-only except the report; degrade gracefully if revenue signals are
    missing; do not edit committees.py.

- id: cx-escalation-sla
  title: Escalation SLA watchdog — nudge stale reviews; safe auto-default past SLA
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_escalation_sla.py` exits 0
  prompt: |
    Add runner/cx_escalation_sla.py with run(): track how long escalated determinations / pending approvals
    have sat unreviewed; nudge (inbox kind='sla_nudge') past a soft SLA, and for LOW-materiality + REVERSIBLE
    items past a hard SLA, default to the panel's recommendation with a logged rationale (owner_overrides
    direction='sla_default') — NEVER for legal/critical/irreversible ones, which keep waiting for a human. So
    nothing stalls silently. Reuse the determination's materiality; no schema change; do not edit committees.py.

- id: cx-decision-templates
  title: Cross-app decision template library from recurring determination patterns
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_decision_templates.py` exits 0
  prompt: |
    Add runner/cx_decision_templates.py with run(): mine determinations across apps for recurring decision
    patterns (similar subjects + stable recommendations) and publish them as reusable "decision templates"
    (inbox kind='decision_template' or a docs/decisions/templates/*.md file) so common calls resolve instantly
    with cited precedent. Idempotent; read-only except the templates; do not edit committees.py.
    CANDIDATE-SHARED: cross-app decision templates.

OPERATOR:
  - None required. All tasks are runner-only, additive, and read tables that already exist — no secrets, deploys, schema migrations, or legal sign-off.
