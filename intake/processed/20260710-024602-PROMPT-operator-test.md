Add a one-line "fleet healthy" status badge to the top of the orchestrator web dashboard
(web/) that reads the latest sentinel_state.json db_up flag — green when up, red when down.
Small, non-material, prefer a cheap local model.
