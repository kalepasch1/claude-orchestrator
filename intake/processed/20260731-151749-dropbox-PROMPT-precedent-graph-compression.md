# Precedent-Graph Compression — corpus-wide zero-token reasoning (operator, 2026-07-31)

Verdict cards currently stand alone. BUILD (illuminati + orchestrator corps):
1. CITATION GRAPH: link cards card->card and card->authority (statute/case/reg) with TYPED edges:
   follows / distinguishes / questions / extends; holding vs dictum weight on each edge.
2. T0 GRAPH TRAVERSAL: the deterministic T0 seat answers NOVEL questions by nearest-precedent
   traversal + distinguishing-factors at ZERO token cost (same trick as the examiner-sim seat,
   extended to the whole corpus). Sub-ms target via the build-time JSON index.
3. 50-500X EXTENSIONS (build all):
   a. TREATMENT SIGNALS: Shepard's-style status per card (followed/distinguished/questioned/
      superseded) maintained nightly by the cheap-model canon author; stale/questioned cards
      surface a warning in gradients that cite them.
   b. GRAPH-WALK PROOFS: every T0 graph answer ships the full traversal chain in the explanation
      dossier (audit-ready citation path: question -> nearest precedent -> distinguishing factors
      -> conclusion) — this is the glass-box marketing artifact.
   c. CONTRADICTION DETECTION: cycle/conflict finding across the graph queues gauntlet bouts to
      resolve doctrinal contradictions — the corpus self-heals its own splits.
   d. WARM-START BENCH: expert-bench seats load the relevant subgraph as grounding, cutting seat
      tokens further and raising citation floors toward the 20-50 goal.
   e. STANDARDS EXPORT: publish the curated public slice of the graph as the standards-registry
      artifact (publication_view rules apply: per-class personas, never expose lens).
All commits kalepasch1 <kalepasch@gmail.com>.
