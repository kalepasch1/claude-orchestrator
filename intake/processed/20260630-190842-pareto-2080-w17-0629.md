PROJECT: pareto-2080

# W17 — Scale, safety & extrapolation layer for the W16 collective-learning + coordination mesh.
# These build on engines ALREADY IN THE REPO (server/utils/: experienceGraph, coordinationMesh,
# contingentCommitment, fairDivision, vendorGraph, negotiationSelfPlay, vendorTwinService,
# anonymize, trustGraduation, simHarness, worldModel). Each task below is a NEW pure-logic engine
# (ESM, no external deps, seedable RNG via the mulberry32 pattern in retirementMonteCarlo.js) plus a
# node:test suite, occasionally wiring into one existing endpoint. DO NOT rewrite the W16 engines —
# import them. Test gate: `npm test` (node --test tests/*.test.js) must not regress beyond the 2
# known pre-existing failures (tests/agentLedger.test.js, tests/bookingSaga.test.js — Prisma-binary
# only). Conventions: parseInt+isNaN guard before Prisma; requireAuth + requireUserIdOwnership in
# handlers; anonymize at WRITE time; money in USD. These are mostly independent (depends: []) so they
# parallelize; the 3 governance/safety ones are material.

- id: offpolicy-eval
  title: Off-policy evaluation gate before promoting a playbook to the network
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/offPolicyEval.test.js` exits 0 AND `node --check server/api/experience/distill.post.js` exits 0
  prompt: |
    Add server/utils/offPolicyEval.js (pure): estimate a candidate playbook/policy's effect FROM LOGGED
    experiences using inverse-propensity weighting (IPW) and a doubly-robust (DR) estimator, given
    logged actions+outcomes (ExperienceRecord shape) and a target policy (tactic-selection rule).
    Export estimatePolicyValue(logged, targetPolicy, {method:'ipw'|'dr'}) -> { value, stderr, n,
    effectiveSampleSize }. Then gate promotion: in server/api/experience/distill.post.js, before calling
    experienceGraph.shouldPromote, require the off-policy estimate to beat the incumbent by a margin with
    adequate effective sample size. Add tests/offPolicyEval.test.js proving: IPW unbiasedness on a
    constructed example, DR lower variance than IPW, and that low ESS blocks promotion. Material: governs
    what tactic ships to all users. Keep `npm test` green.

- id: truthful-clearing
  title: Incentive-compatible (strategy-proof) coalition clearing + payments
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/truthfulClearing.test.js` exits 0
  prompt: |
    Public self-reported intents invite WTP-shading; make the mechanism strategy-proof. Add
    server/utils/truthfulClearing.js (pure) implementing a VCG-style payment rule (or a strategy-proof
    posted-price/uniform-price rule) over a set of intents with reported WTP/qty, composing
    contingentCommitment.clearReverseAuction for the allocation and fairDivision.shapleyValues for credit.
    Export clearTruthful(intents, {rule:'vcg'|'uniform'}) -> { allocation, payments, clearingPrice,
    socialWelfare }. Add tests/truthfulClearing.test.js proving the core incentive property numerically:
    no single agent improves its utility by misreporting WTP (sweep a misreport grid and assert truthful
    is weakly dominant), and payments are individually rational (>=0 utility). Material: governs coalition
    money clearing. Keep `npm test` green.

- id: robust-aggregation
  title: Poisoning/Sybil-resistant aggregation for distill + bandit arms
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/robustAggregation.test.js` exits 0
  prompt: |
    The shared graph is an attack surface. Add server/utils/robustAggregation.js (pure): robust
    estimators (trimmed mean, median-of-means, influence/outlier down-weighting) and a Sybil-resistant
    cohort weighting that weights contributors by trustGraduation score and caps any single
    userHash/cohort's influence. Export robustMean(values, {trim}) , downweightOutliers(records, keyFn),
    and sybilWeightedStats(records, trustByHash). Wrap the aggregation inside experienceGraph.distill and
    experienceGraph.toBanditArms (add an opts.aggregator hook; default to robust). Add
    tests/robustAggregation.test.js proving a minority of poisoned/duplicated records cannot move the
    aggregate beyond a bound, and that a Sybil flood (many records, few real users) is correctly
    discounted. Material: protects what the whole network learns. Keep `npm test` green.

- id: voi-corpus-targeting
  title: Active-learning layer — direct probes at the highest value-of-information gaps
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/voiCorpus.test.js` exits 0
  prompt: |
    Capture is passive; make it directed. Add server/utils/voiCorpus.js (pure): score every
    (goalType × counterparty × context-bucket) cell by value-of-information = uncertainty × frequency ×
    decision-impact, using ExperienceRecord density + playbook confidence + vendorGraph price-fact
    staleness. Export rankProbeTargets(cells, opts) -> ranked gaps, and allocateProbeBudget(targets,
    budget) (greedy under a budget). This tells the mesh where to spend probe intents / re-verification.
    Mirror the VOI shape already used in server/utils/adaptivePrompts.js. Add tests/voiCorpus.test.js
    proving thin+high-impact cells rank above dense+low-impact ones and budget allocation respects the cap.

- id: federated-aggregation
  title: Federated aggregation of client-side gradients/abstractions (no raw data pooled)
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/federatedAggregation.test.js` exits 0
  prompt: |
    Expand the contributing population to privacy-sensitive users by learning from shipped abstractions,
    not raw records. Add server/utils/federatedAggregation.js (pure): secure-ish aggregation of
    per-client weight/stat updates with DP noise (reuse the dp-privacy-budget primitives in anonymize.js
    once present; if not present yet, add a local laplace fallback), robust averaging (compose
    robustAggregation when available), and a staleness/contribution-weighting scheme. Export
    aggregateUpdates(clientUpdates, {epsilon, clip}) -> globalUpdate. Add tests/federatedAggregation.test.js
    proving clipping bounds any single client's influence, DP noise is seeded/bounded, and the aggregate
    converges to the mean on honest inputs. Material: privacy-governing. Keep `npm test` green.

- id: causal-vendor-model
  title: Structural causal vendor model (extrapolate why vendors concede)
  material: no
  model: opus
  depends: []
  proof: `node --test tests/causalVendorModel.test.js` exits 0
  prompt: |
    The twin interpolates; add extrapolation. Add server/utils/causalVendorModel.js (pure): a small
    structural model mapping causal factors (quarter-end proximity, inventory/utilization, competitive
    pressure, churn risk, tenure) -> expected concession, fit from vendorGraph observations. Export
    fitCausalModel(observations) and predictConcession(model, situation) that generalizes to unseen
    vendors sharing structure (pooled/partial-pooling across vendors in a category). Feed
    predictConcession in as a prior for vendorGraph.buildTwin when a vendor is data-thin. Add
    tests/causalVendorModel.test.js proving transfer: a model fit on vendors A,B predicts a held-out
    vendor C in the same category better than the category mean. Keep `npm test` green.

- id: graph-embeddings
  title: Graph embeddings for analogical cross-domain retrieval
  material: no
  model: opus
  depends: []
  proof: `node --test tests/graphEmbeddings.test.js` exits 0
  prompt: |
    Feature-similarity retrieval misses structural analogies. Add server/utils/graphEmbeddings.js (pure,
    no external deps): build a node embedding over the Experience/Vendor graph using a deterministic,
    dependency-free method (random-walk + hashing/co-occurrence factorization, seeded RNG). Export
    embedGraph(nodes, edges, {dim, seed}) and nearestByEmbedding(query, embeddings, k). Wire an optional
    embedding-based path into experienceGraph.retrieve so "this rent negotiation rhymes with that SaaS
    renewal" surfaces across goalTypes. Add tests/graphEmbeddings.test.js proving structurally-analogous
    nodes (same connectivity pattern, different domain) embed closer than unrelated nodes, deterministically.

- id: program-synthesis-loop
  title: Self-improving negotiation programs (strategy synthesis against the twin)
  material: no
  model: opus
  depends: []
  proof: `node --test tests/programSynthesis.test.js` exits 0
  prompt: |
    Improve the agents' own reasoning, not just the data. Add server/utils/programSynthesis.js (pure):
    represent a negotiation strategy as a small typed program (opener, concession schedule, lever
    triggers, stopping rule); mutate/recombine candidates and evaluate each against
    negotiationSelfPlay.simulateNegotiation on the vendor twin; keep an elite set (evolutionary / hill-
    climb, seeded RNG). Export synthesizeStrategy(twin, {generations, population, seed}) -> bestProgram +
    history. Promote a synthesized program only if it beats the current best opener by a margin (compose
    offPolicyEval when available). Add tests/programSynthesis.test.js proving monotonic elite improvement
    over generations and that the output is a runnable strategy for optimizeOpener. Keep `npm test` green.

- id: mesh-aware-life-sim
  title: Mesh-aware life simulation — fold coalition/pre-position gains into the retirement number
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/meshAwareLifeSim.test.js` exits 0
  prompt: |
    Connect the collective layer to the core product number. Add server/utils/meshAwareLifeSim.js (pure):
    given a user's worldModel state + a set of available mesh opportunities (forming coalitions with
    expected savings, pre-positionable predicted events), project the retirement-date / success-prob
    delta by feeding the expected savings streams through the existing retirement math (compose
    simHarness / retirementMonteCarlo without rewriting them). Export projectWithMesh(world, opportunities)
    -> { baseline, withMesh, retirementDaysDelta }. Add tests/meshAwareLifeSim.test.js proving more/larger
    coalition savings strictly reduce the retirement date and that zero opportunities == baseline.
    Keep `npm test` green.

- id: pareto-frontier-trades
  title: Pareto-frontier matching — pair users with orthogonal preferences in one coalition
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/paretoFrontierTrades.test.js` exits 0
  prompt: |
    Value from better matching alone. Add server/utils/paretoFrontierTrades.js (pure): represent each
    member's preference weights over dimensions (price, timing, effort, values constraints); compute the
    Pareto frontier of a coalition's feasible deals and find allocations where members trade on the
    dimension they weight least to gain on the one they weight most (e.g. price-sensitive vs date-sensitive
    both improve). Export frontier(deals), and matchOrthogonal(members) -> pairings that are mutually
    improving. Add tests/paretoFrontierTrades.test.js proving an orthogonal-preference pair achieves a
    Pareto-improvement over the naive equal split, and that the frontier excludes dominated deals.

- id: commitment-trust-graph
  title: Graph-propagated commitment-reliability trust (reliable pools win better terms)
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/commitmentTrust.test.js` exits 0
  prompt: |
    Contingent commitments fail when members bail. Add server/utils/commitmentTrust.js (pure): score each
    agent's follow-through from ContingentCommitment fired-vs-bailed history (Beta-Bernoulli, reuse the
    trustGraduation.recommendTierBayes pattern), then propagate trust through the coalition graph so an
    agent in reliable coalitions inherits a reliability prior. Export reliabilityScore(history),
    propagateTrust(graph, seeds), and a coalitionReliability(members) used to prioritize quorum formation
    and to set the vendor-facing "committed and ready" confidence. Add tests/commitmentTrust.test.js
    proving reliable members rank above flaky ones and that propagation raises trust for agents connected
    to reliable hubs. Keep `npm test` green.

OPERATOR:
  - Federated personalization requires an on-device / client-side adaptation runtime that ships only gradients/abstractions to federated-aggregation — that client runtime is browser/app infra, not a queued repo task; the server aggregation engine is queued above.
  - Cryptographic outcome attestation (tamper-evident experiences) and zero-knowledge eligibility proofs need a signing/verification key + a verifier service — list for security review before enabling as the anti-poisoning root of trust.
  - Off-policy / mechanism-design / robust-aggregation changes alter what auto-promotes and how coalitions clear at scale — keep counsel review on demand-aggregation in the loop before wide rollout (consistent with the W16 operator note).
