PROJECT: pareto-2080

# W16 — Collective-learning Experience Graph + public Coordination Mesh. The pure-logic CORE IS
# ALREADY BUILT, wired, and unit-tested in the repo (76 new node:test cases, all green; full suite
# 864 tests, only the 2 known pre-existing Prisma-binary failures remain: tests/agentLedger.test.js,
# tests/bookingSaga.test.js). DO NOT rewrite the engines — import them. Already in repo:
#   server/utils/anonymize.js (unified privacy primitive; priceIntel/playbooks/experienceGraph delegate to it)
#   server/utils/experienceGraph.js (capture/retrieve/distill/counterfactual/creditAssignment/toBanditArms)
#   server/utils/coordinationMesh.js (intents, shards, formCoalitions, stigmergic signals, publicSnapshot,
#     preemptiveIntentsFromEvent, coalitionToPoolData, intentDbId)
#   server/utils/contingentCommitment.js (evaluate/evaluatePool/clearReverseAuction/resolveFallback)
#   server/utils/fairDivision.js, server/utils/vendorGraph.js, server/utils/negotiationSelfPlay.js,
#   server/utils/vendorTwinService.js, server/utils/darwinCapabilities.js
#   endpoints: /api/experience/{capture,retrieve,distill}, /api/mesh/{intents,snapshot,match,preposition,
#     commitments, commitments/evaluate, coalitions/[id]/instantiate}, /api/negotiation/self-play
#   prisma models ALREADY ADDED to schema.prisma (validates clean) but NOT yet migrated:
#     ExperienceRecord, MeshIntent, ContingentCommitment, VendorProfile, MeshCoalition
#   schema generator already has binaryTargets = ["native","linux-arm64-openssl-3.0.x"].
# Repo conventions (enforce, never break): pages owning <AppNav/> use definePageMeta({ layout: false })
# NOT "none"; parseInt+isNaN guard before every Prisma call; handlers await requireAuth(event) +
# requireUserIdOwnership(event, userId); ESM, no external deps in server/utils engines; money in USD;
# anonymize at WRITE time (k-anonymity, never store raw PII into collective tables). Prod is Vercel
# serverless — crons dual-register in BOTH nuxt.config.ts scheduledTasks AND vercel.json. Test gate:
# `npm test` (node --test tests/*.test.js) must not regress beyond the 2 known pre-existing failures.

- id: w16-prisma-migration
  title: Create the migration for the 5 W16 models already present in schema.prisma
  material: yes
  model: opus
  depends: []
  proof: `npx prisma validate` exits 0
  prompt: |
    The 5 W16 models (ExperienceRecord, MeshIntent, ContingentCommitment, VendorProfile, MeshCoalition)
    are ALREADY defined in prisma/schema.prisma and validate. Create ONE Prisma migration that lands
    exactly these tables (experience_record, mesh_intent, contingent_commitment, vendor_profile,
    mesh_coalition) with their @@map names, indexes, and @@schema("public"). NAME-CHECK every table/column
    against the live DB casing before landing — this repo has a documented P3018 casing-mismatch history
    (see CLAUDE.md "Migration Authoring"). Do NOT alter any existing model. Do NOT run migrate deploy
    against prod (operator does that). After generating, `npx prisma validate` must pass and
    `npx prisma generate` must succeed locally. Keep `npm test` green (no new failures).

- id: instrument-experience-capture
  title: Emit anonymized experiences from existing outreach/negotiation/sweep completion points
  material: no
  model: sonnet
  depends: [w16-prisma-migration]
  proof: `node --test tests/experienceCaptureWiring.test.js` exits 0
  prompt: |
    Nothing currently CALLS experienceGraph.captureExperience, so the graph stays empty — this is the
    "instrument capture first" foundation. Add a thin reusable helper server/utils/recordExperience.js
    that takes a raw bot outcome, runs experienceGraph.captureExperience (write-time anonymization), and
    persists an ExperienceRecord row (fields map: context_bucket->contextBucket, _userHash->userHash).
    Then call it at the real outcome points in the EXISTING flows: negotiation send/close
    (server/api/finance/negotiations*), subscription sweep accept (sweep-proposal/better-option), booking
    confirm, and group-buy negotiate. Each call passes goalType, phase, tactic, counterpartyId, outcome
    {status,savedPct,magnitudeUsd}, cost, signal{completed,purchased}, userId. Add
    tests/experienceCaptureWiring.test.js covering recordExperience's anonymize+shape (pure; mock the
    prisma create) so the proof runs without a DB. Keep `npm test` green.

- id: bandit-crossuser-wiring
  title: Feed cross-user experiences into live NegotiationVariant selection (+ abstract-lever transfer)
  material: no
  model: sonnet
  depends: [instrument-experience-capture]
  proof: `node --test tests/banditCrossUser.test.js` exits 0
  prompt: |
    Close the loop so negotiationBandit learns across ALL users instead of per-session. In the live
    variant-selection path (the caller of negotiationBandit.selectVariant / the NegotiationVariant model),
    build arms via experienceGraph.toBanditArms over recent ExperienceRecord rows for the category, then
    select with the existing UCB1 selectVariant. Additionally support keyBy = an abstract-lever function
    (e.g. "deadline_pressure" / "volume_leverage" derived from moveSequence) so a tactic learned in one
    domain transfers to another. Add tests/banditCrossUser.test.js proving: (a) arms aggregate wins/trials
    across distinct userHashes, (b) the winning cross-user tactic is selected, (c) abstract-lever keying
    groups across goalTypes. Pure test (no DB). Keep `npm test` green.

- id: twin-rehearse-outreach
  title: Make every outreach engine rehearse against the vendor twin before sending
  material: no
  model: sonnet
  depends: [w16-prisma-migration]
  proof: `node --check server/api/finance/negotiations/[id]/send.post.js` exits 0 AND `npm test` does not regress beyond the 2 known pre-existing failures
  prompt: |
    vendorTwinService.loadAndRehearse(prisma, vendorId, opts) already exists and is tested. Wire it into
    the real outreach paths so they rehearse first and attach the optimized opener: negotiation send
    (server/api/finance/negotiations/:id/send), billAudit outreach, and group-buy negotiate
    (server/api/personal/groups/pools/[id]/negotiate.post.js). Before composing the draft, call
    loadAndRehearse to get selfPlay.opener (target discount, levers) and use expectedDiscount as the
    opening ask; persist the (learning-updated) VendorProfile. When a real outcome returns, pass it back
    as `observation` so the twin learns. Do not change endpoint signatures. node --check the touched files;
    keep `npm test` green.

- id: mesh-coordination-cron
  title: Nightly cron — form coalitions, evaluate contingents, pre-position predicted events
  material: yes
  model: sonnet
  depends: [w16-prisma-migration]
  proof: `node -e "const v=require('./vercel.json'); if(!(v.crons||[]).some(c=>c.path.includes('mesh-coordinate')))process.exit(1)"` exits 0
  prompt: |
    Add server/api/cron/mesh-coordinate.post.js (CRON_SECRET-gated, fail-open). Each run: (1) call the
    coordinationMesh.formCoalitions logic over open MeshIntent rows per shard and upsert MeshCoalition
    rows (reuse the logic in server/api/mesh/match.post.js); (2) for goalTypes with quorum coalitions,
    run contingentCommitment.evaluatePool and persist state transitions; (3) for each user with consented
    PredictedLifeEvent rows, run the preposition logic (server/api/mesh/preposition.post.js) to stage
    tentative intents. Dual-register the cron in BOTH nuxt.config.ts scheduledTasks AND vercel.json crons
    (path /api/cron/mesh-coordinate). Material: it autonomously forms pools and stages intents on a
    schedule. Keep `npm test` green.

- id: twin-rehearsal-cron
  title: Nightly twin rehearsal + sim-vs-real divergence (counter-adaptation) alert
  material: yes
  model: sonnet
  depends: [twin-rehearse-outreach]
  proof: `node -e "const v=require('./vercel.json'); if(!(v.crons||[]).some(c=>c.path.includes('twin-rehearse')))process.exit(1)"` exits 0
  prompt: |
    Add server/api/cron/twin-rehearse.post.js (CRON_SECRET-gated, fail-open). For each VendorProfile a
    user is actively facing, pre-compute the optimal opener via vendorTwinService so outreach is instant.
    Track the gap between the twin's predicted discount and the last realized discount; when reality
    diverges beyond a threshold, raise a behavioral/notification signal that the vendor is counter-adapting
    (and flag stale price facts via vendorGraph.staleItems for re-probe). Dual-register in nuxt.config.ts
    scheduledTasks AND vercel.json (path /api/cron/twin-rehearse). Keep `npm test` green.

- id: elasticity-curve-persistence
  title: Persist reverse-auction demand curves → per-category price-elasticity asset
  material: no
  model: sonnet
  depends: [w16-prisma-migration]
  proof: `npx prisma validate` exits 0 AND `node --test tests/elasticityCurve.test.js` exits 0
  prompt: |
    contingentCommitment.clearReverseAuction already returns a demand `curve` per offering. Add an
    ElasticitySnapshot model { id, productKey, segmentKey, curveJson Json, clearingPrice Float,
    committedQty Int, observedAt } (@@map "elasticity_snapshot", @@schema("public"), index
    (productKey, observedAt)) and a follow-on migration (name-check casing). Persist a snapshot whenever
    a reverse auction clears (in /api/mesh/commitments/evaluate). Add server/utils/elasticity.js
    estimateElasticity(snapshots) that fits price->committedQty into an elasticity coefficient, and feed
    it into vendorGraph concession priors + contingent price recommendation. Add tests/elasticityCurve.test.js
    (pure) proving estimateElasticity monotonicity + that a steeper curve yields higher elasticity. Keep
    `npm test` green.

- id: dp-privacy-budget
  title: Differential-privacy noise + global privacy budget + consent/provenance in anonymize.js
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/anonymizeDP.test.js` exits 0
  prompt: |
    anonymize.js is now the single privacy chokepoint — make it the place that enforces differential
    privacy so the collective corpus can EVER be safely used for Loop C (policy fine-tuning). Add (pure,
    no deps): laplaceNoise(value, sensitivity, epsilon) with a seedable RNG (reuse the mulberry32 pattern
    from retirementMonteCarlo.js for determinism in tests); a PrivacyBudget accountant
    {spend(epsilon), remaining()} that refuses queries once the budget is exhausted; a provenance/consent
    tag stamped onto every anonymized record {consentScope, kCohortSize, epsilonSpent}. Expose
    dpAggregate(values, {epsilon, sensitivity, budget}) used by any aggregate/public surface (savings
    index, public snapshot counts). Add tests/anonymizeDP.test.js proving noise is bounded+seeded,
    budget exhaustion blocks further queries, and provenance tags are attached. Material: governs what the
    network may learn from private data — the keystone for any future fine-tuning. Keep `npm test` green.

- id: transferable-reservations
  title: Intent liquidity — reassign a fired coalition slot to another user's matching intent
  material: no
  model: sonnet
  depends: [w16-prisma-migration]
  proof: `node --test tests/reservationTransfer.test.js` exits 0 AND `node --check server/api/mesh/reservations/transfer.post.js` exits 0
  prompt: |
    Add server/utils/reservationTransfer.js (pure): given a fired/settled MeshCoalition slot and a pool of
    open MeshIntents, pick the best-matching replacement intent (reuse coordinationMesh.compatible +
    similarity scoring) WITHOUT exposing identities (operate on pseudonyms/intentIds; resolve userId only
    server-side). Add POST /api/mesh/reservations/transfer that reassigns a slot: marks the donor intent
    released, attaches the recipient's intentId/userId to the coalition member set, and records the
    handoff. requireAuth + requireUserIdOwnership + parseInt guards. Add tests/reservationTransfer.test.js
    (pure) covering best-match selection + that no raw identity crosses the matching boundary. Keep
    `npm test` green.

- id: mesh-demand-map-page
  title: Live "demand map" page rendering the public mesh snapshot
  material: no
  model: sonnet
  depends: [w16-prisma-migration]
  proof: `npx nuxt typecheck` exits 0 (or `node --check` of the page's <script> if typecheck unavailable)
  prompt: |
    Add pages/Mesh/[userId]/index.vue (owns <AppNav/> → definePageMeta({ layout: false }) NOT "none").
    Render GET /api/mesh/snapshot: forming coalitions (size, totalQty, meanWtp), open probes, hot shards,
    and an "intents near you" view by region bucket — pseudonyms + buckets only, never identities. Add a
    one-tap "publish intent" composer posting to /api/mesh/intents and a "pre-position my predicted needs"
    button posting to /api/mesh/preposition. Use the design-system tokens in CLAUDE.md; cast `$fetch as any`.
    Add an AppNav link ("Mesh" → /Mesh/${uid}) and a command-palette entry.

- id: coalition-onetap-ui
  title: One-tap "instantiate group pool" from a formed coalition (GroupBuy/Approvals surface)
  material: no
  model: sonnet
  depends: [mesh-coordination-cron]
  proof: `npx nuxt typecheck` exits 0 (or `node --check` of the touched <script>)
  prompt: |
    Surface quorum-reached MeshCoalition rows in pages/GroupBuy/[userId]/index.vue (and/or the Approvals
    inbox) with a one-tap "Start group negotiation" that calls
    POST /api/mesh/coalitions/:id/instantiate (creates the BuyingPool + Tier-A outreach draft via the
    existing W11 back-end). Show the discount curve / tier thresholds from groupBuying. Design-system
    tokens; `$fetch as any`.

OPERATOR:
  - Run `npx prisma migrate deploy` for the W16 migration(s) (w16-prisma-migration + elasticity-curve-persistence) against a shadow DB then prod, AFTER name-checking casing — the orchestrator must not deploy DB migrations to prod autonomously.
  - Darwin cross-product publish: vendor @darwin/kernel (git subtree per DARWIN_KERNEL_ADOPTION.md) and call darwinCapabilities.publishMeshCapabilities(registry, APP_BASE_URL) at startup so Tomorrow/Smarter can query Pareto's Experience/Vendor graphs. Needs the darwn repo subtree + DARWIN_SIGNING_PRIVATE_KEY_PEM — cross-repo, cannot run as a single queued task here.
  - Cross-product experience pooling (Pareto CONSUMING Tomorrow/Smarter negotiation memory) needs S2S secrets in Vercel prod env + sibling endpoints (HMAC pattern per the existing _s2s helpers).
  - Counsel review before demand-pooling / forward-demand / reverse-auction clearing goes wide at scale (buyer-side demand aggregation can attract price-coordination scrutiny in some markets). Operator/legal sign-off only — never queued.
