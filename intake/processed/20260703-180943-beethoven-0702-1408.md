PROJECT: beethoven

# Third batch of orchestrator-internal CADE improvements. Same conflict-free design as the prior two batches:
# every task is a NEW runner/cx_*.py file auto-discovered + run by cade-extras-harness, so nothing edits
# committees.py / periodic.py / runner.py and none can merge-conflict with the serialized committees chain
# (cade-contracts → … → cade-determination-rlhf), the earlier cx_* batches, or each other. All read tables
# that already exist (determinations, determination_outcomes, committee_scoreboard, committee_reviews) and
# reuse committees.py helpers (db.*, review(), replay_determination, _consensus, model_gateway) — do not
# reimplement or edit committees.py.

- id: cx-ensemble-panels
  title: Ensemble-of-panels agreement gate for top-materiality determinations
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_ensemble_panels.py` exits 0
  prompt: |
    Add runner/cx_ensemble_panels.py with run(): for the highest-materiality recent determinations (bounded,
    cost-safe on subscription models), convene 2-3 INDEPENDENTLY-assembled panels via committees.review() on
    the same subject and compare their verdicts. Record agreement/disagreement (inbox kind='ensemble' + a
    determination_outcomes row source='ensemble'); treat disagreement as a contention signal that should stay
    with a human. Read-only except the digest; reuse committees.review; do not edit committees.py.

- id: cx-determination-drift
  title: Determination drift monitor — replay a sample, alert on flipped outcomes
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_determination_drift.py` exits 0
  prompt: |
    Add runner/cx_determination_drift.py with run(): periodically replay a small sample of older
    determinations on today's evidence (reuse committees.replay_determination) and open an inbox alert
    (kind='drift') when a ruling has flipped (recommendation changed or consensus moved >=0.1), so stale
    determinations don't silently keep governing behavior. Bounded to a few per run. Read-only except the
    alert; do not edit committees.py.

- id: cx-evidence-freshness
  title: Evidence-freshness scoring — flag determinations decided on stale data
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_evidence_freshness.py` exits 0
  prompt: |
    Add runner/cx_evidence_freshness.py with run(): score each recent determination by how current its
    grounding was (age of the app_revenue/merge signals + whether external evidence was available at decision
    time) and flag "decided on stale data" candidates for re-review (inbox kind='stale_evidence'). Pairs with
    the existing external-evidence layer in committees.py (_external_evidence). Read-only except the digest;
    no schema change; do not edit committees.py.

- id: cx-reviewer-trust-tile
  title: Reviewer trust digest — calibration report card as an inbox tile
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_reviewer_trust_tile.py` exits 0
  prompt: |
    Add runner/cx_reviewer_trust_tile.py with run(): summarize committee_scoreboard (accuracy + Brier by
    expert domain) plus the count of dissent_vindicated determinations into a concise inbox digest
    (kind='trust_report'), so the owner can see who's actually been right without opening the console. Sort by
    accuracy; call out the worst-calibrated (highest Brier) domains. Read-only except the digest; no schema
    change; do not edit committees.py. CANDIDATE-SHARED: calibration trust report.

- id: cx-proofpack-anchor
  title: Proof-pack external anchoring digest (tamper-evidence beyond self-reference)
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_proofpack_anchor.py` exits 0
  prompt: |
    Add runner/cx_proofpack_anchor.py with run(): compute a rolling anchor over the latest determinations'
    proof_hash chain (e.g. sha256 of the concatenated tail hashes + a timestamp) and stage it for an
    append-only external anchor as an inbox item (kind='proof_anchor') the owner can forward/sign externally,
    so the hash chain is externally timestamped, not only self-referential. Do NOT send anything itself (no
    secrets); just prepare the digest. Read-only except the digest; do not edit committees.py.

- id: cx-auto-adr
  title: Auto-generated Architecture/Decision Records from material determinations
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_auto_adr.py` exits 0
  prompt: |
    Add runner/cx_auto_adr.py with run(): for recent material determinations, write a short ADR markdown file
    under docs/decisions/ADR-<date>-<slug>.md capturing the decision, the contributors/factions, the
    counter-arguments, and the proof_hash — so the "why" is durable and searchable outside the DB. Idempotent
    (skip if the ADR already exists). Reuse the determination's stored fields; do not edit committees.py; these
    are new doc files so they never conflict with code. CANDIDATE-SHARED: auto-ADR generator.

OPERATOR:
  - cx-proofpack-anchor prepares an external anchor digest but does not send it. To make it a true external
    timestamp, choose a send/anchor channel (e.g. a signed email to the owner, or an object-store append) and
    wire the credential — listed here, never auto-queued.
