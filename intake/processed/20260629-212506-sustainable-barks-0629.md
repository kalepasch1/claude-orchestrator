PROJECT: sustainable-barks

# Repo: /Users/kpasch/Documents/Sustainable_Barks (Nuxt/Vue + Supabase; default branch main).
# HARD COPY RULES from the repo CLAUDE.md — every task MUST obey:
#   - NEVER write "$0 admin fee", "no overhead", or similar (a small admin fee exists, internal).
#   - Use "100% of profits go to shelters" / "Proceeds fund animal rescue shelters" — NOT
#     "your full bundle payment is a tax-deductible charitable contribution".
#   - "Tax-deductible" only as a label. Do NOT add "consult your tax advisor" to selling copy.
#   - Do NOT advertise treats, bandanas, spotlight cards, seasonal toys, gift boxes, "white-glove",
#     or "priority fulfillment" (none are offered).

- id: impact-download-print-pdf
  title: Add a download/print PDF button to the per-hotel impact dashboard
  material: no
  model: haiku
  depends: []
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    A prior session built the shareable per-hotel impact dashboard at pages/impact/[slug].vue
    (toys rescued / dogs welcomed / shelters supported / months active, eco-green design, copy-link
    share). Print styles already exist. Deliverable: add a "Download / Print" action that lets a hotel
    partner save the dashboard as a PDF (use window.print() against the existing print stylesheet, or a
    lightweight client-side html-to-pdf if one is already a dependency — do NOT add heavy new deps).
    Button uses the eco-green tokens + AppIcon, has an accessible label, and is reduced-motion safe.
    Copy must follow the repo CLAUDE.md rules (profits→shelters; no admin-fee mention; no forbidden
    features). After editing, grep your changes for forbidden phrases ("$0", "no overhead",
    "white-glove", "priority fulfillment", "consult your tax", "full bundle payment is a tax-deductible")
    and confirm NONE are present. Verify `npx vue-tsc --noEmit` exits 0.

- id: hotel-shelter-attribution-dogs
  title: Scope the "meet the dogs" strip to the hotel's funded shelters
  material: yes
  model: sonnet
  depends: []
  proof: `npx vue-tsc --noEmit` exits 0 AND grep of changed files for forbidden phrases returns none
  prompt: |
    On pages/impact/[slug].vue the "meet the dogs you helped" strip currently renders generic adoptable
    dogs via PetfinderWidget. Make it real: tie the strip to the specific shelters a hotel's stays
    funded. Steps: add an idempotent migration under supabase/migrations/ for a hotel→shelter attribution
    mapping (e.g. hotel_shelter_attribution { hotel_slug, shelter_name, shelter_petfinder_id?, ... } —
    follow the existing hotel_impact migration conventions and RLS), extend server/api/hotel-impact/[slug]
    (or add a sibling endpoint) to return the attributed shelters fail-soft, and scope the dogs strip to
    those shelters' Petfinder org(s). If PETFINDER_API_KEY is absent, fail soft to the current generic
    behavior with a clean caption — do NOT fabricate adoptable-dog data. Copy follows CLAUDE.md
    (profits→shelters language; no admin-fee mention; no forbidden features); grep changed files for the
    forbidden phrases and confirm none. MATERIAL: schema migration — produce green branch, human approves.

OPERATOR:
  - Add PETFINDER_API_KEY + PETFINDER_SECRET to .env and Vercel env vars to enable live dog lookups
    (per the repo CLAUDE.md "Pending before production").
  - Apply the hotel_shelter_attribution migration in the Sustainable Barks Supabase after approval, and
    seed attribution rows for real hotel partners.
