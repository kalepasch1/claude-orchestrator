PROJECT: apparently

# Innovation Hive — remaining wirings + improvement backlog.
# Full context: apparently/CLAUDE_CODE_PROMPT_HIVE_HANDOFF_2026-06-28.md and
# IMPLEMENTATION_LOG.md entries "Innovation Hive — Phase 1/2/3".
# Engines already built + unit-verified (do NOT rebuild): server/engines/hive/*,
# server/engines/gaming-enablement/{sweeps-regime-classifier,support-entity-exposure}.ts,
# migrations 502_hive_innovation_layer.sql + 503_hive_phase2.sql (written, NOT deployed).
# Conventions: callClaudeStructured + selectModel (never hardcode model strings),
# useTypedServiceClient/useServiceSupabase, logger (no console.log), idempotent migrations,
# pure cores unit-testable. Keep tests/engines/hive.test.ts green.

- id: hive-types-regen
  title: Regenerate Supabase types and de-`any` the 7 new Hive tables
  material: no
  model: haiku
  depends: []
  proof: "`npm run gen:types` exits 0 AND `grep -RnE \"reg_facts|hive_scout_sources|hive_vertical_candidates|support_entity_exposures|sweeps_operator_classifications|shared_artifact_ledger|hive_vertical_autonomy\" types/database.types.ts` returns matches"
  prompt: |
    Run `npm run gen:types` to regenerate types/database.types.ts so the 7 Hive tables
    (reg_facts, hive_scout_sources, hive_vertical_candidates, support_entity_exposures,
    sweeps_operator_classifications, shared_artifact_ledger, hive_vertical_autonomy) are typed.
    NOTE: this assumes migrations 502+503 are already applied to the DB the generator points at
    (that deploy is an OPERATOR step below). If the tables are not yet in the target DB, STOP and
    report that this task is blocked on the migration deploy. Do not invent table types by hand.
    Where the generated Database type now covers these tables, remove the `as any` casts in
    server/engines/hive/*.ts that are no longer needed — but only where tsc stays clean.

- id: hive-test-suite-green
  title: Run the real Hive test suite in CI and fix any environment-only breakage
  material: no
  model: sonnet
  depends: []
  proof: "`npx vitest run tests/engines/hive.test.ts` exits 0 with all cases passing"
  prompt: |
    The Hive engines were verified by standalone Node mirrors only (the build sandbox had a
    mac-built node_modules with a rollup/vitest native-binary mismatch). Run the committed suite
    `npx vitest run tests/engines/hive.test.ts` in a real environment. It exercises the pure cores:
    factContentHash, classifyRegimeDeterministic, computeExposures, detectArbitrage,
    assessmentsToFire, counterArbitrage, rankFollowers, shouldDraft, candidateToConfig,
    tierFromMetrics, computeReuse. Fix only genuine breakage (import paths, alias config); do NOT
    weaken assertions. Ensure the suite is picked up by the repo's normal `npm test`.

- id: hive-verifier-promotion
  title: Wire RLO/citation verification to promote reg_facts unverified→verified
  material: no
  model: sonnet
  depends: [hive-types-regen]
  proof: "`npx vitest run tests/engines/hive/verifier-promotion.test.ts` exits 0 (new test: a fact confirmed against a primary source calls markFactVerified and flips verified=true)"
  prompt: |
    Close the Scout→Cartographer→Verifier loop. reg_facts land verified=false
    (server/engines/hive/reg-fact-store.ts exports markFactVerified(client, factId, confidence)).
    Wire the existing primary-source citation verification (server/utils/citation-verifier.ts +
    courtlistener.ts; server/engines/citation-verification-runner.ts) so that when a fact's source
    statute/order/citation is confirmed, markFactVerified is called with the verification confidence.
    Add a small orchestration function server/engines/hive/fact-verifier.ts:
    verifyPendingFacts(client, {limit}) that pulls current unverified reg_facts, runs them through
    the verifier, and promotes the confirmed ones. Add a unit test with a mocked verifier +
    mocked supabase confirming the promotion path. Do not auto-verify on weak/secondary sources.

- id: hive-arbitrage-enforcement-hook
  title: Fan out arbitrage + counter-arbitrage on enacted/effective/enforced gaming facts
  material: no
  model: sonnet
  depends: [hive-types-regen]
  proof: "`npx vitest run tests/engines/hive/arbitrage-hook.test.ts` exits 0 (a lifecycle_advance to enacted/effective/enforced for a gaming prohibition triggers detectArbitrage + counterArbitrage and returns the assessmentsToFire list)"
  prompt: |
    When processScoutHit/recordFact returns a FactChange with changeType 'lifecycle_advance' (or
    'created') for a gaming prohibition fact whose lifecycle_stage is in {enacted, effective,
    enforced}, call detectArbitrage(...) + counterArbitrage(...) from
    server/engines/hive/regime-arbitrage.ts and (a) compute assessmentsToFire, (b) emit a
    coordination event (reuse server/engines/coordination/flows.ts Flow 1 "regulatory ban detected"
    pattern / event-publisher.ts). Add server/engines/hive/arbitrage-dispatch.ts with a pure
    decision function (testable: given a FactChange + signals, returns the fire list + memo) plus the
    side-effecting emit. Do NOT execute any client-facing action — emit events + persist only.

- id: hive-support-entity-relationship-source
  title: Connect support-entity exposure to the real org↔vendor↔operator relationship graph
  material: no
  model: sonnet
  depends: [hive-types-regen]
  proof: "`npx vitest run tests/engines/hive/support-entity-wiring.test.ts` exits 0 (given a support-entity reg_fact, the wiring enumerates SupportRelationship[] from the relationship source and persists support_entity_exposures rows)"
  prompt: |
    server/engines/gaming-enablement/support-entity-exposure.ts has propagateSupportEntityRule that
    takes SupportRelationship[] as a data seam. Build server/engines/gaming-enablement/
    support-entity-wiring.ts that derives SupportRelationship[] from the platform's real data
    (server/engines/stakeholder-graph.ts / entity-resolution.ts / CRM crm-* / engagement data),
    then calls propagateSupportEntityRule on a support-entity reg_fact and persists exposures.
    Exposures must land status='flagged' (human-confirmed, never auto-finalized). Add a unit test
    with a mocked relationship source + mocked supabase. Keep computeExposures pure/unchanged.

- id: hive-shared-artifact-writes
  title: Record foundation artifacts to the dedup ledger from the document/onboarding pipeline
  material: no
  model: sonnet
  depends: [hive-types-regen]
  proof: "`npx vitest run tests/engines/hive/shared-artifact-writes.test.ts` exits 0 (producing a foundation doc calls recordSharedArtifact with the right artifactType + validUntil; planArtifactReuse returns reuse/refresh/produce)"
  prompt: |
    Wire server/engines/hive/shared-artifacts.ts into the real document/onboarding flow. When a
    foundation artifact is produced (control_person_disclosure, background_check, audited_financials,
    surety_bond, fingerprints, formation_doc, personal_financial_statement) — see
    document-collection-bot.ts / document-classifier.ts / autofill-pipeline.ts — call
    recordSharedArtifact(client,{orgId,artifactType,validUntil,sourceEngagementId}). At the start of
    a new vertical's onboarding call planArtifactReuse(client, orgId, required) and return the
    reuse/refresh/produce plan for the intake UI. Add a unit test (mocked supabase). computeReuse
    stays pure.

- id: hive-autonomy-consumption
  title: Consume hive_vertical_autonomy.trust_tier in the autopilot/Trust-Dial gate
  material: yes
  model: sonnet
  depends: [hive-types-regen]
  proof: "`npx vitest run tests/engines/hive/autonomy-gate.test.ts` exits 0 (an action in a counsel_only vertical is gated to human; auto_pilot allows; sweepstakes_casino/nfa_cftc never exceed co_pilot)"
  prompt: |
    Make autonomy track fact quality. computeVerticalAutonomy (server/engines/hive/
    autonomy-coupling.ts) writes hive_vertical_autonomy.trust_tier per vertical. Read that tier in
    the autopilot gate (server/engines/autopilot/* and compliance-autopilot.ts) so a vertical's
    automation level = its tier (counsel_only → human-gate, co_pilot → propose+approve, auto_pilot →
    auto within existing caps). Honor the co_pilot cap on sweepstakes_casino/nfa_cftc. MATERIAL: this
    changes what may execute without a human, so default-closed (missing/unknown tier → counsel_only)
    and add a unit test proving the gating. Do not loosen any existing guardrail.

- id: hive-regime-classify-at-intake
  title: Classify sweepstakes engagements Regime A vs B at intake and route
  material: no
  model: sonnet
  depends: [hive-types-regen]
  proof: "`npx vitest run tests/engines/hive/intake-regime.test.ts` exits 0 (a dual-currency redeemable operator is tagged regime_b_sweeps_casino, persisted, routed to the sweepstakes_casino vertical + human-review queue; a one-off promo → regime_a → sweepstakes)"
  prompt: |
    Call classifySweepsRegime (server/engines/gaming-enablement/sweeps-regime-classifier.ts) during
    gaming/promo intake (server/engines/promo-ai-intake.ts / client-intake-intelligence.ts). Persist
    to sweeps_operator_classifications and route: regime_a_promotional → vertical 'sweepstakes';
    regime_b_sweeps_casino → vertical 'sweepstakes_casino' + human-review queue (needs_human_review
    is always true for B). Add a unit test (deterministic gate covers it; mock supabase). Do not auto-
    advance a Regime-B engagement past review.

- id: hive-kg-modeled-after-seed
  title: Seed/derive knowledge-graph modeled_after jurisdiction edges for diffusion
  material: no
  model: sonnet
  depends: [hive-types-regen]
  proof: "`npx vitest run tests/engines/hive/diffusion-edges.test.ts` exits 0 (after seeding, forecastSubjectDiffusion returns a non-empty ranked follower list for a subject adopted in >=2 states)"
  prompt: |
    regime-diffusion.ts reads getModeledAfter from knowledge-graph/graph-builder.ts and returns
    empty without edges. Extend seedInitialLinks (graph-builder.ts) with jurisdiction→jurisdiction
    modeled_after edges for the gaming/sweeps states, and add a deriveModeledAfterFromAdoption(client)
    helper that infers candidate edges from observed adoption SEQUENCES in reg_facts (earlier adopter
    → later adopter on the same subject, strength by recency/similarity). Add a unit test that seeds a
    few edges + facts and asserts forecastSubjectDiffusion is non-empty and ranked. Edges are
    advisory; mark derived edges in metadata.

- id: hive-self-discovery-review-gate
  title: Add a validation/dedup/review gate for self-discovered scout sources
  material: no
  model: haiku
  depends: [hive-types-regen]
  proof: "`npx vitest run tests/engines/hive/self-discovery-gate.test.ts` exits 0 (junk/dup/non-resolvable authorities are rejected or queued for review, not auto-activated)"
  prompt: |
    Scout self-discovery (scout-runner.ts) auto-enrolls referencedAuthorities as hive_scout_sources
    with discovered_by='hive'. Add a pure validateDiscoveredSource(source, existing[]) in
    scout-registry.ts: reject malformed/empty URLs, dedup against existing natural key, and set
    is_active=false (pending review) for non-.gov / low-confidence authorities so they don't pollute
    the active set. Wire scout-runner to use it. Add a unit test covering accept / dedup / junk /
    needs-review. Surface needs-review sources for the ops dashboard (task hive-ops-dashboards).

- id: hive-bots-and-assessment
  title: Register Hive coordination bots + add assessHive() self-improvement dimension
  material: no
  model: sonnet
  depends: [hive-types-regen]
  proof: "`npx vitest run tests/engines/hive/assess-hive.test.ts` exits 0 (assessHive returns scout-coverage %, fact verified-ratio, candidate→graduation rate, stale-source count and surfaces criticals)"
  prompt: |
    Register the Hive engines as bots in the coordination layer (server/engines/coordination/*,
    bot registry) and add an assessHive() dimension to the self-improvement assessment engine
    (server/engines/self-improvement/* / assessment-engine equivalent) computing: scout coverage %
    (from coverageReport), reg_facts verified-ratio, candidate→graduation conversion, count of stale/
    failed scout sources. Wire it into the full assessment run. Add a unit test with mocked inputs.

- id: hive-candidate-graduation-ui
  title: Operator one-click "graduate vertical" action for drafted candidates
  material: yes
  model: sonnet
  depends: [hive-types-regen]
  proof: "`npx vitest run tests/api/hive/graduate-candidate.test.ts` exits 0 (approving a 'drafted' candidate calls registerVertical with the stored config and flips status to 'graduated'; non-approved candidates are untouched)"
  prompt: |
    draftReadyCandidates (server/engines/hive/vertical-spawner.ts) stores a NewVerticalConfig +
    playbook in hive_vertical_candidates.drafted_config (status 'drafted'). Build the operator action:
    an authed API route server/api/hive/graduate-candidate.post.ts that, on explicit human approval,
    calls registerVertical(config) (the material step) and flips status to 'graduated'. MATERIAL:
    registering a new live vertical — require auth + explicit approval, never auto-graduate. Add a
    test (mocked registry + supabase). A minimal ops list view of candidates can live in
    hive-ops-dashboards.

- id: hive-premortem-memo
  title: Pre-mortem exposure memo generator (composes all Hive engines)
  material: yes
  model: opus
  depends: [hive-arbitrage-enforcement-hook, hive-support-entity-relationship-source, hive-kg-modeled-after-seed]
  proof: "`npx vitest run tests/engines/hive/premortem-memo.test.ts` exits 0 (given a gaming operator + jurisdictions, buildPremortemMemo returns a structured memo with regime, per-state status map, support-entity exposure, arbitrage paths, counter-arbitrage residuals, and diffusion forecast)"
  prompt: |
    Build server/engines/hive/premortem-memo.ts: buildPremortemMemo(client, {operator, signals,
    jurisdictions}) that COMPOSES the existing engines — classifySweepsRegime + per-state status from
    reg_facts + computeExposures + detectArbitrage + counterArbitrage + forecastSubjectDiffusion —
    into one structured deliverable ("your exposure and your runway"). Render via the existing
    document-assembly path (do not invent a renderer). MATERIAL: it's a client-facing legal-adjacent
    deliverable — issuance stays human-gated (return a draft + needsApproval flag; do not send). Pure
    composition function must be unit-testable with mocked sub-engines. Add the test.

- id: hive-lifecycle-impact-propagation
  title: Lifecycle-advance → client/opinion/license impact fan-out
  material: yes
  model: opus
  depends: [hive-verifier-promotion]
  proof: "`npx vitest run tests/engines/hive/impact-propagation.test.ts` exits 0 (a verified lifecycle_advance fan-outs to every engagement/opinion/license that cites the fact's subject and creates a remediation/supplement task, human-gated)"
  prompt: |
    Build the recurring-revenue engine. On a VERIFIED reg_facts lifecycle_advance, fan out to every
    client/opinion/license/filing that depends on the changed fact and create a remediation/supplement
    task. Build (1) a dependency index mapping engagements/opinions/licenses → reg_facts.subject they
    rely on, and (2) server/engines/hive/impact-propagation.ts that, given a FactChange, resolves
    affected entities and enqueues remediation via regulatory-change-propagator.ts + outbox. MATERIAL:
    it touches client deliverables/filings — generated supplements/remediations are proposals, human-
    approved before send. Add a unit test (mocked index + supabase). Pure resolver function separated
    from side effects.

- id: hive-regulatory-debt-score
  title: Regulatory-debt score per client
  material: no
  model: sonnet
  depends: [hive-shared-artifact-writes]
  proof: "`npx vitest run tests/engines/hive/regulatory-debt.test.ts` exits 0 (computeRegulatoryDebt returns a 0-100 score rising with stale artifacts, lifecycle-advancing facts touching the org, and uncovered jurisdictions)"
  prompt: |
    Build server/engines/hive/regulatory-debt.ts: computeRegulatoryDebt(client, orgId) → a single
    0-100 score (+ component breakdown) from: stale shared_artifact_ledger entries, count of
    lifecycle-advancing reg_facts touching the org's verticals/jurisdictions, and uncovered
    jurisdictions (coverageReport gaps for the org's footprint). Pure scoring core
    computeDebtScore(inputs) unit-tested; thin DB wrapper. Surface on the client/engagement dashboard
    (data only; UI in hive-ops-dashboards).

- id: hive-enforcement-velocity-index
  title: Enforcement-velocity index that auto-tunes scout cadence
  material: no
  model: sonnet
  depends: [hive-types-regen]
  proof: "`npx vitest run tests/engines/hive/enforcement-velocity.test.ts` exits 0 (a domain×state with accelerating fact arrivals yields a higher velocity score and a lowered cadence_hours write)"
  prompt: |
    Build server/engines/hive/enforcement-velocity.ts: from reg_facts arrival timestamps per
    (domain × jurisdiction), compute an acceleration/velocity score (pure: computeVelocity(arrivals)),
    then write back a lowered hive_scout_sources.cadence_hours (and raise client-alert urgency) where
    the regulatory temperature is spiking — so the Hive reallocates its own attention. Pure core
    unit-tested; thin DB write wrapper. Respect a floor (never below 6h) and a ceiling.

- id: hive-coverage-radar-report
  title: Productize coverageReport as a "Regulatory Radar" blank-cell report
  material: no
  model: haiku
  depends: [hive-types-regen]
  proof: "`npx vitest run tests/engines/hive/coverage-radar.test.ts` exits 0 (buildCoverageRadar returns watched vs blank (domain × regime_layer) cells + a prioritized list of blanks to enroll)"
  prompt: |
    Build server/engines/hive/coverage-radar.ts on top of scout-registry.coverageReport(): a pure
    buildCoverageRadar(cells, domains, layers) that returns the full (domain × regime_layer) matrix
    marking blank cells, plus a prioritized enroll-next list (blank high-priority cells first). This
    is both a saleable risk report and the Hive's own build queue. Unit-test the matrix + prioritization.
    Optionally auto-enroll obvious blanks via a follow-up (do not auto-enroll without the review gate
    from hive-self-discovery-review-gate).

- id: hive-filing-ready-gate
  title: Filing-ready confidence gate before the submission boundary
  material: yes
  model: sonnet
  depends: [hive-verifier-promotion, hive-autonomy-consumption]
  proof: "`npx vitest run tests/engines/hive/filing-ready-gate.test.ts` exits 0 (a packet scored below threshold against verified reg_facts is blocked from submission; above-threshold passes)"
  prompt: |
    Build server/engines/hive/filing-ready-gate.ts: immediately before any regulatory submission,
    score the assembled packet against the VERIFIED reg_facts for that jurisdiction+vertical
    (completeness, fee/bond/deadline match, no superseded reliance) and BLOCK below threshold (reuse
    the draft-quality-floor.ts pattern). MATERIAL: it gates real filings — default-closed (no verified
    facts → block + human review). Pure scoring core unit-tested. Wire it as the last check in the
    submission/autopilot path; it must HALT, never submit, on failure.

- id: hive-ops-dashboards
  title: Ops surfacing for candidates, autonomy, exposures, diffusion, coverage, debt
  material: no
  model: sonnet
  depends: [hive-candidate-graduation-ui, hive-regulatory-debt-score, hive-coverage-radar-report]
  proof: "`npx vue-tsc --noEmit` exits 0 AND the new ops page(s)/HTML render the Hive tables (hive_vertical_candidates, hive_vertical_autonomy, support_entity_exposures, reg_facts diffusion, coverage radar, regulatory-debt) without referencing non-existent columns"
  prompt: |
    Surface the Hive in the ops UI, matching the existing ops-*.html / app dashboards pattern. Show:
    vertical candidates (with one-click graduate from hive-candidate-graduation-ui), per-vertical
    autonomy tiers, support-entity exposures (flagged/critical), diffusion forecasts, the coverage
    radar (blank cells), and per-client regulatory-debt. Read-only views over the new tables; reuse
    existing dashboard scaffolding (e.g. ops-gaming-dashboard.html / ops-coverage-score-dashboard.html).
    No new write paths. Prove with vue-tsc clean (and/or the repo's HTML lint) and column-accurate reads.

OPERATOR:
  - Deploy migrations 502_hive_innovation_layer.sql + 503_hive_phase2.sql to the prod Supabase project after name-checking every table/column against the live DB (per CLAUDE.md migration rule). This blocks hive-types-regen and everything depending on it. Self-cleaning round-trip recommended.
  - Provision fetch adapters/API keys for the enforcement/legislative scouts (news-search provider + LegiScan or equivalent) so regulatory-scanner.runScan() for monitor_type 'news_search'/'website_scrape' actually returns content. Without keys the loop runs but discovers nothing.
  - Confirm CRON_SECRET is set in prod and that tick-daily dispatch reaches /api/cron/hive-discovery-tick (consider also tick-hourly for the fast enforcement/legislative layers).
  - Legal sign-off before any Regime-B (sweepstakes_casino) client deliverable is issued and before the filing-ready gate is allowed to pass any real submission; confirm the co_pilot autonomy cap on sweepstakes_casino/nfa_cftc is acceptable.
  - Out-of-scope here (separate AI-cost workstream): tier-down-by-confidence on the RLO/opinion path, persistent jurisdiction-corpus prompt cache, 50-state bot batching, verified-reference citation memory — flag if you want these queued as their own intake.
