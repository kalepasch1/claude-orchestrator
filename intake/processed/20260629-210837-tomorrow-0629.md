PROJECT: tomorrow

# Corpus / query-don't-scrape for the derivatives (ISDA) app. Handoffs: corpus/README.md, corpus/CROSS_PROJECT_PLAN.md.
# Already DONE + deployed to Supabase edetxpcoaiqlqrwyzltw (do NOT redo):
#   corpus_documents/clauses/clause_embeddings/citations/query_log/gaps/source_state tables;
#   fns search_corpus_authority, corpus_coverage, log_corpus_query, corpus_completeness, increment_corpus_fetch_count;
#   harvesters corpus/scripts/{harvest-edgar,harvest-cdm,harvest-courtlistener,harvest-govinfo,harvest-regulations-gov,harvest-lawfirm-guides,extract-citations,backfill-bodies}.mjs + eval/;
#   Prisma models CorpusDocument/CorpusClause/CorpusClauseEmbedding; .github/workflows/corpus-harvest.yml.
# NOTE: the research ENDPOINT was only built in Apparently; Tomorrow has the SQL fns but no /api/research/* route yet.

- id: research-endpoint
  title: Build /api/research/query + /api/research/coverage in Tomorrow
  material: no
  model: sonnet
  depends: []
  proof: `npm run typecheck` exits 0
  prompt: |
    Port the research endpoints from Apparently (server/api/research/query.post.ts + coverage.get.ts) into
    Tomorrow, adapting to Tomorrow's auth + Supabase client conventions (use requireAuth + the typed service
    client used elsewhere in server/api). The SQL fns search_corpus_authority / corpus_coverage /
    log_corpus_query already exist in Tomorrow's DB. Return cited, coverage-aware results; log every query via
    log_corpus_query; flag `quotable` (reference_only never verbatim). Keep it read-only + fail-soft.

- id: seed-source-state
  title: Seed corpus_source_state rows for Tomorrow (currently empty)
  material: yes
  model: haiku
  depends: []
  proof: `test -f prisma/migrations/*corpus_source_state_seed*/migration.sql || ls corpus/db/*source_state_seed*.sql`
  prompt: |
    Tomorrow's corpus_source_state has 0 rows. Write a seed (migration or corpus/db/ SQL file) inserting one
    row per source with earliest_target encoding historical depth: edgar (1933), cdm (open spec), courtlistener
    (1936, CEA-forward case law), govinfo (1936 CEA + CFR Title 17 + U.S. Code), regulations_gov, fedreg
    (1994). Mirror the Apparently corpus_source_state seed shape. Material (prod write) — human applies.

- id: wire-warroom-research
  title: Wire /api/research/query into the war-room negotiation copilot + clause drafting
  material: no
  model: sonnet
  depends: [research-endpoint]
  proof: `npm run typecheck` exits 0
  prompt: |
    Add a corpus-authority enrichment to the war-room copilot + clause-drafting context, mirroring how
    Apparently's server/engines/legal-bots/corpus-authority.ts feeds buildLiveRegulatoryContext. Create a
    fail-soft helper that calls search_corpus_authority (derivatives/CFTC terms for the active matter) and
    appends cited authority into the copilot/drafting context. Additive + advisory only — do NOT change the
    negotiation logic or the §2(h)(7) posture. typecheck must stay at 0 errors.

- id: commit-corpus-migrations
  title: Commit MCP-applied corpus migrations as repo Prisma migrations (parity)
  material: yes
  model: haiku
  depends: []
  proof: `npm run lint:migrations` exits 0
  prompt: |
    The corpus schema (corpus_legal_ingestion, corpus_fetch_count_usage_ranking, corpus_v2_graph_log_gaps,
    plus search/coverage/source_state fns) was applied to Tomorrow prod via MCP but isn't in
    prisma/migrations/. Author matching idempotent migration dirs (camelCase-quoted cols per the repo's
    "Migration Authoring — name-check before landing" rule; these are NEW tables, no FK into the drifted set).
    Run `npm run lint:migrations` — must pass. Already live in prod, so this is parity only. Material.

- id: eu-uk-aus-harvesters
  title: EU/UK/AUS authority harvesters behind the jurisdiction gate
  material: no
  model: opus
  depends: []
  proof: `node --check corpus/scripts/harvest-eurlex.mjs && node --check corpus/scripts/harvest-uk-legislation.mjs && node --check corpus/scripts/harvest-au-frl.mjs`
  prompt: |
    Add three API harvesters mirroring the existing corpus/scripts/harvest-*.mjs pattern (shared lib/common.mjs,
    fail-soft, makeDocument): harvest-eurlex.mjs (EUR-Lex REST — EMIR/ESMA), harvest-uk-legislation.mjs
    (legislation.gov.uk API — FCA/BoE-relevant), harvest-au-frl.mjs (AU Federal Register of Legislation API —
    ASIC). Tag counterparty_segment='regulatory' + product_types per jurisdiction; gate execution behind the
    existing JURISDICTION_GATE_MULTI_JURISDICTION_ENABLED flag (only run when set). Wire into run-all.mjs +
    corpus-harvest.yml. Proof = node --check on all three; live runs are operator-gated.

OPERATOR:
  - Add GOVINFO_API_KEY + REGULATIONS_GOV_API_KEY (+ COURTLISTENER_API_TOKEN if missing) to Tomorrow CI secrets, then run corpus-harvest.yml for the completeness walk.
  - Set JURISDICTION_GATE_MULTI_JURISDICTION_ENABLED + counsel sign-off before running the EU/UK/AUS harvesters live.
  - Approve prod apply of the seed-source-state + commit-corpus-migrations parity migrations.
