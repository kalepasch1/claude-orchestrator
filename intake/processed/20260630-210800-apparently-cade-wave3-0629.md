PROJECT: apparently

# CADE wave 3 (categorical correctness + cost + data-moat levers) — app-side features on the
# reference legal vertical, around @darwin/kernel/cade. Prereqs: waves 1–2 (cade-determine-legal,
# cade-proof-store, cade-invoker, cade-calibration-writeback, cade-advocacy-restyle, cade-meta-redteam,
# cade-outcome-benchmark). Spec: claude-orchestrator/CADE_IMPLEMENTATION_HANDOFF.md.
# Repo rules: AI via server/utils/ai.ts; AI_MODELS constants; typed Supabase; RLS; log AI calls.

- id: cade-formal-verification
  title: Formal-verification layer — machine-check contract/obligation logic
  material: no
  model: opus
  depends: [cade-determine-legal]
  proof: `npx vitest run server/utils/cade/__tests__/formal-verify.test.ts` exits 0
  prompt: |
    Build server/utils/cade/formalVerify.ts: compile clauses/obligations into formal logic
    (temporal/deontic) and machine-check with an embeddable SMT/solver (no native deps — use a
    JS/WASM SMT or a bounded custom checker) for contradictions, unsatisfiable obligations, circular
    dependencies, and unintended loopholes. Attach the machine proof (or counterexample) to the
    determination proof record. Test: a seeded contradictory clause set is flagged with a
    counterexample; a consistent set passes. See chat improvement #1.

- id: cade-kv-prefix-cache
  title: Shared-prefix KV cache + batching across the panel
  material: no
  model: sonnet
  depends: [cade-invoker]
  proof: `npx vitest run server/utils/cade/__tests__/prefix-cache.test.ts` exits 0
  prompt: |
    Optimize the invoker so all personas on one issue share a single grounding/corpus prefix served
    through prompt-cache + batched calls (the breadth pass is maximally redundant at the prefix).
    Record cache-hit rate. Test (mocked client): N personas on one issue issue 1 prefix build + N
    completions, and a cache-hit metric is emitted. See chat #4.

- id: cade-voi-intake
  title: Value-of-information active intake (ask the one missing fact)
  material: no
  model: sonnet
  depends: [cade-determine-legal]
  proof: `npx vitest run server/utils/cade/__tests__/voi.test.ts` exits 0
  prompt: |
    Build server/utils/cade/voi.ts: before a full determination, compute the single missing fact whose
    answer would most collapse uncertainty (expected-information-gain over the cheap pass) and surface
    it as a question; if answered, re-route through the difficulty router. Reuse Pareto's VOI pattern.
    Test: a high-uncertainty issue returns a ranked missing-fact question; a settled one returns none.
    See chat #5.

- id: cade-causal-kg
  title: Causal + knowledge-graph grounding (structure, not just text)
  material: yes
  model: opus
  depends: [cade-determine-legal]
  proof: `npm run gen:types && npx vitest run server/utils/cade/__tests__/causal-kg.test.ts` exits 0
  prompt: |
    Add a cade_obligation_graph store (entities, obligations, triggers, cascades; RLS) built from the
    document; let personas reason over the graph for counterfactuals ("if trigger X fires, which
    obligations cascade?"). Add a migration + typed access. Test: a trigger query returns the correct
    obligation cascade on a sample instrument. See chat #2.

- id: cade-federated-calibration
  title: Privacy-preserved cross-customer federated calibration
  material: yes
  model: opus
  depends: [cade-calibration-writeback]
  proof: `npx vitest run server/utils/cade/__tests__/federated-calibration.test.ts` exits 0
  prompt: |
    Use @darwin/kernel federated primitives (k-anonymity, ε-DP) so every customer's outcomes improve
    the shared reliability priors without exposing data (k>=3, ε budgeted). Test: aggregation suppresses
    sub-k cohorts and adds bounded noise; reliability updates are data-private. See chat #9.

- id: cade-multi-audience-render
  title: One determination → exec / counsel / regulator / layperson renders
  material: no
  model: sonnet
  depends: [cade-advocacy-restyle]
  proof: `npx vitest run server/utils/cade/__tests__/multi-audience.test.ts` exits 0
  prompt: |
    Extend the Advocacy Guild to render one sealed determination into four audience variants (exec
    summary, counsel memo, regulator filing, plain-language client note), each semantic-diff guarded
    and all tracing to the same proof id. Test: four renders preserve substance + share the proof id.
    See chat #12.

- id: cade-synthetic-hardcases
  title: Synthetic hard-case generation to grow the golden set
  material: no
  model: sonnet
  depends: [cade-outcome-benchmark]
  proof: `npx vitest run server/utils/cade/__tests__/synthetic-cases.test.ts` exits 0
  prompt: |
    Generate adversarial hypotheticals (edge cases, novel fact patterns) to expand the golden set
    beyond real history; dedupe against existing cases; tag as synthetic. Test: a generation run adds
    novel, non-duplicate golden cases the suite picks up. See chat #14.

- id: cade-regulator-proof-console
  title: Proof-of-process regulator/examiner console (regtech surface)
  material: no
  model: sonnet
  depends: [cade-proof-store]
  proof: `npx vitest run server/api/cade/__tests__/regulator-console.test.ts` exits 0
  prompt: |
    Build a read-only console + API that lets an examiner verify a firm's determinations were produced
    by the adversarial, documented, signed CADE process (verify proof, view panel/red-team/cert, audit
    trail). Scoped, no privileged data beyond what the firm shares. Test: a sealed determination
    verifies through the console endpoint; tamper is rejected. See chat #8.

- id: cade-adversarial-bounty
  title: Crowdsourced adversarial bounty on published determinations
  material: yes
  model: sonnet
  depends: [cade-meta-redteam]
  proof: `npx vitest run server/api/cade/__tests__/bounty.test.ts` exits 0
  prompt: |
    Add a submission surface where external experts attack consented/published determinations; valid
    surviving attacks reward the submitter and promote the argument into the library; touches reward
    payouts (material). Test: a valid attack is accepted + queued for reward + library promotion; an
    invalid one is rejected. Reward disbursement is OPERATOR-gated. See chat #10.

OPERATOR:
  - Reward disbursement for the adversarial bounty (payment rail + amounts) is operator-configured; the queue/validation is built, payouts are gated.
  - Federated calibration across customers requires a data-sharing consent/legal review before enabling cross-tenant aggregation.
  - Vertical replication (tax / M&A / patent / audit / medical) remains a roadmap program — not queued here.
  - Pure-kernel lifts (SMT helper, KG primitives, federated calibration math) may later move into @darwin/kernel/cade (kernel = claude-orchestrator, no enum slot) — route as kernel tasks.
