PROJECT: hisanta

# HiSanta — social + adaptive-learning backend, phase 2 (worker/automation layer).
# Context: phase 1 shipped LIVE to Supabase project `santas-secret-workshop` (ref whhfugddqehxxbmwutsw)
# via 10 MCP-applied migrations. Phase-1 objects already exist in prod and are smoke-tested:
#   tables: consent_edges, audit_log, circles, circle_members, spark_issuer_grants, community_acts,
#     community_act_completions, spark_donations, peer_teach_sessions, teach_transfer_items,
#     research_panel_enrollment, research_feedback_queue, research_partners, learning_item_variants,
#     bandit_arms, strategy_registry, wellbeing_settings, family_lessons, lesson_practice
#   functions: app_is_guardian_of, app_can_access_child, app_has_consent, issue_sparks, can_peer_teach,
#     verify_teach_transfer, record_retrieval, recommend_arm, delete_family_archive
#   views: leaderboard_lens, research_aggregate_export, parent_control_center, weekly_learning_report, issuer_activity_feed
# RLS convention: guardian = children.guardian_id/co_guardian_id = auth.uid(); family read via family_circle (status='active').
# Helper fns app_is_guardian_of / app_can_access_child MUST stay EXECUTE-able by anon+authenticated (RLS policies call them).
# Privileged DEFINER fns guard with: `if auth.uid() is not null and <not authorized> then raise`. anon EXECUTE revoked.
# Strategy + data-model + compliance source: outputs/HiSanta_Social_and_Learning_Strategy.docx (Appendix A/B).
# Every DB task adds a pgTAP test under supabase/tests/ and must pass `supabase test db`.
# Agents: first `supabase db reset` to confirm a clean baseline; inspect supabase/migrations + supabase/functions before editing.

- id: capture-live-migrations
  title: Commit the 10 phase-1 migrations as versioned repo files so repo == prod
  material: yes
  model: sonnet
  depends: []
  proof: `supabase db reset` exits 0 AND `supabase db diff --linked --schema public` prints no differences
  prompt: |
    Phase-1 schema was applied directly to the remote DB via MCP and is NOT yet in the repo. Capture
    it as ordered migration files in supabase/migrations/ so a fresh `supabase db reset` reproduces prod
    exactly. Pull authoritative DDL from the linked project, then split into named files matching what
    was applied (one file per logical migration, in this order):
      1 histanta_crosscutting_consent_graph (app_is_guardian_of, app_can_access_child, audit_log,
        consent_edges, app_has_consent)
      2 histanta_pillar1_circles_leaderboards (circles, circle_members, coop_quests added cols,
        leaderboard_lens view)
      3 histanta_pillar2_issuers_acts_donations (spark_issuer_grants, issue_sparks, community_acts,
        community_act_completions, spark_donations)
      4 histanta_pillar3_peer_teaching (child_lesson_mastery added cols mastery_prob/durable,
        peer_teach_sessions, teach_transfer_items, coop_participants role cols, can_peer_teach,
        verify_teach_transfer)
      5 histanta_pillar4_research_panel (data_consent added cols, research_panel_enrollment,
        research_feedback_queue, research_partners, research_aggregate_export view)
      6 histanta_pillar5_adaptive_engine (child_lesson_mastery review cols, learning_item_variants,
        bandit_arms, strategy_registry, wellbeing_settings, record_retrieval, recommend_arm)
      7 histanta_pillar6_family_curriculum (family_lessons, lesson_practice, delete_family_archive)
      8 histanta_parent_control_center_views (parent_control_center, weekly_learning_report,
        issuer_activity_feed)
      9 histanta_harden_definer_functions (DEFINER guards + `revoke execute ... from anon`)
      10 histanta_extend_spark_source_types (spark_ledger source_type CHECK superset incl. help,
        kindness, chore, peer_teach, community_act, donation, effort, teamwork)
    Use `create ... if not exists` / `add column if not exists` so reset is idempotent. Verify parity:
    after `supabase db reset`, `supabase db diff --linked --schema public` must report no drift. Do not
    alter behavior — this is a faithful capture only. This task is the baseline; all others build on it.

- id: immutable-spark-ledger
  title: Make spark_ledger append-only with trigger-derived balances
  material: yes
  model: opus
  depends: [capture-live-migrations]
  proof: `supabase test db` exits 0 (supabase/tests/immutable_ledger_test.sql)
  prompt: |
    spark_ledger is the single source of truth, but children.total_sparks is a hand-maintained counter
    (drift risk observed in phase 1). Make points tamper-evident and self-consistent.
    Steps (new migration supabase/migrations/0011_immutable_spark_ledger.sql):
    - Revoke UPDATE and DELETE on public.spark_ledger from anon, authenticated, and service_role; add a
      BEFORE UPDATE/DELETE trigger that RAISES (append-only). Inserts only.
    - Add a trigger AFTER INSERT on spark_ledger that maintains children.total_sparks atomically, and a
      one-time backfill `update children set total_sparks = (select coalesce(sum(amount),0) from
      spark_ledger where child_id = children.id)` so existing rows reconcile.
    - Update issue_sparks / verify_teach_transfer to STOP manually writing total_sparks (the trigger now
      owns it); keep their ledger inserts + audit_log writes. Preserve DEFINER guards.
    pgTAP supabase/tests/immutable_ledger_test.sql: (a) an UPDATE and a DELETE on spark_ledger both throw;
    (b) inserting a ledger row moves children.total_sparks by exactly the amount; (c) backfill makes
    total_sparks equal the ledger sum for a fixture child.

- id: consent-runtime-gate
  title: Enforce app_has_consent() as a hard precondition in privileged flows
  material: yes
  model: opus
  depends: [capture-live-migrations]
  proof: `supabase test db` exits 0 (supabase/tests/consent_gate_test.sql)
  prompt: |
    Phase 1 records consent in consent_edges but does not block actions when consent is absent. Turn
    consent into a runtime gate.
    Steps (new migration supabase/migrations/0012_consent_runtime_gate.sql):
    - issue_sparks: when category='community_act', require app_has_consent(p_child,'community_acts') else raise.
    - verify_teach_transfer: require app_has_consent(teacher_child,'peer_teach') AND app_has_consent(
      learner_child,'peer_teach') else raise.
    - family_lessons insert path: add a BEFORE INSERT trigger requiring app_has_consent(child_id,'media_capture')
      when media_url is not null.
    - Add a `consent_receipts` table (id, consent_edge_id, action, child_id, created_at) and write a receipt
      row inside each gated function on success (for auditability). RLS: guardian/family read, auth insert.
    Keep all existing DEFINER guards and the `auth.uid() is null ⇒ service path` allowance.
    pgTAP supabase/tests/consent_gate_test.sql: gated calls raise without an active edge and succeed (and
    write a receipt) with one; an expired edge (expires_at in past) is treated as missing.

- id: consent-expiry-job
  title: Scheduled job to auto-expire consent edges + notify guardians
  material: yes
  model: sonnet
  depends: [consent-runtime-gate]
  proof: `deno test -A supabase/functions/consent-expiry/index.test.ts` exits 0
  prompt: |
    Build an edge function supabase/functions/consent-expiry/index.ts that: sets status='expired' on
    consent_edges where expires_at < now() and status='active'; writes an audit_log row per expiry; and
    enqueues a guardian notification into notification_queue ("a permission for <child> has expired").
    Add a migration supabase/migrations/0013_consent_expiry_cron.sql that schedules it nightly via pg_cron
    + pg_net (guard with `create extension if not exists`). Include index.test.ts using a mocked Supabase
    client: rows past expiry flip to 'expired' and produce exactly one audit + one notification each; active
    unexpired rows are untouched. (Deployment + pg_cron enablement is an OPERATOR item.)

- id: bkt-mastery-upgrade
  title: Replace heuristic mastery bump with per-skill Bayesian Knowledge Tracing
  material: yes
  model: opus
  depends: [capture-live-migrations]
  proof: `supabase test db` exits 0 (supabase/tests/bkt_mastery_test.sql)
  prompt: |
    Upgrade record_retrieval from the fixed 0.35 bump to true BKT so mastery_prob reflects real learning.
    Steps (new migration supabase/migrations/0014_bkt_mastery.sql):
    - Add a `skills` table (skill text pk, p_init, p_transit, p_slip, p_guess numeric) with sensible defaults
      (0.2/0.15/0.1/0.2) and seed the skills already present in child_lesson_mastery.
    - Rewrite record_retrieval to apply the standard BKT posterior update using the skill's slip/guess, then
      the transit step, clamped [0.02,0.999]. Keep the expanding spaced-retrieval interval + next_review_at
      logic and the durable rule (>=0.85 AND consecutive_delayed_passes>=3). Keep the DEFINER access guard.
    pgTAP supabase/tests/bkt_mastery_test.sql: a correct retrieval raises posterior toward 1 and a wrong one
    lowers it per BKT (not the old constant); four correct delayed passes set durable=true; mastery_prob stays
    within bounds. Lock the calc with explicit expected numbers for one skill's params.

- id: forgetting-decay-job
  title: Nightly forgetting-curve decay + review enqueue
  material: yes
  model: sonnet
  depends: [bkt-mastery-upgrade]
  proof: `deno test -A supabase/functions/mastery-decay/index.test.ts` exits 0
  prompt: |
    Build supabase/functions/mastery-decay/index.ts: for child_lesson_mastery rows where next_review_at < now(),
    apply exponential decay to mastery_prob (half-life scaled by review_interval_days), set durable=false if it
    drops below 0.85, and enqueue a review item into notification_queue. Migration
    supabase/migrations/0015_mastery_decay_cron.sql schedules it daily (pg_cron + pg_net, create-extension-guarded).
    index.test.ts (mocked client): an overdue high-prob row decays and enqueues one review; a not-yet-due row is
    untouched; a decayed row crossing 0.85 flips durable=false. (Deploy/cron = OPERATOR.)

- id: swarm-distillation-job
  title: Nightly swarm distillation — promote winning bandit arms into strategy_registry
  material: yes
  model: opus
  depends: [capture-live-migrations]
  proof: `deno test -A supabase/functions/swarm-distill/index.test.ts` exits 0
  prompt: |
    Build supabase/functions/swarm-distill/index.ts implementing federated STRATEGY sharing (never raw child
    data). Steps: cluster children into anonymized learner-profile buckets (e.g. age band x dominant-modality x
    struggling-skill) WITHOUT exporting identifiers; for each (cluster, lever) compute the best-performing
    bandit arm by mean reward over a minimum trial count; upsert it into strategy_registry (policy jsonb,
    success_metric, trials, experimental=false). Keep a budget cap on experimental arms (do not let experimental
    rows exceed N per lever). recommend_arm already reads strategy_registry as the cold-start seed — do not change
    its signature. Migration supabase/migrations/0016_swarm_distill_cron.sql schedules nightly. index.test.ts
    (mocked client + fixture arms): a clearly-superior arm is promoted; an arm below the trial floor is not;
    no child_id ever appears in a written strategy_registry row. (Deploy/cron = OPERATOR.)

- id: transfer-item-generator
  title: Auto-generate isomorphic transfer items + schedule delayed delivery
  material: yes
  model: sonnet
  depends: [capture-live-migrations]
  proof: `deno test -A supabase/functions/transfer-item-gen/index.test.ts` exits 0
  prompt: |
    When a peer_teach_session is created, the learner needs a NOVEL, isomorphic transfer item delivered after a
    delay (anti-cheating + true-transfer). Build supabase/functions/transfer-item-gen/index.ts: given a session,
    call the LLM to produce an isomorphic variant of the taught skill (same skill, new numbers/names/context),
    insert a teach_transfer_items row with unlock_after = now()+12–24h and requires_teacher_absent=true, and
    enqueue its delivery via notification_queue at unlock_after. Validate the LLM output shape before insert
    (reject if it restates the original). index.test.ts mocks the LLM + client: a session yields exactly one
    transfer item with unlock_after in the 12–24h window and a queued delivery; malformed LLM output inserts
    nothing and surfaces an error. (LLM API key + deploy = OPERATOR.)

- id: teachback-scoring
  title: Score recorded teach-back explanations (correctness + clarity)
  material: yes
  model: sonnet
  depends: [capture-live-migrations]
  proof: `deno test -A supabase/functions/teachback-score/index.test.ts` exits 0
  prompt: |
    Build supabase/functions/teachback-score/index.ts: given a peer_teach_sessions.teachback_media_url, run
    transcription + an LLM rubric scoring correctness and clarity in [0,1], write teachback_ai_score, and set
    teachback_reusable=true only when score>=0.8 AND a moderation check passes (so it can become a reusable
    micro-lesson with consent). Never auto-publish without app_has_consent(teacher_child,'media_capture').
    index.test.ts mocks transcription/LLM/moderation/client: a high-scoring clean explanation sets score and
    reusable=true; a low score sets score and reusable=false; a moderation flag forces reusable=false regardless.
    (Transcription/LLM keys + deploy = OPERATOR.)

- id: wellbeing-enforcement
  title: Make well-being enforceable — session guard + penalty folded into recommend_arm
  material: yes
  model: sonnet
  depends: [capture-live-migrations]
  proof: `supabase test db` exits 0 (supabase/tests/wellbeing_test.sql)
  prompt: |
    wellbeing_settings is currently advisory. Make the optimizer unable to favor longer sessions.
    Steps (new migration supabase/migrations/0017_wellbeing_enforce.sql):
    - Add session_guard(p_child uuid, p_minutes int) returning text ('continue' | 'wind_down' | 'hard_stop')
      using daily_target_min / hard_cap_min from wellbeing_settings (sensible defaults when absent).
    - Modify recommend_arm to subtract a penalty term lambda_session_penalty * minutes_over_target (read from
      wellbeing_settings; minutes-over from today's game_sessions/recommendation duration) from each arm's mean
      reward, so over-target states down-rank engagement-heavy arms. Keep signature and DEFINER guard.
    pgTAP supabase/tests/wellbeing_test.sql: session_guard returns 'continue' under target, 'wind_down' over
    target, 'hard_stop' past hard cap; with minutes far over target, recommend_arm no longer returns the
    highest-raw-reward arm when that arm is the engagement-maximizing one.

- id: leaderboard-matview
  title: Materialize leaderboard standings for scale
  material: yes
  model: sonnet
  depends: [capture-live-migrations]
  proof: `supabase test db` exits 0 (supabase/tests/leaderboard_matview_test.sql)
  prompt: |
    leaderboard_lens recomputes over the whole spark_ledger on every read. Add a materialized view
    leaderboard_standings (same columns as leaderboard_lens, plus rank_by_improvement) in migration
    supabase/migrations/0018_leaderboard_matview.sql, with a unique index for CONCURRENTLY refresh and a
    refresh_leaderboard_standings() function. Keep leaderboard_lens for live reads; the matview is the
    scale path. pgTAP supabase/tests/leaderboard_matview_test.sql: after seeding a circle + ledger rows and
    refreshing, standings match a direct aggregate, and rank_by_improvement orders by growth-vs-baseline
    (a low-absolute, high-growth child outranks a high-absolute, flat child).

- id: seasonal-reset-job
  title: Automated seasonal reset + baseline snapshot (amnesty)
  material: yes
  model: sonnet
  depends: [leaderboard-matview]
  proof: `deno test -A supabase/functions/season-reset/index.test.ts` exits 0
  prompt: |
    Build supabase/functions/season-reset/index.ts: at season rollover, snapshot each circle_members.baseline_sparks
    to the member's current season spark total and bump circles.season, so every child starts fresh (the amnesty
    mechanic). Then refresh leaderboard_standings. Migration supabase/migrations/0019_season_reset_cron.sql
    schedules it at the season boundary via pg_cron. index.test.ts (mocked client + fixtures): after reset,
    baseline_sparks equals prior season total and season is advanced; improvement resets to ~0. (Deploy/cron = OPERATOR.)

- id: research-export-hardening
  title: Gate + log + privacy-protect the research aggregate export
  material: yes
  model: opus
  depends: [capture-live-migrations]
  proof: `supabase test db` exits 0 (supabase/tests/research_export_test.sql)
  prompt: |
    research_aggregate_export is a directly-readable view. Wrap it so every partner read is consent-scoped,
    contract-capped, logged, and noised. Steps (migration supabase/migrations/0020_research_export_hardening.sql):
    - Add columns to research_partners: allowed_categories text[], allowed_age_bands int[], dp_epsilon numeric.
    - Add function export_research_aggregate(p_partner uuid) SECURITY DEFINER that returns rows ONLY for the
      partner's allowed categories/age-bands, enforces the k>=5 floor, adds calibrated Laplace noise to counts
      (dp_epsilon), writes an audit_log row recording the export, and returns NO child identifiers. Revoke anon
      EXECUTE; grant authenticated.
    - Revoke direct SELECT on research_aggregate_export from anon/authenticated (function is the only surface).
    pgTAP supabase/tests/research_export_test.sql: a partner gets only its allowed categories; cohorts with <5
    children never appear; each call writes exactly one audit row; identifiers are absent from output.
    (Legal sign-off on dp_epsilon + COPPA Safe Harbor review = OPERATOR.)

- id: regenerate-ts-types
  title: Regenerate Supabase TypeScript types for the new schema
  material: no
  model: haiku
  depends: [immutable-spark-ledger, consent-runtime-gate, bkt-mastery-upgrade, wellbeing-enforcement, leaderboard-matview, research-export-hardening]
  proof: `npx tsc --noEmit` exits 0
  prompt: |
    Regenerate the Supabase types so the app can use all phase-1 + phase-2 objects. Run
    `supabase gen types typescript --linked --schema public` and write the result to the repo's existing types
    location (find it: e.g. types/supabase.ts or lib/database.types.ts — match current path/import style). Do not
    hand-edit generated output. Ensure the project still typechecks: `npx tsc --noEmit` exits 0. If new RPCs
    (issue_sparks, record_retrieval, recommend_arm, verify_teach_transfer, session_guard, export_research_aggregate)
    are referenced anywhere with stale signatures, update those call sites only as needed to keep the typecheck green.

OPERATOR:
  - Approve + apply each material migration to prod Supabase `santas-secret-workshop` (ref whhfugddqehxxbmwutsw) after merge.
  - Deploy edge functions (consent-expiry, mastery-decay, swarm-distill, transfer-item-gen, teachback-score, season-reset) and enable pg_cron + pg_net on the project.
  - Provision secrets for edge functions: LLM API key (transfer-item-gen, teachback-score, swarm-distill optional), transcription API key (teachback-score), and the service-role key for scheduled jobs.
  - Legal/compliance sign-off on research export differential-privacy epsilon and k-anonymity threshold; confirm COPPA Safe Harbor enrollment before any partner is granted export access.
  - Enable Supabase Auth leaked-password protection (pre-existing advisor warning, project-wide).
