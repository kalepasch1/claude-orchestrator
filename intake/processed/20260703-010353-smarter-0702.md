PROJECT: smarter

# Bring the CADE adaptive expert-panel deliberation + reviewer confidence layer into the Smarter product.
# Prototyped in the orchestrator (runner/committees.py, determinations table, console artifact); this ports
# it into Smarter per CADE_IMPLEMENTATION_HANDOFF.md. Consume @darwin/kernel/cade; never put model/DB
# calls in the kernel. Follow smarter's CLAUDE.md conventions exactly (models via the repo constant, all AI
# calls through the repo's ai util, no raw table access, migrations idempotent).

- id: cade-adapters
  title: CADE adapter layer (invoker, embedder, budget) in server/utils/cade/
  material: no
  model: sonnet
  depends: []
  proof: `npx vue-tsc --noEmit` exits 0 AND `npm test -- server/utils/cade` passes
  prompt: |
    Implement the three thin CADE adapters described in CADE_IMPLEMENTATION_HANDOFF.md §2 under
    server/utils/cade/: embedder.ts (wrap the repo's existing embedding path; fall back to hashEmbedder(64)
    from the kernel), invoker.ts (Invoker.invoke(persona, issue, tier, ctx) -> PersonaPosition; persona
    reasoning-signature system prompt + retrieval grounding + the "no citation, no assertion" rule; tier→model
    via the repo's model constants, never string literals; structured output; log every call), and budget.ts
    (per-determination cost ceiling, max deep-tier personas, wall-clock timeout, recursion-depth + cycle
    detection). Pure adapters only — no product logic in the kernel. Add unit tests under server/utils/cade.

- id: cade-determinations-schema
  title: determinations + certificates + proofs schema (idempotent migration + RLS)
  material: yes
  model: sonnet
  depends: []
  proof: `npm run lint:migrations` exits 0 AND `npx vue-tsc --noEmit` exits 0
  prompt: |
    Add an idempotent, numbered migration creating a `determinations` table (subject/title/position/
    recommendation/consensus_pct/consensus_lo/consensus_hi/confidence/materiality/contributors jsonb/
    factions jsonb/dissent jsonb/pivotal jsonb/certificate jsonb/proof_hash/prev_hash/onepager/created_at)
    plus `determination_outcomes` and `constitution_checks`. Enable RLS on every new table with owner-scoped
    policies matching the repo's existing pattern. Column/table naming must follow the repo's migration
    name-check conventions. No data backfill.

- id: cade-adaptive-panels
  title: Adaptive per-issue expert panel deliberation (triage → debate → consensus)
  material: no
  model: opus
  depends: [cade-adapters]
  proof: `npm test -- deliberation` passes (asserts panel assembly + multi-round consensus + legal veto)
  prompt: |
    Implement adaptive deliberation on top of runDetermination() from @darwin/kernel/cade: a triage step that
    ASSEMBLES the optimal expert personas for each specific issue (composition/count vary per issue; NOT a
    fixed roster), runs opening positions → debate rounds → chair synthesis, and returns a Determination with
    position, confidence, dissent factions, and a preserved minority report. Legal/compliance competence is
    force-seated whenever the issue has legal/privacy/regulatory exposure, and a seated legal expert's
    opposition is an absolute veto. Persist via the determinations table. Add tests covering: composition
    adapts across two different issues; a split panel triggers a debate round; legal veto blocks.

- id: cade-onepager
  title: Reviewer deliberation 1-pager (contributors, factions, consensus %, pivotal, contention flag)
  material: no
  model: sonnet
  depends: [cade-adaptive-panels, cade-determinations-schema]
  proof: `npm test -- onepager` passes (renders sections; shows CONTENTION when consensus < floor)
  prompt: |
    Generate a concise reviewer 1-pager per determination: outcome + consensus % with confidence interval,
    contributors ranked by influence (expandable to full expert position + key risk), positions/factions with
    each faction's share of weighted expertise, the pivotal factor (the single expert whose flip changes the
    outcome), key counter-arguments, and — when consensus is below the reviewer's CALIBRATABLE floor (default
    0.85, per-reviewer override) — a "For your personal consideration" section naming the dissenting experts.
    Store on the determination row. Tests: 1-pager includes contributors + factions; contention section
    appears only when consensus < floor.

- id: cade-constitution-gate
  title: Constitution-as-code predicates gating autonomous execution
  material: yes
  model: sonnet
  depends: [cade-adaptive-panels]
  proof: `npm test -- constitution` passes (money-movement + irreversible determinations are gated to a human)
  prompt: |
    Implement machine-checked predicates that gate every determination before any autonomous action:
    no_autonomous_money_movement (never auto-move money / file / send), legal_veto_is_absolute,
    privacy_seated_on_user_data, irreversible_needs_human. Each predicate returns pass/fail with an audit row
    in constitution_checks. A failed 'block' predicate forces the determination to human approval regardless
    of consensus. CADE determines; the Constitution bounds; a human acts. Tests: a "transfer funds
    automatically" determination is blocked; a clean low-risk determination passes.

- id: cade-proofpack-verifier
  title: Ed25519-signed proof packs + offline verifier
  material: no
  model: sonnet
  depends: [cade-determinations-schema]
  proof: `npm test -- proofpack` passes (sign→verify round-trips; a tampered pack fails verification)
  prompt: |
    Emit an Optimality Certificate + hash-chained proof per determination, signed with Ed25519 using
    DARWIN_SIGNING_PRIVATE_KEY_PEM (from env; skip signing gracefully if absent). Build an offline verifier
    that recomputes the canonical hash, checks the chain linkage to the prior determination, and (if signed)
    verifies the signature. Reuse the repo's verifiableProof/eventLog utilities if present. Tests: valid pack
    verifies; a mutated certificate or broken chain link fails.

- id: cade-ground-truth
  title: Ground-truth outcome instrumentation feeding the calibration flywheel
  material: no
  model: sonnet
  depends: [cade-determinations-schema]
  proof: `npm test -- ground-truth` passes (realized signal writes the determination outcome label)
  prompt: |
    Attribute a realized outcome to each determination from actual post-decision signals (accepted/rejected
    outcome, revenue/retention movement, rollback) and write it as the ground-truth label the calibration
    reads, plus a transparent determination_outcomes row. Never guess when no signal exists — leave
    unlabeled. This is what grounds per-expert reliability, Brier calibration, and dissent-vindication in
    reality rather than proxies. Tests: a rollback labels the outcome negative; a positive revenue delta
    labels positive; no signal leaves it unlabeled.

- id: cade-console
  title: In-app Deliberation Console (determinations list + drill-down 1-pager + ledger verify)
  material: no
  model: sonnet
  depends: [cade-onepager, cade-proofpack-verifier]
  proof: `npx vue-tsc --noEmit` exits 0 AND `npm test -- console` passes
  prompt: |
    Add a Deliberation Console page: a list of determinations (outcome badge, consensus % with CI bar,
    materiality, contention flag) and a drill-down 1-pager (contributors expandable to full memos, faction
    bars, pivotal factor, key counter-arguments). Include a calibratable contention-floor control (persisted
    per reviewer), a contested-only filter, and a "Verify ledger" action that walks the proof hash chain.
    Follow the repo's component + data-access conventions. Tests: list renders; drilling into a determination
    shows contributors + factions; verify reports chain status.

OPERATOR:
  - Provide DARWIN_SIGNING_PRIVATE_KEY_PEM (Ed25519 PKCS8) as a repo/deploy secret so proof packs are signed.
  - Approve the determinations/outcomes/constitution schema migration + RLS policies (material) before merge.
  - Confirm smarter's model-routing env (cheap=breadth, deep=debate, strongest=red-team/synthesis) is set per CADE_IMPLEMENTATION_HANDOFF.md §2.3.
