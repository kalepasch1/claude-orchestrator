PROJECT: smarter

# Context: The ENGINE layer for every workstream is already built, mock-degradable, and isolated-tsc clean
# (server/utils/* + server/api/*, persisted via server/utils/kv.ts → smarter_collections). What remains
# below is UI surfacing, wiring/integration, privilege/auth hardening, live providers, and verification.
# Single source of truth: HANDOFF_MASTER.md (self-sufficient; steps 1–11 + addendum). Per-area detail:
# HANDOFF_INVALUABLE_ASSOCIATE.md, HANDOFF_PRINCIPAL_MODEL.md, HANDOFF_ADMIN_OS_AND_CAMPAIGNS.md,
# HANDOFF_LEGACY_AND_MASTERY_LIBRARY.md, HANDOFF_v17.md. Hard rules: mock-degradable (works with NO API
# key), route generated legal text through qc.ts + Policy Constitution + render <TrustBadge>, respect the
# governance trust dial, persist via kv.ts, never break the seed demo, npx vue-tsc --noEmit clean per commit.

- id: green-baseline
  title: Get the app green across all newly added engines/endpoints
  material: no
  model: sonnet
  depends: []
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Boot-and-typecheck pass over the recently added engine layer. Run `npx vue-tsc --noEmit` and fix any
    type/import errors across: docketSentinel, conflictGraph, originationIntel, credentialVault, oneTapInbox,
    reliabilityScore, matterSponsor, graduationLedger, closingTracker, diligenceSteward, fileSteward,
    executionTracker, precedentHarvester (server/utils/*) and their endpoints under server/api/{docket,
    conflicts,origination,credentials,inbox,closing,diligence,files,admin}. Confirm the new kv collections
    (docket_entries, conflict_screens, matter_sponsor_overrides, graduation_ledger, credential_evidence,
    assignment_outcomes, etc.) round-trip against the smarter_collections Supabase table (read+write only;
    no schema change). Do NOT rebuild engines — only fix wiring/types. This task unblocks all others.

- id: now-default-route
  title: Make Now the default route and surface the new ambient lanes
  material: no
  model: sonnet
  depends: [green-baseline]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    pages/now.vue already exists and server/utils/ambient.ts already emits docket, conflict, and predictive-
    origination lanes. Make Now the primary surface: set it as the default route ('/'), ensure every feed
    action resolves inline via POST /api/actions/resolve (it already supports one_tap, deadline_confirm,
    escalate_blocker, run_swarm, send_status, approve_autopilot, acknowledge_docket, resolve_conflict). Render
    <TrustBadge> on any action carrying qcOverall. Keep mobile-first. Reference: server/api/ambient.get.ts,
    server/api/actions/resolve.post.ts, components/TrustBadge.vue.

- id: nav-drawer
  title: Demote the full nav into an "All tools" drawer behind Now/Ask/Approve
  material: no
  model: haiku
  depends: [now-default-route]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    The app has ~70 pages. Make Now / Ask / Approve the only primary chrome; move every other page into a
    collapsible "All tools" drawer/command palette so the 3-surface model is the default experience. Keep all
    existing routes reachable. Reference components/Sidebar.vue and the pages/ directory.

- id: ask-command-bar
  title: Wire the Ask command bar to the askRouter
  material: no
  model: sonnet
  depends: [green-baseline]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    pages/ask.vue + server/utils/askRouter.ts + server/api/ask exist. Make Ask a universal natural-language
    command bar that routes a typed request to the right engine and renders the answer inline (no navigation).
    Pre-fill the 3 most-likely prompts. Reference HANDOFF_MASTER.md Workstream 0/0.5.

- id: approve-queue
  title: Approve = one unified human-gate queue with TrustBadge + sources
  material: no
  model: sonnet
  depends: [green-baseline]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    pages/approve.vue + server/utils/approvals.ts + server/api/approvals exist. Surface every human-gate
    (judgment, intake, conflict, auto_send, high_risk, outbound) as one queue; each card shows the one-line
    "why", <TrustBadge> (qcOverall), linked sources, and an inline approve/reject/choose-option. Include the
    pending conflict blocks from server/api/conflicts/graph.get.ts (pending[]) and docket at-risk items.

- id: docket-page
  title: Build the /docket never-miss register page
  material: no
  model: haiku
  depends: [green-baseline]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Create pages/docket.vue over the built engine (server/utils/docketSentinel.ts; endpoints
    server/api/docket/{index.get,ingest.post,tick.post,acknowledge.post}.ts). Show at-risk/lapsed entries up
    top with rule citations + jurisdiction badges, grouped by matter, with Acknowledge / Mark-filed buttons
    (POST /api/docket/acknowledge). Add a nav entry.

- id: conflicts-sweep-ui
  title: Extend /conflicts with full-book sweep + new-party screen + sign-off
  material: no
  model: sonnet
  depends: [green-baseline]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    pages/conflicts.vue exists. Wire the newer endpoints: the entity graph + pending blocks
    (GET /api/conflicts/graph), a one-time full-book sweep (GET /api/conflicts/sweep) shown as the first-run
    risk view, a "screen new party" box (POST /api/conflicts/check), and one-tap clear/waive(with note)/decline
    (POST /api/conflicts/resolve). Also wire the batch intake screen (POST /api/conflicts/screen) into the
    matter-open / engagementDraft flow so opening a matter auto-runs it and blocks on a block-hit until waived.

- id: closing-cockpit-ui
  title: Surface the closing/CP tracker readiness + chase in the cockpit
  material: no
  model: sonnet
  depends: [green-baseline]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Engine built: server/utils/closingTracker.ts + server/api/closing/{index.get,checklist.post,status.post,
    chase.post}.ts. In pages/cockpit.vue, surface per-matter closing readiness (satisfied/total/%), the CP/
    deliverable/consent/signature list with status toggles (POST /api/closing/status), at-risk items, and a
    one-tap chase that drafts a nudge to the responsible party (POST /api/closing/chase). Offer a "build
    checklist" action for matters that have none (POST /api/closing/checklist).

- id: diligence-page
  title: Build the /diligence data-room steward page
  material: no
  model: sonnet
  depends: [green-baseline]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Engine built: server/utils/diligenceSteward.ts + server/api/diligence/{index.get,ingest.post,issue.post}.ts.
    Create pages/diligence.vue: upload/register a doc set (POST /api/diligence/ingest), show practice-area
    checklist coverage (covered vs. missing), the issue log (add via POST /api/diligence/issue), and the
    findings summary. Add a nav entry.

- id: files-steward-page
  title: Build the /files steward + execution-tracker page (dry-run + undoable)
  material: no
  model: sonnet
  depends: [green-baseline]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Engines built: server/utils/{fileSteward,executionTracker,precedentHarvester}.ts + server/api/files/
    {steward.get,reorg.post,execution.get,harvest.post}.ts. Create pages/files.vue: naming proposals + the
    risk radar, the reorg planner that ALWAYS shows a DRY-RUN preview first and only executes on explicit
    confirm:true (and is undoable via actionLog), the execution ledger + missing-signature chase + a
    "where's the executed original?" lookup. Do NOT auto-execute file moves. Add a nav entry.

- id: origination-intel-ui
  title: Extend /origination with predictive signals, sponsor portfolio, governor, ownership
  material: no
  model: sonnet
  depends: [green-baseline]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    pages/origination.vue + pages/network.vue exist. Surface the v22/v23 engine (server/utils/originationIntel.ts,
    matterSponsor.ts) via GET /api/origination/intel (predictive + portfolio + conversion) and GET
    /api/origination/ownership: show "ping X now, they're about to need coverage", a sponsor-portfolio board
    (health/decay/next-best-action), the anti-desperation governor check before sending (POST
    /api/origination/governor), a one-tap "this is X's matter" ownership correction (POST
    /api/origination/set-sponsor), and the assignments-won conversion (POST /api/origination/outcome).

- id: credential-vault-ui
  title: Extend /credentials with the evidence vault, autofill, and never-lapse feed
  material: no
  model: sonnet
  depends: [green-baseline]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    pages/credentials.vue exists. Surface server/utils/credentialVault.ts via GET /api/credentials/vault
    (evidence + never-lapse feed), POST /api/credentials/vault-add (store a reusable cert/fingerprint/exhibit),
    and POST /api/credentials/autofill (per-jurisdiction form autofill from one profile, showing filled / on-file
    / expiring / missing). Push never-lapse CLE/dues/renewals into the docket/Now surface.

- id: inbox-reply-by-button
  title: Build the /inbox reply-by-button surface (undoable, autonomy-graduation)
  material: no
  model: sonnet
  depends: [green-baseline]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Engine built: server/utils/oneTapInbox.ts + graduationLedger.ts; endpoints server/api/inbox/{asks.get,
    graduation.get}.ts. Create pages/inbox.vue: inbound asks (meeting confirm, conflicts-OK, "can you cover
    this?") as single bundled taps resolved via POST /api/actions/resolve WITHOUT opening a mailbox; each shows
    undoable + the graduation progress ("18/20 clean → auto"). Reference HANDOFF_MASTER.md step 10/0.5.

- id: owner-auth-gating
  title: Gate admin/internal surfaces (mastery, reliability, ROI) behind ops auth
  material: yes
  model: opus
  depends: [green-baseline]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Add an ops/admin auth guard (mirror existing auth in server/utils/auth.ts) on every owner-only surface so
    firm/associate users can never reach them: server/api/admin/* (mastery, mastery-export, reliability, roi)
    and pages/admin/* + pages/roi.vue. The Reliability Score (server/utils/reliabilityScore.ts) and the ROI
    ledger are INTERNAL-ONLY (firm sale + review) — never associate-facing as a scolding metric. Return 403 for
    non-admins. Add a test asserting a non-admin request is rejected.

- id: distillation-enforce
  title: Route all learning-persist paths through the distillation gate
  material: yes
  model: opus
  depends: [green-baseline]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Engines exist: server/utils/distillation.ts (redact/distill/assertNoClientData) + dataPosture.ts. Make every
    path that persists into the learning store / hivemind pass through distill()/redact() so ONLY anonymized
    process knowledge persists: manualCapture, swarm playbook authoring, dealBible, postMortem, the contact/
    regulator directory, masteryLibrary, precedentHarvester. Call assertNoClientData() as a pre-persist guard
    and log the result to trustReceipts ("0 client identifiers persisted"). Raw client content stays only in
    ephemeral working memory. Do NOT rebuild distillation — wire + guard. Reference HANDOFF_MASTER.md addendum #1.

- id: prepareformodel-egress
  title: Enforce prepareForModel() at the LLM boundary (privilege wall)
  material: yes
  model: opus
  depends: [green-baseline]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    In server/utils/llm.ts and its callers, route outbound text through dataPosture.prepareForModel(workspaceId,
    text) so personal_portal mode strips client identifiers BEFORE any external model call, and firm_sanctioned
    mode routes to the in-tenant/BYO-model provider with no egress. Add a test proving personal_portal redacts
    an injected client name before the model call. Reference HANDOFF_MASTER.md addendum #2.

- id: federated-dp
  title: Federated cross-firm pattern sharing with DP / k-anonymity
  material: yes
  model: opus
  depends: [distillation-enforce]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Extend server/utils/federatedLearning.ts so cross-firm sharing of distilled patterns applies differential-
    privacy / k-anonymity (no single firm's patterns re-identifiable) and supports opt-out of CONTRIBUTING while
    still RECEIVING the shared brain. Only anonymized process knowledge (post-distillation) may participate.
    Reference HANDOFF_MASTER.md addendum #3.

- id: privacy-trust-panel
  title: Surface the privilege-safety guarantee as a trust panel
  material: no
  model: sonnet
  depends: [distillation-enforce, prepareformodel-egress]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Build a privacy/trust panel showing the active posture (firm_sanctioned: in-tenant + no-train + audit;
    personal_portal: redact-before-egress) and the "0 client identifiers persisted" receipts from trustReceipts.
    Headline: "the only legal AI that provably preserves privilege — learns the craft, never your data." Read
    from server/api/privacy/{distill,posture}.post.ts + trust-receipts.

- id: matter-close-hooks
  title: Wire matter-close to dealBible + mastery + resume + worth
  material: no
  model: sonnet
  depends: [green-baseline]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    On matter close, fire (in order): dealBible (closing memo + precedent), masteryLibrary.recordRecipe,
    resume.ingestAssignment, and a worth refresh. Idempotent; mock-degradable. Reference server/utils/
    {dealBible,masteryLibrary,resume,worth}.ts and the matter close path in state.ts / matterRunner.ts.

- id: manualstep-takeover
  title: After a manual step, offer swarm takeover when ready
  material: no
  model: sonnet
  depends: [green-baseline]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    When the user completes a manual step, call manualCapture.recordManualStep then, if
    takeoverReadiness(workspaceId, taskSignature).ready, surface a "let the swarm do this next time" offer in
    Now/Approve. The resolver case 'manual_step' already exists in server/api/actions/resolve.post.ts — wire the
    offer UI + the readiness signal. Reference server/utils/manualCapture.ts.

- id: scheduled-ticks
  title: Run docket/campaign/calendar/status loops on scheduled tasks
  material: no
  model: sonnet
  depends: [docket-page]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Wire recurring execution (using the app's scheduled-task mechanism) for: docket ingest+tick (escalate
    deadlines before they breach), campaign tick, compliance-calendar renewals, and status rollups — each
    surfacing results into Now/Approve. Sends remain gated by the governance trust dial (human-tap default).
    Reference server/api/docket/{ingest,tick}.post.ts, server/api/campaigns/tick.post.ts, complianceCalendar.ts.

- id: email-as-rail
  title: Make email a transmission rail, not an admin surface
  material: no
  model: sonnet
  depends: [now-default-route, inbox-reply-by-button]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Eliminate the inbox as an admin surface. Inbound mail is ingested → triaged → drafted → resolved by the
    swarm and surfaced (only if needed) as a Now/Approve item; outbound goes silently (gated by the trust dial).
    Retire/replace any inbox UI used as a to-do/filing/status tool; Now/Ask/Approve is the only workspace. The
    promise: "you never check email again." Reference server/utils/emailSync.ts + HANDOFF_MASTER.md addendum #5.

- id: unified-sentinel
  title: Merge all never-miss surfaces into one Sentinel
  material: no
  model: opus
  depends: [docket-page, closing-cockpit-ui]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Build one firm-wide "Sentinel" surface + endpoint that merges deadlines, complianceCalendar, obligations,
    docketSentinel, and closingTracker into a single ranked at-risk feed that escalates anything slipping. This
    becomes the cleanest GC/risk-facing view. Reuse the existing engines; do not duplicate their logic.

- id: entity-substrate-wiring
  title: Point reputation/staffing/BD at the shared entity + ownership graphs
  material: no
  model: sonnet
  depends: [green-baseline]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Make conflictGraph.buildEntityGraph the single entity-resolution substrate and matterSponsor the single
    ownership substrate: wire counterpartyReputation, staffing (its conflict-clear + ownership checks), and
    bd/prospecting to consume them instead of their own ad-hoc matching. No behavior regressions; vue-tsc clean.

- id: precedent-freshness
  title: Newer executed clause supersedes the stale template
  material: no
  model: sonnet
  depends: [files-steward-page]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Wire precedentHarvester → playbookLibrary so when a newer executed clause outperforms the template in use,
    the system flags the stale template and proposes the firm-wide update (outcome-indexed). Only anonymized,
    distilled descriptors flow in. Reference server/utils/{precedentHarvester,playbookLibrary}.ts.

- id: cite-check-upgrade
  title: Cite-check upgrades — Bluebook formatting + bad-law watch
  material: no
  model: sonnet
  depends: [green-baseline]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Extend server/utils/citations.ts (which already verifies citation existence) with: (a) Bluebook / local-rule
    formatting normalization, (b) a negative-treatment / bad-law signal ("overruled / criticized / superseded?"),
    and (c) a standing watch that continuously re-checks every authority the firm relies on across live matters
    and raises a Now/Approve alert when good law goes bad. Route output through qc.ts; render <TrustBadge> + linked
    sources. Mock-degradable (seed a small treatment table; live source behind a provider seam — see OPERATOR).

- id: court-efiling-engine
  title: Court e-filing + service engine (validate → file → serve → confirm)
  material: yes
  model: opus
  depends: [green-baseline]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Extend server/utils/{filingValidator,filingSubmission}.ts to PACER/ECF + state e-filing: assemble the
    package, validate 100% against current requirements, file, serve, and capture confirmations. High-risk
    filings DEFAULT to human-tap-on-submit (filingSubmission.canAutoSubmit() must stay false until the user opts
    a type in) — never bypass. Provider/portal automation is the desktop-filer task (depends on this). The live
    portal credentials are OPERATOR items.

- id: desktop-filer-d5
  title: Off-hours desktop-control auto-filer (guardrailed)
  material: yes
  model: opus
  depends: [court-efiling-engine]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Build the D5 desktop-control auto-filer (Claude-in-Chrome / computer-use) that, with explicit per-portal
    authorization, logs into NMLS/state portals/PACER-ECF off-hours and uploads each validated, approved
    document to the correct field. MUST: check filingSubmission.canAutoSubmit(filingType) and refuse to
    auto-submit high-risk filings unless the user opted that type in (default = human taps final Submit); run a
    user-approved dry-run preview first; capture step-by-step screenshot evidence to trustReceipts/evidenceLocker;
    NEVER pay a fee / move money without separate explicit approval; halt + escalate on any unexpected screen; be
    auditable + reversible. Portal credentials + computer-use authorization are OPERATOR items.

- id: realtime-scale
  title: Generalize SSE to a production real-time transport
  material: no
  model: sonnet
  depends: [green-baseline]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Generalize the existing SSE (ambient-sse, hosted-sse) into one real-time transport for ambient + campaigns +
    hosted-room live updates, abstracted behind an interface so a production WebSocket/SSE host can be slotted in.
    Keep the in-process fallback for dev. The production host itself is an OPERATOR/deploy item.

- id: pwa-mobile-shell
  title: PWA / mobile shell with Now + one-tap + capture as the core
  material: no
  model: sonnet
  depends: [now-default-route]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Ship a PWA/mobile shell (manifest + service worker) whose core is Now + one-tap + capture; add an offline
    queue for actions taken without connectivity (replayed on reconnect). This is the highest-retention surface
    for juniors between meetings. Mobile-first; reuse the 3-surface shell.

- id: alpha-verify
  title: End-to-end alpha verification on seed data
  material: no
  model: sonnet
  depends: [now-default-route, approve-queue, docket-page, conflicts-sweep-ui, owner-auth-gating, distillation-enforce, matter-close-hooks]
  proof: `npm run build` exits 0 and `npx vue-tsc --noEmit` exits 0
  prompt: |
    Verify end-to-end on seed data and fix anything broken: Now resolves items inline; Ask routes a query;
    Approve shows every human-gate with TrustBadge; a matter auto-runs with judgment stops landing in Approve;
    a 50-state campaign progresses with shrinking escalations; a filing validates 100% (blocked until complete)
    and a high-risk one shows human-tap; the compliance calendar never lapses; legacy artifacts produce by
    default and bypass only on logged permission; a mature task exports an Apparently WorkstreamDefinition; the
    docket escalates a <7d deadline and Acknowledge stops it; the full-book conflict sweep blocks until waived;
    no client identifiers persist into the learning store. Confirm `npm run build` succeeds.

OPERATOR:
  - SECRETS/KEYS: ANTHROPIC_API_KEY; Gmail/Microsoft Graph OAuth (email rail); DocuSign/Adobe Sign creds (e-sign). Confirm SUPABASE_SERVICE_KEY is set in the deployed env.
  - LIVE PROVIDERS (swap the mock seams once creds exist): NMLS + state regulator portals (regRequirements live sourcing + desktop filer); PACER/ECF + state e-filing logins (court-efiling-engine, desktop-filer-d5); iManage/NetDocuments/SharePoint API (fileSteward DMS adapter); eBilling (Tymetrix/Legal Tracker, LEDES) ; OCR + ASR/voice provider (ingest/live capture); CourtListener treatment data / a Shepardize-equivalent (cite-check-upgrade bad-law signal); job/ATS boards + Claude-in-Chrome (jobCrawler live).
  - COMPUTER-USE AUTHORIZATION: per-portal approval for the desktop-control auto-filer (desktop-filer-d5) — high-stakes government submissions; default is human taps final Submit until the user opts a filing type into auto.
  - DEPLOY/INFRA: production WebSocket/SSE host (realtime-scale); custom domains for hosted surfaces (rooms/intake/profiles); in-tenant / BYO-model deployment + SOC2 (firm_sanctioned posture); a production NER redactor to replace the regex heuristics behind distillation.ts.
  - LEGAL SIGN-OFF: privilege-preserving ToS (no-train / no-retain / no-third-party — Heppner-safe); review + green-light of the lateral-recruiting EXTERNAL launch behind the privacy wall; the lateral placement-fee %; confirm the default-human-tap policy for all high-risk regulatory filings before any auto-submit is enabled.
