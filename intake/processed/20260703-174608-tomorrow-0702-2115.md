PROJECT: tomorrow

# Remaining work for the conviction / hedge-aware engine.
#
# CONTEXT: A verified, tested bundle exists at
#   outputs/tomorrow-conviction-hedge-engine/  (28 unit tests green) — but the
# interactive session repeatedly lost repo-tree writes to a workspace reset, so
# the code lives in that bundle, NOT yet in tomorrow/tomorrow/. Task `install-bundle`
# lands it; almost everything depends on it.
# Repo: Nuxt 4 / TS / Prisma / Supabase. Prod build gate: `npm run build` (runs
#   prisma migrate deploy when DATABASE_URL is set, then nuxt build).
# Conventions: requireUserId (~/server/utils/userAuth), loadPolicy (hive/mandatePolicy),
# usePrisma (~/server/utils/prisma), aiCall + AI_MODELS.fast (~/server/utils/aiConfig),
# requireCronAuth (~/server/utils/cronAuth), sendEmail (~/server/utils/emailService),
# vitest (`npx vitest run`), timestamped idempotent Prisma migrations.
# RULE FOR EVERY TASK: search the repo first and prefer the smallest diff that reuses
# existing code; keep `npm run build` green.

- id: install-bundle
  title: Land the delivered conviction/hedge-aware bundle into the repo tree
  material: no
  model: haiku
  depends: []
  proof: "npm run build exits 0 AND npx vitest run server/utils/hive/__tests__/hedgeDecomposition.test.ts server/utils/hive/__tests__/benchAndResolution.test.ts server/utils/hive/__tests__/cfoSuite.test.ts exits 0 (28 tests)"
  prompt: |
    Copy every file from outputs/tomorrow-conviction-hedge-engine/ into tomorrow/tomorrow/ at the
    identical relative paths (server/utils/hive/*.ts, its __tests__/*.test.ts,
    server/api/hivemind/conviction/*.ts, server/api/cron/*.ts). These are verified — do NOT rewrite
    logic. Only fix an import path if it is stale in the current repo. The three cron files reference
    requireCronAuth/sendEmail — confirm those imports resolve. Ensure `npm run build` stays green
    (no new hard deps; engines are pure). CANDIDATE-SHARED: the pure factor/belief engines.

- id: regenerate-belief-canvas-system
  title: Regenerate the earlier belief-canvas conviction system (was wiped by the workspace reset)
  material: no
  model: opus
  depends: [install-bundle]
  proof: "npm run build exits 0 AND npx vitest run server/utils/hive/__tests__/convictionBelief.test.ts server/utils/hive/__tests__/convictionCalibration.test.ts server/utils/hive/__tests__/edgeRadar.test.ts server/utils/hive/__tests__/convictionRegret.test.ts server/utils/hive/__tests__/convictionBasketDecayPremortem.test.ts server/utils/hive/__tests__/convictionPhase2.test.ts server/utils/hive/__tests__/convictionService.test.ts exits 0 (74 tests)"
  prompt: |
    Rebuild the belief-canvas system lost to the workspace reset. Pure engines (server/utils/hive/):
    convictionBelief (belief tuple + fractional-Kelly sizing reconciled to mandate: boost ceiling,
    single-event cap, override flag, calibration extension), convictionCalibration (Brier, reliability
    bins, earned sizing multiplier), edgeRadar (EV-ranked unexpressed edge), convictionRegret
    (undersized/mandate-blocked/oversized ledger), convictionBasket (correlation-weighted mandate-
    clamped basket), convictionDecay (edge half-life + broadcast tier), convictionPremortem (MNPI
    source gate + already-priced-in), convictionOutcomesData (Prisma join + snapshot join),
    marketProbability (venue quotes->implied prob), convictionLeaderboard (suggested Kelly, specialist
    ranking, softmax capital routing), contractCorrelation (structural correlation heuristic; REUSE
    riskFactors.cosine), catalystPublicity (already-priced-in from market signals), convictionSnapshot
    (fail-soft sizing snapshot persistence), convictionRegretDigest (weekly sweep), convictionService
    (pure façade + version + surface manifest). Endpoints under server/api/hivemind/conviction/:
    belief-size.post, calibration.get, regret.get, parse.post (aiCall NL->belief), premortem.post
    (folds in catalystPublicity), basket.post, edge-radar.get, market-price.get (scanRetailPricing +
    catalog fallback), suggested-kelly.get, leaderboard.get, capture.post, captures.get,
    contract-correlations.get. Cron: conviction-regret-digest.get. Write matching vitest files so the
    proof shows 74 passing. Keep `npm run build` green. Do NOT touch pages/*.vue here (see wire-canvas-ui).
    Persistence-dependent endpoints must fail-soft when the new Prisma models are absent (they land in a
    separate task). CANDIDATE-SHARED: belief-sizing + calibration + regret engines.

- id: prisma-models-snapshot-capture
  title: Prisma migration + models for ConvictionSizingSnapshot and ConvictionCapture
  material: yes
  model: sonnet
  depends: [install-bundle]
  proof: "npx prisma validate exits 0 AND npm run build exits 0"
  prompt: |
    Add two Prisma models to prisma/schema.prisma in repo style (@id @default(dbgenerated(gen_random_uuid))
    @db.Uuid, @db.Timestamptz(6), Json?, @@index, @@map snake_case): ConvictionSizingSnapshot
    (convictionId @unique, orgId, recommendedSize/justifiedSize/mandateBlocked Decimal(20,2), boundBy
    String default 'edge', snapshotJson Json?, createdAt) -> conviction_sizing_snapshots; ConvictionCapture
    (orgId, rawText, channel default 'app', parsedJson Json?, contractType String?, status default 'draft',
    createdAt, updatedAt) -> conviction_captures. Add ONE timestamped idempotent migration
    (CREATE TABLE IF NOT EXISTS + indexes, double-quoted camelCase columns). Run `npx prisma generate`.
    Edit ONLY prisma/schema.prisma + the new migration dir. MATERIAL: schema change — human approves merge.

- id: persist-target-exposure
  title: Persist each CFO's target net factor exposure (the drift/letter keystone)
  material: yes
  model: sonnet
  depends: [prisma-models-snapshot-capture]
  proof: "npx vitest run server/utils/hive/__tests__/targetExposure.test.ts exits 0 AND npm run build exits 0"
  prompt: |
    Add Prisma model TargetExposure (orgId @unique, targetJson Json, tolerancesJson Json?, scenarioRulesJson
    Json?, updatedAt) + ONE idempotent timestamped migration + `prisma generate` (depends on
    prisma-models-snapshot-capture to avoid schema.prisma merge conflicts). Add server/utils/hive/
    targetExposure.ts (PURE: validate/normalize a FactorVector against ALL_FACTORS from riskFactors.ts;
    diff current-vs-target) + vitest targetExposure.test.ts (proof). Add GET/PUT
    /api/hivemind/conviction/target-exposure (requireUserId, usePrisma, fail-soft). Then rewire
    hedge-drift.get and the risk-letter cron to compare against the persisted target instead of the
    current book (fall back to current book when no target set). MATERIAL: schema change.

- id: factor-betas-feeder
  title: Learn factor loadings from realized settlement returns (feeder + learned lookup)
  material: yes
  model: sonnet
  depends: [persist-target-exposure]
  proof: "npx vitest run server/utils/hive/__tests__/factorBetaFeeder.test.ts exits 0 AND npm run build exits 0"
  prompt: |
    Add Prisma model ContractFactorLoading (contractType @unique, loadingsJson Json, sampleSize Int,
    updatedAt) + ONE idempotent migration + `prisma generate` (depends on persist-target-exposure so
    schema.prisma edits serialize — no parallel schema conflicts). Add server/utils/hive/
    factorBetaFeeder.ts: assemble ReturnObservation[] from settlement/mesh price history via usePrisma
    (fail-soft to []), call estimateLoadingsFromReturns + blendWithPrior + sampleWeight from
    factorBetas.ts, upsert the blended loadings. Add loadingsForLearned(contractType) that reads the row
    and falls back to riskFactors.loadingsFor. Cron /api/cron/conviction-factor-betas (requireCronAuth).
    Unit-test the pure assemble/blend path with synthetic data (factorBetaFeeder.test.ts). MATERIAL: schema.

- id: full-factor-covariance
  title: Factor covariance matrix + variance-minimizing optimizer mode
  material: no
  model: opus
  depends: [factor-betas-feeder]
  proof: "npx vitest run server/utils/hive/__tests__/factorCovariance.test.ts exits 0 AND npm run build exits 0"
  prompt: |
    Add server/utils/hive/factorCovariance.ts (PURE): estimate the factor covariance from the same
    ReturnObservation feed (sample covariance + shrinkage toward a diagonal target). Extend
    bookOptimizer.solveToTarget with an optional objective:'variance' mode that minimizes portfolio
    factor variance subject to hitting named targets (ridge-regularized quadratic reusing the existing
    Gaussian-elimination helper in bookOptimizer.ts). Default least-squares behavior MUST stay byte-for-
    byte unchanged (add, don't rewrite). Tests: covariance symmetric + PSD after shrinkage; variance mode
    yields lower estimated variance than the plain solve on a correlated synthetic book. NOTE: edits
    bookOptimizer.ts — do not run in parallel with any other task that edits it.

- id: scenario-conditioned-targets
  title: Scenario-conditioned target exposure (pre-position as regime probabilities shift)
  material: no
  model: sonnet
  depends: [persist-target-exposure]
  proof: "npx vitest run server/utils/hive/__tests__/scenarioTargets.test.ts exits 0 AND npm run build exits 0"
  prompt: |
    Add server/utils/hive/scenarioTargets.ts (PURE): resolve an effective target FactorVector from a base
    target + scenarioRules (stored on TargetExposure.scenarioRulesJson) + a regime-probability input, using
    STRESS_SCENARIOS keys from tailFactors.ts (e.g. "net-flat credit unless recession prob > 0.4, then
    short credit"). Compose with predictionSignals if that module exists (fail-soft import). Have
    target-exposure GET resolve the effective target when scenario rules are present. Unit-test: base
    target when calm, shifted when a scenario fires (scenarioTargets.test.ts). Reads riskFactors/tailFactors
    only; edits target-exposure endpoint — chain after persist-target-exposure (already depended).

- id: wire-canvas-ui
  title: Wire belief canvas + book/solve/approval-card panels into the conviction page
  material: no
  model: sonnet
  depends: [regenerate-belief-canvas-system]
  proof: "npm run build exits 0"
  prompt: |
    In pages/app/conviction.vue add, behind a toggle (Nuxt auto-imports components/composables), the
    ConvictionCanvas (belief tuple + Kelly), a Book Factor Exposure panel (GET /book-exposure), a
    Solve-for-intent panel (POST /solve-intent), and the one-tap Approval Card (POST /approval-card) whose
    commitPayload posts to /api/hivemind/conviction. Add composables/useConvictionBelief.ts and
    components/conviction/*.vue (the outputs-bundle history has reference ConvictionCanvas.vue +
    useConvictionBelief.ts). Changes must be ADDITIVE — do not remove existing composer behavior. This is
    the ONLY task that edits pages/app/conviction.vue. Prove with `npm run build` (the real gate; SFCs must
    compile in prod).

- id: bench-kpi-dashboard-tile
  title: Read-only CFO tile — bench-win-rate + regret
  material: no
  model: haiku
  depends: [wire-canvas-ui]
  proof: "npm run build exits 0"
  prompt: |
    Add a small read-only component that calls GET /api/hivemind/conviction/bench-win-rate and /regret and
    renders the north-star (did convictions beat the market), win-rate, and net edge left on the table.
    Additive, theme-agnostic, no new deps. Edits a dashboard page + a new component only (not
    conviction.vue). Prove with `npm run build`.

- id: shadow-book-cost-to-close
  title: Spike — live 'cost to neutralize residual risk' number (feature-flagged)
  material: no
  model: sonnet
  depends: [install-bundle]
  proof: "npx vitest run server/utils/hive/__tests__/costToClose.test.ts exits 0 AND npm run build exits 0"
  prompt: |
    Prototype server/utils/hive/costToClose.ts (PURE): given the book's net FactorVector (from
    bookFactorExposure) and per-factor proxy instruments, compute the notional + estimated bps cost to
    fully neutralize residual factor risk at current pricing (reuse bookOptimizer.solveToTarget with an
    all-zero target). Add GET /api/hivemind/conviction/cost-to-close behind a feature flag
    (isFeatureEnabled) so it is dark by default. Unit-test the pure cost math (costToClose.test.ts). Keep
    `npm run build` green. CANDIDATE-SHARED: cost-to-close applies to any factor-modeled book.

OPERATOR:
  - Enable new cron endpoints in vercel.json + set CRON_SECRET: /api/cron/conviction-resolution, /api/cron/conviction-regret-digest, /api/cron/conviction-risk-letter, /api/cron/conviction-factor-betas.
  - Apply Prisma migrations to Supabase (prisma migrate deploy) after the material schema tasks are approved (snapshot/capture, target-exposure, contract-factor-loading).
  - Confirm scanRetailPricing / getMispricingPrediction in hive/convictionRouter.ts return live venue data in prod; provide Kalshi/Polymarket keys if a live feed is required (market-price + premortem publicity depend on it).
  - Legal/compliance sign-off on the MNPI source-gate + attestation copy before real users hit premortem.post over-mandate gating.
