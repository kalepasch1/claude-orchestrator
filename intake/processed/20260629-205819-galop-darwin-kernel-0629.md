PROJECT: galop

- id: darwin-vendor
  title: Vendor @darwin/kernel into the repo (additive)
  material: no
  model: haiku
  depends: []
  proof: `node --experimental-strip-types -e "import('./vendor/darwin-kernel/src/passport/index.ts').then(m=>process.exit(m.buildPassport?0:1))"` exits 0
  prompt: |
    Copy packages/darwin-kernel/src from the orchestrator repo (env ORCHESTRATOR_REPO,
    default ../beethoven/claude-orchestrator) into vendor/darwin-kernel/src, EXCLUDING
    cade/. Add vendor/darwin-kernel/package.json. Integration points live in racefeed/lib
    and supabase/ edge functions. Do not import it yet. See DARWIN_KERNEL_ADOPTION.md.

- id: darwin-kyc-passport
  title: Mint a portable passport on KYC/geo pass
  material: yes
  model: sonnet
  depends: [darwin-vendor]
  proof: `node --experimental-strip-types --test racefeed/lib/__tests__/darwinPassport.test.ts` exits 0
  prompt: |
    On the provider-seam success path (KYC + geolocation pass), mint the credential that
    turns a Galop player into a one-click prospect for Pareto/Tomorrow.
    Steps:
    1. Where the KYC/geo providers return success, build a passport with
       galop.galopKycClaims({geoRegion, sanctionsClear:true}) over deriveSubject(verifiedId)
       and persist it (darwin_passports). Record a consent grant only when the player opts
       into cross-product offers.
    2. Add test racefeed/lib/__tests__/darwinPassport.test.ts: a passing KYC result yields a
       verifyPassport-valid passport carrying kyc_verified + geo_allowed + sanctions_clear.

- id: darwin-govern-rpcs
  title: Govern money-flow RPCs with the kernel constitution
  material: yes
  model: sonnet
  depends: [darwin-vendor]
  proof: `node --experimental-strip-types --test racefeed/lib/__tests__/darwinGovern.test.ts` exits 0
  prompt: |
    Route money-flow through the kernel constitution (additive — keep the
    server-authoritative settlement as the source of truth).
    Steps:
    1. Build galop.galopConstitution() (escalate cash_out/operator_payout/commingle_pool;
       deny reveal_winner_pre_lock).
    2. Before the corresponding RPC effects, call governAction() and enforce the verdict;
       persist receipts (fail-soft).
    3. Add test darwinGovern.test.ts asserting cash_out → escalate and
       reveal_winner_pre_lock → deny.

- id: darwin-publish-capabilities
  title: Publish kyc_geo_gate + provably_fair_settlement capabilities
  material: no
  model: sonnet
  depends: [darwin-vendor]
  proof: `node --experimental-strip-types --test racefeed/lib/__tests__/darwinCapabilities.test.ts` exits 0
  prompt: |
    Publish galop.galopCapabilities so the reusable KYC/geo gate and the server-authoritative
    "provably fair" settlement primitive are available portfolio-wide.
    Steps:
    1. Map each capability endpoint to the existing route/edge function.
    2. Publish the specs (seed) and add test darwinCapabilities.test.ts that publishes +
       instantiates kyc_geo_gate against a stub handler.

OPERATOR:
  - Apply vendor/darwin-kernel/sql/0001_darwin_kernel.sql to the Galop Supabase project (use the SHARED project so passports are portable across products).
  - Set DARWIN_SIGNING_PRIVATE_KEY_PEM in the Galop host/edge env (shared portfolio anchor).
