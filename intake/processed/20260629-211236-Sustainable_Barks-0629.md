PROJECT: Sustainable_Barks

# NOTE TO ORCHESTRATOR: this repo is NOT in the known project enum
# (smarter|tomorrow|apparently|galop|hisanta|pareto-2080|darwn). Repo lives at
# /Users/kpasch/Documents/Sustainable_Barks (Nuxt 3 + Supabase + Vercel). A
# worktree/repo mapping for "Sustainable_Barks" likely must be registered before
# these tasks can route — see OPERATOR. All feature code below is ALREADY written
# and in the working tree; the tasks are verification, test-locking, and two
# end-to-end loop completions, not greenfield.

- id: sb-build-verify
  title: Full Nuxt production build passes for the new advocacy/calculator/order code
  material: no
  model: sonnet
  depends: []
  proof: `npm run build` exits 0
  prompt: |
    Context: a large feature set was added to the Sustainable_Barks repo and validated
    only at the SFC/TS-transpile level (the authoring sandbox could not run `nuxt build`
    due to a missing native ARM binary). Run a real production build and fix any genuine
    build/type/auto-import errors introduced by these NEW/CHANGED files only:
      - composables/useBundleMath.ts, composables/useShare.ts
      - components/RoiCalculator.vue, components/ShareLinkGenerator.vue
      - pages/index.vue, pages/contact.vue, pages/help-out.vue, pages/projection.vue,
        pages/shelters.vue, pages/brands.vue, pages/join/[code].vue, pages/join/index.vue
      - components/AppNavbar.vue, components/AppFooter.vue
      - server/api/start-order.post.ts, server/api/volunteer.post.ts,
        server/api/impact/total.get.ts, server/api/advocate/generate.post.ts,
        server/api/advocate/[code].get.ts
    Steps: `npm install` then `npm run build`. If it fails, fix ONLY the offending new
    code (match existing repo conventions: Nuxt auto-imports for composables/components,
    `($fetch as any)` casts are NOT used here — plain $fetch/useFetch is the repo norm;
    `serverSupabaseServiceRole` + utils/rateLimit + utils/sendEmail patterns). Do NOT
    refactor unrelated files, do NOT change pricing numbers in useBundleMath.ts, do NOT
    touch the SQL migrations. Keep the build green. Report any error you fixed.

- id: sb-bundlemath-tests
  title: Lock the pricing/ROI/ESG math with unit tests
  material: no
  model: sonnet
  depends: [sb-build-verify]
  proof: `npx vitest run` exits 0
  prompt: |
    The pure functions in composables/useBundleMath.ts (computeRoi, esgProjection,
    formatUsd, BUNDLE_TIERS) drive every customer-facing cost and the "to shelters"
    figure, so they must be regression-locked. Add vitest minimally:
      - `npm i -D vitest` and add `"test": "vitest run"` to package.json scripts.
      - Create composables/__tests__/useBundleMath.test.ts with assertions:
        * BUNDLE_TIERS perStay matches price/staysPerBundle for all 3 tiers (essential
          $75/10=7.5, signature 95/15≈6.333, luxury 125/25=5) within 1e-9.
        * computeRoi(20,'signature'): annualStays=1040, bundlesPerYear=ceil(1040/15)=70,
          annualCost=70*95=6650; costPerStay≈annualCost/1040; toShelters=annualCost*0.5;
          monotonic: more weeklyStays => annualCost non-decreasing.
        * computeRoi clamps weeklyStays<1 to 1 (safeWeekly) and rounds.
        * esgProjection(20,'signature') returns toysRescuedPerYear=1040*6=6240 and
          lbsDivertedPerYear=round(6240*0.3)=1872; shelterFundingPerYear==toShelters.
        * formatUsd(6650) === '$6,650'.
    Do not change useBundleMath.ts logic. proof: `npx vitest run` exits 0.

- id: sb-autoreplenish-cron
  title: Auto-replenish reminder cron — surface due standing orders to the team
  material: no
  model: sonnet
  depends: [sb-build-verify]
  prompt: |
    Make the "auto-replenish" order option (captured in pages/contact.vue and persisted
    by server/api/start-order.post.ts into orders.auto_replenish / orders.delivery_frequency
    via migration 018) actually actionable. Add server/api/cron/auto-replenish-reminders.post.ts
    modeled exactly on the existing server/api/cron/renewal-reminders.post.ts (same auth
    pattern, serverSupabaseServiceRole, utils/sendEmail.sendNotificationEmail). It must:
      - select orders where auto_replenish = true and status in ('delivered','paid'),
      - group by delivery_frequency, and email an internal digest ("N hotels due for
        monthly/biweekly/quarterly replenishment") to NOTIFICATION_EMAIL.
      - NO money movement, NO customer emails, NO writes that change order status — read +
        notify only. Fail-soft (try/catch around email; never throw on send failure).
    This is notification-only (non-material). proof: `npm run build` exits 0.
    NOTE: functionally requires migration 018 applied in prod (OPERATOR) — but the code
    must build and run regardless since the Supabase client here is untyped.

- id: sb-join-qr
  title: Admin can generate a /join/[code] QR for the physical bundle card (offline entry to the advocacy loop)
  material: no
  model: sonnet
  depends: [sb-build-verify]
  prompt: |
    The advocacy loop's offline entry point is a QR on the printed bundle welcome card
    that lands on /join/[code]. The repo already has pages/admin/welcome-card.vue using the
    `qrcode` npm package, and POST /api/advocate/generate returns { code, shareUrl } where
    shareUrl = https://sustainablebarks.com/join/<code>. Extend pages/admin/welcome-card.vue
    with a small "Generate bundle-card QR" panel that: takes a hotel/partner name, calls
    POST /api/advocate/generate with { partnerType:'hotel', partnerName }, renders a QR of
    the returned shareUrl (reuse the existing qrcode usage in that file), and shows the
    code + a copy button. Do not alter the existing welcome-card QR logic; add alongside it.
    Match existing styling/util patterns in the file. proof: `npm run build` exits 0.

OPERATOR:
  - REGISTER PROJECT: add a worktree/repo mapping for "Sustainable_Barks" →
    /Users/kpasch/Documents/Sustainable_Barks (Nuxt 3). It is not in the known project
    enum, so these tasks won't route until it's registered.
  - APPLY MIGRATIONS (material, prod DB): run supabase/migrations/018_growth_engine.sql
    and 019_advocacy.sql against Supabase project bjyiljavsctvptfplqyo. A combined,
    idempotent paste-ready file exists at repo root: RUN_THESE_MIGRATIONS.sql (Dashboard →
    SQL Editor → New query → paste → Run). The connected Supabase account here does NOT
    include this project and .env has no Postgres connection string, so it cannot be
    automated from these sessions.
  - DEPLOY: after sb-build-verify is green, deploy to Vercel (prod deploy).
  - SEED (optional, lights up the live counters): insert real rows into `disbursements`
    (shelter_name, amount_usd, toys_rescued) as you pay shelters; the homepage + /shelters
    counters and /api/impact/total stay dormant/at "founding" copy until then. Do NOT seed
    fabricated numbers pre-launch.
  - LEGAL/CPA: confirm the reframed tax one-liner ("a deductible business amenity expense" +
    "100% of profits fund 501(c)(3) shelters, receipt included") before scaling copy. All
    "fully/100% tax-deductible" overclaims were already removed from the site.
