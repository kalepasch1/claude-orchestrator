PROJECT: tomorrow

- id: ecp-lock-branch
  title: Extract the durable ECP bundle into the repo and commit it to a branch
  material: yes
  model: haiku
  depends: []
  proof: `git -C /Users/kpasch/Documents/tomorrow/tomorrow ls-files server/utils/otc/ecp | wc -l` returns >= 30 AND `git -C /Users/kpasch/Documents/tomorrow/tomorrow log --oneline -1` shows the ECP commit
  prompt: |
    The complete ECP credentialing subsystem was built this session but the repo working tree keeps
    getting reset by the self-improvement automation, so the files are only durably stored in the
    outputs bundle `ecp-module-complete.tar.gz` (mirrored under the session outputs folder `ecp-src/`).
    Land it permanently:
    1. Extract `ecp-src/` into the tomorrow repo at the correct paths: server/utils/otc/ecp/*,
       server/utils/otc/ecp/__tests__/*, server/api/{otc/ecp,ecp-portal,partner/ecp,cron}/*,
       pages/ecp/*, pages/app/otc/ecp/*.
    2. Re-apply the two tracked-file edits idempotently: add `'eligibility'` to `ProofKind` in
       server/utils/proof/verifiableProof.ts and to the KINDS array in server/api/proof/index.post.ts;
       add `{ path: '/app/otc/ecp/console', label: 'ECP Credentials', icon: Shield }` to
       components/app/AppSidebar.vue after the Decisions nav item.
    3. Commit to a dedicated branch `feat/ecp-credentialing` (do NOT push to main).
    4. IMPORTANT: pause or exclude server/utils/otc/ecp/**, server/api/*ecp*/**, pages/ecp/**,
       pages/app/otc/ecp/** from the self-improvement loop's working-tree reset so it stops wiping this.

- id: ecp-typecheck-green
  title: Make the ECP subsystem typecheck clean under the repo tsconfig
  material: no
  model: sonnet
  depends: [ecp-lock-branch]
  proof: `npx vue-tsc --noEmit -p tsconfig.json` reports 0 NEW errors in files under server/utils/otc/ecp, server/api/*ecp*, pages/ecp, pages/app/otc/ecp (vs the documented stale-Prisma baseline) AND `npx vitest run server/utils/otc/ecp/__tests__/` passes 23/23
  prompt: |
    The ECP engines + vitest suite pass (23 tests across ecp.test.ts + ecp2.test.ts) but a full
    Nuxt/Prisma typecheck was never run in-repo. Run the repo typecheck and fix type errors introduced
    by the ECP files ONLY (do not touch unrelated baseline errors). Watch for: Nuxt auto-import globals
    in the new API routes (defineEventHandler/readBody/getRouterParam/createError/getHeader/getQuery/
    getRequestHeader), the EcpEvidence/EcpDetermination imports, and BigInt usage in zkRange.ts (ensure
    tsconfig target/lib supports BigInt literals; guard or bump lib if not). Keep vitest green.

- id: ecp-prisma-persistence
  title: Durable Prisma persistence for ECP credentials, sessions, revocations, and audit
  material: yes
  model: opus
  depends: [ecp-typecheck-green]
  proof: `npm run lint:migrations` is clean AND `npx vitest run server/utils/otc/ecp/__tests__/` passes AND a new persistence round-trip test writes+reads an ecp_credential row (gated behind RUN_DB_TESTS=1 so it never hits prod by default)
  prompt: |
    Today the ECP stores are KV-backed (persistentStore over coordination_tasks). Add real tables so
    issuance/revocation/audit are examiner-grade and queryable. Follow the tomorrow migration governance
    in CLAUDE.md EXACTLY (name-check every identifier against schema.prisma; camelCase quoted columns +
    snake @@map; gen_random_uuid() PKs; mirror 20260618100000_bank_hedging_vertical). Tables:
    ecp_credentials, ecp_sessions, ecp_determinations, ecp_revocations, ecp_audit_entries (INSERT-only,
    hash-chained). Add Prisma models; make the ecp stores DB-primary with the existing KV as fail-open
    fallback (mirror creditStore.ts). DO NOT deploy the migration to prod — leave it for OPERATOR after a
    shadow dry-run. Additive and reversible only.

- id: ecp-settlement-gate
  title: Enforce the trade-authorization token as a hard gate in the settlement engine
  material: yes
  model: opus
  depends: [ecp-typecheck-green]
  proof: `npx vitest run server/utils/otc/settlement/__tests__` passes with a NEW test asserting a bilateral settlement is refused when no valid mutual-eligibility trade-authorization token is present, and allowed when it is
  prompt: |
    Wire tradeGate.ts into the Phase-1/2 non-custodial settlement engine (server/utils/otc/settlement/*)
    so a bilateral swap cannot produce a settlement instruction unless a valid mutual-eligibility
    trade-authorization token (tradeGate.authorizeTrade / canOpenTrade) is present and neither party's
    ECP credential is revoked. This makes §2(e) ECP compliance structurally enforced at money-movement,
    not advisory. Keep posture: instruction-ref only, no funds move, fail-closed (missing/invalid token
    ⇒ no instruction). Add unit tests. Do not change the SETTLEMENT_SWEEP_ENABLED default (stays OFF).

- id: ecp-live-sanctions-seam
  title: Replace the in-memory sanctions denylist with a live OFAC/KYB provider seam
  material: yes
  model: sonnet
  depends: [ecp-typecheck-green]
  proof: `npx vitest run server/utils/otc/ecp/__tests__/` passes with a NEW test that injects a mock provider returning a sanctions hit and asserts determineAndIssue returns nextStep 'screening_block' and mints no credential
  prompt: |
    screening.ts checks an in-memory denylist. Add a pluggable async provider interface
    (OfacProvider/KybProvider) with the in-memory list as default and a documented adapter stub for a
    real feed (OFAC SDN API / Chainalysis). Fail-CLOSED: provider error or hit ⇒ block. NO hardcoded API
    keys — read from env; list the required env var under OPERATOR. Wire through determineAndIssue via
    the existing screener option. Tests use a mock provider (no network).

- id: ecp-eas-onchain-submitter
  title: Real EAS on-chain submitter behind the existing gated dry-run seam
  material: yes
  model: sonnet
  depends: [ecp-typecheck-green]
  proof: `npx vitest run server/utils/otc/ecp/__tests__/` passes; a NEW test asserts the real submitter is only invoked when ECP_EAS_ENABLED=true AND a signer is configured, else returns the dry-run receipt (submitted:false)
  prompt: |
    easAdapter.ts has a pluggable EcpChainSubmitter with a gated dry-run default. Implement a real
    submitter posting the ECP claim to Ethereum Attestation Service (schema ECP_EAS_SCHEMA, refUID from
    the credential contentHash, revocable) via ethers + the EAS SDK. OFF by default: only activate when
    ECP_EAS_ENABLED=true AND a signer key is present; never sign/post in tests. NO keys in code — env
    only; list ECP_EAS_ENABLED, EAS_CONTRACT_ADDRESS, EAS_SIGNER_KEY, RPC_URL under OPERATOR. Tests use a
    mock submitter (no network).

- id: ecp-treasury-reader
  title: Real on-chain TreasuryReader for zero-touch evidence extraction
  material: no
  model: sonnet
  depends: [ecp-typecheck-green]
  proof: `npx vitest run server/utils/otc/ecp/__tests__/` passes with a NEW test that injects a mock reader and asserts evidenceFromOnChainTreasury maps a priced balance into totalAssetsUsd and returns null when unpriceable
  prompt: |
    evidenceExtractor.ts defines TreasuryReader as a seam returning null. Implement a real reader that
    values an address's holdings in USD (stablecoins + major tokens via RPC + a price source), behind the
    seam so the pure functions stay testable. Fail-LOUD (return null) when it cannot price — never
    fabricate a balance (reserveOracle discipline). NO keys in code; list RPC_URL / PRICE_API under
    OPERATOR. Tests use a mock reader only.

- id: ecp-recert-batch-cron
  title: Batch recertification sweep cron across all entities
  material: no
  model: haiku
  depends: [ecp-lock-branch]
  proof: `node --check server/api/cron/ecp-recert.get.ts` passes AND the cron is registered in BOTH vercel.json and nuxt.config.ts scheduledTasks AND `npx vitest run server/utils/otc/ecp/__tests__/` still passes
  prompt: |
    recertSentinel.ts + /api/cron/ecp-recert currently require a single entityId. Add a batch sweep that
    enumerates all entities with issued credentials (index in ecpStore), buckets by expiry, and alerts on
    expiring_soon (reuse the platform Slack/email notify pattern). Dual-register the cron in vercel.json
    and nuxt.config.ts scheduledTasks, CRON_SECRET-gated. Read-only + fail-open.

- id: ecp-coordination-hygiene
  title: Register ECP coordination bots + assessment dimension + coherence-scanner entries
  material: no
  model: sonnet
  depends: [ecp-lock-branch]
  proof: `npx vitest run server/utils/coordination/__tests__/integrationCoherenceScanner.test.ts` passes AND runFullAssessment includes an assessEcpCredentialing dimension covered by a unit test
  prompt: |
    Fold ECP into platform self-monitoring per CLAUDE.md conventions: (1) add ecp-issuance-bot,
    ecp-recert-sentinel, ecp-screening-bot to BOT_IDENTITIES in coordination-client.ts +
    ensureBotsRegistered in self-improvement-runner.mjs; (2) add assessEcpCredentialing to
    assessment-engine.ts wired into runFullAssessment (issuance rate, escalation/screening-block rate,
    expiry backlog, audit-chain integrity) with correct AssessmentFinding/ProposalDraft field names +
    0..1 score; (3) add the new ECP cron routes to the coherence scanner's expected sets so parity stays
    green. Keep the existing coherence test green.

- id: ecp-eligibility-amm
  title: Autonomous eligibility+guarantee market-maker composing extractor/precredential/marketplace
  material: yes
  model: opus
  depends: [ecp-settlement-gate, ecp-treasury-reader]
  proof: `npx vitest run server/utils/otc/ecp/__tests__/` passes with a NEW test proving one tick end-to-end via injected seams (no DB/network): a mock on-chain candidate + a guarantor offer yields a pre-credentialed eligible entity and a fulfillable guarantor match
  prompt: |
    Build an autonomous "eligibility AMM" agent (pure orchestration + injected seams) that each tick:
    scans candidate on-chain treasuries (evidenceExtractor + TreasuryReader seam) -> computes claimable
    pre-credentials (precredential.computeClaimable) -> matches open guarantor offers to obligor needs
    (guarantorMarketplace.matchOffers/fulfillMatch) -> emits proposed bilateral IOIs into the War Room
    seam. Mandate-bounded + kill-switch (reuse autonomy/constitution gate); NEVER auto-executes a trade
    or moves funds (proposals only). Fully testable via injected seams. Env flag default OFF. Composes the
    whole ECP stack into a self-running counterparty-manufacturing loop.

OPERATOR:
  - Set prod env for go-live gates (all default OFF; flip only when ready): ECP_S2S_SECRET; ECP_EAS_ENABLED + EAS_CONTRACT_ADDRESS + EAS_SIGNER_KEY + RPC_URL (ecp-eas-onchain-submitter); sanctions provider API key (ecp-live-sanctions-seam); RPC_URL/PRICE_API (ecp-treasury-reader). Assistant cannot enter secrets.
  - Deploy the ecp Prisma migration (ecp-prisma-persistence) to Supabase after a shadow dry-run + name-check — do NOT let the loop auto-deploy it.
  - Counsel sign-off required before enabling: cross-jurisdiction eligibility (EU/UK/AUS rule sets), carrier/consumer intermediation paths, and any live on-chain issuance. US-only determination is the current scope.
  - Pause/scope the self-improvement automation so it stops resetting the ECP working tree (see ecp-lock-branch step 4).
