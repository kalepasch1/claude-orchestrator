PROJECT: darwn

- id: opportunity-persistence-model
  title: Persist opportunities with earlyAccess/surfaceAt so gating can't be client-suppressed
  material: yes
  model: sonnet
  depends: []
  proof: `npx prisma validate` exits 0 AND `npx vitest run server/utils/__tests__/opportunity-store.test.ts` exits 0
  prompt: |
    NOTE: darwn's git repo root IS the nested app dir (the repo you check out already contains
    nuxt.config.ts, prisma/, BRAND.md at top level). Pulse design language (jade #0E9C8E) — never
    reintroduce crimson #921A12.

    Today early-access gating in server/api/handoff/[oppId]/index.get.ts reads the early-access
    flag from a passthrough query param (?earlyAccess=1) with a TODO(opportunity-store) because no
    persisted opportunity row exists. Deliverable: add the persistence so the flag is authoritative.
    1. Add a Prisma model (e.g. Opportunity) with at least { id, ...existing opp fields it needs,
       earlyAccess Boolean @default(false), surfaceAt DateTime? } and a generated migration. Match
       existing schema conventions (look at the HandoffThread/HandoffMessage models added earlier).
    2. Add server/utils/opportunityStore.ts with getOpportunity(oppId) reading the row, plus a tiny
       isEarlyAccess(opp, now) helper (true when earlyAccess and now < surfaceAt). Keep an in-memory
       fallback consistent with the existing handoff/referral store pattern.
    3. Add server/utils/__tests__/opportunity-store.test.ts proving isEarlyAccess windowing + a
       missing opp fails soft (no throw). Do NOT wire the endpoint here (next task). TS strict-clean.
    MATERIAL: schema/migration — produce green branch, do not auto-merge.

- id: handoff-early-access-from-store
  title: Read early-access flag from the persisted opportunity in the handoff endpoint
  material: no
  model: sonnet
  depends: [opportunity-persistence-model]
  proof: `npx vitest run server/api/handoff/__tests__/early-access.test.ts` exits 0
  prompt: |
    Replace the ?earlyAccess=1 passthrough in server/api/handoff/[oppId]/index.get.ts with a read
    from opportunityStore.getOpportunity(oppId) + isEarlyAccess(). Keep the existing perk check:
    an early-access opp returns 404 to a caller lacking the early_access perk (perk resolved from
    caller.memberId via resolveHandoffCaller + memberHasPerk in server/utils/referralStore.ts —
    server-authoritative, never a client claim). Add server/api/handoff/__tests__/early-access.test.ts:
    early-access opp hidden from non-perk member, shown to perk member, normal opp shown to all.
    Anonymity invariant: never expose who holds which perk; checks use only the caller's own
    session-resolved id.

- id: ranked-match-feed-priority
  title: Add a ranked match-feed endpoint that honors the priority_matching perk
  material: no
  model: sonnet
  depends: [opportunity-persistence-model]
  proof: `npx vitest run server/api/match/__tests__/feed-ranking.test.ts` exits 0
  prompt: |
    There is currently NO ranked, multi-opportunity match feed — only the per-opp double-blind
    handoff. The referral perk system (server/utils/referralStore.ts: memberPerks(memberId),
    evaluatePerks) defines a priority_matching perk with no consumption point yet (explicit TODO).
    Deliverable: add a server endpoint (e.g. server/api/match/feed.get.ts) that returns an ORDERED
    list of opportunities for the calling member (member id resolved server-side via
    resolveHandoffCaller — own data only). When the caller holds priority_matching, boost their
    ordering (surface qualifying matches earlier / more of them); otherwise default order. Respect
    early-access gating (reuse isEarlyAccess + early_access perk so non-perk members never see gated
    opps). Keep masking/anonymity: the feed returns the same coarse anonymized signal the handoff
    view uses — never identities. Add server/api/match/__tests__/feed-ranking.test.ts: a member with
    priority_matching ranks ahead of one without; gated opps excluded for non-perk members; never
    throws on unknown member. No crimson; Pulse tokens only if any view is touched.

- id: referral-standing-materialization
  title: Materialize ReferralStanding (RLS per-member) + transactional update on join
  material: yes
  model: sonnet
  depends: []
  proof: `npx prisma validate` exits 0 AND `npx vitest run server/utils/__tests__/referral-standing-materialization.test.ts` exits 0
  prompt: |
    Today getStandingForMember re-walks the referral chain in-process on every request
    (server/utils/referralStore.ts). Deliverable: materialize it.
    1. Add a Prisma model ReferralStanding { memberId @id, totalCredit Float, updatedAt } + migration,
       with an RLS policy pinning each row to its own member (no cross-member read) — mirror the
       double-blind RLS pattern from the handoff persistence work.
    2. On a referral join, update the affected ancestors' standing transactionally (reuse the
       existing decayed-credit math creditAtDistance / chain walk — do not change the model).
    3. getStandingForMember reads the materialized row when persistence is enabled, falling back to
       the in-memory walk (HANDOFF_PERSISTENCE/in-memory pattern). evaluatePerks stays unchanged.
    4. Add server/utils/__tests__/referral-standing-materialization.test.ts proving the materialized
       total equals the in-process walk for an A→B→C→D chain and that the update is idempotent.
    INVARIANT: standing is private own-data-only; no ranking, no public number. MATERIAL: schema +
    RLS — produce green branch, do not auto-merge.

OPERATOR:
  - Apply the handoff/referral + new Opportunity/ReferralStanding migrations in a real DB env
    (shadow-DB dry run, then prisma migrate deploy); the sandbox cannot run the Prisma engine.
  - Set HANDOFF_PERSISTENCE=prisma (and any referral/standing persistence flag) in env AFTER the
    migrations are applied + RLS verified.
  - concierge_review perk has no surface yet — wire it to a human-ops review queue when that
    surface exists (backlog, no code seam today).
  - One-time scratch cleanup (mount blocked rm in the Cowork sandbox): `git clean -f _parsevue.mjs _attrtest.mjs`.
