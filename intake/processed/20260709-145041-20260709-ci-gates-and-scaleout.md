PROJECT: beethoven

# Scale plan implementation (2026-07-09, owner-directed): move test/build gates to GitHub
# Actions and make runner scale-out one-command. Existing capability to build on:
# INTEGRATION_MODE=pr path in runner.py/pr_integrate.py, per-repo test_cmd in projects table,
# fleet_control gateway, SETUP-MAC2.md manual steps.

- id: relfix-ci-workflow-templates
  title: Generate GitHub Actions gate workflows for every fleet repo (agent branches + dev)
  material: yes
  model: opus
  depends: []
  proof: `python3 -m pytest runner/tests -q -k ci_workflow` exits 0
  prompt: |
    Build runner/ci_workflows.py: generates a .github/workflows/orch-gate.yml for each fleet
    repo from its stack (read projects table test_cmd + repo package.json): triggers on push to
    agent/** and orchestrator/dev; jobs: install (with dependency caching), test, build; concurrency
    group per branch (cancel superseded runs); 25-min timeout. Emit the workflow content per repo
    into .runtime/ci_workflows/<project>/orch-gate.yml AND queue one small intake task per project
    (canonical format, drop into intake/) that commits the workflow to that repo — so each repo's
    workflow lands through its own normal task flow with review. Stack specifics: Nuxt repos need
    node 22 + npm ci; hisanta is Expo (node test runner); beethoven is pytest. Include a
    pareto-2080 note to skip the 2 known Prisma-binary flaky tests in CI (they pass on linux —
    verify, don't assume). Unit tests: template generation per stack fixture validates as YAML and
    contains the required trigger branches.

- id: relfix-pr-native-integration-rollout
  title: PR-native integration mode — CI gates replace local test runs, auto-merge on green
  material: yes
  model: opus
  depends: [relfix-ci-workflow-templates]
  proof: `python3 -m pytest runner/tests -q -k pr_integrate` exits 0
  prompt: |
    Flip integration to PR-native per project, gated + reversible: (1) review/extend
    pr_integrate.py: open_pr pushes the agent branch (already pushed by branch-share) and opens a
    PR against orchestrator/dev; wait_and_merge polls check runs and auto-merges on green
    (squash), returns CHECKS_FAILED with the failing job log tail on red (feed build_fixer);
    (2) per-project rollout flag ORCH_INTEGRATION_MODE_<project> read through the fleet_config
    gateway so we can flip one repo at a time (start: beethoven, then smarter); default remains
    the local train; (3) the merge train treats PR-mode projects as pass-through (card -> PR ->
    poll) so serialization/dedupe/push-verification still apply; (4) local test runs SKIPPED for
    PR-mode projects — CI is the gate; the runner lane frees as soon as the PR opens (this is the
    10-30X: lanes stop blocking on 5-30min test runs). gh CLI or GITHUB_TOKEN REST — match
    whatever pr_integrate.py already uses. Tests: mocked GitHub API lifecycle (open, green-merge,
    red-fail, timeout), rollout flag respected, train pass-through.

- id: relfix-runner-bootstrap-script
  title: One-command runner bootstrap for new machines (Mac or Linux)
  material: no
  model: sonnet
  depends: []
  proof: `bash -n scripts/bootstrap-runner.sh` exits 0 AND `python3 -m pytest runner/tests -q -k bootstrap` exits 0
  prompt: |
    Turn SETUP-MAC2.md's manual steps into scripts/bootstrap-runner.sh: takes SUPABASE_URL +
    SUPABASE_SERVICE_KEY (+ optional repo list), clones/updates all fleet repos to the standard
    ~/Documents layout (read projects table repo_path, apply db.localize_repo_path convention),
    writes runner/.env from .env.example + provided secrets, installs deps (python3, node via
    brew/apt detection, ollama optional flag), installs the launchd plists via
    scripts/setup-scheduler.sh on macOS or a systemd unit on Linux (generate it), registers the
    host in runner_heartbeats, and runs one sentinel cycle as smoke test. Idempotent (re-run =
    update). Keep secrets out of argv (env or prompted). A pytest validates the generated systemd
    unit + env template rendering (pure functions extracted into runner/bootstrap_lib.py).

OPERATOR:
  - After relfix-pr-native-integration-rollout merges: create a GitHub fine-grained PAT with repo+workflow scope for the runner (GITHUB_TOKEN in runner/.env) if gh auth isn't already set; then flip beethoven first via fleet_config ORCH_INTEGRATION_MODE_beethoven=pr and watch one cycle.
  - Runner scale-out is now purchase-gated only: each new Mac mini/Studio or Linux VM = clone repo + `bash scripts/bootstrap-runner.sh`. Approve hardware when ready.
