PROJECT: beethoven

# Priority V15 / Trojun fleet completion DAG. The runtime landed previously; this intake closes the
# integration, measurement, migration, and release gaps without assuming headline multipliers.

- id: v15-00-baseline-contract-audit
  title: Establish the V15 runtime baseline and fleet contract
  material: no
  model: sonnet
  depends: []
  proof: `cd packages/darwin-kernel && npm test -- --runInBand` exits 0, or the repository's documented scoped equivalent exits 0, and the report cites the exact V15 source and commit
  prompt: |
    Audit the already-landed V15 adaptive runtime rather than rebuilding it. Locate the implementation
    corresponding to commits b3d38813 and e2834ef5 (or their merged descendants), enumerate its public
    interfaces, feature flags, persistence formats, telemetry, privacy boundaries, and current consumers.
    Produce a checked-in fleet contract and a gap matrix for galop/racefeed, tomorrow, smarter,
    pareto-2080, apparently, beethoven/orchestrator, vigil, hisanta/santas-secret-workshop,
    predictions/prediction-markets-institute, and Trojun/legacy illuminati. Treat 50X-500X figures as
    hypotheses: never claim them unless reproduced by a benchmark with baseline, dataset, samples,
    percentiles, resource envelope, and correctness parity. Extend existing Darwin kernel modules only.

- id: v15-01-speculative-execution-chains
  title: Harden two-tier speculative execution chains
  material: no
  model: sonnet
  depends: [v15-00-baseline-contract-audit]
  proof: focused tests demonstrate top-3 topology speculation plus fine-grained graph speculation, deterministic winner selection, cancellation, bounded work, and exact parity with non-speculative execution
  prompt: |
    Complete proposal 1 in the shared V15 runtime. Chain topology-level lazy/self-assembling speculation
    with computation-graph speculation. Evaluate at most the three highest-confidence downstream paths in
    parallel, select only a correctness-equivalent result, cancel losers, enforce per-query CPU/memory/time
    budgets, and fall back deterministically. Add repeat-pattern telemetry and controlled benchmarks.

- id: v15-02-fractal-holographic-retrieval
  title: Harden fractal-key holographic associative retrieval
  material: yes
  model: sonnet
  depends: [v15-00-baseline-contract-audit]
  proof: focused tests cover encode/query/consolidate/version migration, bounded reconstruction error, collision isolation, tenancy/privacy boundaries, and measured memory use against full-signal storage
  prompt: |
    Complete proposal 2. Store dominant-scale fractal coefficients as versioned holographic keys, preserve
    constant-time indexed lookup semantics where the implementation supports them, and integrate the memory
    consolidation sleep cycle for safe re-encoding. Add corruption recovery, key-version migration,
    tenant/app isolation, retention controls, and benchmarks that report actual compression and recall.

- id: v15-03-spike-attention-budget
  title: Harden spike-driven attention budgeting
  material: no
  model: sonnet
  depends: [v15-00-baseline-contract-audit]
  proof: deterministic tests prove idle modules receive zero execution budget, threshold crossings receive bounded budget, starvation is prevented, and polling fallback preserves correctness
  prompt: |
    Complete proposal 3. Allocate attention only on significance-threshold spike events, integrate the
    existing neuromorphic router, and make idle cost observable. Add hysteresis/debounce, fairness and
    starvation guards, overload shedding, deterministic replay, and a safe clock-driven fallback.

- id: v15-04-topology-self-distillation
  title: Harden self-distilling topology compression
  material: yes
  model: sonnet
  depends: [v15-00-baseline-contract-audit]
  proof: tests cover merge eligibility, teacher-student parity thresholds, recursive compression manifests, rollback, provenance, and automatic rejection when quality or safety regresses
  prompt: |
    Complete proposal 4. Distill eligible merged topology subgraphs into smaller inference nodes while
    retaining immutable lineage, evaluation fixtures, safety constraints, rollback artifacts, and a shadow
    comparison period. Compression must be opt-in, resource-bounded, reproducible, and automatically refused
    when confidence, calibration, policy behavior, or tail latency exceeds configured tolerances.

- id: v15-05-curriculum-error-correction
  title: Harden curriculum-guided predictive error correction
  material: no
  model: sonnet
  depends: [v15-00-baseline-contract-audit]
  proof: simulations demonstrate time-varying channel prediction, bounded adaptive redundancy, no payload corruption, stable recovery under concept drift, and retransmission metrics versus a fixed baseline
  prompt: |
    Complete proposal 5. Learn per-channel and time-window error risk, pre-emptively tune redundancy within
    strict caps, and use curriculum gap detection to schedule remedial channel-pair training. Preserve
    end-to-end integrity checks, backoff, observability, and deterministic fixed-redundancy fallback.

- id: v15-06-zero-copy-memory-federation
  title: Harden zero-copy holographic memory federation
  material: yes
  model: sonnet
  depends: [v15-02-fractal-holographic-retrieval]
  proof: integration tests cover all ten app identities, shared-buffer key/query/result exchange, ownership and lifetime safety, ACL denial, schema/version mismatch, timeout, crash recovery, and copy/allocation telemetry
  prompt: |
    Complete proposal 6. Extend the existing zero-copy ring-buffer protocol to transport versioned
    holographic keys and correlated results. Enforce per-app capabilities, tenant boundaries, consent,
    redaction, quotas, generation counters, ownership/lifetime safety, and crash recovery. Networked or
    cross-host deployments must use the established secure transport rather than pretending shared memory
    removes a real network boundary. Measure allocations and latency honestly.

- id: v15-07-fractal-causal-attention
  title: Harden multi-scale fractal causal attention graphs
  material: yes
  model: sonnet
  depends: [v15-00-baseline-contract-audit]
  proof: walk-forward tests prevent temporal leakage, cover fine and coarse scales, compare calibrated prediction horizons to single-scale baselines, and report uncertainty plus intervention limitations
  prompt: |
    Complete proposal 7. Add simultaneous causal features at configured temporal scales to the V13 graph,
    align timestamps and missingness correctly, prevent look-ahead leakage, and preserve provenance for each
    edge. Separate observational association from causal claims and expose calibrated uncertainty. Benchmark
    with walk-forward splits and realistic drift.

- id: v15-08-metabolic-spike-budgeting
  title: Harden metabolic spike and capacity budgeting
  material: no
  model: sonnet
  depends: [v15-03-spike-attention-budget]
  proof: load tests cover rest/wake/grow/shrink transitions, prove resting modules do no scheduled work, preserve wake latency SLOs, prevent oscillation/starvation, and report energy/compute proxies against baseline
  prompt: |
    Complete proposal 8. Integrate metabolism state with spike routing and attention budgets so resting
    modules do not receive work, while queued demand wakes capacity predictably. Add hysteresis, minimum
    residency, priority wake paths, overload behavior, capacity ceilings, traceable state transitions, and a
    fail-safe mode that cannot strand critical work.

- id: v15-09-adversarial-anomaly-curriculum
  title: Harden adversarial anomaly curriculum and federation
  material: yes
  model: sonnet
  depends: [v15-00-baseline-contract-audit]
  proof: tests cover curriculum promotion/demotion, privacy-safe federated exchange, poison resistance, holdout novelty evaluation, calibrated thresholds, and false-positive/false-negative reporting
  prompt: |
    Complete proposal 9. Generate increasingly difficult synthetic anomalies from sanitized failure-mode
    schemas, federate learnings without leaking raw app or tenant data, and integrate consensus safely. Add
    poisoning defenses, diversity constraints, immutable provenance, holdout families, calibration, and human
    review for high-impact responses. Report actual detection metrics; do not convert an improvement ratio
    into an accuracy claim.

- id: v15-10-self-assembling-query-topologies
  title: Harden ephemeral self-assembling query compilation topologies
  material: no
  model: sonnet
  depends: [v15-01-speculative-execution-chains, v15-03-spike-attention-budget, v15-08-metabolic-spike-budgeting]
  proof: concurrency tests demonstrate pattern detection, isolated precompiled cluster formation, cache warming, admission control, invalidation and dissolution, with exact query-result parity and throughput/latency measurements
  prompt: |
    Complete proposal 10. Detect repeated normalized query patterns across authorized apps and form bounded,
    purpose-built processing clusters with compiled plans, preconnections, budget reservation, and warmed
    caches. Guard against tenant-key collisions, stale plans, cache poisoning, unbounded cardinality, and
    burst starvation. Dissolve clusters deterministically and preserve a generic execution fallback.

- id: v15-11-orchestrator-fleet-control
  title: Wire V15 through orchestrator fleet control and improvement processing
  material: yes
  model: sonnet
  depends: [v15-01-speculative-execution-chains, v15-02-fractal-holographic-retrieval, v15-03-spike-attention-budget, v15-04-topology-self-distillation, v15-05-curriculum-error-correction, v15-06-zero-copy-memory-federation, v15-07-fractal-causal-attention, v15-08-metabolic-spike-budgeting, v15-09-adversarial-anomaly-curriculum, v15-10-self-assembling-query-topologies]
  proof: orchestrator tests verify capability negotiation, per-app flags, staged rollout, telemetry ingestion, kill switches, rollback, queue idempotency, and dependency-aware release gating for every registered fleet project
  prompt: |
    Add the V15 capability manifest to the orchestrator's existing fleet-control, deployment-binding,
    improvement, and release-train paths. Do not create a second scheduler or deployment system. Provide
    per-capability and per-project flags, compatibility negotiation, schema migrations where genuinely
    required, canary cohorts, telemetry/benchmark collection, circuit breakers, rollback, and queue dedupe.
    Release progression must depend on correctness and measured SLO gates, not headline multipliers.

- id: v15-12-trojun-canonical-identity
  title: Canonicalize Trojun identity while retaining safe legacy aliases
  material: yes
  model: sonnet
  depends: [v15-00-baseline-contract-audit]
  proof: registry and routing tests resolve Trojun canonically, legacy illuminati references remain compatible only where required, no duplicate project identity is created, and release bindings contain a tested Trojun alias plan
  prompt: |
    Complete the Illuminati-to-Trojun migration across orchestrator project registry, routing aliases,
    worktree discovery, queue attribution, deployment bindings, dashboards, documentation, package/CLI names,
    and telemetry dimensions. Preserve explicit backwards compatibility for existing database project IDs,
    API clients, URLs, and queued tasks; do not bulk-rewrite immutable history. Add an idempotent migration
    and rollback. A durable Trojun hostname must be provisioned through the existing release train before
    retiring any working legacy hostname.

PROJECT: racefeed

- id: v15-20-rollout-galop
  title: Roll out all V15 capabilities to Galop
  material: yes
  model: sonnet
  depends: [v15-11-orchestrator-fleet-control]
  proof: Galop scoped unit/integration tests and production build pass; capability contract test covers all ten V15 features with flags-off parity and no cross-tenant data leakage
  prompt: |
    Integrate the shared V15 runtime into Galop/racefeed through the existing Darwin-kernel adapter. Cover all
    ten capabilities, app-specific query patterns and anomaly fixtures, telemetry, flags, consent, resource
    budgets, kill switch, and rollback. Do not fork the shared runtime.

PROJECT: tomorrow

- id: v15-21-rollout-tomorrow
  title: Roll out all V15 capabilities to Tomorrow
  material: yes
  model: sonnet
  depends: [v15-11-orchestrator-fleet-control]
  proof: Tomorrow scoped unit/integration tests and production build pass; capability contract test covers all ten V15 features with flags-off parity and no cross-tenant data leakage
  prompt: |
    Integrate the shared V15 runtime into Tomorrow through its existing Darwin-kernel adapter. Cover all ten
    capabilities, domain-safe anomaly/causal fixtures, telemetry, flags, consent, budgets, kill switch, and
    rollback. Do not introduce autonomous financial, insurance, or legal decisions.

PROJECT: smarter

- id: v15-22-rollout-smarter
  title: Roll out all V15 capabilities to Smarter
  material: yes
  model: sonnet
  depends: [v15-11-orchestrator-fleet-control]
  proof: Smarter scoped unit/integration tests and production build pass; capability contract test covers all ten V15 features with flags-off parity and no cross-tenant data leakage
  prompt: |
    Integrate the shared V15 runtime into Smarter through the existing adapter. Cover all ten capabilities,
    communications privacy, telemetry, flags, consent, budgets, kill switch, and rollback. Raw messages and
    tenant memory must never enter federated training or retrieval without an established authorization path.

PROJECT: pareto-2080

- id: v15-23-rollout-pareto
  title: Roll out all V15 capabilities to Pareto
  material: yes
  model: sonnet
  depends: [v15-11-orchestrator-fleet-control]
  proof: Pareto scoped unit/integration tests and production build pass; capability contract test covers all ten V15 features with flags-off parity and no cross-tenant data leakage
  prompt: |
    Integrate the shared V15 runtime into Pareto through the existing adapter. Cover all ten capabilities,
    provenance-sensitive retrieval and causal fixtures, telemetry, flags, consent, budgets, kill switch, and
    rollback. Preserve calibrated uncertainty and source attribution.

PROJECT: apparently

- id: v15-24-rollout-apparently
  title: Roll out all V15 capabilities to Apparently
  material: yes
  model: sonnet
  depends: [v15-11-orchestrator-fleet-control]
  proof: Apparently scoped unit/integration tests and production build pass; capability contract test covers all ten V15 features with flags-off parity, privilege boundaries, and no cross-tenant data leakage
  prompt: |
    Integrate the shared V15 runtime into Apparently through the existing adapter. Cover all ten capabilities,
    legal/privilege-safe retrieval and anomaly fixtures, telemetry, flags, consent, budgets, kill switch, and
    rollback. Do not weaken CADE or human-review gates.

PROJECT: beethoven

- id: v15-25-rollout-orchestrator
  title: Roll out all V15 capabilities to the orchestrator runtime
  material: yes
  model: sonnet
  depends: [v15-11-orchestrator-fleet-control]
  proof: runner and orchestrator integration suites pass with all V15 flags off and in canary mode; queue correctness, single-instance, release gating, and rollback tests pass
  prompt: |
    Consume the shared V15 runtime in the orchestrator's live query, memory, anomaly, topology, and budgeting
    paths using existing schedulers and fleet control. Exercise all ten capabilities under canary flags and
    prove legacy behavior when disabled. Preserve queue invariants and single-instance protections.

PROJECT: vigil

- id: v15-26-rollout-vigil
  title: Roll out all V15 capabilities to Vigil
  material: yes
  model: sonnet
  depends: [v15-11-orchestrator-fleet-control]
  proof: Vigil scoped unit/integration tests and production build pass; capability contract test covers all ten V15 features with flags-off parity and no cross-tenant data leakage
  prompt: |
    Integrate the shared V15 runtime into Vigil through the existing adapter. Cover all ten capabilities,
    security-focused anomaly fixtures, telemetry, flags, consent, budgets, kill switch, and rollback. Ensure
    speculative or distilled paths cannot bypass enforcement or evidence capture.

PROJECT: santas-secret-workshop

- id: v15-27-rollout-hisanta
  title: Roll out all V15 capabilities to Hisanta
  material: yes
  model: sonnet
  depends: [v15-11-orchestrator-fleet-control]
  proof: Hisanta scoped unit/integration tests and production build pass; capability contract test covers all ten V15 features with flags-off parity, child/family safety boundaries, and no cross-tenant data leakage
  prompt: |
    Integrate the shared V15 runtime into Hisanta through the existing adapter. Cover all ten capabilities,
    child/family-safe data boundaries, telemetry, flags, consent, budgets, kill switch, and rollback. Default
    federation and adversarial learning to the strictest applicable privacy posture.

PROJECT: prediction-markets-institute

- id: v15-28-rollout-predictions
  title: Roll out all V15 capabilities to Predictions
  material: yes
  model: sonnet
  depends: [v15-11-orchestrator-fleet-control]
  proof: Predictions scoped unit/integration tests and production build pass; capability contract test covers all ten V15 features with flags-off parity and no cross-tenant data leakage
  prompt: |
    Integrate the shared V15 runtime into Predictions through the existing adapter. Cover all ten capabilities,
    leakage-free temporal evaluation, calibrated forecasts, telemetry, flags, consent, budgets, kill switch,
    and rollback. Never use future labels or market outcomes in speculative precomputation benchmarks.

PROJECT: illuminati

- id: v15-29-rollout-trojun
  title: Roll out all V15 capabilities to Trojun
  material: yes
  model: sonnet
  depends: [v15-11-orchestrator-fleet-control, v15-12-trojun-canonical-identity]
  proof: Trojun scoped unit/integration tests and production build pass; capability contract test covers all ten V15 features with flags-off parity, enforcement equivalence, and no cross-tenant data leakage
  prompt: |
    Integrate the shared V15 runtime into Trojun, using the legacy illuminati queue identity only until the
    canonical registry migration is live. Cover all ten capabilities, CADE/enforcement equivalence,
    telemetry, flags, consent, budgets, kill switch, and rollback. Update user-facing identity to Trojun
    without breaking supported clients or historical evidence.

PROJECT: beethoven

- id: v15-30-fleet-release-verification
  title: Release-train and verify the complete V15 Trojun fleet rollout
  material: yes
  model: sonnet
  depends: [v15-20-rollout-galop, v15-21-rollout-tomorrow, v15-22-rollout-smarter, v15-23-rollout-pareto, v15-24-rollout-apparently, v15-25-rollout-orchestrator, v15-26-rollout-vigil, v15-27-rollout-hisanta, v15-28-rollout-predictions, v15-29-rollout-trojun]
  proof: release-train evidence records each app's merged commit, migrations, deployment URL and immutable deployment ID, health/capability smoke results, telemetry gates, benchmark report, rollback artifact, and verified Trojun canonical alias
  prompt: |
    After every dependency is merged and green, run the existing release train for staged canary rollout;
    never deploy directly. Apply reviewed idempotent migrations through the established migration path, verify
    all ten apps plus orchestrator end to end, and record immutable evidence. Compare measured results to
    baselines and label unsupported 50X-500X expectations explicitly as unverified. Promote capability by
    capability only when correctness, privacy, safety, cost, and tail-latency gates pass; otherwise roll back
    the affected capability/app and queue a bounded remediation task.

OPERATOR:
  - Review and approve material rollout gates, database migrations, cross-app memory federation, and staged production promotion through the existing release train; no direct production deployment is authorized.
