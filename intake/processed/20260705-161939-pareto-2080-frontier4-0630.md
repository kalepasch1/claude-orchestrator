PROJECT: pareto-2080

# Frontier batch 4 — new optimization SCOPE (generative product design, state-contingent policy),
# self-improvement science (causal experimentation, outcome-truth), and more concrete-dollar engines
# (tax-return optimization, household CRO, transaction-rights). Same pattern as the existing
# server/utils/ engines: pure ESM, no deps, seedable RNG, a node:test in tests/,
# explain.makeExplanation on every decision. Inputs passed as arguments (no DB dependency). Proof for
# each = its node:test passes. Don't regress the suite beyond the 2 known pre-existing failures
# (agentLedger, bookingSaga — local Prisma-binary only).
#
# Invariants (never break): free (no fee/affiliate/data-sale); non-discretionary by default (prepare,
# human commits); non-custodial; agent-of-the-user; writeback on every completed transaction;
# reconstructed vendor/underwriting models stay CONSUMER-FACING/INTERNAL. Legal-adjacent engines are
# UPL-safe (detect + prepare + cite; user files).

- id: generative-product-design
  title: Specify the ideal product and let vendors bid to BUILD it (design-by-demand, not menu-shopping)
  material: no
  model: opus
  depends: []
  proof: `node --test tests/generativeProductDesign.test.js` exits 0
  prompt: |
    Add server/utils/generativeProductDesign.js (pure). Stop shopping the menu; specify the OPTIMAL
    product for the user (rate + term + no-prepay + features) and turn it into an RFQ vendors bid to
    build. Compose pricingGridReconstruction (which vendors could profitably build it — feasibility from
    their cost curve) + syntheticBundle (assemble from components if no single vendor fits). Provide
    `specifyIdealProduct(userGoal, constraints)` -> a structured product spec, `feasibleVendors(spec,
    grids)` -> vendors whose cost curve supports it + their expected price, and `toBuildRfq(spec,
    feasibleVendors)` -> the design-by-demand RFQ. Tests: an ideal spec is emitted from a goal; a vendor
    whose cost curve can't support the spec is excluded; the build-RFQ names feasible vendors + expected
    price; falls back to a synthetic bundle when no single vendor is feasible.

- id: state-contingent-policy
  title: A user-authored state-contingent standing-policy language over autonomyPolicy
  material: no
  model: opus
  depends: []
  proof: `node --test tests/stateContingentPolicy.test.js` exits 0
  prompt: |
    Add server/utils/stateContingentPolicy.js (pure). Extend autonomyPolicy from static caps into a
    user-authored standing STRATEGY: "if rate < X auto-execute; if income drops auto-defer; if provider
    adds a fee auto-exit". Define a small, safe, declarative rule DSL {when: condition-over-world-state,
    then: action-intent, bounds} and an evaluator that, given current world state, emits the triggered
    action-intents — each still routed through the EXISTING autonomyPolicy.decideAction (so all caps /
    kill-switch / prepare-by-default invariants still apply; a triggered rule can at most PROPOSE, never
    bypass the gate). Provide `parsePolicy(rules)`, `evaluatePolicy(policy, worldState)` -> triggered
    intents, `validatePolicy(policy)`. Tests: a rate-threshold rule fires only below threshold; a
    triggered intent still passes through decideAction (over-cap => hold, not auto); malformed rule
    rejected; conflicting rules resolved deterministically.

- id: causal-experimentation
  title: Opt-in, benefit-only controlled experiments with real causal estimation (self-improvement science)
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/causalExperimentation.test.js` exits 0
  prompt: |
    Add server/utils/causalExperimentation.js (pure). Turn the platform into a proper experiment engine:
    which negotiation tactic / timing / framing actually CAUSES better outcomes, via controlled assignment
    + causal estimation (not just bandit correlation). Provide `assign(userId, experimentId, {arms,
    seed})` (deterministic hash-based assignment), `estimateEffect(outcomes)` -> {ate, ci, pValue,
    nPerArm} (difference-in-means + a simple regression-adjusted estimator), and `guardrails(experiment)`
    that enforce OPT-IN + BENEFIT-ONLY (no arm may be expected-worse for the user than the current
    default; harmful arms are rejected before assignment). Material: it experiments on real users — the
    ethics guardrail is load-bearing. Tests: assignment is balanced + deterministic; ATE recovered on
    seeded data; an arm that is expected-worse-than-default is rejected by guardrails; CI narrows with n.

- id: outcome-truth-graph
  title: Extend provider honesty from price to the whole experience (did they actually deliver?)
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/outcomeTruthGraph.test.js` exits 0
  prompt: |
    Add server/utils/outcomeTruthGraph.js (pure). Beyond rate honesty: track whether a provider actually
    delivered — was the deal real, did fine print bite, was service as promised, did fees appear post-
    close. Aggregate k-anonymously into a per-provider outcome-truth score across dimensions
    {priceHonesty, feeSurprise, serviceDelivery, finePrintRisk}. Compose providerHonesty (price side).
    Provide `recordExperience(providerId, dims)`, `scoreProvider(experiences, {minCohort})` ->
    per-dimension + composite score + badge (k-anon suppressed below floor). Feed into rfqTargeting as a
    reputation multiplier. Tests: consistent bad-service reports lower the score; k-anon suppresses thin
    cohorts; composite weights the dimensions; badge thresholds correct.

- id: tax-return-optimization
  title: Assemble the optimal legal tax return end-to-end (every credit/election), prepared for filing
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/taxReturnOptimization.test.js` exits 0
  prompt: |
    Add server/utils/taxReturnOptimization.js (pure). Beyond deduction-shopping: assemble the optimal
    LEGAL return — every credit, the best elections, filing-status choice, above/below-the-line routing —
    and prepare it for filing. Compose the EXISTING tax engines (taxOptimizer.js, deductionOptimizer.js,
    taxAdvantaged.js) + the tax-prep flow. Provide `optimizeReturn(taxProfile)` -> {filingStatus,
    creditsFound[], electionsChosen[], estRefundOrOwedUsd, vsBaselineUsd, formsPrepared[],
    requiresUserReview:true}. UPL/PTIN note: prepares + explains; the actual e-file is an operator rail and
    the user reviews/signs. Material: touches tax filing. Tests: a missed credit is found vs baseline;
    filing-status optimization picks the lower-tax status; every return carries requiresUserReview +
    vsBaseline.

- id: household-cro
  title: Predictive net-worth / tail-risk defense (household chief risk officer)
  material: no
  model: opus
  depends: []
  proof: `node --test tests/householdCro.test.js` exits 0
  prompt: |
    Add server/utils/householdCro.js (pure). Downside protection is worth more than upside optimization
    for most households. Continuously monitor tail risks to net worth — job loss, market crash, uninsured
    event, single-asset concentration, liquidity crunch — and pre-position defenses BEFORE they hit.
    Cross-pollinate Tomorrow's CRO-in-a-box posture to retail (advisory/plan-only; see tomorrow CLAUDE.md).
    Compose the EXISTING resilience.js + insuranceHealth.js + fraudProtection.js. Provide
    `assessTailRisks(worldModel)` -> ranked {risk, probability, netWorthAtRiskUsd, defense} and
    `prepositionDefenses(risks, stack)` -> ordered defensive moves (emergency-fund sizing, coverage,
    de-concentration, liquidity). Plan-only, non-custodial. Tests: a concentrated single asset flags
    de-concentration; thin emergency fund flags buffer-building; ranks by net-worth-at-risk.

- id: transaction-rights-automation
  title: Auto-pursue everyday-purchase rights (warranty, price protection, chargeback, returns)
  material: yes
  model: sonnet
  depends: []
  proof: `node --test tests/transactionRights.test.js` exits 0
  prompt: |
    Add server/utils/transactionRights.js (pure). Extend rights-enforcement from financial products to
    everyday purchases: warranty claims, price-protection refunds, chargebacks (Reg Z / card-network),
    returns within window, extended-warranty payouts. From purchase records, DETECT eligible claims and
    PREPARE each (rule cited, facts, draft) for the user to file / one-tap. UPL-safe: detect + prepare +
    cite; user files. Material: disputes/rights path. Tests: a price drop within the protection window
    flags a refund; a defective item in warranty flags a claim; an unauthorized charge flags a chargeback;
    every output carries the rule + requiresUserFiling.

OPERATOR:
  - causal-experimentation: opt-in + benefit-only is a load-bearing ethics guardrail — confirm the consent + IRB-lite review posture before any live experiment runs. No arm may be expected-worse than the user's current default.
  - tax-return-optimization: PTIN/e-file is an operator rail; the engine prepares + explains, the user reviews/signs. Confirm the tax-prep compliance posture (not tax advice = the return is the user's; "see a CPA/EA for judgment").
  - transaction-rights-automation + (frontier3) rights-enforcement: UPL-safe detect+prepare+cite only; user files. Live filing is an operator rail.
  - household-cro: plan-only / non-custodial (advisory); any defensive move that buys a product routes through the normal prepare-then-user-commit flow.
