PROJECT: beethoven

# Repo: /Users/kpasch/Documents/beethoven/claude-orchestrator (the orchestrator's own UI lives in web/).
# NOTE: this project is the orchestrator itself — these tasks modify the runner's own dashboard repo.
# Treat the schema task as material (human approves the merge).

- id: run-logs-table-and-shape
  title: Add a run_logs table + realtime publication and a typed LogLine shape
  material: yes
  model: sonnet
  depends: []
  proof: `cd web && npx vue-tsc --noEmit` exits 0
  prompt: |
    A prior session added web/components/LogView.vue (per-line level styling via icon+text, copy-line/
    copy-all, follow-tail toggle, role="log" aria-live) but it renders SAMPLE lines with a visible
    "sample · stream not bound" badge and a TODO(bind-stream). Build the real backing store.
    1. Add an idempotent migration under supabase/migrations/ (NNN_run_logs.sql): create table run_logs
       { id uuid pk default gen_random_uuid(), run_id uuid references runs(id) on delete cascade,
       ts timestamptz not null default now(), level text not null check (level in
       ('debug','info','warn','error')), source text, message text not null }; index on (run_id, ts);
       add the table to the supabase_realtime publication so the web app can subscribe. Keep RLS
       consistent with the repo's existing tables (read policy matching how runs/tasks are read).
    2. In web/, define the typed LogLine shape (ts/level/source/message) in one shared module so
       LogView and index.vue import it from a single source of truth (the prior session noted the
       state→tone map is currently mirrored — co-locate the LogLine type with it).
    Do NOT bind the UI yet (next task). MATERIAL: schema + realtime publication — produce green
    branch, human approves merge. Proof: the web app typechecks clean with the new shared type.

- id: logview-bind-realtime-stream
  title: Bind LogView + dashboard to the run_logs realtime stream
  material: no
  model: sonnet
  depends: [run-logs-table-and-shape]
  proof: `cd web && npx vue-tsc --noEmit` exits 0
  prompt: |
    Replace LogView's sample data with the real stream. In web/pages/index.vue, subscribe to the
    run_logs realtime channel (mirror the existing Supabase realtime subscription pattern already used
    for tasks/runs/runner_heartbeats), maintain a bounded in-memory ring buffer of recent LogLine rows,
    and pass them to <LogView :lines>. Remove the "sample · stream not bound" badge when bound; keep the
    sample fallback only when no rows exist. Preserve LogView's follow-tail behavior, copy affordances,
    icon+text level styling, and role="log" aria-live. Stamp lastEventAt on new log rows so the Mission
    Control strip's live clock reflects log activity too. Token-driven; no logo change.

OPERATOR:
  - Apply the run_logs migration + realtime publication in the orchestrator's Supabase
    (ref eatfwdzfurujcuwlhdgj) after the green branch is approved.
  - One-time scratch cleanup (mount blocked rm in the Cowork sandbox): `rm web/_psfc_tmp.mjs`.
