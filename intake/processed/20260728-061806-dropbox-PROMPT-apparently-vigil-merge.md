# apparently: merge Vigil into Apparently, gaming exams for all jurisdictions, PLOEH unification, code+email risk surveillance

SUBMITTED-BY: kale@smrter.us (operator) via Cowork strategy session 2026-07-27. (Rename this file to PROMPT-… to activate.)

Operator decision of record (updated 2026-07-27, round 2): **Vigil FULLY combines into Apparently — one application, one codebase, one auth, one nav.** Not a side module. Vigil (/Users/kpasch/Documents/vigil, separate live repo, ~4,900 files, 138 vigil_* tables) is absorbed; agency-vs-entity separation becomes RLS roles inside Apparently; Vigil the deployed app runs until parity, then the brand retires (operator-approved). Method remains contract-extraction (vigil/VIGIL_STRATEGY_AND_MVP.md): extract stable contracts into a versioned package; do NOT naively copy 4,900 files — but the end state is full absorption, and Illuminati is a first-class part of the combined stack (continuous mock-exams + email scanning + decision coordination run through it at all times). Target outcome: a regulated entity NEVER needs another application for anything a regulator requires. Strategy reference: PORTFOLIO_STRATEGY_V2 Part 4.4 (beethoven repo root).

## 1. Merge architecture (contract-extraction, phased)
- Phase A — package extraction: create `packages/supervision-kernel/` (in the apparently repo) containing Vigil's stable contracts + pure engines: AuthorityPack schema + evaluator + industrialCompiler + sourceRegistry (vigil/server/domain/authority/*), intensive-review catalog+engine (36 executable thresholded rules incl. gaming-integrity), deterministic CAMELS + receipts (vigil/server/domain/financial/*), examinations (executablePlaybook, multiRegulatorCompiler, sampleEngine), lifecycle catalog/engine, relationship protocol state machine (vigil/server/domain/relationship/*). Port Vigil's 43 Vitest specs alongside — they are the merge-correctness proof.
- Phase B — data migration: port the data catalogs verbatim as versioned seed data: official-sources (~90 pinned regulator sources — the citation spine), sample-exams (40 scenarios: catalog/federal/expanded), authority-packs demo JSONs, document-templates, examiner-agents (59 defs), regulatory-coverage (28 regulator profiles + readiness computation), verticals. Schema: port the needed subset of vigil's 33 Supabase migrations into apparently's migration stream (idempotent IF NOT EXISTS per apparently conventions; RLS default-deny; agency/entity separation preserved).
- Phase C — surfaces: mount an `app/pages/supervision/` (or `/exams/`) area in Apparently reproducing the core Vigil workspace flows (examinations, authority, intensive-reviews, financial, coverage). Reuse ht-ui. The regulator-facing (agency) side and entity-facing side keep their RLS separation.
- Phase D — cutover: the unmerged apparently branch `origin/codex/vigil-receiver` (authenticated VIGIL receiver + middleware allowances) should be reviewed and either merged or superseded. Vigil the deployed app keeps running until parity; brand retirement is an operator decision, not this task.
- WARNING: vigil/SPEC.md and its 7.1MB .orch-context-cache.json are misleading artifacts — do not treat SPEC.md as ground truth; it describes an unrelated app.
- Proof: supervision-kernel test suite (ported specs) green inside apparently; `npm run typecheck` clean; seed data loads; one end-to-end exam scenario (fdic-risk-management or gaming-bsa-exam) runs through the ported engine.

## 2. Gaming operator exams — ALL jurisdictions (gaming is the initial entry point)
- Current coverage (post-merge) is only NV + NJ + NIGC/tribal + FinCEN/IRS. Industrialize using the ported industrialCompiler + promotion lifecycle (catalogued → verified → compiled → golden_replay → examiner_approved): add pinned official sources + authority packs + intensive-review rules + exam playbooks for every US commercial gaming jurisdiction (~30 states: PA, MI, IL, IN, OH, CO, LA, MS, MO, IA, KS, MD, MA, NY, WV, AZ, AR, CT, DE, FL, KY, ME, MT, NE, NH, NM, ND, OK, OR, RI, SD, TN, VA, WA, WY as applicable — verify which have commercial gaming regulators and skip those without), plus tribal (NIGC deepened per-compact where public), plus the international majors: UKGC, Malta MGA, Ontario AGCO/iGO, Gibraltar, Isle of Man, Curaçao. One jurisdiction per task; each task pins real official sources (statutes/regs/MICS/audit guides) with canonical URLs and produces at minimum: source catalog entries, one authority pack draft with checks[], and one exam/licensing playbook. Sources must be real and cited — never invent URLs; mark anything unverifiable as `catalogued` not `verified`.
- Proof per jurisdiction: pack compiles through the evaluator; citations resolve (URL format validation); tests assert rule thresholds parse.

## 3. PLOEH/trigger-oracle hardening (the regulatory-risk-swap rail to Tomorrow)
- Consolidate the TWO parallel signing schemes (server/utils/ploeh-s2s.ts signs `${ts}.${rawBody}`; server/engines/ploeh/s2s.ts signs `ploeh:{route}:{ts}`) into one, with a durable nonce/replay store (Supabase table, not in-process Map that fails soft) — coordinate the scheme choice with Tomorrow's verifier (tomorrow/server/utils/otc/ploeh/s2sBridge.ts).
- Build the push loop: a cron `regulatoryTriggerPublisher` that watches reg-fact-store lifecycle changes and event-taxonomy evaluations, and POSTs trigger-spec registrations and trigger-proofs to Tomorrow's /api/apparently/ploeh/* endpoints; with retry + confirmation logging.
- Build an ops dashboard page: oracle firings, manual_review backlog (stale/divergent observations), spec registry, proof history.
- Proof: integration test with mocked Tomorrow endpoint; replay attempt rejected across two simulated instances; dashboard renders from fixtures.

## 4. Un-dormant code-risk and email-risk surveillance
- Wire `server/engines/_dormant/legal-compliance--code-binding-analyzer.ts` (and drift detector) to a git-push webhook endpoint + repo-connect flow; findings persist with severity + cited pattern + recommended clause/disclosure; logged via ai-call-logger.
- Extend email-triage from intake-routing to risk surveillance: an inbound scan pass producing findings (severity, cited quote, category: privilege/UPL/PII/regulatory/commitment) into a findings table with trend/backlog queries. This is the Apparently side of the "review communications like code" pipeline; Smarter will consume the same findings shape.
- Proof: pushing a fixture diff yields expected findings; fixture mailbox yields findings with citations; RLS on new tables verified by npm run check:rls.

## 5. Full regulatory lifecycle matrix (users never leave the app)
- For each regulator × entity-type in the coverage catalog, enumerate + implement EVERY interaction class as templates + executable checklist + filing generation + status tracking: licensing/registration; periodic reporting (call reports, MSB filings, GGR reports); examinations (playbooks + workpapers + full exam books/guidance/regulator manuals as pinned sources); **change-of-control submissions** and **new employee/controller/key-person additions** (Apparently's disclosure control-graph + change-report engines already compute the deltas — wire them straight into filing generation); correspondence (no-action letter requests, interpretive-letter requests, extension requests, objection/response — Vigil's relationship protocol state machine is the engine); remediation (MRA/consent-order tracking, CAPs). Gaming regulators first, FI stack second.
- Proof: for two pilot regulators (Nevada GCB, FinCEN) every interaction class has a working template + generated filing artifact + status object; matrix coverage report generated from the catalog.

## 6. Regulatory omniscience corpus
- Ingestion program: EVERYTHING the covered agencies have ever published, as far back as digitized — all no-action/interpretive letters (back to the 1930s where available), every exam manual version, speeches/testimony, advisories, enforcement actions, proposals + comment files, and case law interpreting each agency (courtlistener.ts integration exists; CFTC/Congress/Federal Register feeds exist). Pipeline: fetch → dedupe → embed (pgvector) → knowledge-graph link (regulator/topic/rule/entity-type) → temporal validity (temporal-state engine) → **precedential-weight grade** (statute > rule > formal guidance > NAL > enforcement > speech > staff FAQ). Build per-agency source adapters incrementally (CFTC + FinCEN + Nevada first); never fabricate sources — anything unverifiable stays `catalogued`.
- This corpus backs the graded option ladders (Illuminati sidecar): risk gradients must cite the authority chain (e.g., "NAL 26-14 explicitly permits → low risk"; "isolated 2025 enforcement, distinguishable → elevated").
- Proof: corpus API returns authority chains with weights for fixture queries; ingestion idempotent; per-agency coverage stats page.

## 6b. Apparently OS — "launch and run your own boutique firm" surface
- Apparently OS is the product OTHER lawyers use to launch + run their own independent boutique firms ON Apparently (distinct from Apparently Law, which is our first-party firm — see HOLD-PROMPT-apparently-law-site). Build it as a workspace inside the Apparently app that composes what this merge already provides: the multi-entity org graph (item 7 — for the firm's own entity + its clients), the full regulatory-lifecycle matrix (item 5 — the firm files/reports/exams on behalf of its clients), the omniscience corpus (item 6), Smarter coordination (embedded), Illuminati interception, and Madeus fleets (embedded per the madeus-platform prompt) — all under the firm's own tenant with its own constitution.
- Boutique lifecycle: apply → entity formation filings (reuse the lifecycle matrix) → practice profile + jurisdictions/expertise tags → malpractice via the scored data product (concept 11) → provisioned firm tenant → LISTED in the boutique directory that Apparently Law's routing consumes (item 3 of the apparently-law prompt) for tier-2 referrals. Token/subscription billing (align with the apparently-law token unit).
- Boundary: these are INDEPENDENT firms (customers), not part of Apparently Law. Data isolation between boutique tenants is a hard requirement (RLS + tenant scoping, same as the madeus multi-tenant isolation).
- Proof: a fixture boutique tenant provisions, appears in the directory (S2S), files a client change-of-control off the lifecycle matrix, and is isolated from another boutique tenant's data.

## 7. Multi-entity org graph (in-house teams with subsidiaries/sisters/parents)
- Promote the disclosure control-graph to the tenancy layer: parent/subsidiary/sister/affiliate structure as a first-class object. One GC sees consolidated risk registers, per-entity licenses/filings/exams, cross-entity approvals; graph edits (new controller, ownership change) auto-generate the change-of-control filings from item 5. This is the Apparently expression of the multi-startup cockpit concept.
- Proof: fixture org graph renders consolidated + per-entity views; an ownership-change edit produces a draft CIC filing.

## 8. Perpetually-updating living memos/documents (HEADLINE product — strategy 5.3)
- Every memo/opinion becomes a LIVING document bound to the omniscience corpus (item 6) + the PLOEH trigger oracle: when any relied-upon authority changes (statute/rule/NAL/enforcement posture/compliance-requirement added or removed, favorable OR adverse — e.g. sweeps casinos becoming illegal in a state), the memo auto-revises (approval-gated per config), flags exactly what changed + which conclusions moved, and notifies the holder. Sold on subscription — the point-in-time-PDF killer.
- Two layers sold together: (1) information protection (you always know the moment your position changes); (2) optional economic protection — an outcome perpetual (Tomorrow, via the hedge-recommendation gate) that pays on adverse change / monetizes favorable change. The memo IS the trigger spec: Apparently authored the conclusion + monitors the authorities → it is the natural settlement oracle. Wire memo→trigger-spec emission to the PLOEH publisher (item 3).
- Proof: a fixture memo re-revises when a linked authority changes in the corpus, diffs the moved conclusions, and emits a trigger spec; subscription/notify path tested.

## 9. Autonomous regulatory-interaction studio + coalitions + winning-argument corpus (strategy 5.4/6.2)
- Drafts strategic regulatory submissions favoring the specific user: NAL requests, interpretive/guidance requests, NPRM/rulemaking comments, exemption/relief petitions, extension requests. Uses the omniscience corpus (cite favorable authority + distinguish adverse), the user's facts/posture, CADE adversarial pre-read, and approval-probability scoring of drafting strategies. Positive outcomes obtained emit as positive regulatory triggers (item 3 / Tomorrow 4.2).
- COALITION ENGINE: detect when multiple platform users draft toward the same regulatory position (same docket/rule/argument cluster via corpus embeddings) and propose a COMBINED submission — each user opts in with explicit approval (disclosed genuine joint comment, NEVER astroturf — hard guardrails: real parties, disclosed, individually approved, genuine interest), engine synthesizes the strongest combined letter drawing the best argument from each, files with all signatories. Works across in-house teams AND boutiques.
- WINNING-ARGUMENT CORPUS: a research pass over the omniscience corpus ingesting the FULL lifecycle of past matters (proposal → every comment → agency response-to-comments → final rule → interpreting litigation) and scoring WHICH arguments moved the outcome (adopted language traced to comments; arguments the agency credited/rejected; positions that survived judicial review) → per-agency, per-argument-type empirical "what wins" playbook. The studio drafts every submission from that playbook (lead with historically-prevailing structures, pre-empt objections that historically killed submissions, cite authorities that actually persuaded).
- Proof: generates a NAL request + rulemaking comment with cited authority chains + a playbook-derived strategy ranking; coalition detection groups two fixture users on the same docket and produces a combined letter gated on both approvals; winning-argument scorer ranks arguments for a fixture historical matter.

## 9b. All-document prove-then-complete drafting (strategy 6.1)
- Apply the reusable "prove-then-complete" primitive (shared contract with the Tomorrow prompt item 10b) to EVERY Apparently generator: legal opinions, memos, filings, license applications, regulatory submissions, contracts, compliance policies. Generate the impressive analytical core / first sections under the token plan as a value proof; gate the complete deliverable (full document set, submission-ready filing, complete application) behind tokens/subscription. Configurable teaser tranche per doc type; adaptive paywall line where feasible (learned per doc type).
- Proof: two Apparently doc types (e.g. legal opinion + license application) produce a usable teaser and gate completion behind a token/subscription check.

## 12. Two always-on jurisdiction swarms (strategy 6.3)
- (A) Maintenance/red-team swarm: runs continuously against every jurisdiction's authority packs/sources/exam protocols/lifecycle matrices — re-verifies every pinned source is live+current (amendments/repeals/superseded guidance), adversarially tries to break each executable check, cross-checks fresh regulator publications + the corpus ingest, files findings that auto-correct (approval-gated) or open review. Specialize the orchestrator coherence-bot pattern to regulatory content as a STANDING swarm (not a periodic scan).
- (B) Expansion swarm: every day, comprehensively researches + stands up NEW jurisdictions (US states, tribal compacts, international regimes beyond the initial gaming set; extend to full FI/MSB/securities map over time) at the same intensity — full 4.4 industrialization pipeline run autonomously + continuously.
- Both report to the standing-bot dashboard with per-jurisdiction freshness + coverage KPIs (put these on the public marketing surface later — "every jurisdiction, verified daily, adversarially tested").
- Register both as coordination bots; dual-register any crons; fail-open.
- Proof: maintenance swarm flags a fixture stale/broken source + check; expansion swarm stands up a fixture new jurisdiction through the pipeline; KPIs computed.

## 10. Gaming expansion optimizer (strategy 5.5a)
- From the operator's existing business model + already-submitted data, compute per-jurisdiction: (a) whether any NEW information is required (surface "one-click, no new data" expansions), (b) projected time-to-approval + total cost, (c) strategic fit → a single X/100 expansion score, top-N ranked. Compose the disclosure control-graph + form-population + fee-calculator engines. Score projections calibrate off exam/approval outcomes over time.
- Proof: fixture operator yields ranked jurisdictions with scores + a "no new data required" flag where inputs already exist + a generated filing for the top pick.

## 11. Department/division portals + negotiation rooms (strategy 5.5b)
- Per-function portals (legal, compliance, strategy, finance, marketing, product, ops, HR/payroll), each with role-appropriate superintelligence, custom approvals, and guidance in that function's vocabulary (Madeus clarify + CADE co-think translate intent). Cross-functional decisions route into negotiation rooms for multi-stakeholder collaboration, including OUTSIDE stakeholders via private-account links or email-only participation (reuse the Tomorrow war-room email-bridge pattern). This is the concrete "everyone contributes / everyone is an engineer" build; every input is attributed (steering_events) and constitution-bounded.
- Reuse/extend Smarter's embedded coordination surface (per the smarter prompt) for the approval/board mechanics; the portals are the per-department skins.
- Proof: two department portals render role-scoped approvals + guidance; a decision opens a negotiation room; an email-only outside stakeholder input bridges in and is attributed.

## 13. Protection storefront + living-memo-funds-its-own-worst-case bundles (operator round 5; strategy 7.2, 8.4)
- Protection storefront: for each CEPL-graded material finding in the risk register, auto-generate the matching protection product — a swap (ECPs) or a standby line / loan feature (non-ECPs) via Tomorrow S2S, priced live behind Tomorrow's recommendation gate (always-available via synthetic pool). The register becomes a storefront: exposure → dollar cost to transfer → one-click protect. Findings from email (item on email surveillance), the Illuminati code sidecar, and exams all flow into the same storefront.
- Flagship bundle (LEAD THE APPARENTLY PITCH WITH THIS): the living memo (item 8) + a standby line as one subscription — the memo watches the law and, if the law moves against the client, (a) auto-revises to say exactly what changed AND (b) opens a pre-committed standby line (Tomorrow S2S) to fund the response. Advisory + monitoring + warning + money in one product. Sell curated per-segment Protection Packs (Founder / Gaming Operator / Personal Wealth) as token/subscription bundles with standby line + living memo pre-composed. Included-protection tiers: premium subscriptions bundle a baseline standby allowance (member "has" protection, doesn't buy it); above baseline burns tokens.
- Retail note: non-ECP clients always reach protection via the licensed-lender/broker standby rail (they face a bank/SBLOC product, never a swap) — this is Apparently's small-business/retail on-ramp.
- NON-ECP EARLY-STAGE ACCESS (operator round 5; strategy 9.3 — push hard): the storefront must serve pre-revenue / early-stage / non-ECP users at the EARLIEST stage cheaply. A pre-revenue startup can carry an enforcement-defense standby or regulatory-change standby for a small monthly fee from onboarding day 1, funded behind the wall by the tranche pool via Tomorrow S2S. Protection grows with them (loan-wrapped standby → included membership protection → ECP direct hedge as they scale). Wire the storefront's protect action to the Tomorrow standby primitive + day-1 warehouse (never decline — synthetic-pool floor always returns a real offer).
- CADE AGREEMENT SUITE lives HERE (strategy 9.1; Tomorrow consumes via S2S): CADE authors + optimizes + maintains the versioned, counsel-blessed template library for the entire standby economy — the contingent credit facility / standby-line agreement (9.1a loan-commitment-fee structure), the ISDA hedge docs, the tranche/participation docs (bilateral-ECP vs Reg D note), the ECP capacity-provider agreement, and all disclosures. The maintenance/red-team swarm (item 12) continuously re-tests the standby-fee characterization as law evolves. Templates auto-populate per deal and a counsel-approved change propagates everywhere instantly.
- Proof: a fixture finding renders a priced protect action reaching a non-ECP via the standby rail; CADE auto-populates each agreement in the suite for a fixture standby deal + flags a characterization drift when a fixture rule changes; living-memo+standby bundle provisions a subscription that (on a mocked reg change) revises the memo AND opens a mocked standby line; a Protection Pack bundle prices via tokens.

## Conventions
- Follow apparently CLAUDE.md strictly: typed Supabase helpers only, selectModel() (no hardcoded models), Zod on inputs, RLS default-deny, idempotent migrations, ai-call-logger on every AI call. Repo hygiene: do not add root scratch files.
