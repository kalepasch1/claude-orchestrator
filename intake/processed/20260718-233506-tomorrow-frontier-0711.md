PROJECT: tomorrow

# Frontier improvements for Tomorrow, embedded in its OS surfaces (DecisionBudget/5-95 tiles already live in
# shared/contracts; war-room; autonomy cockpit). LOAD-BEARING: additive only; posture-NEUTRAL; must NOT regress the
# posture-compliance suite (server/utils/compliance/postureInvariants) — no order book / no click-to-execute / bilateral
# IOI-only; ECP gate intact. Conventions (tomorrow/CLAUDE.md): pure server/utils (no Date.now/Math.random/I/O — pass
# time/seed in); requireVerifiedUser on sensitive routes; migration name-check + `npm run lint:migrations`; structured
# `[CODE] message` errors; KV for ephemeral, Prisma for system-of-record. INSPECT FIRST — extend existing engines
# (riskFabric, liquidityMining, fairness, privacyBudget, verifiableProof); do NOT duplicate.

- id: tomorrow-capital-allocator
  title: Pure portfolio EV x risk allocation PROPOSER across verticals (propose-only, bilateral-safe)
  material: yes
  model: opus
  depends: []
  proof: `npx vitest run --config vitest.pure.config.ts server/utils/allocation/capitalAllocator.pure.test.ts` exits 0 AND `npx tsc --noEmit --skipLibCheck` exits 0
  prompt: |
    Add server/utils/allocation/capitalAllocator.ts — a PURE function that ranks Tomorrow's books/verticals by expected
    value adjusted for risk (reuse riskFabric + fairness engines; pass time/seed/inputs in — no Date.now/Math.random)
    and returns a PROPOSED capital/attention allocation within per-vertical floors/ceilings and the global risk budget.
    It ONLY proposes — it emits an IOI/hedge-sizing proposal that feeds the EXISTING approval flow; it must NOT execute,
    auto-match, or create any order-book path (posture: bilateral-only). Add capitalAllocator.pure.test.ts asserting:
    EV×risk ranking respects floors/ceilings + global budget; a starved high-EV vertical is proposed more; NO execution
    side-effect exists. If an allocator already exists, extend it; keep the posture suite green.

- id: tomorrow-capital-allocator-cockpit
  title: Allocation cockpit tile (5/95) — proposed split as Outcome + one-knob + proof reveal
  material: no
  model: sonnet
  depends: [tomorrow-capital-allocator]
  proof: `npx tsc --noEmit --skipLibCheck` exits 0
  prompt: |
    Add a components/app allocation tile rendering the proposer output in the OS 5/95 pattern (DecisionBudget contract
    in shared/contracts): the recommended allocation as the Outcome (fact), a single Recommended "stage IOIs" action
    that routes into the existing approval flow (never auto-executes), and a "see the math" reveal (per-vertical EV,
    risk, rationale = the proof). Use components/ui design-system + Chart.js as elsewhere; add a war-room/cockpit entry.
    Display + propose only; a human commits. Typecheck clean.

- id: tomorrow-judgment-compression
  title: Operator-preference model over war-room approvals (pre-decide non-material; float novel/material)
  material: yes
  model: opus
  depends: []
  proof: `npx vitest run --config vitest.pure.config.ts server/utils/approvals/operatorPreference.pure.test.ts` exits 0
  prompt: |
    Add server/utils/approvals/operatorPreference.ts — a PURE predictor over prior approval decisions returning
    { suggestedDecision, confidence, similarPriorIds } for a new card. Fail-closed: NEVER suggest auto for material,
    irreversible, ECP-gated, or novel cards — those always require an explicit human tap; only non-material,
    high-confidence, has-precedent cards may surface as "tap to confirm." Emit a decisions-per-outcome metric. Pure
    (seed/inputs passed in). Add operatorPreference.pure.test.ts: material/ECP card never auto-suggested; high-confidence
    non-material w/ precedent pre-fills; low-confidence none; metric emitted. Must not regress the posture suite.

- id: tomorrow-judgment-compression-ui
  title: Embed pre-decisions + batching into the war-room approvals surface
  material: no
  model: sonnet
  depends: [tomorrow-judgment-compression]
  proof: `npx tsc --noEmit --skipLibCheck` exits 0
  prompt: |
    Extend the war-room / approvals surface so each card shows the operator-preference suggestion + confidence + a
    "why (similar prior)" reveal, batches same-kind NON-material cards into one "confirm all like this," and floats
    novel/material/ECP-gated cards to the top as the only real-judgment items. Show a decisions-per-outcome stat.
    Material/irreversible/ECP cards can NEVER be inside a batch confirm — enforce in UI. Reuse components/ui + tokens;
    add a command-palette/sidebar entry. Typecheck clean.

- id: tomorrow-federated-core
  title: Privacy-preserving cross-tenant aggregation core (on the existing privacy budget)
  material: yes
  model: opus
  depends: []
  proof: `npx vitest run --config vitest.pure.config.ts server/utils/federation/secureAggregate.pure.test.ts` exits 0
  prompt: |
    Add server/utils/federation/secureAggregate.ts — a PURE core that turns per-tenant signals into DP/secure-aggregate
    contributions (hashed fingerprints / noised aggregates; NEVER raw data), building ON the existing privacyBudget
    engine. Expose contribute(tenantSignal, epsilonBudget) -> aggregateDelta and combine(aggregates) with strict
    privacy-budget accounting; tenant isolation preserved. Add secureAggregate.pure.test.ts: raw data never leaves;
    epsilon budget is debited and enforced (over-budget rejected); combine is order-independent; a single tenant cannot
    be re-identified from the aggregate. Live cross-tenant transport is OPERATOR (secrets) — this task is the core +
    tests only. Must not regress the posture suite.

OPERATOR:
  - Live cross-tenant federation transport (the actual multi-tenant network) needs secrets + counsel on the privacy posture; the core + budget accounting ship now, wiring is operator-set.
  - Any Prisma persistence added for allocation/approval history must pass `npm run lint:migrations` (name-check) and is deployed by the operator, not the runner.
