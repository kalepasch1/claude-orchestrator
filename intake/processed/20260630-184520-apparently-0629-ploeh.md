PROJECT: apparently

# Perpetual Legal-Opinion Event Hedge (PLOEH) — APPARENTLY side (oracle / trigger / living memo).
# Pairs with intake/tomorrow-0629-ploeh.md (the perpetual instrument + underwriting + settlement side),
# joined by a signed S2S seam. Builds on the Hive (server/engines/hive/*), the pre-mortem memo, the
# diffusion forecaster (regime-diffusion.ts), enforcement-velocity index, and examiner-twin queued in
# intake/apparently-0629.md and intake/apparently-0629-frontier.md (referenced by prose, not cross-file depends).
# FIRST STEP for the agent: grep the repo for any existing "perpetual legal opinion event hedge" / PLOEH
# scaffolding (e.g. `grep -ri "perpetual.*opinion\|ploeh\|opinion.*hedge" server`) and EXTEND it; only create
# new files where none exists.
# BRIGHT LINE (non-negotiable, test-enforced): every trigger is a PARAMETRIC / INDEX event over observable
# reg_facts — NOT indemnity of actual loss. This keeps it a swap, not insurance. Reuse product-legality-engine.ts
# + prediction-markets-regulatory.ts guards; mirror Tomorrow's assertParametricOnly/assertNotStatutoryInsurance.
# Conventions: typed SupabaseClient<Database>; callClaudeStructured + selectModel; logger; idempotent
# name-checked migrations; pure cores unit-testable; material/legal steps human-gated.

- id: compliance-event-taxonomy
  title: Canonical compliance-event taxonomy + deterministic trigger predicates over reg_facts
  material: no
  model: opus
  depends: []
  proof: "`npx vitest run tests/engines/ploeh/event-taxonomy.test.ts` exits 0 (evaluateTrigger fires correctly across all event types against reg_fact fixtures; a stale/divergent observation returns manual_review, never an auto-fire)"
  prompt: |
    Define every compliance/regulatory risk as a machine-readable parametric trigger. Engine
    server/engines/ploeh/event-taxonomy.ts: a ComplianceEventType union covering the FULL surface —
    statutory_ban, license_denial, license_revocation, suspension, rule_change, fee_increase, bond_increase,
    effective_date_reached, enforcement_action (incl. enforced-under-existing-law), support_entity_prohibition,
    sponsor_bank_loss, charter_loss, rate_cap_enacted, true_lender_ruling, registration_lapse, deadline_missed,
    authority_supersession (a cited statute/case the opinion relied on is superseded/overruled), examiner_adverse_action,
    sanctions_finding, aml_adverse_finding. Each ComplianceTriggerSpec = {eventType, subject, jurisdiction,
    predicate} where predicate is a DETERMINISTIC function over reg_facts (subject + jurisdiction +
    lifecycle_stage transition, e.g. proposed_bill→effective, or enforcement_observed=true). Pure
    evaluateTrigger(spec, regFacts, asOf) → {fired:boolean, basis, quality:'ok'|'stale'|'divergent'} with a
    settlement-quality gate (multi-source / staleness / divergence → manual_review, mirroring Risk Studio's
    trigger oracle). Unit-test fire/no-fire across event types + the quality gate. NO indemnity-of-loss triggers —
    enforce the parametric bright line in code + test.

- id: compliance-risk-vector-oracle
  title: Living-memo risk vector — per-event probability/time/severity (the pricing oracle)
  material: no
  model: opus
  depends: [compliance-event-taxonomy]
  proof: "`npx vitest run tests/engines/ploeh/risk-vector.test.ts` exits 0 (computeRiskVector returns per-event {probability, timeToEventDist, severity, confidence}; raising the diffusion probability or enforcement-velocity input monotonically raises the relevant event probability)"
  prompt: |
    The perpetual memo's risk metrics ARE the hedge's pricing oracle. Engine
    server/engines/ploeh/risk-vector.ts: computeRiskVector(client, {subject, jurisdictions, eventTypes}) →
    a ComplianceRiskVector = per (eventType × jurisdiction) {probability, timeToEventDist, severity/exposure,
    confidence}. Compose existing signals: forecastSubjectDiffusion (regime-diffusion.ts) for spread
    probability, the enforcement-velocity index for acceleration, examiner-twin for adverse-action likelihood,
    and reg_facts lifecycle position for imminence; blend with base rates. Pure blending core
    blendRiskVector(inputs) unit-tested for monotonicity. This vector is emitted to Tomorrow for pricing —
    keep it a clean, serializable, signed-payload-ready shape. Advisory; no execution.

- id: perpetual-memo-binding
  title: Bind a living opinion to a hedge; version the risk-vector timeseries on every fact change
  material: yes
  model: sonnet
  depends: [compliance-risk-vector-oracle]
  proof: "migration name-checks clean AND `npx vitest run tests/engines/ploeh/memo-binding.test.ts` exits 0 (a reg_fact change touching the memo's subjects re-emits + version-appends the risk vector; the bound hedge id is preserved across versions)"
  prompt: |
    Make the opinion perpetual and bound to its hedge. Migration supabase/migrations/<next>_ploeh_bindings.sql:
    table ploeh_bindings (id, opinion_id, org_id, subject, jurisdictions jsonb, bound_hedge_ref, trigger_specs
    jsonb, status, created_at) + ploeh_risk_vector_history (binding_id, version, risk_vector jsonb,
    emitted_at) + RLS. Engine server/engines/ploeh/memo-binding.ts: createBinding(...), and
    onRegFactChange(client, factChange) that recomputes computeRiskVector for every binding whose subjects are
    touched and version-appends to history (the memo never "matures" — it re-emits). Wire onRegFactChange into
    the reg-fact-store change path (reg-fact-store.recordFact already returns FactChange). Unit-test the
    re-emit + versioning (mocked oracle + supabase). MATERIAL: schema.

- id: compliance-event-observer
  title: Event observation + settlement-quality gate + proof-of-trigger attestation
  material: yes
  model: opus
  depends: [compliance-event-taxonomy]
  proof: "`npx vitest run tests/engines/ploeh/event-observer.test.ts` exits 0 (a fired trigger backed by quality sources mints a proof-of-trigger that verifies offline; stale/divergent sources → manual_review; a non-fired trigger mints nothing)"
  prompt: |
    Decide, provably, whether a trigger fired. Engine server/engines/ploeh/event-observer.ts:
    observeTrigger(client, spec, asOf) evaluates the predicate against current reg_facts + the event-source
    feeds, applies the settlement-quality gate (median/multi-source, staleness window, cross-source divergence →
    manual_review), and on a clean fire mints a proof-of-trigger via the attestation-signing-core util
    (server/utils/attestation-signing.ts from intake/apparently-0629-frontier.md) binding {spec, firedAt,
    reg_fact ids/hashes, observation sources}. verifyTriggerProof(envelope) is offline-verifiable. This is what
    makes the payout a clean parametric settlement, not a disputed claim. Unit-test fire+proof / stale→manual /
    non-fire / tamper. MATERIAL: it authorizes a downstream payout.

- id: ploeh-s2s-bridge-apparently
  title: Signed S2S feed of trigger specs + risk vectors + trigger proofs to Tomorrow
  material: yes
  model: sonnet
  depends: [compliance-risk-vector-oracle, compliance-event-observer]
  proof: "`npx vitest run tests/api/ploeh-s2s.test.ts` exits 0 (signMessage/verify round-trips; a tampered or expired envelope is rejected; the payload matches the agreed PLOEH schema consumed by tomorrow-0629-ploeh)"
  prompt: |
    Build the apparently→tomorrow seam (mirror the existing gaming S2S bridge pattern, HMAC-SHA256, 300s window,
    constant-time compare; env PLOEH_S2S_SECRET). Routes under server/api/ploeh/s2s/: emit current trigger specs,
    the latest risk vectors per binding, and trigger proofs when an event fires. Define the shared payload schema
    in shared/schemas/ploeh.ts (Zod) so both repos agree. Unit-test sign/verify/expiry/tamper + schema validation.
    MATERIAL: cross-system trust boundary. Do NOT send funds or instructions — this feed is risk data + proofs only;
    Tomorrow prices/settles on its side.

- id: hedge-funded-prepositioning
  title: Structure a fired-event payout as funding the fallback-license pre-positioning
  material: yes
  model: sonnet
  depends: [perpetual-memo-binding]
  proof: "`npx vitest run tests/engines/ploeh/funded-prepositioning.test.ts` exits 0 (on a fired event, planFundedRemediation converts the payout into a concrete fallback-license filing plan with file-by dates; the plan is drafted, not executed)"
  prompt: |
    Make the hedge deliver CONTINUITY, not just cash. Engine server/engines/ploeh/funded-prepositioning.ts:
    planFundedRemediation(client, {binding, firedEvent, payout}) composes predictive-prepositioning
    (server/engines/hive/prepositioning.ts from the frontier intake) so a fired ban/loss event converts the hedge
    payout into the fallback-license filing the client now needs (e.g. CA dual-currency banned → file the
    prediction-market/promotional fallback), with file-by dates and the cross-vertical artifact reuse plan
    (shared-artifacts.ts). Pure planning core unit-tested. Output is a DRAFTED plan + needsApproval; never
    auto-files. MATERIAL: it directs payout/filings.

- id: parametric-supplement-issuance
  title: On trigger fire, auto-draft the supplement opinion alongside the hedge payout
  material: yes
  model: sonnet
  depends: [compliance-event-observer]
  proof: "`npx vitest run tests/engines/ploeh/supplement-issuance.test.ts` exits 0 (a verified fired event generates a supplement-opinion draft for the bound opinion with needsApproval=true; no event → nothing)"
  prompt: |
    Legal + financial remediation in one fire. Engine server/engines/ploeh/supplement-issuance.ts: on a verified
    trigger proof for a bound opinion, auto-draft the supplement opinion (reuse opinion-staleness-detector.ts +
    opinion-response-drafter.ts + the lifecycle-impact-propagation work) capturing exactly what changed and its
    effect on the prior conclusion. Returns a DRAFT + needsApproval (human-gated issuance — never auto-issue legal
    work). Unit-test the fire→draft path (mocked drafters). MATERIAL: legal work product.

- id: ploeh-ops-surface-apparently
  title: Living-memo + bound-hedge dashboard (risk-vector timeseries, trigger status, supplement)
  material: no
  model: sonnet
  depends: [perpetual-memo-binding, ploeh-s2s-bridge-apparently]
  proof: "`npx vue-tsc --noEmit` exits 0 AND the surface renders the risk-vector history, live trigger status per spec, the bound hedge ref/mark (from the S2S feed), and any drafted supplement — against real columns"
  prompt: |
    Surface PLOEH in the app/ops UI (reuse existing dashboard scaffolding). One view per binding: the perpetual
    memo's current conclusion + confidence, the per-event risk-vector timeseries (ploeh_risk_vector_history),
    live trigger status per ComplianceTriggerSpec, the bound hedge reference + latest mark/funding (read from the
    Tomorrow S2S feed), fired-event proofs, and any drafted supplement/funded-remediation plan awaiting approval.
    Read-only over the new tables + S2S feed; no new write paths. Prove with vue-tsc clean + column-accurate reads.

OPERATOR:
  - Counsel sign-off on the swap-vs-insurance characterization for compliance-EVENT parametric swaps specifically (this is the legal crux — the trigger must be a genuine index/observable event, not indemnity of the client's actual loss). Confirm ECP-gating + that each event type sits inside the permitted swap allowlist before any binding goes live.
  - Provision PLOEH_S2S_SECRET in both apparently and tomorrow prod env (and GitHub secrets); it is the trust boundary between the oracle and the underwriter.
  - Provision the attestation signing key (shared with the frontier intake's attestation-signing-core) so trigger proofs carry real signatures.
  - Deploy the new migrations (ploeh_bindings, ploeh_risk_vector_history) to prod after name-checking against the live Supabase project.
  - Human approval remains required before any supplement opinion is issued and before any funded-remediation filing is executed.
