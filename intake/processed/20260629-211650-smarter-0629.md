PROJECT: smarter

- id: pageheader-rollout-remaining
  title: Apply the PageHeader primitive + text-2xl heading scale to the remaining pages
  material: no
  model: haiku
  depends: []
  proof: `npx vue-tsc --noEmit` exits 0 AND `grep -L "PageHeader" pages/triage.vue pages/screener.vue pages/conflicts.vue pages/clients/index.vue` returns nothing
  prompt: |
    A prior session added a reusable PageHeader component and rolled the new text-2xl heading scale
    across 7 top page titles (board, campaigns, governance, clients, triage, screener, conflicts).
    ~42 lower-traffic pages still use ad-hoc/inconsistent title markup.
    Deliverable: migrate the remaining page-level titles to <PageHeader title="…" :subtitle="…">,
    importing it the same way the already-migrated pages do. Work through pages/**/*.vue that render
    a page-level <h1>/title that is NOT yet PageHeader; do not touch in-card or section sub-headings.
    Keep all existing props/behavior; this is presentational only. Verify `npx vue-tsc --noEmit`
    stays at 0 errors (no new type errors introduced). Token-driven; no logo changes.

OPERATOR:
  - (none — purely presentational, typecheck-gated.)
