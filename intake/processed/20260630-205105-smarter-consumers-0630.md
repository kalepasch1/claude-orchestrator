PROJECT: smarter

# Consumer wiring for Smarter, sequenced AFTER the tomorrow shared contracts pin (cross-app ordering doctrine).
# Depends on smarter-test-harness (from smarter-comms-intel intake) for proofs. Mirror tomorrow shared/contracts into
# types/integration.ts as this repo already does for warRoomSync/xappSignal. Ground truth: server/utils/governance.ts
# (trust dial), server/utils/policy.ts (KILL_SWITCH_GATED includes 'external_share'), server/api/warroom/bridge.post.ts
# (WarRoomBridge push/pull), server/utils/obligations.ts, getCockpit(). Stack Nuxt3+Supabase; @ht/ui tokens.

- id: smarter-5-95-adopt
  title: Apply the shared autonomy-budget + DecisionReceipt to the obligations/matter surface
  material: no
  model: sonnet
  depends: [smarter-test-harness]
  proof: `npx vitest run tests/five-95.test.ts` exits 0 AND `npm run build` exits 0
  prompt: |
    Consume the tomorrow shared autonomy-budget engine + decisionReceipt contract (mirror into types/integration.ts).
    Wrap the obligations/matter surface so each item renders as a 95% resolved Outcome (fact) + ONE Recommended
    discretionary knob + on-demand proof = sourceQuote + the existing policy receipt + a signed DecisionReceipt.
    Logic/types tested; a clean `npm run build` is part of the proof. Do not bypass governance/policy.

- id: smarter-family-war-room
  title: Trusted-circle war room over warRoomSync for flagged obligations/scams
  material: yes
  model: opus
  depends: [smarter-test-harness]
  proof: `npx vitest run tests/family-warroom.test.ts` exits 0
  prompt: |
    Reuse the WarRoomBridge push/pull + receipt pattern (server/api/warroom/bridge.post.ts, server/utils/agents.ts) to
    open a trusted-circle/family room when an item is flagged (overdue obligation or high-risk inbound comm). Route every
    share/escalation through governance.ts trust level + policy.ts (external_share is KILL_SWITCH_GATED — honor the
    global kill-switch). Enforce the PII barrier on shared content (refs/summaries, not raw bodies). Tests: flag -> room
    opens; external_share blocked under kill-switch; trust-level gating of auto vs awaiting_approval. Mock the network.

- id: constitution-versioning
  title: Version + hash-pin the Policy Constitution; expose the hash receipts cite
  material: yes
  model: sonnet
  depends: [smarter-test-harness]
  proof: `npx vitest run tests/constitution-hash.test.ts` exits 0
  prompt: |
    Make server/utils/policy.ts's constitution serialize to a stable, versioned hash (constitutionHash) that
    DecisionReceipt cites. Bumping any rule changes the hash; the same ruleset reproduces the same hash. Expose
    getConstitution() -> { version, hash, rules }. Tests: deterministic hash, hash changes on rule edit, receipts carry
    the in-force hash. This is the RegTech anchor — what rules governed any action, verifiable after the fact.

OPERATOR:
  - Counsel sign-off on the UPL boundary before family-war-room auto-shares external legal content; keep external_share kill-switch-gated until signed.
  - Live cross-app delivery (cockpit feed, family-room invites) needs the S2S secret + sibling endpoints — wire after contracts pin.
