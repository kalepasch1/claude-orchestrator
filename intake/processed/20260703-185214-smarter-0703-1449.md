PROJECT: smarter

# WAVE THREE — the fusion/compounding layer. Every task here DEPENDS on ids from the two
# prior intakes (smarter-0702: metrics-ingest, predictive-timing, revenue-attribution;
# smarter-0703-1346: synthetic-audience-sim, genconfig-bandit-spike, crosspost-graph,
# social-gap-detector) so it only runs AFTER those are built + merged. All new-file or
# additive-DB except genconfig-consumer (the single social.ts editor — chained after the
# other social.ts editor, predictive-timing, so no merge conflict). Prod build gate =
# `nuxt build`. Social data plane = orchestrator Supabase eatfwdzfurujcuwlhdgj via
# server/utils/orch.ts. See smarter/HANDOFF_SOCIAL_OMNICHANNEL.md (v24/v25).

- id: preflight-orchestrator
  title: Pre-publish gate fusing reception + quality + best-time into one queue step
  material: no
  model: sonnet
  depends: [synthetic-audience-sim, predictive-timing]
  proof: `cd smarter && npm run build` succeeds AND POST /api/growth/preflight with {postId} runs reception+quality+timing and either queues the post at best_post_slot or returns a hold reason — verified by the row's status/scheduled_at on eatfwdzfurujcuwlhdgj
  prompt: |
    Compose the three checks into one pre-flight, as a NEW endpoint only (do not edit social.ts
    or socialTick.ts). `server/api/growth/preflight.post.ts` ({ postId, minReception?, minBand? }):
    read the post from eatfwdzfurujcuwlhdgj; run `simulateReception` (server/utils/audienceSim.ts,
    from synthetic-audience-sim) and read the CADE band already on post.meta.cade; if reception <
    threshold OR band in (weak,untenable) → set status='draft' with meta.preflight={held,reasons};
    else schedule at `best_post_slot(account_id, platform)` (RPC from predictive-timing) via the
    existing `schedule_social_post`/setPost path and set status='queued'. Reuse existing RPCs by
    import/rpc call only. Fail-soft when a dependency isn't present. CANDIDATE-SHARED: a generic
    reception×quality×timing pre-flight for any content app.

- id: genconfig-consumer
  title: Wire the gen-config bandit into generation + stamp gen_config for attribution
  material: no
  model: sonnet
  depends: [genconfig-bandit-spike, metrics-ingest, predictive-timing]
  proof: `cd smarter && npm run build` succeeds AND a generated post carries meta.gen_config {model,temperature,promptVariant} on eatfwdzfurujcuwlhdgj so record_post_metrics attributes reward to that arm
  prompt: |
    Close the gen-config loop. Smallest diff to server/utils/social.ts generateContent: before
    generating, call the recommender from server/utils/genConfigBandit.ts (from genconfig-bandit-spike)
    to pick {model,temperature,promptVariant}; use it for the LLM call; and stamp the chosen config
    into the returned object so callers persist it to post.meta.gen_config (the generate/fillDrafts
    handlers already spread meta). This is the ONLY wave-three task that edits social.ts — it depends
    on predictive-timing (the other social.ts editor) so they never run in parallel. Keep behavior
    identical when no bandit data (default arm). Do not touch socialTick.ts.

- id: audience-overlap-populate
  title: Populate the cross-venture audience-overlap graph from real follower data
  material: no
  model: sonnet
  depends: [crosspost-graph]
  proof: `cd smarter && npm run build` succeeds AND running the new endpoint writes growth_audience_overlap rows (app_a, app_b, overlap) on eatfwdzfurujcuwlhdgj so crosspost_targets ranks by real overlap
  prompt: |
    crosspost-graph created growth_audience_overlap + crosspost_targets but left overlap as a
    placeholder. Add NEW endpoint `server/api/growth/audience-overlap.post.ts` that estimates
    pairwise overlap between the apps' connected accounts (from any follower/engager data available;
    where none, fall back to a shared-topic proxy from recent post hashtags/topics) and upserts
    growth_audience_overlap. Do NOT edit crosspost.post.ts. Live follower scraping is OPERATOR
    (Chrome/API); ship the proxy path so build + a dry-run populate succeed without it.

- id: icp-scheme-rank
  title: Rank the scheme marketplace by predicted fit for a specific ICP
  material: yes
  model: sonnet
  depends: [synthetic-audience-sim]
  proof: live SQL on eatfwdzfurujcuwlhdgj shows a new `rank_marketplace_for_icp(p_icp text, p_objective text)` function (additive; existing rank_marketplace untouched) AND `cd smarter && npm run build` succeeds
  prompt: |
    Let a brand-new account start with the scheme most likely to work for ITS audience, not just
    the globally-best one. Additive migration to eatfwdzfurujcuwlhdgj: NEW function
    `rank_marketplace_for_icp(p_icp, p_objective)` that returns marketplace schemes blended by
    outcome_score AND a predicted-fit score. Add `server/api/growth/scheme-fit.get.ts` that calls
    simulateReception (server/utils/audienceSim.ts) on each scheme's template against the ICP to
    produce the fit score, then orders. Do NOT modify the existing rank_marketplace function or
    loops.post.ts. Reference: promote_scheme_run, rank_marketplace, growth_scheme.

- id: northstar-kpi
  title: North-star rollup — answered-from-strategy rate (quality×reception×timing→conversion)
  material: no
  model: sonnet
  depends: [revenue-attribution]
  proof: `cd smarter && npm run build` succeeds AND GET /api/growth/northstar returns {published, passed_preflight, converted, answered_from_strategy_rate, revenue} computed via a new read-only RPC on eatfwdzfurujcuwlhdgj
  prompt: |
    One number that ties it together. Additive read-only migration to eatfwdzfurujcuwlhdgj: NEW
    function `social_northstar(p_app text default null)` returning published posts, how many passed
    pre-flight (meta.preflight not held / CADE band strong|defensible), tracked conversions +
    revenue (growth_social_link, from revenue-attribution), and the answered-from-strategy rate
    (converted / published). Add read-only endpoint `server/api/growth/northstar.get.ts`. New files
    + new function only; do not edit existing RPCs or pages.

OPERATOR:
  - audience-overlap-populate: live follower/engager overlap needs Chrome/API access to each platform's audience — the proxy (shared-topic) path ships without it; wire real overlap once platform read access is available.
  - These all run only after the smarter-0702 + smarter-0703-1346 tasks merge (enforced via depends); no action needed beyond approving the material merges (icp-scheme-rank).
