PROJECT: apparently

- id: factory-unblock-chatgpt-reconcile-apparently-recover-stash-17
  title: Unblock chatgpt-reconcile-apparently-recover-stash-17 (stuck BLOCKED)
  material: no
  proof: npm run typecheck
  prompt: |
    Task 'chatgpt-reconcile-apparently-recover-stash-17' has been stuck in state BLOCKED for over 60 minutes. Recorded note: cowork-executor-v6.5: NO-ARTIFACT-JUSTIFIED — stash@{17} (67138a2364e2, 25 files / +3905) NOT ported as-is: the API layer is an unauthenticated cross-org IDOR. This is the precise defect the task prompt warned about ("one read an attacker-suppliable request header as the caller identity"), found by diffing against master rather than trusting the additive-only verdict. EVIDENCE: all 11 new GET routes are 7-8 line wrappers with ZERO auth — grep for requireOrgMembership|requireAuth|requireAdmin|getServerSession|requireUser returns 0 across every one. Each takes the tenant key straight from the query string, e.g. server/api/compliance-risk/report.get.ts: `const orgId = q.orgId as string || "org_001"; return generateComplianceStatusReport(orgId)`. Any unauthenticated caller can read ANY org's compliance, disclosure, tax and deal-health data by changing ?orgId=. Master's convention for exactly this is requireOrgMembership from ~/server/utils/org-membership (see server/api/activity/feed.get.ts). "+3905/-0, purely additive" is true and irrelevant — additive can still be a new attack surface. WHAT IS MISSING to unblock: (1) every one of the 11 routes must derive orgId from the authenticated session via requireOrgMembership, never from getQuery, and the 'org_001' default must be deleted; (2) the 10 engines (~2200 lines, genuine domain value, no security surface of their own) can then be ported behind those gated routes, but need TS tests — the stash ships only two PYTHON tests, and both cover the ops-hive-dashboard.html surface, not the engines, so the family rule "ported code must come with a passing test" is currently unmet; (3) ops-hive-dashboard.html needs an owner decision, it is a loose root-level HTML surface. Recommend re-queuing as a build task to write the gated routes + engine tests, not as a mechanical stash port. No commit fabricated; stash left read-only and untouched.
    
    Diagnose the root cause (build failure, merge conflict, flaky test, or a genuine blocker needing a design decision) and fix it, or if it's a duplicate/obsolete task, close it with a reason. Do not just retry blindly — read the actual error.
