PROJECT: pareto-2080

# CADE wiring — Pareto/2080: high-stakes personal-finance decisions on the existing pure engines.
# Engine: @darwin/kernel/cade (built+tested). Spec: claude-orchestrator/CADE_IMPLEMENTATION_HANDOFF.md.
# Repo rules: pure engines in server/utils/* with tests/*.test.js (node:test), ESM, seedable RNG,
# money in USD; `$fetch as any` cast; layout:false rule for self-nav pages.

- id: cade-embedder-par
  title: CADE Embedder (kernel hashEmbedder fallback)
  material: no
  model: haiku
  depends: []
  proof: `node --test server/utils/cade/tests/embedder.test.js` exits 0
  prompt: |
    Build server/utils/cade/embedder.js implementing kernel `Embedder` using kernel hashEmbedder(64)
    (no external embedding dependency needed at Pareto's stakes). node:test unit test. See HANDOFF §2.1.

- id: cade-roster-json
  title: Personal-finance Standing Roster (investors/economists + specialists)
  material: no
  model: sonnet
  depends: []
  proof: `node --test server/utils/cade/tests/roster.test.js` exits 0
  prompt: |
    Build server/utils/cade/roster/personal.json + loader: role='authority' Bogle/Markowitz/
    Kahneman-style minds; role='advisor' tax + decumulation + actuarial specialists; role='adversary'
    (tax-hike + bad-sequence scenarios); role='reviewer' (future-self, IRS). Diversity in priorsTag.
    node:test asserts roles present. See HANDOFF §3.1/§4.4.

- id: cade-montecarlo-bridge-par
  title: Monte Carlo bridge over retirementMonteCarlo.js → Distribution
  material: no
  model: sonnet
  depends: []
  proof: `node --test server/utils/cade/tests/montecarlo.test.js` exits 0
  prompt: |
    Build server/utils/cade/monteCarlo.js wrapping retirementMonteCarlo.js + uncertainty.js into a
    kernel `Distribution` (samples[]); seedable RNG for determinism. node:test verifies summary
    percentiles. See HANDOFF §5.3.

- id: cade-invoker-par
  title: CADE Invoker (personal-finance grounding + tiering)
  material: no
  model: sonnet
  depends: [cade-embedder-par]
  proof: `node --test server/utils/cade/tests/invoker.test.js` exits 0
  prompt: |
    Build server/utils/cade/invoker.js implementing kernel Invoker.invoke. Ground in the relevant
    pure-engine outputs (e.g. decumulation, taxAdvantaged); tier cheap=Haiku/deep=Sonnet via the
    repo's Anthropic model choices; return PersonaPosition with citations to the engine results.
    node:test with a mocked model. See HANDOFF §2.2.

- id: cade-determine-decisions
  title: runDetermination on high-stakes personal decisions
  material: yes
  model: opus
  depends: [cade-invoker-par, cade-roster-json, cade-montecarlo-bridge-par]
  proof: `node --test server/utils/cade/tests/determine.test.js` exits 0
  prompt: |
    Wrap the high-stakes calls (decumulation sequencing, windfall allocation, Roth conversion,
    buy-vs-rent, insurance crossover): build an IssueSpec with issue.distribution from the Monte
    Carlo bridge, call runDetermination with a lighter roster/budget. node:test asserts a
    determination + value + proof on a Roth-conversion sample. See HANDOFF §4.4.

- id: cade-endpoint-tile
  title: CADE decision endpoint + 5%-surface tile (one number + dissent toggle)
  material: no
  model: sonnet
  depends: [cade-determine-decisions]
  proof: `node --test server/utils/cade/tests/endpoint.test.js` exits 0
  prompt: |
    Add GET /api/personal/cade/:decision returning the determination; add a deathTimer/Twin tile that
    shows the single number + confidence, with a "see the debate" toggle revealing dissent + the
    proof. Endpoint handler is unit-tested against a mocked determination. See HANDOFF §4.4.

- id: cade-proof-keep
  title: Persist + render the plain-language proof the user keeps
  material: no
  model: sonnet
  depends: [cade-determine-decisions]
  proof: `node --test server/utils/cade/tests/proof.test.js` exits 0
  prompt: |
    Persist Determination.proof (Prisma or JSON record) and render a plain-language, signed,
    replayable record the user can keep. Round-trip + verify test. See HANDOFF §3.3.

OPERATOR:
  - Set DARWIN_SIGNING_PRIVATE_KEY_PEM in Pareto prod env (shared proof anchor) if signed proofs are enabled.
