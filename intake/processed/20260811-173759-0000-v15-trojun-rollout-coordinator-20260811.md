PROJECT: beethoven

- id: v15-fleet-rollout-coordinator
  title: Coordinate and close the complete V15 Trojun fleet rollout
  material: yes
  model: sonnet
  depends: []
  proof: the queue contains all 24 idempotent slugs defined in intake/000-v15-trojun-fleet-rollout-20260811.md (or their terminal history), their dependency graph is valid, and v15-30-fleet-release-verification cannot run before every app rollout succeeds
  prompt: |
    Own the end-to-end processing of the approved V15/Trojun initiative. The canonical detailed DAG is
    `intake/000-v15-trojun-fleet-rollout-20260811.md`; ingest or reconcile every one of its 24 idempotent
    tasks without duplicating existing live/terminal work. Preserve the ten shared capability-hardening
    tasks, orchestrator fleet-control wiring, the canonical Trojun identity migration, all ten app rollout
    tasks, and the final release-train verification gate. Repair routing aliases when a legacy registered
    project name is required, but keep Trojun as the user-facing canonical name. Do not bypass release
    backpressure, approvals, privacy/safety checks, tests, migrations, or the release train. Treat claimed
    50X-500X improvements as benchmark hypotheses until measured with correctness parity. Continuously
    reconcile missing/failed children into bounded remediation tasks and finish only when immutable release
    evidence exists for the complete fleet or an explicit operator gate remains.

OPERATOR:
  - Approve material database migrations, cross-app memory federation, and staged production promotion only through the existing release train.
