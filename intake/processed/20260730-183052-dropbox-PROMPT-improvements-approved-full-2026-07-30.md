# OPERATOR-APPROVED: Full Improvements Document — build in full (2026-07-30)

The operator has given FINAL APPROVAL to the entire improvements document below (§1–§9f incl. §1b Standing Opportunities display family). Decompose and build ALL sections across Tomorrow, Apparently, Pareto, Vigil, illuminati/Foulkon, and the orchestrator as specified.

BINDING RULES:
- Where an already-queued shard covers a section (capability-os, tomorrow-foulkon-hedge-bridge, foulkon-decision-instrument, policy-waterfall-engine, terminal-permissions-framework, ambient-teach-skills, universal-connectors, video-education-hub, cross-app-build-progress-console, vigil-gaming-regulator-portal), treat this document as that shard's binding spec — extend/verify it, do NOT duplicate work.
- Surfaces present STANDING RESULTS, never search buttons ("View available IOIs", never "start a search").
- Multilateral = discovery/optimization only; execution = coordinated NAMED bilateral legs, all-or-nothing (N5/N6). IOI clicks submit indications, never execute (§2(h)(7)).
- Decision triad display names FINAL: Accept & Monitor / File & Resolve / Hedge & Proceed. Instrument name FINAL: Adaptive Perpetual Line. Guide price until profile_coverage complete.
- Subscription plans (4 Claude Max + ChatGPT + Ollama) never count against USD budgets; route model_policy prefer_free except gauntlet seats/judges.
- All commits authored kalepasch1 <kalepasch@gmail.com>. No prod pushes (ORCH_PUSH_ON_RELEASE stays false; merge-train to dev only).
- Embed into EXISTING tables/surfaces, never duplicate parallel ones. Versioned-everything: never edit prior document versions, always new version.

--- APPROVED DOCUMENT FOLLOWS (verbatim) ---

# Improvements for Final Review & Approval — 2026-07-30
### Everything from this round in one place. Items marked ⚡DONE are live now; items marked 🔨QUEUED are decomposed in the fleet awaiting your approval of this document before the builds run to completion; items marked 👤YOU need one operator action.

---

## 1. Tomorrow Hedge Integration: IOI cost display → 1-click activation → portable identity

**The flow (for approval):** every material gradient option already shows the **expected IOI price** for the user's available instrument (Hedge & Proceed, guide-price rules per profile coverage). This extends it into execution:

1. **"View available IOIs" — not "start a search" (operator revision):** the Tomorrow hivemind relationship search runs **continuously and autonomously** from the moment the identity links — by the time a user looks, their relationship agents have already returned results. The panel shows the standing breakdown: **bilateral IOIs** (single named counterparty covers the risk — lowest single price shown) and **multilateral structures** with the aggregated cost when a combination beats any single counterparty. *On your structural question — you're essentially right, with one legal-posture precision that matters:* there is no single multi-party instrument. "Multilateral" in our architecture means the *discovery and optimization* are multilateral (the PTRRS risk fabric / network-netting engine finds the optimal combination across the whole network), but **execution decomposes into a coordinated SET of named bilateral legs** — each leg one named counterparty, proposed together, consented all-or-nothing per component (the N5/N6 invariants: never a pool, never a CCP novation, never mutualized). So the display reads: *"Best single counterparty: 42bp · Best 3-leg combination: 31bp aggregate (all-or-nothing set)"* — and choosing either opens the standard consent flow, never executing on the click. The user decides hedge-or-not by comparing the standing IOI price against the quantified exposure (`expected_loss_usd` + `exposure_class`), with the same guidance Tomorrow users get natively.
**1b. The Standing Opportunities display family — Tomorrow, Apparently, and Pareto (operator revision + audit finding):**

**⚠️ Audit finding on Tomorrow itself:** the backend already runs exactly as you specified — the hive relationship cycle fires every 5 minutes (relationship orchestration, auto-IOI submission, standing acceptance, netting, mesh update) — **but the mesh UI still presents as discover-on-demand**: a user clicks "discover" to be shown what the agents already knew. The same insight-inversion you ordered for Apparently applies to our own flagship first. The fix, everywhere: **surfaces present standing results, never search buttons.**

*The display family (same components, per-app idiom — Tomorrow full depth, Apparently decision-adjacent, Pareto consumer-simple):*

1. **Standing Opportunities board** — the core panel: what your relationship agents currently hold, refreshed continuously. Best single bilateral IOI, best multi-leg combination with aggregate price, freshness timestamp ("as of 4 minutes ago"), one-click into consent flow.
2. **Coverage map** — your risk book vs. what the network can currently cover: each exposure shows covered-at-what-price / partially coverable / no standing counterparty (with "the network is recruiting for this" when the capacity engines are actively seeking the other side).
3. **Price-improvement watch** — standing IOI prices tracked over time per exposure: "hedging this risk got 9bp cheaper this month." Turns the panel into something worth checking, and *waiting* into a visible, priced decision.
4. **Netting-opportunity ticker** — when the multilateral engine finds a new combination that beats your best single leg, it surfaces as an event ("new 3-leg set available: 31bp vs. your 42bp single"), not something to hunt for.
5. **Missed-opportunity ledger** — the honest one: opportunities that expired unactioned and what they'd have cost vs. what the exposure did. Over time this is the strongest internal argument for acting on the agents' work — or proof the user was right to wait. Either way, trust.
6. **Relationship-agent activity feed** — what your agents did this week (counterparties evaluated, indications refreshed, combinations tested), because an agent whose work is invisible earns no trust; a feed showing 1,400 evaluations behind one surfaced opportunity earns it instantly.

*The 50-500X:* **the coverage map doubles as the network's demand signal** — every "no standing counterparty" entry aggregates (k≥3) into exactly what the capacity-recruitment engines should manufacture or recruit next, so user visibility and network growth are the same machine; **threshold standing-orders** — a user can set "notify me / auto-open consent when this exposure's cover drops below 35bp," converting the watch into a standing instruction that respects every ceiling and consent gate; and **the Pareto version is the retail wedge** — the same board rendered consumer-simple ("your income-dip protection: available now at $14/mo, was $19 last month") makes the Adaptive Perpetual Line feel like a live market working *for* them, which no insurance product has ever felt like.

2. **Portable identity (never sign up twice):** connecting repo/email/docs in Apparently auto-provisions the Tomorrow account (linked-identity token, one consent screen on first touch) and enrolls the company in the Tomorrow hivemind — so hedge-opportunity discovery starts running against their book *autonomously* from day one, before they ever visit Tomorrow.
3. **"View more" for non-admins — scoped sub-accounts:** admins auto-approve **limited-scope sub-accounts** per user: a linked-but-not-full private Tomorrow data room (the Pareto-style display room pattern — data views only) as the default; full-platform sub-user access grantable per person. Every sub-user session is fully traceable (who logged in, what they viewed, every decision/change attributed) and carries **per-user hedge-approval ceilings** (max hedge cost + permitted hedge types without further approval) — the same ceiling machinery as the permissions framework, one consistent model.

**50-500X:** (a) the *hedge-adjacency graph* — because portable identities join the hivemind, Tomorrow can propose *multi-party* opportunities across Apparently customers with offsetting exposures (the network-netting engine already exists; this is its retail feeder); (b) the sub-account data room doubles as the **board/investor view** — a founder shares a read-only risk room with their board the same way they'd share a deck; (c) hedge-decision outcomes (hedged vs. didn't, and what happened) feed the calibration record — over time we can show "companies that hedged flagged risks avoided X% of realized losses," the marketing stat no one else can compute.

---

## 2. Approvals: aging visibility + workload-aware reassignment

**For approval:** every approval row (expanded row + a dedicated comprehensive page) shows: outstanding approvals with **live age timers**, *who specifically* hasn't approved, their current queue depth, and the audit/guidance context needed to decide. **Reassignment:** admins reassign with one click; **auto-reassignment** triggers when the assignee's workload is measurably too high (higher-priority items queued above this one beyond a threshold) — rerouting to the configured backup approver with more bandwidth. This ports the Smrter capability into Apparently and upgrades both.

**50-500X:** (a) **approval SLA forecasting** — from each approver's historical velocity, show "expected approval: Thursday" at submission time, and route to whoever *forecasts* fastest, not just who has fewest items; (b) **decision-context pre-brief** — the row carries an auto-drafted 3-line brief (what, why, risk, precedent) so approvers decide in the notification without opening five tabs — measured approval latency is the KPI; (c) **vacation/absence inference** — no activity in N hours during business days auto-suggests rerouting *before* the SLA breaches.

---

## 3. Consistency-sentinel drift → the standard approval workflow

**For approval:** drift findings (documents say X, code/product does Y) enter the **same approval/remediation pipeline as everything else** — same queue, same risk scoring, same 1-click remediate, same audit chain — so fixing drift feels identical to approving any other improvement. A drift finding is just another card: Foulkon-scored, department-routed, waterfall-linked.

---

## 4. Vercel consolidation — "web" becomes the only project

**⚡DONE (repo side):** verified **nothing in the repo pushes to the `claude-orchestrator` project** — no root link, no workflow deploy; its deploys come only from its own dashboard Git integration. Added a **root `vercel.json` guard** (`git.deploymentEnabled: false`) so any future project pointed at the repo root can never silently build — the recreation vector is closed for this and serves as the pattern for all other apps (one project per repo; never bare `vercel` in a folder — always `vercel link` first; `.vercel` stays gitignored).

**👤YOU — then it is safe to delete `claude-orchestrator`:**
1. In the **`web` project settings**: confirm Git integration is connected to the repo, **Root Directory = `web`**, production branch = `master`.
2. **Copy any env vars** that exist only on the `claude-orchestrator` project into `web` (check for SUPABASE_*, CRON_SECRET, VERCEL_PROJECT_ID_* per-app analytics ids).
3. Update the GitHub secret **`VERCEL_PROJECT_ID` → `prj_suDyNKd73FKb7qh6m5QSUd0d8NKs`** (the `web` project id) — it's used by the supabase-preview workflow's env wiring.
4. Then delete `claude-orchestrator`. **No code or improvements can be lost** — git is the source of truth; Vercel projects are only build targets, and the survivor rebuilds everything from the same repo.

---

## 5. Teach-skills hivemind — honest status + confirmation path

**Status, honestly:** the ambient-learning build is **🔨QUEUED (decomposed in the fleet), not yet recording** — no app is capturing user-activity episodes today. What IS live now: the fleet's own learning loops (learn-from-merges, committee precedent, corps evolution) — machine-side, not user-activity-side. On your approval of this doc, the queued build ships the observer → task miner → skill synthesis pipeline into Apparently first (per your emphasis), then Smrter/Pareto/Tomorrow, with **cross-customer skill genetics** (steps recombine across workspaces under k≥3 privacy) as specified. Acceptance in the build: a live counter per app in the progress console — "episodes captured / tasks mined / skills synthesized" — so "is it actually learning?" is a number you can check, never an assertion.

---

## 6. Versioned everything + the zero-cost data room

**For approval:** no in-place edits, anywhere. Every change creates a **new version**; prior versions always viewable, labeled simply (`v3 — 2026-07-30 — why`), with agents auto-organizing folders so the **active version is always unmistakable** (pinned, badged, one per folder). Per medium: *documents* → full new version per change; *email* → immutable by nature, threading + supersession links ("this commitment supersedes the 7/12 one"); *code* → git already is the version system, surfaced with the same simple labels (the version viewer reads git, stores nothing).

**The data-room storage answer (the cost problem):** the diligence data room follows the zero-copy rule with one exception class. Default: the room is a **view over the customer's own storage** (connector-backed, digest-pinned — we render, they store). Exception (diligence packages that must be immutable + shareable): **content-addressed cold storage** — store each version once by hash (dedupe is automatic: v2 of a 40-page doc that changed one page stores ~one page of new content with the right chunking), in cheap object storage (S3/R2 class, cents/GB), never in Postgres. Projected cost: even a heavy data room runs dollars/month, not thousands.

**50-500X:** (a) the **version diff is the product** — every new version auto-carries "what changed and why it matters" (the same redline machinery), so diligence reviewers read deltas, not documents; (b) **audit-grade share links** — every data-room view by an external party (investor, regulator, acquirer) is logged with what they opened and for how long — deal intelligence for free; (c) supersession links feed the waterfall engine — a superseded policy version automatically flags every document that cited it.

---

## 7. Video — UX-first: answers stay text, video earns its moments

**Revised per your direction:** video never interrupts a quick answer. The placement logic:
- **Quick answers stay text.** Full stop.
- **"Explain this deeper" button** on topics where a video chapter exists — opt-in, embedded at the exact chapter timestamp, only on request or during ideation-mode collaboration.
- **The weekly digest embeds curated clips** — the bespoke newsletter already renders per-client; now a relevant 90-second chapter rides along as the *context* for why a change matters to them.
- **The commute briefing (the 50-500X):** an auto-assembled **audio/video morning update per client** — "here's what to think about today" — built from Brian's recorded segments + a generated bespoke intro naming *their* company's specifics, delivered as a listenable artifact for the drive in. Brian records general segments once; the assembly engine personalizes infinitely. This is the retention habit no firm can match: your regulatory counsel briefs you every morning, by name, for the cost of one recording session a month.
- Attribution, §5.8b disclosures, and Brian's 1-click sign-off apply to every assembled artifact exactly as to raw uploads.

---

## 9. The Capability OS — full Smrter merge into Apparently (audit finding + build)

**⚠️ AUDIT FINDING (honest):** the Apparently×Smrter "integration" today is only a **linkage bridge** — workspace↔org linking, a cross-platform event ledger, and a cross-sell engine. Smrter's actual capability surface (**CRM, marketing, content production, approvals, agents, arbitration, apprenticeship, account vault, and ~40 more API domains**) is *not* operable from Apparently — you'd still need both apps open. The one-app-unification build queued days ago was **stuck** (1 blocked, 12 dedupe-quarantined); I requeued the blocked shard at top priority under a superseding spec and queued the full **Capability OS** build.

**For approval — the Capability OS:**
1. **Capability registry + per-user activatable tabs:** every Smrter capability becomes a module a user activates as a tab — by browsing the list or by asking the terminal ("add CRM to my workspace") — configured *to that user*, never generic. The build's acceptance case is your exact example: **Brian's OS** = biz-dev/CRM, customer outreach, marketing content, video production, article review+production; **yours** adds Medium/social management for kalepasch accounts with the consilium/thought-leadership pipeline feeding drafts.
2. **One implementation, one surface:** capabilities execute against Smrter's engines through a shared service layer (existing S2S/HMAC patterns) — Smrter stays standalone for its own users, Apparently users never leave Apparently, zero forked logic.
3. **Capability bots, hivemind-wired (required):** one named bot per tab (crm-bot, outreach-bot, content-bot, video-bot, article-bot…), each learning on its own capability *and subscribed to every other capability's events* for the user, the org, and (k≥3, privacy-tiered) the whole hivemind. Real cross-capability reasoning: a CRM note about a prospect's licensing question surfaces in the content bot ("write the article that answers this"), the outreach bot ("they're exam-season busy — delay the sequence"), and the company-context gradient. Bots create tasks/edits/workflows/approvals autonomously *within each user's permission ceiling* and propose above it — the same constitution machinery, no new gates.
4. **Content/social pipeline:** article + video production flows through the commission gate and the marketing-copy permission dimension before anything publishes to Medium/socials.

**50-500X:** (a) **capability genetics** — bots share learned step-patterns through the teach-skills engine, so an outreach cadence learned in one org becomes a proposable template in another; (b) **"compose a tab"** — users combine capability primitives into custom views (outreach + article review in one tab for Brian) as configuration, not code; (c) **pre-populated activation** — turning on CRM immediately shows contacts and pipeline *inferred from already-connected email*, because a tab that arrives full converts and a tab that arrives empty churns — the activation moment IS the demo.

**9b. Nav & activation UX (operator revision — now binding on the build, patched into all queued shards):**

*Four activation paths, all first-class:* **prompt** (terminal request activates + configures the tab), **recommendation** (the OS proposes a capability with a 1-click activate chip when it observes the need — an email thread full of prospect questions triggers "Add CRM?", *with the pre-populated preview shown before activation* so the user sees the value before clicking), **role/department** (admins assign default tab sets per role — a new compliance hire lands with the compliance set already active), and **search** (command-palette search over the registry with inline activate/connect).

*Nav discipline:* six tabs or fewer render flat; **above six, tabs auto-group into collapsible categories** (Legal & Compliance / Growth & Marketing / Operations / Finance / Intelligence) so the sidebar never overwhelms at first glance. Within groups, tabs order by *that user's* usage frequency, with pin/unpin always available.

*The 50-500X on top:* **adaptive surfacing** — the nav is context- and time-aware (the approvals tab badges when items wait; the content tab rises on publishing days) but **never auto-hides an active tab**, because predictability beats cleverness and a user must always find what they activated where they left it; **focus modes** — one-click role lenses ("Outreach mode" collapses everything except CRM + outreach + content), saved per user, which is how one OS serves Brian-the-rainmaker and Brian-the-attorney in the same day; **usage-earned defaults** — the org's actual usage patterns become the suggested default sets for the next hire in that role, so onboarding config writes itself; and full keyboard-first navigation on the existing command-palette pattern, because the fastest tab switch is the one that never touches the mouse.

**9c. Feature-level sub-swarms (operator revision — agents per feature, not just per tab):**

Every *feature within* a tab gets its own agent (in CRM: a pipeline agent, a contact-enrichment agent, a follow-up agent, a deal-risk agent; in content: a drafting agent, an SEO agent, a distribution agent, a performance agent) — organized as a **sub-swarm under the tab bot**, always speaking to each other for the user, the org, and (k≥3) the hivemind. The design rule that makes this powerful instead of expensive: **hierarchical, not mesh**. Feature agents don't all talk to all (N×M chatter is a cost bomb and a noise bomb — the same anti-mob lesson as the expert corps); they publish to their *tab bot*, which curates what crosses tab boundaries, and the tab bots share the event bus. Feature agents are **deterministic-first** (watching their feature's data with code, not tokens) and escalate to model reasoning only when something material moves — so a thousand sub-agents cost almost nothing at idle and think only when there's something to think about.

*The 50-500X inside it:* **sub-swarm specialization mirrors the expert corps** — feature agents earn standing the same way (their proposals accepted vs. ignored = their Elo), so over time the follow-up agent that actually converts gets more autonomy than the one that annoys; **cross-feature choreography** — the pipeline agent noticing a stalled deal triggers the content agent to draft the exact collateral that unsticks it and the outreach agent to time its delivery, one choreographed play instead of three notifications; and **swarm templates** — a whole tab's sub-swarm configuration (which agents, which autonomy levels, which choreographies) exports as a template through capability genetics, so "Brian's outreach machine" becomes an installable pattern for the next attorney on the network.

**9d. Metering, billing & the cost engine (operator revision — every capability metered, everything routed at lowest cost):**

*Metering:* every capability/tab meters its **token usage per user per workspace** against the chosen subscription plan — with the meter honest and always visible (the existing "token metering honest" launch rule extended to every capability): each tab shows its month-to-date usage, each agentic action attributes its cost to its capability, and plan limits warn before they bite, never after.

*The cost engine — Madeus/orchestrator routing, productized:* every agentic call from every capability bot and sub-swarm routes through the same optimization stack the fleet itself runs on: **subscription/free-first tranches** (local models → cheap tier → subscription capacity → paid API only when quality demands it), **capability-aware model selection** (the follow-up agent drafting a nudge email runs on the cheapest tier; the compliance gradient runs on the tier the risk demands), **deterministic-first agents** (code before tokens, always), **caching and dedupe** (the same question asked by two sub-agents answers once), and **night-shifting** (non-urgent swarm work — enrichment, digests, learning cycles — batches into the overnight local-model window at ~$0). The margin math this produces: our metered *cost* floor drops far below what any competitor paying list-price API rates can offer, which means we can price capabilities aggressively *and* keep margin.

*The 50-500X inside it:* **per-capability cost/value scoring** — every agent's spend is continuously scored against the value it produced (tasks completed, approvals earned, outcomes), and the router shifts budget toward high-value agents automatically — the hivemind literally optimizes its own P&L per user; **the cost meter as a trust surface** — customers see not just usage but *savings* ("your swarm ran 2,300 actions this month; 91% on zero-cost capacity — est. $47 metered vs. $410 at list API rates"), turning our routing efficiency into a visible reason to stay; and **plan-fit suggestions** — the meter recommends the right subscription tier from actual usage patterns, up *or down*, because a customer who trusts the meter to downgrade them is a customer for life.

**9e. The universal meter bar + admin limit controls (operator revision — consistent on every page):**

One **persistent, consistent meter element on every page** of every app, per individual user: month-to-date tokens/cost, plan remaining, and a color state (comfortable / pacing-high / near-limit) — same component, same position, everywhere, so the meter becomes ambient literacy rather than a settings page nobody visits. **Admin controls, basic → advanced:** basic = per-user monthly limits with one slider and an org default; advanced = per-capability sub-limits (Brian's video production capped separately from his CRM), per-role limit templates, burst allowances (a launch week can borrow from next month, flagged), scheduled limits (contractor access expires with the engagement), and hard-vs-soft limit semantics (soft = warn and continue billing overage per plan; hard = agents degrade to propose-only rather than stopping cold — **an out-of-tokens user never loses visibility, only autonomous execution**).

*The 50-500X:* **predictive pacing** — the meter forecasts month-end from the current trajectory ("on pace for 140% of plan — mostly video production; cap it or bump the plan?") so limits are managed *before* they bind; **value-denominated display toggle** — admins can view the meter in outcomes, not tokens ("$47 spent → 3 filings prepared, 210 CRM actions, 4 articles drafted"), which is how a CFO decides the plan is cheap; and **limit-as-constitution** — spend limits live in the same Risk & Strategy Constitution as risk ceilings, so one governing document answers both "what may agents do?" and "what may they spend doing it?"

**9f. Internal builds through the same vibe tools, calibrated (operator revision — SHIPPED in the schema):**

The self-service vibe/autonomous coding tools build **internal capabilities, not just customer-facing product** — same approval rounds, same protective guidance, same tribunal. The calibration fix is now in the shipped schema (`ExposureSurface: 'internal' | 'customer_facing' | 'regulator_facing'` in `rapidGradient.ts`, wired through the gradient endpoint): **internal tools price on a dampened regulatory lens** (no customer/regulator exposure) while keeping data/security dimensions at full weight (an internal dashboard can still leak PII), customer-facing runs standard calibration, and regulator-facing (filings, portal content) runs the strictest. The surface is displayed on every gradient so the reader knows which lens priced it — and startup teams stop getting friction-paused over low-risk internal dashboards while still getting caught before a risky external ship.

---

## 8. Fleet actions already executed this round (for the record)
- **Remote pull+restart triggered for BOTH Macs** via the fleet-control plane (self-retrying until Mac 1's committer sweeps, then both converge — no terminal needed).
- Safe-prefix gateway fix (OLLAMA_/COMMITTEE_/LEGAL_DOCKET/SEMANTIC_DEDUPE/SWARM_/CADE_ keys now actually apply fleet-wide — they were silently ignored before).
- `EnforcementSpec` extended with `expected_loss_usd`, `exposure_class` (incl. criminal_referral), `exposure_detail`, `as_of` — quantified exposure in every risk display, real-time-adaptive by construction.
- Self-directed department routing (additive-only subscriptions) + quantified exposure patched into all four live builds.
- Policy Waterfall Engine queued; ollama canary running; ~190 falsely-killed tasks restored; recovery sweep at top priority.

**On your approval of this document, the queued builds (1, 2, 3, 5, 6, 7 + waterfall) proceed to completion under the standard QA/merge gates. Item 4 needs your three dashboard steps first.**
