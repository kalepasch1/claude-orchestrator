PROJECT: tomorrow

# The Tomorrow side of the joint "ecosystem private rate oracle" (the Pareto side — index production,
# k-anon+DP, reconstruction — is in intake/pareto-2080-0630.md, tasks oracle-index-export-pareto +
# pricing-grid-reconstruction). Here we make Pareto's attested rate indices a CONTRACT-GRADE oracle:
# ingested, proof-wrapped, governed, and usable as OTC settlement source + as new contract underlyings.
# This is the two-sided flywheel: Pareto real closings -> indices -> Tomorrow contract settlement;
# Tomorrow OTC executions -> new real-price observations -> back into the oracle.
#
# Reuse Tomorrow's EXISTING infra (do not rebuild): server/utils/proof/verifiableProof.ts (C1, Ed25519,
# content-addressed, stateless verify), server/utils/risk/modelValidation.ts (SR 11-7 governance/drift),
# the oracle/trigger gateway pattern (oracleGateway / triggerOracle / reserveOracle with median +
# staleness + cross-source divergence), privacyBudget.ts, and the riskFabric. Tests use vitest
# (`npx vitest run ...`). Keep the bilateral / non-custodial / ECP posture; posture grep must stay clean.
# All tasks are material (oracle feeds contract settlement) and human-gated.

- id: oracle-ingest-pareto-indices
  title: Ingest Pareto's attested rate indices into a governed Tomorrow oracle feed
  material: yes
  model: opus
  depends: []
  proof: `npx vitest run server/utils/oracle/__tests__/rateOracleIngest.test.ts` exits 0
  prompt: |
    Add server/utils/oracle/rateOracleIngest.ts. Consume the rate-index manifest + values produced by
    Pareto (oracleIndexExport.indexManifest / buildIndices — shape documented in
    intake/pareto-2080-0630.md). Treat each Pareto index as ONE source in a multi-source aggregator that
    mirrors the existing reserveOracle/triggerOracle pattern: median aggregation, staleness gate,
    cross-source divergence (anti-manipulation), fail-loud-null when no fresh data. Validate k-anonymity
    floor + that only AGGREGATED indices arrive (reject anything resembling row-level/personal data).
    Expose `getRateIndex(productKey, segmentKey)` -> {value, asOf, sources, divergence, quotable}. Pure
    aggregation core + a thin KV-backed feed store (no migration). Tests: median across sources; stale
    source dropped; divergent source flagged; rejects a non-aggregated payload; fail-loud-null on empty.

- id: oracle-proof-carrying-readings
  title: Wrap every rate-oracle reading in a C1 verifiable proof (offline-verifiable settlement source)
  material: yes
  model: opus
  depends: [oracle-ingest-pareto-indices]
  proof: `npx vitest run server/utils/oracle/__tests__/rateOracleProof.test.ts` exits 0
  prompt: |
    A private oracle is only contract-acceptable if a counterparty can verify a reading WITHOUT trusting
    us. Extend server/utils/proof/verifiableProof.ts ProofKind with 'rate_oracle' (mirror how 'safety'/
    'credit'/'capital' were added) and add server/utils/oracle/rateOracleProof.ts: `mintReadingProof(
    {productKey, segmentKey, value, asOf, sources, manifestHash})` -> a content-addressed, Ed25519-signed
    VerifiableProof whose public inputs let anyone recompute the digest and verify offline via the
    existing stateless verifyProof. Add a public GET route to fetch + verify a reading proof (reuse the
    existing /api/proof/[id]/verify pattern). Tests: reading proof verifies; tampering value/asOf fails;
    foreign-key rejected; stateless JSON-copy verify works.

- id: oracle-sr117-governance
  title: Register the rate oracle under SR 11-7 governance with drift gating
  material: yes
  model: sonnet
  depends: [oracle-ingest-pareto-indices]
  proof: `npx vitest run server/utils/oracle/__tests__/rateOracleGovernance.test.ts` exits 0
  prompt: |
    A settlement oracle must be governed. Register the rate oracle as a model in the EXISTING
    server/utils/risk/modelValidation.ts SR 11-7 registry: record predicted-vs-realized (index value vs
    later actual closings fed back from Pareto), compute drift, and gate — RED drift => freeze the oracle
    as a settlement source (contracts fall back to a designated public benchmark or pause), YELLOW =>
    watch, GREEN => ok. Add server/utils/oracle/rateOracleGovernance.ts with `driftGate()` +
    `oracleSettlementAllowed(productKey)`. Surface on the existing model-risk dashboard. Tests: RED drift
    freezes settlement; realized-vs-predicted updates drift; gate per product.

- id: oracle-indices-as-underlyings
  title: Allow OTC contracts to reference Pareto rate indices as the settlement underlying
  material: yes
  model: opus
  depends: [oracle-proof-carrying-readings, oracle-sr117-governance]
  proof: `npx vitest run server/utils/otc/__tests__/rateIndexUnderlying.test.ts` exits 0
  prompt: |
    Add server/utils/otc/rateIndexUnderlying.ts enabling rate-index-settled bilateral ECP swaps within
    the EXISTING swap-only allowlist (these are parametric/index payment swaps, already permitted): e.g.
    rate-lock swap (pays the borrower if the index rate rises before lock), deposit-beta swap, premium-
    hedge swap. Each contract references getRateIndex(...) for settlement, requires a proof-carrying
    reading (oracle-proof-carrying-readings) at settlement, and only settles when
    oracleSettlementAllowed(productKey) is true (drift gate). Keep bilateral/non-custodial/ECP posture;
    NO order book, NO mutualization. Do NOT auto-enable as live trading — clearance/compute + proof only
    until counsel sign-off (mirror the novelInstruments mint-clearance pattern). Tests: a rate-lock swap
    settles correctly off a proof-carrying index reading; settlement refused when the drift gate is RED;
    refused without a valid reading proof; posture invariants asserted (bilateral, non-custodial).

OPERATOR:
  - Counsel sign-off REQUIRED before any rate-index-settled contract goes live: (a) a PRIVATE oracle as a derivatives settlement source (disclosure, governance, fallback), (b) the cross-entity data/privacy boundary with Pareto (aggregated attested indices only; never row-level), (c) consumer rate-hedging products' characterization (parametric swap vs insurance bright line — reuse the existing assertParametricOnly / assertNotStatutoryInsurance posture).
  - S2S secret PARETO_TOMORROW_S2S_SECRET set in Tomorrow + Pareto prod env (HMAC per the existing _s2s helpers). Assistant cannot enter secrets.
  - Confirm no Tomorrow operator/affiliate is ever a counterparty to a rate-index swap (existing N8 disinterested-operator invariant).
  - Cross-repo handshake: oracle-ingest-pareto-indices consumes the output of pareto-2080-0630.md task `oracle-index-export-pareto`; sequence the Pareto producer first. (Dependency is cross-file and cannot be expressed in `depends:` — coordinate at merge.)
