PROJECT: pareto-2080

# DESIGN-SPEC task (not implementation). Produce a design document a human reviews BEFORE any code.
# Output is a markdown doc in the repo; proof = the doc exists with the required sections. No engine,
# no tests-of-behavior yet — this de-risks a big architectural change before it becomes real work.

- id: designspec-global-cross-user-optimization
  title: Design spec — platform-wide (cross-user) joint optimization with a hard fairness constraint
  material: no
  model: opus
  depends: []
  proof: `test -f docs/DESIGN_global_cross_user_optimization.md && grep -qi "fairness constraint" docs/DESIGN_global_cross_user_optimization.md && grep -qi "individual rationality" docs/DESIGN_global_cross_user_optimization.md`
  prompt: |
    Write docs/DESIGN_global_cross_user_optimization.md. Today every user is optimized independently.
    This spec designs OPTIMIZING ACROSS THE WHOLE USER BASE JOINTLY — flexible users' timing flexibility
    can subsidize urgent users' needs; cohort formation, auction timing, and capacity allocation are
    solved globally — WITHOUT ever making any user worse off than their standalone outcome.
    Required sections:
      1. Problem framing — why per-user local optima leave value on the table (worked example:
         staggering/pooling RFQ timing across users raises everyone's clearing rate).
      2. Objective + decision variables — global welfare objective over cohort assignment, timing, and
         capacity routing; what the platform actually chooses.
      3. HARD CONSTRAINTS (load-bearing): individual rationality (every user >= their standalone
         outcome), no-envy where feasible, and the existing invariants (non-discretionary, non-custodial,
         no cross-user data leakage / k-anon). State them formally.
      4. Mechanism — how to solve it (LP/assignment or matching formulation), and how it degrades
         gracefully to per-user optimization when the global solve is infeasible or slow.
      5. Fairness accounting — how surplus created by the global solve is attributed/shared (Shapley or
         proportional), and how a user verifies they weren't sacrificed.
      6. Anti-gaming — prevent a user gaming "flexibility" to extract others' surplus.
      7. Interfaces — which existing engines it wraps (rfqMesh, demandAuction, myersonAuction,
         standingAuction, forwardDemand) and the data it needs.
      8. Rollout — shadow-mode (compute the global plan, compare to per-user, never act) before any live
         use; metrics to prove it never hurt a user.
    Keep it a spec (diagrams-in-prose + pseudocode ok); no production code. Flag every point that needs
    a human/counsel decision.

OPERATOR:
  - This is a design doc for human review. Implementation is a separate future task, gated on approval of the fairness/individual-rationality guarantees.
