PROJECT: smarter

# CADE position + recipient-alignment on the contract/engagement-letter draft flow.
# Distinct from the omnichannel-social intake (smarter-0702.md) — different subsystem.
# The core is already shipped in-repo: server/utils/positionEngine.ts +
# server/utils/recipientAlignment.ts + components/PositionPanel.vue, wired into
# server/api/ai/draft.post.ts and components/ComposePanel.vue. These are the remainders.

- id: cade-recipient-selector-compose
  title: Recipient selector in the ComposePanel draft flow
  material: no
  model: sonnet
  depends: []
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Add a recipient picker to `components/ComposePanel.vue`: a dropdown of the
    `RECIPIENT_REGISTRY` ids exported from `server/utils/recipientAlignment.ts`
    (court/regulator/counterparty) plus an optional named-person field. Set
    `recipientId` / `recipientPerson` on the `/api/ai/draft` POST body (already accepted
    by `server/api/ai/draft.post.ts`). The extended `components/PositionPanel.vue`
    already renders the returned `alignment` (concurrence + anticipated RFIs), so this
    is input + plumbing only.

- id: cade-smarter-position-override
  title: Position override record endpoint (parity with apparently)
  material: yes
  model: haiku
  depends: []
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Add `server/api/ai/position-override.post.ts` recording a licensed-counsel override
    (attorney, barId, tenability, band, note) so a materially-weak self-service draft
    can be cleared + audited — parity with apparently's
    `server/api/legal-opinions/[id]/position-override.post.ts`. Persist to the smarter
    store used for audit (kv.ts or the orch client, matching how ComposePanel drafts are
    logged). Return `{ ok, overrideApplied }`. ComposePanel's PositionPanel already emits
    `apply-override`; wire the emit to POST this endpoint.

- id: cade-adopt-shared-core
  title: Adopt the shared CADE core once apparently extracts it
  material: yes
  model: sonnet
  depends: []
  proof: `npx vitest run` (smarter) exits 0 AND the golden-vector contract test passes
  prompt: |
    BLOCKED until apparently task `shared-cade-core-extract` lands the single
    source-of-truth module. Then replace smarter's triplicated pure core in
    `server/utils/positionEngine.ts` (computeWinRateKPI, auditOverrides, solveMultiParty,
    buildFallbackLadder, routeByTenability, scoreTenability, evaluateSelfServiceGate)
    and the recipient-alignment pure core with imports/re-exports from the shared
    module. MUST preserve byte-identical outputs (golden vectors). Keep smarter's
    Qc-committee → weakness-ledger adapter local. Route to human approval.

OPERATOR:
  - None (draft flow is user-initiated; no new secrets/migrations for the selector or override endpoint unless the override is persisted to the orch Supabase, in which case apply the parity table there).
