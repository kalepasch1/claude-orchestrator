PROJECT: tomorrow

# Wave 3 — international translation memory for the multi-jurisdiction corpus.
# Corpus deployed on Supabase edetxpcoaiqlqrwyzltw. Depends conceptually on the EU/UK/AUS harvesters
# (tomorrow-0629.md eu-uk-aus-harvesters).

- id: translation-memory
  title: Translation memory for EU/UK/AU authority (language-agnostic retrieval)
  material: yes
  model: sonnet
  depends: []
  proof: `npx vitest run server/utils/__tests__/translation-memory.test.ts` exits 0
  prompt: |
    Non-English EU authority must be retrievable in English. Add corpus_documents columns orig_lang +
    canonical_en (migration); on ingest of a non-English doc, store the original body AND a canonical English
    translation (aiCall), index the English for FTS/embeddings while preserving the original for citation. Pure
    helper server/utils/translation-memory.ts `needsTranslation(lang)` + `pickIndexText(doc)` unit-tested
    (English passthrough; non-English uses canonical_en when present, else original). Material (schema + write).

OPERATOR:
  - Approve prod apply of the orig_lang/canonical_en columns (edetxpcoaiqlqrwyzltw).
  - Only meaningful once the EU/UK/AUS harvesters run (jurisdiction gate + counsel sign-off).
  - aiCall (Anthropic) key required for translation at ingest.
