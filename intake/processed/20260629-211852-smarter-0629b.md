PROJECT: smarter

- id: ethics-opinion-memo
  title: Internal multi-jurisdiction legal-ethics analysis memo
  material: yes
  model: opus
  depends: []
  proof: `npx -y markdownlint-cli2 docs/ethics-opinion-memo.md` exits 0
  prompt: |
    Draft docs/ethics-opinion-memo.md as INTERNAL counsel work product (the user
    is counsel and is the reviewer/approver). Analyze the Smarter + Apparently +
    Perpetual Memos + Perpetual Contracts + Smarter Scholars model against the
    rules of professional conduct, with explicit headings per issue: UPL (Rule
    5.5), Confidentiality (1.6), Supervision (5.1/5.3), Conflicts (1.7/1.9),
    Business transactions with a client (1.8(a)), Law-related/ancillary services
    (5.7), Fee-splitting & ownership (5.4), Lawyer advertising/solicitation rules
    (7.x) as applied to the Apparently "research draft" funnel and the public
    Perpetual Contracts page. Add a per-jurisdiction flag table for CA, NY, TX,
    NC, DE noting where each state is more aggressive (e.g., UPL, ancillary
    services). For each issue give: the rule, how the current design addresses it,
    residual risk, and a recommended mitigation. Conclude with an "Open items for
    outside-counsel confirmation" list. State clearly this is internal analysis /
    work product, not a published opinion and not advice to any third party.
    Reference docs/perpetual-contracts-structuring.md if present.

- id: consensus-engine-impl
  title: Reference implementation of the Consensus Engine (mocked providers)
  material: no
  model: opus
  depends: []
  proof: `python3 -m pytest -q prototype/consensus_engine/tests` exits 0
  prompt: |
    Build a standalone, self-contained reference implementation under
    prototype/consensus_engine/ (a prototype module, independent of the main app
    stack, so it runs in isolation). Implement, following docs/
    consensus-engine-spec.md if present: (1) a Provider abstraction interface so
    models are added/swapped without coupling; (2) MOCK providers only — no live
    API calls, no secrets, deterministic fake outputs for tests (Claude, GPT,
    Gemini, Grok, DeepSeek, GLM stand-ins); (3) a router that selects providers by
    a configurable per-subtask competence score; (4) an aggregation/judge step
    with adversarial-debate rounds and confidence scoring; (5) a computed
    decorrelation score over the panel's (mocked) error vectors; (6) the
    5%-surface summary object ("reviewed across N / X simulations / Y resolved /
    Z flagged"). Add prototype/consensus_engine/tests with pytest covering router
    selection, aggregation, decorrelation math, and the summary contract. Include
    a short README noting that live keys + spend caps are an operator prerequisite
    for any non-mock run. No secrets in code.

OPERATOR:
  - Live multi-model API keys (OpenAI, Google, xAI, DeepSeek, Z.AI, etc.) + per-model spend caps — required only for non-mock Consensus Engine runs; secrets, do not commit.
  - Outside-counsel confirmation of the open items in docs/ethics-opinion-memo.md before any go-live (counsel/user signs off on the internal memo first).
  - Marketing site domain + hosting/deploy (e.g., Vercel) — deploy step.
