PROJECT: pareto-2080

# Frontier batch — reframes + optimization-theory upgrades + two counsel-APPROVED conduct-sensitive
# engines. Same pattern as the existing engines in server/utils/: pure ESM, no deps, seedable RNG,
# a node:test in tests/, explain.makeExplanation on every decision/recommendation. Inputs are passed
# as arguments (no DB dependency). Proof for each = its node:test passes. Don't regress the suite
# beyond the 2 known pre-existing failures (agentLedger, bookingSaga — local Prisma-binary only).
#
# Invariants (never break): free (no fee/affiliate/data-sale); non-discretionary by default (prepare,
# human commits); non-custodial; agent-of-the-user; writeback on every completed transaction;
# reconstructed vendor models stay CONSUMER-FACING/INTERNAL — never sold, never fed back to vendors.
# Two engines (demand-timing-orchestrator, collusion-detector) are conduct-sensitive: counsel (the
# operator) has APPROVED building them, but they MUST structurally enforce consumer-benefit-only and
# must NOT auto-act/auto-report — they produce advisory output only. See each prompt.
#
# Cross-file note: myerson-auction, twin-negotiator, and consumer-rate-benchmark conceptually consume
# outputs of pareto-2080-0630.md (pricingGridReconstruction / oracleIndexExport). They take those as
# function arguments, so they build+test independently with synthetic inputs; wire to the real
# producers at merge.

- id: household-alm
  title: Optimize the household balance sheet like a bank treasury (asset-liability management)
  material: no
  model: opus
  depends: []
  proof: `node --test tests/householdAlm.test.js` exits 0
  prompt: |
    Add server/utils/householdAlm.js (pure). Stop optimizing products one-by-one; optimize the household
    as a treasury. Given assets (deposits, bonds, brokerage) and liabilities (mortgage, loans, cards) with
    rates + durations, compute: net interest margin (weighted earn-rate minus weighted pay-rate), duration
    gap (asset vs liability duration mismatch), and the ranked set of moves that shrink the spread / close
    the gap (refi, CD-ladder, prepay, reallocate) in $/yr and duration terms. Compose the EXISTING
    decumulation.js, portfolioOptimizer.js, retirementLiabilities.js, debtOptimizer.js (import, don't
    reinvent). Return { nimBps, durationGapYears, lostSpreadUsdPerYear, moves:[{action, deltaNimBps,
    deltaUsdPerYear, durationEffect}] }. Tests: a wide pay/earn spread surfaces the highest-$ move first;
    closing a duration gap is recommended when assets are much shorter/longer than liabilities; NIM math
    is correct on a hand-checked book.

- id: lifetime-rate-dp
  title: Optimize the lifetime SEQUENCE of rate decisions (global DP, not greedy)
  material: no
  model: opus
  depends: []
  proof: `node --test tests/lifetimeRateDp.test.js` exits 0
  prompt: |
    Add server/utils/lifetimeRateDp.js (pure). Local greedy optimization of each rate decision leaves
    value vs the global optimum. Model the ordered sequence of major rate decisions over a horizon
    (buy/refi/switch-deposit/ladder/draw-down) as a finite-horizon dynamic program: states = household
    balance-sheet + rate regime; actions = the decisions; reward = after-tax cash flow + retirement-day
    value (use the EXISTING valueLedger.js exchange rate). Provide `optimizeSequence(state0, decisionSet,
    {horizonYears, scenarios})` -> the value-maximizing policy (ordered actions + timing) and the value
    gap vs a greedy baseline. Keep it a transparent backward-induction DP over a discretized state grid
    (deterministic, seedable; reuse retirementMonteCarlo's mulberry32 for any scenario draws). Tests: the
    DP policy value >= the greedy policy value on a seeded case; correct optimal-stopping on a simple
    lock-vs-wait subproblem; horizon + discounting respected.

- id: myerson-auction
  title: Mechanism-design-optimal demand auction (Myerson reserves from reconstructed cost curves)
  material: no
  model: opus
  depends: []
  proof: `node --test tests/myersonAuction.test.js` exits 0
  prompt: |
    Add server/utils/myersonAuction.js (pure). Upgrade the naive demandAuction clearing to a
    surplus-optimal, truthful auction. Because pricingGridReconstruction gives each vendor's cost curve,
    set personalized reserve prices and run a Vickrey-style (second-price) mechanism so vendors are
    incentivized to bid their true floor, maximizing user surplus. Provide `runOptimalAuction({block,
    bids, vendorCostCurves, lowerIsBetter})` -> { allocations, clearing, userSurplusUsd, reserves,
    truthful:true }. Implement reserves = the reconstructed floor + a small margin; second-price payment
    rule; capacity + min-volume respected (reuse demandAuction's structure). Tests: a vendor bidding above
    its reserve still pays the second-price; user surplus >= naive best-bid clearing on a seeded case;
    truthful-bidding is a dominant strategy on a 2-vendor example (bidding true floor weakly dominates).

- id: twin-negotiator
  title: Train the negotiator offline against reconstructed vendor simulators (digital-twin RL)
  material: no
  model: opus
  depends: []
  proof: `node --test tests/twinNegotiator.test.js` exits 0
  prompt: |
    Add server/utils/twinNegotiator.js (pure). The negotiationBandit learns from costly real reps. Use the
    reconstructed vendor models as a SIMULATOR: roll out many negotiation episodes offline against faithful
    vendor twins and learn an improved opening/counter policy before any real message. Provide
    `simulateEpisode(policy, vendorTwin, seed)` (deterministic), `trainPolicy(vendorTwins, {episodes,
    seed})` -> a policy (opening + counter schedule) that beats the bandit's cold-start, and
    `policyToFanoutSeed(policy)` so negotiationFanout/negotiationBandit start warm. Keep it transparent
    (tabular/parametric policy improvement, seeded — NOT opaque deep RL). Tests: trained policy's mean
    simulated outcome beats a fixed baseline on seeded twins; determinism (same seed => same policy);
    converges within the episode budget.

- id: real-options-timing
  title: Price lock/float/switch/fix-vs-variable decisions as real options with an exercise boundary
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/rateOptions.test.js` exits 0
  prompt: |
    Add server/utils/rateOptions.js (pure). Each timing decision (lock now vs float, switch vs wait, fix
    vs variable) is an option with an optimal exercise boundary. Value them with a transparent
    optimal-stopping / binomial-lattice model on the rate process (compose predictionSignals.js for the
    drift/vol regime). Provide `valueLockVsFloat({currentRate, vol, drift, horizonDays, notional})` ->
    { floatOptionValueUsd, lockValueUsd, exerciseBoundaryRate, recommendation }. Self-contained pricer
    (no external dep). Tests: higher vol raises the float option value; the exercise boundary moves the
    right direction with drift; lock recommended when the boundary is crossed.

- id: consumer-rate-benchmark
  title: Publish governed, methodology-transparent named rate benchmarks (a "SOFR for consumer rates")
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/rateBenchmark.test.js` exits 0
  prompt: |
    Add server/utils/rateBenchmark.js (pure). Promote the oracle indices (oracleIndexExport) into NAMED,
    versioned, methodology-transparent public benchmarks ("Pareto Super-Prime 30yr Index", "National HYSA
    Index") so external parties could reference them — the standard-setting moat. Provide
    `publishBenchmark(indices, {name, methodologyVersion})` -> { benchmarkId, value, asOf, methodology
    (human-readable: sources, k, dp-epsilon, segment def), contributors (count only), revisionPolicy }.
    Enforce: k-anonymity floor (>=5 distinct), DP-noised public figure, NO row-level/personal data, full
    methodology disclosure. MATERIAL: a published benchmark is reputationally/regulatorily sensitive
    (benchmark administration is regulated in some jurisdictions, e.g. EU BMR) — this task builds the pure
    producer + methodology doc only; actual publication is gated on the OPERATOR counsel item. Tests:
    suppresses sub-k; methodology object complete; revision policy present; no personal fields in output.

- id: framing-eligibility
  title: Discover the best TRUTHFUL representation + membership/charter eligibility per user
  material: yes
  model: sonnet
  depends: []
  proof: `node --test tests/framingEligibility.test.js` exits 0
  prompt: |
    Add server/utils/framingEligibility.js (pure). A user often qualifies under multiple LEGITIMATE
    framings (W2 vs business income, joint vs individual, primary vs investment) and memberships/charters
    they don't know about (an eligible credit union, a state program). Using the reconstructed grids, find
    which TRUTHFUL framing/charter prices best, and surface eligibility paths. HARD CONSTRAINT: truthful
    representations only — choosing among true characterizations (like standard vs itemized), NEVER
    fabricating facts. Each suggestion must carry an `attestableBasis` (the true fact that makes the
    framing valid) and a `requiresUserConfirmation:true` flag. Provide `bestTruthfulFraming(profile,
    grids)` and `eligibilityPaths(profile, charterRules)` -> ranked options with bpSaved + attestableBasis.
    Tests: picks the cheaper of two valid framings; never returns a framing without an attestableBasis;
    flags an ineligible charter out.

- id: user-behavior-twin
  title: Model the USER (action latency, risk tolerance) to personalize autonomy gating + pre-staging
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/userBehaviorTwin.test.js` exits 0
  prompt: |
    Add server/utils/userBehaviorTwin.js (pure). Optimization value is lost to user inaction. From the
    user's revealed history (which staged actions they commit/ignore, how fast, at what stakes), estimate
    action-latency by category, stakes sensitivity, and risk tolerance, then output gating hints:
    `predictActionLatency(history, category)`, `inferRiskTolerance(history)`, and
    `personalizeAutonomy(profile, defaults)` -> suggested per-category caps/quiet-hours/trust-floor nudges
    for autonomyPolicy (suggestions only — the user still configures). Tests: a fast-committer on bills
    gets a higher suggested bills cap; an ignorer of high-stakes items gets a lower trust floor there;
    deterministic on a fixed history.

- id: demand-timing-orchestrator
  title: Consumer-benefit demand-timing — align cohort RFQs to vendor windows (counsel-approved, guardrailed)
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/demandTiming.test.js` exits 0
  prompt: |
    Add server/utils/demandTiming.js (pure). Time a cohort's RFQs to vendors' most-competitive windows
    (quarter-end quota, post-rate-cut) so USERS get better prices. Counsel (operator) has approved building
    this. It MUST structurally stay on the consumer-benefit side of the line and NEVER tip into
    manipulation: provide `recommendSendWindow(cohort, vendorWindows, marketCalendar)` -> advisory
    {window, reason, expectedBpBenefit} ONLY — it returns a RECOMMENDATION, never auto-sends, never
    coordinates to move/withhold supply, never targets harming a vendor. Bake invariants into the code +
    tests: output is advisory-only (no side effects); refuses/zeros out if the rationale is "move the
    market price" rather than "catch the vendor's competitive window"; per-user benefit must be the
    objective. Include a `complianceNote` field on every output restating the consumer-benefit basis.
    Tests: recommends a quarter-end window with positive expectedBpBenefit; the objective is per-user bp,
    not price movement; output has no side effects; a manipulation-framed input is rejected.

- id: collusion-detector
  title: Detect tacit cross-vendor price coordination (advisory report only; counsel-approved)
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/collusionDetector.test.js` exits 0
  prompt: |
    Add server/utils/collusionDetector.js (pure). The oracle uniquely sees cross-vendor pricing in real
    time, so it can flag suspicious parallel pricing / lockstep moves / signaling. Counsel (operator) has
    approved building the DETECTION engine. It MUST be advisory only: `detectParallelism(observations,
    {window})` -> { suspicionScore, pattern (parallel_move|lockstep_breaks|signaling), evidence (the
    aligned moves) } and `buildReport(findings)` -> a human-reviewable report object. It MUST NOT auto-file
    or auto-surface to anyone — surfacing to a regulator is a human/counsel decision (the engine produces
    the report; an OPERATOR item, not code, decides what to do with it). No PII; vendor-level aggregates
    only. Tests: flags an injected lockstep pattern; low score on independent pricing; report is a plain
    object with evidence; no side effects / no I/O.

OPERATOR:
  - consumer-rate-benchmark: counsel sign-off REQUIRED before any benchmark is publicly published or referenced externally — benchmark administration is regulated in some jurisdictions (e.g. EU BMR); confirm methodology disclosure, revision policy, and administrator obligations. The pure producer + methodology doc can be built/tested now; publication is gated.
  - collusion-detector: the engine only DETECTS and produces a report. Whether/how to surface any finding to a regulator or counterparty is a counsel decision made per-case — never wire auto-surfacing. Confirm liability/positioning posture before acting on any report.
  - demand-timing-orchestrator: counsel approved building it as advisory-only consumer-benefit timing. Keep it advisory; if it is ever wired to auto-send or to coordinate supply, that is a new counsel decision.
  - framing-eligibility: truthful-representation only; every suggestion requires user confirmation of the attestable basis. Confirm the consumer-compliance posture (this is choosing among true characterizations, not fabricating facts) before it drives any application.
  - EXCLUDED by request (do NOT queue): Tomorrow counterparty-of-last-resort; viral better-rate referral cohorts. Hold for a later decision.
