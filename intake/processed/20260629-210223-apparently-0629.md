PROJECT: apparently

- id: contracts-apparently
  title: Pin Tomorrow-signal contract + outbox handler interface + HMAC test
  material: no
  model: sonnet
  depends: []
  proof: `npx vitest run tests/engines/tomorrow-signal-contract.test.ts` exits 0
  prompt: |
    Add shared/contracts/tomorrow-signal.ts mirroring Tomorrow's xappSignal plus the existing
    rewards/gaming S2S shape, and an outbox handler interface for operational-risk signals. Add a
    fixed-vector HMAC contract test alongside tests/engines/rewards-s2s-contract.test.ts (same
    signing convention as server/engines/rewards/s2s.ts). See HANDOFF-cowork-master-build.md §5.

- id: apparently-tomorrow-client
  title: Apparently -> Tomorrow operational-risk S2S client + outbox handler
  material: yes
  model: sonnet
  depends: [contracts-apparently]
  proof: `npx vitest run tests/engines/tomorrow-client.test.ts` exits 0
  prompt: |
    Build server/utils/tomorrow-client.ts: an HMAC-SHA256 signer (reuse server/engines/rewards/s2s.ts
    patterns) that POSTs operational-risk signals to Tomorrow's /api/apparently/{bank,fabric,gaming,
    risk-studio}/signal endpoints, plus server/engines/outbox/handlers/operational-risk-handler.ts with
    retry + dead-letter. Enforce the information barrier: allowlist fields only (no participant PII /
    contract terms / internal pricing). Test: a signed signal round-trips and non-allowlisted fields
    are stripped before egress. Material: S2S secrets + cross-app data egress.

- id: apparently-signal-emitters
  title: Wire the 4 detection engines to emit signals to Tomorrow
  material: yes
  model: sonnet
  depends: [apparently-tomorrow-client]
  proof: `npx vitest run tests/engines/signal-emitters.test.ts` exits 0
  prompt: |
    Wire each detection engine to emit an xappSignal via the outbox operational-risk handler on its
    existing trigger: licensing/renewal-engine.ts (critical <30d), regulator-intel/portal-monitor.ts
    (filing.status_change), disclosure/burden-estimator.ts (filing-burden delta), email-triage/
    triage-engine.ts (high-risk inbound). Test (one per engine): a signal is emitted on the trigger
    and is information-barrier clean. Material: cross-app data egress.

OPERATOR:
  - Set TOMORROW_API_BASE_URL and REWARDS_S2S_SECRET / GAMING_S2S_SECRET (plus any new operational-risk S2S secret) in Apparently's env; keep counsel-gated feature flags (FEATURE_REWARD_TOKEN_RAILS, FEATURE_GAMBLING_LAW_LAYER) OFF until legal sign-off.
