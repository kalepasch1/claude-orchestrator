PROJECT: tomorrow

# Supersedes the perpetual-contracts framing in earlier tomorrow-0629 files.
# Final spec: an OTC exchange for CLAUSE-LEVEL legal-risk hedging, settled by an
# autonomous Determinations Engine (human-default where ISDA-covered; autonomous-
# default in the white space), with a Clause Registry, a clause risk heatmap, and
# a market->drafting feedback loop. We protect against externally-adjudicated
# adverse OUTCOMES per clause; we do NOT guarantee document correctness.

- id: legal-event-clause-definitions
  title: Canonical Clause & Legal-Event Definitions + trigger schema
  material: yes
  model: opus
  depends: []
  proof: `npx -y markdownlint-cli2 docs/legal-event-clause-definitions.md` exits 0
  prompt: |
    Write docs/legal-event-clause-definitions.md — the standardized taxonomy that
    makes clause-level hedges fungible (the ISDA-Definitions analog for legal
    risk). Define: (1) the machine-readable CLAUSE schema Apparently/Smarter emit
    (clause type, jurisdiction, governing law, dependencies on statutes/regs/
    precedents/agency guidance); (2) the taxonomy of ADVERSE OUTCOMES per clause,
    all exogenous + externally adjudicated (clause struck, narrowed, disallowed,
    rendered non-compliant by a rule change, or construed adversely in a dispute)
    — explicitly NOT "the clause was drafted wrong" (no correctness guaranty);
    (3) objective, verifiable trigger definitions for each adverse outcome with the
    evidence source; (4) versioning + governance of the definitions. State this is
    a market-standard design doc, not a filing and not legal advice.

- id: clause-registry-design
  title: Clause Registry + legacy-document decomposition/mapping
  material: yes
  model: opus
  depends: [legal-event-clause-definitions]
  proof: `npx -y markdownlint-cli2 docs/clause-registry-design.md` exits 0
  prompt: |
    Write docs/clause-registry-design.md. Design a canonical Clause Registry: each
    registry clause carries its adverse-outcome definitions (from docs/legal-event-
    clause-definitions.md) and a live protection price. Cover: (1) registry data
    model + identity/versioning of canonical clauses; (2) a decomposition/mapping
    method that ingests ANY document — including legacy/third-party paper — and
    maps its clauses to registry entries so the entire existing legal corpus
    becomes hedgeable, not just Apparently output (this is the TAM-expansion move);
    (3) confidence handling for imperfect matches; (4) how registry pricing drives
    the clause risk heatmap. Design only, no secrets.

- id: determinations-engine-spec
  title: Autonomous Determinations Engine spec (white-space default + Ledger + appeal)
  material: yes
  model: opus
  depends: [legal-event-clause-definitions]
  proof: `npx -y markdownlint-cli2 docs/determinations-engine-spec.md` exits 0
  prompt: |
    Write docs/determinations-engine-spec.md. The engine rules on whether a
    clause's adverse outcome occurred, using the adversarial expert-agent panel
    (same mechanism as Smarter opinion drafting/review). MUST specify: (1) DEFAULT
    LOGIC — human panel is default where ISDA-style frameworks already cover the
    contract/resolution type; the AUTONOMOUS engine is the DEFAULT in the white
    space (clauses/issues that never had a resolution mechanism due to cost or
    oversight); autonomous is ALWAYS offered as an emphasized alternative option
    with a value display (cost + time-to-resolution vs. the human/ISDA path);
    (2) the Superiority Ledger — published calibration (Brier/log-loss), accuracy
    vs. eventual ground truth, speed, cost, overturn-on-appeal rate, tracked per
    determination; the default flips to autonomous by PROOF against published
    thresholds; (3) the staked escalation/appeal for economic finality (stake to
    force higher-tier re-determination; overturned = refund + counterparty pays,
    confirmed = forfeit); (4) latency tiers (instant / standard-with-escalation /
    human) as a priced axis; (5) reproducibility (anyone can re-run) and audit;
    (6) the settlement-as-a-service adjacency. Design only; note live model API
    keys + caps are an operator prerequisite.

- id: otc-exchange-design
  title: Tomorrow OTC market structure for clause-level legal-risk swaps
  material: yes
  model: opus
  depends: [legal-event-clause-definitions]
  proof: `npx -y markdownlint-cli2 docs/otc-exchange-design.md` exits 0
  prompt: |
    Write docs/otc-exchange-design.md. Design Tomorrow as the OTC venue for
    perpetual clause hedges. Cover: (1) venue role (matched-principal dealer /
    central book), perpetual FUNDING-RATE mechanics (perp-swap style, positions
    persist + re-price to the live probability mark), netting/compression across
    participants; (2) the layered market — OTC core (parametric, exogenous,
    ECP-only -> swap characterization) + carrier/indemnity wrapper (retail/actual-
    loss) + embedded warranty (service remedy); (3) counterparties/capacity
    (reinsurers, ILS, specialist legal-risk funds) and legal risk as an
    uncorrelated asset class; (4) a LEMA (Legal Event Master Agreement) outline;
    (5) clause-risk tranching and corporate portfolio (aggregate) hedging;
    (6) the Legal Risk Yield Curve data product from the priced flow. Design only,
    no secrets. Honest framing: reduces (not eliminates) specific perils; the firm
    is never the dealer/issuer for its own clients.

- id: clause-pricing-heatmap-design
  title: Clause pricing, risk heatmap, and market->drafting feedback loop
  material: yes
  model: sonnet
  depends: [clause-registry-design, otc-exchange-design]
  proof: `npx -y markdownlint-cli2 docs/clause-pricing-heatmap.md` exits 0
  prompt: |
    Write docs/clause-pricing-heatmap.md. Define: (1) how each clause's protection
    is priced (Tomorrow market probability + Consensus Engine ensemble + the
    underwriting information edge from having drafted it); (2) the clause RISK
    HEATMAP — a per-clause green/red risk X-ray of any document, with the premium
    as a quoted confidence/"truth" signal; (3) the MARKET->DRAFTING FEEDBACK LOOP:
    risky/expensive clause prices flow back into Apparently/Smarter drafting so
    the next draft steers to safer clauses (the self-de-risking flywheel);
    (4) adverse-selection + moral-hazard controls (exogenous triggers; drafting
    walled from settlement; drafter never a counterparty). Worked numeric
    examples. Methodology only, no live data.

- id: determinations-superiority-ledger-prototype
  title: Prototype the Superiority Ledger (backtest + calibration harness)
  material: no
  model: opus
  depends: [determinations-engine-spec]
  proof: `python3 -m pytest -q prototype/superiority_ledger/tests` exits 0
  prompt: |
    Build a standalone prototype under prototype/superiority_ledger/ (independent
    of the main app stack). Implement per docs/determinations-engine-spec.md:
    (1) a backtest harness that replays historical legal-event scenarios (use
    synthetic/fixture data, no external calls, no secrets) through a MOCK
    autonomous panel and a MOCK human panel; (2) calibration metrics (Brier score,
    log-loss), accuracy vs. ground truth, overturn rate, plus cost + time-to-
    resolution comparison; (3) the value-display object (autonomous vs human:
    cost, latency, calibration) and a threshold check that decides whether
    autonomous qualifies as default for a given contract type. Add
    prototype/superiority_ledger/tests with pytest covering the metric math, the
    threshold gate, and the value-display contract. README notes live model keys
    are an operator prerequisite for non-mock runs. No secrets.

- id: perpetual-clause-hedge-prototype
  title: Reference prototype of the clause-hedge lifecycle (mocked)
  material: yes
  model: opus
  depends: [determinations-engine-spec, clause-pricing-heatmap-design]
  proof: `python3 -m pytest -q prototype/clause_hedge/tests` exits 0
  prompt: |
    Build a standalone prototype under prototype/clause_hedge/ (independent of the
    main app stack). Implement per the design docs: (1) clause + adverse-outcome
    trigger parsing from the definitions schema; (2) a MOCK Determinations oracle
    that accepts simulated external-adjudication events and verifies triggers
    (no live data/secrets); (3) a funding-rate pricing stub with deterministic
    inputs and a clause heatmap output; (4) the issue -> active -> triggered ->
    settled state machine with a MOCK counterparty; (5) an Assurance Wrap that
    bundles multiple clause hedges for one document sized to the heatmap;
    (6) audit log. Add prototype/clause_hedge/tests with pytest covering trigger
    verification, funding/pricing math, full lifecycle settlement, the wrap
    bundling, idempotent re-delivery, and an information-wall assertion (MNPI /
    drafting-side fields never cross to the market-side interface). README: this
    is a prototype; carrier/CFTC/secrets are operator prerequisites for real
    issuance. No secrets in code.

OPERATOR:
  - Legal characterization sign-off per instrument (swap vs insurance vs financial-guaranty) with regulatory counsel BEFORE any live issuance; state insurance/gambling counsel where relevant.
  - Licensed carrier/reinsurer/ILS capacity for the indemnity/retail wrapper — external counterparties, not code.
  - CFTC swap/venue registration + reporting posture; swap-dealer de-minimis analysis for Tomorrow's dealer role.
  - Production market/counterparty API credentials + exposure/spend caps — secrets, do not commit.
  - Licensing of real historical legal-event datasets for non-mock Superiority Ledger backtests.
  - Production deployment of any live issuance/settlement path — gated on the above.
