PROJECT: pareto-2080

# CORRECTION WAVE. The test suite is RED (≈28 failures / 1979) and multiple agent-authored engines
# merged with real defects, incl. a wrong tax constant. Three fixes are ALREADY DONE by hand and
# committed to the working tree — do NOT redo: (a) irsFloatOptimizer MFS safe-harbor threshold
# ($75k is MFS-only; everyone else $150k), (b) hiveNimFloat.js converted CJS→ESM, (c) new
# server/utils/taxConstants.js source-of-truth + tests. Prove with `node --test tests/*.test.js`.
# Every task's proof is a passing test; NONE may pass by weakening an assertion to a wrong value —
# fix the CODE to the correct real-world rule. Reference existing engines; ESM only ("type":"module").

- id: gate-esm-cjs-guard
  title: Make the merge gate fail on CJS-in-ESM and on any red test
  material: yes
  model: sonnet
  depends: []
  proof: `node scripts/lint-esm.mjs` exits non-zero when a server/utils or tests file uses require()/module.exports, and exits 0 on the current tree after fixes
  prompt: |
    ROOT CAUSE: hiveNimFloat.js merged using CommonJS `require`/`module.exports` in an ESM project —
    its test could never have passed, yet it merged. The gate is not catching this.
    DELIVERABLE: add `scripts/lint-esm.mjs` that scans `server/utils/**` and `tests/**` for
    `require(`, `module.exports`, or `exports.` and exits non-zero listing offenders. Wire it into the
    npm `test` script (run lint BEFORE `node --test`) so a CJS file fails the gate. Document in
    CLAUDE.md that engines/tests are ESM-only. Do not rewrite unrelated files; only add the linter +
    package.json script + doc line.
    PROOF: `node scripts/lint-esm.mjs` exits 0 on a clean tree and non-zero if a CJS token is present.

- id: tax-golden-conformance
  title: Golden-value tax conformance suite + migrate tax engines onto taxConstants.js
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/taxGoldenConformance.test.js` exits 0
  prompt: |
    A wrong MFS threshold merged because tax numbers are hard-coded per engine. server/utils/taxConstants.js
    now exists (safeHarborAgiThreshold, safeHarborMultiplier, standardDeduction — MFS=$75k, others=$150k).
    DELIVERABLE: (1) migrate irsFloatOptimizer.js and every other tax engine that hard-codes a threshold
    (grep for 150000 / 75000 / 12% brackets / standard deduction) to import from taxConstants.js — single
    source of truth. (2) Create tests/taxGoldenConformance.test.js with worked examples from IRS Pub 505
    / Pub 17 (estimated-tax safe harbor by status incl. MFS, standard deduction by status). Every tax
    engine must reconcile to these golden values. Correct the CODE, never the golden number.
    PROOF: `node --test tests/taxGoldenConformance.test.js` exits 0.

- id: fix-fdic-spreading-semantics
  title: Resolve capacityUsd-vs-insured-cap ambiguity in fdicSpreading
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/fdicSpreading.test.js` exits 0
  prompt: |
    server/utils/fdicSpreading.js has two conflicting behaviors: `capacityUsd` in the rate surface is
    treated as a placement cap, but the "residual uninsured" test expects the full $250k insured cap per
    bank to be usable regardless of capacityUsd. Decide the model explicitly: FDIC insured headroom per
    institution is min($250k insured cap, capacityUsd the bank will actually accept). Then make
    uninsuredAfterUsd = max(0, total − Σ insuredHeadroom). Reconcile BOTH tests: "residual uninsured is
    reported" ($1M across 3×$250k ⇒ $250k uninsured) AND "allocation respects per-institution capacity
    limits". If those two tests are mutually inconsistent, adjust the test whose expectation contradicts
    real FDIC rules and document why in a comment. Also fix "empty rate surface handles gracefully".
    PROOF: `node --test tests/fdicSpreading.test.js` exits 0.

- id: fix-remaining-engine-tests
  title: Fix the ~13 remaining red tax/treasury engine tests to correct logic
  material: yes
  model: opus
  depends: [tax-golden-conformance]
  proof: `node --test tests/moneyVelocity.test.js tests/megaBackdoorRoth.test.js tests/charitableBunching.test.js tests/assetLocation.test.js tests/rothConversionLadder.test.js tests/estimatedTaxAnnualizer.test.js tests/selfInsuranceCrossover.test.js` exits 0
  prompt: |
    These engines merged with failing tests. Fix each to the correct real-world rule (tax numbers via
    taxConstants.js), not by weakening assertions: moneyVelocity (safeComputeMoneyVelocity must wrap
    errors and return an error object, not throw); megaBackdoorRoth; charitableBunching (MFS threshold);
    assetLocation; rothConversionLadder; estimatedTaxAnnualizer (Form 2210 Sched AI); selfInsuranceCrossover.
    For any test that encodes a WRONG rule (e.g. joint using the $75k threshold), correct the test to real
    law and note it. Run the full suite afterward to confirm no new breakage.
    PROOF: the listed `node --test ...` command exits 0.

- id: fix-quarantine-invariant
  title: Restore quarantinedReader invariant — no untrusted-text logging
  material: yes
  model: sonnet
  depends: []
  proof: `node --test tests/quarantinedReader.test.js` exits 0
  prompt: |
    SECURITY: `tests/quarantinedReader.test.js` "invariant 5 - no untrusted-text logging" is failing.
    Untrusted external content (emails/documents ingested via emailIntel/smarterDocumentAdapter) must
    never be written to logs and must never be able to trigger a Tier-C action. Find why invariant 5
    regressed and restore it. Do not loosen the invariant. Confirm smarterDocumentAdapter still routes
    untrusted content only to Tier-A/Tier-B.
    PROOF: `node --test tests/quarantinedReader.test.js` exits 0.

- id: wire-engines-into-briefing
  title: Wire the stranded tax/fdic/medical/velocity engines into the treasury briefing
  material: no
  model: sonnet
  depends: [fix-remaining-engine-tests]
  proof: `node --test tests/treasuryBriefingWiring.test.js` exits 0
  prompt: |
    ~20 merged engines produce ZERO user-visible value because nothing feeds their output into
    assembleTreasuryBriefing. In server/utils/personalTreasury.js add a pure helper
    `buildDepartmentSignals(inputs)` that maps engine outputs → the signal shape {department,title,
    annualValueUsd|oneTimeValueUsd,status,tier,urgency}: irsFloatOptimizer→tax, fdicSpreading→treasury,
    medicalBillAudit→legal/negotiator, moneyVelocity→treasury (idle-drag), selfInsuranceCrossover→
    (insurance via legal). Then update server/api/personal/treasury/index.get.js to call the real engines
    (defensive try/catch, skip on missing data) and pass their signals into the briefing. Keep the existing
    response shape; only ADD signals. NOTE: index.get.js is actively edited — rebase before writing.
    PROOF: create tests/treasuryBriefingWiring.test.js asserting buildDepartmentSignals emits a `tax`
    department when given an irsFloat result with recoverable float, and that assembleTreasuryBriefing
    includes it; exits 0.

- id: treasury-ui-tax-velocity-section
  title: Surface tax dept, money-velocity, and FDIC insurance status on the Treasury page
  material: no
  model: sonnet
  depends: [wire-engines-into-briefing]
  proof: `npx nuxt build` exits 0
  prompt: |
    In pages/Treasury/[userId].vue add sections that render the newly-wired signals when present, using the
    existing design-system classes (.card/.k/.v/.hl/.liq-grid) and the deptIcon map (tax:🧾): (1) a "Tax
    department" card showing recoverable IRS float + top pre-emptive tax actions; (2) a "Money velocity"
    card showing velocityScore and idle-drag $; (3) an "FDIC coverage" line in the liquidity section showing
    insured vs uninsured. Guard every section with v-if on the data. Do NOT use layout:false (keep default
    layout). Respect the existing intentional edits (JIT control, approve button, ledger rollup).
    PROOF: `npx nuxt build` exits 0.

- id: requeue-tax-keystone
  title: Re-queue the purged tax keystone with concrete, buildable prompts
  material: no
  model: opus
  depends: [tax-golden-conformance]
  proof: `node --test tests/taxReturnEngine.test.js tests/acaMagiController.test.js tests/gapYearTaxOptimizer.test.js tests/taxDepartment.test.js` exits 0
  prompt: |
    The wave-3 tax keystone was purged by the intake janitor as "non-actionable"; it never got built.
    Build these four PURE-LOGIC engines (ESM, taxConstants.js for all numbers, makeExplanation), each with
    a node:test: (1) server/utils/taxReturnEngine.js — compute AGI, taxable income, ordinary+LTCG-stacked
    tax, credits (CTC/EITC/education/saver's), AMT check, withholding reconciliation, refund/owe, line-
    itemized. (2) server/utils/acaMagiController.js — choose withdrawal mix (Roth/basis invisible;
    traditional/gains visible) to land MAGI under a chosen ACA cliff. (3) server/utils/gapYearTaxOptimizer.js
    — jointly optimize MAGI × Roth conversion × IRMAA × 0% LTCG across gap years (compose #2 +
    rothConversionLadder.js). (4) server/utils/taxDepartment.js — year-round orchestrator producing a ranked
    pre-emptive action list + quarterly schedule + a filing packet from #1. Each test asserts a hand-computed
    golden case. E-file/PII stays OPERATOR.
    PROOF: the four `node --test ...` files exit 0.

OPERATOR:
  - Investigate why the intake janitor purged the wave-3 tax tasks as "garbage/non-actionable" so the
    requeue-tax-keystone task is not purged again; adjust the janitor heuristic or whitelist these slugs.
  - After the suite is green, run the full `node --test tests/*.test.js` in Linux CI with the
    prisma binaryTargets fix so agentLedger/bookingSaga pass and the gate is trustworthy end-to-end.
