PROJECT: apparently

# Frontier 20-200X layer — examiner twin, outcome learning, system-of-record, trust-as-product, distribution.
# Builds ON the Hive layer already shipped (server/engines/hive/*, gaming-enablement/*) and the
# wirings queued in intake/apparently-0629.md (verifier promotion, arbitrage hook, exposure source,
# autonomy gate, etc. — referenced by id in prose, NOT by cross-file depends).
# Conventions: typed Supabase client SupabaseClient<Database> from ~/types/database.types (the Hive
# engines were just migrated to typed clients — match that); callClaudeStructured + selectModel (never
# hardcode model strings); logger (no console.log); idempotent name-checked migrations; pure cores
# unit-testable; material/legal steps human-gated. Reuse existing engines, do not rebuild:
# examiner-behavior-model.ts, simulated-regulator.ts, regulator-mood.ts, examiner-tracker.ts,
# outcome-attribution.ts, success-rate-ledger.ts, universal-field-mapper.ts, verifiable-credentials/,
# compliance-passport/, audit-chain.ts, autonomous-faq-builder.ts, sponsor-matching/,
# competitive-intelligence.ts, portal-auto-submission.ts, portal-health-monitor.ts, draft-quality-floor.ts.

# ── FOUNDATIONS (several frontier tasks depend on these) ─────────────────────

- id: outcome-label-store
  title: Filing-outcome label store (approval/denial/deficiency as training signal)
  material: yes
  model: sonnet
  depends: []
  proof: "migration name-checks clean (`SELECT to_regclass('public.filing_outcomes')` is NULL pre-apply) AND `npx vitest run tests/engines/learning/outcome-label-store.test.ts` exits 0 (recordOutcome persists a labeled outcome keyed by jurisdiction×vertical×examiner; query returns it)"
  prompt: |
    Create the labeled-outcome substrate every learning loop needs. Migration
    supabase/migrations/<next>_filing_outcomes.sql: table filing_outcomes (id, org_id, engagement_id,
    vertical_key, jurisdiction, examiner_id nullable, outcome enum{approved,denied,deficiency,withdrawn},
    deficiency_category nullable, cycle_days int, predicted_confidence numeric, notes, source, created_at;
    RLS + service-role policy; idempotent). Engine server/engines/learning/outcome-label-store.ts:
    recordOutcome(client, input) + queryOutcomes(client, {verticalKey,jurisdiction,examinerId,limit}) +
    a pure summarizeOutcomes(rows) → {approvalRate, avgCycleDays, topDeficiencyCategories}. Typed client.
    Unit test the pure summarizer + a mocked-client persist/query. MATERIAL: new schema. Do NOT wire it to
    auto-change behavior here — that's outcome-learning-loop.

- id: attestation-signing-core
  title: Harden verifiable-credential signing/verify core (offline-verifiable)
  material: yes
  model: opus
  depends: []
  proof: "`npx vitest run tests/utils/attestation-signing.test.ts` exits 0 (sign→verify round-trips; tampered payload fails; verification needs no platform secret — public-key/offline verifiable)"
  prompt: |
    Expose a single, audited signing/verification primitive that the passport, proof-carrying, and embed
    tasks all reuse. Build server/utils/attestation-signing.ts on top of the existing
    verifiable-credentials/ + audit-chain.ts: signAttestation(payload) → {payload, contentHash(SHA-256),
    signature, publicKeyId, issuedAt} and verifyAttestation(envelope) that is STATELESS — recompute the
    content hash and check the signature against the embedded/public key, requiring NO platform secret
    (third parties must be able to verify offline). Use asymmetric signing (Ed25519); the private key
    comes from env (OPERATOR provisions it). With no key, mint content-addressed-only envelopes
    (algorithm:'none') for tests. Unit-test: round-trip, tamper-detection, foreign-key rejection,
    unsigned content-only. MATERIAL: it's the trust root for legal attestations.

- id: applicant-ontology
  title: Write-once applicant ontology (canonical entity/person/ownership graph → any form)
  material: yes
  model: opus
  depends: []
  proof: "migration name-checks clean AND `npx vitest run tests/engines/ontology/applicant-ontology.test.ts` exits 0 (a fact captured once resolves into field values for >=2 different vertical forms via the mapper)"
  prompt: |
    Push universal-field-mapper.ts into a canonical ontology so every atomic fact about an
    entity/person/ownership is captured once and auto-maps to ANY form in ANY vertical/jurisdiction.
    Migration supabase/migrations/<next>_applicant_ontology.sql: applicant_facts (org_id, subject_type
    {entity,person,ownership_edge}, subject_id, attribute, value jsonb, source, valid_until, confidence,
    created_at) + RLS. Engine server/engines/ontology/applicant-ontology.ts: upsertFact, getFacts,
    and a pure mapFactsToForm(facts, formSchema) that resolves a form's fields from the canonical facts
    (reuse universal-field-mapper's field dictionary). Unit-test the pure mapper across two vertical form
    schemas (e.g. gaming_supplier + msb) proving one fact set fills both. MATERIAL: schema. This is the
    write-once substrate that makes new verticals near-zero-marginal-cost.

# ── KILL THE DEFICIENCY LOOP ─────────────────────────────────────────────────

- id: examiner-twin
  title: Per-jurisdiction examiner digital twin (pre-submission deficiency prediction)
  material: no
  model: opus
  depends: [outcome-label-store]
  proof: "`npx vitest run tests/engines/examiner/examiner-twin.test.ts` exits 0 (given a packet fixture + examiner profile, predictDeficiencies returns ranked likely deficiencies with citations; an empty/complete packet predicts none)"
  prompt: |
    Compose examiner-behavior-model.ts + simulated-regulator.ts + regulator-mood.ts + examiner-tracker.ts
    + the filing_outcomes history (outcome-label-store) into an adversarial examiner twin. Build
    server/engines/examiner/examiner-twin.ts: predictDeficiencies(client, {packet, verticalKey,
    jurisdiction, examinerId?}) → ranked predicted deficiencies (category + rationale + the prior outcomes
    that support each) BEFORE submission, using callClaudeStructured(selectModel('adversarial_review')) with
    the examiner's history injected. This is distinct from the filing-ready gate (facts) — it simulates the
    human reviewer. ADVISORY only (no submission). Unit-test with a mocked AI + fixture history. Surface its
    output into the pre-submission review UI.

- id: outcome-learning-loop
  title: Closed-loop learning — outcomes tune determination/draft/confidence per segment
  material: yes
  model: opus
  depends: [outcome-label-store]
  proof: "`npx vitest run tests/engines/learning/outcome-learning.test.ts` exits 0 (feeding labeled approvals/denials shifts the per-(jurisdiction×vertical) calibration weights and the next prediction moves in the expected direction; weights persist)"
  prompt: |
    Turn filing_outcomes into a calibration loop. Engine server/engines/learning/outcome-learning.ts:
    from labeled outcomes, derive per-(jurisdiction×vertical×examiner) adjustments to (a) the determination
    engine's risk weights, (b) draft-generator emphasis (which sections cause deficiencies), (c) confidence
    scoring calibration. Store adjustments in the applied_config KV pattern the platform already uses; read
    them in the relevant engines via a getEvolvedParam-style hook. Pure core: deriveAdjustments(outcomes,
    prior) unit-tested (monotone: more denials on a factor → higher predicted risk on it). MATERIAL: it
    changes what the determination/draft engines produce — gate material weight changes for human approval
    (reuse the self-improvement materiality gate); non-material calibration auto-applies. Do not let a single
    outcome swing weights (require a minimum sample).

- id: filing-warranty
  title: Filing warranty / E&O risk pricing from confidence + realized error rate
  material: yes
  model: opus
  depends: [outcome-label-store]
  proof: "`npx vitest run tests/engines/trust/filing-warranty.test.ts` exits 0 (computeWarranty returns price + coverage from confidence and segment error-rate; below the quality floor it returns offered=false; high-confidence+low-error returns a bounded price)"
  prompt: |
    Because every step is logged + confidence-scored, the platform can price its own error risk and offer a
    guarantee law firms can't. Engine server/engines/trust/filing-warranty.ts: pure computeWarranty({
    packetConfidence, segmentErrorRate (from filing_outcomes), exposureUsd, verticalKey}) →
    {offered:boolean, priceCents, coverage, rationale}. Below the draft-quality-floor or with thin outcome
    data → offered:false (default-closed). Unit-test the pricing monotonicity + floor. MATERIAL & legal/
    financial: this is a binding commitment — it must NOT auto-issue; it produces a quotable offer object
    only, and OPERATOR/counsel + an underwriting decision gate actual issuance (see OPERATOR).

# ── BECOME THE SYSTEM OF RECORD ──────────────────────────────────────────────

- id: license-passport
  title: Live, verifiable license/compliance passport (counterparty-checkable)
  material: yes
  model: opus
  depends: [attestation-signing-core]
  proof: "`npx vitest run tests/api/passport.test.ts` exits 0 (issue a passport for an entity; a public verify endpoint confirms signature + currency with no auth/secret; an expired or tampered passport is rejected)"
  prompt: |
    The move that makes Apparently the system of record. Build on compliance-passport/ +
    attestation-signing-core: issuePassport(client, entityId) assembles the entity's current license/
    compliance status (from licenses/applications/compliance_status) into a time-boxed, Ed25519-signed
    envelope; a PUBLIC verify route server/api/passport/[id]/verify.get.ts confirms signature + that the
    status is still current — offline-verifiable, no platform secret. Counterparties (banks, processors,
    the support entities in support-entity-exposure.ts) verify instantly. Unit-test issue/verify/expiry/
    tamper. MATERIAL: it's an authoritative compliance credential — issuance is service-role + entity-
    consented; the verify route is read-only public. This weaponizes the support-entity work into a
    two-sided network (a processor checks the passport before serving an operator).

- id: predictive-prepositioning
  title: Predictive license pre-positioning from growth trajectory + diffusion
  material: no
  model: sonnet
  depends: []
  proof: "`npx vitest run tests/engines/hive/prepositioning.test.ts` exits 0 (given a client footprint + growth trajectory, planPrepositioning returns the future licenses needed with file-by dates ahead of need)"
  prompt: |
    Turn compliance from reactive to anticipatory. Engine server/engines/hive/prepositioning.ts:
    planPrepositioning(client, {orgId, currentFootprint, trajectory}) combines the org's expansion plan with
    forecastSubjectDiffusion (server/engines/hive/regime-diffusion.ts — note: it relies on KG modeled_after
    edges seeded by the hive-kg-modeled-after-seed task in intake/apparently-0629.md) + per-vertical
    processing timelines (vertical registry getTimelines) to produce the licenses the client will need 1-2
    quarters out and the file-by date for each (work backward from need date − processing time − prep). Pure
    core unit-tested with fixtures. Advisory output; no auto-filing.

# ── TRUST AS A PRODUCT ───────────────────────────────────────────────────────

- id: proof-carrying-compliance
  title: Proof-carrying filings/opinions (externally verifiable, privacy-preserving)
  material: yes
  model: opus
  depends: [attestation-signing-core]
  proof: "`npx vitest run tests/engines/trust/proof-carrying.test.ts` exits 0 (a filing/opinion emits a proof that verifies offline that it was produced under the constitution against verified reg_facts; altering any cited fact or the constitution hash invalidates it)"
  prompt: |
    Make autonomous legal work auditable by regulators/insurers without exposing client data. Engine
    server/engines/trust/proof-carrying.ts: on opinion/filing finalize, mintComplianceProof({subjectId,
    constitutionHash, verifiedFactIds[], stepDigest}) via attestation-signing-core → a verifiable envelope
    binding the work product to (a) the policy constitution version it ran under and (b) the verified
    reg_facts it relied on. verifyComplianceProof(envelope, {factResolver}) re-checks offline. Where client
    data is sensitive, commit to hashes not raw content (ZK-style commitment is acceptable as hash-commit
    now; flag true ZK as a follow-up). Unit-test verify + invalidation on changed fact/constitution. MATERIAL:
    trust attestation over legal work.

- id: enforcement-driven-acquisition
  title: Enforcement radar → exposed-entity identification → compliant outreach drafts
  material: yes
  model: sonnet
  depends: []
  proof: "`npx vitest run tests/engines/gtm/enforcement-acquisition.test.ts` exits 0 (given an enforcement wave fact + relationship graph, identifyExposed returns the exposed operators/support-entities and draftOutreach returns per-entity drafts; nothing is sent)"
  prompt: |
    Turn the regulatory firestorm into a lead engine. Engine server/engines/gtm/enforcement-acquisition.ts:
    on a gaming enforcement reg_fact (lifecycle enforced/effective), identifyExposed(client, fact) uses the
    support-entity relationship source + sponsor-matching/ + competitive-intelligence.ts to list exposed
    operators AND their support entities, then draftOutreach(entity, fact) drafts compliant outreach citing
    that entity's SPECIFIC exposure. Pure identify/draft cores unit-tested. MATERIAL & compliance-sensitive:
    DRAFTS ONLY — never auto-send (CAN-SPAM/solicitation review is an OPERATOR/legal gate); queue drafts for
    human review.

# ── DISTRIBUTION / GTM ───────────────────────────────────────────────────────

- id: compliance-api-embed-kit
  title: Compliance-as-an-API embed kit ("am I licensed to do X here?")
  material: yes
  model: opus
  depends: [applicant-ontology]
  proof: "`npx vitest run tests/api/embed-kit.test.ts` exits 0 (an embed token issues; the determination endpoint returns a vertical/jurisdiction licensing determination; an un-allowlisted origin is rejected; the determination is compute-only, never a filing)"
  prompt: |
    Become infrastructure others build on. Embed kit so fintechs/neobanks/gaming platforms embed licensing
    determinations + filing workflows. server/engines/embed/embed-kit.ts: issueEmbedToken(orgId, {scopes,
    allowedOrigins}) + a determination function answerLicensingQuestion({vertical, jurisdiction, activity})
    reusing the vertical registry + reg_facts (NOT a new rules engine). Public, CORS-validated, token-scoped
    route server/api/embed/[token]/determine.post.ts — COMPUTE-ONLY (returns a determination; never submits a
    filing). Unit-test token issue/scope, determination, origin rejection. MATERIAL: new public API surface +
    auth; default-deny CORS; rate-limited.

- id: drafts-as-precedent
  title: Opinion precedent corpus (index our own work product, retrieve to strengthen the next)
  material: no
  model: sonnet
  depends: []
  proof: "`npx vitest run tests/engines/corpus/drafts-as-precedent.test.ts` exits 0 (finalizing an opinion indexes it as firm_work_product; a semantically similar query retrieves it; superseded opinions are excluded)"
  prompt: |
    Every opinion becomes proprietary searchable precedent that strengthens the next. Build
    server/engines/corpus/drafts-as-precedent.ts: indexWorkProductPrecedent(opinion) on finalize (embed +
    store as firm_work_product, reusing the corpus/embedding infra already in the repo — see CLAUDE.md notes
    on corpus + indexWorkProductPrecedent) and retrievePrecedent(query) feeding the opinion drafter/RLO
    shared context. Exclude superseded/withdrawn. Unit-test index + retrieve + supersession exclusion (mock
    the embedder). This is a compounding, unforkable moat.

- id: autonomous-faq-kb
  title: Always-current public knowledge base auto-generated from verified reg_facts
  material: no
  model: sonnet
  depends: []
  proof: "`npx vitest run tests/engines/corpus/faq-kb.test.ts` exits 0 (buildKbEntries from a set of verified reg_facts produces SEO-structured entries; a reg_fact lifecycle change marks the affected entry stale for regeneration)"
  prompt: |
    Capture organic top-of-funnel demand law firms can't maintain at 50-state × N-vertical granularity.
    Extend autonomous-faq-builder.ts → server/engines/corpus/faq-kb.ts: buildKbEntries(facts) generates
    public, SEO-structured Q&A ("is sweepstakes-casino legal in <state> in 2026") ONLY from VERIFIED current
    reg_facts (never unverified), each entry carrying its source_url + last-updated. onFactChange(factId)
    marks affected entries stale for regeneration so the KB self-updates the day a fact changes. Pure
    generation + staleness cores unit-tested. Output is data (the publishing surface is a separate UI task).

- id: frontier-ops-surfaces
  title: Ops/UX surfaces for examiner-twin, passport, warranty, KB, embed, precedent
  material: no
  model: sonnet
  depends: [examiner-twin, license-passport, filing-warranty, autonomous-faq-kb, compliance-api-embed-kit, drafts-as-precedent]
  proof: "`npx vue-tsc --noEmit` exits 0 AND the new surfaces render examiner-twin predictions, passport issue/verify, warranty quotes, KB entries, embed-token management, and precedent search against real columns"
  prompt: |
    Surface the frontier layer in the ops/app UI matching the existing ops-*.html / app dashboard pattern
    (read-mostly; reuse scaffolding). Pages: pre-submission examiner-twin deficiency predictions; passport
    issue + public verify status; warranty quote viewer (offer object only, issuance gated elsewhere);
    auto-FAQ KB browser; embed-token management + scopes; opinion precedent search. No new write paths beyond
    those the engines expose. Prove with vue-tsc clean + column-accurate reads.

OPERATOR:
  - Provision the Ed25519 signing key (env, e.g. ATTESTATION_SIGNING_PRIVATE_KEY_PEM, ideally HSM/KMS-backed) for attestation-signing-core; publish the public key so third parties can verify passports/proofs offline. Blocks real-signature behavior in license-passport + proof-carrying.
  - Deploy the three new migrations (filing_outcomes, applicant_ontology, and any passport/embed tables) to prod after name-checking against the live Supabase project. Blocks the tasks that read/write them.
  - Legal/underwriting sign-off REQUIRED before filing-warranty issues any binding guarantee and before any E&O coverage is represented to clients — the engine only produces quotable offers; do not enable issuance without counsel + an underwriting decision.
  - Counsel review of license-passport as an authoritative compliance representation (liability if status is wrong) and of proof-carrying-compliance claims before either is shown to regulators/counterparties.
  - CAN-SPAM / solicitation / UPL review before any enforcement-driven-acquisition outreach is sent — drafts only until cleared.
  - Public API terms of service, auth model, and rate-limit/abuse review before compliance-api-embed-kit goes live externally.
  - SEO/publishing decision + disclaimer review before the autonomous-faq-kb is published publicly (it states legal status by state).
