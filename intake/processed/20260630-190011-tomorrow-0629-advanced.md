PROJECT: tomorrow

# Wave 2 — reg-diff / final-rule prediction + cross-corpus entity link to live deal flow.
# Depends conceptually on tomorrow-0629.md (research-endpoint). Corpus deployed on Supabase edetxpcoaiqlqrwyzltw.

- id: reg-diff-engine
  title: Reg-diff engine (current rule vs proposed rule vs comments)
  material: no
  model: opus
  depends: []
  proof: `npx vitest run server/utils/__tests__/reg-diff.test.ts` exits 0
  prompt: |
    Add server/utils/reg-diff.ts `diffRules(currentText, proposedText)` (section-aligned textual diff +
    aiCall summary of substantive changes) and `summarizeComments(commentDocs)` (who argued what, grouped by
    position). Expose POST /api/research/reg-diff { currentDocId, proposedDocId } returning the structured diff +
    comment landscape. Pure diff helper unit-tested; aiCall summary fail-soft. Read-only over the corpus.

- id: final-rule-prediction
  title: Predict how a pending proposal lands (NPRM -> final-rule model)
  material: yes
  model: opus
  depends: [reg-diff-engine]
  proof: `npx vitest run server/utils/__tests__/rule-predict.test.ts` exits 0
  prompt: |
    The corpus is its own training set: it contains (NPRM, comments, final rule) triples. Build a baseline
    predictor server/utils/rule-predict.ts `predictAdoption(proposedSection, comments)` that scores likelihood a
    proposed provision survives to the final rule (start heuristic: comment-volume + sentiment + agency-history
    features; structured for a learned model later). Persist predictions to a rule_predictions table (migration)
    for later backtesting against actual outcomes. Unit-test the feature/scoring logic on synthetic triples.
    Material (new table). Forward-looking product clients pay for.

- id: corpus-entity-dealflow-link
  title: Link corpus entities to live counterparties (war-room intel)
  material: yes
  model: sonnet
  depends: []
  proof: `npx vitest run server/utils/__tests__/entity-link.test.ts` exits 0
  prompt: |
    Connect the corpus to live deal flow: match a war-room counterparty / participant name against entities
    extracted from enforcement/litigation docs (fuzzy normalized match) and surface "counterparty X was named in
    CFTC enforcement 2023 (cite)" in the war room. Add server/utils/entity-link.ts `linkEntity(name, entities)`
    (normalized fuzzy match + score threshold) and an advisory call into the war-room intel panel. Reads corpus
    entity data (depends on Apparently entity-knowledge-graph pattern; replicate the extractor for Tomorrow's
    corpus). Unit-test the matcher (true match, near-miss rejection). Material — it writes an intel/alert record
    against a live counterparty; fail-soft + advisory only, no posture change.

OPERATOR:
  - Approve prod apply of rule_predictions + any entity tables (edetxpcoaiqlqrwyzltw).
  - final-rule-prediction backtesting needs the historical NPRM->final-rule triples — depends on the completeness walk having ingested final rules + their NPRMs + comments.
