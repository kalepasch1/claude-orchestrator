PROJECT: tomorrow

# Optimization + remediation batch from 2026-07-08 full-fleet review (CADE, war room,
# optimization loops, OTC generation, IOI matching, security, UI/UX).
# Every task: verify the cited finding in-repo FIRST (files may have moved); if already
# fixed, note it in the PR and do the nearest unfixed adjacent item. Follow CLAUDE.md:
# pure functions in server/utils (no Date.now/IO), requireAuth FIRST in sensitive routes,
# fail-closed, structured errors '[CODE] msg', migrations immutable once applied.

- id: warroom-email-bridge-hardening
  title: Fail-closed email bridge — block low-quality extraction, validate clause mapping, per-sender rate limit
  material: yes
  model: opus
  depends: []
  proof: `npx vitest run` exits 0 AND `npx tsc --noEmit --skipLibCheck` exits 0
  prompt: |
    Harden server/utils/warRoom/emailBridge.ts (war room inbound email → live_clause_edits pipeline):
    1) The emission guard scoreExtractionQuality() currently sets lowQuality=true but processing
       continues (approx line 107). Make it fail-closed: when lowQuality, do NOT create phantom
       live_clause_edits or trigger bots; persist the NegotiationEmail with a status like
       'extraction_rejected' and surface a UI-visible reason so the user can re-upload.
    2) mapPositionsToClauses() consumes aiCall() output unvalidated. Add a Zod schema for the
       mapping result; every mapped clause type must exist in the clause template source of truth;
       unknown clause types are dropped and logged, never created. Use the existing fail-open aiCall
       fallback pattern only for availability, never for validity.
    3) Rate limiting is per-workspace only (5/min). Add a composite key (workspace + sender address
       hash) so one noisy sender can't exhaust a workspace budget, and keep behavior deterministic/testable
       (inject clock per repo convention — no Date.now() in pure utils).
    4) Record provenance on every live_clause_edit created by the bridge: createdVia='email_bridge',
       source email id.
    Add unit tests for all four behaviors (malformed PDF fixture → rejected; hallucinated clause
    type → dropped; rate limit composite key; provenance fields present). Do not weaken any existing test.

- id: cron-market-data-fail-closed
  title: Never fix lifecycle events at 0 when market data is unavailable
  material: yes
  model: sonnet
  depends: []
  proof: `npx vitest run` exits 0 AND `npx tsc --noEmit --skipLibCheck` exits 0
  prompt: |
    server/api/cron/trigger-processor.get.ts falls back to fixingValue = 0 when the market data
    service / FRED / spot fetcher are unavailable (approx lines 156, 200, 262). A silent 0 fixing
    corrupts downstream cash flows. Change to fail-closed:
    - Introduce a marketDataResult type { value: number|null, source: string } in a pure util.
    - When no source returns data: do NOT write a fixing; mark the lifecycle event
      'fixing_deferred' (or nearest existing status), log a structured error '[MKT_DATA_UNAVAILABLE] ...',
      and create an escalation/approval record so a human sees it.
    - Retry on the next cron run; make the deferral idempotent (no duplicate escalations — use
      deterministic IDs per repo convention).
    Unit-test: all-sources-down → no fixing row, one escalation, event deferred; source recovers →
    fixing applied. Keep the pure logic in server/utils (no I/O), wire I/O in the cron handler.

- id: workflow-trigger-persistence
  title: Persist war-room workflow trigger dedupe state (survives redeploys)
  material: no
  model: sonnet
  depends: []
  proof: `npx prisma validate` exits 0 AND `npm run lint:migrations` exits 0 AND `npx vitest run` exits 0
  prompt: |
    server/utils/warRoom/workflowCron.ts dedupes trigger firings via in-memory state
    (lastRunInMemory, approx line 61) which is lost on every Vercel deploy, so email/deadline/
    health/silence triggers can double-fire. Add a small Prisma model (e.g. WorkflowTriggerLog:
    roomId, triggerKey, lastFiredAt, unique(roomId, triggerKey)) with a migration named per
    convention (timestamp + slug), upsert-based writes (replay-safe), and use it as the dedupe
    check in workflowCron. Keep the decision function pure (pass lastFiredAt in as a param).
    The same pattern applies to bot27-optimizer per-org cadence state if it shares the in-memory
    store. Tests: trigger fires once per window across two simulated cold starts.

- id: ioi-lifecycle-hardening
  title: IOI creation validation, race-safe expiry, conditional-IOI fail-closed evaluation
  material: no
  model: sonnet
  depends: []
  proof: `npx vitest run` exits 0 AND `npx tsc --noEmit --skipLibCheck` exits 0
  prompt: |
    Three IOI lifecycle gaps:
    1) Creation: API routes creating IndicationOfInterest accept expiresAt in the past. Add Zod
       validation (expiresAt > now, sane max horizon) at the route; reject with structured error.
    2) Expiry race: /api/cron/expire-stale-iois.get.ts uses updateMany without an updatedAt/state
       guard, so overlapping cron runs can double-process. Make the transition guarded (WHERE state
       still active AND expiresAt < now) and idempotent; if raw SQL is needed use Prisma-managed
       patterns only (no shell SQL).
    3) Conditional IOIs: trigger-processor.ts (approx lines 368-402) logs "live metric data not
       connected" and silently skips evaluation forever. Make this explicit fail-closed: mark
       conditional IOIs as 'evaluation_unavailable' with an audit log entry once (not every run),
       and wire a getLiveMetric(metric) seam that returns null today but is unit-testable, so
       connecting real data later is a one-file change.
    Unit tests for all three. Also verify OrderBookEntry↔IOI referential integrity: deleting/expiring
    an order book entry must not orphan an active IOI — add the guard or FK if missing.

- id: otc-generator-validation
  title: Generator V3 — SHA256 fingerprints, schema-validate LLM drafts, LLM circuit breaker
  material: no
  model: sonnet
  depends: []
  proof: `npx vitest run` exits 0 AND `npx tsc --noEmit --skipLibCheck` exits 0
  prompt: |
    server/utils/otc/generatorV3.ts:
    1) fingerprintOf() uses SHA1 (approx lines 185-190). Switch to SHA256. Keep dedup working across
       the transition: either dual-check (old sha1 field + new) or backfill in a migration —
       do NOT mutate applied migrations; add a new one if schema changes.
    2) LLM-drafted proposals (under-represented themes, expandPerpetualFromCatalysts) are inserted
       unvalidated. Define a Zod ProposalDraftSchema (all required fields incl. consequenceThreshold,
       resolutionAuthority with sane bounds); parse LLM output; on failure fall back to template-only
       expansion for that theme (fail-open for availability, never for validity), log a structured warning.
    3) Circuit breaker: track recent LLM call failures; above a threshold, skip LLM drafting for the
       rest of the run and use templates only, so one Anthropic outage can't starve all segments.
       Keep breaker state injectable/pure for tests.
    4) Verify useCaseKey() bandit-arm derivation is deterministic for identical (segment, theme) —
       add an idempotence unit test; fix if model-card noise leaks into the key.
    Unit tests for each. Do not change scorer/promoter contracts.

- id: product-gates-and-secret-separation
  title: Enforce isProductAllowed default-deny gates + separate API-key hash secret
  material: yes
  model: opus
  depends: []
  proof: `npx vitest run` exits 0 AND `npx tsc --noEmit --skipLibCheck` exits 0
  prompt: |
    CLAUDE.md mandates default-deny product allowlists, but a repo-wide search finds no
    isProductAllowed usage in server/api. Remediate:
    1) Create server/utils/productGates.ts: requireProductAccess(event|user, productId) backed by an
       allowlist (env PRODUCT_ENABLED flags or config map per CLAUDE.md). Unknown product => refuse
       (fail-closed, structured error '[PRODUCT_NOT_ALLOWED] ...').
    2) Apply it to the product-scoped route families (OTC verticals under server/api/otc/**, war-room
       creation, order-book/IOI creation) immediately after requireAuth/requireVerifiedUser. Inventory
       the route families first and list them in the PR description; don't blanket-wrap health/static routes.
    3) userAuth.ts hashes API keys with TOKEN_SECRET; introduce API_KEY_HASH_SECRET (fallback to
       TOKEN_SECRET when unset so nothing breaks, warn loudly in prod) so key-hashing can rotate
       independently. Also confirm production hard-fails when TOKEN_SECRET is unset (unsigned tokens
       are forgeable) — if it only warns, make it throw in prod.
    Tests: gate denies unknown product; allows enabled; API keys verify under both secrets during
    fallback window. Flag every route you gated in the PR body for reviewer sign-off.

- id: app-ui-error-loading-states
  title: Error boundaries + skeleton loaders for app/firm pages
  material: no
  model: sonnet
  depends: []
  proof: `npx vitest run` exits 0 AND `npx tsc --noEmit --skipLibCheck` exits 0
  prompt: |
    The 280+ pages under pages/app/ and pages/firm/ lack a consistent error/loading treatment.
    Add to components/ui/: ErrorState.vue (message + retry slot) and SkeletonLoader.vue variants
    (card, table, form) matching the existing design-system conventions in components/ui/. Then
    adopt them on the highest-traffic surfaces only (war room dashboard, room list, OTC contract
    views, IOI/order-book pages) using useAsyncData status — do not attempt a mass refactor of all
    pages. Keep diffs small and consistent; no new dependencies. Component unit tests for the two
    new UI components.

OPERATOR:
  - Set CRON_SECRET_PREVIOUS retirement policy: rotate CRON_SECRET and delete the previous value within 7 days (cronAuth accepts old secret indefinitely until removed).
  - Provision API_KEY_HASH_SECRET in Vercel env before merging product-gates-and-secret-separation; then schedule removal of TOKEN_SECRET fallback.
  - Confirm TOKEN_SECRET is set in ALL environments (prod + preview); unsigned-token fallback is forgeable.
  - Decide product allowlist initial contents (which OTC verticals are GA) before the gate task deploys.
