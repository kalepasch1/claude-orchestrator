PROJECT: tomorrow

# CADE wave 3 — the Determination Warranty as a PERPETUAL LEGAL-CONTRACT EVENT-HEDGING product,
# plus execution-twin / mechanism-design / firm-opponent models. Around @darwin/kernel/cade.
# Prereqs: waves 1–2 (cade-finance-determine, cade-conformal-finance, cade-negotiation-vehicle,
# cade-proof-store-tom). Spec: CADE_IMPLEMENTATION_HANDOFF.md.
# COORDINATE with existing Tomorrow stack: perpetualContracts / perpetualLegs (re-strike),
# instrumentAllowlist (SWAP_ONLY_MODE: binary_event_swap + hedge_perpetual), otc/synthInsurance
# (characterization: parametric/event trigger, NOT indemnity of actual loss), pricingEngine /
# premiumEngine / capitalReliefEngine, instantSettlement, evaluateConstitution, assertEcpCounterparty.
# Repo rules: Prisma migrations name-checked + `npm run lint:migrations`; constitution outer gate;
# add new sensitive paths to BOTH materiality classifiers; pure tests via vitest.pure.config.ts.
# §1a: advisory + ref-only; NO autonomous money movement; live-money rails operator/counsel-gated.

- id: warranty-models-migration
  title: Prisma models for determination-warranty contracts/triggers/settlements
  material: yes
  model: sonnet
  depends: []
  proof: `npm run lint:migrations` exits 0
  prompt: |
    Add Prisma models CadeWarrantyContract / CadeWarrantyTrigger / CadeWarrantySettlement (@@map
    snake tables, camelCase quoted cols, gen_random_uuid PKs), each linked to a CADE determination
    proof id + the underlying perpetual leg id. NAME-CHECK every identifier vs schema.prisma + live DB.
    Do NOT deploy to prod (operator-gated). lint:migrations clean. See chat #6.

- id: warranty-trigger-spec
  title: Parametric adverse-legal-event trigger taxonomy (characterized as event swap)
  material: yes
  model: opus
  depends: [cade-finance-determine, cade-conformal-finance]
  proof: `npx vitest run --config vitest.pure.config.ts server/utils/cade/warranty/__tests__/trigger.test.ts` exits 0
  prompt: |
    Build server/utils/cade/warranty/trigger.ts: the OBSERVABLE parametric trigger set for "the
    determination was wrong" — clause struck / opinion reversed on appeal / regulator rejection /
    filing denied — each defined as an index/event observable (court docket, regulator action), NOT
    indemnity of actual loss. Enforce characterization via otc/synthInsurance guardrails
    (assertParametricOnly + assertNotStatutoryInsurance). Test: a valid event trigger passes; an
    actual-loss-indemnity framing is rejected at the bright line. See chat #6.

- id: warranty-pricer
  title: Warranty pricer — price off the CADE conformal error rate
  material: yes
  model: opus
  depends: [warranty-trigger-spec]
  proof: `npx vitest run --config vitest.pure.config.ts server/utils/cade/warranty/__tests__/pricer.test.ts` exits 0
  prompt: |
    Build server/utils/cade/warranty/pricer.ts: PD = the determination's conformal error rate
    (cade-conformal-finance); premium/spread = PD × notional + capital charge + margin, reusing
    pricingEngine / premiumEngine / capitalReliefEngine. Lower CADE confidence ⇒ higher warranty
    price (and vice-versa) — this is the "insured advice" monetization. Test: premium rises monotonically
    as conformal error rate rises; capital charge applied once. See chat #6.

- id: warranty-perpetual-contract
  title: Mint the perpetual legal-contract event-hedging contract (bound to a determination)
  material: yes
  model: opus
  depends: [warranty-pricer, warranty-models-migration]
  proof: `npx vitest run --config vitest.pure.config.ts server/utils/cade/warranty/__tests__/contract.test.ts` exits 0
  prompt: |
    Build server/utils/cade/warranty/contract.ts: mint a PERPETUAL event-hedging contract bound to a
    determination proof id, using perpetualContracts + a hedge_perpetual / binary_event_swap leg from
    the instrument allowlist (SWAP_ONLY_MODE-compliant). ECP-gated (assertEcpCounterparty), bilateral.
    The contract re-strikes as the determination's basis changes (perpetualLegs.reStrikePerpetualLeg) —
    a living warranty. Test: mint refuses non-ECP + disallowed instruments; a valid mint returns a
    perpetual contract referencing the proof id. See chat #6.

- id: warranty-constitution-gate
  title: Constitution + ECP + characterization gates on the warranty flow
  material: yes
  model: sonnet
  depends: [warranty-perpetual-contract]
  proof: `npx vitest run --config vitest.pure.config.ts server/utils/cade/warranty/__tests__/gates.test.ts` exits 0
  prompt: |
    Wrap mint/price/settle with evaluateConstitution() (outer gate, fail-closed), the ECP gate, and the
    parametric-characterization gate; deny/escalate per posture; no autonomous money movement. Test:
    a money-move/novate action escalates; a non-ECP counterparty is blocked; valid advisory flow allows.
    See chat #6 + HANDOFF §1.

- id: warranty-settlement
  title: Warranty settlement on trigger fire (ref-only, proven)
  material: yes
  model: opus
  depends: [warranty-perpetual-contract]
  proof: `npx vitest run --config vitest.pure.config.ts server/utils/cade/warranty/__tests__/settlement.test.ts` exits 0
  prompt: |
    Build server/utils/cade/warranty/settlement.ts: on an observed adverse-event trigger (oracle /
    quality-gated reading), compute the payout and settle through the existing instantSettlement +
    proof path — REF-ONLY instruction, no funds moved, constitution-gated. Idempotent settlement id.
    Test: above-trigger pays a proven amount + emits a verifiable settlement proof; stale/divergent
    oracle → manual review (no auto-settle). See chat #6.

- id: warranty-living-monitor
  title: Living warranty — re-price + re-strike on determination-basis change (ARR)
  material: yes
  model: sonnet
  depends: [warranty-perpetual-contract]
  proof: `npx vitest run --config vitest.pure.config.ts server/utils/cade/warranty/__tests__/living.test.ts` exits 0
  prompt: |
    When a determination's basis shifts (living-determination monitor), re-run the pricer and
    re-strike the perpetual warranty leg; expose a subscription/ARR surface ("we re-certify + re-price
    your warranted positions"). Test: a simulated basis change triggers a re-price + a re-strike delta
    on the perpetual leg. See chat #6 / #7.

- id: warranty-economics-proof-link
  title: Warranty economics surface + determination↔warranty proof linkage
  material: no
  model: sonnet
  depends: [warranty-settlement]
  proof: `npx vitest run --config vitest.pure.config.ts server/utils/cade/warranty/__tests__/economics.test.ts` exits 0
  prompt: |
    Link each warranty contract to its CADE determination proof (bidirectional) and add an economics
    surface (premium, capital freed, expected payout, live mark). Test: the linkage resolves both ways
    and economics compute from the priced contract. See chat #6.

- id: cade-execution-digital-twin
  title: Execution digital-twin — forward-simulate contracts/loans/hedges over time
  material: no
  model: opus
  depends: [cade-finance-determine]
  proof: `npx vitest run --config vitest.pure.config.ts server/utils/cade/__tests__/digital-twin.test.ts` exits 0
  prompt: |
    Build a CADE post-stage that simulates a contract/loan/hedge forward through scenarios + time to
    surface failure states a static read misses (a covenant that becomes impossible in year 3, an
    indemnity cascade under correlated default). Reuse existing Tomorrow scenario/twin machinery. Test:
    a seeded time-bomb covenant is detected. See chat #3.

- id: cade-mechanism-design
  title: Mechanism-design stage — structure the optimal deal, not just score it
  material: yes
  model: opus
  depends: [cade-finance-determine]
  proof: `npx vitest run --config vitest.pure.config.ts server/utils/cade/__tests__/mechanism-design.test.ts` exits 0
  prompt: |
    Add a stage that proposes an incentive-compatible, optimal risk-allocation structure (better terms
    that did not exist yet), not only an evaluation of proposed terms. Constitution-bounded; advisory.
    Test: for a sample deal, the stage returns a structure that dominates the input on the stated
    objective. See chat #13.

- id: cade-firm-opponent-models
  title: Firm-specific opponent models from public filings (provenance-gated)
  material: yes
  model: sonnet
  depends: [cade-negotiation-vehicle]
  proof: `npx vitest run --config vitest.pure.config.ts server/utils/cade/__tests__/firm-opponent.test.ts` exits 0
  prompt: |
    Build adversary personas of SPECIFIC opposing firms from their public filings/briefs (provenance
    gate = checkable invariant, public sources only) so war-gaming is against the actual playbook.
    Test: provenance gate rejects a non-public source; a firm model produces a distinct adversary
    position. See chat #11.

OPERATOR:
  - Deploy the warranty Prisma migration to prod after name-check + shadow dry-run; then `prisma generate`.
  - COUNSEL: confirm the determination-warranty is characterized as a bilateral ECP parametric/event swap (not insurance/indemnity) and stays within SWAP_ONLY_MODE; sign off before any live counterparty.
  - Live-money settlement rails remain operator/counsel-gated (settlement is ref-only until then).
  - Set DARWIN_SIGNING_PRIVATE_KEY_PEM in Tomorrow prod (warranty + determination proofs share the anchor).
  - Reinsurance/netting of the warranty book (see further-improvements) is a follow-on program.
