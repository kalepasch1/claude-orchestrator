PROJECT: tomorrow

# Completeness Credit System — 10-200X extensions.
# Strategy/context: tomorrow repo COMPLETENESS_CREDIT_10X_EXTENSIONS.md (read A1 before
# building anything regulatory). Foundation: COMPLETENESS_CREDIT_IMPLEMENTATION_PLAN.md (cc-* tasks).
# HARD INVARIANT every task: UNMARGINED. Never add/require/hold/manage margin or collateral.
# Counterparty risk = price, ISDA credit terms, credit limits, matching priority, access, mutual fund.

- id: ext-contracts
  title: Shared types + schema + API stubs for all 10X extension modules
  material: yes
  model: opus
  depends: [cc-completeness-score]
  proof: `npx tsc --noEmit` exits 0 AND `npx prisma validate` exits 0
  prompt: |
    Contracts ONLY (types, Prisma schema, API stubs) for the extensions in
    COMPLETENESS_CREDIT_10X_EXTENSIONS.md. No business logic.
    - server/utils/risk/extensionsTypes.ts (NEW): export interfaces SolvencyIndexPoint, PassportConsent,
      CompletionRoute, CompletionSlaCommitment, FederatedCalibrationMetric, GamingFlag, StreamingTermsTick,
      PredictiveCompletionStage, ExamEvidencePack, CapitalReliefEstimate, ModelCard.
    - prisma/schema.prisma: ADD models SolvencyIndexSnapshot, PassportConsent, CompletionRoute,
      CompletionSlaCommitment, AntiGamingFlag, FederatedCalibrationRun. ADD a consent flag on
      SwapOrganization for external passport sharing. Generate the migration; do NOT apply (operator deploys).
    - API stubs returning 501: server/api/index/sector.get.ts, server/api/passport/[orgId].get.ts,
      server/api/terms/stream.get.ts.
    No implementation beyond stubs.

- id: ext-model-card-generator
  title: SR 11-7 model documentation pack generator for the HCS / residual-ES engine
  material: yes
  model: opus
  depends: [ext-contracts, cc-residual-exposure-engine]
  proof: `node scripts/genModelCard.mjs && test -f docs/model-validation/HCS_MODEL_CARD.md` exits 0
  prompt: |
    Generate an auditable model-documentation pack from the engine (assumptions, inputs, factor model,
    stress methodology, basis treatment, haircuts/cap, limitations, known weaknesses per
    COMPLETENESS_CREDIT_10X_EXTENSIONS.md A1). Implement scripts/genModelCard.mjs (NEW) that introspects
    completenessScore.ts + residualExposure.ts config and writes docs/model-validation/HCS_MODEL_CARD.md
    plus a reproducible evidence index. Documentation/generator only — does not change scoring.

- id: ext-basis-empirical
  title: Replace stylized basis/vol multipliers with empirically-calibrated, backtested per-index basis
  material: yes
  model: opus
  depends: [ext-contracts, cc-residual-exposure-engine]
  proof: `npx vitest run basisEmpirical` exits 0
  prompt: |
    Critical for model validation (A1). In server/utils/risk/studio/basis/basisModel.ts (and residualExposure
    consumers) replace stylized constants (e.g. CREDIT_VOL_MULT=3) with per-index basis estimated from
    historical index-vs-loss data in backtest-output/; expose a calibration function and a basis backtest.
    Conservative when data is thin. Test basisEmpirical.test.ts: calibrated basis reproduces a held-out
    sample within tolerance; thin-data index falls back to a conservative (wider) basis, never narrower.

- id: ext-capital-relief-mapping
  title: Advisory own-book RWA/EaR reduction + IRB-PD input export (NOT counterparty-capital relief)
  material: yes
  model: sonnet
  depends: [ext-contracts]
  proof: `npx vitest run capitalRelief` exits 0
  prompt: |
    Per A1's REFRAMED claim. server/utils/risk/capitalRelief.ts (NEW): from a verified, passport-backed hedge,
    compute an indicative reduction in the firm's OWN EaR/RWA on that book, and export an HCS-derived PD input
    a counterparty's IRB model could consume. Advisory only; must NOT assert counterparty-capital relief. Every
    output carries a disclaimer string. Test capitalRelief.test.ts: a fully exposure-matched hedge yields a
    positive own-book reduction; output includes the disclaimer; no field implies counterparty capital relief.

- id: ext-solvency-index-engine
  title: Sector completeness indices (k-anonymous) — the Tomorrow Solvency Index
  material: yes
  model: opus
  depends: [ext-contracts]
  proof: `npx vitest run solvencyIndex` exits 0
  prompt: |
    server/utils/risk/solvencyIndex.ts (NEW): from CompletenessSnapshot, compute per-sector HCS distribution
    indices (median, percentiles, trend), enforcing k-anonymity (>= K contributors, env SOLVENCY_INDEX_MIN_K
    default 5) and reusing RiskOracleSnapshot privacy patterns. Persist SolvencyIndexSnapshot. Test
    solvencyIndex.test.ts: a sector with < K contributors is suppressed; index is stable under contributor
    reordering; no single org is re-identifiable.

- id: ext-solvency-index-api
  title: Public/subscriber endpoint for the Solvency Index
  material: no
  model: sonnet
  depends: [ext-contracts, ext-solvency-index-engine]
  proof: `npx vitest run indexApi` exits 0
  prompt: |
    Implement server/api/index/sector.get.ts (the stub): return k-anonymous sector index points only; never
    raw org rows. Gate subscriber-only fields behind auth. Test indexApi.test.ts: suppressed sectors are
    absent; response contains no org identifiers.

- id: ext-passport-bureau-api
  title: Consented external API exposing SCG + Solvency Passport (credit-bureau-for-hedgedness)
  material: yes
  model: opus
  depends: [ext-contracts, cc-solvency-passport]
  proof: `npx vitest run passportBureau` exits 0
  prompt: |
    server/api/passport/[orgId].get.ts (implement the stub): return a firm's SCG + passport proof ONLY when a
    valid PassportConsent grant exists for the requesting party; otherwise 403. Expose grade + signed proof,
    never raw positions/residual detail. Add consent CRUD (server/api/settings/passport-consent.put.ts). Test
    passportBureau.test.ts: no consent -> 403; with consent -> grade+proof, no position data leaks.

- id: ext-passport-widget
  title: Embeddable SCG/passport widget for lenders, carriers, auditors
  material: no
  model: sonnet
  depends: [ext-passport-bureau-api]
  proof: `node -e "require('@vue/compiler-sfc').parse(require('fs').readFileSync('components/PassportWidget.vue','utf8'))"` exits 0
  prompt: |
    components/PassportWidget.vue (NEW) + a tiny embeddable loader: renders a firm's SCG badge + passport
    verification link from the consented bureau API. Read-only; no position data. Keep it self-contained so it
    can be embedded on a third-party page.

- id: ext-completion-router
  title: Proactive cross-offset completion routing across all participants
  material: yes
  model: opus
  depends: [ext-contracts, cc-residual-exposure-engine, cc-acceptance-policy-and-matching]
  proof: `npx vitest run completionRouter` exits 0
  prompt: |
    server/utils/risk/completionRouter.ts (NEW): scan firm-wide residual vectors across orgs, detect natural
    cross-offsets (one org's residual offsets another's), and propose pre-packaged completing trades that raise
    BOTH sides' HCS, respecting each side's CounterpartyAcceptancePolicy. Output CompletionRoute proposals;
    do not auto-execute (proposals only). Test completionRouter.test.ts: two orgs with opposing residuals get a
    paired route that raises both HCS; an acceptance-policy mismatch suppresses the route.

- id: ext-completion-sla
  title: Completion-of-last-resort for firms above a completeness threshold
  material: yes
  model: opus
  depends: [ext-contracts, ext-completion-router, cc-mutual-default-fund]
  proof: `npx vitest run completionSla` exits 0
  prompt: |
    server/utils/risk/completionSla.ts (NEW): for orgs >= env COMPLETION_SLA_MIN_HCS (default 90), source the
    final completing trades on demand from safe-firm supply + mutual-fund backstop; record CompletionSlaCommitment.
    No margin. Capital-commitment policy is operator-gated (see OPERATOR). Test completionSla.test.ts: an eligible
    org gets a commitment that closes its residual gap; an ineligible (low-HCS) org does not.

- id: ext-federated-calibration
  title: Federated improvement of residual-ES / basis / PD with accuracy-vs-N metric
  material: yes
  model: opus
  depends: [ext-contracts, cc-completeness-score]
  proof: `npx vitest run federatedCalibration` exits 0
  prompt: |
    Extend server/utils/hive/federatedLearning.ts (or add server/utils/risk/federatedCalibration.ts) to fold each
    onboarded book into residual-ES/basis/PD calibration WITHOUT exposing individual positions (privacy-preserving
    aggregation, k-anonymity). Emit FederatedCalibrationMetric capturing accuracy vs number of contributors N.
    Test federatedCalibration.test.ts: calibration error decreases (monotone non-increasing) as N grows on
    synthetic data; no individual contribution is recoverable.

- id: ext-anti-gaming
  title: Anti-gaming detectors + randomized measurement timing
  material: yes
  model: opus
  depends: [ext-contracts, cc-completeness-score]
  proof: `npx vitest run antiGaming` exits 0
  prompt: |
    server/utils/risk/antiGaming.ts (NEW): detect window-dressing around measurement dates, wrong-way hedges that
    look complete but aren't, and basis-risk arbitrage; add randomized measurement timing and feed flags into the
    through-the-cycle score. Persist AntiGamingFlag. Test antiGaming.test.ts: a hedge unwound right after a
    measurement date is flagged; a genuine persistent hedge is not; flags lower the credited completeness.

- id: ext-streaming-terms
  title: Intraday streaming of price/terms/limit deltas + cost-of-incompleteness ticker
  material: yes
  model: sonnet
  depends: [ext-contracts, cc-pricing-cva-spread]
  proof: `npx vitest run streamingTerms` exits 0
  prompt: |
    server/api/terms/stream.get.ts (implement the stub) as an SSE/polling endpoint emitting StreamingTermsTick
    (current HCS, cvaSpreadBps, creditTermsTier, exposureLimit, and a live "cost of being incomplete" figure) as
    HCS moves intraday. No margin fields. Test streamingTerms.test.ts: ticks reflect an HCS change within one
    interval; cost-of-incompleteness is 0 at HCS 100 and positive below.

- id: ext-predictive-completion
  title: Pre-emptive staging of completing trades ahead of rate moves / regulatory dates
  material: yes
  model: opus
  depends: [ext-contracts, cc-nudge-impact-engine]
  proof: `npx vitest run predictiveCompletion` exits 0
  prompt: |
    Extend server/utils/intelligence/demandForecaster.ts (+ server/utils/risk/predictiveCompletion.ts NEW) to
    auto-stage completing trades AHEAD of predicted triggers (rate moves, regulatory dates) so HCS never dips
    below an org's target. Staged proposals only (respect the auto-completion mandate gate). Test
    predictiveCompletion.test.ts: an upcoming trigger that would drop HCS below target produces a staged
    completing trade dated before the trigger.

- id: ext-exam-evidence
  title: Auto-generated FFIEC / board hedging-program narrative
  material: yes
  model: sonnet
  depends: [ext-contracts]
  proof: `npx vitest run examEvidence` exits 0
  prompt: |
    server/utils/legalDocuments/hedgingNarrative.ts (NEW): from extracted loan-level + UBPR data + the org's
    hedge positions, generate an FFIEC/risk-committee/board narrative (ExamEvidencePack) documenting the hedging
    program and resulting residual. Content generation only; regulatory filing/sign-off is operator-gated. Test
    examEvidence.test.ts: the pack cites the org's actual exposures and hedges and reports the residual.

- id: ext-verification
  title: Full-suite verification + extensions report
  material: no
  model: opus
  depends: [ext-model-card-generator, ext-basis-empirical, ext-capital-relief-mapping, ext-solvency-index-engine, ext-solvency-index-api, ext-passport-bureau-api, ext-passport-widget, ext-completion-router, ext-completion-sla, ext-federated-calibration, ext-anti-gaming, ext-streaming-terms, ext-predictive-completion, ext-exam-evidence]
  proof: `npx tsc --noEmit && npx prisma validate && npx vitest run` exits 0 AND `test -f docs/COMPLETENESS_CREDIT_10X_REPORT.md`
  prompt: |
    Run the whole suite (tsc, prisma validate, full vitest) and write docs/COMPLETENESS_CREDIT_10X_REPORT.md
    summarizing each extension's status, the k-anonymity/privacy guarantees, and confirming no margin references
    and no unsupported counterparty-capital-relief claims remain anywhere.

OPERATOR:
  - Independent third-party SR 11-7 model validation of the HCS/residual-ES engine before any external capital claim (depends on ext-model-card-generator + ext-basis-empirical output).
  - Counsel review of ALL capital-relief framing (ext-capital-relief-mapping): must not imply counterparty-capital relief unsupported by current SA-CCR/CVA rules; own-book + IRB-PD-input framing only.
  - Regulator / IRB-bank model-risk engagement to accept HCS as a PD input (pilot one adopter).
  - Commercial + data-licensing legal sign-off before publishing the Solvency Index (ext-solvency-index-api).
  - Consent/ToS + partner agreements (lenders, carriers, rating agencies) for the passport bureau API (ext-passport-bureau-api).
  - Capital-commitment sign-off for the Completion SLA backstop (ext-completion-sla).
  - Apply Prisma migrations to prod (ext-contracts schema additions).
