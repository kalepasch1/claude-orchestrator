PROJECT: pareto-2080

# Frontier batch 3 — NEW value sources (income side, unenforced rights, forgotten money, life-event
# pre-positioning) + the trust/data infrastructure that makes autonomous action legitimate at scale.
# Same pattern as the existing server/utils/ engines: pure ESM, no deps, seedable RNG, a node:test in
# tests/, explain.makeExplanation on every decision. Inputs passed as arguments (no DB dependency).
# Proof for each = its node:test passes. Don't regress the suite beyond the 2 known pre-existing
# failures (agentLedger, bookingSaga — local Prisma-binary only).
#
# Invariants (never break): free (no fee/affiliate/data-sale); non-discretionary by default (prepare,
# human commits); non-custodial; agent-of-the-user; writeback on every completed transaction;
# reconstructed vendor models stay CONSUMER-FACING/INTERNAL. Legal-adjacent engines are UPL-safe:
# they DETECT + PREPARE + cite the rule; they never give legal judgment and always leave "see a
# licensed attorney" for true legal questions (mirror the existing secondOpinion / Novelty-Sentinel
# pattern). The user files / one-taps; the system never represents the user as counsel.

- id: income-negotiation
  title: Point the negotiation stack at the INCOME side (salary, equity, freelance/contract rates)
  material: no
  model: opus
  depends: []
  proof: `node --test tests/incomeNegotiation.test.js` exits 0
  prompt: |
    Add server/utils/incomeNegotiation.js (pure). For most people, earning beats saving by an order of
    magnitude, yet nothing here touches income. Reuse the EXISTING negotiation stack — negotiationBandit.js
    (UCB scripts), negotiationFanout.js, priceIntel.js (fair-value benchmark), equityComp.js — and apply
    it to the income side: salary raise, comp/equity package, freelance/contract rate, invoice terms.
    Provide `benchmarkComp(role, region, level, marketObs)` -> fair-band (k-anon), `planRaiseAsk(current,
    benchmark, leverage)` -> {targetUsd, opening, script, evidence}, `evaluateOffer(offer, benchmark)` ->
    accept/counter/walk with the counter. Market comp data arrives as observations (same shape as rates).
    Tests: below-market comp yields a raise target above current; a strong-leverage input raises the ask;
    an at/above-market offer is accepted; a lowball is countered toward the benchmark.

- id: rights-enforcement
  title: Detect + prepare consumer-rights claims (FCRA, Reg E, TILA, junk fees, PMI removal, escrow)
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/rightsEnforcement.test.js` exits 0
  prompt: |
    Add server/utils/rightsEnforcement.js (pure). Huge value is locked behind rights users don't exercise:
    FCRA credit-report disputes, Reg E unauthorized-charge reversals, TILA disclosure defects, junk-fee
    clawbacks, PMI auto-termination at 78% LTV, escrow-overage refunds, retroactive fee waivers. Given the
    user's accounts/statements/credit data, DETECT eligible claims and PREPARE each (the rule cited, the
    facts, a draft dispute/letter) for the user to file / one-tap send. Cross-reference Apparently's legal
    corpus for the rule text (structured input here; the live corpus fetch is an operator S2S item).
    UPL-SAFE: detect + prepare + cite only; never assert legal judgment; every claim carries
    {rule, citation, facts, draft, requiresUserFiling:true, seeAttorneyIf}. Material: legal-adjacent +
    touches disputes. Tests: a >78% LTV mortgage flags PMI removal; a duplicate/unauthorized charge flags
    Reg E; an escrow overage flags a refund; every output carries a citation + requiresUserFiling.

- id: lost-money-recovery
  title: Hunt forgotten accounts, unclaimed property, class-action settlements, overpaid escrow
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/lostMoneyRecovery.test.js` exits 0
  prompt: |
    Add server/utils/lostMoneyRecovery.js (pure). Actively surface money the user forgot or never knew was
    theirs: prior-employer 401(k)s, state unclaimed-property/escheatment, class-action settlement
    eligibility, un-refunded deposits, overpaid escrow, data-breach settlements. Compose the EXISTING
    moneyOnTable.js. Given the user's employment/address/account history, produce ranked
    {source, estimatedUsd, claimSteps, confidence}. (The live registry lookups are an operator data/rail
    item; this engine does the matching + claim-plan generation from provided datasets.) Tests: a
    prior-employer with no rollover flags a likely orphaned 401(k); an address match to an unclaimed-
    property record surfaces a claim; ranks by estimatedUsd; each carries claimSteps.

- id: life-event-prepositioning
  title: Predict a life event and pre-position the whole financial stack before it lands
  material: no
  model: opus
  depends: []
  proof: `node --test tests/lifeEventPreposition.test.js` exits 0
  prompt: |
    Add server/utils/lifeEventPreposition.js (pure). Predict marriage, a child, a job change, a move, an
    inheritance, or retirement (compose the EXISTING LifeEvents engine / familyUrgency.js signals) and
    output the ordered set of moves to pre-position the ENTIRE stack BEFORE the event: coverage changes,
    account/beneficiary updates, tax elections, credit runway (compose the queued continuousCreditOpt),
    liquidity buffer (householdJitLiquidity), and rate locks. Provide `anticipate(profile, signals)` ->
    {event, likelihood, horizonDays} and `prepositionPlan(event, stack)` -> ordered {move, why, leadDays}.
    Tests: a predicted child event schedules coverage + liquidity + beneficiary moves with correct lead
    times; a predicted move schedules a domicile/rate review; ordering respects lead days.

- id: legal-data-portability-rail
  title: Exercise GDPR/CCPA/open-banking data-access rights as a sanctioned data pull (pure planner)
  material: yes
  model: sonnet
  depends: []
  proof: `node --test tests/dataPortabilityRail.test.js` exits 0
  prompt: |
    Add server/utils/dataPortabilityRail.js (pure). Providers are LEGALLY compelled (GDPR Art. 15/20,
    CCPA, open-banking) to hand the user their own data — a robust, legitimate complement to the browser
    read-rail. Build the PURE planner: given a provider + jurisdiction, produce the correct data-access /
    portability request (right invoked, legal basis, scope, format, statutory deadline) and a parser spec
    for the returned package into the world-model/observation shape. Provide `buildAccessRequest(provider,
    jurisdiction, userRef)` and `planIngest(returnedFormat)`. Material: touches the legal-rights path +
    PII handling. The actual send/receipt is the operator rail; this plans + validates. Tests: EU user =>
    GDPR Art.15 request with the 30-day deadline; CA user => CCPA; scope limited to the user's own data;
    an unsupported jurisdiction is flagged.

- id: proof-carrying-advice
  title: Wrap every recommendation in a verifiable fiduciary proof (portable, court/spouse/lender-checkable)
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/proofCarryingAdvice.test.js` exits 0
  prompt: |
    Add server/utils/proofCarryingAdvice.js (pure). Make advice quality PROVABLE: wrap each recommendation
    (its explain.makeExplanation inputs/assumptions/result/rationale/sources) in a content-addressed,
    signed proof so the user can verify to a third party that the advice was fiduciary-grade and
    reproducible. Mirror Tomorrow's C1 verifiableProof approach (content hash + signature + stateless
    verify); reimplement a minimal pure signer here (HMAC or ed25519 via node:crypto) OR emit the payload
    shaped for Tomorrow's /api/proof to mint (operator S2S). Provide `mintAdviceProof(explanation, key)`
    and `verifyAdviceProof(proof)` (stateless). Tests: a proof verifies; tampering the recommendation
    fails; two identical recommendations are content-addressed to the same id; verify needs no secret.

- id: federated-model-training
  title: Train the reconstruction/pricing models federatedly — no raw user data leaves control
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/federatedTraining.test.js` exits 0
  prompt: |
    Add server/utils/federatedModelTraining.js (pure). Strengthen the moat WITHOUT moving raw data: train
    the pricing/reconstruction models from per-user LOCAL model updates that are aggregated centrally —
    only gradients/updates aggregate, never raw financial data. Add DP noise + secure-aggregate semantics
    (mirror Tomorrow's privacyBudget / cleanRoom / federatedRiskIntel — reimplement minimally, pure).
    Provide `localUpdate(localData, globalModel)` -> update (no raw data in it), `secureAggregate(updates,
    {epsilon, k})` -> new global model (refuses below k contributors; bounded DP noise). Material:
    privacy/data-handling posture. Tests: aggregate never contains a raw record; refuses < k contributors;
    aggregated model improves on a seeded task; DP noise bounded.

- id: adversarial-second-opinion
  title: Mandatory adversarial red-team on every recommendation before surface/commit
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/adversarialSecondOpinion.test.js` exits 0
  prompt: |
    Add server/utils/adversarialSecondOpinion.js (pure). Before any recommendation is surfaced — and
    ESPECIALLY before any auto-commit — an independent adversarial pass red-teams it. Compose the EXISTING
    secondOpinion.js. Provide `redTeam(recommendation, context)` -> {objections[], failureModes[],
    confidenceAfter, verdict:'pass'|'revise'|'block'} and a gate `gateAutoCommit(recommendation)` that
    downgrades a decideAction AUTO to prepare_and_hold when the red-team verdict isn't 'pass'. Tests: a
    recommendation with an unmodeled tax hit gets 'revise'; a clearly-bad one 'block'; gate flips a weak
    AUTO to hold; a clean recommendation passes.

- id: cross-border-arbitrage
  title: Joint domicile / tax / banking jurisdiction optimization for the mobile/expat segment
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/crossBorderArbitrage.test.js` exits 0
  prompt: |
    Add server/utils/crossBorderArbitrage.js (pure). For mobile/expat/remote users, jointly optimize
    domicile, banking, and tax jurisdiction. Compose the EXISTING geoArbitrage.js, crossBorderCompliance.js,
    visaSolver.js. Provide `optimizeJurisdiction(profile, candidates)` -> ranked {domicile, bankingHub,
    estAnnualUsd, taxTreatyNotes, visaFeasible, requiresCounsel:true}. Material: cross-border tax/legal is
    high-stakes — every recommendation is flagged requiresCounsel and is advisory only. Tests: a high-tax
    domicile with a feasible low-tax treaty alternative surfaces the move; visa-infeasible options are
    filtered; each output carries requiresCounsel + treaty notes.

- id: debt-graph-restructuring
  title: Globally-optimal debt payoff as a graph problem (transfer chains, consolidation cascades)
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/debtGraphRestructuring.test.js` exits 0
  prompt: |
    Add server/utils/debtGraphRestructuring.js (pure). Treat payoff as graph optimization, not linear
    avalanche/snowball: balance-transfer chains (0% windows), consolidation, refinance sequencing, minimum
    total interest over the horizon. Compose the EXISTING debtOptimizer.js as the baseline to beat.
    Provide `optimizePayoffGraph(debts, transferOffers, {horizonMonths})` -> {sequence, totalInterestUsd,
    monthsToFree, vsAvalancheUsd}. Deterministic search (seedable). Tests: the graph plan's total interest
    <= avalanche on a seeded case; a 0% transfer chain is used when it lowers total interest; respects
    transfer fees + limits.

OPERATOR:
  - rights-enforcement: UPL-safe (detect + prepare + cite only; user files). Needs Apparently legal-corpus S2S for live rule text, and counsel confirmation of the consumer-rights posture per claim type before any dispute is auto-prepared at scale.
  - lost-money-recovery + legal-data-portability-rail: the live registry/data-access SENDS are operator rails (state unclaimed-property lookups, GDPR/CCPA request delivery); the engines plan + parse only.
  - proof-carrying-advice + federated-model-training: can reuse Tomorrow's C1 verifiableProof + privacy infra via S2S (secret PARETO_TOMORROW_S2S_SECRET) rather than the minimal local reimplementation — operator choice.
  - cross-border-arbitrage: every recommendation is advisory + requiresCounsel; do not let it drive an actual domicile/tax action without counsel sign-off.
  - Legal keystone unchanged: free / non-discretionary / non-custodial / agent-of-user; no fee/affiliate/data-sale; reconstructed vendor + underwriting models never sold or fed back to vendors.
