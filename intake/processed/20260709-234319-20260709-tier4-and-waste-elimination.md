PROJECT: beethoven

# Tier-4 + workflow waste-elimination (2026-07-09, owner-directed). As before: extend existing
# modules (named), never duplicate; cross-repo changes via generated per-repo intake tasks.

- id: relfix-replay-routing-lab
  title: Offline routing/prompt lab — replay history against variants at zero spend
  material: no
  model: opus
  depends: []
  proof: `python3 -m pytest runner/tests -q -k replay_lab` exits 0
  prompt: |
    replay.capture records every run (task, model, account, prompt, confidence). Build
    runner/replay_lab.py: load N historical tasks with known outcomes; re-run VARIANTS offline —
    different prompt templates, pattern injections, model/vendor routing — using local models
    only (zero spend) or dry-run scoring; compare predicted proof-pass vs actuals; output a
    ranked report (.runtime/replay_lab/<date>.md) and, for a variant beating baseline by a
    margin, file an approval card proposing the change (knob or prompt-template) via the
    fleet_config/pattern mechanisms. knob_tuner (relfix-closed-loop-tuning) consumes lab results
    as priors when merged. Tests: variant harness with synthetic replays, ranking, card payload.

- id: relfix-semantic-ai-cache
  title: Gateway-level semantic cache for repeated AI calls (30-60% token cut)
  material: yes
  model: opus
  depends: []
  proof: `python3 -m pytest runner/tests -q -k sem_cache` exits 0
  prompt: |
    Planner/classifier/judge calls repeat near-identically across agents. In the shared AI-call
    seam (caching.py already does stable-prefix context caching; model_gateway routes calls):
    add runner/semantic_cache.py — embed the call payload (existing embed provider w/ 429
    fallback), look up prior responses above cosine threshold (env, default 0.97) in a local
    SQLite store (.runtime/sem_cache.db, LRU capped); serve cached response for DETERMINISTIC
    call classes only (classification, scoring, judging — explicit allowlist by operation tag;
    never for code drafting). Metrics: hits/misses/estimated-tokens-saved to scoreboard. Tests:
    threshold behavior, allowlist enforcement, LRU eviction, corrupted-db fail-soft (bypass).

- id: relfix-experiment-harness
  title: Product experiments as tasks — flag, variant preview, metric eval, auto-promote
  material: yes
  model: opus
  depends: []
  proof: `python3 -m pytest runner/tests -q -k experiment` exits 0
  prompt: |
    Turn the fleet into a growth machine: runner/experiment_harness.py + an 'experiment' task
    kind. An experiment task defines: hypothesis, target app, variant implementation prompt,
    metric (from the telemetry conventions in relfix-telemetry-priority), eval window, promote/
    revert criteria. Flow: variant built as a normal agent task behind a feature flag (each app's
    existing flag convention; document per-app in the task), deployed to staging/preview, metric
    collected over the window, then harness files the verdict card: promote (flag on in prod via
    a follow-up task) or revert (flag removed). Guardrails: experiments are ALWAYS material
    (owner approves start + promote), max concurrent experiments per app (env), auto-revert on
    error-rate spike. Tests: lifecycle state machine, verdict logic, guardrails with synthetic
    metrics.

- id: relfix-self-updating-docs
  title: Every merge auto-refreshes module docs + CLAUDE.md sections
  material: no
  model: sonnet
  depends: []
  proof: `python3 -m pytest runner/tests -q -k autodocs` exits 0
  prompt: |
    Post-merge hook (the seam where merged_diff_library.record fires): runner/autodocs.py
    generates/updates a short module-level doc summary for files touched by the merge (cheap
    local model; scrubbed through the banner/PII filter from bandit-outcome-decontamination) and
    stages CLAUDE.md convention updates through the SAME reviewed pipeline as convention-sync
    (never direct writes — emit an intake task per repo when a convention section is stale).
    Cap: one docs task per repo per day (batch the day's merges). Feeds fleet-RAG's nightly
    index. Tests: summary generation with mocked model, batching, pipeline emission format.

- id: relfix-dependency-immune
  title: Scheduled dependency/security updates with agent-fixed breakages
  material: yes
  model: sonnet
  depends: []
  proof: `python3 -m pytest runner/tests -q -k dep_immune` exits 0
  prompt: |
    runner/dep_immune.py (periodic, weekly + on-demand): per fleet repo, run npm outdated +
    npm audit (pip-audit for beethoven); classify: security-critical (queue qafix- task
    immediately, jump lane), major-with-breaking-risk (queue normal task with the changelog
    summary in the prompt), patch/minor (fold into ONE microbatch bump task per repo). Each bump
    task's proof is the repo's full gate. Never auto-merge a security bump without the gate.
    Dedupe by (repo, package, version). Tests: classification, task generation format, dedupe.

- id: relfix-code-intel-service
  title: Tree-sitter symbol graph served to agents (precise defs/refs instead of grep)
  material: no
  model: opus
  depends: []
  proof: `python3 -m pytest runner/tests -q -k code_intel` exits 0
  prompt: |
    First-pass rate is weakest on multi-file refactors where agents grep blindly. Build
    runner/code_intel.py: tree-sitter (py-tree-sitter + language packs for ts/tsx/vue-script/
    python) indexing each fleet repo into a SQLite symbol store (.runtime/code_intel/<repo>.db):
    definitions, references, imports, call edges. Refresh incrementally on merge (post-merge
    hook) + nightly full. Expose to agents two ways: (a) a CLI (scripts/code-intel query ...)
    documented in the pattern library so any agent can call it from its worktree; (b) inject a
    'SYMBOLS' section (target file's defs/refs) into prompts for refactor-kind tasks at the same
    seam as patterns/RAG. Vue SFCs: parse script blocks only, fail-soft per file. Tests: indexing
    fixture repos, query CLI, incremental refresh, prompt injection budget.

- id: relfix-autonomous-roadmap
  title: EV-gated roadmap proposals — the fleet proposes what to build next, owner approves
  material: yes
  model: opus
  depends: []
  proof: `python3 -m pytest runner/tests -q -k roadmap_prop` exits 0
  prompt: |
    The endgame loop, gated correctly: runner/roadmap_proposer.py (periodic, weekly; ACTIVE only
    when drain mode is off AND queue depth < QUEUE_GEN_CEILING — never fight the drain). Inputs:
    revenue_attribution/kind_roi, telemetry app_signals, capability registry gaps, demand_mining
    outputs (re-enabled solely as an input to THIS, not as a direct task generator). Output: a
    weekly roadmap proposal card per app — max 3 initiatives, each with EV rationale, rough task
    DAG (planner dry-run), and cost estimate. Owner approves a card -> the DAG drops into
    intake/ as a normal batch. NOTHING queues without the approval. Replaces the scattershot
    improve/scout generation with owner-in-the-loop portfolio strategy. Tests: gating conditions,
    proposal payload, approval->intake emission (parseable by intake_watcher.parse).

# ── workflow waste-elimination (preemptive; every observed friction this week) ──

- id: relfix-db-poll-diet
  title: Collapse the Supabase poll storm — one batched status RPC + realtime where it counts
  material: yes
  model: opus
  depends: []
  proof: `python3 -m pytest runner/tests -q -k poll_diet` exits 0
  prompt: |
    ~40 periodic jobs each make several small GETs per cycle — the poll storm that helped crush
    the old compute tier. Two-part diet: (1) a Postgres function fleet_status() (idempotent
    migration) returning one JSON blob: queue counts by state/project, top-N claimable ids,
    open card counts, pressure, deploy_health, heartbeats — and runner/fleet_status.py caching
    it process-wide (TTL 30s); migrate the heaviest readers (autopilot, queue_counters,
    integration_sweeper pre-scan, dashboards' API) to consume it. (2) evaluate supabase realtime
    subscription for tasks-table changes in the RUNNER only (claim wakeups instead of polling) —
    implement behind ORCH_REALTIME_CLAIMS=false default with a fallback to polling; enable via
    fleet_config after observation. Measure: request-count before/after logged to scoreboard.
    Tests: RPC response shape (mocked), cache TTL, fallback path.

- id: relfix-branch-gc
  title: Stale agent-branch garbage collection (origin fetch times are ballooning)
  material: no
  model: haiku
  depends: []
  proof: `python3 -m pytest runner/tests -q -k branch_gc` exits 0
  prompt: |
    Hundreds of stale agent/* branches accumulate on origin per repo (branch-share pushes +
    historical churn), inflating every fetch. runner/branch_gc.py (periodic, daily): for each
    fleet repo, delete origin agent/* branches that are (a) fully merged into staging/prod, or
    (b) older than 14 days AND their task is MERGED/QUARANTINED/DONE-reconciled. NEVER delete:
    branches for QUEUED/RUNNING/BLOCKED tasks, or younger than 48h. Dry-run mode first
    (ORCH_BRANCH_GC_APPLY=false default; report to ops card), flip to apply via fleet_config
    after one clean report. Tests: age/state guards, dry-run report, apply path with fixture
    remote.

- id: relfix-prompt-cache-audit
  title: Verify Anthropic prompt caching + adopt Batches API for non-urgent call classes
  material: no
  model: sonnet
  depends: []
  proof: `python3 -m pytest runner/tests -q -k prompt_cache` exits 0
  prompt: |
    caching.py builds a stable cached context prefix — audit that it's actually effective:
    instrument cache_read/cache_write token counts from API responses into the scoreboard;
    verify the prefix is byte-stable across calls (any timestamp/uuid in the prefix kills the
    cache — fix offenders). Second: adopt the Anthropic Message Batches API (50% cost) for the
    explicitly non-latency-sensitive classes: nightly knowledge/pattern extraction, autodocs
    summaries, replay-lab scoring, failure clustering — a runner/batch_api.py helper with
    submit/poll/collect and a fallback to sync calls. Tests: prefix stability check on the real
    prompt builder, batch lifecycle mocked, fallback.

- id: relfix-log-and-cache-hygiene
  title: Rotate runtime logs, cap caches, clean stale IDE-agent processes (report-only)
  material: no
  model: haiku
  depends: []
  proof: `python3 -m pytest runner/tests -q -k hygiene` exits 0
  prompt: |
    Observed waste on the runner Mac: unbounded .runtime/logs growth, a 14MB
    .orch-context-cache.json rewritten repeatedly, and stale VS Code claude debug processes
    running for DAYS eating RAM. runner/hygiene.py (periodic, 6h): size-rotate .runtime/logs/*
    (keep 3x10MB per log), vacuum/LRU-cap the context cache and any sem_cache/code_intel dbs,
    and REPORT (ops note only — never kill) editor/agent processes older than 24h with their
    RSS so Macey can close them. Tests: rotation, cap enforcement, report format.

OPERATOR:
  - relfix-experiment-harness + relfix-autonomous-roadmap keep you in the loop by design: experiment starts/promotions and weekly roadmap cards await your approval in the dashboard.
  - relfix-db-poll-diet includes one idempotent migration (fleet_status function) — it deploys with the normal flow; no dashboard action needed now that compute is upgraded.
  - relfix-branch-gc ships in dry-run: glance at its first report card, then flip ORCH_BRANCH_GC_APPLY=true in fleet_config.
  - Stale VS Code agent processes (3+ days old, seen on this Mac) will appear in hygiene reports — close those windows when convenient; they're pure RAM waste.
