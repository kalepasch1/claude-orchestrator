PROJECT: apparently

# CADE fast-follows (batch 2). Each depends on a parent module from the earlier
# batches (already processed), so zero file/merge conflict. Additive + build-gated
# (nuxt `npm run build`).

- id: cade-twin-drift-alarm
  title: Decider-twin calibration drift alarm (predicted vs realized P(win))
  material: no
  model: sonnet
  depends: [cade-decider-twin, position-outcome-endpoint]
  proof: `npm run build` exits 0 AND `npx vitest run tests/engines/twin-drift.test.ts` exits 0
  prompt: |
    Add a PURE aggregator `server/engines/legal-bots/twin-drift.ts`:
    `computeTwinDrift(predictions, outcomes)` compares the decider-twin's predicted P(win)
    (from `decider-twin.ts`) against realized outcomes (from `position_outcomes`, read via
    the KPI util) per recipient, and flags recipients whose drift exceeds a band as
    "twin needs re-learning". Pure + unit-tested (drift math, banding, empty input). Surface
    the flag in the KPI endpoint response (additive field). Feeds the persona-learner. No
    live model. CANDIDATE-SHARED: twin-drift.

- id: cade-precedent-gap-harvest
  title: Emit a corpus gap when no strong firm precedent exists for a recipient/issue
  material: no
  model: sonnet
  depends: [cade-precedent-outcome-ranking]
  proof: `npm run build` exits 0 AND `npx vitest run tests/engines/precedent-gap.test.ts` exits 0
  prompt: |
    When `precedent-ranking.ts` returns no rung above a strength floor for a
    recipient/issue, emit a `corpus_gaps` row (the table + gap mechanism already exist in
    the corpus v2 layer — see apparently CLAUDE.md "Corpus v2") so the existing harvest
    cron prioritizes filling it. Smallest diff: a pure `detectPrecedentGap(rankedHits,
    {recipientId, issue})` + fail-soft insert reusing the corpus client. Unit test the
    gap-trigger threshold. Closes the drafting→corpus-growth loop.

- id: cade-voi-batching
  title: VOI intake returns a short top-k question set with a stopping rule
  material: no
  model: sonnet
  depends: [cade-voi-intake]
  proof: `npm run build` exits 0 AND `npx vitest run tests/engines/voi-batching.test.ts` exits 0
  prompt: |
    Extend `voi-intake.ts` with `topKQuestions(candidates, ledger, { maxK })` returning the
    top-k highest-VOI questions as one short intake form, with a stopping rule that halts
    when the marginal expected tenability lift of the next question flattens (below epsilon).
    Pure + unit-tested (ordering, stopping rule, k cap). Edits voi-intake.ts → chained via
    depends on its creation task.

- id: cade-receipt-client-export
  title: One-page client/regulator export of the signed concurrence receipt
  material: yes
  model: sonnet
  depends: [cade-faithfulness-in-receipt]
  proof: `npm run build` exits 0
  prompt: |
    Render the signed concurrence receipt (weakness-ledger dispositions, tenability band,
    recipient-readiness, faithfulness attestation, signature) as a one-page client/
    regulator-facing export. Smallest diff + REUSE FIRST: search the repo for an existing
    PDF/HTML export util or the docx/pdf pipeline; if none fits, render a printable HTML
    receipt route `app/pages/opinions/[id]/receipt.vue` (read-only, self-contained styles).
    Do not recompute anything — format the existing signed receipt object. Client-facing
    legal artifact → material.

OPERATOR:
  - None new (receipts reuse AGENT_SIGNING_SECRET already listed).
