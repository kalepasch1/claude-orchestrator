PROJECT: tomorrow

# Second-wave 20-200X vectors. Feasibility-trimmed after code review:
#  - #10 adversarial digital twin: SKIPPED (already queued as 'adversary-twin').
#  - #11 provenance/lineage: TRIMMED to a small extension (Ed25519 verifiableProof already exists).
#  - securitization/"verified-hedged note": moved to the mutual/synthetic workstream where it belongs.
# Compose existing production primitives (compressionEngine, closeoutNettingEngine, RiskOracleSnapshot,
# demandForecaster, riskOntology, HedgeBundle, verifiableProof). UNMARGINED invariant holds throughout.

- id: sw-contracts
  title: Shared types + schema + API stubs for second-wave vectors
  material: yes
  model: opus
  depends: [cc-completeness-score]
  proof: `npx tsc --noEmit` exits 0 AND `npx prisma validate` exits 0
  prompt: |
    Contracts ONLY (types, schema, API stubs) for: external netting utility, HCS standard/registry, systemic
    risk map, stability simulator, demand-foresight feed, vertical packs, agent-to-agent API, covenant verifier,
    number lineage. Add server/utils/risk/secondWaveTypes.ts (NEW) with the interfaces, prisma models where noted
    per task, and 501 API stubs. Generate migration; do not apply. No business logic.

- id: sw-netting-utility-api
  title: External multilateral netting/compression utility (accepts off-platform trades)
  material: yes
  model: opus
  depends: [sw-contracts]
  proof: `npx vitest run nettingUtility` exits 0
  prompt: |
    Wrap the existing server/utils/otc/compressionEngine.ts + closeoutNettingEngine.ts as an external-facing
    multilateral netting utility: server/api/netting/submit.post.ts accepts a batch of trades (including ones NOT
    originated on Tomorrow), runs multilateral compression, returns the netted set + a verifiableProof of the
    compression. Authenticated; no funds move (non-custodial). Test nettingUtility.test.ts: a 3-party cycle
    compresses to reduced gross notional; the proof verifies.

- id: sw-systemic-riskmap
  title: k-anonymous systemic residual-risk map (macroprudential product)
  material: yes
  model: opus
  depends: [sw-contracts]
  proof: `npx vitest run systemicRiskMap` exits 0
  prompt: |
    server/utils/risk/systemicRiskMap.ts (NEW): from RiskOracleSnapshot + CompletenessSnapshot, aggregate where
    unhedged residual is concentrating by sector/factor (k-anonymous, >= K contributors). Output a map suitable
    for a regulator/central-bank dashboard; never expose individual orgs. Test: a concentration shows only when
    >= K contributors; no org is re-identifiable.

- id: sw-stability-simulator
  title: Counterfactual system-stability simulator (which trades most reduce network residual)
  material: yes
  model: opus
  depends: [sw-contracts, sw-systemic-riskmap]
  proof: `npx vitest run stabilitySimulator` exits 0
  prompt: |
    server/utils/risk/stabilitySimulator.ts (NEW): run the federated book through correlated stress and rank the
    trades that most reduce SYSTEM-WIDE stressed residual ES; expose them as targets for incentives (priority/
    rebate multiplier). Test: on a synthetic network, the identified trade lowers system ES more than a random one.

- id: sw-demand-foresight-feed
  title: Fair-access demand-foresight feed for liquidity providers
  material: yes
  model: sonnet
  depends: [sw-contracts]
  proof: `npx vitest run demandForesight` exits 0
  prompt: |
    Productize server/utils/intelligence/demandForecaster.ts as a fair-access forecast feed: aggregate,
    time-delayed, k-anonymous predicted hedging demand LPs can pre-position against, with tighter pricing flowing
    back to complete firms. Enforce fair-access rules (no per-org leakage, uniform delay). Test demandForesight.test.ts:
    feed contains no single-org identifiers and respects the configured delay.

- id: sw-vertical-packs
  title: Per-vertical completeness packs (turnkey exposure ontology + completing structures)
  material: yes
  model: sonnet
  depends: [sw-contracts]
  proof: `npx vitest run verticalPacks` exits 0
  prompt: |
    server/utils/risk/verticalPacks.ts (NEW): using server/utils/otc/synthInsurance/moat/riskOntology.ts +
    HedgeBundle, define per-vertical packs (community-bank CRE-concentration, self-funded-employer medical-trend,
    gaming promo-tail) that map a vertical's typical exposures to pre-built completing structures raising HCS.
    Test: applying a pack to a matching org proposes structures that reduce its residual.

- id: sw-agent-api
  title: Agent-to-agent (machine-to-machine) hedging API
  material: yes
  model: opus
  depends: [sw-contracts]
  proof: `npx vitest run agentApi` exits 0
  prompt: |
    Expose server/utils/agents/unifiedAgent.ts via an authenticated agent-to-agent API (server/api/agent/negotiate.post.ts)
    so a firm's treasury bot can negotiate completeness with Tomorrow's bots for routine flow, gated by the org's
    Policy Constitution + mandate limits. Human gate still applies to material actions. Test agentApi.test.ts: a
    within-mandate request returns a proposed action; an over-limit request requires human gate.

- id: sw-covenant-verifier
  title: Completeness-linked covenant verifier (SCG >= X embedded in third-party contracts)
  material: yes
  model: sonnet
  depends: [sw-contracts, cc-solvency-passport]
  proof: `npx vitest run covenantVerifier` exits 0
  prompt: |
    server/utils/risk/covenantVerifier.ts (NEW) + server/api/covenant/check.get.ts: let a lender/counterparty
    define a covenant (e.g. SCG >= B) and continuously verify an org against it, emitting a signed pass/breach
    attestation (reuse verifiableProof). Consent-gated like the passport bureau. Test: an org below threshold emits
    a signed breach; above emits a signed pass; no consent -> 403.

- id: sw-number-lineage
  title: Attach verifiable lineage to every HCS / price / term output
  material: yes
  model: sonnet
  depends: [sw-contracts]
  proof: `npx vitest run numberLineage` exits 0
  prompt: |
    Extend server/utils/proof/verifiableProof.ts usage so each published HCS, quoted price, and ISDA terms tier
    carries a lineage record (input data version, model version, upstream proof digests). Add
    server/utils/risk/numberLineage.ts. Test numberLineage.test.ts: a produced HCS carries a resolvable lineage
    chain; tampering any input invalidates it.

- id: sw-verification
  title: Second-wave full-suite verification + report
  material: no
  model: opus
  depends: [sw-netting-utility-api, sw-systemic-riskmap, sw-stability-simulator, sw-demand-foresight-feed, sw-vertical-packs, sw-agent-api, sw-covenant-verifier, sw-number-lineage]
  proof: `npx tsc --noEmit && npx prisma validate && npx vitest run` exits 0 AND `test -f docs/SECOND_WAVE_REPORT.md`
  prompt: |
    Run tsc, prisma validate, full vitest; write docs/SECOND_WAVE_REPORT.md summarizing each vector, its
    k-anonymity/fair-access guarantees, and confirming no margin references and no unsupported claims remain.

OPERATOR:
  - Legal + commercial terms for opening the netting utility to off-platform trades (connectivity, liability, SDR reporting).
  - Regulator engagement + data-licensing for the systemic risk map (macroprudential product).
  - Fair-access / market-conduct review of the demand-foresight feed (no preferential leakage).
  - Publish HCS methodology as an open standard + stand up the certification registry (separate governance decision).
  - Counsel review of completeness-linked covenant language before third parties embed it.
