PROJECT: tomorrow

- id: offset-interest-recharacterize
  title: Recharacterize manufactureSyntheticSeller → ideal-counterparty target (no synthetic seller)
  material: no
  model: sonnet
  depends: []
  proof: `npx vitest run --config vitest.pure.config.ts --pool=forks server/utils/otc/lending/__tests__/enhancements.test.ts` exits 0
  prompt: |
    The platform model is always-on REAL bot↔bot discovery — there is no synthetic seller and no
    Tomorrow-as-counterparty backstop. offsetRecruiter.ts `manufactureSyntheticSeller` is mis-framed:
    it implies a fabricated counterparty. Recharacterize it as a recruitment TARGET (an ideal-
    counterparty profile to source a REAL agent), not a book that stands up a trade.
    Steps:
    - In server/utils/otc/lending/offsetRecruiter.ts rename `manufactureSyntheticSeller` →
      `buildOffsetTarget(residualPositions, targetLabel?, coverageFraction?)` returning an
      `OffsetTarget { targetLabel, neededExposures: FactorExposure[] }` (the negated residual = what a
      real counterparty would need to hold), NOT a PartnerBook. Keep the empty-label throw (a target
      must be named for the mesh). Keep idealCounterpartyProfile unchanged.
    - Update server/api/otc/lending/recruit.post.ts: replace the `synthetic` field with `offsetTarget`
      (body param `targetLabel`), returning the OffsetTarget. No behavior implying Tomorrow takes a side.
    - Update server/utils/otc/lending/__tests__/enhancements.test.ts to test buildOffsetTarget (returns
      needed exposures opposite-signed to the residual; throws on empty label). Keep all other lending
      tests green. Update the barrel export in index.ts.

- id: lending-mesh-ioi-bridge
  title: Publish lending residual + offset targets into the always-on IOI mesh
  material: no
  model: sonnet
  depends: [offset-interest-recharacterize]
  proof: `npx vitest run --config vitest.pure.config.ts --pool=forks server/utils/otc/lending/__tests__/meshBridge.test.ts` exits 0
  prompt: |
    Wire lending discovery into the EXISTING always-on bilateral IOI mesh (B1) so real partner agents
    discover and negotiate the hedge — immediacy is emergent from the mesh, not a backstop.
    Steps:
    - Inspect server/utils/otc/rings/ioiMesh.ts exports (StandingInterest, discoverPairings,
      publishStandingIoi/getStandingBook — KV namespace `ioi:standing`) and A2A types in
      server/utils/agents/a2aProtocol.ts. Do NOT change those modules' signatures.
    - New pure module server/utils/otc/lending/meshBridge.ts: `residualToStandingInterests(partnerId,
      residualPositions)` maps each residual factor to a StandingInterest (opposite-side, non-firm,
      anonymized) in the ioiMesh shape; `offsetTargetToStandingInterest(target)` maps an OffsetTarget
      (from offset-interest-recharacterize) the same way. Pure mapping only.
    - Add server/api/otc/lending/mesh/publish-interests.post.ts (requireAuth): builds the partner's
      residual (persistence.buildPartnerBook), maps to standing interests, and publishes them via the
      ioiMesh publish fn. Advisory / non-firm; no execution.
    - Tests server/utils/otc/lending/__tests__/meshBridge.test.ts: a long-duration residual produces a
      receive/short-duration standing interest of matching notional; anonymized until pairing; empty
      residual → no interests. Pure (no DB — test the mapper).

- id: cross-vertical-offset-discovery
  title: One mesh across verticals — lending residual offset by gaming/overlay/event-options agents
  material: no
  model: opus
  depends: [lending-mesh-ioi-bridge]
  proof: `npx vitest run --config vitest.pure.config.ts --pool=forks server/utils/otc/lending/__tests__/crossVerticalMesh.test.ts` exits 0
  prompt: |
    A bank loan's residual can be absorbed by a gaming operator's opposite tail, a stablecoin-overlay
    desk, or an event-options book — every vertical publishes to the unified ledger and speaks A2A, so
    all verticals' agents are potential counterparties. Discover cross-vertical offsets.
    Steps:
    - New pure module server/utils/otc/lending/crossVerticalMesh.ts. `discoverCrossVerticalOffsets
      (residualPositions, candidatePositionsBySource)` where candidatePositionsBySource is a map of
      StrategySource → RiskPosition[] (from server/utils/otc/unifiedPositions.ts fetchUnifiedPositions
      / getUnifiedPositions, injected). Reuse networkNetting / marginalRisk to rank which vertical +
      counterparty most reduces the residual ES, returning {source, counterpartyRef, residualReduction
      Usd, decorrelationScore}. Named bilateral only; operatorAffiliate false.
    - Tests server/utils/otc/lending/__tests__/crossVerticalMesh.test.ts: a gaming tail position that
      is opposite-signed to the loan residual ranks above an uncorrelated one; empty candidates → empty
      result. Pure (inject positions; no DB).

- id: anticipated-risk-hedging
  title: Contingent IOIs for anticipated / future exposures (hedge before it's booked)
  material: no
  model: opus
  depends: []
  proof: `npx vitest run --config vitest.pure.config.ts --pool=forks server/utils/otc/lending/__tests__/anticipatedHedging.test.ts` exits 0
  prompt: |
    Agents reason about FUTURE exposure — pipeline loans not yet booked, scheduled maturities/rolls,
    seasonal claims, planned originations — and pre-arrange contingent hedges that auto-activate when
    the exposure materializes. Hedge risk before it hits the book.
    Steps:
    - New pure module server/utils/otc/lending/anticipatedHedging.ts. `AnticipatedExposure { factorKey,
      riskClass, expectedSigmaUsd, expectedAtEpochMs, probability }`. `projectAnticipatedResidual
      (currentBook, anticipated[], asOfMs)` rolls forward the residual; `contingentInterests
      (anticipated[], triggerRule)` emits standing interests flagged contingent with an activation
      predicate (probability ≥ p AND within horizon). Compose with anticipationMesh
      (server/utils/intelligence/anticipationMesh.ts) — inspect its exports, do not change them.
    - Tests server/utils/otc/lending/__tests__/anticipatedHedging.test.ts: a high-probability near-term
      exposure yields a contingent interest; a low-probability/far one is filtered; projected residual
      folds in the anticipated sigma. Pure, asOfMs injected (no Date.now).

- id: lending-residual-ring-discovery
  title: Multilateral ring discovery for the lending residual (A→B→C→A) via the hive
  material: no
  model: opus
  depends: [lending-mesh-ioi-bridge]
  proof: `npx vitest run --config vitest.pure.config.ts --pool=forks server/utils/otc/lending/__tests__/ringDiscovery.test.ts` exits 0
  prompt: |
    Pairwise offset search misses cycles: A→B→C→A nets everyone when no single A↔B offset exists.
    Discover offsetting rings for the lending residual; each leg stays named bilateral (N6). Composes
    with existing ring infra (server/utils/otc/rings/selfHealingRing.ts, meshGateway.ts) — do not
    duplicate or modify them; this is lending-residual-specific ring FINDING only.
    Steps:
    - New pure module server/utils/otc/lending/ringDiscovery.ts. `findOffsetRings(partners, {maxRing
      Size=4})` searches for cyclic offset chains where the sum around the ring reduces each member's
      residual (greedy per-factor cycle detection on the partners' net factor vectors). Return rings as
      ordered lists of named bilateral legs {fromPartnerId, toPartnerId, factorKey, offsetSigmaUsd} with
      the per-member residual reduction. No pool/CCP; operator never a party.
    - Tests server/utils/otc/lending/__tests__/ringDiscovery.test.ts: three partners with no pairwise
      offset but a closing 3-cycle produce a ring that reduces all three; a set with a direct pairwise
      offset still returns the (shorter) match; no cycle → empty. Pure.

- id: privacy-preserving-matching
  title: Encrypted offset discovery so competing institutions can share the mesh
  material: no
  model: opus
  depends: []
  proof: `npx vitest run --config vitest.pure.config.ts --pool=forks server/utils/otc/lending/__tests__/privateMatching.test.ts` exits 0
  prompt: |
    The richest offsets are between rival institutions, but no bank will expose its book to a
    competitor. Match offsets WITHOUT revealing positions, using the existing privacy primitives.
    Steps:
    - New pure module server/utils/otc/lending/privateMatching.ts. `blindFactorCommitment(positions,
      salt)` → per-factor commitments that hide magnitude/direction beyond a coarse bucket; `matchBlind
      (commitmentsA, commitmentsB)` → whether an offset exists + a privacy-safe magnitude band, WITHOUT
      either side learning the other's exact vector. Reuse server/utils/ai/cleanRoom.ts (secure
      aggregate) and server/utils/risk/privacyBudget.ts (Laplace/epsilon). No raw position leaves the
      boundary.
    - Tests server/utils/otc/lending/__tests__/privateMatching.test.ts: two opposite-signed books match
      as an offset without the transcript containing either raw sigma; two same-signed do not; a single
      party's exact value is never recoverable from the commitment. Pure.

- id: timing-intelligence-trigger
  title: Learned negotiation-timing trigger (strike when both sides benefit most)
  material: no
  model: sonnet
  depends: []
  proof: `npx vitest run --config vitest.pure.config.ts --pool=forks server/utils/otc/lending/__tests__/timingTrigger.test.ts` exits 0
  prompt: |
    "When the timing is right" is a model: strike when both parties' marginal benefit peaks, a curve
    level is hit, or a regulatory/quarter-end date pressures a side. Same trades, better entry.
    Steps:
    - New pure module server/utils/otc/lending/timingTrigger.ts. `negotiationTiming({partyABenefitBp,
      partyBBenefitBp, curveLevel, curveThreshold, daysToRegDate, mandateUrgency})` → {trigger:boolean,
      score, reason}. Trigger when BOTH benefits exceed a floor AND (curve favorable OR reg-date
      pressure OR urgency). Compose conceptually with server/utils/warRoom/responseTimingIntel.ts and
      the negotiation bandit (inspect, do not modify).
    - Tests server/utils/otc/lending/__tests__/timingTrigger.test.ts: both-benefit + favorable curve →
      trigger; one-sided benefit → no trigger; imminent reg date raises the score. Pure, deterministic.

- id: reliability-weighted-routing
  title: Relationship/reliability-weighted offset routing from hive memory
  material: no
  model: sonnet
  depends: []
  proof: `npx vitest run --config vitest.pure.config.ts --pool=forks server/utils/otc/lending/__tests__/reliabilityRouting.test.ts` exits 0
  prompt: |
    Route discovery toward counterparties that historically actually fill and settle, not just the
    best paper offset. Reuse credit-by-design + relationship signals so the mesh gets smarter.
    Steps:
    - New pure module server/utils/otc/lending/reliabilityRouting.ts. `rankByReliability(candidates,
      relationshipStats)` where each candidate carries its residual-reduction (from offsetRecruiter)
      and relationshipStats provides {fillRate, settlementReliability, compositeCredit} (shape from
      server/utils/otc/rings/creditStore.ts / relationship orchestrator — inspect, inject; do not
      modify). Blend: 0.6·residualReduction-rank + 0.4·reliability. Return re-ranked candidates.
    - Tests server/utils/otc/lending/__tests__/reliabilityRouting.test.ts: between two equal-offset
      candidates the more reliable ranks first; a slightly-worse-offset but far-more-reliable candidate
      can overtake; missing stats defaults to neutral. Pure.

- id: forward-appetite-broadcast
  title: Forward-appetite broadcasting → live demand-shaping via the mesh
  material: no
  model: sonnet
  depends: [lending-mesh-ioi-bridge]
  proof: `npx vitest run --config vitest.pure.config.ts --pool=forks server/utils/otc/lending/__tests__/forwardAppetite.test.ts` exits 0
  prompt: |
    Agents publish not just current IOIs but FUTURE appetite ("I'll want duration in Q3"), so
    counterparties shape originations to meet it. Turns the queued demand-shaping report into live
    forward commitments in the mesh.
    Steps:
    - New pure module server/utils/otc/lending/forwardAppetite.ts. `ForwardAppetite { partnerId,
      factorKey, riskClass, wantSigmaUsd, fromEpochMs, toEpochMs }`. `broadcastAppetite(demandTargets,
      windowMs)` converts demandShaping originationTargets (from the queued natural-hedge-demand-shaping
      module if present; else from offsetRecruiter.idealCounterpartyProfile) into forward-dated standing
      interests; `matchForwardAppetite(appetites, incomingExposure)` finds a pre-committed counterparty
      for a new exposure. Pure.
    - Tests server/utils/otc/lending/__tests__/forwardAppetite.test.ts: a duration-seeking appetite
      matches an incoming long-duration loan within its window; outside the window it does not; empty
      demand → no appetite. Pure, epochs injected.

- id: auto-clear-in-mandate
  title: Zero-latency clearance for in-mandate mutually-beneficial hedges (plan-only)
  material: no
  model: opus
  depends: [lending-mesh-ioi-bridge, timing-intelligence-trigger]
  proof: `npx vitest run --config vitest.pure.config.ts --pool=forks server/utils/otc/lending/__tests__/autoClear.test.ts` exits 0
  prompt: |
    The mesh only delivers immediacy if a found, in-mandate, mutually-beneficial hedge can clear
    without human latency. Produce the CLEARANCE DECISION (plan-only) — actual execution + money paths
    stay operator/legal-gated and are explicitly out of scope here.
    Steps:
    - New pure module server/utils/otc/lending/autoClear.ts. `clearanceDecision({pairing, timing,
      mandateA, mandateB, constitutionDecision})` → action ∈ {auto_clear, escalate, hold}. auto_clear
      only when: both parties' benefit > floor (from timing-intelligence-trigger), notional within both
      mandates, allowed structure, AND constitutionDecision === 'allow' (accept a pre-evaluated
      decision arg — do NOT call evaluateConstitution inside the pure fn). Mirror the mandate-authorized
      standing-acceptance pattern (ioiMesh bot137) without executing.
    - Tests server/utils/otc/lending/__tests__/autoClear.test.ts: in-mandate + both-benefit + allow →
      auto_clear; over-cap → escalate; constitution deny → hold; weak timing → hold. Pure, no execution.

OPERATOR:
  - Cross-institution / competitor participation in the mesh (privacy-preserving-matching) requires
    data-sharing agreements + a privacy/security review before real competing books are matched, even
    though the matching itself never reveals raw positions.
  - Executing any mesh-discovered hedge (auto-clear-in-mandate proposals, ring legs, cross-vertical
    matches) against real counterparties requires the Policy Constitution mandate armed + per-leg
    consent + money-path/legal sign-off. All planners/deciders ship plan-only.
  - Anticipated/forward-appetite commitments that create binding future obligations need counsel review
    before they are represented as anything more than non-firm indications.
  - Calibrate stylised params against real data: timing thresholds, reliability blend weights, privacy
    epsilon/bucket coarseness, ring-search depth, anticipated-exposure probabilities.
