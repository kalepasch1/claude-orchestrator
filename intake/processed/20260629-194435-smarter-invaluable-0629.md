PROJECT: smarter

- id: smarter-docket-conflicts-ui
  title: UI for Docket Sentinel + Conflict Graph + intake conflict screen
  material: no
  model: sonnet
  depends: [smarter-get-green]
  proof: npx vue-tsc --noEmit exits 0; /docket and /conflicts render on seed; intake screen blocks on a block-hit
  prompt: |
    INVALUABLE_ASSOCIATE item 1. Build /docket (never-miss register: at-risk/lapsed up top with rule
    citations + Acknowledge/Mark-filed, grouped by matter, jurisdiction badges) and /conflicts
    (entity graph + pending block conflicts, one-tap clear/waive-with-note/decline, "screen new
    party" box). Wire conflicts/screen into the matter-open / engagementDraft flow so opening a
    matter auto-runs it and blocks until sign-off. Engines are built — wire/surface, don't rebuild.

- id: smarter-citecheck-upgrades
  title: Cite-check upgrades — Bluebook normalize + bad-law signal + standing watch
  material: no
  model: sonnet
  depends: [smarter-get-green]
  proof: npx vue-tsc --noEmit exits 0; a test flags a seeded bad-law authority via QC + TrustBadge
  prompt: |
    INVALUABLE_ASSOCIATE item 2. Extend citations.ts: (a) Bluebook/local-rule formatting
    normalization, (b) negative-treatment/bad-law signal (overruled/criticized/superseded), (c) a
    standing watch that re-checks authorities the firm relies on and raises a Now/Approve alert when
    good law goes bad. Route through qc.ts; render TrustBadge + linked sources. Mock-degradable
    (seed a small treatment table; live via provider seam).

- id: smarter-closing-tracker-ui
  title: Live CP / closing tracker UI
  material: no
  model: sonnet
  depends: [smarter-get-green]
  proof: npx vue-tsc --noEmit exits 0; /cockpit + Now show CP tracker; a chase fires on a stale consent
  prompt: |
    INVALUABLE_ASSOCIATE item 3. Surface closingTracker (built): track every CP/deliverable/consent/
    signature with status + owner + auto-chase on learned cadence, escalating up-chain when stale.
    Feed dealBible at close. Surface in /cockpit + Now. Persist via kv.ts.

- id: smarter-diligence-ui
  title: Due-diligence / data-room steward page
  material: no
  model: sonnet
  depends: [smarter-get-green]
  proof: npx vue-tsc --noEmit exits 0; /diligence renders checklist + issue log on seed
  prompt: |
    INVALUABLE_ASSOCIATE item 4. Surface diligenceSteward (built): ingest a doc set -> practice-area
    DD checklist -> issue log -> findings summary; distill anonymized review templates via
    distillation.ts. Add /diligence page + nav.

- id: smarter-filesteward-ui
  title: File steward + execution tracker + precedent harvester UI (dry-run + undo)
  material: no
  model: sonnet
  depends: [smarter-get-green]
  proof: npx vue-tsc --noEmit exits 0; a mass file-move shows a dry-run preview + one-tap undo
  prompt: |
    INVALUABLE_ASSOCIATE item 5. Surface fileSteward/executionTracker/precedentHarvester (built):
    taxonomy/naming + dedupe/lineage + misfile risk radar; execution lifecycle + "where's the
    executed original"; executed docs -> anonymized templates. Mass file-moves: dry-run preview +
    one-tap undo (actionLog) + reuse legacyCompliance dual-rail/bypass gate. DMS behind provider seam.

- id: smarter-sentinel-unify
  title: Amplifier — unify never-miss surfaces into one Sentinel
  material: no
  model: sonnet
  depends: [smarter-docket-conflicts-ui]
  proof: npx vue-tsc --noEmit exits 0; one Sentinel view aggregates deadlines+compliance+docket+CP
  prompt: |
    INVALUABLE_ASSOCIATE item 7 amplifier. Merge deadlines + complianceCalendar + outstanding
    obligations + docket + CP tracker into one firm-wide "Sentinel" view that escalates anything
    slipping. Point counterpartyReputation/staffing/bd at conflictGraph.buildEntityGraph (one entity
    substrate). Keep the Reliability Score internal-only.

OPERATOR:
  - DMS / e-filing creds: iManage/NetDocuments/SharePoint API, PACER/ECF + state e-filing logins
  - Citation provider for live bad-law/treatment (CourtListener / Shepardize-equivalent) behind citations seam
  - Court e-filing & service (PACER/ECF) auto-filer — human-tap default, per-portal auth, screenshot evidence
