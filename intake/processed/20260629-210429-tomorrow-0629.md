PROJECT: tomorrow

# Completeness Credit System — unmargined ISDA creditworthiness scoring + rewards.
# Full context in tomorrow repo: COMPLETENESS_CREDIT_IMPLEMENTATION_PLAN.md,
# completeness-credit.tasks.yaml, COMPLETENESS_CREDIT_MASTER_PROMPT.txt.
# HARD INVARIANT for EVERY task: Tomorrow is UNMARGINED. Never add, require, call,
# hold, or manage margin/collateral. Counterparty risk is expressed only via price,
# ISDA credit terms, credit limits, matching priority, access, and the mutual fund.

- id: cc-contracts
  title: Shared contracts — types, schema, API stubs for the Completeness Credit System
  material: yes
  model: opus
  depends: []
  proof: `npx tsc --noEmit` exits 0 AND `npx prisma validate` exits 0
  prompt: |
    Define SHARED CONTRACTS ONLY (types, Prisma schema, API route stubs) — NO business logic.
    See COMPLETENESS_CREDIT_IMPLEMENTATION_PLAN.md Phase 0 and completeness-credit.tasks.yaml task `contracts`.
    - server/utils/risk/completenessTypes.ts (NEW): export interfaces ResidualExposureSnapshot,
      CompletenessScore (hcs 0-100, perRiskClassHcs, haircuts{verification,basis,modelRisk,rollover},
      cappedCreditFraction), SyntheticCreditworthiness (scg A-F, hedgeAdjustedPd, exposureLimitUsd,
      cvaSpreadBps, creditTermsTier 1-5), CounterpartyAcceptancePolicy (minScg, minHcs?,
      perRiskClassMinHcs?, enabled), CompletenessImpactEstimate (deltaHcs, deltaResidualEsUsd,
      deltaCvaSpreadBps, deltaCreditTermsTier, deltaExposureLimitUsd, rationale), MutualFundContribution,
      SolvencyPassport. Exact field shapes are in completeness-credit.tasks.yaml.
    - prisma/schema.prisma: ADD models CompletenessSnapshot, MutualFundLedger, SolvencyPassportReceipt;
      ADD columns to SwapOrganization: publishedHcs (Decimal?), publishedScg (String?),
      acceptancePolicyJson (Json?). Mark legacy margin models (SwapMarginAccount, SwapMarginCall,
      SwapMarginTransaction, OtcMarginCall, OtcCollateralHolding, CollateralDeposit,
      CollateralYieldAccrual) and ExecutedSwap margin fields with `/// @deprecated unmargined-ISDA`
      comments — do NOT delete yet (cc-legacy-margin-removal handles deletion).
    - server/api/completeness/me.get.ts (NEW stub, return 501); server/api/settings/counterparty-policy.get.ts
      and .put.ts (NEW stubs, return 501).
    No implementation beyond stubs. Generate the Prisma migration but do NOT apply it (operator deploys).

- id: cc-residual-exposure-engine
  title: Firm-wide residual exposure engine (stressed residual ES across the whole book)
  material: yes
  model: opus
  depends: [cc-contracts]
  proof: `npx vitest run residualExposure` exits 0
  prompt: |
    Implement server/utils/risk/residualExposure.ts (NEW). Read-only imports from
    server/utils/otc/lending/loanRiskMapper.ts, server/utils/risk/marginalRisk.ts,
    server/utils/risk/studio/discovery/contractRiskExtractor.ts. Build the firm-wide factor-exposure
    vector from loan-level (loanRiskMapper) + UBPR-derived bank_exposure_maps + Risk-Studio
    contract-extracted exposures; subtract all hedges (on-platform ExecutedSwap/ProtectionPosition +
    verified off-platform SwapExposure). Compute STRESSED, correlated residual Expected Shortfall via
    marginalRisk using crossStrategyRiskGovernor stress scenarios. Return ResidualExposureSnapshot with
    perRiskClass breakdown and dataQuality; persist to CompletenessSnapshot. Do NOT touch pricing,
    matching, or UI. Add server/utils/risk/__tests__/residualExposure.test.ts: a synthetic book where
    hedges fully offset one risk class yields residualEsUsd~=0 for that class while grossEsUsd>0.

- id: cc-completeness-score
  title: HCS + Synthetic Creditworthiness (haircuts, cap, convex reward); remove recommendedImPct
  material: yes
  model: opus
  depends: [cc-contracts, cc-residual-exposure-engine]
  proof: `npx vitest run completenessScore` exits 0
  prompt: |
    Implement server/utils/risk/completenessScore.ts (NEW) and EDIT
    server/utils/intelligence/counterpartyCreditScorer.ts. HCS = 1 - (stressed residual ES / gross ES)
    per risk class and aggregate, scaled 0-100. Apply haircuts verification x (1-basis) x (1-modelRisk)
    x (1-rollover) using basisModel.ts and snapshot.dataQuality; enforce cappedCreditFraction
    (env COMPLETENESS_CAP, default 0.85); apply a CONVEX reward curve (quadratic above HCS 85,
    env COMPLETENESS_CONVEXITY). Output CompletenessScore + SyntheticCreditworthiness (map
    HCS+hedgeAdjustedPd -> SCG A-F, cvaSpreadBps, creditTermsTier, exposureLimitUsd). In
    counterpartyCreditScorer.ts: consume these, set hedgeAdjustedPd and exposureLimitUsd, and REMOVE
    recommendedImPct entirely (unmargined — no IM). Persist publishedHcs/publishedScg on SwapOrganization.
    Test completenessScore.test.ts: HCS monotonic increasing in hedge coverage; reward strictly convex
    above 85; credited fraction never exceeds cap.

- id: cc-pricing-cva-spread
  title: Completeness -> tighter quoted spread (reduced CVA charge)
  material: yes
  model: sonnet
  depends: [cc-contracts, cc-completeness-score]
  proof: `npx vitest run pricing` exits 0
  prompt: |
    Edit ONLY server/api/pricing/personalized.get.ts and server/api/pricing/private-adjustment.post.ts.
    Use SyntheticCreditworthiness.cvaSpreadBps to reduce the CVA component baked into the quoted spread
    (privateAdjustment): higher HCS -> tighter toward mid; low HCS -> wider credit spread. NO margin or
    collateral logic. Test pricing.test.ts: an A-grade counterparty quote spread < an E-grade quote spread
    for identical underlying risk.

- id: cc-isda-credit-terms-tier
  title: ISDA credit-terms tiering (the unmargined analog of "post less")
  material: yes
  model: sonnet
  depends: [cc-contracts, cc-completeness-score]
  proof: `npx vitest run creditTermsTier` exits 0
  prompt: |
    Implement server/utils/legalDocuments/creditTermsTier.ts (NEW) referenced from
    server/utils/legalDocuments/sourceOfTruthAppendix.ts. Map creditTermsTier 1-5 to concrete ISDA
    Schedule terms — Additional Termination Event thresholds, NAV/ratings triggers on/off, cure-period
    length, CSA-required flag (ALWAYS false; we are unmargined) — best terms at tier 1 (highest HCS).
    NEVER emit a Credit Support Annex or any margin requirement. Output a structured terms object the doc
    generator consumes. Test creditTermsTier.test.ts: tier 1 has highest ATE threshold and no ratings
    trigger; CSA-required is false at every tier. Final ISDA language needs counsel sign-off (see OPERATOR).

- id: cc-rebate-multiplier
  title: HCS multiplier on maker/conviction rebates
  material: yes
  model: sonnet
  depends: [cc-contracts, cc-completeness-score]
  proof: `npx vitest run phase3Economics` exits 0
  prompt: |
    Edit ONLY server/utils/hive/phase3Economics.ts. Scale MakerRebate and ConvictionRebate payouts by a
    completeness multiplier (1.0 at HCS 50, up to env COMPLETENESS_REBATE_MAX default 2.0 near HCS 100),
    convex near the top. Keep existing rebate accounting intact; no margin/collateral. Extend the
    phase3Economics tests: multiplier monotonic in HCS and capped.

- id: cc-acceptance-policy-and-matching
  title: Counterparty acceptance gating (min SCG/HCS, two-sided) + completeness-priority matching
  material: yes
  model: opus
  depends: [cc-contracts, cc-completeness-score]
  proof: `npx vitest run matching` exits 0
  prompt: |
    USER-REQUESTED. Complete server/api/settings/counterparty-policy.get.ts and .put.ts (read/write
    CounterpartyAcceptancePolicy on SwapOrganization.acceptancePolicyJson: minScg, minHcs,
    perRiskClassMinHcs, enabled). Enforcement: in server/utils/engines/matchingEngine.ts extend the
    existing _minCreditScore gate so a candidate is excluded if EITHER side's acceptance policy is unmet
    (two-sided filter by published SCG/HCS). In server/engine/sbmp-matcher.ts add a completeness term to
    calculateMatchScore so higher-HCS counterparties rank first. No margin logic. Test matching.test.ts:
    a firm with minScg=B never matches a D counterparty; among eligible, higher HCS ranks higher.

- id: cc-score-visibility-ui
  title: Always-on score visibility — persistent badge + dashboard panel + GET endpoint
  material: no
  model: sonnet
  depends: [cc-contracts, cc-completeness-score]
  proof: `npx vitest run completeness` exits 0
  prompt: |
    USER-REQUESTED. Implement server/api/completeness/me.get.ts (return HCS, SCG, residual ES, terms tier,
    exposure limit, estimated $ benefit for the caller's org). Add components/CompletenessBadge.vue (NEW —
    compact, persistent; mount in the app header/layout so it shows on EVERY page) and
    components/CompletenessPanel.vue (NEW — dashboard breakdown by risk class). Reuse existing war-room
    dashboard styling; live/poll refresh; NO margin fields anywhere in the UI. Test: GET /api/completeness/me
    returns the documented shape for a seeded org; a component test mounts CompletenessBadge and renders HCS+SCG.

- id: cc-nudge-impact-engine
  title: Per-contract completeness nudge (delta HCS/ES/price/terms alongside existing benefits)
  material: no
  model: opus
  depends: [cc-contracts, cc-completeness-score, cc-score-visibility-ui]
  proof: `npx vitest run completenessImpact` exits 0
  prompt: |
    USER-REQUESTED. Implement server/utils/risk/completenessImpact.ts (NEW): given a candidate
    contract/quote, recompute residual exposure as if executed and return CompletenessImpactEstimate
    (deltaHcs, deltaResidualEsUsd, deltaCvaSpreadBps, deltaCreditTermsTier, deltaExposureLimitUsd,
    rationale). Surface this delta in the contract/quote consideration UI and components/CompletenessPanel.vue
    ALONGSIDE (not replacing) the existing hedging / risk-management / profit / risk-dispersion readout.
    Wire proactive suggestions through server/utils/intelligence/demandForecaster.ts +
    server/utils/hive/bot27-autonomousOptimizer.ts ("hedge X to move your score A->B"). Test
    completenessImpact.test.ts: a contract that offsets an open exposure returns deltaHcs>0 and
    deltaResidualEsUsd<0; a redundant contract returns deltaHcs~=0.

- id: cc-mutual-default-fund
  title: Completeness-priced mutual default fund (the only mutualized loss-absorbing layer)
  material: yes
  model: opus
  depends: [cc-contracts, cc-completeness-score]
  proof: `npx vitest run mutualFund` exits 0
  prompt: |
    Implement server/utils/risk/mutualFund.ts (NEW), persist to MutualFundLedger. Contribution scales
    INVERSELY with HCS (low HCS pays more; high HCS pays little but stays covered); apply a through-the-cycle
    floor so a shock does not simultaneously cut everyone's contribution. Size to env MUTUAL_FUND_TARGET_PCTL
    (default 99th-pctl of network stressed residual gap). Expose getContribution(orgId) and
    fundCoverageStatus(). No margin/collateral. Test mutualFund.test.ts: contribution decreasing in HCS;
    floor respected; total >= target.

- id: cc-solvency-passport
  title: Solvency Passport — signed, privacy-preserving proof of creditworthiness
  material: yes
  model: sonnet
  depends: [cc-contracts, cc-completeness-score]
  proof: `npx vitest run solvencyPassport` exits 0
  prompt: |
    Implement server/utils/risk/solvencyPassport.ts (NEW), persist to SolvencyPassportReceipt. Reuse the
    existing SHA-256 settlement-proof helper to sign {orgId, asOf, hcs, scg, residualEsUsd}. Expose
    issue(orgId) and verify(receipt). Privacy: expose grade + proof only, never raw position data. Test
    solvencyPassport.test.ts: an issued receipt verifies; tampering the hcs fails verification. External-facing
    attestation wording needs counsel sign-off (see OPERATOR).

- id: cc-legacy-margin-removal
  title: Remove legacy margin/collateral artifacts (unmargined cleanup) + migration
  material: yes
  model: opus
  depends: [cc-contracts, cc-completeness-score, cc-pricing-cva-spread, cc-isda-credit-terms-tier, cc-rebate-multiplier, cc-acceptance-policy-and-matching]
  proof: `grep -rI "InitialMargin\|recommendedImPct\|MarginAccount\|CollateralYield\|OtcMarginCall\|CollateralDeposit" server prisma | grep -v _archive | wc -l` returns 0 AND `npx prisma validate` exits 0 AND `npx tsc --noEmit` exits 0
  prompt: |
    Now that the unmargined completeness system is in place, delete/neutralize legacy margin artifacts in
    prisma/schema.prisma and all server references: SwapMarginAccount, SwapMarginCall,
    SwapMarginTransaction, OtcMarginCall, OtcCollateralHolding, CollateralDeposit, CollateralYieldAccrual,
    and the margin fields on ExecutedSwap (yesInitialMargin, noInitialMargin, yesCurrentMargin,
    noCurrentMargin, and lastMarkToMarket where margin-only). Provide a Prisma migration (do NOT apply —
    operator deploys). Grep for and remove remaining imports/usages. Do NOT break non-custodial settlement,
    which does not need margin.

- id: cc-calibration-and-verification
  title: Calibration loop (realized non-payment vs predicted PD) + full-suite verification + report
  material: no
  model: opus
  depends: [cc-residual-exposure-engine, cc-completeness-score, cc-acceptance-policy-and-matching, cc-score-visibility-ui, cc-nudge-impact-engine, cc-mutual-default-fund, cc-solvency-passport, cc-legacy-margin-removal]
  proof: `npx tsc --noEmit && npx prisma validate && npx vitest run` exits 0 AND `test -f docs/COMPLETENESS_CREDIT_VERIFICATION_REPORT.md`
  prompt: |
    Implement server/utils/risk/completenessCalibration.ts (NEW) + tests. Use the riskcoreEval golden set +
    backtest-output/ to compare realized non-payment vs predicted hedge-adjusted PD and emit a recalibration
    report. Then run the whole suite (tsc, prisma validate, full vitest) and write
    docs/COMPLETENESS_CREDIT_VERIFICATION_REPORT.md summarizing the HCS distribution across seeded orgs and
    confirming zero remaining margin references.

OPERATOR:
  - Apply Prisma migrations to Supabase (cc-contracts schema additions; cc-legacy-margin-removal deletions) — staged deploy with backup; not queued.
  - Set governance env params before go-live: COMPLETENESS_CAP (default 0.85), COMPLETENESS_CONVEXITY, COMPLETENESS_REBATE_MAX (default 2.0), MUTUAL_FUND_TARGET_PCTL (default 99), and default minimum acceptance SCG (suggested B) — business sign-off required.
  - Legal/counsel sign-off on ISDA credit-terms-tier Schedule language (cc-isda-credit-terms-tier) and on external-facing Solvency Passport attestation wording (cc-solvency-passport) before either is exposed to counterparties or regulators.
  - Confirm the four open governance parameters in COMPLETENESS_CREDIT_IMPLEMENTATION_PLAN.md section 8 with the business owner.
