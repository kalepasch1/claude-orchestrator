PROJECT: pareto-2080

# Frontier batch 2 — engines that change WHICH QUESTION the system asks (substitution, shedding,
# global prioritization) plus the highest-dollar liquidity/underwriting/tax pieces. Same pattern as the
# existing server/utils/ engines: pure ESM, no deps, seedable RNG, a node:test in tests/,
# explain.makeExplanation on every decision. Inputs passed as arguments (no DB dependency). Proof for
# each = its node:test passes. Don't regress the suite beyond the 2 known pre-existing failures
# (agentLedger, bookingSaga — local Prisma-binary only).
#
# Invariants (never break): free (no fee/affiliate/data-sale); non-discretionary by default (prepare,
# human commits); non-custodial; agent-of-the-user; writeback on every completed transaction;
# reconstructed vendor models stay CONSUMER-FACING/INTERNAL — never sold, never fed back to vendors.
#
# Cross-file note: substitution-search, self-play-floor, underwriting-reconstruction, and
# anticipatory-exit conceptually consume pricingGridReconstruction / framingEligibility (queued in the
# prior pareto-2080 files). They take those as arguments, so they build+test independently with
# synthetic inputs; wire to the real producers at merge.

- id: substitution-search
  title: Search the substitution space (don't buy this product at all) not just the price space
  material: no
  model: opus
  depends: []
  proof: `node --test tests/substitutionSearch.test.js` exits 0
  prompt: |
    Add server/utils/substitutionSearch.js (pure). The biggest win is often substituting the product
    category, not shopping within it: don't refi -> HELOC + invest the delta; don't buy term life ->
    self-insure past the crossover; don't finance -> pay cash (or vice versa). Given a user goal + balance
    sheet, enumerate legitimate SUBSTITUTES for a proposed product and rank by after-tax lifetime value.
    Compose the EXISTING loanVsCash (lenderOptimizer.js), insuranceHealth.js (self-insurance crossover),
    derivativesAdvisor.js, debtOptimizer.js — import, don't reinvent. Return ranked
    {substitute, deltaLifetimeUsd, rationale, requiresUserConfirmation}. Tests: self-insure beats term
    life past the funded crossover; pay-cash beats financing when the loan rate exceeds expected
    after-tax invest return; the incumbent product wins when no substitute dominates.

- id: negative-space-optimizer
  title: Detect products the user is better off WITHOUT (shed/consolidate as a first-class move)
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/negativeSpace.test.js` exits 0
  prompt: |
    Add server/utils/negativeSpace.js (pure). Continuously scan the held book for holdings to shed:
    overlapping insurance coverage, redundant subscriptions, dominated funds (higher fee/lower return than
    a peer), coverage outgrown (e.g. life insurance past need), cards whose annual fee no longer earns out.
    Compose the EXISTING cardPortfolio.js (keep/cancel/downgrade), subscriptionRadar.js, investmentCosts.js.
    Return ranked {holding, action:'cancel'|'consolidate'|'downgrade', annualSavingsUsd, reason,
    riskNote?}. Tests: two overlapping policies flag the redundant one; a dominated fund is flagged vs a
    cheaper equivalent; a card whose fee exceeds its earned rewards is flagged to downgrade.

- id: networth-velocity-prioritizer
  title: One north-star scale so ANY action across ANY engine is rankable (global next-best-action)
  material: no
  model: opus
  depends: []
  proof: `node --test tests/networthVelocity.test.js` exits 0
  prompt: |
    Add server/utils/networthVelocity.js (pure). Tie every candidate action — rate switch, tax move,
    timing call, cancellation, substitution — to ONE north-star (retirement-day velocity / lifetime net
    worth) so they are comparable on a single scale and the system always does the globally highest-value
    next thing. Compose the EXISTING valueLedger.js (normalize cash/points/deductions/time -> USD ->
    retirement-days) + optimizationScore.js. Provide `scoreAction(action, ctx)` ->
    { retirementDayDelta, usdLifetimeDelta, effortCost, leverage } and `rankNextBest(actions, ctx)` ->
    the ordered global queue. Tests: a $/yr recurring action outranks a one-time of equal nominal; effort
    cost breaks ties; the ranking is a total order and stable.

- id: household-jit-liquidity
  title: JIT liquidity + float/rewards routing for the household's working capital
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/householdJitLiquidity.test.js` exits 0
  prompt: |
    Add server/utils/householdJitLiquidity.js (pure). Route every inflow/outflow through the account that
    maximizes float + rewards + yield, and keep idle cash in the highest safe yield with a JIT buffer for
    outflows. This mirrors Tomorrow's personal-treasury stack (personalTreasury / bonusRotationScheduler /
    jitLiquidityRail / depositBonusOptimizer — see the tomorrow CLAUDE.md W16). Reimplement the RETAIL
    core here, pure: `routeCashflow(flows, accounts)` -> per-flow best account (yield/rewards/float),
    `sizeJitBuffer(outflowForecast, uncertainty)` -> instant-liquidity tranche vs stays-invested
    remainder, `idleCashSweep(balances, buffer, yieldMenu)` -> sweep plan. Non-custodial: outputs are
    staged instructions, moves no funds. Tests: an outflow routes to the account that preserves the most
    float/rewards; buffer sizing covers the forecast at the target confidence; idle cash above buffer
    sweeps to the top safe yield.

- id: underwriting-reconstruction
  title: Reconstruct each vendor's APPROVAL model (not just its rate card) to maximize approval odds
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/underwritingReconstruction.test.js` exits 0
  prompt: |
    Add server/utils/underwritingReconstruction.js (pure). Extend the vendor-modeling posture from pricing
    to APPROVAL: from observed application outcomes {vendorId, productKey, profileFeatures, approved,
    conditions}, fit each vendor's approval model and recover its decision boundaries. Provide
    `fitApprovalModel(outcomes, {vendorId, productKey})` -> { boundaries, marginalFeatureEffects, n },
    `predictApproval(model, profile)` -> { probability, bindingConstraints[] }, and
    `optimizeApplicationPlan(profile, models)` -> which vendor(s), in what order, with which truthful
    document characterization, to maximize approval while minimizing inquiry damage (compose the queued
    framingEligibility for truthful framings + creditScore for inquiry effects). CONSUMER-FACING/INTERNAL
    ONLY; truthful representations only. Material: extends the reconstruction posture. Tests: recovers a
    synthetic approval boundary; predictApproval calibrated on held-out; application ordering reduces
    modeled inquiry damage vs naive all-at-once.

- id: dynamic-tax-realization
  title: Jointly optimize lot selection + timing-of-realization with each switch decision
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/dynamicTaxRealization.test.js` exits 0
  prompt: |
    Add server/utils/dynamicTaxRealization.js (pure). After-tax normalization is static; this is dynamic:
    when a switch triggers a taxable event (sell a position to move banks/brokerages, surrender a policy),
    jointly optimize which tax lots to realize and WHEN, against the rate gain from switching. Compose the
    EXISTING taxOptimizer.js + decumulation.js. Provide `optimizeSwitchRealization({switchGainUsd, lots,
    holdingPeriods, bracket, washSaleRisk})` -> { lotsToRealize, timing:'now'|'defer_to_LT'|'stagger',
    netAfterTaxUsd, note } and compare to the naive "switch immediately, realize all". Tests: deferring a
    near-LT lot to cross the long-term boundary beats immediate realization when the rate gain is small;
    wash-sale risk blocks a harvest; a large rate gain justifies realizing now.

- id: self-play-floor
  title: Self-play red-team to the theoretical global-minimum price (vendor x framing x timing)
  material: no
  model: opus
  depends: []
  proof: `node --test tests/selfPlayFloor.test.js` exits 0
  prompt: |
    Add server/utils/selfPlayFloor.js (pure). Run an internal adversary that JOINTLY exploits every
    reconstructed pricing grid seam, every truthful framing, and every timing window to find the
    theoretical floor price for a profile — then set that as the negotiation target so the system never
    settles for a local best. Compose pricingGridReconstruction (grids), framingEligibility (truthful
    framings), rateOptions/predictionSignals (timing). Provide `findGlobalFloor(profile, grids, framings,
    timingModel)` -> { floorRate, viaVendor, viaFraming, viaWindow, upliftVsBestSingleBp }. Deterministic
    search over the discretized joint space (seedable). Tests: the joint floor <= the best single-grid
    price; the winning (vendor, framing, window) combo is reported; only truthful framings are considered.

- id: anticipatory-exit
  title: Predict provider deterioration (rate creep / new fee / distress) and pre-stage the exit
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/anticipatoryExit.test.js` exits 0
  prompt: |
    Add server/utils/anticipatoryExit.js (pure). Predict when a user's current provider is about to get
    worse — rate creep, a fee about to be introduced, service decay, financial distress — BEFORE it lands,
    and pre-stage the exit. Compose pricingGridReconstruction.detectReprice (vendor moving overlays) +
    dealWatcher.js patterns. Provide `predictDeterioration(vendorGridHistory, signals)` ->
    { likelihood, expectedHarmBp, horizonDays, driver } and `shouldPreStageExit(prediction, bestAlt)` ->
    boolean + a staged-exit descriptor. Tests: a rising-overlay trend raises deterioration likelihood; a
    stable vendor scores low; pre-stage triggers only when a better alternative exists and expected harm
    clears a threshold.

OPERATOR:
  - underwriting-reconstruction: CONSUMER-FACING/INTERNAL ONLY (never sold, never fed back to vendors) — same posture as pricing-grid reconstruction; truthful representations only. Confirm consumer-compliance posture before it drives real application ordering.
  - substitution-search / framing dependencies: any suggestion that changes how a user is represented or what product they buy requires user confirmation of the truthful basis before it drives an application.
  - household-jit-liquidity is non-custodial (staged instructions only). The actual money movement remains the operator session-rail runtime with the user's own one-tap; this engine plans, it does not move funds.
