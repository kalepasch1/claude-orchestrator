PROJECT: tomorrow

# Additive extensions to the business-model-aware CURATION layer.
# The base layer (server/utils/curation/**, /api/curation/**, /api/cron/**,
# pages/app/curated/index.vue) was landed by a prior intake
# (tomorrow-curation-0702, now processed) whose foundational task id is
# `curation-layer-land`, and whose frontier-deltas task id is
# `curation-frontier-product-deltas`. Every task below is ADDITIVE (creates NEW
# files only) so it cannot merge-conflict with the in-flight curation tasks that
# edit dashboard.get.ts / index.vue / the Prisma schema. Each depends on the base
# task so it runs after the layer exists. If server/utils/curation is somehow
# absent in the worktree, run `BASE=$(pwd) bash outputs/install-curation-layer.sh`
# first. Keep the prod build (`npm run build` -> nuxt build) green — that is the
# merge gate.

- id: curation-conformance-corpus
  title: Golden regulatory conformance fixtures for the metric calculators
  material: no
  model: sonnet
  depends: [curation-layer-land]
  proof: `npm run build` exits 0 AND `npx vitest run server/utils/curation/__tests__/conformance.test.ts` passes
  prompt: |
    Add server/utils/curation/__tests__/conformance.test.ts + a fixtures file
    server/utils/curation/__tests__/fixtures/regulatory-worked-examples.json holding
    published worked examples (a bank RC-R style CET1/LCR/NSFR set, a NAIC RBC ACL
    example, a Solvency II SCR/MCR example, a UPMIFA spending example). Assert the
    pure calculators in server/utils/curation/regulatoryMetrics.ts reproduce each
    published figure within tolerance. Test-only, additive; no source changes. This
    regression-proofs the layer against future refactors and rule changes.
    CANDIDATE-SHARED: regulatory worked-example corpus reusable by apparently/smarter.

- id: curation-driver-attribution
  title: Metric-move driver attribution module + read-only endpoint
  material: no
  model: sonnet
  depends: [curation-layer-land]
  proof: `npm run build` exits 0 AND `npx vitest run server/utils/curation/__tests__/attribution.test.ts` passes
  prompt: |
    Add a PURE module server/utils/curation/attribution.ts exporting
    attributeMove(metricId, priorInputs, currentInputs) that decomposes a metric
    change into additive driver contributions (e.g. CET1 move split into RWA change
    vs capital change; VaR move into vol vs exposure) using the provenance inputs
    already captured by valueAssembler/regulatoryMetrics. Return ordered
    contributions summing to the total delta. Add __tests__/attribution.test.ts.
    Add a read-only GET /api/curation/attribution endpoint that composes it over
    two curation snapshots. Additive files only. CANDIDATE-SHARED: attribution.ts.

- id: curation-uncertainty-bands
  title: Uncertainty propagation to curated metrics (confidence intervals)
  material: no
  model: sonnet
  depends: [curation-layer-land]
  proof: `npm run build` exits 0 AND `npx vitest run server/utils/curation/__tests__/uncertainty.test.ts` passes
  prompt: |
    Add a PURE module server/utils/curation/uncertainty.ts that propagates input
    uncertainty through the metric calculators to a value + [p10,p90] band (analytic
    for ratios, seeded Monte-Carlo for VaR/SCR). Model it on the uncertainty pattern
    used elsewhere in the platform (search for an existing uncertainty/propagate
    util and REUSE it if present; otherwise implement a small seedable version). Add
    __tests__/uncertainty.test.ts asserting bands widen with input variance and
    collapse to the point value at zero variance. Additive only; do not change the
    curator output shape (expose via a separate helper). CANDIDATE-SHARED: uncertainty.ts.

- id: curation-narrative-generator
  title: Board/regulator narrative generator from diff + provenance
  material: no
  model: sonnet
  depends: [curation-layer-land]
  proof: `npm run build` exits 0 AND `npx vitest run server/utils/curation/__tests__/narrative.test.ts` passes
  prompt: |
    Add a PURE module server/utils/curation/narrative.ts exporting
    buildNarrative(diff, panels) that produces a deterministic, template-based
    plain-text narrative of the quarter's posture ("CET1 fell 30bps driven by X;
    remediation Y available") from curationDiff output + evaluated panels +
    provenance. NO LLM call in the pure core (keep it deterministic + testable); an
    optional polish pass may be layered later. Add __tests__/narrative.test.ts.
    Expose GET /api/curation/narrative. Additive only. CANDIDATE-SHARED: narrative.ts.

- id: curation-whatif-console
  title: What-if console page — client-side posture/frontier simulation
  material: no
  model: sonnet
  depends: [curation-frontier-product-deltas]
  proof: `npm run build` exits 0
  prompt: |
    Add a NEW page pages/app/curated/whatif.vue (do NOT edit index.vue). It loads
    the tenant curated values from /api/curation/dashboard and lets the user adjust
    a candidate hedge (notional slider) and see posture + frontier recompute live by
    calling POST /api/curation/simulate (added by the in-flight
    curation-remediation-preview-ui task; if that endpoint is absent, add a thin
    server/api/curation/simulate.post.ts here that calls solveFrontier +
    candidateActionsFor). Reuse constraintFrontier/remediationSimulator; no new
    engine logic. Keep the prod build green. No money movement (pure simulation).

- id: curation-group-supervision
  title: Multi-entity group supervision roll-up module + endpoint
  material: yes
  model: opus
  depends: [curation-layer-land]
  proof: `npm run build` exits 0 AND `npx vitest run server/utils/curation/__tests__/groupSupervision.test.ts` passes
  prompt: |
    Many tenants are holding companies with regulated subsidiaries across
    frameworks (bank + insurer sub + broker-dealer). Add a PURE module
    server/utils/curation/groupSupervision.ts that takes per-entity curated
    dashboards and rolls them into a group view: worst binding constraint per
    entity, group solvency posture (MIN across entities), inter-entity capital
    fungibility flags, and a consolidated attention queue. Add
    __tests__/groupSupervision.test.ts. Expose GET /api/curation/group. Material:
    reads across multiple entities — keep RLS default-deny and aggregate-only; a
    caller may only see groups they own. CANDIDATE-SHARED: groupSupervision.ts.

- id: curation-streaming-spike
  title: Event-driven metric recompute spike (behind a flag)
  material: no
  model: sonnet
  depends: [curation-live-engines-adapter]
  proof: `npm run build` exits 0 AND `npx vitest run server/utils/curation/__tests__/recomputeGraph.test.ts` passes
  prompt: |
    Prototype intraday, event-driven curation behind a feature flag
    (CURATION_STREAMING_ENABLED, default OFF). Add a PURE
    server/utils/curation/recomputeGraph.ts that, given a changed input id, returns
    only the metric ids that must recompute (a static dependency map from input ->
    affected metrics; reuse the platform computeGraph pattern if one exists). Add
    __tests__/recomputeGraph.test.ts. Add a guarded POST /api/curation/recompute
    that no-ops unless the flag is set. Additive; flag OFF so prod behavior is
    unchanged and the build stays green. Spike to prove the reactive path.

OPERATOR:
  - Group-supervision (curation-group-supervision) exposes cross-entity data — review RLS + ownership scoping before enabling in prod.
  - Set CURATION_STREAMING_ENABLED only after the streaming spike is reviewed; leave OFF by default.
  - Any narrative "polish" LLM pass must go through the logged AI-call path (cost/audit) — not part of the deterministic core task.
