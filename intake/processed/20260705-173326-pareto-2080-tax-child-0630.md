PROJECT: pareto-2080

# Wave 3: the Tax Department ("kill TurboTax" — pre-emptive, year-round, not April-only),
# Smarter email/document ingestion, and child optimization. All slices are PURE-LOGIC engines
# (server/utils/*.js, ESM, makeExplanation from ./explain.js) + node:test, run with
# `node --test tests/<name>.test.js`. No money movement / e-file / schema / PII in these slices —
# filing, cross-service federation, PHI handling, and the insurance vehicle are OPERATOR cards.
# Reuse existing engines: taxOptimizer, taxAdvantaged, deductionOptimizer, decumulation,
# retirementAccountRules, socialSecurity, benefitsEligibility, charitableOptimizer, emailIntel,
# quarantinedReader, healthcarePriceArbitrage, childPlanning, goodsSourcing, geoArbitrage,
# insuranceHealth, providerFinder. Context: CLAUDE.md + TREASURY_HANDOFF_PROMPT.md.

# ---------------- TAX DEPARTMENT ----------------

- id: aca-magi-controller
  title: ACA MAGI / subsidy-cliff controller — pick the withdrawal mix to hit a MAGI target
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/acaMagiController.test.js` exits 0
  prompt: |
    Build `server/utils/acaMagiController.js` (pure logic, reuse taxAdvantaged.js ACA MAGI math).
    For an early retiree drawing from mixed accounts, choose the withdrawal source mix (Roth/basis =
    MAGI-invisible; traditional/gains = MAGI-visible) that funds annual spend while landing MAGI just
    under the chosen ACA subsidy/CSR cliff. Inputs: annualSpendNeed, accounts{roth,basis,traditional,
    taxableGainsRate}, householdSize, state, targetCliff (e.g. 150/200/250% FPL). Outputs:
    withdrawalMix, projectedMAGI, subsidyPreservedUsd, cliffHeadroom, warnings, explanation. TEST:
    a mix exists that stays under the cliff → returns it with subsidyPreserved>0; if spend forces
    MAGI over the cliff, flag it and return the least-overage mix.

- id: roth-conversion-ladder
  title: Multi-year Roth-conversion ladder optimizer for low-income gap years
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/rothConversionLadder.test.js` exits 0
  prompt: |
    Build `server/utils/rothConversionLadder.js` (pure logic, reuse taxAdvantaged + retirementAccountRules).
    Across N gap years, choose annual traditional→Roth conversion amounts that fill low brackets
    cheaply now while reducing future RMD/bracket pressure. Inputs: perYear projected other income,
    bracket schedule, traditionalBalance, horizon, ages (RMD start), optional IRMAA thresholds.
    Outputs: perYearConversion[], lifetimeTaxDelta vs no-ladder, rmdReductionUsd, explanation. TEST:
    filling the 12% bracket in a zero-income year is recommended; conversions stop before crossing a
    materially higher bracket unless future RMDs justify it; lifetimeTaxDelta is negative (savings).

- id: gap-year-tax-optimizer
  title: Joint gap-year tax optimizer — MAGI × Roth conversion × IRMAA × cap-gain brackets
  material: no
  model: opus
  depends: [aca-magi-controller, roth-conversion-ladder]
  proof: `node --test tests/gapYearTaxOptimizer.test.js` exits 0
  prompt: |
    Build `server/utils/gapYearTaxOptimizer.js` (pure logic) composing acaMagiController +
    rothConversionLadder. These levers FIGHT each other (a Roth conversion raises MAGI and can blow
    the ACA cliff / an IRMAA tier / the 0% LTCG bracket). Solve them JOINTLY per year: maximize
    (ACA subsidy preserved + LTCG-at-0% harvested + long-run tax saved from conversions) minus
    (conversion tax + IRMAA surcharges). Inputs: multi-year income/spend, accounts, ACA + IRMAA +
    LTCG thresholds, horizon. Outputs: perYearPlan[{conversion, magiTarget, ltcgHarvest, subsidy,
    irmaaTier}], lifetimeValueUsd, explanation. TEST: the joint plan is >= either lever alone on a
    scenario where they conflict; it never silently blows the ACA cliff to chase a conversion without
    accounting for the lost subsidy.

- id: tax-return-engine
  title: Compute a full 1040 + common schedules from structured inputs (the compute core)
  material: no
  model: opus
  depends: []
  proof: `node --test tests/taxReturnEngine.test.js` exits 0
  prompt: |
    Build `server/utils/taxReturnEngine.js` (pure logic) — the "kill TurboTax" compute core. Given a
    structured tax-year input (wages, 1099-INT/DIV/B, cap gains w/ lots, Sched C, deductions,
    credits, filing status, dependents, state), compute AGI, taxable income, tax (ordinary + LTCG
    stacking), credits (CTC, EITC eligibility, education, saver's), AMT check, total liability,
    withholding reconciliation, and refund/owe. Return a line-itemized result object mirroring 1040
    lines + a schedules breakdown + explanation. Deterministic; cite the bracket/threshold constants
    as assumptions (tax-year param). NO e-file, no PII storage — pure math. TEST tests/taxReturnEngine.test.js:
    a W-2 + standard-deduction case matches hand-computed liability; LTCG stacking is correct; a
    withholding surplus yields the right refund.

- id: tax-doc-autoingest
  title: Map W-2/1099/1098/1095-A documents into the tax-return-engine input schema
  material: no
  model: sonnet
  depends: [tax-return-engine]
  proof: `node --test tests/taxDocAutoingest.test.js` exits 0
  prompt: |
    Build `server/utils/taxDocAutoingest.js` (pure logic). Normalize parsed tax documents (W-2,
    1099-INT/DIV/B/NEC, 1098, 1095-A, K-1) — the structured output emailIntel/Smarter produce — into
    the exact input object `taxReturnEngine` consumes. Handle multiples (two W-2s sum), missing
    fields (flag as needed), and duplicates (dedupe by issuer+box). Outputs: taxReturnInput,
    missingDocs[], warnings, explanation. TEST tests/taxDocAutoingest.test.js: two W-2s aggregate;
    a 1099-B with lots maps to cap-gain inputs; a missing 1095-A for an ACA user is flagged.

- id: estimated-tax-annualizer
  title: Annualized-installment estimated-tax method for lumpy income (no penalty, no overpay)
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/estimatedTaxAnnualizer.test.js` exits 0
  prompt: |
    Build `server/utils/estimatedTaxAnnualizer.js` (pure logic). For variable/lumpy income, compute
    each quarter's minimum required estimated payment via the annualized-installment method (Form
    2210 Schedule AI logic) so the user avoids underpayment penalties WITHOUT prepaying evenly on
    income not yet earned. Inputs: perQuarter actual income, withholding, prior-year liability + AGI
    (safe harbor), filing status. Outputs: perQuarterRequired[], safeHarborUsd, penaltyRiskFlag,
    explanation. Complements irsFloatOptimizer (which handles W-2 withholding). TEST: back-loaded
    income yields small early payments + larger later ones, all penalty-safe; steady income matches
    the regular method.

- id: hsa-shoebox-optimizer
  title: HSA stealth-Roth — pay OOP now, invest HSA, bank receipts for tax-free reimbursement later
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/hsaShoebox.test.js` exits 0
  prompt: |
    Build `server/utils/hsaShoebox.js` (pure logic). Decide when to pay medical costs from cash and
    let the HSA stay invested (triple-tax + decades of tax-free growth), tracking a receipt ledger of
    unreimbursed qualified expenses redeemable tax-free anytime later. Inputs: hsaBalance, expected
    return, annual qualified OOP, cashBufferOk(bool), horizon. Outputs: payFromCash(bool per period),
    projectedHsaGrowthUsd vs paying-from-HSA, bankedReceiptsUsd (tax-free withdrawal capacity),
    explanation. TEST: with adequate cash + long horizon → pay-from-cash and bank receipts wins;
    tight cash → pay-from-HSA; banked receipts accumulate correctly.

- id: mega-backdoor-roth
  title: Detect after-tax 401(k) + in-plan Roth conversion capacity and size contributions
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/megaBackdoorRoth.test.js` exits 0
  prompt: |
    Build `server/utils/megaBackdoorRoth.js` (pure logic, reuse employerPlanOptimizer). Given plan
    features (allowsAfterTax, allowsInPlanConversion or in-service withdrawal), employee deferrals,
    employer match, and the year's overall 415(c) limit, compute the additional after-tax
    contribution that can be converted to Roth and the lifetime tax-free growth captured. Inputs:
    salary, deferralYTD, matchUsd, limits, planFeatures. Outputs: afterTaxCapacityUsd,
    convertibleUsd, eligible(bool + reason), explanation. TEST: a plan with after-tax + in-plan
    conversion yields capacity up to the 415(c) gap; a plan without the feature returns eligible:false.

- id: charitable-bunching-scheduler
  title: DAF bunching + appreciated-stock donation, coordinated with high-income/conversion years
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/charitableBunching.test.js` exits 0
  prompt: |
    Build `server/utils/charitableBunching.js` (pure logic, reuse charitableOptimizer). Decide which
    years to "bunch" multiple years of giving into a DAF to clear the standard-deduction hurdle, and
    when to donate long-term appreciated stock (avoid cap-gain AND deduct fair value) vs cash. Inputs:
    annualGivingIntent, standardDeduction, otherItemized, appreciatedLots, marginalRate, horizon,
    optional high-income years to target. Outputs: bunchYears[], donateStockPlan[], taxSavedVsAnnual,
    explanation. TEST: bunching two years clears the standard deduction and beats annual giving;
    donating appreciated stock beats cash when a lot has large unrealized gain.

- id: asset-location-optimizer
  title: Tax-aware asset location — place each holding in its lowest-drag account
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/assetLocation.test.js` exits 0
  prompt: |
    Build `server/utils/assetLocation.js` (pure logic; complement portfolioOptimizer, don't rewrite).
    Given target allocation + accounts (taxable, traditional, Roth) and per-asset tax characteristics
    (bond interest, qualified vs non-qualified dividends, turnover), place assets to minimize annual
    tax drag (bonds/high-income in tax-deferred; high-growth in Roth; tax-efficient equity in taxable).
    Inputs: allocation, accountBalances, assetTaxProfiles, marginalRates. Outputs: placementMap,
    annualTaxDragBeforeUsd/AfterUsd, dragSavedUsd, explanation. TEST: bonds land in tax-deferred and
    growth in Roth; drag decreases vs a naive proportional placement.

- id: tax-department-orchestrator
  title: Year-round tax bot — run every tax engine pre-emptively, emit a living plan + filing packet
  material: no
  model: opus
  depends: [gap-year-tax-optimizer, tax-return-engine, hsa-shoebox-optimizer, charitable-bunching-scheduler, asset-location-optimizer]
  proof: `node --test tests/taxDepartment.test.js` exits 0
  prompt: |
    Build `server/utils/taxDepartment.js` (pure logic) — the "kill TurboTax" surface: not an
    April-only return, but a YEAR-ROUND plan. Compose gapYearTaxOptimizer, rothConversionLadder,
    acaMagiController, hsaShoebox, charitableBunching, assetLocation, estimatedTaxAnnualizer,
    irsFloatOptimizer (from wave 2), and taxReturnEngine into: (1) a running current-year projection,
    (2) a ranked pre-emptive action list with per-action $ and deadlines (the guidance TurboTax never
    gives), (3) a quarterly estimated-tax schedule, (4) a year-end filing packet from taxReturnEngine.
    Inputs: the user's full tax world (accounts, income, docs, prefs). Outputs: projection,
    actionList[] (ranked by $/urgency, each a Tier-A/B descriptor), quarterlySchedule, filingPacket,
    totalTaxSavedUsd, explanation. TEST tests/taxDepartment.test.js: it returns a non-empty ranked
    actionList with dollar impacts; the filing packet reconciles to taxReturnEngine; actions carry
    deadlines.

# ---------------- SMARTER INGESTION (in-repo adapter; federation = OPERATOR) ----------------

- id: smarter-document-adapter
  title: Ingest Smarter email/document events → route to the right Pareto bot (quarantined)
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/smarterDocumentAdapter.test.js` exits 0
  prompt: |
    Build `server/utils/smarterDocumentAdapter.js` (pure logic, reuse emailIntel MAIL_SCHEMA +
    quarantinedReader pattern). Normalize an inbound Smarter document/email event into Pareto's
    internal schema and ROUTE it: tax_doc → taxDocAutoingest, bill/medical → medicalBillAudit +
    negotiator, insurance → selfInsuranceCrossover, statement → vault/spend. CRITICAL: untrusted
    external content NEVER triggers a Tier-C action (quarantined-reader invariant) — routing only
    proposes Tier-A/Tier-B. Inputs: smarterEvent. Outputs: { normalized, route, proposedTier,
    quarantined:true }, explanation. TEST tests/smarterDocumentAdapter.test.js: a tax_doc routes to
    tax ingest; a hospital bill routes to medical audit; nothing yields tier C; malformed events are
    rejected safely.

- id: medical-bill-auto-audit
  title: Audit a medical bill for errors + fair price, draft a Tier-A dispute, check charity care
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/medicalBillAudit.test.js` exits 0
  prompt: |
    Build `server/utils/medicalBillAudit.js` (pure logic, reuse healthcarePriceArbitrage). Given a
    parsed itemized medical bill, detect common errors (duplicate line items, unbundling/upcoding,
    quantity errors, out-of-network balance-billing that may violate the No Surprises Act), compare
    each CPT/DRG to a fair-price benchmark, estimate overcharge, generate a Tier-A dispute-letter
    draft, and screen charity-care/financial-assistance eligibility by income vs FPL. Inputs: bill
    lineItems[], benchmarks, householdIncome, householdSize, insurerStatus. Outputs: flaggedItems[],
    estimatedOverchargeUsd, disputeDraft, charityCareEligible(bool+reason), explanation. Draft only —
    SENDING is Tier-A via agentLedger. TEST: a duplicated line + an upcode are flagged with overcharge
    $; a low-income patient is charity-care eligible; a clean bill yields no flags.

# ---------------- CHILD OPTIMIZATION ----------------

- id: child-goal-package-builder
  title: Default child goal packages — global sourcing + negotiation, birth → teen
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/childGoalPackage.test.js` exits 0
  prompt: |
    Build `server/utils/childGoalPackage.js` (pure logic, compose childPlanning + goodsSourcing +
    geoArbitrage). Given a child's age (or expected birth) and the family's residence country,
    compile the full package of needed items/services by life stage (newborn gear, childcare,
    healthcare cadence, education) with global sourcing windows + Tier-A negotiation opportunities and
    a birth→teen cost curve localized to the country of residence. Inputs: childAge/birthDueDate,
    residenceCountry, budgetPref. Outputs: stages[{ageRange, items[], estCostLocalized, sourcing[],
    negotiable[]}], totalToTeenUsd, explanation. TEST tests/childGoalPackage.test.js: a newborn
    yields the newborn stage package; residence country changes localized costs; each stage carries
    sourcing windows.

- id: child-medical-proximity-flag
  title: Flag retirement-location proximity to quality pediatric/emergency care + fold in child med costs
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/childMedicalProximity.test.js` exits 0
  prompt: |
    Build `server/utils/childMedicalProximity.js` (pure logic, compose geoArbitrage + providerFinder
    + insuranceHealth + childPlanning). For each candidate retirement/residence location, score
    proximity + quality of pediatric and emergency care, flag locations that are risky for a family
    with young children, and fold expected child medical costs (well-child cadence, ER risk) into the
    location's total cost — accounting for self-insurance and/or global insurance coverage. Inputs:
    candidateLocations[], childAges, insuranceProfile. Outputs: perLocation{ pediatricScore,
    emergencyProximity, riskFlag, expectedChildMedicalUsd, adjustedTotalCost }, ranked, explanation.
    TEST: a remote location with poor pediatric access is flagged and penalized; a location near a
    children's hospital ranks better; child medical cost is added to the location total.

- id: communal-insurance-pool-model
  title: Pure model for a Pareto communal insurance vehicle (ECP) hedging catastrophic child/medical cost
  material: no
  model: opus
  depends: []
  proof: `node --test tests/communalInsurancePool.test.js` exits 0
  prompt: |
    Build `server/utils/communalInsurancePool.js` (pure logic) — the risk-pooling + swap-pricing
    MODEL for a Pareto communal insurance vehicle that hedges catastrophic child/medical costs across
    members, with the tail risk laid off via a swap (the Tomorrow integration prices/executes the
    swap — NOT in this slice). Inputs: members[] (risk profile, coverage requested), lossDistribution
    params, poolCapitalUsd, swapAttachmentPoint, premiumLoad. Outputs: perMemberPremiumUsd, poolExpectedLossUsd,
    swapNotionalUsd + swapPremiumUsd (priced from the tail beyond attachment), solvencyRatio,
    explanation. MODEL only — the regulated vehicle, capital, and swap execution are OPERATOR cards.
    TEST tests/communalInsurancePool.test.js: premiums cover expected loss + load; raising the swap
    attachment lowers swap premium but raises pool retained risk; solvencyRatio computed; zero
    members → no crash.

OPERATOR:
  - IRS/state e-file integration + PII handling + preparer compliance (Circular 230 / PTIN) required
    to actually FILE the return taxReturnEngine computes. The engine computes; filing is human/partner
    gated. (legal/secret)
  - Smarter ↔ Pareto federation: cross-service auth (S2S tokens), a data-sharing agreement, and the
    live event stream so Smarter's parsed emails/documents flow into smarterDocumentAdapter in real
    time. Pareto is NOT currently connected to Smarter — this is the link. (secret/legal)
  - Medical-bill dispute SENDING + provider negotiation transmission, incl. any PHI/HIPAA handling for
    bills ingested from Smarter. Drafts are generated in-repo; transmission is gated. (legal/secret)
  - Tomorrow ↔ Pareto federation to price + execute the swap that lays off the communal pool's tail
    risk (communalInsurancePool consumes a swap price; Tomorrow provides it). (secret/partner)
  - Legal/regulatory structure to operate the communal insurance vehicle (ECP): risk-bearing entity,
    state insurance law, capital/solvency, reinsurance/swap counterparty. Do not operate a pool
    without counsel. (legal)
  - Global child-item sourcing + logistics vendor integrations per residence country. (operator)
