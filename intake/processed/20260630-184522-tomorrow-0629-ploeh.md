PROJECT: tomorrow

# Perpetual Legal-Opinion Event Hedge (PLOEH) — TOMORROW side (perpetual instrument / pricing / netting / settlement).
# Pairs with intake/apparently-0629-ploeh.md (the oracle/trigger/living-memo side). Apparently emits, over a
# signed S2S feed, a ComplianceRiskVector (per-event probability/time/severity) + ComplianceTriggerSpecs +
# offline-verifiable proof-of-trigger. Tomorrow turns compliance risk into a hedgeable factor class, prices it at
# MARGINAL contribution to a netted cross-client book, sources capacity from the other side, and settles parametrically.
# REUSE (do not rebuild): server/utils/risk/marginalRisk.ts (Euler allocation + incrementalContribution),
# server/utils/otc/riskFabric/perpetualLegs.ts (synthesize/reStrikePerpetualLeg), perpetualContracts.ts,
# otc/lending/networkNetting.ts + offsetRecruiter.ts + valueAttribution.ts + loanRiskMapper.ts (the factor-mapper
# pattern), Risk Studio studio/oracle/{triggerOracle,settlementResolver}.ts + studio/ils/ilsOriginator.ts +
# synthInsurance/instantSettlement.ts + proofOfFloor, instrumentAllowlist (SWAP_ONLY_MODE), guardrails
# assertEcpCounterparty / assertParametricOnly / assertNotStatutoryInsurance, the gaming S2S bridge pattern.
# BRIGHT LINE (test-enforced): parametric/index trigger only — NOT indemnity of actual loss; bilateral ECP; swap-only;
# non-custodial (settlement is an instruction REF + proof, never custody). FIRST STEP: grep for any existing PLOEH /
# perpetual-opinion-hedge scaffolding and extend it.
# Conventions: pure cores unit-testable (no DB/HTTP/AI in unit tests); fp-exact netting invariants; vitest pure config
# where applicable (npx vitest run --config vitest.pure.config.ts ...).

- id: ploeh-s2s-bridge-tomorrow
  title: Ingest apparently's signed risk vectors + trigger specs + trigger proofs
  material: yes
  model: sonnet
  depends: []
  proof: "`npx vitest run server/utils/otc/ploeh/__tests__/s2sBridge.test.ts` exits 0 (verifies an apparently-signed PLOEH envelope; rejects tamper/expiry; parses to the shared ploeh schema)"
  prompt: |
    Build the tomorrow side of the HMAC S2S seam (env PLOEH_S2S_SECRET; mirror server/api/apparently/gaming/* +
    _s2s.ts). Routes server/api/apparently/ploeh/{risk-vector,trigger-spec,trigger-proof}.post.ts that verify the
    signature (300s window, constant-time) and ingest into a KV store. Share the Zod payload schema with apparently
    (shared/ploeh schema). Unit-test verify/expiry/tamper/parse. MATERIAL: cross-system trust boundary; risk data +
    proofs only (no funds).

- id: compliance-risk-factor-mapper
  title: Map a ComplianceRiskVector into canonical risk-factor exposures (hedgeable factor class)
  material: no
  model: opus
  depends: [ploeh-s2s-bridge-tomorrow]
  proof: "`npx vitest run server/utils/otc/ploeh/__tests__/factorMapper.test.ts` exits 0 (maps a risk vector to per-(eventType×jurisdiction) 1-sigma $ factor exposures; concentration is additive; expected-loss = Σ probability×severity)"
  prompt: |
    Make compliance risk a first-class factor, like loanRiskMapper.ts makes a loan one. Pure
    server/utils/otc/ploeh/complianceRiskMapper.ts: mapRiskVectorToExposures(vector) → ComplianceFactorExposure[]
    keyed by (eventType × jurisdiction), each a 1-sigma $ exposure derived from probability × severity, with
    correlation tags (e.g. a CA statutory_ban correlates with NY/other modeled-after states — from the diffusion
    structure) so the book can net. Also expectedLoss(vector). Pure, fp-exact, unit-tested (additivity +
    EL). No DB/AI.

- id: perpetual-compliance-hedge-instrument
  title: Perpetual parametric compliance swap (fires on trigger; funding re-fractionalization; no maturity)
  material: yes
  model: opus
  depends: [compliance-risk-factor-mapper]
  proof: "`npx vitest run server/utils/otc/ploeh/__tests__/instrument.test.ts` exits 0 (payoff fires iff the trigger fired; a risk-vector update re-strikes the funding leg conserving value; instrument has no maturity/roll; parametric-only + ECP guards throw on violation)"
  prompt: |
    The instrument. Build server/utils/otc/ploeh/perpetualComplianceHedge.ts on the hedge-perpetual primitives
    (perpetualLegs.synthesizePerpetualLeg / reStrikePerpetualLeg + perpetualContracts + Risk Studio parametric
    families): a perpetual swap whose payoff fires on a ComplianceTriggerSpec and whose funding rate / notional
    RE-STRIKES each interval from the latest ComplianceRiskVector (probability ↑ as more states adopt ⇒ funding
    re-fractionalizes) — perpetual, no maturity, no roll basis. Enforce assertParametricOnly +
    assertNotStatutoryInsurance + assertEcpCounterparty + the swap-only allowlist (throw on violation, test it).
    Pure structuring core unit-tested for: fires-on-trigger, value-conserving re-strike, no-maturity. MATERIAL:
    new instrument (clearance/structuring only — no live position).

- id: marginal-compliance-pricing
  title: Price the hedge at marginal contribution to the netted cross-client compliance book
  material: yes
  model: opus
  depends: [compliance-risk-factor-mapper]
  proof: "`npx vitest run server/utils/otc/ploeh/__tests__/marginalPricing.test.ts` exits 0 (Σ marginal == portfolio ES fp-exact via Euler; an offsetting/correlated client lowers the new hedge's marginal premium and flags redundant)"
  prompt: |
    The 20-200X pricing move: a client's compliance hedge is priced at its MARGINAL contribution to the whole
    netted book, not standalone. Reuse marginalRisk.ts (Euler allocation, incrementalContribution, redundant flag,
    diversificationBenefit) over the ComplianceFactorExposure book. Build
    server/utils/otc/ploeh/compliancePricing.ts: priceComplianceHedge(book, newHedge) → {premiumBp,
    marginalEs, redundant, diversificationBenefit}. A correlated state-ban event already covered by the book ⇒
    ~0 marginal ⇒ near-0 premium (don't double-charge). Pure, fp-exact, unit-tested (Euler sum == ES; redundancy).

- id: compliance-capacity-netting
  title: Self-funding bilateral capacity — net operators long vs short a compliance event
  material: yes
  model: opus
  depends: [perpetual-compliance-hedge-instrument, marginal-compliance-pricing]
  proof: "`npx vitest run server/utils/otc/ploeh/__tests__/capacityNetting.test.ts` exits 0 (opposite-signed exposures to the same event net ~entirely; network residual << Σ standalone; legs are NAMED bilateral (N6), operator never a counterparty (N8))"
  prompt: |
    Capacity from the other side: a ban helps incumbents and hurts the banned — opposite exposures to the same
    event. Reuse networkNetting.ts + offsetRecruiter.ts + standingFabric to net compliance exposures across clients
    into named bilateral legs, so the book is largely self-funded without external reinsurers. Build
    server/utils/otc/ploeh/complianceCapacity.ts: netComplianceBook(participants) + proposeBilateralOffsets(...)
    + recruitOffsets(residual, candidates). Enforce N6 (named bilateral, no mutualization/pool) + N8 (operator not
    a counterparty). Pure, unit-tested (residual<Σstandalone, opposite-sign nets, named legs). MATERIAL.

- id: ploeh-trigger-settlement
  title: Verified-trigger → instant proven parametric settlement (non-custodial, proof-carrying)
  material: yes
  model: opus
  depends: [perpetual-compliance-hedge-instrument, ploeh-s2s-bridge-tomorrow]
  proof: "`npx vitest run server/utils/otc/ploeh/__tests__/settlement.test.ts` exits 0 (an apparently trigger-proof that verifies offline → computes payoff + mints a settlement proof + emits an instruction REF; an unverified/stale proof → manual_review; no funds move)"
  prompt: |
    On a fired event, settle — provably, instantly, non-custodially. Build
    server/utils/otc/ploeh/complianceSettlement.ts: settleOnTriggerProof(hedge, triggerProofEnvelope) that (a)
    re-verifies apparently's offline proof-of-trigger, (b) computes the parametric payoff, (c) mints a
    settlement proof (reuse synthInsurance/instantSettlement + proofOfFloor pattern), (d) emits a non-custodial
    instruction REF (no custody, no funds movement here). Stale/unverified/divergent proof ⇒ manual_review.
    Idempotent settlement id. Pure core unit-tested (verified→settle+proof, unverified→manual, idempotency,
    no-funds). MATERIAL: authorizes payout instruction.

- id: ploeh-value-attribution
  title: Two-sided value ledger — client premium-saved vs partner ROE/tail reduction
  material: no
  model: sonnet
  depends: [marginal-compliance-pricing]
  proof: "`npx vitest run server/utils/otc/ploeh/__tests__/valueAttribution.test.ts` exits 0 (the split sums to total value fp-exact; pass-through curve tunable; shows premium-vs-traditional-insurance saving)"
  prompt: |
    Quantify why this beats insurance. Reuse valueAttribution.ts: build
    server/utils/otc/ploeh/complianceValueAttribution.ts: attributeComplianceValue({premiumBp, marginalEs,
    traditionalInsuranceQuote, passThroughFraction}) → two-sided ledger (client: premium saved vs a standalone
    compliance-insurance quote; partner: ROE uplift + tail-ES reduction), tunable pass-through curve. Pure,
    fp-exact, unit-tested. This is the board-ready "the netted parametric hedge is Nx cheaper than per-event
    insurance" number.

- id: compliance-book-ils
  title: Securitize the netted compliance-risk book into synthetic tranches (Risk Studio ILS)
  material: yes
  model: opus
  depends: [compliance-capacity-netting]
  proof: "`npx vitest run server/utils/otc/ploeh/__tests__/ils.test.ts` exits 0 (diversified compliance parcels → first-loss/mezz/senior synthetic tranches with diversification benefit; route gated OFF by default flag returns 410)"
  prompt: |
    Turn the aggregate compliance-risk book into capacity. Reuse studio/ils/ilsOriginator.ts: build
    server/utils/otc/ploeh/complianceIls.ts: originateComplianceIls(parcels) packaging diversified parametric
    compliance parcels into first-loss/mezz/senior SYNTHETIC tranches (bilateral ECP, NOT securities), with the
    √n diversification benefit + basis-load spread. Gate behind PLOEH_ILS_ENABLED (counsel, default OFF → route
    410). Pure tranche core unit-tested. MATERIAL + counsel-gated.

OPERATOR:
  - Counsel sign-off (paired with apparently's) that compliance-EVENT parametric swaps are swaps, not insurance, and sit inside SWAP_ONLY_MODE allowlist; confirm ECP-gating on every counterparty; do not enable any live position without it.
  - Provision PLOEH_S2S_SECRET (shared with apparently) in tomorrow prod env + GitHub secrets.
  - PLOEH_ILS_ENABLED stays OFF until counsel clears the tranche structure; capacity/underwriting decision required before any compliance book is reinsured or tranched.
  - Deploy any new PLOEH Prisma models/migrations after a name-check + shadow dry-run (per tomorrow's migration rule); the engines run fail-open on KV until then.
  - Live settlement moves toward real payouts — the instruction-emission path stays ref-only until a scoped, key-backed go-live decision (mirror the Phase-2 rails posture).
