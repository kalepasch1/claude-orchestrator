PROJECT: darwn

# Kernel-side (packages/darwin-kernel, @darwin/kernel/cade) enhancements to the expert
# roster + atemporal reasoning + shared calibration. Keep the kernel PURE: no model
# calls, no DB, no product logic — structure + math + interfaces only. Tests are
# node:test; keep the suite green. Additive only.

- id: global-historical-roster
  title: Expand the persona roster to the best global + historical experts
  material: no
  model: opus
  depends: []
  proof: `cd packages/darwin-kernel && node --test` exits 0
  prompt: |
    Extend the roster typing + selection so the standing roster is not US-centric:
    support experts across all legal systems and disciplines, historical and
    living, worldwide, grounded in PRIMARY sources translated into English with a
    provenance tag on the source + translation. Kernel changes only: add optional
    Persona fields (jurisdiction/tradition, era, sourceLang, translationProvenance)
    and ensure selectPanel/filterByCompetence/clusterFactions handle a large,
    diverse roster without regressions; keep diversity in priorsTag/values, not in
    facts. Add golden tests in the kernel test suite proving panel selection stays
    balanced (no single tradition dominates when competence ties) and that
    provenance fields round-trip. Do NOT put the actual roster data or retrieval in
    the kernel — that stays in the per-app seed scripts/adapters; here we enable it
    structurally. node --test stays green.

- id: atemporal-framework-update
  title: Atemporal reasoning — experts reason across all history and update frameworks
  material: no
  model: opus
  depends: []
  proof: `cd packages/darwin-kernel && node --test` exits 0
  prompt: |
    Strengthen the atemporal-grounding contract (handoff §5.1/§5.2) at the kernel
    level: add a pure "framework-update" operator interface so a persona's
    reasoning method can be applied to TODAY's full fact/authority set — i.e., a
    historical expert reasons in their characteristic method but over all events
    before AND after their era, never leaning on superseded authority. Kernel adds:
    (a) types for a persona's reasoning-signature vs. their dated factual priors
    (separate the two so facts can be refreshed without changing method); (b) a
    pure transform that, given a persona + the current authority set + an
    anachronism/superseded index (passed in, not fetched), yields the updated
    position inputs; (c) a validity hook so an adversary "false-analogy/anachronism
    hunter" can challenge outputs. Add node:test cases proving: superseded-authority
    inputs are downweighted, method is preserved while facts update, and the
    transform is deterministic. Pure kernel only; the index + retrieval are injected
    by adapters. Honest scope note in the code: this models rigorous atemporal
    synthesis, not literal omniscience — every assertion remains citation-bound.

- id: shared-calibration-flywheel
  title: One cross-product reliability store interface (compounding moat)
  material: no
  model: opus
  depends: []
  proof: `cd packages/darwin-kernel && node --test` exits 0
  prompt: |
    Implement the kernel-side shared interface (handoff §6.1) so ALL products'
    calibration write-backs feed the same persona reliability priors: legal
    outcomes (Apparently/smarter) and financial outcomes (Tomorrow/Pareto) update
    one reliability model. Kernel provides the pure interface + math (Bayesian
    reliability update, separate tracking of authority vs. advisor reliability,
    decay, and the read used by selectPanel); persistence stays per-app via an
    injected store port. Add node:test cases covering the update math, authority/
    advisor separation, and that a persona's reliability monotonically reflects a
    stream of correct/incorrect realized outcomes. Pure kernel; no DB. Green suite.

OPERATOR:
  - Live model API keys + budgets are needed only when apps run non-mock determinations against this roster; not a kernel concern.
  - Sourcing/licensing of translated primary-source corpora (global/historical) for the per-app retrieval adapters — data-licensing decision.
