PROJECT: apparently

- id: position-outcome-endpoint
  title: Record realized position outcomes (prevailed) via an endpoint
  material: yes
  model: haiku
  depends: []
  proof: `npm run typecheck` exits 0
  prompt: |
    Add `server/api/legal-opinions/[id]/position-outcome.post.ts`, mirroring the
    existing `server/api/legal-opinions/[id]/alignment-outcome.post.ts`. Body:
    `{ tenability, band, materiallyWeak, benchScore, prevailed }`. Insert a row into
    `position_outcomes` (already defined in migration `515_position_outcomes_and_overrides.sql`).
    Use the service Supabase client, fail-soft (never 500 on an un-migrated env).
    This is what feeds `computeWinRateKPI` / `loadPositionKpi` in
    `server/utils/position-kpi.ts` — today nothing writes realized outcomes, so the
    win-rate KPI is always empty. Do not change the position engine.

- id: recipient-alignment-tests
  title: Unit tests for recipient-alignment + KPI pure logic
  material: no
  model: haiku
  depends: []
  proof: `npx vitest run tests/engines/recipient-alignment.test.ts tests/engines/position-kpi.test.ts` exits 0
  prompt: |
    Add vitest coverage (pure/injected — NO live model) for the modules already
    shipped: `server/engines/legal-bots/recipient-alignment.ts` and
    `server/utils/position-kpi.ts`. Assert: `resolveRecipient` mapping
    (de_chancery / occ_licensing / nv_gcb / cftc_dmo / sec_corpfin / generic
    fallback + specificPerson); `scoreAlignment` concurrence gate (recipientReady
    only when 0 blocking RFI + concurrence≥0.8 + tone≥0.75 + completeness≥0.8);
    `runAlignmentPass` converges+ready with injected reviewer/reviser and
    unresolved-blocking → not-ready; `computeAlignmentKpi` recall/precision/F1 on a
    fixed anticipated-vs-actual set; `heuristicLearnProfile` extracts house verbiage +
    RFI-trigger sentences; and `recommendThresholdAdjustment` guards (n≥30, |Δ|≥0.05,
    lift>0.1). These mirror assertions already verified out-of-tree; port them in.

- id: threshold-auto-apply
  title: Consume the auto-tuned per-forum materially-weak threshold
  material: yes
  model: sonnet
  depends: [position-outcome-endpoint]
  proof: `npm run typecheck` exits 0 AND `npx vitest run tests/engines/position-threshold.test.ts` exits 0
  prompt: |
    The nightly `POST /api/cron/position-threshold-tune` already writes the
    empirically-optimal cutoff into `position_thresholds` (migration 516). Make the
    drafting path CONSUME it. Constraints: do NOT modify `scoreTenability` or
    `evaluateSelfServiceGate` signatures/outputs — they are locked byte-identical
    across apparently/smarter/tomorrow by a golden-vector contract (see apparently
    CLAUDE.md "Position engine v2"). Instead add, in `server/utils/position-kpi.ts`,
    `loadActiveThreshold(supabase, forum): Promise<number>` (latest `applied=true`
    row for the forum, default 0.55), and a thin wrapper
    `gateWithThreshold(tenability, mode, override, threshold)` that recomputes
    `materiallyWeak = tenability<threshold || openCritical>=1` before calling the
    existing gate. Wire `generateLegalOpinionDraft` to fetch the forum threshold and
    pass it. Unit-test the override at 0.55 (unchanged) and a moved threshold.

- id: recipient-selector-ui
  title: Recipient selector in the opinion request/draft UI
  material: no
  model: sonnet
  depends: []
  proof: `npm run typecheck` exits 0
  prompt: |
    Add a recipient picker to the opinion request flow (`app/pages/legal-opinions/request.vue`
    or `app/pages/opinions/submit.vue`): a dropdown of `RECIPIENT_REGISTRY` ids
    (Delaware Court of Chancery, OCC Licensing, NV GCB, CFTC DMO, SEC Corp Fin,
    generic) plus an optional "named individual" text field. Thread the choice as
    `recipientId` / `recipientPerson` into the draft request so it reaches
    `generateLegalOpinionDraft` opts (already supported). The existing
    `PositionPanel.vue` already renders `draft.recipient_alignment`, so no new
    display work — just the input + plumbing.

- id: kpi-dashboard
  title: Ops dashboard page for position + alignment KPIs
  material: no
  model: sonnet
  depends: [position-outcome-endpoint]
  proof: `npm run typecheck` exits 0
  prompt: |
    Add a read-only ops page `app/pages/ops/cade-kpi.vue` that calls
    `GET /api/firm-api/positions/kpi` (win-rate, calibration gap, Brier, lift,
    recommended threshold, override audit) and `GET /api/firm-api/positions/alignment-kpi`
    (recall / precision / F1 / avgSurprises). Render as cards + a small table with
    self-contained scoped styles (match the app design system). No writes.

- id: concurrence-receipt
  title: Signed, immutable concurrence receipt per finalized opinion
  material: yes
  model: sonnet
  depends: [position-outcome-endpoint]
  proof: `npm run typecheck` exits 0 AND `npx vitest run tests/engines/concurrence-receipt.test.ts` exits 0
  prompt: |
    Add migration `518_concurrence_receipts.sql` (default-deny RLS, service-role
    insert only) and `server/utils/concurrence-receipt.ts` exporting
    `makeConcurrenceReceipt(draft)` which freezes `{ weakness_ledger, tenability,
    self_service_gate, recipient_alignment.assessment, recipient id/persona }` and
    signs the canonical JSON with HMAC-SHA256 over `AGENT_SIGNING_SECRET` (same
    pattern as the agent-ledger signing elsewhere). Persist on opinion finalize
    (`indexFinalizedOpinion` path), fail-soft. Unit test: signature determinism +
    that a mutated payload fails verification. Gives the firm a defensible "why this
    was recipient-ready" record.

- id: persona-learner-cron
  title: Nightly decider-corpus persona learner
  material: yes
  model: sonnet
  depends: [recipient-alignment-tests]
  proof: `npm run typecheck` exits 0
  prompt: |
    Turn seeded RecipientProfiles into LEARNED ones from each decider's own writing.
    Add migration `519_learned_recipient_profiles.sql` (default-deny RLS) and
    `POST /api/cron/persona-learn.post.ts` that, per seeded profile in
    `RECIPIENT_REGISTRY`, pulls that decider's documents from `corpus_documents`
    (filter by source/jurisdiction/issuer), runs `learnRecipientProfile(samples, base,
    { augmenter })` with an Anthropic augmenter (use `server/utils/ai.ts` / model
    policy), and upserts the learned profile. Extend `resolveRecipient` to prefer a
    learned profile when one exists (fail-soft to seeded). Keep the pure
    `heuristicLearnProfile` floor.

- id: shared-cade-core-extract
  title: Single source-of-truth for the position+alignment pure core
  material: yes
  model: opus
  depends: [recipient-alignment-tests]
  proof: `npm run typecheck` exits 0 AND `npx vitest run tests/engines/cade-core-golden.test.ts` exits 0
  prompt: |
    The pure core (computeWinRateKPI, auditOverrides, solveMultiParty,
    buildFallbackLadder, routeByTenability, scoreTenability, evaluateSelfServiceGate,
    computeAlignmentKpi, scoreAlignment, resolveRecipient, RECIPIENT_REGISTRY) is
    currently TRIPLICATED byte-identical across apparently / smarter / tomorrow and
    kept in lockstep only by discipline. Extract it into ONE shared source of truth
    (local workspace package OR a generated file synced by a committed golden-vector
    contract test). First deliverable = apparently side only: move the core into
    `shared/cade-core/` and re-export from the existing module paths so nothing
    downstream changes; add `tests/engines/cade-core-golden.test.ts` that pins the
    exact vectors currently used (10/10 outputs). MUST preserve byte-identical
    outputs. High-blast-radius — route to human approval. (smarter/tomorrow adopt the
    shared core in their own project intakes after this lands.)

OPERATOR:
  - Apply migrations 514,515,516,517 (+518,519 once the receipt/persona tasks land) to the live Supabase project (apparently `oosolxvlfyifkhjohdzq`) — needs DB credentials.
  - Set `AGENT_SIGNING_SECRET` in the apparently env before enabling concurrence receipts.
  - Schedule the crons `position-threshold-tune` and `persona-learn` (nightly) in the deploy platform.
