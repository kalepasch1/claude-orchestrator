PROJECT: tomorrow

- id: regime-stressed-correlation
  title: Regime-aware stressed correlation block in the marginal-risk engine
  material: no
  model: opus
  depends: []
  proof: `npx vitest run --config vitest.pure.config.ts --pool=forks server/utils/risk/__tests__/marginalRiskRegime.test.ts` exits 0
  prompt: |
    The marginal-risk engine (server/utils/risk/marginalRisk.ts) currently uses a static
    correlation block (CLASS_CORR + defaultCorrelation). Add an optional regime-aware
    correlation so the network residual is priced under stress (calm/stress/crisis), not just
    calm — credibility-critical for the lending network's tail.
    Steps:
    - Reuse server/utils/otc/riskFabric/regimeCorrelation.ts (classifyRegime / getRegimeCorrelationSet).
      Add a `regimeCorrelation(regime)` CorrelationFn factory to marginalRisk.ts that scales the
      default cross-class correlations toward 1.0 as regime worsens (crisis ⇒ credit/tail/equity
      co-move). Keep existing defaultCorrelation as the 'calm' case (backward compatible).
    - The `corr?: CorrelationFn` arg is already supported by portfolioRisk / marginalContributions /
      incrementalContribution — no signature breaks; just expose the factory.
    - Add server/utils/risk/__tests__/marginalRiskRegime.test.ts: crisis-regime portfolio ES of a
      diversified book ≥ calm-regime ES (correlations rise under stress); calm case equals the
      current defaultCorrelation output exactly (regression lock).
    - Do NOT change networkNetting/swapOptimizer defaults; additive only. Pure, no DB/IO.

- id: network-pricing-stress-toggle
  title: Wire stressed-correlation toggle into network pricing + coverage
  material: no
  model: sonnet
  depends: [regime-stressed-correlation]
  proof: `npx vitest run --config vitest.pure.config.ts --pool=forks server/utils/otc/lending/` exits 0 (all lending tests pass)
  prompt: |
    Let lending network pricing optionally price under a stressed regime using the new
    regimeCorrelation factory.
    Steps:
    - server/utils/otc/lending/networkNetting.ts: add an optional `corr?` passthrough to
      computeNetworkResidual / loanNetworkMarginalFraction / proposeNetworkLegs (default unchanged).
    - server/utils/otc/lending/persistence.ts priceLoanNetwork: accept opts.regime
      ('calm'|'stress'|'crisis') and pass the matching CorrelationFn into the marginalRisk calls.
    - Extend server/api/otc/lending/network/coverage.get.ts and price.post.ts to accept ?regime= /
      body.regime (default 'calm').
    - Add 2 tests to server/utils/otc/lending/__tests__/networkNetting.test.ts: crisis regime raises
      the network residual vs calm; calm is unchanged. Keep all existing lending tests green.

- id: network-marginal-simm
  title: Network-marginal SIMM contribution per loan/policy
  material: no
  model: sonnet
  depends: []
  proof: `npx vitest run --config vitest.pure.config.ts --pool=forks server/utils/otc/lending/__tests__/simmMarginal.test.ts` exits 0
  prompt: |
    Surface the second capital drag (initial margin) at the margin, mirroring marginal-RWA pricing.
    Build server/utils/otc/lending/marginalSimm.ts (pure): given a loan/policy mapping and the
    network book, compute the position's marginal SIMM via existing
    server/utils/otc/unifiedSimmMapper.ts (computeUnifiedSimm) — SIMM(book+pos) − SIMM(book). Expose
    `marginalSimmForPosition(networkBook, pos)` returning { standaloneSimmUsd, marginalSimmUsd,
    nettingCreditUsd }. Reuse the lending UnifiedPosition shapes; PTRRS legs already excluded by the
    mapper. Add server/utils/otc/lending/__tests__/simmMarginal.test.ts: marginal SIMM < standalone
    when the book offsets; super-additivity (sum of standalone ≥ portfolio SIMM). Pure, inject
    positions, no DB/IO.

- id: proof-of-inferiority-quote
  title: Attach incumbent counterfactual (proof-of-inferiority) to the borrower quote
  material: no
  model: sonnet
  depends: []
  proof: `npx vitest run --config vitest.pure.config.ts --pool=forks server/utils/otc/lending/__tests__/counterfactual.test.ts` exits 0
  prompt: |
    Make every quote carry a defensible "vs incumbent per-product hedging" delta.
    Steps:
    - Add server/utils/otc/lending/counterfactual.ts (pure): `incumbentRate(loan, assessment)` =
      advised rate priced on STANDALONE capital + each risk hedged separately at full premium (no
      netting). `counterfactualDelta(advisedNetworkRatePct, incumbentRatePct)` → bp saved.
    - Extend server/utils/otc/lending/originationQuote.ts buildBorrowerQuote to include
      `incumbentRatePct` and `savingsVsIncumbentBp` (optional args; compute only when supplied).
    - Tests in server/utils/otc/lending/__tests__/counterfactual.test.ts: incumbent ≥ advised
      network rate; delta ≥ 0; reduces to advised when no book context. Keep originationQuote's
      existing tests green.

- id: recruiter-capacity-sourcing
  title: Route recruiter ideal-profile to the capacity exchange (sourcing, not just ranking)
  material: no
  model: sonnet
  depends: []
  proof: `npx vitest run --config vitest.pure.config.ts --pool=forks server/utils/otc/lending/__tests__/recruitSourcing.test.ts` exits 0
  prompt: |
    Close the recruiter loop: turn the ideal-counterparty profile into a standing sourcing request
    against the existing capacity substrate.
    Steps:
    - Add server/utils/otc/lending/recruitSourcing.ts (pure): `buildSourcingRequest(idealProfile,
      requestingPartnerId)` → a structured request {factorKey, riskClass, neededSigmaUsd, side} per
      residual factor, mapping to server/utils/otc/capacityExchange types (inspect that module's
      exported request/candidate shapes; do not change them). Advisory only; operatorAffiliate false;
      never solicits.
    - Tests in server/utils/otc/lending/__tests__/recruitSourcing.test.ts: one request per ideal
      factor; side opposite the residual sign; empty residual ⇒ no requests. Pure, no network calls.
    - Do NOT call the live exchange or fabric orchestrator (that wiring is operator/legal-gated).

- id: shapley-settlement-transfers
  title: Shapley-priced internal transfers so absorbers are paid (flywheel incentive)
  material: no
  model: opus
  depends: []
  proof: `npx vitest run --config vitest.pure.config.ts --pool=forks server/utils/otc/lending/__tests__/shapleySettlement.test.ts` exits 0
  prompt: |
    Make joining the network rational for risk-absorbers, not just risk-shedders.
    Build server/utils/otc/lending/shapleySettlement.ts (pure) on top of
    networkNetting.shapleyNettingBenefit: `settlementTransfers(partners)` → named bilateral transfer
    legs where partners benefiting MORE than they contribute pay a fair fee to partners who absorb
    risk, so net-of-transfer benefit tracks each partner's marginal absorption. Sum of transfers == 0
    (zero-sum, fp-exact); every leg named bilateral (N6); operator never a party (N8). Tests in
    server/utils/otc/lending/__tests__/shapleySettlement.test.ts: transfers sum to ~0; an absorber
    receives a positive net transfer; a pure beneficiary pays. Advisory ledger only — no money movement.

- id: lending-assessment-dimension
  title: assessLending self-assessment dimension + coordination bots
  material: no
  model: haiku
  depends: []
  proof: `npx vitest run --config vitest.pure.config.ts --pool=forks server/utils/coordination/__tests__/integrationCoherenceScanner.test.ts` exits 0
  prompt: |
    Register the lending vertical with platform self-monitoring now that 'loan' is a ledger publisher.
    Steps:
    - Add `assessLending()` to server/utils/coordination/assessment-engine.ts and wire into
      runFullAssessment() (check: loans ingested, network publish freshness, ECON_CAPITAL_WEIGHT
      sanity). Use AssessmentFinding/ProposalDraft field names exactly (severity 'info'|'warning'|
      'critical'; proposed_change/title/source_bot).
    - Add bot identities `lending-pricing-bot` + `network-netting-bot` to coordination-client.ts
      BOT_IDENTITIES and ensureBotsRegistered (scripts/self-improvement-runner.mjs), engine_path
      pointed at the lending routes.
    - Ensure server/utils/coordination/__tests__/integrationCoherenceScanner.test.ts still passes
      (EXPECTED_LEDGER_PUBLISHERS already includes 'loan').

- id: lending-ui-enhancement-surfaces
  title: UI surfaces for recruiter / premium / quote+proof
  material: no
  model: sonnet
  depends: [proof-of-inferiority-quote]
  proof: `node sfc-check.cjs pages/app/otc/lending/recruit.vue pages/app/otc/lending/policy.vue pages/app/otc/lending/quote.vue` exits 0
  prompt: |
    Add front-ends for already-built routes. Mirror the dark-theme pattern of
    pages/app/otc/lending/studio.vue. Create:
    - pages/app/otc/lending/recruit.vue → POST /api/otc/lending/recruit (ideal profile + ranked
      offsets table + optional synthetic seller).
    - pages/app/otc/lending/policy.vue → POST /api/otc/lending/policy/price (premium twin; line/peril
      form, marginal-RBC premium build-up).
    - pages/app/otc/lending/quote.vue → POST /api/otc/lending/quote (one borrower rate + breakdown +
      "mint proof" button calling POST /api/otc/lending/proof; show verifyUrl + savingsVsIncumbentBp
      from proof-of-inferiority-quote).
    - Add the three to components/app/AppSidebar.vue under the Bank Hedging section (icons
      Calculator/Radar already imported; reuse or add from lucide). Verify each compiles via
      sfc-check.cjs.

- id: lending-prisma-models
  title: Add Prisma models for loan tables (schema only, name-checked, no deploy)
  material: yes
  model: sonnet
  depends: []
  proof: `npx prisma validate` exits 0 AND `npm run lint:migrations` exits 0
  prompt: |
    Migration prisma/migrations/20260628000000_lending_vertical/ exists (3 tables: loan_applications,
    loan_pricing_recommendations, loan_swap_optimizations; camelCase quoted cols, snake @@map) but NO
    Prisma models were added and it is NOT deployed. Add the matching models to prisma/schema.prisma
    ONLY (do not deploy, do not db push, do not migrate dev):
    LoanApplication→loan_applications, LoanPricingRecommendation→loan_pricing_recommendations,
    LoanSwapOptimization→loan_swap_optimizations. Name-check every column against the migration SQL
    verbatim (TEXT gen_random_uuid() PKs, @db.Decimal, @db.Timestamptz(6), Json fields, snake @@map,
    @@index names = idx_*). FKs to "User" as plain Int? scalars (no back-relation on the large User
    model). Run `prisma validate` + `npm run lint:migrations`. Do NOT run migrate deploy.

OPERATOR:
  - Deploy migration 20260628000000_lending_vertical to prod Supabase (project edetxpcoaiqlqrwyzltw)
    AFTER lending-prisma-models merges — name-check + shadow dry-run, then `prisma migrate deploy` +
    `prisma generate`. Until then the vertical runs on the coordination_tasks KV store (fail-open).
  - Calibrate ECON_CAPITAL_WEIGHT (swapOptimizer.ts), RATE_VOL_BP/CREDIT_VOL_MULT (loanRiskMapper.ts),
    PERIL_VOL/RBC_FACTOR (policyRiskMapper.ts) against real IRRBB / economic-capital / NAIC RBC data —
    needs partner data, not code.
  - Counsel sign-off before any live origination flow consumes /api/otc/lending/quote, before
    executing the cross-partner fabric proposal (network/fabric-run) through executeFabricRun with
    real counterparties, and before settlementTransfers settle any real fee (PTRRS posture / CFTC
    NAL 26-20; money-path + consent gated — out of scope for autonomous merge).
