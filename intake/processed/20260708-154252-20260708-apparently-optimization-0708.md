PROJECT: apparently

# Optimization + remediation batch from 2026-07-08 full-fleet review (licensing/registration,
# owner/controller/employee data collection, memo drafting + RLO review, AI-call hygiene,
# RLS/security, UI/UX, coverage). Every task: verify the cited finding in-repo FIRST; if already
# fixed, note it and do the nearest unfixed adjacent item. Follow CLAUDE.md: RLS for all access
# control, typed Supabase helpers only (no raw .from()), AI via callClaudeStructured + ai-call-logger,
# Zod validation in shared/schemas/, idempotent migrations, pino logger (no console.log).

- id: application-state-machine-guard
  title: Enforce licensing application status transitions server-side
  material: yes
  model: sonnet
  depends: []
  proof: `npm run typecheck` exits 0 AND `npm test` exits 0
  prompt: |
    Application status is only a CHECK constraint (draft|review|approved|submitted — see
    supabase/migrations/001_initial_schema.sql ~lines 88/145/165); nothing prevents draft→submitted
    directly. Add an explicit transition validator engine (server/engines/application-state-validator.ts,
    pure + unit-tested: allowed-transitions map, role requirements per transition) and call it in every
    route that mutates application status. Reject invalid transitions with a structured 4xx. Also freeze
    target_jurisdictions once status leaves 'draft' (changes require reverting to draft), since mid-flight
    jurisdiction changes invalidate the requirements/fees analysis. Tests:
    tests/engines/application-state-transitions.test.ts covering every legal and illegal edge, plus the
    jurisdiction-lock behavior. Idempotent migration only if a helper column is genuinely needed.

- id: pii-encryption-and-portal-rls
  title: Extend PII encryption coverage + tighten key_person_documents RLS
  material: yes
  model: opus
  depends: []
  proof: `npm run check:rls` exits 0 AND `npm test` exits 0 AND `npm run typecheck` exits 0
  prompt: |
    Migration 136_pii_encryption.sql encrypts only ssn/dob/address on key_persons; phone, personal
    email, mother's maiden name, middle name and similar identity fields used in licensing
    applications remain plaintext. Extend the existing encryption pattern (same util that handles
    the current *_encrypted columns) to the remaining sensitive key-person and employee fields, with
    an idempotent migration (IF NOT EXISTS) plus a backfill script in scripts/ (NOT run autonomously —
    document it for the operator). Second: key_person_documents RLS (040_key_person_portal.sql ~line 99)
    scopes by org_id but not by invitation — add a policy so portal reads require the document's
    invitation_id to match the requesting invitation context, preventing cross-invitee leakage inside
    one org. Third: personal vault (151_personal_vault_portability.sql) has no export/delete API —
    add GET export + DELETE endpoints for a user's own vault data (RLS-scoped, audited) to cover
    GDPR/CCPA data-subject requests. Integration tests hit real Supabase per repo convention.

- id: audit-logger-reliability
  title: Make audit logging reliable (no fire-and-forget loss)
  material: no
  model: sonnet
  depends: []
  proof: `npm test` exits 0 AND `npm run typecheck` exits 0
  prompt: |
    server/utils/audit-logger.ts logAuditEvent() (~lines 33-50) fires async and swallows failures, so
    a failed insert silently drops compliance-relevant audit rows. Change to: await the insert on
    compliance-critical paths (PII access, status transitions, opinion publish); on failure, retry with
    bounded exponential backoff; if still failing, write a structured pino error including the full
    event payload so it is recoverable from logs, and increment a counter surfaced via an ops view.
    Do NOT add new infra (no Redis) — keep it in-process and simple. Unit tests mock DB failure and
    assert retry + loud failure. Sweep callers: any audit call guarding a compliance action must not
    proceed if the audit write ultimately fails (fail-closed) — flag such call sites in the PR.

- id: ai-call-hygiene-sweep
  title: Ban hardcoded model IDs in engines, glob-based verify-ai-calls allowlist, default spend caps
  material: no
  model: sonnet
  depends: []
  proof: `npm run check:ai-calls` exits 0 AND `npm run lint` exits 0 AND `npm test` exits 0
  prompt: |
    Three hygiene gaps: 1) ~18 files under server/engines/ (e.g. promo-ai-refiner.ts,
    promo-competitor-scraper.ts, key-person-profile-engine.ts) reference model IDs directly instead of
    AI_MODELS / selectModel(taskType). Replace every hardcoded model string; ensure the existing
    ban-hardcoded-model ESLint rule actually covers server/engines/ (extend rule config if scoped too
    narrowly). 2) scripts/verify-ai-calls.mjs exempts 'structured-batch.ts' via an exact-string Set
    (~lines 39-41) — a rename silently breaks the exemption; switch to resolved-path/glob matching and
    fail loudly when an allowlisted path no longer exists. 3) New orgs have no default AI spend cap
    (cost-governor caps are manual): on org creation insert a tier-based default cap row. Tests for
    each; run the full check suite.

- id: rlo-review-gates
  title: Human-in-loop reviewer checkpoint + citation-confidence floor + model provenance on opinions
  material: yes
  model: sonnet
  depends: []
  proof: `npm test` exits 0 AND `npm run typecheck` exits 0
  prompt: |
    Legal-opinion pipeline (server/engines/legal-opinion-engine.ts, rlo-triple-check-engine.ts):
    1) Drafts can flow to submission without an explicit human reviewer sign-off. Extend the opinion
       state machine: draft → review_requested → reviewed → approved → submitted, where the
       reviewed→approved transition requires a user with reviewer role (RLS/role-checked, not
       app-side only). 2) Triple-check citation verification produces confidence scores
       (~lines 54-71) that gate nothing: enforce a floor — any critical citation below 0.65 blocks
       auto-advance and requires explicit reviewer acknowledgment recorded in the audit log.
    3) The engine hardcodes SONNET_MODEL (~line 9): route through AI_MODELS/selectModel and stamp
       each draft/section with model + version (model_version column, idempotent migration) so
       drift is traceable. Unit tests: role-gated transitions (403 without reviewer), low-confidence
       citation blocks, provenance populated.

- id: rate-limit-and-body-limit-hardening
  title: Alert on rate-limit breaches + systematic body limits on POST routes
  material: no
  model: sonnet
  depends: []
  proof: `npm test` exits 0 AND `npm run lint` exits 0
  prompt: |
    1) server/utils/rate-limiter.ts throws on breach but records nothing — log breaches via pino with
       org + tier + route and persist a row (or counter) so abuse patterns are visible in ops views.
       Keep the in-memory sliding window (serverless restarts are a known limitation; note it in code)
       but make the window state injectable for tests. 2) assertMaxLength/input-limits are applied
       ad-hoc (copilot.post.ts and a few others). Add scripts/check-body-limits.mjs that greps all
       server/api/**/*.post.ts for readBody( without a corresponding limit assertion and wire it as
       npm run check:body-limits; then fix the routes it flags (research/query.post.ts and any other
       unbounded-JSON handlers), using LIMITS constants. Add the check to the existing CI script list.

- id: service-role-least-privilege
  title: Split monolithic service_role FOR ALL policies into least-privilege grants
  material: yes
  model: opus
  depends: []
  proof: `npm run check:rls` exits 0 AND `npm test` exits 0
  prompt: |
    212_rls_remediation.sql gives service_role FOR ALL USING(true) WITH CHECK(true) on ~35 tables —
    a compromised background job can read/write everything. Per CLAUDE.md the intent is append-only
    (INSERT) on ledgers and READ on reference data. Write a new idempotent migration that, for each
    table, replaces the blanket policy with the minimal operations the server code actually uses.
    Inventory usage first (grep typed-client calls per table) and include the inventory table in the
    PR description. Ledger-like tables (ai_call_log, audit/event tables): INSERT + SELECT only.
    Reference/config tables: SELECT only. Keep full access ONLY where a job genuinely updates
    (document each). Run the full integration test suite against real Supabase to prove nothing
    breaks; anything that fails gets an explicit, documented policy rather than blanket true.

- id: dashboard-escaping-and-a11y
  title: Escape %%PLACEHOLDER%% substitutions + WCAG labels on portal intake forms
  material: no
  model: sonnet
  depends: []
  proof: `npm test` exits 0 AND `npm run typecheck` exits 0
  prompt: |
    1) public/ HTML dashboards use %%PLACEHOLDER%% token substitution; substituted values are not
       HTML-escaped, so org-supplied strings (company name etc.) can inject markup. Centralize
       substitution in one server util that (a) validates placeholder keys against an allowlist and
       (b) HTML-escapes values by default (opt-out only for server-authored trusted fragments).
       Unit test with a <script> payload. 2) Portal data-collection forms (app/pages/portal/[token].vue,
       employee/[token].vue) collect owner/controller/employee PII without proper label/aria
       associations — add explicit label-for/aria-label on every input, error text linked via
       aria-describedby, and keyboard-reachable controls. Keep styling unchanged.

OPERATOR:
  - Run the PII backfill script from pii-encryption-and-portal-rls against prod after the migration deploys (script in scripts/, requires service key).
  - Review + approve the service-role policy inventory before that migration is applied to prod.
  - Set default spend-cap tiers (starter/pro amounts) — code will ship with placeholders in config.
  - Nominate at least one user per org with the reviewer role before rlo-review-gates deploys, or drafts will queue at review_requested.
