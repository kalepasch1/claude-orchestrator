PROJECT: tomorrow

# Autonomous business-model-aware DASHBOARD CURATION layer for Tomorrow.
# The pure engine + tests are already built and verified (79 vitest tests green,
# strict tsc clean). Source of truth is a single idempotent installer bundle the
# user holds: outputs/install-curation-layer.sh (regenerates every file under
# server/utils/curation/**, server/api/curation/**, server/api/cron/**,
# composables/useCuratedDashboard.ts, pages/app/curated/index.vue).
# These tasks LAND that code and then wire it into production. Everything below
# is genuinely remaining integration work; the algorithms themselves exist.
# Curation design keys every decision on InstitutionType (== capitalLiberation
# CapitalSegment). Registry-driven: adding a metric/segment is a data edit.
# NOTE: separate from tomorrow-0702.md (ECP) — this is the curation workstream.

- id: curation-layer-land
  title: Land the verified curation-layer modules + tests into the repo
  material: no
  model: sonnet
  depends: []
  proof: `npx vitest run server/utils/curation` exits 0 (79 tests pass)
  prompt: |
    Land the curation layer under server/utils/curation/. If the verified bundle
    outputs/install-curation-layer.sh is present in the worktree (ask the operator
    to drop it in if not), run `BASE=$(pwd) bash outputs/install-curation-layer.sh`
    then verify. Otherwise reconstruct from that bundle's spec. Files:
    server/utils/curation/{types,institutionTypes,metricRegistry,regulatoryMetrics,
    ragStatus,remediationRouter,peerBenchmark,selfTuning,provenance,
    regulatoryRelevance,dashboardCurator,navBlueprint,valueAssembler,thresholdPolicy,
    remediationSimulator,constraintFrontier,filingGenerator,salienceBandit,
    curationDiff}.ts + __tests__/{regulatoryMetrics,institutionAndRegistry,
    scoringAndRouting,dashboardCurator,advanced}.test.ts +
    server/api/curation/dashboard.get.ts + server/api/cron/curation-scan.post.ts +
    composables/useCuratedDashboard.ts + pages/app/curated/index.vue.
    All modules are pure (no Prisma/HTTP) except the api/cron/composable/page.
    InstitutionType reuses ~/server/utils/otc/capitalLiberation/types CapitalSegment.
    Do NOT edit any existing file. Prove with the vitest command; also confirm
    `npx tsc` on the folder is clean.

- id: curation-institutiontype-persist
  title: Persist institutionType column on the tenant + backfill
  material: yes
  model: sonnet
  depends: [curation-layer-land]
  proof: `npx prisma validate` exits 0 AND `npx vitest run server/utils/curation` exits 0
  prompt: |
    The business-model spine currently resolves at request time
    (server/utils/curation/institutionTypes.ts resolveInstitutionType). Add a
    persisted institutionType column (nullable String) plus subType, jurisdictions,
    primaryRegulators to the tenant-bearing model(s) (BankProfile/GamingOperator
    and/or User) via an idempotent migration (ADD COLUMN IF NOT EXISTS). Add a
    backfill script under scripts/ that computes institutionType via
    resolveInstitutionType from existing signals (charter/entityType/naicsCode/
    profile links) and writes it. Update server/api/curation/dashboard.get.ts to
    prefer the persisted value (pass it as the `explicit` signal) and fall back to
    the resolver. Keep RLS default-deny; no data exposure change. Do NOT deploy the
    migration to prod (OPERATOR handles deploy).

- id: curation-live-engines-adapter
  title: Pure buildEngineInputs() mapping live risk engines into the value-assembler
  material: no
  model: sonnet
  depends: [curation-layer-land]
  proof: `npx vitest run server/utils/curation/__tests__/liveEngines.test.ts` exits 0
  prompt: |
    Add a pure helper server/utils/curation/liveEngineInputs.ts exporting
    buildEngineInputs(bundle) that maps outputs of the existing engines into the
    valueAssembler EngineInputs shape: varEngine (server/utils/engines/varEngine.ts:
    varUsd,cvarUsd), stressConsole (server/utils/risk/stressConsole.ts: worst-
    scenario loss), concentrationAnalyzer (server/utils/risk/studio/portfolio/
    concentrationAnalyzer.ts: hhi + top single/counterparty), capitalLiberation
    economics (capitalFreedPerPremium). Accept already-computed engine result
    objects (do NOT call Prisma/HTTP here) so it stays pure and testable. Add
    __tests__/liveEngines.test.ts covering the mapping + capital normalization.
    Then wire server/api/curation/dashboard.get.ts to call the real engines and
    pass their results through buildEngineInputs into assembleValues.

- id: curation-gapfill-reg-inputs
  title: Feed LCR/NSFR/SCR/RBC calculator inputs from bank & insurer profiles
  material: yes
  model: sonnet
  depends: [curation-layer-land]
  proof: `npx vitest run server/utils/curation/__tests__/regInputs.test.ts` exits 0
  prompt: |
    server/utils/curation/regulatoryMetrics.ts already computes LCR/NSFR/Solvency II
    SCR-MCR/NAIC RBC. Add the fields these need (HQLA levels, 30d in/out-flows,
    ASF/RSF, eligible own funds + SCR + MCR, total adjusted capital + ACL RBC) to
    the relevant insurer/bank profile models via an idempotent migration where a
    field is missing. Add a pure server/utils/curation/regInputsFromProfile.ts that
    extracts these into the valueAssembler regulatory-input shape, with a
    __tests__/regInputs.test.ts fixture proving lcr/nsfr/scr/rbc compute for a
    sample bank and a sample insurer. Wire into dashboard.get.ts. RLS unchanged.
    Do NOT deploy the migration (OPERATOR).

- id: curation-nav-embed
  title: Segment-aware sidebar — embed /app/curated via navBlueprint
  material: no
  model: sonnet
  depends: [curation-layer-land]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Replace the binary legal/institutional split in components/app/AppSidebar.vue
    (which uses useFirmPersona) with a segment-aware default driven by
    server/utils/curation/navBlueprint.ts navSectionsFor(institutionType). Always
    surface a "Curated" nav item routing to /app/curated. Fetch the tenant
    institutionType from /api/curation/dashboard (or a light /api/curation/whoami).
    Keep the existing sections; only reorder/gate by the blueprint. Do not break
    the legal persona path. Prove the SFC + types compile.

- id: curation-threshold-persist
  title: Persist per-tenant threshold overrides + wire thresholdPolicy context
  material: yes
  model: sonnet
  depends: [curation-institutiontype-persist]
  proof: `npx vitest run server/utils/curation/__tests__/scoringAndRouting.test.ts` exits 0 AND `npx prisma validate` exits 0
  prompt: |
    Add a CurationThresholdOverride model (tenant id, metricId, greenAt, redAt;
    default-deny RLS, service-role write) via idempotent migration. In
    server/api/curation/dashboard.get.ts build the thresholdPolicy TenantContext
    from persisted values (totalAssets, jurisdictions, riskAppetite) and merge
    overrides, then pass resolveThresholds(defs, ctx) into curateDashboard. Extend
    the existing scoringAndRouting test with an override case proving a stricter
    edge flips status. Do NOT deploy the migration (OPERATOR).

- id: curation-snapshot-diff-alerts
  title: Snapshot store + nightly curationDiff alerts (autonomous scan -> push)
  material: yes
  model: sonnet
  depends: [curation-layer-land]
  proof: `npx vitest run server/utils/curation/__tests__/diffAlerts.test.ts` exits 0 AND `npx prisma validate` exits 0
  prompt: |
    Add a CurationSnapshot model (tenant id, institutionType, posture, panel
    statuses json, createdAt; default-deny RLS) via idempotent migration. Extend
    server/api/cron/curation-scan.post.ts to load yesterday's snapshot, run
    diffCuration (server/utils/curation/curationDiff.ts), write today's snapshot,
    and emit an alert payload for newBreaches/worsened on binding constraints.
    Factor the alert-building into a pure server/utils/curation/diffAlerts.ts with
    __tests__/diffAlerts.test.ts. Register the task in nuxt.config.ts scheduledTasks
    (nightly). No email/Slack send here (that is an OPERATOR item). Do NOT deploy
    the migration (OPERATOR).

- id: curation-frontier-product-deltas
  title: Feed constraintFrontier real action deltas from the Risk-Studio catalog
  material: no
  model: sonnet
  depends: [curation-layer-land]
  proof: `npx vitest run server/utils/curation/__tests__/frontierDeltas.test.ts` exits 0
  prompt: |
    Add server/utils/curation/productDeltas.ts mapping each RISK_STUDIO_PRODUCTS
    entry (server/utils/curation/remediationRouter.ts) to a CandidateAction with a
    metric-delta vector (e.g. srt_compression: +cet1, -lcr; cat_overlay: +rbc,
    +cat_reserve_adequacy). Provide candidateActionsFor(institutionType, values).
    Add __tests__/frontierDeltas.test.ts asserting solveFrontier
    (constraintFrontier.ts) picks the most posture-improving action for a bank in
    CET1 breach and reports consumed constraints. Pure only.

- id: curation-remediation-preview-ui
  title: Quantified remediation preview on the curated page
  material: no
  model: sonnet
  depends: [curation-frontier-product-deltas]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Add a POST /api/curation/simulate.post.ts that takes the tenant values +
    candidateActionsFor(...) and returns solveFrontier results. In
    pages/app/curated/index.vue, on each attention/remediation row show the
    simulateAction preview (projected new value, posture improvement, constraints
    consumed) from the endpoint. Prove types/SFC compile.

- id: curation-filing-pipeline-bind
  title: Bind filingGenerator to the regulatory filing pipeline
  material: yes
  model: sonnet
  depends: [curation-layer-land]
  proof: `npx vitest run server/utils/curation/__tests__/filingBind.test.ts` exits 0
  prompt: |
    Wire server/utils/curation/filingGenerator.ts generateFiling(...) into
    server/utils/compliance/regulatoryFilingPipeline.ts so the regulator view can
    emit a real submission artifact (RC-R / NAIC RBC / Solvency II QRT skeleton
    with citations). Add a pure adapter server/utils/curation/filingBind.ts +
    __tests__/filingBind.test.ts asserting a bank regulator view produces an RC-R
    filing object with all lines citation-backed. No live filing submission here
    (material: examiner-facing artifact).

- id: curation-peer-cohort-service
  title: k-anon peer-benchmark cohort aggregation by segment
  material: yes
  model: sonnet
  depends: [curation-institutiontype-persist]
  proof: `npx vitest run server/utils/curation/__tests__/peerCohort.test.ts` exits 0
  prompt: |
    Add server/utils/curation/peerCohort.ts that builds an anonymized same-segment
    cohort of a metric's values from persisted tenant data and enforces the
    k-anonymity floor (returns nothing below k) before handing to
    peerBenchmark.percentileRank. Provide a service function + a guarded query.
    Add __tests__/peerCohort.test.ts proving the k floor blocks small cohorts and
    the percentile is direction-adjusted. Reads must stay RLS-safe / aggregate-only
    (material: touches cross-tenant data).

- id: curation-engagement-salience-persist
  title: Persist engagement + apply salienceBandit multipliers
  material: yes
  model: sonnet
  depends: [curation-layer-land]
  proof: `npx vitest run server/utils/curation/__tests__/scoringAndRouting.test.ts` exits 0 AND `npx prisma validate` exits 0
  prompt: |
    Add CurationEngagement (tenant, role, metricId, impressions, opens) +
    CurationSalienceFeedback (metricId, acted) models via idempotent migration
    (default-deny RLS). Add endpoints to record impressions/opens and acted/ignored.
    In server/api/curation/dashboard.get.ts load engagement -> selfTuning
    engagementBonus and salience multipliers -> salienceBandit
    updateSalienceMultipliers, and pass into curateDashboard. Extend the existing
    scoringAndRouting test with a bandit-application assertion. Do NOT deploy the
    migration (OPERATOR).

- id: curation-reg-radar-corpus
  title: Per-tenant regulatory radar from the corpus harvesters
  material: no
  model: sonnet
  depends: [curation-layer-land]
  proof: `npx vitest run server/utils/curation/__tests__/regRadar.test.ts` exits 0
  prompt: |
    Replace the hardcoded calendar feed with the corpus harvesters. Add
    server/utils/curation/regRadar.ts that adapts corpus/regulatory items into the
    RegulatoryItem shape and calls relevantRegulatoryItems
    (server/utils/curation/regulatoryRelevance.ts) filtered by the tenant
    institutionType + jurisdictions. Expose via GET /api/curation/reg-radar. Add
    __tests__/regRadar.test.ts with fixture items proving a bank sees Basel items
    (binding) and not EU-only Solvency II items.

- id: curation-coherence-bot
  title: curation-coherence-bot that auto-enqueues registry coverage gaps
  material: yes
  model: opus
  depends: [curation-live-engines-adapter]
  proof: `npx vitest run server/utils/curation/__tests__/coherenceBot.test.ts` exits 0
  prompt: |
    Add server/utils/curation/coherenceBot.ts: a PURE scanner
    scanCoverageGaps(perSegmentCoverage) that, per InstitutionType, compares the
    applicable metric set (metricsForInstitution) against which metrics are actually
    computed and returns structured gap findings (segment, missing metricIds,
    suggested instrumentation goal). Add a thin runner that turns findings into
    enqueueCodeImprovementRequest calls (server/utils/coordination/
    codeImprovementGenerator.ts) with source_bot 'curation-coherence-bot',
    respecting CODE_IMPROVEMENT_ENABLED. Add __tests__/coherenceBot.test.ts on the
    pure scanner (a segment with 0 computed metrics yields a gap; full coverage
    yields none). Do NOT edit assessment-engine/gitAutomation/improvement-applier
    (edit-forbidden self-improvement core). Material: writes to the improvement queue.

- id: curation-posture-invariants
  title: Property-based invariants on the curated posture (safety at scale)
  material: no
  model: sonnet
  depends: [curation-frontier-product-deltas]
  proof: `npx vitest run server/utils/curation/__tests__/invariants.test.ts` exits 0
  prompt: |
    Add __tests__/invariants.test.ts with property-based tests (seeded random
    values over many segments) asserting: (1) curated posture is monotone — making
    any single metric healthier never worsens posture; (2) a binding-constraint
    red always appears in the attention queue and sets posture to red; (3) a
    positive-improvement frontier action never lowers overall posture. Mirror the
    existing C5 invariants pattern (server/utils/otc/verification/invariants.ts).

- id: curation-kpi-module
  title: answered-from-curation north-star KPI module + endpoint
  material: no
  model: sonnet
  depends: [curation-live-engines-adapter]
  proof: `npx vitest run server/utils/curation/__tests__/kpi.test.ts` exits 0
  prompt: |
    Add server/utils/curation/kpi.ts computing the north-star: fraction of tenants
    whose binding constraint is computed AND has a green-path action, plus coverage
    and breach-resolution rates. Pure function over an array of per-tenant curated
    summaries. Expose GET /api/curation/kpi. Add __tests__/kpi.test.ts. NOTE: wiring
    this KPI into the self-improvement prioritization edits
    server/utils/coordination/assessment-engine.ts, which is edit-forbidden for the
    autonomous runner — that hookup is an OPERATOR item, not part of this task.

OPERATOR:
  - Run the Prisma migrations from the material tasks against the Tomorrow prod DB (edetxpcoaiqlqrwyzltw) after human review + shadow dry-run; do NOT let the loop auto-deploy.
  - Set CODE_IMPROVEMENT_ENABLED=true (and any autonomy env) so curation-coherence-bot actually enqueues in prod; confirm dedup + cost caps.
  - Wire the answered-from-curation KPI into assessment-engine prioritization — edits the edit-forbidden self-improvement core; requires a human commit.
  - MATERIALITY-GATED AUTONOMOUS REMEDIATION that EXECUTES hedges / moves money (constraintFrontier -> agent ledger Tier B/C): design sign-off + human approval required; never auto-queue money movement.
  - Examiner read-only regulator PORTAL (RLS-scoped external access) — security + legal review of external data exposure.
  - Regulator-feedback ingestion loop (real examiner RFIs/comments -> registry salience) — needs a data source + legal review.
  - Shared cross-app curation service (apparently / smarter / pareto-2080) with a golden-vector parity contract — cross-repo coordination, out of scope for a single tomorrow intake.
  - Productize k-anon peer percentiles as a sellable benchmark data product — privacy/legal sign-off.
  - Connect email/Slack delivery for the nightly curation diff alerts (secrets/deploy).
