PROJECT: apparently

# CADE super-intelligence SPIKES (20X–500X ideas). Each is a SMALL, self-contained
# NEW module + unit test, additive and OFF by default — nothing is wired into the live
# draft path except by the final flag-gated integration task, which is chained (depends)
# behind the already-queued file-editors (threshold-auto-apply, concurrence-receipt) so
# there are NO merge conflicts with the prior batch (apparently-0702.md, already processed).
# The pure modules are CANDIDATE-SHARED with smarter/tomorrow via the shared-cade-core.
# Prod build (nuxt) is the merge gate — every proof runs `npm run build`.

- id: cade-voi-intake
  title: Value-of-information intake — the one missing fact that most moves tenability
  material: no
  model: sonnet
  depends: []
  proof: `npm run build` exits 0 AND `npx vitest run tests/engines/voi-intake.test.ts` exits 0
  prompt: |
    New PURE module `server/engines/legal-bots/voi-intake.ts`. Given a matter's known
    facts + the current WeaknessLedger/TenabilityAssessment (from position-engine.ts),
    rank a supplied set of candidate "unknown facts" by expected tenability lift if
    answered (value of information), and return the single highest-VOI question to ask
    the user BEFORE drafting. Pure + deterministic scoring core (each candidate carries a
    prior probability + a tenability delta if true/false → expected |Δtenability|);
    optional injected AI to propose candidate unknowns from the intake text. Do NOT wire
    into the draft path here (the flag-gated integration task does that). Unit test the
    ranking + tie-breaks. No new deps. Reuse types from position-engine.ts.

- id: cade-decider-twin
  title: Decider digital twin — Bench returns a P(win) distribution, not a point
  material: no
  model: sonnet
  depends: []
  proof: `npm run build` exits 0 AND `npx vitest run tests/engines/decider-twin.test.ts` exits 0
  prompt: |
    New module `server/engines/legal-bots/decider-twin.ts` that WRAPS (does not modify)
    the golden-locked Bench in position-engine.ts. Export `simulateDecider(draftText,
    recipientProfile, { sampler })` returning a distribution over outcomes
    (pWin, p10/p50/p90, likely conditions[], likely dissentThemes[]) via repeated
    injected Bench samples + aggregation. `sampler` is injected so the test is
    deterministic (no live model). Reuse RecipientProfile from recipient-alignment.ts.
    Pure aggregation core is unit-tested; the AI sampler is the production default.
    CANDIDATE-SHARED: decider-twin.

- id: cade-adversary-league
  title: Adversary league — a growing roster of specialized Red-team strategies
  material: no
  model: sonnet
  depends: []
  proof: `npm run build` exits 0 AND `npx vitest run tests/engines/adversary-league.test.ts` exits 0
  prompt: |
    New module `server/engines/legal-bots/adversary-league.ts`: a registry of named Red
    strategies (adverse_authority, factual_gap, jurisdictional_edge, tone_mismatch,
    conclusory_leap), each a `RedTeam` (the injectable type already in position-engine.ts).
    Export `runLeague(draftText, mandate, strategies)` that unions their attacks into one
    WeaknessLedger, and `promoteNewMember(failureCase)` that appends a strategy derived
    from a real miss. Pure orchestration over injected RedTeams (deterministic test);
    the AI-backed strategies are the production default. Reuse WeaknessLedger/RedTeam
    types. CANDIDATE-SHARED: adversary-league.

- id: cade-abstention-routing
  title: Calibrated abstention + specialist routing below a confidence floor
  material: no
  model: sonnet
  depends: []
  proof: `npm run build` exits 0 AND `npx vitest run tests/engines/abstention.test.ts` exits 0
  prompt: |
    New PURE module `server/engines/legal-bots/abstention.ts`. Given a
    TenabilityAssessment + AlignmentAssessment + a confidence estimate, decide
    `{ abstain: boolean, reason, routeTo: specialistTag | null }`. Below a floor (or on
    high uncertainty / low concurrence) it abstains from a self-service answer and routes
    to a specialist tag matched from the matter's vertical/forum (small static map, e.g.
    prediction_markets→cftc_specialist, licensing→charter_specialist). Deterministic +
    unit-tested. Meshes with the existing self-service gate (this is a softer,
    pre-gate signal — does NOT change evaluateSelfServiceGate). CANDIDATE-SHARED: abstention.

- id: cade-faithfulness-audit
  title: Explanation-faithfulness audit — does the rationale entail the conclusion?
  material: no
  model: sonnet
  depends: []
  proof: `npm run build` exits 0 AND `npx vitest run tests/engines/faithfulness-audit.test.ts` exits 0
  prompt: |
    New module `server/engines/legal-bots/faithfulness-audit.ts`: export
    `auditFaithfulness(draft, { checker })` that returns flagged claims where the stated
    rationale does not entail the conclusion (hidden leaps), each with severity. `checker`
    is injected (deterministic test); the AI entailment checker is the production default.
    Returns a structured report that can fold into the WeaknessLedger as `open` items.
    Catches persuasive-but-unsound reasoning the issue-spotting Red team misses.
    CANDIDATE-SHARED: faithfulness-audit.

- id: cade-pareto-drafting
  title: Multi-objective Pareto frontier over aggressiveness × tenability
  material: no
  model: sonnet
  depends: []
  proof: `npm run build` exits 0 AND `npx vitest run tests/engines/pareto-drafting.test.ts` exits 0
  prompt: |
    New PURE module `server/engines/legal-bots/pareto-drafting.ts`: given N candidate
    drafts each with an aggressiveness score + a TenabilityAssessment, compute the
    non-dominated (Pareto-optimal) set and label the knee point, so counsel can pick a
    point on the strength/safety curve per client risk appetite. Pure + unit-tested
    (dominance, knee selection, ties). No model calls. Reuse TenabilityAssessment.
    CANDIDATE-SHARED: pareto-drafting.

- id: cade-drafts-as-precedent
  title: Index accepted opinions as firm precedent on finalize (drafts-as-precedent moat)
  material: yes
  model: sonnet
  depends: [concurrence-receipt]
  proof: `npm run build` exits 0
  prompt: |
    Wire the drafts-as-precedent flywheel using the EXISTING helper
    `indexWorkProductPrecedent` in `server/utils/corpus-precedent.ts` (referenced by
    `indexFinalizedOpinion` in server/engines/legal-opinion-engine.ts). On opinion
    finalize, index the accepted work product as `firm_work_product` precedent tagged
    with recipient id + realized outcome, so future matters retrieve what actually worked
    for THIS decider/issue. Smallest diff; fail-soft; reuse existing code. depends on
    `concurrence-receipt` because BOTH edit the `indexFinalizedOpinion` path — chained to
    avoid a merge conflict. Keep the corpus write behind the existing corpus availability
    guard. This is the compounding moat.

- id: cade-wire-spikes-behind-flags
  title: Integrate the CADE spikes into the draft pipeline behind default-off flags
  material: yes
  model: opus
  depends: [cade-voi-intake, cade-decider-twin, cade-adversary-league, cade-abstention-routing, cade-faithfulness-audit, cade-pareto-drafting, threshold-auto-apply, concurrence-receipt]
  proof: `npm run build` exits 0 AND `npx vitest run tests/engines/position-engine.test.ts` exits 0
  prompt: |
    In `server/engines/legal-opinion-engine.ts` `generateLegalOpinionDraft`, add guarded,
    DEFAULT-OFF integration of the new modules, each behind an env/config flag
    (CADE_VOI, CADE_DECIDER_TWIN, CADE_ADVERSARY_LEAGUE, CADE_ABSTENTION, CADE_FAITHFULNESS,
    CADE_PARETO). When off (default), behavior is byte-identical to today — so the prod
    build + existing position-engine tests stay green. When on: run VOI pre-draft,
    league during the adversarial pass, faithfulness audit folding into the ledger,
    decider-twin distribution attached to draft.representation, abstention as an advisory
    signal, pareto over the posture variants. depends lists every module task PLUS the
    two already-queued file-editors of this file (threshold-auto-apply, concurrence-receipt)
    to serialize edits and avoid merge conflicts. Attach outputs additively to the draft
    object; do not change existing fields.

OPERATOR:
  - Add the CADE_* feature flags to the apparently env (all default off) before enabling any spike in production.
  - Corpus write access for drafts-as-precedent (firm_work_product docs) must be enabled on the corpus store.
