PROJECT: pareto-2080

# Follow-on to pareto-2080-0629.md (the production-wiring file). These are NEW pure-logic engines
# (same pattern as the 27 already in server/utils/: ESM, no deps, seedable RNG, a node:test in
# tests/, attach explain.makeExplanation to any decision/recommendation). They take their inputs as
# arguments (like marketGraph does) — NO DB dependency; the DB feed is the prior queued file. Proof
# for each = its node:test passes. Do NOT regress the suite beyond the 2 known pre-existing failures
# (tests/agentLedger.test.js, tests/bookingSaga.test.js — local Prisma-binary mismatch only).
#
# Governing invariants (never break): free (no fee/affiliate/data-sale); non-discretionary by default
# (prepare, human commits); non-custodial; agent-of-the-user; every completed transaction writes back
# to the market graph (recordMarketOutcome.requireWriteback). Reconstructed vendor models and the
# oracle index export are CONSUMER-FACING / INTERNAL ONLY — never sold, never fed back to vendors
# (antitrust). Build sequence: after-tax + doc-intake first (data quality lifts everything), then the
# reconstruction engine + oracle export, then the rest.

- id: after-tax-normalization
  title: Normalize every commoditized product to one after-tax-after-fee-after-reward effective value
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/afterTaxNormalize.test.js` exits 0
  prompt: |
    Add server/utils/afterTaxNormalize.js (pure). The headline rate is not comparable across products:
    a 6% mortgage with deductible interest, a muni yield, an HSA-routed dollar, and a 5% taxable APY
    differ after tax. Provide `afterTaxValue(product, raw, userTaxContext)` that returns one comparable
    scalar (annual after-tax-after-fee-after-reward $ or effective rate) per product in the
    commoditizedProducts registry. Compose the EXISTING engines: taxOptimizer.js, deductionOptimizer.js,
    investmentCosts.js, decumulation.js (import, don't reinvent). Handle: deductibility (mortgage
    interest, SALT caps via deductionOptimizer), tax-advantaged routing (HSA/401k/IRA via taxAdvantaged),
    muni/treasury tax treatment, reward $ as after-tax income vs rebate. Expose `toComparable(productKey,
    raw, ctx)` so marketGraph/reverseAuction can rank on after-tax value. Add tests/afterTaxNormalize.test.js
    covering: deductible-mortgage beats nominal-equal non-deductible; muni vs taxable crossover at a high
    bracket; HSA dollar > taxable dollar; reward-$ net of bracket.

- id: document-intake-ground-truth
  title: Cross-pollinate the Document-Intake Standard to read actual rate/LTV/DTI/fees off user docs
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/intakeExtraction.test.js` exits 0
  prompt: |
    Self-reported segments (creditBand, LTV) are noisy; the market graph is only as good as its inputs.
    Tomorrow and Apparently have a battle-tested Document-Intake Standard (quality-gated extraction,
    verbatim grounding, consensus passes, lowQuality flag, fail-open). Port the PURE extraction core to
    Pareto as server/utils/intakeExtraction.js: given a parsed statement / closing-disclosure / quote
    PDF's text, extract ground-truth fields (actual note rate, APR, LTV, DTI, fees/points, premium) WITH
    a verbatim quote backing each field (never assert a field without a grounding passage) and a quality
    score; fail-open to lowQuality rather than throwing. Map extracted fields into the
    commoditizedProducts segment dims (commoditizedProducts.projectSegment) so observations become
    verified, not claimed. Do NOT build the doc-reading runtime here (that's the operator browser/upload
    runtime) — keep this a pure text->fields extractor. Reference the intake principles in the
    tomorrow/apparently CLAUDE.md ("Document-Intake Standard"). Tests: extracts rate+LTV+DTI from a
    sample statement string with grounding quotes; flags lowQuality on garbled input; never asserts an
    ungrounded field.

- id: pricing-grid-reconstruction
  title: Reconstruct each vendor's pricing function f_vendor(profile, product, time) from observed quotes/closings
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/pricingGridReconstruction.test.js` exits 0
  prompt: |
    Add server/utils/pricingGridReconstruction.js (pure). From a labeled dataset of observations
    {vendorId, productKey, profileFeatures, rate, kind, at} (the same observations recordMarketOutcome
    produces), fit a per-(vendor,product) pricing model and recover its structure. Provide:
      - `fitVendorGrid(observations, {productKey, vendorId})` -> { breakpoints (tier edges per feature
        e.g. FICO/LTV/DTI bands), marginalBpPerFeature, promoWindows, residualStd, n, confidence }.
        Use a transparent, testable method (segmented/piecewise linear regression + detected breakpoints
        via change-point on sorted feature bins; deterministic, seedable — NO opaque ML).
      - `predictQuote(grid, profile, time)` -> { rate, lowerCI, upperCI } (predict a vendor's quote
        BEFORE applying; calibrated uncertainty).
      - `findTierCliffs(grid, profile)` -> ranked smallest profile changes that cross a break, each with
        bpSaved + the lever ("pay down $X to cross 80% LTV -> 30bp").
      - `softSpots(grids[], profile)` -> which vendor's grid is cheapest for THIS exact profile.
      - `detectReprice(gridHistory)` -> flag when a vendor moved overlays/promo/breaks, with bp + when.
    CONSUMER-FACING / INTERNAL ONLY: this exists to route users to cheaper prices (pro-competitive). It
    must NEVER be sold or fed back to vendors. Tests: recovers a known synthetic 3-tier grid's breakpoints
    and marginal bp; predictQuote within CI on held-out points; findTierCliffs returns the correct cross;
    detectReprice flags an injected overlay shift.

- id: oracle-index-export-pareto
  title: Build k-anon + DP attested rate indices from marketGraph for cross-app/oracle export
  material: yes
  model: opus
  depends: [pricing-grid-reconstruction]
  proof: `node --test tests/oracleIndexExport.test.js` exits 0
  prompt: |
    Add server/utils/oracleIndexExport.js (pure). Promote marketGraph from a per-app surface to an
    exportable PRIVATE rate-index producer. Provide `buildIndices(observations, {minCohort, epsilon})`
    that emits, per (productKey, segment) cell: a median/p10 index value, distinctUsers, and a
    differential-privacy-noised public figure — gated by k-anonymity (distinctUsers >= minCohort, floor
    >= 5 for any exportable figure) and an epsilon-DP Laplace mechanism (mirror the privacy approach used
    in tomorrow's privacyBudget/federatedRiskIntel — reimplement minimally here, pure). Where real data
    is thin, optionally fill from pricingGridReconstruction.predictQuote with widened uncertainty (flag
    `synthetic:true`). Output is AGGREGATED ATTESTED INDICES ONLY — never row-level, never personal data.
    Expose `indexManifest()` describing each published index (id, productKey, segment, method, k, epsilon)
    so the Tomorrow side can consume + proof-wrap it. Tests: suppresses sub-k cells; DP noise bounded;
    synthetic fill flagged; manifest lists only k-safe indices. NOTE: actual S2S transport + consent are
    operator items (see OPERATOR); this task is the pure producer only.

- id: causal-rate-lever
  title: Personalized "what moves YOUR rate, in bp and $" from the reconstructed grids
  material: no
  model: sonnet
  depends: [pricing-grid-reconstruction]
  proof: `node --test tests/causalRateLever.test.js` exits 0
  prompt: |
    Add server/utils/causalRateLever.js (pure). Unlike prequalification's generic credit recs, this is
    segment-causal and per-vendor: given a user profile + the reconstructed grids
    (pricingGridReconstruction), rank the levers that move THIS user's price most (utilization, LTV, DTI,
    account age, timing), each quantified in bp and lifetime $ for the specific product/notional, and name
    the vendor+tier the lever unlocks. Compose findTierCliffs + predictQuote. Return ranked
    {lever, bp, lifetimeUsd, vendorId, note}. Tests: ranks the highest-$ lever first; matches a known
    synthetic grid's cliff; lifetime $ scales with notional.

- id: macro-timing
  title: Best WEEK to apply, not just the best provider (macro + quota tape)
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/macroTiming.test.js` exits 0
  prompt: |
    Add server/utils/macroTiming.js (pure). Compose the EXISTING predictionSignals.js (macro regime feed)
    plus calendar effects (treasury-auction cadence, lender end-of-quarter quota windows) to recommend a
    timing action for a rate application: {action:'apply_now'|'wait', expectedMoveBp, horizonDays, reason}.
    e.g. "apply in ~9 days, rates likely soften 15bp" or "apply now — quarter-end, lenders hungry".
    Tests: a soft-rate signal => wait with negative expectedMoveBp; quarter-end => apply_now; output bp is
    finite and reasoned.

- id: incumbent-retention-rfq
  title: Route one retention RFQ to the current provider before staging a switch
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/incumbentRetention.test.js` exits 0
  prompt: |
    Add server/utils/incumbentRetention.js (pure). Before a switch is staged, generate a retention RFQ to
    the user's CURRENT provider (retention desks carry hidden match budgets; keeping the incumbent has
    zero switching cost). Compose standingAuction incumbent logic + reverseAuction's "beat this" anchor.
    Provide `buildRetentionAsk(incumbent, bestChallenger, switchingCostUsd)` -> {askToMatchUsd,
    netBenefitIfMatched, worthAsking} and `decideRetainVsSwitch(incumbentResponse, bestChallenger,
    switchingCostUsd)` -> 'retain'|'switch' net of switching cost. Tests: incumbent matches within
    threshold => retain; incumbent refuses => switch; switching cost can flip a marginal switch to retain.

- id: provider-honesty-score
  title: Score advertised/quoted-vs-closed gap + bait patterns; down-rank dishonest vendors
  material: no
  model: sonnet
  depends: [pricing-grid-reconstruction]
  proof: `node --test tests/providerHonesty.test.js` exits 0
  prompt: |
    Add server/utils/providerHonesty.js (pure). Per vendor, measure the systematic gap between
    advertised/quoted rates and ACTUALLY CLOSED rates (and quote-then-bait: quote improves then worsens at
    closing). A vendor whose quotes diverge from what their closing-calibrated reconstructed grid predicts
    is baiting. Provide `scoreHonesty(observations, grid)` -> {honestyScore 0..1, advertisedVsClosedBp,
    baitRate, badge}. Feed the score into rfqTargeting as a down-rank multiplier (export a helper
    `honestyPenalty(score)`). Tests: a vendor that closes at the quote scores high; a systematic
    quote-better-close-worse vendor scores low + flags bait.

- id: uncertainty-banded-rate
  title: Express bestAchievable as a confidence band and gate autonomy on band tightness
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/uncertaintyBandedRate.test.js` exits 0
  prompt: |
    Add server/utils/uncertaintyBandedRate.js (pure). Compose the EXISTING uncertainty.js with
    marketGraph to express achievable rate as a distribution + confidence band ("likely 5.92-6.01%, 80%
    confidence") instead of a point. Provide `bandedAchievable(cell|observations, {confidence})` ->
    {point, lower, upper, confidence, tight:boolean}. Provide `autonomyGate(band, maxBandBp)` -> boolean
    that only permits hands-free auto-commit when the band is tighter than maxBandBp (wire-ready for
    autonomyPolicy: a wide band => prepare_and_hold). Tests: wide observations => wide band => gate false;
    tight => gate true; confidence raises width.

- id: continuous-credit-optimization
  title: Standing background process that pre-positions the borrower to their best tier
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/continuousCreditOpt.test.js` exits 0
  prompt: |
    Add server/utils/continuousCreditOpt.js (pure). A standing optimizer that schedules
    qualification-improving actions (inquiry batching/rate-shop windows, utilization timing across
    statement dates, oldest-account preservation) so the user is in their best pricing tier the MOMENT
    demand forms. Compose creditScore.js (estimateScore/preApplicationPlan/simulateAction) + forwardDemand
    (align the plan to the earliest upcoming event). Provide `planCreditRunway(profile, upcomingEvents)` ->
    ordered {action, targetDate, estPointGain, unlocksTier?} so the runway lands before the event. Tests:
    utilization action scheduled before the statement cut preceding the earliest event; ordering by
    point-gain; respects a no-new-inquiry window before a big application.

- id: cross-cohort-playbook-transfer
  title: Transfer winning negotiation sequences across structurally-similar cohorts
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/cohortPlaybookTransfer.test.js` exits 0
  prompt: |
    Add server/utils/cohortPlaybookTransfer.js (pure). Compose the EXISTING playbooks.js: instead of
    relearning per cohort, transfer the winning negotiation sequence (script variant + anchor + timing)
    from a source cohort to structurally-similar target cohorts (same productKey, adjacent segment by a
    similarity metric). Provide `similarCohorts(target, library, {minSim})` and
    `transferPlaybook(sourcePlaybook, targetCohort)` -> seeded variant priors for negotiationFanout/
    negotiationBandit so a new cohort starts from proven plays, not cold. Tests: adjacent-segment match
    selected over distant; transfer seeds the bandit with the source's winning variant; dissimilar cohort
    is not transferred.

OPERATOR:
  - Cross-app/oracle export (oracle-index-export-pareto) ships only AGGREGATED, ANONYMIZED, ATTESTED INDICES (k-anon >= 5 + epsilon-DP), never row-level/personal data. Needs counsel sign-off on the cross-entity privacy/consent boundary + consent language covering anonymized-aggregate contribution before any index actually leaves Pareto. The pure producer can be built/tested now; the S2S transport is gated on this.
  - S2S secret PARETO_TOMORROW_S2S_SECRET set in Vercel prod (Pareto) + Tomorrow env (HMAC pattern per the Tomorrow/Apparently _s2s helpers). Assistant cannot enter secrets.
  - Document-intake runtime that actually READS the user's statements/closing docs (browser-session/upload/OCR) is the operator session-rail runtime, not this queued engine — this task builds only the pure text->fields extractor.
  - HARD AVOID (do not implement, do not queue): selling reconstructed vendor pricing or any pricing intel; feeding reconstructed vendor models back to vendors; any fee/affiliate/data-sale. These break the legal keystone and add antitrust/trade-secret exposure.
