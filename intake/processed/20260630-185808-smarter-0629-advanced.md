PROJECT: smarter

# Wave 2 — negotiation-outcome moat + corpus-grounded generation.
# Depends conceptually on smarter-0629.md (deploy-search-fns, research-endpoint). Handoff: corpus/README.md,
# corpus/reference/contract-playbooks.json. Recall tables live on Supabase olaxnyrzoptjcntrrjgn.

- id: outcome-weighted-precedent
  title: Outcome-weighted clause precedent (use the language that wins)
  material: yes
  model: opus
  depends: []
  proof: `npx vitest run server/utils/__tests__/outcome-rank.test.ts` exits 0
  prompt: |
    The deepest moat: rank clause precedent by how it correlated with WON / favorably-closed negotiations, not
    just text match. Migration: table clause_outcomes(clause_id text, negotiation_id text, outcome text
    (won|lost|stalled), accepted boolean, closed_at timestamptz). On negotiation close, record per-clause
    outcomes (wire into the existing close/post-mortem path). Add a pure helper server/utils/outcome-rank.ts
    `outcomeBoost(baseRank, stats)` and blend it into the clause-precedent ranking used by the copilot. Unit-test
    that a clause with a high win-rate outranks a textually-equal clause with a poor record. Material (new table
    + write path on close).

- id: corpus-constrained-generation
  title: Generate only attested / enforceable clause language
  material: no
  model: sonnet
  depends: []
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Reduce unenforceable drafts: when the copilot drafts a clause, constrain generation to language attested in
    corpus_clauses (retrieval-augmented: retrieve the closest accepted clauses for the type, instruct the model
    to adapt them rather than invent), and flag any output sentence that has no close corpus support as
    "novel — review". Add a server util that retrieves support passages + computes a support score; wire into the
    draft path, fail-soft. vue-tsc 0 errors.

- id: deadline-extraction
  title: Extract deadlines/effective-dates from authority -> matter calendar
  material: yes
  model: sonnet
  depends: []
  proof: `npx vitest run server/utils/__tests__/deadline-extract.test.ts` exits 0
  prompt: |
    Turn primary-law dates into client action items. Add server/utils/deadline-extract.ts
    `extractDeadlines(text)` (regex + aiCall fallback for comment-period close, effective date, compliance date)
    and persist matches to a matter_deadlines table (migration) keyed to the active matter/contract type, feeding
    the existing checklist/tasks/deadlines UI from corpus/reference/contract-playbooks.json. Unit-test the
    deterministic date extractors. Material (new table + write).

OPERATOR:
  - Approve prod apply of clause_outcomes + matter_deadlines migrations (olaxnyrzoptjcntrrjgn).
  - outcome-weighted-precedent needs real closed-negotiation history to be meaningful — backfill from existing post-mortems if available.
