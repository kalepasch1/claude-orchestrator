PROJECT: beethoven

# Orchestrator-as-coding-terminal (2026-07-08). Goal: the orchestrator web app becomes the single
# place Macey manages ALL coding — composing work, watching execution live, approving, and
# launching ad-hoc agent sessions — replacing direct VS Code / Claude Code / other CLI use.
# Architecture spec already in repo: MULTI-VENDOR-CODING-AGENTS.md (AgentInterface contract:
# initialize/execute/run_tests/remediate_error/ask_orchestrator/commit_changes/rollback). Read it
# first; build to it. All existing invariants hold: confidence gate before merge, approval cards
# for material, outcomes.usd recorded, worktree isolation, bandit learns from outcomes.

- id: agent-adapter-layer
  title: Vendor-agnostic CodingAgent interface + Claude Code adapter (reference implementation)
  material: yes
  model: opus
  depends: []
  proof: `python3 -m pytest runner/tests -q` exits 0
  prompt: |
    Implement the AgentInterface from MULTI-VENDOR-CODING-AGENTS.md as a real abstraction in
    runner/agents/: base class (initialize, execute, run_tests, remediate_error, ask_orchestrator,
    commit_changes, rollback) + a registry (agents/registry.py) mapping vendor ids to adapters with
    capability metadata (supports_mcp, supports_hooks, max_context, local_vs_api, cost_class,
    strengths tags). Refactor the existing Claude Code invocation in runner.py into
    agents/claude_code.py as the reference adapter WITHOUT changing runner behavior (same env vars,
    same worktree flow, same outcome reporting) — this is a refactor with golden-path tests, not a
    rewrite. runner.py selects the agent via the registry (default: claude_code) using a per-task
    'agent' field when present (add to task row read path; column may already exist — check; if
    not, new migration with default 'claude_code'). Tests: registry resolves; claude_code adapter
    passes a full mocked task lifecycle identical to pre-refactor behavior; unknown agent id fails
    closed to claude_code with a logged warning.

- id: vendor-adapters-cli
  title: Adapters for Codex CLI, Gemini CLI, and Ollama/DeepSeek local models
  material: yes
  model: opus
  depends: [agent-adapter-layer]
  proof: `python3 -m pytest runner/tests -q` exits 0
  prompt: |
    Add three adapters to runner/agents/ implementing the CodingAgent interface:
    1) codex.py — OpenAI Codex CLI (headless exec mode in the worktree; capture diff + test output).
    2) gemini.py — Gemini CLI (same pattern: non-interactive prompt mode, worktree cwd).
    3) ollama_local.py — local models via Ollama (deepseek-coder / qwen-coder etc.): a
       plan-edit-test loop driven by the runner (the runner applies edits the model proposes,
       runs tests, feeds failures back) since local models have no agentic CLI.
    Requirements for all: detect binary/daemon availability at startup and register only available
    vendors (fail-soft — a missing CLI never wedges the runner, per repo convention); map each
    vendor's exit states onto the common outcome contract (tests_passed, files_changed, usd —
    local models record usd=0 but tokens counted); respect remediate_error with bounded retries;
    NEVER pass secrets into prompts; per-vendor timeout + kill handling. Populate the registry
    capability metadata honestly per vendor. Unit tests use fake subprocess fixtures (no real CLIs
    in CI): lifecycle per adapter, unavailable-binary path, timeout kill, outcome mapping.

- id: vendor-aware-routing-and-prompts
  title: Extend bandit routing across vendors + per-vendor prompt capability profiles
  material: no
  model: sonnet
  depends: [vendor-adapters-cli]
  proof: `python3 -m pytest runner/tests -q` exits 0
  prompt: |
    1) Routing: extend bandit.py arms from model-only to (vendor, model) pairs, seeded with
       model_router.py heuristics per task kind (e.g. big refactor → claude_code/opus; mechanical
       sweep → cheap vendor; privacy-sensitive → ollama_local). Exclude infra failures from reward
       (decontamination task); throughput-per-dollar objective stays, with local models' $0 cost
       weighted by wall-clock so they don't trivially dominate.
    2) Prompting: create runner/agents/profiles/ — one capability profile per vendor describing
       how to prompt it best (context budget, whether it reads CLAUDE.md itself or needs it inlined,
       tool/MCP availability, diff format expectations, known failure modes). planner.py and the
       task prompt builder consume the profile of the ROUTED vendor so every generated prompt is
       tailored to the executing agent automatically (e.g. inline conventions for ollama_local,
       reference-by-path for claude_code).
    Tests: routing respects availability + task kind; profile injection changes prompt content per
    vendor; cold-start falls back to heuristics.

- id: web-task-composer
  title: Dashboard task composer — natural language → canonical intake preview → queue
  material: no
  model: sonnet
  depends: []
  proof: `cd web && npm run build` exits 0
  prompt: |
    Add a "Compose" page to web/: Macey types a goal in natural language (plus project picker,
    optional vendor/model override, material flag); the server route calls the existing planner
    path (or /api/queue for single tasks) to produce a canonical task block preview (id/title/
    material/model/depends/proof/prompt) shown in an editable form; on confirm it queues via the
    existing POST /api/queue contract (extend to accept the full block incl. proof + agent field —
    keep backward compatible). Include a "batch" mode that renders a full intake-format file
    preview and submits each task. Auth: same session gating as the rest of the dashboard. Keep
    components small and follow existing web/ conventions. Proof is the web build; add a server
    route unit test if a test harness exists in web/ (check first).

- id: web-live-run-console
  title: Live run console — task detail with streaming-ish logs, diff view, actions
  material: no
  model: opus
  depends: [web-task-composer]
  proof: `cd web && npm run build` exits 0
  prompt: |
    Build the execution console page in web/ that replaces watching a terminal: task detail view
    with (a) status timeline (QUEUED→RUNNING→...); (b) log pane polling the runner's reported
    output artifacts (runner already writes .runtime/logs and reports to Supabase — expose what
    exists via a server route; if per-turn log rows aren't persisted yet, add a compact
    task_log_chunks table written by the runner, upsert-based, capped per task); (c) files-changed /
    diff summary from the outcome row; (d) actions: retry, cancel/BLOCK, bump priority, change
    vendor for retry — each via existing state-transition rules (no QUEUED→MERGED shortcuts);
    (e) the approval card inline when the task is material. Polling only (SPEC: no realtime
    streaming requirement). Runner-side change (log chunk writes) must be fail-soft. Tests: runner
    log-chunk writer unit test (python3 -m pytest runner/tests -q must also pass); web build green.

- id: web-session-launcher
  title: Ad-hoc interactive coding sessions on any repo, launched and steered from the dashboard
  material: yes
  model: opus
  depends: [agent-adapter-layer, web-live-run-console]
  proof: `python3 -m pytest runner/tests -q` exits 0 AND `cd web && npm run build` exits 0
  prompt: |
    The "terminal replacement" piece: from the dashboard, start an ad-hoc session (pick project +
    vendor + instruction) that the Mac runner executes immediately in a worktree via the adapter
    layer, with the console page as the interface: Macey can send follow-up instructions
    (ask_orchestrator/steer channel — a sessions + session_messages pair of tables, runner polls
    for new operator messages between agent turns), watch output chunks, and end with
    commit-and-queue-for-integration (normal confidence gate + approval flow) or discard
    (rollback + worktree cleanup). Guardrails: sessions are still worktree-isolated; material
    integration still needs approval; session rows record vendor, usd, outcome like any task
    (outcomes invariant); max concurrent sessions env-capped; abandoned sessions auto-expire and
    roll back. Tests: session lifecycle state machine (pure, injected clock), steer-message
    delivery ordering, expiry rollback, outcome row written.

- id: capability-activation-registry
  title: Vendor capability activation from the dashboard (skills/MCP/hooks per project)
  material: no
  model: sonnet
  depends: [vendor-aware-routing-and-prompts]
  proof: `python3 -m pytest runner/tests -q` exits 0 AND `cd web && npm run build` exits 0
  prompt: |
    Surface and control vendor capabilities from the orchestrator instead of per-machine config:
    a capabilities settings page (web/) backed by a fleet-config-style table (reuse the existing
    fleet_control.py gateway pattern — ORCH_-prefixed keys, no secrets in values) that toggles,
    per project: which vendors are eligible, which Claude Code skills/MCP servers the adapter
    enables for that repo (passed via the adapter's session config), default proof commands, and
    prompt-profile overrides. The runner reads these through the existing config gateway at task
    start. Fail-closed: an unknown/disabled capability key is ignored with a warning, never an
    error. Tests: config round-trip via gateway; adapter receives the toggles; secret-looking
    values rejected at write time (existing safe-key rule).

OPERATOR:
  - Install/authenticate vendor CLIs on the runner Mac as you want them available: `codex` (OpenAI), `gemini` (Google), `ollama pull deepseek-coder-v2` etc. Adapters register only what's present — no action needed for vendors you skip.
  - Set MAX_CONCURRENT_SESSIONS (suggest 2) and per-vendor spend caps in runner .env when the session launcher lands.
  - The dashboard becomes a code-execution surface: confirm Vercel dashboard access stays restricted to your + partner emails (Supabase magic-link allowlist).
