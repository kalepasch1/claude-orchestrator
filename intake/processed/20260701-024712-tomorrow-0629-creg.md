PROJECT: tomorrow

# Self-driving legal opinion — TOMORROW side (STANDARDIZED CEPL hedge market + open/close rebalancing + deep netting).
# Coordinated 3-repo build with smarter-0629-creg.md + apparently-0629-creg.md; builds on the PLOEH intake
# (tomorrow-0629-ploeh.md) already queued — REUSE its factor mapper / perpetual instrument / marginal pricing /
# settlement rather than rebuilding. THE KEY REVISION: replace one-bespoke-hedge-per-contract with an AGGREGATION of
# STANDARDIZED, FUNGIBLE per-primitive perpetual legs ($1 unit each). A contract = a CEPL basket of standardized legs.
# Coverage is maintained by OPENING new legs and CLOSING old — explicitly NO NOVATION (test-enforced). Standardization
# = liquidity: every client's "CA ban" leg is the same instrument, so they pool, net, and price off one curve.
# REUSE: perpetualLegs.ts, perpetualContracts.ts, marginalRisk.ts, otc/lending/networkNetting.ts + offsetRecruiter.ts +
# valueAttribution.ts, Risk Studio oracle/instantSettlement/ilsOriginator, bank/capitalReliefEngine, IOI mesh (B1/B2),
# instrumentAllowlist (SWAP_ONLY_MODE), guardrails assertEcpCounterparty/assertParametricOnly/assertNotStatutoryInsurance.
# BRIGHT LINE (test-enforced): parametric-only; bilateral ECP; swap-only; non-custodial (ref + proof, never custody).
# Perpetual funding re-fractionalization removes temporal/roll basis; reference basis is handled by the standardized
# library + open/close re-weighting + the bespoke residual (apparently side). FIRST STEP: grep for existing scaffolding.
# Pure cores unit-testable (npx vitest run --config vitest.pure.config.ts ...).

- id: cepl-standardized-instrument
  title: Standardized per-primitive perpetual parametric leg ($1 unit, fungible) — replaces bespoke
  material: yes
  model: opus
  depends: []
  proof: "`npx vitest run server/utils/otc/cepl/__tests__/instrument.test.ts` exits 0 (a standardized primitive leg fires on its parametric trigger, re-fractionalizes funding on a probability update with no maturity/roll, and two identical-primitive legs are fungible/nettable; parametric+ECP+swap-only guards throw on violation)"
  prompt: |
    Build server/utils/otc/cepl/standardizedLeg.ts on perpetualLegs/perpetualContracts: a STANDARDIZED perpetual
    parametric leg keyed to one CEPL primitive, $1 payoff unit, funding-rate re-fractionalization from the primitive's
    calibrated probability (perpetual — no maturity, no roll). Fungibility is the point: same primitive_key ⇒ identical,
    nettable instrument. Enforce assertParametricOnly + assertNotStatutoryInsurance + assertEcpCounterparty +
    SWAP_ONLY_MODE (throw + test). Pure structuring core unit-tested (fires-on-trigger, value-conserving re-strike,
    no-maturity, fungibility). Clearance/structuring only — no live position. MATERIAL.

- id: cepl-basket-execution-rebalance
  title: Execute a CREG as a CEPL basket; maintain coverage via open/close (NO novation)
  material: yes
  model: opus
  depends: [cepl-standardized-instrument]
  proof: "`npx vitest run server/utils/otc/cepl/__tests__/rebalance.test.ts` exits 0 (executing a basket opens the weighted standardized legs; a re-weight delta is satisfied by OPENING new legs and CLOSING/offsetting old with overlapping coverage — assert NO novation call anywhere and zero coverage gap)"
  prompt: |
    "Hedge this opinion" = execute the CEPL basket (portfolio of standardized legs). Engine
    server/utils/otc/cepl/basketExecution.ts: executeBasket(basket) opens the weighted legs; rebalance(delta) —
    driven by the memo's re-weight deltas over S2S — maintains continuous coverage by OPENING the new-weight legs
    first, then CLOSING/offsetting the reduced ones (overlap so net coverage never gaps). EXPLICITLY NO NOVATION:
    exposure changes are effected purely by opening/closing standardized positions. Test-enforce: no novation path is
    invoked, and coverage overlap holds through any re-weight (incl. a full regime migration sweeps→prediction-market).
    Pure planner core unit-tested. MATERIAL.

- id: cepl-deep-netting-pricing
  title: Marginal pricing over the dense shared standardized book (+ cross-risk netting)
  material: yes
  model: opus
  depends: [cepl-standardized-instrument]
  proof: "`npx vitest run server/utils/otc/cepl/__tests__/netting.test.ts` exits 0 (Σ marginal == portfolio ES fp-exact via Euler over the standardized book; an offsetting client's leg lowers the new leg's marginal premium; a compliance leg nets against a correlated rate/credit exposure in the unified book)"
  prompt: |
    Standardization makes the book DENSE, so netting is deep and premiums collapse toward the systemic residual.
    Reuse marginalRisk.ts (Euler) + networkNetting.ts + offsetRecruiter.ts over the standardized CEPL book. Engine
    server/utils/otc/cepl/pricing.ts: priceLeg(book, newLeg) → {premiumBp, marginalEs, redundant,
    diversificationBenefit}. ALSO net the compliance factor against the client's OTHER Tomorrow exposures
    (rate/credit/FX) via the unified positions/SIMM/governor so it's priced at true marginal contribution to their
    whole surface. Pure, fp-exact, unit-tested (Euler sum==ES, redundancy, cross-risk net). MATERIAL.

- id: cepl-primitive-liquidity-market
  title: Continuous two-sided market per primitive (fungible legs → real liquidity)
  material: yes
  model: opus
  depends: [cepl-standardized-instrument]
  proof: "`npx vitest run server/utils/otc/cepl/__tests__/market.test.ts` exits 0 (opposite-side standing interests in the same primitive discover each other and pair bilaterally; a primitive with buyers+sellers shows a two-sided indicative curve; posture stays non-price-forming/bilateral IOI, no CLOB)"
  prompt: |
    Because legs are fungible, demand pools into a real market per primitive. Reuse the always-on bilateral IOI mesh
    (B1/B2) — NOT a central order book (keep §2(h)(7) posture: bilateral, non-price-forming, no click-to-execute by
    the operator). Engine server/utils/otc/cepl/market.ts: publishInterest(primitiveKey, side, size),
    discoverPairings(primitiveKey) surfacing opposite-side counterparties, and an indicative (stale, disclaimed)
    two-sided curve per primitive. Incumbent-vs-banned operators naturally take opposite sides of the same "ban"
    primitive. Unit-test pairing + indicative curve + posture guards. MATERIAL.

- id: cepl-tradeable-indices
  title: Tradeable compliance-risk indices (defined baskets of standardized primitives)
  material: yes
  model: sonnet
  depends: [cepl-primitive-liquidity-market]
  proof: "`npx vitest run server/utils/otc/cepl/__tests__/indices.test.ts` exits 0 (an index = a defined weighted basket of primitives; its level = the weighted primitive marks; adding/removing a constituent reprices deterministically)"
  prompt: |
    Now indices are real (like CDX/iTraxx). Engine server/utils/otc/cepl/indices.ts: defineIndex(name, constituents:
    weighted primitive keys), indexLevel(index, marks) = weighted primitive marks, and an index leg = a basket of
    standardized legs. Seed sector indices (e.g. US Sweeps-Casino Ban Risk, Lending True-Lender Risk). Pure, unit-tested
    (level computation, reconstitution). MATERIAL: it's a tradeable benchmark.

- id: cepl-warranty-self-reinsure
  title: "Opinion-error event" as a CEPL primitive (lay off warranty risk into the same market)
  material: yes
  model: sonnet
  depends: [cepl-standardized-instrument]
  proof: "`npx vitest run server/utils/otc/cepl/__tests__/warranty-primitive.test.ts` exits 0 (an opinion-error primitive prices from the platform's realized error rate; the platform can open a leg to offset its own warranty exposure; parametric-only enforced)"
  prompt: |
    The warranty self-reinsures. Define an "apparently_opinion_error" CEPL primitive whose (parametric) trigger is a
    realized, adjudicated opinion-error event and whose probability is the platform's realized error rate (from the
    filing-outcome/warranty engines). Engine server/utils/otc/cepl/warrantyPrimitive.ts lets the platform open a leg
    to lay off its own warranty exposure into the same book. Pure pricing core unit-tested. MATERIAL + coordinate with
    the warranty legal/underwriting gate.

- id: cepl-jit-settlement-prefunding
  title: Diffusion-driven JIT settlement pre-funding (instant payout on fire)
  material: yes
  model: sonnet
  depends: [cepl-basket-execution-rebalance]
  proof: "`npx vitest run server/utils/otc/cepl/__tests__/jit-prefund.test.ts` exits 0 (as a primitive's calibrated probability crosses thresholds, the JIT rail pre-stages settlement liquidity so a fired trigger settles instantly; ref-only, no custody)"
  prompt: |
    Make "the law changed" and "you're paid" happen in the same hour. Reuse the JIT liquidity rail: engine
    server/utils/otc/cepl/jitPrefund.ts pre-stages settlement liquidity as a primitive's calibrated probability
    (from apparently's oracle) crosses thresholds, so settleOnTriggerProof (from the PLOEH settlement task) executes
    instantly. Non-custodial: pre-staging is instruction planning + capacity reservation, not custody. Pure ladder
    core unit-tested. MATERIAL.

- id: cepl-capital-relief-framing
  title: Recognized-hedge capital relief for regulated clients (premium as cost-of-capital reduction)
  material: yes
  model: sonnet
  depends: [cepl-deep-netting-pricing]
  proof: "`npx vitest run server/utils/otc/cepl/__tests__/capital-relief.test.ts` exits 0 (a recognized CEPL hedge computes RWA/capital relief for a bank/insurer client; unrecognized/weak-basis hedges yield zero relief — fail-closed)"
  prompt: |
    For bank/MSB/insurance clients, reframe premium as freed capital. Reuse bank/capitalReliefEngine + the D3 capital-
    recognition (fail-closed, basis-haircut) so a recognized, basis-adequate CEPL hedge computes RWA/capital relief.
    Engine server/utils/otc/cepl/capitalRelief.ts: computeCeplCapitalRelief(hedge, book) → {rwaRelief, capitalFreed,
    capitalFreedPerPremiumDollar}; unrecognized or weak-basis ⇒ zero (fail-closed). Pure core unit-tested. MATERIAL.

- id: cepl-s2s-bridge-tomorrow
  title: Ingest apparently CEPL baskets + re-weight deltas; drive execution/rebalance/pricing
  material: yes
  model: sonnet
  depends: [cepl-basket-execution-rebalance, cepl-deep-netting-pricing]
  proof: "`npx vitest run server/utils/otc/cepl/__tests__/s2s.test.ts` exits 0 (verifies an apparently-signed CEPL basket + re-weight delta; routes to executeBasket/rebalance/price; tamper/expiry rejected)"
  prompt: |
    Tomorrow's hub of the seam (PLOEH_S2S_SECRET). Ingest CEPL baskets + calibrated probabilities + re-weight deltas
    from apparently, and route them to executeBasket / rebalance / priceLeg. Validate against shared/schemas. Unit-test
    verify/route/tamper. MATERIAL: trust boundary; ref-only, no funds.

- id: cepl-ops-surface-tomorrow
  title: Standardized-book dashboard (per-primitive market, basket, netting, indices, capital relief)
  material: no
  model: sonnet
  depends: [cepl-primitive-liquidity-market, cepl-deep-netting-pricing, cepl-tradeable-indices]
  proof: "`npx vue-tsc --noEmit` exits 0 AND the surface renders per-primitive indicative curves, a client's basket + marginal premium, the netted book, indices, and capital relief against real data"
  prompt: |
    Surface the standardized hedge book: per-primitive indicative two-sided curves, a client's CEPL basket + marginal
    premium + diversification benefit, the netted book, sector indices, and capital-relief for regulated clients.
    Read-mostly; reuse existing dashboard scaffolding. Prove vue-tsc clean.

OPERATOR:
  - Counsel sign-off (paired with apparently) that standardized CEPL legs are parametric swaps (not insurance), sit in SWAP_ONLY_MODE, and are ECP-gated; and that the per-primitive bilateral IOI market preserves the §2(h)(7) non-price-forming posture (no CLOB, no operator click-to-execute).
  - Provision PLOEH_S2S_SECRET (shared) + attestation key.
  - Deploy any new Prisma models/migrations after name-check + shadow dry-run; engines run fail-open on KV until then.
  - Capital-relief recognition and warranty-primitive underwriting require counsel + an underwriting decision before represented to clients.
  - Settlement instruction-emission stays ref-only until a scoped, key-backed go-live (mirror Phase-2 rails posture).
  - Approve shared/schemas/{creg,cepl}.ts version jointly with smarter + apparently before dependent tasks build.
