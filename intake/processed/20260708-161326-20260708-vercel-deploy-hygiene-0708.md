PROJECT: darwn

- id: deployfix-darwn-prod-0ms-builds
  title: Fix darwn production deploys failing at 0ms (before build starts)
  material: yes
  model: opus
  depends: []
  proof: `npx nuxi build` exits 0
  prompt: |
    darwn production Vercel deploys have failed for 6+ days with instant (0ms, no-log) errors;
    last good prod deploy is 267 days old. Repo root is fine (package.json + nuxt.config.ts at
    git root, no submodules), so this is config-level. Steps:
    1) Ensure main builds locally: check out main (do NOT touch agent/* worktrees), npm ci,
       npx nuxi build — fix any build breaks found (commit minimal fixes to main).
    2) Create/verify vercel.json at repo root: framework nuxt, buildCommand npm run build, and add
       ignoreCommand skipping orchestrator branches:
       bash -c 'case "$VERCEL_GIT_COMMIT_REF" in agent/*|recover*|rework*) exit 0;; *) exit 1;; esac'
       (agent branches are now pushed to origin fleet-wide for the two-Mac branch-share fix; without
       this every agent push burns a Vercel preview build.)
    3) Check .vercel/ linkage and NODE_OPTIONS heap (fleet convention: --max-old-space-size=6144,
       never 7168+ on standard builders).
    4) Note in the PR anything only the operator can fix in the Vercel dashboard (root directory
       setting, env vars) — list exact setting names and values.

PROJECT: tomorrow

- id: deployfix-vercel-ignore-agent-branches-tomorrow
  title: Skip Vercel builds for agent/recovery branches (vercel.json ignoreCommand)
  material: no
  model: haiku
  depends: []
  proof: `node -e "JSON.parse(require('fs').readFileSync('vercel.json'))"` exits 0
  prompt: |
    Add to vercel.json (create if absent, preserve all existing keys including crons):
    "ignoreCommand": "bash -c 'case \"$VERCEL_GIT_COMMIT_REF\" in agent/*|recover*|rework*) exit 0;; *) exit 1;; esac'"
    Reason: the orchestrator now pushes agent/* branches to origin (two-Mac branch-share fix);
    without this, every agent branch push triggers a wasted Vercel preview build and can starve
    production build slots. exit 0 = skip build. Verify JSON stays valid and no other keys change.

PROJECT: apparently

- id: deployfix-vercel-ignore-agent-branches-apparently
  title: Skip Vercel builds for agent/recovery branches (vercel.json ignoreCommand)
  material: no
  model: haiku
  depends: []
  proof: `node -e "JSON.parse(require('fs').readFileSync('vercel.json'))"` exits 0
  prompt: |
    Add to vercel.json (create if absent, preserve all existing keys including crons):
    "ignoreCommand": "bash -c 'case \"$VERCEL_GIT_COMMIT_REF\" in agent/*|recover*|rework*) exit 0;; *) exit 1;; esac'"
    Reason: the orchestrator now pushes agent/* branches to origin (two-Mac branch-share fix);
    without this every agent branch push triggers a wasted Vercel preview build. Keep JSON valid.

PROJECT: smarter

- id: deployfix-vercel-ignore-agent-branches-smarter
  title: Skip Vercel builds for agent/recovery branches (vercel.json ignoreCommand)
  material: no
  model: haiku
  depends: []
  proof: `node -e "JSON.parse(require('fs').readFileSync('vercel.json'))"` exits 0
  prompt: |
    Add to vercel.json (create if absent, preserve all existing keys):
    "ignoreCommand": "bash -c 'case \"$VERCEL_GIT_COMMIT_REF\" in agent/*|recover*|rework*) exit 0;; *) exit 1;; esac'"
    Reason: the orchestrator now pushes agent/* branches to origin (two-Mac branch-share fix);
    without this every agent branch push triggers a wasted Vercel preview build. Keep JSON valid.

PROJECT: pareto-2080

- id: deployfix-vercel-ignore-agent-branches-pareto-2080
  title: Skip Vercel builds for agent/recovery branches (vercel.json ignoreCommand)
  material: no
  model: haiku
  depends: []
  proof: `node -e "JSON.parse(require('fs').readFileSync('vercel.json'))"` exits 0
  prompt: |
    Add to vercel.json (create if absent — NOTE this repo dual-registers crons in vercel.json,
    preserve them exactly):
    "ignoreCommand": "bash -c 'case \"$VERCEL_GIT_COMMIT_REF\" in agent/*|recover*|rework*) exit 0;; *) exit 1;; esac'"
    Reason: agent/* branches are now pushed to origin fleet-wide; skip their preview builds.
    Keep JSON valid and crons untouched.

PROJECT: santas-secret-workshop

- id: deployfix-vercel-ignore-agent-branches-santas-secret-workshop
  title: Skip Vercel builds for agent/recovery branches (vercel.json ignoreCommand)
  material: no
  model: haiku
  depends: []
  proof: `node -e "JSON.parse(require('fs').readFileSync('vercel.json'))"` exits 0
  prompt: |
    Add to vercel.json (create if absent, preserve existing keys):
    "ignoreCommand": "bash -c 'case \"$VERCEL_GIT_COMMIT_REF\" in agent/*|recover*|rework*) exit 0;; *) exit 1;; esac'"
    Reason: agent/* branches are now pushed to origin fleet-wide; skip their preview builds so the
    Expo-web Vercel project doesn't burn build slots. Keep JSON valid.

PROJECT: racefeed

- id: deployfix-vercel-ignore-agent-branches-racefeed
  title: Skip Vercel builds for agent/recovery branches (vercel.json ignoreCommand)
  material: no
  model: haiku
  depends: []
  proof: `node -e "JSON.parse(require('fs').readFileSync('vercel.json'))"` exits 0
  prompt: |
    Add to vercel.json (create if absent, preserve existing keys):
    "ignoreCommand": "bash -c 'case \"$VERCEL_GIT_COMMIT_REF\" in agent/*|recover*|rework*) exit 0;; *) exit 1;; esac'"
    Reason: agent/* branches are now pushed to origin fleet-wide; skip their preview builds.
    Keep JSON valid.

PROJECT: beethoven

- id: deployfix-web-vercel-ignore-agent-branches
  title: Skip Vercel builds for agent/recovery branches on the dashboard project (web/vercel.json)
  material: no
  model: haiku
  depends: []
  proof: `node -e "JSON.parse(require('fs').readFileSync('web/vercel.json'))"` exits 0
  prompt: |
    Add to web/vercel.json (create if absent, preserve existing keys):
    "ignoreCommand": "bash -c 'case \"$VERCEL_GIT_COMMIT_REF\" in agent/*|recover*|rework*) exit 0;; *) exit 1;; esac'"
    Reason: agent/* branches are now pushed to origin fleet-wide (branch-share fix in
    runner/runner.py); skip their preview builds. Keep JSON valid.

- id: deploy-watch-alarm-surfacing
  title: Surface deploy_health ERROR/alarm rows as approval cards + dashboard banner
  material: no
  model: sonnet
  depends: []
  proof: `python3 -m pytest runner/tests -q` exits 0 AND `cd web && npm run build` exits 0
  prompt: |
    deploy_health had darwn in last_deploy_state=ERROR with alarm=true for 6+ days and
    sustainable-barks in ERROR with nobody notified — deploy_watch records state but nothing
    surfaces it. Add: (1) in the runner's periodic deploy_watch pass, when an app's
    last_deploy_state is ERROR for more than a threshold (env ORCH_DEPLOY_RED_ALERT_HOURS,
    default 6), file ONE approval/ops card per app (dedupe by title) and, if the project exists
    in the tasks DB, auto-queue a deployfix-<app>-<date> task (dedupe by slug, reuse the
    existing deployfix conventions so it jumps the queue via the release-fix lane); (2) a red
    banner strip on the web dashboard listing apps currently in deploy ERROR with age. Unit
    tests: threshold gating, dedupe, card payload; web build green.

OPERATOR:
  - sustainable-barks: the About-page video (public/videos/piperComesHome.mov, ~500MB, git-ignored) does not exist on Vercel — build is fixed (runtime binding) but the video 404s in prod. Upload it to Vercel Blob/CDN and swap the URL, or re-encode to a smaller mp4 and commit.
  - darwn: if the deployfix task reports dashboard-only settings (root directory, env vars), apply them in Vercel → darwn → Settings; CLI cannot change them.
  - Gemini CLI is installed but unauthenticated: run `gemini` once in Terminal (Google login) or add GEMINI_API_KEY to runner/.env; codex uses the existing OPENAI_API_KEY.
