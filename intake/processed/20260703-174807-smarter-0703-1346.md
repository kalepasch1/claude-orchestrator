PROJECT: smarter

# Second wave for the omnichannel social marketer. The core + the 12 tasks in the prior
# intake (smarter-0702, now processed: metrics-ingest, loops-ui, voice-learner, x/meta/
# reddit/medium adapters, predictive-timing, revenue-attribution, brand-safety-gate,
# conversation-inbox, campaign-from-goal) are NOT re-listed. Everything here is deliberately
# scoped to NEW files or DB-only migrations so it CANNOT merge-conflict with those tasks —
# none of these edit socialTick.ts, social.ts, loops.post.ts, pages/growth-loops.vue, or
# components/AppShell.vue (owned by the queued 12). Social data plane = orchestrator Supabase
# eatfwdzfurujcuwlhdgj, reached from smarter via server/utils/orch.ts (orchClient). Prod build
# gate = `nuxt build`. See smarter/HANDOFF_SOCIAL_OMNICHANNEL.md (v24/v25).

- id: social-tests-harness
  title: Vitest harness + unit tests for the pure social/CADE logic
  material: no
  model: sonnet
  depends: []
  proof: `cd smarter && npm run build` succeeds AND `cd smarter && npx vitest run` passes with tests for positionEngine.scoreTenability, cade heuristic ledger, and social.ts heuristic content
  prompt: |
    Smarter has no test runner (package.json scripts: dev/build/preview/check:rls) so the
    merge gate is build-only. Add vitest as a devDependency, a `"test": "vitest run"` script,
    and `vitest.config.ts` (node env). Add `tests/` unit tests for the PURE, dependency-free
    logic already in the repo: server/utils/positionEngine.ts `scoreTenability` (band +
    materiallyWeak thresholds), server/utils/cade.ts `heuristicLedger` (via a tiny exported
    seam if needed — smallest diff), and the `heuristic()` content builder in
    server/utils/social.ts (char limits per platform). Do NOT change runtime behavior; if a
    function isn't exported, add a narrow named export only. Keep `npm run build` green.
    CANDIDATE-SHARED: this vitest harness pattern applies to tomorrow/apparently too.

- id: media-render-seam
  title: Render media briefs into actual assets (model seam, own endpoint)
  material: no
  model: sonnet
  depends: []
  proof: `cd smarter && npm run build` succeeds AND POST /api/growth/media with a postId whose meta has a media brief returns {asset:null, status:'seam'} when no image key is set (fail-soft), or an asset URL when configured
  prompt: |
    generateMediaBrief already attaches an image/video brief to post.media (server/utils/social.ts).
    Nothing renders it. Add NEW files only: `server/utils/adapters/media.ts` `renderMedia(brief)`
    (calls an image/video model behind env IMAGE_MODEL_API_KEY; returns {url} or fail-soft
    {asset:null,status:'seam'} when unset) and `server/api/growth/media.post.ts`
    ({ postId }) that reads the post's media[0] brief from eatfwdzfurujcuwlhdgj, renders it,
    and writes the asset back onto growth_social_post.media[0].asset_url. Do NOT edit
    socialTick.ts or social.ts. OPERATOR handles the key. Keep build green.

- id: synthetic-audience-sim
  title: Pre-publish reception predictor (synthetic ICP personas)
  material: no
  model: sonnet
  depends: []
  proof: `cd smarter && npm run build` succeeds AND POST /api/growth/simulate with {platform,text} returns a 0..1 predicted-engagement score + persona notes (heuristic when no API key)
  prompt: |
    CADE scores content QUALITY; this predicts RECEPTION before spending a slot. Add NEW files
    only: `server/utils/audienceSim.ts` `simulateReception({platform, kind, text, icp?})` that
    runs 3-5 LLM ICP personas (server/utils/llm.ts getAnthropicClient) to predict scroll-stop,
    like, and reply likelihood → a blended 0..1 score + one-line persona reactions; deterministic
    heuristic fallback when no key. Add `server/api/growth/simulate.post.ts` exposing it. This is
    RECOMMEND-ONLY (do not auto-filter or wire into social.ts) so it can't conflict with
    predictive-timing/generate. CANDIDATE-SHARED: reusable reception check for any content app.

- id: genconfig-bandit-spike
  title: Bandit that recommends the best generation config per platform (recommend-only)
  material: no
  model: sonnet
  depends: []
  proof: `cd smarter && npm run build` succeeds AND GET /api/growth/gen-config?platform=linkedin_personal returns a recommended {model,temperature,promptVariant} with its historical mean-reward (or a default when no data)
  prompt: |
    The engagement bandit (pick_variant) tunes hooks; extend the IDEA to the generation config
    itself. Add NEW files only: `server/utils/genConfigBandit.ts` (UCB over a small arm set of
    {model, temperature, promptVariant}, reading historical reward from growth_social_post.metrics
    joined by meta.gen_config on eatfwdzfurujcuwlhdgj) and `server/api/growth/gen-config.get.ts`
    that returns the recommended arm per platform. RECOMMEND-ONLY — do NOT wire into social.ts's
    generateContent (that path is owned by other tasks); a later task can consume it. Fail-soft
    default arm when <10 samples. Becomes useful once metrics-ingest (prior intake) is flowing.

- id: crosspost-graph
  title: Cross-venture amplification — winning post seeds sibling accounts across apps
  material: yes
  model: sonnet
  depends: []
  proof: live SQL on eatfwdzfurujcuwlhdgj shows a growth_audience_overlap table + a `crosspost_targets(p_post_id)` function returning sibling accounts across apps; AND `cd smarter && npm run build` succeeds
  prompt: |
    apparently/tomorrow/darwn/personal share one Growth OS — let a win on one venture seed the
    others. Migration to eatfwdzfurujcuwlhdgj (idempotent, RLS default-deny): `growth_audience_overlap
    (app_a, app_b, overlap numeric, updated_at)` + `crosspost_targets(p_post_id)` returning
    connected accounts on OTHER apps whose platform matches (ranked by overlap). Add NEW endpoint
    `server/api/growth/crosspost.post.ts` ({ postId }) that calls crosspost_targets and REUSES the
    existing `atomizeContent` (import from server/utils/social.ts — do not edit it) to stage
    platform-fit derivative drafts on the sibling accounts (status draft, needs_generation). Do
    NOT auto-publish. Reference: team_amplify, atomizeContent, HANDOFF_SOCIAL_OMNICHANNEL.md v25.

- id: social-gap-detector
  title: Nightly gap detector that emits improvement suggestions (self-writing backlog)
  material: yes
  model: sonnet
  depends: []
  proof: live SQL on eatfwdzfurujcuwlhdgj shows a growth_intake_suggestion table populated by `detect_social_gaps()` (e.g. connected account with 0 posts in 7d, scheme with no accounts, drafts stuck needs_generation); AND `cd smarter && npm run build` succeeds
  prompt: |
    Close the loop on the system improving itself. Migration to eatfwdzfurujcuwlhdgj (idempotent,
    RLS default-deny): `growth_intake_suggestion(id, kind, app, ref, detail jsonb, severity, status,
    created_at)` + `detect_social_gaps()` that scans and inserts rows for: connected accounts with
    no posted content in 7 days, active accounts with autonomy='approval' and a backlog of queued
    items, schemes never applied to any connected account, drafts stuck needs_generation>48h,
    accounts in health 'watch/paused', and platforms with zero tracked-link clicks. Add
    `server/api/cron/social-gaps.get.ts` (CRON_SECRET-guarded like the other crons) that runs it
    and returns the fresh suggestions. Do NOT edit vercel.json in a way that drops existing crons —
    if you add a cron entry, ADD to the array (schedule e.g. "0 4 * * *"). These suggestions are
    surfaced to the operator; a beethoven-side consumer that turns high-severity ones into intake
    tasks is noted in OPERATOR (human-gated). CANDIDATE-SHARED: gap→backlog pattern for all apps.

OPERATOR:
  - IMAGE_MODEL_API_KEY (for media-render-seam) — set in smarter env / Vercel to render real image/video assets; unset = briefs only.
  - Review growth_intake_suggestion output before any auto-conversion to intake. RECOMMENDED beethoven follow-up (human-gated, separate intake file): a consumer that reads high-severity growth_intake_suggestion rows and drafts intake/*.md tasks for approval — do NOT let the growth system write directly into the orchestrator intake without a human in the loop.
  - crosspost-graph seeds DRAFTS on sibling company pages (Tomorrow/Darwn/Sustainable Barks); a human should approve cross-posting brand voice across ventures before those accounts go autonomy='auto'.
