PROJECT: tomorrow

- id: ecp-lock-branch
  title: Land the durable ECP bundle in the repo on a branch and stop the loop wiping it
  material: yes
  model: haiku
  depends: []
  proof: `npm run build` exits 0 with server/utils/otc/ecp present AND `npx vitest run server/utils/otc/ecp/__tests__/` passes 23/23 AND `git -C /Users/kpasch/Documents/tomorrow/tomorrow ls-files server/utils/otc/ecp | wc -l` >= 30
  prompt: |
    The complete ECP credentialing subsystem was built but the repo working tree keeps getting reset by
    the tomorrow self-improvement automation, so it survives only in the outputs bundle
    `ecp-module-complete.tar.gz` (mirrored under the session outputs folder `ecp-src/`). Land it:
    1. Extract `ecp-src/` into the repo at: server/utils/otc/ecp/*, server/utils/otc/ecp/__tests__/*,
       server/api/{otc/ecp,ecp-portal,partner/ecp,cron}/*, pages/ecp/*, pages/app/otc/ecp/*.
    2. Re-apply two tracked edits idempotently: add `'eligibility'` to `ProofKind` in
       server/utils/proof/verifiableProof.ts AND to the KINDS array in server/api/proof/index.post.ts;
       add `{ path: '/app/otc/ecp/console', label: 'ECP Credentials', icon: Shield }` to
       components/app/AppSidebar.vue after the Decisions nav item.
    3. Commit to branch `feat/ecp-credentialing` (never main).
    4. Exclude server/utils/otc/ecp/**, server/api/*ecp*/**, pages/ecp/**, pages/app/otc/ecp/** from the
       self-improvement loop's working-tree reset (scripts/self-improvement-runner.mjs FORBIDDEN/reset
       path list) so it stops wiping this. Smallest diff; reuse existing patterns.

- id: ecp-prodbuild-green
  title: Confirm the ECP subsystem keeps the Nuxt production build green in-repo
  material: no
  model: sonnet
  depends: [ecp-lock-branch]
  proof: `npm run build` exits 0 AND `npx vitest run server/utils/otc/ecp/__tests__/` passes 23/23
  prompt: |
    The ECP engines + vitest pass, but a full Nuxt PRODUCTION build was never run in-repo. Run
    `npm run build` and fix ONLY ECP-introduced breakages (do not touch unrelated baseline issues).
    Likely culprits: Nuxt auto-import globals in the new server routes (defineEventHandler/readBody/
    getRouterParam/createError/getHeader/getQuery/getRequestHeader), the EcpEvidence/EcpDetermination
    imports, and BigInt literals in zkRange.ts (ensure tsconfig target/lib >= ES2020 or guard). Keep the
    smallest diff. This task is the guard that everything else builds on.

- id: ecp-prisma-persistence
  title: Durable Prisma persistence for ECP credentials/sessions/revocations/audit (DB-primary, KV fallback)
  material: yes
  model: opus
  depends: [ecp-prodbuild-green]
  proof: `npm run build` exits 0 AND `npm run lint:migrations` is clean AND `npx vitest run server/utils/otc/ecp/__tests__/` passes AND a new round-trip test (gated behind RUN_DB_TESTS=1 so it never hits prod) writes+reads an ecp_credential row
  prompt: |
    ECP stores are KV-backed (persistentStore over coordination_tasks). Add real tables for
    examiner-grade, queryable records. Follow tomorrow migration governance in CLAUDE.md EXACTLY
    (name-check every identifier vs schema.prisma; camelCase quoted columns + snake @@map;
    gen_random_uuid() PKs; mirror 20260618100000_bank_hedging_vertical). Tables: ecp_credentials,
    ecp_sessions, ecp_determinations, ecp_revocations, ecp_audit_entries (INSERT-only, hash-chained).
    Add Prisma models; make ecpStore.ts + revocationRegistry.ts + auditLog.ts DB-primary with the existing
    KV as fail-open fallback (mirror creditStore.ts). DO NOT deploy the migration to prod — leave for
    OPERATOR after a shadow dry-run. Edits ecpStore.ts/revocationRegistry.ts/auditLog.ts/schema.prisma.

- id: ecp-recert-batch-cron
  title: Batch recertification sweep cron across all entities
  material: no
  model: haiku
  depends: [ecp-prisma-persistence]
  proof: `npm run build` exits 0 AND the cron is registered in BOTH vercel.json and nuxt.config.ts scheduledTasks AND `npx vitest run server/utils/otc/ecp/__tests__/` passes
  prompt: |
    recertSentinel.ts + /api/cron/ecp-recert require a single entityId. Add a batch sweep enumerating all
    entities with issued credentials (add an enumerate to ecpStore.ts — CHAINED after ecp-prisma-persistence
    since both edit ecpStore.ts), bucket by expiry, alert on expiring_soon (reuse the platform Slack/email
    notify pattern). Dual-register the cron in vercel.json + nuxt.config.ts scheduledTasks, CRON_SECRET-gated.
    Read-only + fail-open. Smallest diff.

- id: ecp-settlement-gate
  title: Enforce the trade-authorization token as a hard gate in the settlement engine
  material: yes
  model: opus
  depends: [ecp-prodbuild-green]
  proof: `npm run build` exits 0 AND `npx vitest run server/utils/otc/settlement/__tests__` passes with a NEW test asserting a bilateral settlement is refused without a valid mutual-eligibility trade-authorization token and allowed with one
  prompt: |
    Wire tradeGate.ts into the Phase-1/2 non-custodial settlement engine (server/utils/otc/settlement/*)
    so a bilateral swap cannot produce a settlement instruction unless a valid mutual-eligibility
    trade-authorization token (tradeGate.authorizeTrade/canOpenTrade) is present and neither party's ECP
    credential is revoked. Makes §2(e) ECP compliance structurally enforced at money-movement, not
    advisory. Keep posture: instruction-ref only, no funds move, fail-closed (missing/invalid ⇒ no
    instruction). Do NOT change SETTLEMENT_SWEEP_ENABLED default (stays OFF). Add unit tests. Edits
    settlement engine files only.

- id: ecp-live-sanctions-seam
  title: Pluggable live OFAC/KYB sanctions provider seam (fail-closed), default = in-memory list
  material: yes
  model: sonnet
  depends: [ecp-prodbuild-green]
  proof: `npm run build` exits 0 AND `npx vitest run server/utils/otc/ecp/__tests__/` passes with a NEW test injecting a mock provider returning a hit and asserting determineAndIssue returns nextStep 'screening_block' and mints no credential
  prompt: |
    screening.ts checks an in-memory denylist. Add a pluggable async provider interface (OfacProvider/
    KybProvider), in-memory default, plus a documented adapter stub for a real feed (OFAC SDN API /
    Chainalysis). Fail-CLOSED: provider error or hit ⇒ block. NO hardcoded keys — env only; list under
    OPERATOR. Wire through determineAndIssue's existing screener option. Edits screening.ts + ecpService.ts
    (CHAINED off ecp-prodbuild-green; note ecpService.ts is shared — no other queued task edits it). Tests
    use a mock provider (no network). CANDIDATE-SHARED: sanctions/KYB provider seam (other apps issuing
    credentials could reuse it).

- id: ecp-eas-onchain-submitter
  title: Real EAS on-chain submitter behind the existing gated dry-run seam (OFF by default)
  material: yes
  model: sonnet
  depends: [ecp-prodbuild-green]
  proof: `npm run build` exits 0 AND `npx vitest run server/utils/otc/ecp/__tests__/` passes with a NEW test asserting the real submitter runs only when ECP_EAS_ENABLED=true AND a signer is configured, else returns the dry-run receipt (submitted:false)
  prompt: |
    easAdapter.ts has a pluggable EcpChainSubmitter with a gated dry-run default. Implement a real
    submitter posting the ECP claim to Ethereum Attestation Service (schema ECP_EAS_SCHEMA, refUID from the
    credential contentHash, revocable) via ethers + EAS SDK. OFF by default: activate only when
    ECP_EAS_ENABLED=true AND a signer key present; never sign/post in tests or the build. NO keys in code —
    env only; list ECP_EAS_ENABLED/EAS_CONTRACT_ADDRESS/EAS_SIGNER_KEY/RPC_URL under OPERATOR. Add the dep
    lazily/optional so `npm run build` stays green without it. Edits easAdapter.ts only. Tests use a mock.

- id: ecp-treasury-reader
  title: Real on-chain TreasuryReader for zero-touch evidence extraction (fail-loud)
  material: no
  model: sonnet
  depends: [ecp-prodbuild-green]
  proof: `npm run build` exits 0 AND `npx vitest run server/utils/otc/ecp/__tests__/` passes with a NEW test injecting a mock reader and asserting evidenceFromOnChainTreasury maps a priced balance into totalAssetsUsd and returns null when unpriceable
  prompt: |
    evidenceExtractor.ts defines TreasuryReader as a seam returning null. Implement a real reader valuing an
    address's holdings in USD (stablecoins + majors via RPC + a price source), behind the seam so pure
    functions stay testable. Fail-LOUD (return null) when it cannot price — never fabricate a balance
    (reserveOracle discipline). NO keys in code; list RPC_URL/PRICE_API under OPERATOR. Optional/lazy deps so
    the build stays green. Edits evidenceExtractor.ts only. Tests use a mock reader.

- id: ecp-coordination-hygiene
  title: Register ECP bots + assessment dimension + coherence-scanner entries
  material: no
  model: sonnet
  depends: [ecp-prodbuild-green]
  proof: `npm run build` exits 0 AND `npx vitest run server/utils/coordination/__tests__/integrationCoherenceScanner.test.ts` passes AND runFullAssessment includes an assessEcpCredentialing dimension covered by a unit test
  prompt: |
    Fold ECP into platform self-monitoring per CLAUDE.md: (1) add ecp-issuance-bot, ecp-recert-sentinel,
    ecp-screening-bot to BOT_IDENTITIES in coordination-client.ts + ensureBotsRegistered in
    self-improvement-runner.mjs; (2) add assessEcpCredentialing to assessment-engine.ts wired into
    runFullAssessment (issuance rate, escalation/screening-block rate, expiry backlog, audit-chain
    integrity) with correct AssessmentFinding/ProposalDraft field names + 0..1 score; (3) add the new ECP
    cron routes to the coherence scanner expected sets so parity stays green. Keep coherence test green.

- id: ecp-shared-verify-package
  title: Extract the offline ECP verify + gate SDK into a reusable in-repo package (spike, flag-free, pure)
  material: no
  model: sonnet
  depends: [ecp-prodbuild-green]
  proof: `npm run build` exits 0 AND a new vitest for the package verifies a signed credential offline (no network) and rejects a tampered one
  prompt: |
    Package the client-side verification (sdk.ts offlineVerifyCredential + buildSdkSnippet +
    buildSolidityModule) into a self-contained folder (e.g. packages/ecp-sdk or server/utils/otc/ecp/sdk/)
    with zero server-only imports, so protocols and your OTHER apps can verify a Tomorrow ECP credential
    with no Tomorrow secret. Pure re-org + a small test; smallest diff; keep the existing routes working by
    re-exporting. CANDIDATE-SHARED: ecp-sdk offline verifier — reusable by apparently / darwn / racefeed and
    external ERC-3643 issuers.

- id: ecp-eligibility-amm
  title: Autonomous eligibility+guarantee market-maker (proposals only, flag OFF) — spike
  material: yes
  model: opus
  depends: [ecp-settlement-gate, ecp-treasury-reader, ecp-live-sanctions-seam]
  proof: `npm run build` exits 0 AND `npx vitest run server/utils/otc/ecp/__tests__/` passes with a NEW test proving one tick end-to-end via injected seams (no DB/network): a mock on-chain candidate + a guarantor offer yields a pre-credentialed eligible entity and a fulfillable guarantor match
  prompt: |
    Build an autonomous "eligibility AMM" agent (new file; pure orchestration + injected seams) that each
    tick: scans candidate on-chain treasuries (evidenceExtractor + TreasuryReader seam) -> computes claimable
    pre-credentials (precredential.computeClaimable) -> matches open guarantor offers to obligor needs
    (guarantorMarketplace.matchOffers/fulfillMatch) -> emits PROPOSED bilateral IOIs into the War Room seam.
    Mandate-bounded + kill-switch (reuse autonomy/constitution gate); NEVER auto-executes a trade or moves
    funds (proposals only). Env flag default OFF so `npm run build` and prod behavior are unchanged. Fully
    testable via injected seams. New file only (no edits to shared files). Composes the whole ECP stack.

- id: ecp-continuous-compliance-feed
  title: Scheduled signed compliance attestation export (regulator push) — spike behind flag
  material: yes
  model: sonnet
  depends: [ecp-coordination-hygiene]
  proof: `npm run build` exits 0 AND a NEW test asserts buildExaminerReport output is wrapped in a signed VerifiableProof and the scheduled task is a no-op when ECP_COMPLIANCE_FEED_ENABLED is unset
  prompt: |
    Turn the examinerExport pull into a scheduled PUSH: a cron (dual-registered, CRON_SECRET-gated) that
    builds the examiner report, wraps it in a C1 signed proof, and (when ECP_COMPLIANCE_FEED_ENABLED=true +
    a destination is configured) posts it to a configured endpoint; otherwise no-op. Reuse examinerExport.ts
    + verifiableProof + the platform notify/webhook pattern. Flag OFF by default so build + prod are
    unchanged. NO endpoint/secret in code — env only, listed under OPERATOR. New cron file + a tiny helper;
    do not edit examinerExport.ts's signature.

OPERATOR:
  - Set prod env for go-live gates (all default OFF; flip only when ready): ECP_S2S_SECRET; ECP_EAS_ENABLED + EAS_CONTRACT_ADDRESS + EAS_SIGNER_KEY + RPC_URL (ecp-eas-onchain-submitter); sanctions provider API key (ecp-live-sanctions-seam); RPC_URL/PRICE_API (ecp-treasury-reader); ECP_COMPLIANCE_FEED_ENABLED + destination (ecp-continuous-compliance-feed).
  - Deploy the ecp Prisma migration (ecp-prisma-persistence) to Supabase after a shadow dry-run + name-check — do NOT let the loop auto-deploy it.
  - Counsel sign-off before enabling: cross-jurisdiction eligibility (EU/UK/AUS rule sets), carrier/consumer intermediation, any live on-chain issuance, and the continuous compliance feed's destination. US-only determination is the current scope.
  - Pause/scope the tomorrow self-improvement automation so it stops resetting the ECP working tree (ecp-lock-branch step 4).
