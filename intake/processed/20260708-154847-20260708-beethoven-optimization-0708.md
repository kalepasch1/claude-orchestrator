PROJECT: beethoven

# From 2026-07-08 fleet review of the orchestrator itself (runner lifecycle, bandit routing,
# confidence gates, capability registry, cross-app learning propagation, intake robustness).
# Verify findings in-repo first. SPEC.md invariants govern: privacy.scrub() on every path writing
# user text to capabilities/knowledge/capability_versions; provenance.record() on publish and
# instantiate; upsert-only DB writes; outcomes.usd before DONE/MERGED; confidence gate before merge.

- id: bandit-outcome-decontamination
  title: Stop bandit + knowledge loops learning from infrastructure failures
  material: no
  model: sonnet
  depends: []
  proof: `python3 -m pytest runner/tests -q` exits 0
  prompt: |
    Two contamination paths corrupt the learning loops: 1) bandit.py treats rate-limit/usage-cap
    failures as model failures, so UCB1 arms get penalized for infrastructure events ("You've hit
    your weekly limit" banners have even leaked into learned CLAUDE.md sections in fleet repos —
    see tomorrow/smarter "Learned from merged work" blocks). Classify outcome failures:
    infra (rate limit, account exhaustion, network) vs model (tests failed, bad diff). Infra
    outcomes are excluded from bandit reward updates and from throughput-per-dollar stats.
    2) feedback.py / the knowledge pipeline ingests raw run output including error banners into
    knowledge/learned sections. Add a scrubber that drops known infra-banner patterns and anything
    that isn't a convention/do-avoid statement before writing knowledge or CLAUDE.md "Learned"
    sections; also add a one-off cleanup that removes existing banner pollution from
    memory/knowledge stores it manages (do NOT edit other repos' CLAUDE.md here — file follow-up
    tasks per project instead, via a generated report). Unit tests: rate-limit outcome → no bandit
    update; banner text → filtered; legit convention → kept.

- id: privacy-scrub-completion
  title: Complete privacy.scrub coverage (domain + contract fields) per SPEC invariant
  material: yes
  model: sonnet
  depends: []
  proof: `python3 -m pytest runner/tests -q` exits 0
  prompt: |
    SPEC.md requires privacy.scrub() on every path that writes user-supplied text to capabilities,
    knowledge, or capability_versions. Review found scrubbing skips the domain and contract fields
    on capability publish/version paths. 1) Route ALL user-text fields (including domain, contract,
    and any metadata blobs) through privacy.scrub() in capability.py publish() and version().
    2) Add a regression test that publishes a capability containing seeded PII (email, phone, name
    patterns) in every field and asserts nothing unscrubbed lands in the DB rows. 3) Add a scan
    utility (runner/privacy_audit.py) that checks existing capabilities/knowledge rows for the
    seeded PII patterns and reports counts — output only, operator decides on remediation.
    Keep provenance.record() calls intact (SPEC invariant).

- id: intake-watcher-robustness
  title: Harden intake_watcher parsing + quarantine malformed drops
  material: no
  model: sonnet
  depends: []
  proof: `python3 -m pytest runner/tests -q` exits 0
  prompt: |
    runner/intake_watcher.py hand-parses the canonical format with no error reporting: a malformed
    block is silently skipped, an unknown project prints and drops tasks, and a file that half-parses
    still moves to processed/ (losing the rest). Improve without changing the canonical format:
    1) Collect parse diagnostics (line numbers, reason) per file; if ANY task block failed to parse
       or referenced an unknown project, move the file to intake/quarantine/ (create it) with a
       .diagnostics.txt sidecar instead of processed/, and ingest nothing from it (all-or-nothing per
       file, so a partial drop can be fixed and re-dropped idempotently).
    2) Validate fields: model ∈ {haiku,sonnet,opus} (else default via model_router with a warning),
       depends slugs must exist within the same file or already in the queue, proof non-empty for
       material tasks.
    3) Keep idempotency (existing-slug skip) and OPERATOR card emission unchanged.
    Unit tests in runner/tests/test_intake_watcher.py: good file ingests; bad model defaults; unknown
    project quarantines whole file; duplicate slug skipped; dangling depends flagged.

- id: meta-optimizer-loops
  title: Implement the unbuilt meta-optimization loops (B1-B3, D2) from PROMPT-meta-optimizer.md
  material: yes
  model: opus
  depends: [bandit-outcome-decontamination]
  proof: `python3 -m pytest runner/tests -q` exits 0
  prompt: |
    PROMPT-meta-optimizer.md specifies meta-loops that are still unimplemented (review found B1-B3
    and D2 missing). Read that file first — it is the spec. Implement in priority order the loops it
    labels B1, B2, B3 and D2, following its own acceptance criteria, with these constraints:
    - Build on the decontaminated outcome data (depends on bandit-outcome-decontamination).
    - Every loop's writes are upsert-based and RLS-safe per SPEC.md; any cross-project task the
      meta-optimizer generates goes through the normal intake format (drop a file into intake/),
      NOT direct DB inserts, so intake validation applies.
    - Material changes the loops propose must surface as approval cards, never auto-applied
      (confidence gate invariant).
    - Each loop gets unit tests with synthetic outcome fixtures proving it improves the metric it
      targets and does nothing on insufficient data (cold start).
    If PROMPT-meta-optimizer.md's scope exceeds one task, implement B-loops fully and stub D2 behind
    a flag with a follow-up OPERATOR note in the PR rather than shipping it half-wired.

- id: spec-drift-reconciliation
  title: Run spec-vs-code reconciliation and fix cheap drift
  material: no
  model: sonnet
  depends: [privacy-scrub-completion]
  proof: `python3 -m pytest runner/tests -q` exits 0
  prompt: |
    SPEC.md says spec.py compares spec to code weekly and files reconciliation tasks. Run that
    comparison now (or implement the check for any invariant it doesn't yet cover): verify each
    SPEC invariant has an enforcing code path + test — upsert-only writes, outcomes.usd before
    DONE/MERGED, confidence gate before pr_integrate merge, provenance on publish/instantiate,
    context_embed provider separation. For each invariant lacking a test, add a cheap regression
    test; for each genuinely violated invariant that is small to fix, fix it in this task; for
    anything large, emit a report file (REPORT-spec-drift-0708.md, repo root) listing the gap and a
    proposed follow-up task block in canonical intake format. Do not rewrite SPEC.md except to fix
    factual staleness, and note any such edit in the PR.

OPERATOR:
  - Rotate the Supabase service key and move runner secrets out of plaintext .env into macOS Keychain (or at minimum chmod 600 + confirm .env is git-ignored); review .env.growth too.
  - Review the privacy_audit.py scan output after privacy-scrub-completion merges and decide on backfill scrubbing of existing capability/knowledge rows.
  - Clean the polluted "Learned from merged work" sections in tomorrow/ and smarter/ CLAUDE.md files (rate-limit banners) — 2-line manual edit per repo, or approve the follow-up tasks the decontamination task generates.
  - Approve API-token rotation cadence for web dashboard tokens (review flagged no rotation).
