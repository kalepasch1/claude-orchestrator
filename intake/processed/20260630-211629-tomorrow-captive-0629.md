PROJECT: tomorrow

- id: proof-backed-captive-structuring
  title: Self-insurance captive structuring funded by proof-lowered risk
  material: yes
  model: opus
  depends: []
  proof: `npx vitest run server/utils/otc/captive/__tests__/captive.test.ts` exits 0
  prompt: |
    Stand up the structuring engine for a captive that self-insures the portfolio at the lower,
    PROVABLE risk level — capturing the spread between a third-party insurer's price and the
    proof-backed risk. Reuse Tomorrow's parametric / Risk-Studio engines to structure the captive's
    own hedges. Pure engine + tests (no live capital movement; characterization-safe).
    Steps:
    1. Add server/utils/otc/captive/captiveStructurer.ts: sizeCaptive({ exposureUsd, baselineLossRate,
       riskReductionFactor }) where riskReductionFactor (0..1) comes from the kernel
       quantifyRiskReduction over the portfolio's assurance dossiers (accept it as an input; default
       0 = no credit). Compute: provableLossRate = baselineLossRate * (1 - riskReductionFactor),
       requiredCaptiveCapital, and the spread vs a supplied marketPremium.
    2. structureCaptiveHedges(captive) -> reuse the existing parametric/Risk-Studio structuring to lay
       off the captive's tail (parametric layers only; assertParametricOnly), returning the hedge legs
       + residual.
    3. Add server/utils/otc/captive/__tests__/captive.test.ts: higher riskReductionFactor lowers
       required capital and widens the captured spread (monotonic); structured hedges cover the tail
       above the captive's retention; a 0 factor yields no proof credit.
    NOTE: structuring/advisory only — no funds move; keep the ECP + parametric bright lines.

OPERATOR:
  - Vendor @darwin/kernel into tomorrow (see DARWIN_KERNEL_ADOPTION.md) so quantifyRiskReduction can feed the captive; until then pass riskReductionFactor explicitly.
  - Captive formation, domicile, and any real capital are legal/actuarial decisions — counsel + actuary sign-off before anything leaves advisory.
