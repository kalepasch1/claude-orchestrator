PROJECT: pareto-2080

# Consumer wiring for Pareto, sequenced AFTER the tomorrow shared contracts pin. Invariants (never break): free /
# non-discretionary (prepare, human commits) / non-custodial / agent-of-user / writeback on completed transactions.
# Conventions: server/utils pure engines + tests/*.test.js (node:test); requireAuth+requireOwnership; parseInt guards;
# `$fetch as any`; pages owning <AppNav/> => definePageMeta({ layout:false }); design tokens per CLAUDE.md. Test gate:
# `npm test` no regress beyond the 2 known failures (agentLedger, bookingSaga). Builds on commitmentLedger from the
# pareto-2080-comms-intel intake.

- id: pareto-cockpit-consume
  title: Surface the shared autonomy-budget + DecisionReceipt on the approvals cockpit
  material: no
  model: sonnet
  depends: []
  proof: `npx nuxt typecheck` exits 0 (or `node --check` of the page's <script> if typecheck unavailable)
  prompt: |
    Extend pages/Approvals/User/[userId].vue to consume the unified Commitment contract + the shared autonomy-budget +
    DecisionReceipt (mirror the contracts locally as the repo does). Render each item as a 95% resolved Outcome + one
    Recommended one-tap commit (POST .../staged-actions/:id/commit, actor:'human' only) + on-demand proof
    (sourceRef + signed receipt + counterfactual ribbon from GET /api/finance/counterfactual). Tokens; `$fetch as any`;
    layout:false; sidebar + command-palette entry. Never auto-commit (invariant 2).

- id: counterfactual-comms
  title: Extend the counterfactual ledger to comms/obligation outcomes
  material: no
  model: sonnet
  depends: []
  proof: `node --check server/utils/counterfactualComms.js` exits 0 AND a new tests/counterfactualComms.test.js passes under `npm test` (no regress beyond the 2 known failures)
  prompt: |
    Add server/utils/counterfactualComms.js — a PURE extension that maps comms/obligation outcomes (obligations
    auto-closed, follow-ups that recovered a provider promise, scams blocked, $ negotiated) into the categories
    server/utils/counterfactualLedger.rollup consumes, so the existing counterfactual surface shows captured vs forgone
    across finance AND comms. node:test fixtures; deterministic; no Prisma import in the engine.

OPERATOR:
  - Pareto<->Smarter S2S to receive CommitmentSyncEvent + emit cockpit items needs PARETO_SMARTER_S2S_SECRET + sibling endpoints (HMAC per _s2s helpers) — wire after the tomorrow federated-commitment contract pins.
  - Any persistence the cockpit consume requires beyond existing tables: `npx prisma migrate deploy` is operator-run (orchestrator must not deploy migrations).
