PROJECT: apparently

# CADE spike FAST-FOLLOWS — each extends a parent module from the prior batches
# (apparently-0702.md + apparently-0703-1347.md, already processed) via `depends`, so
# there is ZERO file/merge conflict: a fast-follow only runs after its parent lands.
# All additive + default-off; prod build (nuxt `npm run build`) is the merge gate.

- id: cade-precedent-outcome-ranking
  title: Rank firm-precedent retrieval by realized outcome × recency × recipient-match
  material: yes
  model: sonnet
  depends: [cade-drafts-as-precedent]
  proof: `npm run build` exits 0 AND `npx vitest run tests/engines/precedent-outcome-ranking.test.ts` exits 0
  prompt: |
    Once `cade-drafts-as-precedent` is indexing accepted work product as firm precedent,
    add a PURE re-ranking scorer `server/engines/legal-bots/precedent-ranking.ts` that
    re-orders retrieved precedent by (realized-outcome weight × recency decay ×
    recipient-id match), on top of the existing semantic similarity. Smallest diff: a pure
    `rankPrecedent(hits, { recipientId })` function + unit test on a fixed hit set. Wire it
    as an optional post-processor in the precedent retrieval path (server/utils/corpus-precedent.ts
    consumer) behind the existing corpus guard. The firm corpus becomes a win-predictor,
    compounding per decider. CANDIDATE-SHARED: precedent-ranking.

- id: cade-voi-intake-ui
  title: Surface the single highest-VOI question in the opinion-request UI
  material: no
  model: sonnet
  depends: [cade-voi-intake, recipient-selector-ui]
  proof: `npm run build` exits 0
  prompt: |
    Reuse `voi-intake.ts` (`server/engines/legal-bots/voi-intake.ts`) to compute the one
    missing fact that most moves tenability, and surface it as a single pre-draft question
    in the opinion-request flow next to the recipient selector added by
    `recipient-selector-ui`. Add a tiny endpoint `GET /api/legal-opinions/voi-question`
    (or fold into the existing estimate/request endpoint) that returns the top question;
    render one input; pass the answer into the draft request facts. Smallest diff, no new
    deps. Edits the same request page as recipient-selector-ui → chained via depends.

- id: cade-twin-calibrated-thresholds
  title: Cold-start the materially-weak threshold from the decider twin's P(win)
  material: yes
  model: sonnet
  depends: [cade-decider-twin, threshold-auto-apply]
  proof: `npm run build` exits 0 AND `npx vitest run tests/engines/twin-threshold.test.ts` exits 0
  prompt: |
    Blend `decider-twin.ts` simulated P(win) into the threshold recommender in
    `server/utils/position-kpi.ts` so a decider with few REALIZED outcomes still gets a
    calibrated per-forum cutoff (simulated + realized, shrinking toward realized as n
    grows). Pure addition: `blendThreshold(realizedKpi, twinSummary)`; do NOT change
    scoreTenability/evaluateSelfServiceGate (golden-locked). Unit test the blend + the
    n→∞ limit equals the realized recommendation. Edits position-kpi.ts which
    threshold-auto-apply also edits → chained via depends.

- id: cade-league-eval-set
  title: Turn the adversary league into a build-gating regression eval bank
  material: no
  model: sonnet
  depends: [cade-adversary-league]
  proof: `npm run build` exits 0 AND `npx vitest run tests/engines/adversary-league-eval.test.ts` exits 0
  prompt: |
    Add `tests/engines/adversary-league-eval.test.ts` that pins a bank of "drafts that must
    survive attack strategy X" using the deterministic (injected) league from
    `adversary-league.ts`. Each known failure mode becomes a regression case: if a future
    prompt/model change lets a strategy land a previously-neutralized attack, the test
    fails. This is the quality ratchet that makes autonomous CADE changes safe to merge.
    Pure/injected — no live model. CANDIDATE-SHARED: league-eval-harness.

- id: cade-abstention-routing-inbox
  title: Route abstained matters to a specialist assignment inbox
  material: yes
  model: sonnet
  depends: [cade-abstention-routing]
  proof: `npm run build` exits 0
  prompt: |
    When `abstention.ts` returns `{ abstain:true, routeTo }`, persist an assignment so the
    matched human specialist is notified. Reuse any existing assignment/approval inbox in
    the app (search server/api for an approvals/assignments handler — the ops UI already
    has an approval queue); if none fits, add a minimal
    `POST /api/legal-opinions/[id]/route-specialist.post.ts` inserting into an
    `assignment` table (default-deny RLS migration) fail-soft. Smallest diff, reuse first.
    Converts "we don't know" into a warm handoff.

- id: cade-faithfulness-in-receipt
  title: Fold the faithfulness audit into the concurrence receipt attestation
  material: yes
  model: sonnet
  depends: [cade-faithfulness-audit, concurrence-receipt]
  proof: `npm run build` exits 0 AND `npx vitest run tests/engines/concurrence-receipt.test.ts` exits 0
  prompt: |
    Extend `server/utils/concurrence-receipt.ts` (from the `concurrence-receipt` task) so
    the signed receipt includes the `faithfulness-audit.ts` result — a machine-checked
    "reasoning verified: no unsupported leaps" attestation with the flagged-claim count.
    Keep the HMAC signature covering the added field; update the receipt unit test for
    determinism + tamper detection. Edits concurrence-receipt.ts → chained via depends.

- id: cade-pareto-in-dashboard
  title: Render the strength/safety frontier + a firm risk-appetite dial in the KPI dashboard
  material: no
  model: sonnet
  depends: [cade-pareto-drafting, kpi-dashboard]
  proof: `npm run build` exits 0
  prompt: |
    Add a strength-vs-tenability scatter (from `pareto-drafting.ts` non-dominated set) to
    the `app/pages/ops/cade-kpi.vue` page created by `kpi-dashboard`, plus a per-client-tier
    "risk appetite" selector (a point on the curve) stored in a small config so drafts can
    later auto-select a rung. Read-only + config; self-contained scoped styles. Edits the
    kpi-dashboard page → chained via depends.

OPERATOR:
  - If `cade-abstention-routing-inbox` adds an assignment table, apply that migration to the live apparently Supabase.
  - Specialist-tag → human mapping (who is the cftc_specialist / charter_specialist) needs a human to populate.
