PROJECT: beethoven

# Hyperscale levers (2026-07-09, owner-directed): the 20X-1000X batch. Build on what exists —
# do NOT reinvent modules named in each prompt. Every task: inspect current code first; if the
# fleet already partially built it, extend rather than duplicate.

- id: relfix-agents-in-ci
  title: Run mechanical agent tasks in GitHub Actions — lanes become hardware-unbounded
  material: yes
  model: opus
  depends: []
  proof: `python3 -m pytest runner/tests -q -k ci_agent` exits 0
  prompt: |
    Extend the CI story (see relfix-ci-workflow-templates / relfix-pr-native-integration-rollout,
    likely merged or in-flight) from gates to EXECUTION: a workflow template
    (.github/workflows/orch-agent.yml, generated per repo by runner/ci_workflows.py) that runs an
    agentic coder for a task inside a GitHub Actions runner: checkout, install, run a headless
    coder (codex exec / aider with a local-model API or ANTHROPIC key from repo secrets), commit
    to agent/<slug>, push. Dispatch: runner/ci_dispatch.py claims tasks flagged lane=ci (add a
    claim-path filter: only kinds in docs/chore/lint/mechanical/test with no deps), fires
    repository_dispatch with slug+prompt payload, and polls run status into the task row —
    the local lane frees immediately. Guardrails: per-repo concurrent CI-agent cap (env), never
    send crown-jewel/sensitive tasks (reuse _task_sensitivity), secrets only from GitHub repo
    secrets. Tests: dispatch payload build, sensitivity exclusion, status polling with mocked API.

- id: relfix-enqueue-transplant
  title: Enqueue-time near-duplicate detection — transplant merged patches instead of re-drafting
  material: no
  model: opus
  depends: []
  proof: `python3 -m pytest runner/tests -q -k transplant` exits 0
  prompt: |
    merged_diff_library, patch_transplant, result_cache and pgvector embeddings all exist but fire
    at RUN time. Move the match to ENQUEUE time: in intake_watcher (and prompt_factory's insert
    path), embed the new task prompt and query the merged-diff library + capabilities for
    similarity above a threshold; on a strong hit, tag the task row (note: transplant-candidate,
    ref to the source diff) AND force_coder to the cheapest lane so the agent starts from the
    prior patch (inject via the existing _reuse_context pattern from integration_sweeper). On a
    VERY strong hit for mechanical kinds, auto-fold: mark the task DECOMPOSED with a single child
    that is a pure transplant-apply task. Never auto-fold material tasks. Tests: threshold
    behavior with synthetic embeddings, material exclusion, reuse-context injection.

- id: relfix-microbatch-compiler
  title: Compile 20-50 same-repo mechanical tasks into one agent session
  material: no
  model: opus
  depends: []
  proof: `python3 -m pytest runner/tests -q -k microbatch` exits 0
  prompt: |
    batch_mechanical.py already folds mechanical tasks (PROTECTED_PREFIXES respected). Raise it
    into a compiler: group QUEUED mechanical/docs/lint/chore tasks per project (cap
    ORCH_MICROBATCH_SIZE default 25), synthesize ONE combined prompt with a numbered manifest
    and per-item proof commands, run as one task in one worktree, then verify per-item: parse the
    agent's per-item completion report, run each item's proof, and mark each member task
    MERGED/TESTFAIL individually (members already have rows — reuse the fold mechanism's
    bookkeeping). A red item does not sink the batch: it's re-queued solo. Tests: grouping caps,
    manifest synthesis, per-item verdict fan-out, red-item requeue.

- id: relfix-failure-immune-system
  title: Every TESTFAIL/BUILDFAIL root cause becomes a permanent fleet-wide check
  material: no
  model: sonnet
  depends: []
  proof: `python3 -m pytest runner/tests -q -k immune` exits 0
  prompt: |
    generator_feedback.py already summarizes failures. Extend into an immune system:
    runner/failure_immunizer.py (periodic, 6h): cluster recent TESTFAIL/BUILDFAIL notes by
    root-cause pattern (build_fixer logs + notes); for each recurring cluster (>=3 occurrences),
    emit (a) a pattern-library entry (integrates with fleet-pattern-library task's format if
    merged; else a patterns/ JSON) injected into future prompts for that project, and (b) where
    mechanically checkable (import-path depth bugs, missing auth-gate, hardcoded model strings,
    casing-mismatch migrations — all observed this week), generate a grep/lint check line and
    queue ONE small task per affected repo to add it to that repo's check chain. Dedupe by
    cluster fingerprint. Tests: clustering on synthetic notes, check-line generation, dedupe.

- id: relfix-contract-first-verify
  title: Planner emits executable acceptance tests BEFORE code tasks; verify becomes deterministic
  material: yes
  model: opus
  depends: []
  proof: `python3 -m pytest runner/tests -q -k contract_first` exits 0
  prompt: |
    planner.py already does contract-first DAG decomposition conceptually. Make contracts
    EXECUTABLE: for each planned code sub-task, the planner first emits a sibling test-authoring
    task whose deliverable is a failing acceptance test (committed to the repo, marked
    xfail/todo per that repo's runner), and the code task's proof becomes "that test passes +
    suite green". Wire pipeline_contract.py so verify checks the specific acceptance test file
    exists and flips to passing. For UI-facing work add an optional preview-QA hook: a task
    template that runs Playwright against the Vercel preview URL asserting the acceptance
    criteria (skeleton + one working example on the beethoven dashboard is enough; other repos
    adopt via propagation). Tests: planner output shape (test task precedes code task, deps
    correct), proof rewriting, pipeline_contract verification path.

- id: relfix-reset-aware-scheduling
  title: Schedule premium-model work around subscription reset windows; nights run local-saturated
  material: no
  model: sonnet
  depends: []
  proof: `python3 -m pytest runner/tests -q -k reset_aware` exits 0
  prompt: |
    account_pool.py rotates accounts on exhaustion and the claim loop routes models via
    bandit/agentic_coders. Add time-awareness: runner/reset_scheduler.py maintains per-account
    known/learned reset timestamps (parse the limit-banner text already captured in outcomes —
    'resets Jul 8 at 6am' style — into account_pool state); claim-time hook: when an account is
    within N hours of reset with budget exhausted, material/Claude-tier tasks defer (stay QUEUED)
    while easy work keeps flowing to local models; immediately after a reset, temporarily raise
    the Claude share to burn fresh budget on the material backlog. Overnight window (env
    ORCH_NIGHT_LOCAL_START/END): easy-offload share forced to 1.0. All decisions logged to the
    scoreboard. Tests: banner parsing, defer/boost windows with injected clock.

- id: relfix-closed-loop-tuning
  title: Meta-optimizer auto-tunes lanes/batch-caps/offload-share from scoreboard metrics
  material: yes
  model: opus
  depends: [relfix-reset-aware-scheduling]
  proof: `python3 -m pytest runner/tests -q -k autotune` exits 0
  prompt: |
    Close the control loop: runner/knob_tuner.py (periodic, 2h) reads scoreboard metrics
    (merged/day, usd_per_merge, first-pass rate, queue depth trend, lane utilization, RAM
    headroom) and proposes bounded adjustments to a WHITELISTED knob set via the fleet_config
    gateway: ORCH_EASY_OFFLOAD_SHARE (0.5-1.0), MERGE_TRAIN_LOW_RISK_BATCH (4-16),
    ORCH_MICROBATCH_SIZE (10-50), governor lane limit bias, RELEASE_MIN_BATCH (3-10). Rules:
    one knob per cycle, +-1 step, hill-climb on merged/day per dollar with a 24h evaluation
    window; auto-revert if the metric degrades two windows running; every change writes an ops
    card (visibility, not approval) and is journaled. Hard bounds in code, never from config.
    Tests: hill-climb step logic, revert-on-degrade, bounds enforcement with synthetic metrics.

OPERATOR:
  - relfix-agents-in-ci needs GitHub repo secrets set per repo before enabling (OPENAI_API_KEY or ANTHROPIC key for the CI coder) — the task will list exactly which; add via GitHub repo Settings → Secrets.
  - relfix-closed-loop-tuning's knob changes surface as ops cards — glance at them in the dashboard the first week to sanity-check the hill-climb.
