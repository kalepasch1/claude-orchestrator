PROJECT: smarter

# Strategy batch 2 — the autonomy + data-product frontier. The engine layer for prior workstreams is
# built (server/utils/* + server/api/*, kv-persisted). These tasks extend existing engines; do NOT rebuild
# them. Same hard rules: mock-degradable (works with NO API key), route generated legal text through qc.ts +
# Policy Constitution + render <TrustBadge>, respect the governance trust dial, persist via kv.ts, never break
# the seed demo, `npx vue-tsc --noEmit` clean per commit. Cross-batch deps (distillation-enforce, federated-dp
# from smarter-0629.md) are referenced in prompts, not in depends[].

- id: judgment-capture
  title: Capture per-partner judgment decisions (options + choice + rationale)
  material: no
  model: sonnet
  depends: []
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Build server/utils/judgmentModel.ts (+ endpoint server/api/judgment/capture.post.ts) that records every
    judgment point the system escalates: the 3–5 options surfaced (from the war-room / Approve flow), which the
    partner chose, and the stated reason — keyed per principalId. Reuse the option shape already used by
    server/utils/approvals.ts (ApprovalItem.options) and the principal identity from server/utils/principalModel.ts.
    Persist via kv (hydrateMerge by id). Mock-degradable. This is pure capture — no prediction yet.

- id: judgment-model
  title: Per-partner judgment model that predicts the partner's likely call
  material: yes
  model: opus
  depends: [judgment-capture]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Extend server/utils/judgmentModel.ts so that, given a new judgment point + the candidate options, it predicts
    the option THIS partner would most likely choose, with a confidence and a "because last time on X you chose Y"
    rationale. Surface it as the pre-ranked recommended option in the Approve / war-room flow (it augments, never
    replaces, the human gate). HARD GUARDRAIL: it must NOT auto-execute a legal-judgment decision — above a
    confidence/risk threshold it still routes to the human via the governance trust dial; log every prediction +
    outcome to trustReceipts so the model's accuracy is auditable. Feeds principalModel's learning loop. This is
    the mechanism that moves the system from "stops for judgment" toward "proposes the partner's own call."

- id: autopilot-bounds
  title: Let a partner set concession ladders + walk-away bounds once
  material: no
  model: sonnet
  depends: []
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Add a per-deal/per-clause bounds config (concession ladder steps + walk-away thresholds + which clauses are
    in-bounds for autonomy) persisted via kv, editable by the partner. Reuse the clause/position shapes in
    server/utils/dealIntel.ts + server/data/marketStandards.ts and the hosted-room model in server/utils/hosted.ts.
    Endpoint to read/write the bounds. No autonomous action yet — this just captures the guardrails.

- id: negotiation-autopilot
  title: Self-driving war room — conduct rounds within the partner's bounds
  material: yes
  model: opus
  depends: [autopilot-bounds]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Build the engine that, inside a hosted negotiation room, conducts rounds autonomously WITHIN the
    autopilot-bounds: auto-counter along the concession ladder, hold at the walk-away, and ESCALATE to the human
    the moment a counterparty position crosses a boundary or proposes an out-of-bounds term. Every auto-sent round
    requires human-tap by default (trust dial), is fully audited to trustReceipts, and is reversible. Reuse
    server/utils/{hosted,dealIntel,counterpartyReputation}.ts. Reference HANDOFF_ADMIN_OS_AND_CAMPAIGNS.md
    (war-room) + the negotiation room pages/room/[token].vue.

- id: portfolio-runner
  title: Portfolio runner — run the whole book nightly into one brief
  material: no
  model: opus
  depends: []
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Build server/utils/portfolioRunner.ts (+ endpoint) — an orchestrator over server/utils/matterRunner.ts that
    runs EVERY open matter in the associate's book and produces one "state of your book + the 3 decisions only you
    can make" brief: per-matter autonomy %, what was auto-advanced, what's blocked, and the escalations ranked.
    It convenes the existing per-matter runner; it does not duplicate its logic. Auto-advances stay under the trust
    dial. Surface the brief in Now. Intended to run via a scheduled task (the macro form of "you woke up to N done").

- id: evolutionary-playbooks
  title: A/B-test playbook variants across the network, keep the winner
  material: yes
  model: opus
  depends: []
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Extend server/utils/playbookLibrary.ts so playbooks carry variants, and postMortem/manualCapture outcomes feed
    a measured success metric per variant; the best-performing variant is promoted automatically (with the prior
    kept for rollback). Cross-network learning MUST route through the distillation gate (server/utils/distillation.ts)
    and the federated DP path (server/utils/federatedLearning.ts — see smarter-0629.md's distillation-enforce +
    federated-dp) so only anonymized process knowledge participates and no firm's data is re-identifiable. Log
    promotions to trustReceipts. Reference server/utils/{postMortem,manualCapture,masteryLibrary}.ts.

- id: work-unit-catalog
  title: Expose mastery recipes as priced fixed-cost "work units"
  material: yes
  model: sonnet
  depends: []
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    In smarter, expose mature mastery-library recipes (server/utils/masteryLibrary.ts, ranked by
    productizationScore) as priced "work unit" descriptors — fixed-cost deliverable + estimated effort saved +
    margin metadata — via server/utils/apparentlyExport.ts and a catalog endpoint (owner/admin-gated). This is the
    smarter-side catalog only; the storefront + payment + delivery live in the Apparently repo (OPERATOR). Money/
    pricing metadata is present, so this is material. Do not implement billing here.

- id: compliance-feed
  title: Compliance-as-a-feed — sell the requirement-version diff
  material: no
  model: sonnet
  depends: []
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Build a "what changed this quarter" diff feed from the versioned requirement graph in
    server/utils/regRequirements.ts (it already version-hashes requirement sets + has a change radar): per
    filing-type × jurisdiction, emit added/removed/changed requirements between versions, as a subscribable feed
    endpoint + a simple view. Mock-degradable from the seeded packs; live sourcing + subscription billing are
    OPERATOR. This monetizes infrastructure already maintained.

- id: tactics-graph
  title: Counterparty / opposing-counsel tactics graph as a product
  material: yes
  model: opus
  depends: []
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Aggregate per-counterparty / per-opposing-counsel tactics across closed rooms into a dossier product:
    where they fold, concession speed, opening posture, time-of-day patterns, settle-vs-litigate tendencies.
    Extend server/utils/counterpartyReputation.ts; the cross-firm aggregation MUST go through the distillation
    gate + federated DP/k-anonymity (see smarter-0629.md) so no single firm's data is re-identifiable. Surface a
    dossier view + a product-grade export. Cross-firm data product → material; selling it needs the legal review
    in OPERATOR.

- id: calibration-surface
  title: Make calibrated "I'm not sure" a first-class visible signal
  material: no
  model: sonnet
  depends: []
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    server/utils/calibration.ts already computes confidence. Surface calibrated confidence — and an explicit "low
    confidence / I'm not sure, here's why" state — as a first-class signal on every generated output (extend
    components/TrustBadge.vue), in the Approve queue, and as the input that drives the autonomy thresholds (a
    low-confidence item never auto-runs). Honesty-as-trust: it is the headline that lets firms safely widen the
    trust dial.

- id: defensibility-ledger
  title: Exportable plain-English defensibility / audit ledger
  material: no
  model: sonnet
  depends: []
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Build an exportable, plain-English audit trail over server/utils/{trustReceipts,evidenceLocker,actionLog}.ts:
    for every autonomous action — what was done, why, the sources, the QC verdict, who approved, and the undo
    state — rendered as a GC-/bar-/insurer-acceptable record (per matter + firm-wide). Read-only export (PDF/CSV/
    JSON). This is the artifact a risk committee needs to approve turning autonomy up.

- id: cross-vertical-abstraction
  title: Generalize the requirement-graph + credentialing to new verticals
  material: no
  model: opus
  depends: []
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Refactor the jurisdiction/requirement model (server/data/jurisdictions.ts + server/utils/{credentials,
    credentialVault,regRequirements}.ts) into a profession/jurisdiction-agnostic schema with ZERO behavior change
    for the existing legal/bar flows, then add ONE new vertical as proof of generality using seed data only (e.g.
    financial-advisor registration / FINRA-adjacent, or a non-US bar). No DB migration — seed data + types only.
    Demonstrates the same engine powers new regulated professions (each a TAM multiplier feeding the same network).

OPERATOR:
  - MARKETPLACE (work-unit-catalog): the storefront, payment rails (Stripe), and self-service delivery live in the Apparently repo — wire smarter's catalog to it; set the margin/price per work unit; tax/invoicing.
  - SUBSCRIPTION BILLING: monetization + entitlements for the compliance-feed, the tactics-graph dossier, and the defensibility-ledger product (pricing, gating, billing integration).
  - LEGAL/COMPLIANCE SIGN-OFF: selling the counterparty/opposing-counsel tactics graph needs privacy + antitrust + professional-responsibility review before any cross-firm data product ships; confirm the cross-firm sharing rests on the privilege-preserving ToS + federated DP (federated-dp in smarter-0629.md).
  - AUTONOMY POLICY: human sign-off on the default confidence/risk thresholds for judgment-model predictions and negotiation-autopilot auto-send (recommend human-tap default until a clean track record exists, mirroring the high-risk-filing policy).
  - LIVE SOURCING: compliance-feed live requirement sourcing reuses the NMLS/state/FINRA/SEC provider seam + creds already listed in smarter-0629.md's OPERATOR block.
