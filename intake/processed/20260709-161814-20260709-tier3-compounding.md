PROJECT: beethoven

# Tier-3 compounding batch (2026-07-09, owner-directed). Rules as before: inspect existing
# modules first (several named below), extend not duplicate; cross-repo effects always land via
# generated per-repo intake tasks, never direct writes to other repos.

- id: relfix-local-lora-distill
  title: Fine-tune local models on the fleet's own merged diffs (MLX LoRA -> Ollama)
  material: yes
  model: opus
  depends: []
  proof: `python3 -m pytest runner/tests -q -k distill` exits 0
  prompt: |
    merged_diff_library holds verified-good patches for OUR codebases — a free training set.
    Build runner/distill.py: (1) export a training corpus from merged_diff_library + task prompts
    (prompt -> diff pairs, per-repo tagged, PII/secret-scrubbed via privacy.scrub, capped and
    deduped); (2) a fine-tune runner using mlx_lm (Apple Silicon: `mlx_lm.lora --train`) against a
    qwen2.5-coder base, emitting an adapter; (3) conversion/serving path: fuse + convert to GGUF
    for Ollama (document the exact commands; automate what's runnable, operator-note what needs
    the big-RAM window); (4) an eval harness FIRST-CLASS: hold out 50 merged tasks, measure
    first-pass proof success of base vs tuned model on transplant-style drafts — the tuned model
    only enters the coder pool (ollama_catalog) if it beats base by a configured margin.
    Corpus building + eval harness + pool gating are the deliverables with tests (mock the actual
    training call); the first real training run is operator-triggered (RAM). Nightly corpus
    refresh in the periodic list, training run monthly via ops card.

- id: relfix-digital-twin-qa
  title: Synthetic-user journeys run against staging before every release-train promotion
  material: yes
  model: opus
  depends: []
  proof: `python3 -m pytest runner/tests -q -k twin_qa` exits 0
  prompt: |
    Make the 'QA' in batch-QA real: runner/twin_qa.py + a personas/ directory of Playwright
    journey specs per app (start with THREE: apparently licensing-intake happy path, smarter
    negotiation-room open/portal flow, beethoven dashboard login+queue view; skeleton for others).
    Hook: release_train's QA gate calls twin_qa.run(project, staging_url) — resolve the staging
    URL from the Vercel preview of orchestrator/dev (deploy_health/vercel project mapping);
    journeys run headless (npx playwright, install task-side), artifacts (trace + screenshots on
    failure) saved to .runtime/twin_qa/<project>/ and linked in the failure note; a red journey
    BLOCKS promotion and files a qafix- task with the trace path. Personas assert user-visible
    outcomes (form submits, room renders, data persists), not pixels. Auth for staging: env-based
    test accounts, operator-note which are needed. Tests: gate wiring with mocked playwright
    runner, red-blocks-promotion, artifact linking.

- id: relfix-telemetry-priority
  title: Production telemetry drives queue priority (EV scheduler gets real signals)
  material: no
  model: sonnet
  depends: []
  proof: `python3 -m pytest runner/tests -q -k telemetry_ev` exits 0
  prompt: |
    ev_scheduler.py scores on MRR heuristics + outcome stats. Feed it reality:
    runner/telemetry_ingest.py (periodic 1h) pulls per-app signals already available — 
    deploy_health states, each app's Supabase ai_call_log volumes/costs where readable, Vercel
    deployment error rates (vercel CLI ls per linked project), and any app-emitted usage counters
    (define a tiny convention: apps push a metrics row; document it, don't build app-side here).
    Normalize into ctx['app_signals'] = {project: {usage_trend, error_rate, cost_burn}} consumed
    by ev_scheduler.score(): error spikes boost fix-kind tasks for that project; rising usage
    boosts feature tasks; dead apps sink. All fail-soft (missing signal = neutral). Tests:
    score shifts with synthetic signals, neutrality on absence.

- id: relfix-shared-kernel-extraction
  title: Detect duplicated cross-app implementations and extract vendored shared packages
  material: yes
  model: opus
  depends: []
  proof: `python3 -m pytest runner/tests -q -k kernel` exits 0
  prompt: |
    The propagation loop copies fixes between repos; better: extract once. runner/kernel_miner.py
    (periodic, daily): embed function/module-level chunks across fleet repos (reuse context_embed
    provider), cluster near-duplicates appearing in 3+ repos (expected hits: auth-gate helpers,
    RLS query wrappers, comm normalizers, retry/backoff utils, token-contract CSS). For each
    cluster above threshold: file ONE approval card proposing extraction with the member file
    list, then on approval queue the extraction tasks: create the package under the vendoring
    convention that already exists (vendor/ht-ui pattern: source-of-truth package + vendored
    copies + CI allowlist), plus one small per-repo adoption task. NEVER auto-extract without the
    card. Tests: clustering on synthetic chunks, card payload, task generation format (parseable
    by intake_watcher.parse).

- id: relfix-invariants-as-code
  title: SPEC invariants become generated property tests + runtime asserts
  material: no
  model: sonnet
  depends: []
  proof: `python3 -m pytest runner/tests -q -k invariant` exits 0
  prompt: |
    spec.py compares SPEC.md to code weekly as prose. Make invariants executable:
    runner/invariant_tests.py — a generated pytest module where each SPEC.md invariant that is
    mechanically checkable gets a real check: upsert-only writes (grep db.insert callsites for
    upsert/ON CONFLICT), privacy.scrub on capability-write paths (call-graph grep), outcomes row
    before DONE/MERGED (transition guard test against db.py logic), confidence gate before
    pr_integrate merge (code-path assert), launchd plists use EnvironmentVariables. Plus runtime:
    a lightweight assert_invariant(name, cond) helper that logs violations to resource_events
    (never raises — fail-soft) sprinkled at the 5 highest-value spots. spec.py gains a section
    mapping each invariant -> its test id so drift reports show WHICH check covers WHAT. Tests
    included by construction; suite must stay hermetic (no live DB — mock db).

- id: relfix-material-red-team
  title: Re-scope debate machinery to adversarial review of material diffs only
  material: yes
  model: sonnet
  depends: []
  proof: `python3 -m pytest runner/tests -q -k red_team` exits 0
  prompt: |
    Colosseum/committees burned compute as periodic noise (now lean-moded off). Salvage the value:
    runner/material_red_team.py — invoked ONLY from the merge train when a card's risk level is
    'sensitive' (the existing _risk_level), before the ff step: one adversarial pass by a cheap
    strong model (route via agentic_coders: local qwen or haiku) with a focused prompt — find
    security holes, auth bypasses, data leaks, invariant violations in THIS diff; structured
    findings (Zod-like schema via json). Findings above severity threshold -> block with TESTFAIL-
    style note + qafix task; below -> attach as note, proceed. Budget: hard cap per day (env),
    skip silently when exhausted. Tests: only-sensitive invocation, block/pass thresholds,
    budget cap. Do NOT re-enable the periodic colosseum jobs.

- id: relfix-fleet-rag-memory
  title: Full-fleet retrieval memory injected into every agent prompt
  material: no
  model: opus
  depends: []
  proof: `python3 -m pytest runner/tests -q -k fleet_rag` exits 0
  prompt: |
    context_embed ranks files; raise it to true retrieval: runner/fleet_rag.py — index (pgvector
    'knowledge' table, existing embedding provider with the 429 fallback) over: merged diff
    summaries, task postmortems/notes, CLAUDE.md conventions, pattern library entries, sentinel/
    incident logs, and REPORT-*.md docs. At prompt-build time (the same seam where patterns are
    injected — planner + task prompt builder), retrieve top-k relevant chunks for the task prompt
    (k=5, token-budgeted, dedupe against already-injected patterns) and add a 'FLEET MEMORY'
    section. Index refresh piggybacks nightly on the distill corpus job. Retrieval is fail-soft
    (DB down = skip). Tests: chunking, budget enforcement, dedupe vs patterns, fail-soft path.

OPERATOR:
  - relfix-local-lora-distill: the first real training run wants a big-RAM window (or the future Mac Studio) — it will surface as an ops card with the exact command; approve when convenient.
  - relfix-digital-twin-qa: create staging test accounts for apparently + smarter (task will list the env var names).
  - relfix-shared-kernel-extraction: extraction proposals arrive as approval cards — approve/deny per cluster.
