PROJECT: pareto-2080

# Remaining work for the W16 "personal treasury / replace-the-bank" initiative.
# Already done & unit-tested in-repo (do NOT redo): depositBonusOptimizer, bonusRotationScheduler
# (greedy + exact branch-and-bound solver), jitLiquidityRail (+ interchange), jitBufferSizing,
# personalTreasury (orchestrator + cascadeCrossDepartment + treasuryCounterfactual +
# graduateTrustFromLedger), depositBonusData, endpoints under /api/personal/treasury/* + the
# /api/cron/treasury-ledger handler, /Treasury/:userId page, Prisma models TreasuryLedgerEntry &
# TreasuryTrustLane (validated, NOT migrated). Full context: see repo files TREASURY_HANDOFF_PROMPT.md
# and the W16 row in CLAUDE.md. Engine tests run with `node --test tests/*.test.js`.

- id: ledger-rollup-shape
  title: Flatten the treasury ledger rollup so the read endpoint matches the page contract
  material: no
  model: haiku
  depends: []
  proof: `node --test tests/treasuryLedgerShape.test.js` exits 0
  prompt: |
    BUG: `server/api/personal/treasury/ledger.get.js` returns `rollup` straight from
    `counterfactualLedger.rollup()`, whose shape is nested: `{ captured:{annualUsd,oneTimeUsd},
    forgone:{annualUsd,...}, incumbentCost:{annualUsd,...}, capturedRetirementDays, headline }`.
    But `pages/Treasury/[userId].vue` (Ledger rollup section) reads FLAT fields:
    `ledgerData.rollup.capturedUsd`, `.forgoneUsd`, `.incumbentCostUsd`, `.retirementDaysGained`.
    So the UI shows blanks.
    DELIVERABLE: add a pure helper `flattenRollup(rollup)` to `server/utils/counterfactualLedger.js`
    (exported) that returns `{ capturedUsd, capturedOneTimeUsd, forgoneUsd, incumbentCostUsd,
    retirementDaysGained, headline }` (capturedUsd = captured.annualUsd + captured.oneTimeUsd;
    retirementDaysGained = capturedRetirementDays ?? null). Update `ledger.get.js` to return
    `rollup: flattenRollup(cfRollup(...))`. Do NOT change the page.
    TEST: create `tests/treasuryLedgerShape.test.js` (node:test) asserting flattenRollup maps a
    sample nested rollup to the exact flat keys the page reads.

- id: jit-buffer-world-wiring
  title: Feed predictive JIT buffer sizing from the live world model instead of a synthetic mean
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/treasuryWorldWiring.test.js` exits 0
  prompt: |
    GOAL (TREASURY_HANDOFF_PROMPT.md item 5): `GET /api/personal/treasury` currently sizes the
    instant tranche from a synthetic `monthlyExpense/30` mean. Wire it to real forecast inputs.
    DELIVERABLE: add a pure helper `buildBufferInputs({ forecastResult, fundTargets, liquidUsd })`
    to `server/utils/jitBufferSizing.js` that derives `{ dailyOutflowMean, dailyOutflowSd,
    upcomingOutflows, totalLiquidUsd, fundTargets }` from a `cashFlowForecast.forecast()` result
    (use its daily-net series for mean/sd; map large scheduled debits to upcomingOutflows) and then
    calls the existing `sizeFromWorld`. Update `server/api/personal/treasury/index.get.js` to call
    `cashFlowForecast.forecast()` + `standingFunds.computeFundTargets()` when transaction data is
    available, falling back to the current synthetic path when it is not. Keep the response shape of
    `liquidityPosition` identical.
    TEST: create `tests/treasuryWorldWiring.test.js` asserting buildBufferInputs derives a sensible
    mean/sd from a sample forecast series and that the emergency-fund floor still applies.

- id: trust-lane-autonomy-enforcement
  title: Make persisted TreasuryTrustLane tiers actually gate autonomy in approvalPolicy
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/approvalPolicy.test.js` exits 0
  prompt: |
    GOAL (TREASURY_HANDOFF_PROMPT.md item 6): trust lanes are computed + persisted but do not yet
    change what auto-executes. Make the per-department lane tier the autonomy decision.
    DELIVERABLE: extend `server/utils/approvalPolicy.js` so `decideApproval(...)` accepts an optional
    `laneTier` (and `laneMaxTier`) in its input and uses it: laneTier 'A' => AUTO (still never above
    maxTier; money/credit/tax/treasury cap at 'B'), 'B' => one-click, 'C' => always approve. This is
    PURE LOGIC — do not add DB reads here. Then update `server/utils/agentLedger.js#createAction`
    to look up the user's `TreasuryTrustLane` for the action's department (defensive: wrap in
    try/catch, default to no lane => current behavior) and pass laneTier through. Preserve all
    existing approvalPolicy behavior when no lane is supplied (backward compatible).
    SAFETY: money-moving departments (treasury, credit, tax) must NEVER auto-execute above Tier-B
    regardless of lane tier; assert this in tests. A reversal/Tier-C action always requires approval.
    TEST: extend `tests/approvalPolicy.test.js` with cases: lane 'A' on a non-money lane auto-approves;
    lane 'A' on a money lane caps at one-click (not AUTO); no lane => unchanged decision.

- id: deposit-bonus-crawl-normalize
  title: Pure normalize/dedupe transformer for a live deposit-bonus catalog feed
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/depositBonusCrawl.test.js` exits 0
  prompt: |
    GOAL (TREASURY_HANDOFF_PROMPT.md item 3, code-only slice): build the deterministic core of a
    live bonus crawler so the optimizer can later run on real offers. NO network, NO DB, NO schema
    here — just the transform + tests (the scheduled crawl, DepositBonusOffer table, and secrets are
    OPERATOR items below).
    DELIVERABLE: create `server/utils/depositBonusCrawl.js` exporting `normalizeOffer(raw)` →
    the exact offer shape consumed by `server/utils/depositBonusOptimizer.js` /
    `bonusRotationScheduler.js` (id, institution, type, bonusUsd, minDepositUsd, holdDays, baseApy,
    clawbackDays, requiresDirectDeposit, ddWindowDays, requirements[], eligibility{}, cooldownDays,
    expiresAt), plus `dedupeOffers(offers)` (collapse by institution+type, keep best bonus-adjusted
    value) and `mergeWithCatalog(crawled, seed)` (crawled wins; seed from depositBonusData.js fills
    gaps). Mirror the normalization style of `server/utils/listingCrawler.js`.
    TEST: create `tests/depositBonusCrawl.test.js` covering normalize of a messy raw record, dedupe
    of two same-institution offers, and that mergeWithCatalog preserves seed offers not in the crawl.

- id: treasury-approve-affordance
  title: One-tap Approve on the Treasury action queue wired to existing approvals endpoints
  material: no
  model: sonnet
  depends: [ledger-rollup-shape]
  proof: `npx nuxt build` exits 0
  prompt: |
    GOAL (TREASURY_HANDOFF_PROMPT.md item 7, UI slice): in `pages/Treasury/[userId].vue`, add an
    "Approve" button to each row in the "Needs your tap" actionQueue that POSTs to the existing
    approvals endpoints (`/api/approvals/:id/approve` — confirm the exact path/shape used by
    `pages/Approvals/User/[userId].vue` and reuse it). Optimistically remove the row on success and
    show a small inline result; show an error inline on failure. Do NOT add a new endpoint. Keep the
    page on the default layout (no layout:false — see the comment at the top of the file). Respect the
    existing intentional page edits (the JIT funding control and ledger sections must remain).
    PROOF: `npx nuxt build` must complete with exit 0.

- id: prisma-linux-binarytargets
  title: Add linux binaryTarget to the Prisma generator so CI can run DB-touching tests
  material: yes
  model: haiku
  depends: []
  proof: `npx prisma validate` exits 0
  prompt: |
    CONTEXT: `tests/agentLedger.test.js` and `tests/bookingSaga.test.js` fail ONLY in Linux CI
    because Prisma's query engine is generated for darwin-arm64. Add the Linux target so the
    orchestrator's worktree CI can exercise those tests.
    DELIVERABLE: in `prisma/schema.prisma`, set the generator block to
    `binaryTargets = ["native", "linux-arm64-openssl-3.0.x"]` (keep "native" for local macOS). Change
    NOTHING else in the schema. Do not run migrations.
    PROOF: `npx prisma validate` exits 0. (Note: a follow-up `npx prisma generate` will download the
    Linux engine; that is fine in CI.)

OPERATOR:
  - Run the Prisma migration for `TreasuryLedgerEntry` + `TreasuryTrustLane` against Supabase
    (`npx prisma migrate dev --name treasury_ledger_and_trust_lanes`, then `migrate deploy` for prod,
    then `prisma generate`). Models are validated in schema.prisma but unmigrated — endpoints/cron
    that touch them will 500 until this runs. (DB access; schema/migration = material.)
  - Schedule the nightly `POST /api/cron/treasury-ledger` (Vercel `crons` in vercel.json) and set the
    `CRON_SECRET` env var; the handler already enforces the `x-cron-secret` header + `treasury_ledger`
    kill switch. Optionally register a durable `treasury_ledger` handler in jobRunner. (Deploy + secret.)
  - The instant rail (TREASURY_HANDOFF_PROMPT.md item 4): contract + integrate SoFiUSD / SoFi Pay /
    Galileo for card settlement + cross-border, plus RTP / FedNow / Visa-Direct fallback rails and an
    SBLOC/portfolio-line provider for the "pledge, don't sell" source. Compliance: Reg E (debit),
    BSA/AML/KYC via partner, money-transmission posture, FDIC caveat on stablecoin balances. Until
    live, `POST /api/personal/treasury/jit-fund` only PROPOSES a signed Tier-C action. (Partners +
    legal/regulatory + secrets.)
  - Card-program interchange economics: define Pareto's interchange share contractually with the
    issuer/program manager so jitLiquidityRail's interchange-capture revenue is real. (Legal/contract.)
  - Live deposit-bonus data: license a feed or stand up the scheduled crawl built by
    `deposit-bonus-crawl-normalize`, add a `DepositBonusOffer` table + migration, and wire nightly
    refresh with a "terms last verified" timestamp. (Schema/migration + network/secrets.) Never
    auto-open an account — opening stays Tier-A/Tier-C through agentLedger.
  - End-to-end/Playwright run of the four `/api/personal/treasury/*` endpoints + the page against a
    seeded test user. (Needs a running app + DB.)
