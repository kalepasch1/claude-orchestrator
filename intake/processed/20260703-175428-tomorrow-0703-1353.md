PROJECT: tomorrow

# Net-new deliverables only (the ECP core + hardening are already queued in tomorrow-0702.md and
# tomorrow-0703-1344.md; slugs there are idempotent). Everything below is a NEW FILE / new module so
# it runs parallel-safe and never merge-conflicts with the queued work. All depend on
# `ecp-prodbuild-green` (the in-repo prod-build guard from tomorrow-0703-1344.md) and keep
# `npm run build` green; flagged features default OFF so prod behavior is unchanged.

- id: ecp-universal-transfer-gate
  title: Generalize the trade-authorization token into a reusable on-chain transfer gate (new file)
  material: yes
  model: sonnet
  depends: [ecp-prodbuild-green]
  proof: `npm run build` exits 0 AND `npx vitest run server/utils/otc/ecp/__tests__/` passes with a NEW test asserting a non-swap transfer (repo/RWA-subscription/lending-draw shape) is refused without a valid ECP token and allowed with one, and refused when either party is revoked
  prompt: |
    tradeGate.ts authorizes bilateral SWAPS. Add a new file server/utils/otc/ecp/transferGate.ts that
    REUSES tradeGate/verifyEcpCredential to authorize any generic regulated on-chain transfer (a
    TransferSpec: kind, amountUsd, sender, receiver) — the same ECP-both-sides + not-revoked check, so the
    ECP rail can gate repo, RWA subscriptions, and lending draws, not just swaps. Pure + testable; do NOT
    edit tradeGate.ts or the settlement engine (compose by import only) so this stays parallel-safe with
    ecp-settlement-gate. CANDIDATE-SHARED: transferGate — any app gating on-chain transfers on ECP status.

- id: ecp-capacity-tokenization
  title: Tokenize guarantee capacity into transferable participations (new file)
  material: yes
  model: opus
  depends: [ecp-prodbuild-green]
  proof: `npm run build` exits 0 AND `npx vitest run server/utils/otc/ecp/__tests__/` passes with a NEW test that slices a guarantor's capacity into tranches, tokenizes a participation, and blocks transfer of a participation to a non-ECP recipient
  prompt: |
    New file server/utils/otc/ecp/guaranteeCapacityToken.ts that turns guarantee capacity into a tradable
    secondary market: REUSE guarantorYield.trancheCapacity + the EXISTING rings/capacityFormation
    (tokenizeParticipation/transferParticipation — search server/utils/otc/rings) so a guarantor's tranche
    becomes a named bilateral participation. Guards: recipient must hold a valid ECP credential
    (verifyEcpCredential), concentration + appetite checks from capacityFormation, operator never a holder
    (N8). Pure engine + tests; new file only, no edits to guarantorYield.ts or guarantorMarketplace.ts.
    Fee accrues to the guarantor. Env flag default OFF.

- id: ecp-zk-set-membership
  title: Zero-knowledge set-membership eligibility (entity-type / jurisdiction) — new file, spike
  material: no
  model: opus
  depends: [ecp-prodbuild-green]
  proof: `npm run build` exits 0 AND `npx vitest run server/utils/otc/ecp/__tests__/` passes with a NEW test proving membership in an allowed set in zero knowledge (member verifies true, non-member cannot produce a valid proof, tampered proof fails) without revealing which member
  prompt: |
    New file server/utils/otc/ecp/zkSetMembership.ts extending the PROVEN Pedersen/Chaum-Pedersen OR-proof
    machinery in zkRange.ts (import its group params + bit-OR primitive; do NOT edit zkRange.ts or
    zkPredicateCredential.ts) to prove "attribute ∈ allowedSet" (e.g. jurisdiction ∈ {US} or entityType ∈
    {corporation, commodity_pool}) with zero disclosure of which element. Add a credential wrapper
    buildZkMembershipCredential mirroring zkPredicateCredential's shape (kind:'eligibility', proof chained
    by digest). Fully self-contained crypto + tests (keep bits/set small for speed). Premium private tier;
    parallel-safe new files only.

- id: ecp-reputation-pricing
  title: Reputation-driven dynamic pricing across gate/guarantee/hedge fees (new file)
  material: no
  model: sonnet
  depends: [ecp-prodbuild-green]
  proof: `npm run build` exits 0 AND `npx vitest run server/utils/otc/ecp/__tests__/` passes with a NEW test asserting a higher reputation band yields a lower gate/guarantee fee and 'watch' yields a surcharge, monotonic and bounded
  prompt: |
    New file server/utils/otc/ecp/reputationPricing.ts (pure) that reads a reputation snapshot
    (computeReputation from reputationScore.ts — import only, do NOT edit reputationScore.ts or
    ecpService.ts to avoid conflict with ecp-live-sanctions-seam) and returns risk-adjusted multipliers for
    gate-check fees, guarantee fees (compose with guarantorYield.priceGuaranteeFee), and hedge spreads.
    Monotonic (better band ⇒ lower fee), clamped to sane bounds. Pure + tested. Wiring into the actual fee
    paths is a separate follow-on task later; this ships the engine only, build-green.

- id: ecp-mutual-eligibility-sbt
  title: Non-transferable on-chain ECP claim (soulbound) + reference contract — spike behind flag
  material: yes
  model: sonnet
  depends: [ecp-prodbuild-green]
  proof: `npm run build` exits 0 AND `npx vitest run server/utils/otc/ecp/__tests__/` passes with a NEW test asserting the SBT claim payload is built deterministically from a credential and the reference Solidity source is emitted; the on-chain write path is a no-op unless ECP_SBT_ENABLED is set
  prompt: |
    New file server/utils/otc/ecp/ecpSbt.ts that mirrors easAdapter's gated-dry-run pattern to represent
    the ECP credential as a NON-TRANSFERABLE (soulbound) on-chain claim a wallet/protocol can read directly,
    plus a buildSbtSolidity() reference (extends sdk.buildSolidityModule — import, do not edit sdk.ts). OFF
    by default (ECP_SBT_ENABLED); the submit path stays a dry-run/no-op without a signer so `npm run build`
    and prod are unchanged. NO keys in code — env only, list under OPERATOR. New file only. Tests use the
    dry-run path (no network). CANDIDATE-SHARED: soulbound-credential pattern reusable by other issuers.

OPERATOR:
  - Env for go-live gates (all default OFF): ECP_SBT_ENABLED + signer/RPC (ecp-mutual-eligibility-sbt); any capacity-token transfer rail keys (ecp-capacity-tokenization). Assistant cannot enter secrets.
  - Counsel sign-off before enabling: tokenized guarantee-capacity participations (securities characterization review), the soulbound on-chain claim, and any live on-chain issuance. US-only ECP determination remains the current scope.
  - Cross-app ECP federation (apparently / darwn / racefeed each recognizing the Tomorrow ECP claim) needs one intake file PER consuming repo — not queued here because each is a different PROJECT; they depend on the queued `ecp-shared-verify-package`. Say the word and a separate file per repo will be dropped.
