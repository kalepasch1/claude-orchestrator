PROJECT: apparently

# CADE (Consensus & Adversarial Determination Engine) wiring — Apparently legal surface.
# Engine already built + tested in packages/darwin-kernel/src/cade (exported @darwin/kernel/cade).
# Full spec: claude-orchestrator/CADE_IMPLEMENTATION_HANDOFF.md (§ references below).
# Repo rules: AI calls via server/utils/ai.ts; no hardcoded model strings (AI_MODELS);
# typed Supabase clients only; RLS on every table; log AI calls (ai-call-logger).

- id: cade-embedder
  title: CADE Embedder adapter (Voyage + cache, hashEmbedder fallback)
  material: no
  model: sonnet
  depends: []
  proof: `npx vitest run server/utils/cade/__tests__/embedder.test.ts` exits 0
  prompt: |
    Build server/utils/cade/embedder.ts implementing the kernel `Embedder` interface
    (`embed(text):number[]`) from @darwin/kernel/cade. Wrap the repo's existing Voyage path
    (voyage-3, 1024-dim) used by the corpus; cache by sha256(text)+model in pgvector; fall back to
    the kernel `hashEmbedder(64)` when Voyage is unavailable. Add a unit test with a stubbed Voyage
    client asserting determinism + fallback. See HANDOFF §2.1.

- id: cade-budget-guard
  title: CADE per-determination budget + recursion/cycle guard
  material: no
  model: haiku
  depends: []
  proof: `npx vitest run server/utils/cade/__tests__/budget.test.ts` exits 0
  prompt: |
    Build server/utils/cade/budget.ts: a per-determination cost ceiling, max-deep-personas cap,
    wall-clock timeout, and a cycle detector keyed on issue id (complements the kernel maxDepth).
    On breach, signal "return best tier-2 answer + escalate". Pure + unit-tested. See HANDOFF §2.3.

- id: cade-invoker
  title: CADE Invoker adapter (persona prompt + grounding + model tiering)
  material: no
  model: opus
  depends: [cade-embedder, cade-budget-guard]
  proof: `npx vitest run server/utils/cade/__tests__/invoker.test.ts` exits 0
  prompt: |
    Build server/utils/cade/invoker.ts implementing kernel `Invoker.invoke(persona,issue,tier,ctx)`.
    System prompt = persona reasoning signature (priorsTag + grounded bio) + the hard rule "cite a
    real corpus doc or do not assert" + the atemporal-grounding rule. Retrieve grounding from
    corpus_documents + legal_bot_memories + regulator_information_answers via persona.corpus_filter.
    Tier→model via AI_MODELS constants: cheap=Haiku (breadth), deep=Sonnet (debate), adversary/
    synthesis=Opus. Return PersonaPosition{stance,text,confidence,embedding(via cade-embedder),
    citations(validate exist; drop hallucinated + downweight), subQuestions(technical→councils)}.
    Prompt-cache the static prefix (cacheSystemPrefix); log every call via ai-call-logger. All calls
    through server/utils/ai.ts. Unit-test with a mocked callClaude. See HANDOFF §2.2.

- id: cade-roster-schema
  title: cade_personas / cade_proofs / cade_calibration tables (RLS, typed)
  material: yes
  model: sonnet
  depends: []
  proof: `npm run gen:types && npm run typecheck` exits 0
  prompt: |
    Add an idempotent numbered migration in supabase/ creating: cade_personas(id text pk, name,
    role text, competence jsonb, authority numeric, reliability numeric, priors_tag text,
    exploration bool default false, corpus_filter jsonb, roster_class text, version int,
    active bool default true, created_at timestamptz default now()); cade_proofs(id text pk,
    issue_id text, digest text, signature text, public_key_pem text, record jsonb, created_at);
    cade_calibration(id uuid pk default gen_random_uuid(), determination_id text, persona_ids
    text[], predicted jsonb, realized jsonb, created_at). RLS enabled on all three with explicit
    SELECT policies + service-role INSERT only on cade_proofs/cade_calibration. Regenerate types
    (gen:types). See HANDOFF §3.1/§3.3/§3.4.

- id: cade-roster-seed
  title: Seed the legal Standing Roster + mosaic + adversaries + reviewers
  material: yes
  model: opus
  depends: [cade-roster-schema]
  proof: `npx vitest run server/utils/cade/__tests__/roster.test.ts` exits 0
  prompt: |
    Build scripts/seed-cade-roster-legal.ts + server/utils/cade/roster.ts (loadRoster(domain)).
    Seed cade_personas: role='authority' for every U.S. Supreme Court Justice (historical+sitting),
    federal appellate giants, and state high-court justices (rosterClass tags), each with a
    competence vector + corpus_filter over their real opinions; role='discipline' mosaic (game
    theory, behavioral econ, philosophy, history, complexity, statistics, linguistics);
    role='adversary' (opposing counsel, skeptical regulator, trustee); role='reviewer' archetypes.
    Diversity lives in priorsTag/values, not facts. Tag a curated exploration:true heretic set.
    Test asserts loadRoster('legal') returns >= a threshold count with all required fields + at
    least one of each role. See HANDOFF §3.1/§3.2.

- id: cade-decompose-legal
  title: Decompose legal docs/opinions into IssueSpec[] contestable units
  material: no
  model: sonnet
  depends: [cade-invoker]
  proof: `npx vitest run server/utils/cade/__tests__/decompose.test.ts` exits 0
  prompt: |
    Build server/utils/cade/decompose.ts: toIssues(doc) splitting a clause set / opinion into atomic
    IssueSpec units (kind:'legal', rosterClass, requiredCompetence from a clause→competence
    classifier, materiality from a heuristic). Pure + unit-tested on a sample contract. See HANDOFF
    §0/§4.1.

- id: cade-determine-legal
  title: Wire runDetermination into legal drafting + opinion engine
  material: yes
  model: opus
  depends: [cade-roster-seed, cade-decompose-legal]
  proof: `npx vitest run server/engines/legal-bots/__tests__/cade-determine.test.ts` exits 0
  prompt: |
    Wrap server/engines/legal-bots + legal-opinion-engine.ts: per contestable unit call
    runDetermination(issue, roster, {invoker,embedder}, {sign:true,maxRounds:4,maxDepth:2}). The
    legacy multi-pass-review Haiku→Sonnet becomes the cheap tier inside the invoker. Add endpoints
    POST /api/cade/determine (one issue) and POST /api/cade/draft (decompose+determine a document),
    returning determination + dissent + certificate. Integration test with mocked invoker asserts a
    determination + proof + preserved dissent. See HANDOFF §4.1.

- id: cade-proof-store
  title: Persist + serve CADE proof packs; unified proof verifier surface
  material: yes
  model: sonnet
  depends: [cade-determine-legal]
  proof: `npx vitest run server/utils/cade/__tests__/proof-store.test.ts` exits 0
  prompt: |
    Persist Determination.proof into cade_proofs (typed client). Add GET /api/cade/proof/:id and a
    verifier surface that recomputes sha256Canonical(record), checks the embedded Ed25519 key, and
    renders the Optimality Certificate + factions + red-team + councils. Test: store→load→verify
    round-trip + tamper detection. See HANDOFF §3.3.

- id: cade-anachronism-sentinel
  title: Anachronism Sentinel (superseded/overruled index + citation check)
  material: yes
  model: opus
  depends: [cade-invoker]
  proof: `npx vitest run server/utils/cade/__tests__/anachronism.test.ts` exits 0
  prompt: |
    Build server/utils/cade/anachronism.ts: a superseded/overruled/disproven index built from the
    corpus + LegiScan/corpus-harvest/regulator feeds. The invoker checks every cited authority
    against it and forces re-argument on live ground; flag + downweight positions leaning on dead
    authority. Unit-test with a seeded overruled case. See HANDOFF §5.1.

- id: cade-advocacy-restyle
  title: Advocacy Guild restyle adapter (firewalled, semantic-diff guarded)
  material: no
  model: sonnet
  depends: [cade-determine-legal]
  proof: `npx vitest run server/utils/cade/__tests__/advocacy.test.ts` exits 0
  prompt: |
    Implement the PackageDeps.restyle(substance,reviewer,hedge) adapter (Opus) that repackages the
    FIXED determination for the modeled reviewer with honest hedging when confidence<0.7, then call
    kernel packageForReviewer (runs the semantic-diff guard). On substancePreserved===false, reject
    + retry. Test: faithful restyle preserves substance; a meaning-altering restyle is rejected.
    See HANDOFF §5.5.

- id: cade-calibration-writeback
  title: Calibration flywheel write-back (outcomes → persona reliability)
  material: yes
  model: sonnet
  depends: [cade-proof-store]
  proof: `npx vitest run server/utils/cade/__tests__/calibration.test.ts` exits 0
  prompt: |
    Write back real outcomes (opinion approved/rejected, clause litigated via filing_outcome_log)
    into cade_calibration and update each seated persona's reliability (reputation-as-stake; track
    advisor reliability separately from authority). Feed reliability into the next panel selection.
    Test: a failed-outcome write lowers the implicated personas' reliability deterministically.
    See HANDOFF §3.4/§6.1.

- id: cade-redteam-saas
  title: Red-Team-as-a-Service endpoint (Stage 5 standalone) + results page
  material: no
  model: sonnet
  depends: [cade-invoker]
  proof: `npx vitest run server/api/cade/__tests__/red-team.test.ts` exits 0
  prompt: |
    Add POST /api/cade/red-team: takes an externally-drafted clause/opinion, runs only the
    adversarial stage with adversary personas, returns the findings + a signed "we tried to break
    it" proof. Add a public results page. Test with a mocked invoker asserting findings + signed
    proof. See HANDOFF §6.5.

- id: cade-living-determination
  title: Living determinations — re-run on monitored-authority change
  material: yes
  model: sonnet
  depends: [cade-proof-store]
  proof: `npx vitest run server/utils/cade/__tests__/living-determination.test.ts` exits 0
  prompt: |
    Wire LegiScan/corpus-harvest/regulator/regulatory monitors as triggers: when a cited authority
    changes, re-run runDetermination on every stored proof whose record cited it, diff the outcome,
    and alert the customer ("your opinion's basis shifted"). Test: a simulated authority change
    re-runs exactly the determinations that cited it. See HANDOFF §5.6.

- id: cade-assessment-dimension
  title: assessCade self-assessment dimension + bot registration
  material: no
  model: haiku
  depends: [cade-determine-legal]
  proof: `npx vitest run server/utils/coordination/__tests__/assessCade.test.ts` exits 0
  prompt: |
    Add assessCade() to runFullAssessment (golden-issue recall, calibration drift,
    cost-per-determination, cache-hit rate, proof-coverage %). Register cade-roster-curator,
    anachronism-sentinel, calibration-flywheel-bot, living-determination-monitor in
    coordination-client BOT_IDENTITIES + ensureBotsRegistered. Test asserts the dimension returns a
    normalized 0..1 score. See HANDOFF §7.

OPERATOR:
  - Counsel sign-off on Tribunal/judicial-analytics data provenance before scaling reviewer modeling (public sources only).
  - Set DARWIN_SIGNING_PRIVATE_KEY_PEM (Ed25519 PKCS8) in Apparently prod env so proof packs carry the stable anchor.
  - SHARED-KERNEL TASK (no project enum slot): build the cross-product calibration interface in claude-orchestrator/packages/darwin-kernel (HANDOFF §6.1) so Apparently + Tomorrow write to one reliability store — queue as a kernel/orchestrator task.
  - Seed-roster corpus access: confirm corpus_documents coverage for state high courts before the cade-roster-seed run.
