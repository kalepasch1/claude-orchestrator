PROJECT: pareto-2080

# Port Smarter's promise/obligation intelligence into Pareto IN FULL, expressed in Pareto's own primitives.
# In Pareto this means tracking commitments arising from financial workflows + provider/counterparty comms
# ("provider said they'd match by the 10th", refund promised, rate-lock expiry, CD maturity, renewal deadline),
# reconciling them, and pre-staging NON-DISCRETIONARY one-tap follow-ups — never auto-committing.
# Ground truth + house rules (read first): server/utils/* are PURE engines with tests/*.test.js (node:test);
# server/utils/forwardDemand.js already models rate-lock/CD/renewal events; server/utils/recordMarketOutcome.js
# (requireWriteback invariant); server/utils/sessionRail.js (StagedAction prepare->commit, actor:'human');
# server/utils/counterfactualLedger.js (rollup). Endpoints under server/api/finance/*. INVARIANTS (never break):
# (1) free — no fee/affiliate/data-sale; (2) non-discretionary by default — prepare, human commits; (3) non-custodial;
# (4) agent-of-user; (5) every completed transaction writes back (requireWriteback). Conventions: requireAuth(event)+
# requireOwnership(event,uid); parseInt+isNaN guard before Prisma; `$fetch as any`; pages owning <AppNav/> use
# definePageMeta({ layout:false }); crons dual-register in nuxt.config.ts scheduledTasks AND vercel.json; NEVER store
# raw PII. Test gate: `npm test` must not regress beyond the 2 known pre-existing failures (agentLedger, bookingSaga).

- id: commitment-engine
  title: Pure commitment-ledger engine (extract + reconcile), mirroring Smarter's Obligation contract
  material: no
  model: sonnet
  depends: []
  proof: `node --check server/utils/commitmentLedger.js` exits 0 AND a new tests/commitmentLedger.test.js passes under `npm test` (no regress beyond the 2 known failures)
  prompt: |
    Add server/utils/commitmentLedger.js as a PURE engine (no Prisma import, no network). Mirror Smarter's
    Obligation shape for future S2S portability: Commitment = { owner:'me'|'provider', kind, dueDate,
    status:'open'|'fulfilled'|'overdue'|'waived', sourceRef, productKey?, providerId? }.
    - extractCommitments(messages|negotiationEvents) -> Commitment[] (deterministic heuristics; you may reuse any
      existing parsing util, but keep it pure and node:test-able).
    - reconcileCommitments(list, signals) -> auto-close fulfilled, mark overdue by dueDate, leave open otherwise.
    Add tests/commitmentLedger.test.js (node:test) covering extract + reconcile + overdue transition. Agent-of-user,
    non-custodial, no fees. Do NOT store PII — sourceRef is a message/event id, not a body.

- id: commitment-prisma-model
  title: Prisma Commitment model + ONE migration (name-checked)
  material: yes
  model: opus
  depends: [commitment-engine]
  proof: `npx prisma validate` exits 0
  prompt: |
    Add a Commitment model to prisma/schema.prisma and create ONE migration. NAME-CHECK every table/column
    against the live DB casing rules first (see CLAUDE.md "Migration Authoring — name-check before landing";
    this repo has documented P3018 casing failures). Fields: id, userId, providerId?, productKey?, kind, dueDate,
    status, sourceRef, createdAt, updatedAt. Index (userId, status) and (dueDate). NEVER store raw PII (no bodies,
    no counterparty names beyond an opaque providerId). Do NOT run `prisma migrate deploy` (operator does prod). 
    `npx prisma validate` must pass.

- id: commitment-endpoints
  title: List + reconcile endpoints, with writeback on fulfilled provider commitments
  material: yes
  model: sonnet
  depends: [commitment-prisma-model]
  proof: `node --check server/api/finance/commitments.get.js && node --check server/api/finance/commitments/reconcile.post.js` exits 0 AND `npm run check:writeback` exits 0
  prompt: |
    Add GET /api/finance/commitments (list the user's Commitment rows) and POST /api/finance/commitments/reconcile.
    Both: requireAuth(event) + requireOwnership(event, uid) + parseInt+isNaN guards. reconcile runs
    commitmentLedger.reconcileCommitments and persists status changes. When a provider commitment being fulfilled
    completes a real transaction (e.g., the matched rate is taken), call recordMarketOutcome.requireWriteback(...)
    and annotate the handler `// @writeback-required` so scripts/check-writeback-invariant.mjs passes.

- id: commitment-staged-followups
  title: Overdue/at-risk commitment -> pre-staged NON-DISCRETIONARY one-tap follow-up (daily cron)
  material: yes
  model: sonnet
  depends: [commitment-endpoints]
  proof: `node --check server/api/cron/commitment-sweep.get.js` exits 0 AND `node -e "const v=require('./vercel.json'); if(!(v.crons||[]).some(c=>c.path.includes('commitment-sweep')))process.exit(1)"` exits 0
  prompt: |
    Add server/api/cron/commitment-sweep.get.js (CRON_SECRET-gated, fail-open) and dual-register it in BOTH
    nuxt.config.ts scheduledTasks AND vercel.json crons (prod is Vercel serverless). For each overdue/at-risk
    Commitment, pre-stage a StagedAction via server/utils/sessionRail (e.g., RFQ re-fire / escalation / switch) so it
    appears in the approvals inbox. NEVER auto-commit — actor is 'human' only (invariant 2). If a swept follow-up
    completes a transaction, writeback (annotate `// @writeback-required`). parseInt guards throughout.

- id: commitment-cockpit-surface
  title: Surface commitments + counterfactual in the approvals/cockpit (5/95, one-tap)
  material: no
  model: sonnet
  depends: [commitment-staged-followups]
  proof: `npx nuxt typecheck` exits 0 (or `node --check` of the page's <script> if typecheck unavailable)
  prompt: |
    Extend pages/Approvals/User/[userId].vue (own <AppNav/> => definePageMeta({ layout:false })) to render
    Commitment items from GET /api/finance/commitments: show the 95%-resolved Outcome (auto-reconciled status), a
    single Recommended one-tap action that commits the pre-staged follow-up (POST .../staged-actions/:id/commit), and
    on-demand proof (sourceRef + counterfactual ribbon from GET /api/finance/counterfactual: captured vs forgone).
    Use design-system tokens from CLAUDE.md; cast `$fetch as any`; add sidebar + command-palette entry.

OPERATOR:
  - `npx prisma migrate deploy` for the Commitment migration against shadow DB then prod (operator only — orchestrator must not deploy DB migrations).
  - Pareto<->Smarter S2S to share ONE commitment/obligation contract + federated follow-up: needs PARETO_SMARTER_S2S_SECRET in both envs + sibling endpoints (HMAC per the _s2s helpers). Keep the contract identical to Smarter's Obligation shape.
  - Confirm legal posture (free / non-discretionary / non-custodial / agent-of-user) with counsel before any autonomous follow-up path beyond prepare-stop-at-submit.
  - Do NOT add any fee/affiliate/lead-gen/data-sale path (breaks invariant 1 and reopens the "for compensation" regulatory trigger).
