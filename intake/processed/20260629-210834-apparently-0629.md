PROJECT: apparently

# Curated regulatory corpus — remaining work. Context handoffs (read first):
#   corpus/FLYWHEEL_STRATEGY.md, corpus/IMPROVEMENTS.md
# Already DONE + deployed to Supabase oosolxvlfyifkhjohdzq (do NOT redo):
#   tables corpus_documents/clauses/clause_embeddings/citations/query_log/gaps/source_state;
#   fns search_corpus_authority, corpus_coverage, log_corpus_query, corpus_completeness, increment_corpus_fetch_count;
#   endpoints POST /api/research/query + GET /api/research/coverage; server/utils/corpus-precedent.ts;
#   server/engines/legal-bots/corpus-authority.ts wired into buildLiveRegulatoryContext;
#   corpus/scripts/{harvest-fedreg,harvest-govinfo,harvest-regulations-gov,harvest-courtlistener,extract-citations,backfill-bodies}.mjs + eval/.

- id: doc-embeddings-migration
  title: Add document-level embeddings table + embed step for semantic search
  material: yes
  model: sonnet
  depends: []
  proof: `npx vitest run server/utils/__tests__/corpus-embed.test.ts` exits 0
  prompt: |
    Today the only embeddings table is corpus_clause_embeddings (clause-level). Semantic authority search
    needs DOCUMENT-level vectors. Create supabase/migrations/<ts>_corpus_document_embeddings.sql:
    `create table corpus_document_embeddings (doc_id text primary key references corpus_documents(doc_id)
    on delete cascade, embedding vector(1024), created_at timestamptz default now());` + an HNSW
    vector_cosine_ops index + service_role RLS policy (mirror corpus_clause_embeddings exactly).
    Add an `--embed-docs` mode to corpus/scripts/ingest-to-db.mjs that selects body_fetched docs lacking
    an embedding and calls Voyage voyage-3 (reuse the existing embedClauses() Voyage call pattern; no-op
    without VOYAGE_API_KEY). Add a small pure helper `voyageBatch(texts)` and a vitest that mocks fetch and
    asserts batching + graceful no-key behavior. Do NOT run against prod (human applies the migration).

- id: hybrid-rrf-search
  title: Hybrid FTS+vector retrieval via Reciprocal Rank Fusion in /api/research/query
  material: no
  model: opus
  depends: [doc-embeddings-migration]
  proof: `npx vitest run server/utils/__tests__/rrf.test.ts` exits 0 && `npx nuxi typecheck` exits 0
  prompt: |
    Make the query layer hybrid. Add server/utils/rrf.ts exporting `rrfFuse(lists: {doc_id:string}[][], k=60)`
    (standard Reciprocal Rank Fusion: score += 1/(k+rank) per list). In server/api/research/query.post.ts,
    when an embedding for the query is available, run BOTH the existing search_corpus_authority RPC (FTS) AND
    a vector search (new RPC `match_corpus_documents(query_embedding vector, p_limit int)` over
    corpus_document_embeddings — add that SQL fn in a migration too, ordered after doc-embeddings-migration),
    then fuse with rrfFuse and return the fused order. Fail-soft: if no VOYAGE key / no embeddings, fall back
    to FTS-only (current behavior) — never break. Unit-test rrf.test.ts: fusing two ranked lists yields the
    expected blended order and dedupes by doc_id. The eval (corpus/eval/run-eval.mjs) must still pass.

- id: citation-graph-expansion
  title: 1-hop citation-graph query expansion in the research endpoint
  material: no
  model: sonnet
  depends: []
  proof: `npx vitest run server/utils/__tests__/cite-expand.test.ts` exits 0 && `npx nuxi typecheck` exits 0
  prompt: |
    After the primary search in server/api/research/query.post.ts, optionally expand results along the
    citation graph (corpus_citations): for the top hits, also fetch (a) docs they cite and (b) docs that cite
    them, via target_doc_id joins, and append as a `related_authority` array (deduped, capped at 8) with an
    `edge` field ('cites' | 'cited_by'). Gate behind a body param `expand:true` (default false). Add a pure
    helper `dedupeRelated(primary, related)` and unit-test it. Read-only; no schema change.

- id: authority-weighting
  title: Authority weighting (doc_type tier + citation in-degree) in ranking
  material: yes
  model: opus
  depends: []
  proof: `npx vitest run server/utils/__tests__/authority-weight.test.ts` exits 0
  prompt: |
    Add a corpus-PageRank-lite signal. Migration: a SQL fn `corpus_indegree()` returning (target_doc_id,
    indegree) from corpus_citations, and extend search_corpus_authority's ranking to multiply by a doc_type
    authority tier (statute/final_rule > rule_proposal/order > no_action_letter/advisory > policy_statement >
    press_release/speech) and by (1 + ln(1+indegree)*0.15). Put the tier weights + rank formula in a pure TS
    helper server/utils/authority-weight.ts and unit-test that a heavily-cited final rule outranks a one-off
    advisory at equal FTS rank. Provide the migration SQL; human applies to prod (material).

- id: superseded-freshness-alerts
  title: Surface superseded/stale authority at query time
  material: no
  model: sonnet
  depends: []
  proof: `npx nuxi typecheck` exits 0
  prompt: |
    In server/api/research/query.post.ts, join each result to its freshness/supersession status: include
    `superseded:boolean` (from corpus_documents.superseded) and, when available, a freshness flag from the
    existing citation_freshness_scores table. Add a top-level `warnings` array listing any returned doc that
    is superseded or flagged stale ("⚠ <title> may be superseded — verify before citing"). Read-only.

- id: universe-estimate-script
  title: Auto-compute universe_estimate from sequential IDs -> coverage_pct
  material: yes
  model: sonnet
  depends: []
  proof: `npx vitest run corpus/scripts/__tests__/universe.test.ts` exits 0 && `node --check corpus/scripts/compute-universe.mjs`
  prompt: |
    Sequential issuer IDs (CFTC release numbers like 9259-26, SEC AP numbers) make the universe size
    COMPUTABLE. Write corpus/scripts/compute-universe.mjs: for each source with sequential ids, derive
    universe_estimate (max-id - min-id within a year, summed across covered years) and UPDATE
    corpus_source_state.universe_estimate + coverage_pct. Factor the math into a pure helper
    `estimateUniverse(ids:string[])` and unit-test it on synthetic CFTC-style ids. The DB write path is
    material (writes prod source_state) — gate for human; the pure helper + node --check are the proof.

- id: gap-autoharvest-cron
  title: Gap -> targeted-harvest cron (self-healing completeness)
  material: yes
  model: sonnet
  depends: []
  proof: `node --check corpus/scripts/gap-harvest.mjs` && `npx nuxi typecheck` exits 0
  prompt: |
    Close loop 3 of the flywheel. Add server/api/cron/corpus-gap-harvest.get.ts (CRON_SECRET-gated, mirror an
    existing cron route) that reads corpus_gaps WHERE status='open', and for each enqueues a targeted harvest
    (set status='harvesting'; write a coordination job row the existing harvesters can consume — reuse the
    regulator-artifact pipeline pattern). Add corpus/scripts/gap-harvest.mjs as the worker that runs the
    matching harvester for a gap's query/source then marks status='filled'. Register the cron in vercel.json +
    nuxt.config scheduledTasks (daily). Material (cron/prod).

- id: wire-precedent-finalize
  title: Index finalized opinions as precedent (drafts-as-precedent loop)
  material: yes
  model: sonnet
  depends: []
  proof: `npx nuxi typecheck` exits 0
  prompt: |
    server/utils/corpus-precedent.ts::indexWorkProductPrecedent() exists but is never called. Call it on the
    opinion-finalize path in server/engines/legal-opinion-engine.ts (after an opinion is approved/finalized),
    fire-and-forget (.catch(()=>{})), passing { title, body, vertical, outcome, refId }. Touches the legal
    work-product path -> material. Verify typecheck stays at 0 errors.

- id: coverage-ops-page
  title: Ops dashboard page for corpus completeness + gaps
  material: no
  model: sonnet
  depends: []
  proof: `npx nuxi typecheck` exits 0
  prompt: |
    Add pages/ops/corpus-coverage.vue (ops-session gated, mirror an existing /ops page) that calls
    GET /api/research/coverage and renders: per-source captured / universe_estimate / coverage_pct bar,
    open-gaps list, and recent-query "answered-from-corpus" rate. No new endpoint needed.

- id: commit-corpus-migrations
  title: Commit the MCP-applied corpus migrations as repo files (parity)
  material: yes
  model: haiku
  depends: []
  proof: `ls supabase/migrations | grep -c corpus` prints >= 3
  prompt: |
    The corpus schema was applied to prod via the Supabase MCP but isn't in supabase/migrations/. Write the
    exact applied SQL as timestamped migration files for parity: corpus_legal_ingestion (tables + FTS),
    corpus_research_query_and_source_state (search/coverage/source_state fns), corpus_fetch_count_usage_ranking,
    corpus_v2_graph_log_gaps (citations/query_log/gaps + upgraded search + log_corpus_query + completeness).
    Copy the SQL from the deployed objects (see corpus/IMPROVEMENTS.md). These already exist in prod, so this
    is repo parity only. Material (migrations).

OPERATOR:
  - Set VOYAGE_API_KEY in CI + prod env to activate the semantic layer (doc-embeddings + hybrid-rrf).
  - Trigger .github/workflows/corpus-harvest.yml with COURTLISTENER_API_TOKEN + REGULATIONS_GOV_API_KEY + GOVINFO_API_KEY to run the completeness walk (fills universes to 100%, backfills bodies, computes embeddings).
  - Approve prod apply of doc-embeddings, authority-weighting, universe-estimate, gap-autoharvest migrations/writes.
