PROJECT: pareto-2080

# W18 — Platform / market-creation layer on top of W16 (collective brain) + W17 (scale & safety).
# These turn Pareto from "a smarter agent" into "the marketplace other agents and products clear
# through": a vendor-facing demand exchange, demand-as-an-asset credentials, an agent-to-agent
# commerce protocol, a unified cross-product experience passport, a generative marketplace simulator
# to train policies in-silico, verifiable/ZK outcome trust, collective action beyond price, and a
# self-extending roadmap engine. Build on engines ALREADY IN REPO (server/utils/: coordinationMesh,
# contingentCommitment, fairDivision, experienceGraph, vendorGraph, negotiationSelfPlay,
# vendorTwinService, anonymize, agentLedger, darwinCapabilities, simHarness, worldModel,
# trustGraduation) and on the W17 engines once they land (offPolicyEval, robustAggregation,
# truthfulClearing — compose them WHEN PRESENT, with a graceful fallback if not). Each task is a NEW
# pure-logic engine (ESM, no external deps, seedable RNG via mulberry32) + node:test suite, sometimes
# wiring one endpoint. DO NOT rewrite existing engines. Conventions: parseInt+isNaN before Prisma;
# requireAuth + requireUserIdOwnership in handlers; anonymize at WRITE time; HMAC signing reuses the
# agentLedger AGENT_SIGNING_SECRET pattern; money in USD. Test gate: `npm test` must not regress
# beyond the 2 known pre-existing failures (agentLedger, bookingSaga — Prisma-binary only).

- id: demand-exchange-engine
  title: Vendor-facing demand exchange — vendors bid on forming coalitions; clear the auction
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/demandExchange.test.js` exits 0
  prompt: |
    Flip the funnel: expose forming-coalition demand to vendors who BID for it. Add
    server/utils/demandExchange.js (pure): take a coalition (coordinationMesh.formCoalitions output) +
    a set of vendor bids {vendorId, price, qty, terms} and clear a sealed-bid auction that maximizes
    member welfare (lowest qualifying price that fills the coalition's totalQty within its clearingWtp),
    composing contingentCommitment.clearReverseAuction for the demand curve and truthfulClearing (when
    present) for incentive-compatible payments. Export buildDemandBook(coalitions) (vendor-viewable,
    pseudonymous, from coordinationMesh.publicSnapshot — NO identities) and clearExchange(coalition, bids)
    -> { winner, clearingPrice, fill, perMemberPrice, allocation }. Add tests/demandExchange.test.js
    proving the lowest-price filling bid wins, partial fills are handled, and no bid above clearingWtp can
    win. Material: vendor-side market clearing. Keep `npm test` green.

- id: demand-exchange-endpoint
  title: Demand-exchange endpoints — vendor book (read) + bid submission (write)
  material: yes
  model: sonnet
  depends: [demand-exchange-engine]
  proof: `node --check server/api/exchange/book.get.js && node --check server/api/exchange/bids.post.js` exits 0
  prompt: |
    Add GET /api/exchange/book — returns demandExchange.buildDemandBook over open MeshCoalition rows
    (pseudonymous aggregates only; enforce the k-anonymity floor; NO userIds). Add
    POST /api/exchange/bids — a vendor submits a bid against a coalition; persist it and, when the
    coalition is full, run demandExchange.clearExchange and write the result back as a MeshCoalition status
    transition + (optionally) instantiate the BuyingPool via the existing coalition->pool bridge. Vendor
    auth is via a vendor API key (not user auth); user-side reads still requireAuth. parseInt guards.
    node --check both files; keep `npm test` green.

- id: demand-credential
  title: Verified-demand credential — issue/verify a portable, signed, PII-free demand instrument
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/demandCredential.test.js` exits 0
  prompt: |
    Make aggregated demand a portable instrument. Add server/utils/demandCredential.js (pure): issue a
    credential {productKey, spec, window, wtpBand, memberCount, kCohort, issuedAt, nonce} that attests a
    block of verified demand WITHOUT any PII, signed with HMAC-SHA256 (reuse the agentLedger signing
    pattern; key from env). Export issueDemandCredential(block, secret), verifyDemandCredential(cred,
    secret), and isFresh(cred, ttl). Enforce a k-anonymity floor (memberCount >= k) before issuance. Add
    tests/demandCredential.test.js proving a tampered credential fails verification, sub-k blocks are
    refused, and the payload contains no identity fields. Material: it is a tradeable instrument + signing.
    Keep `npm test` green.

- id: a2a-protocol
  title: Agent-to-agent negotiation protocol + capability attestation (the interop core)
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/a2aProtocol.test.js` exits 0
  prompt: |
    Define the machine-to-machine negotiation protocol so a Pareto agent can transact with a vendor agent.
    Add server/utils/a2aProtocol.js (pure): a typed message schema (HELLO, OFFER, COUNTER, ACCEPT,
    REJECT, SETTLE) + a deterministic state machine validating legal transitions; capability attestation
    = a signed authorization token {agentId, userId, maxAmountUsd, category, expiresAt} (reuse the
    agentLedger mintApprovalToken/verifyApprovalToken HMAC pattern) that proves an agent may commit on a
    user's behalf up to a cap. Export buildMessage, validateTransition(prev, next), mintCapability,
    verifyCapability, and enforceCap(capability, amountUsd). Add tests/a2aProtocol.test.js proving illegal
    transitions are rejected, an expired/over-cap capability cannot SETTLE, and a tampered capability
    fails verification. Material: governs authorized money commitment. Keep `npm test` green.

- id: a2a-endpoint
  title: A2A negotiation endpoint — server-side protocol handshake
  material: yes
  model: sonnet
  depends: [a2a-protocol]
  proof: `node --check server/api/a2a/negotiate.post.js` exits 0
  prompt: |
    Add POST /api/a2a/negotiate implementing one step of a2aProtocol: accept an inbound message + the
    counterparty's capability token, validate the state transition and capability (verifyCapability +
    enforceCap), consult vendorTwinService for the response move, and return the next protocol message.
    On SETTLE, route through agentLedger (Tier-C signed token) — never auto-move money without the
    existing approval flow. requireAuth for the user-initiated side; verify the counterparty capability
    for the agent side. parseInt guards. node --check; keep `npm test` green.

- id: experience-passport
  title: Unified cross-product experience passport (portable trust/experience claims)
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/experiencePassport.test.js` exits 0
  prompt: |
    Make the corpus ONE asset across products. Add server/utils/experiencePassport.js (pure): build a
    portable, signed, anonymized passport of a user's PORTABLE claims — reliability score
    (commitmentTrust when present), negotiation-experience bands, trust-lane tiers (trustGraduation) —
    with NO raw PII, following the buildPassport/claim shape in DARWIN_KERNEL_ADOPTION.md. Export
    buildExperiencePassport(subjectClaims, secret), verifyPassport(passport, secret), and
    selectClaims(passport, scopes) for minimal disclosure. Add tests/experiencePassport.test.js proving
    verification, that only whitelisted claim types serialize (no PII leakage), and minimal-disclosure
    scoping works. Material: cross-product identity/claims. Keep `npm test` green.

- id: market-generative-sim
  title: Generative marketplace simulator (users + vendors + mesh) for in-silico training
  material: no
  model: opus
  depends: []
  proof: `node --test tests/marketGenerativeSim.test.js` exits 0
  prompt: |
    Move learning off the costly real world. Add server/utils/marketGenerativeSim.js (pure, seeded):
    a parametric generative model that samples synthetic users (WTP/flexibility/goal distributions) and
    vendors (concession structures), runs them through the real coordinationMesh + contingentCommitment +
    vendorGraph twins, and emits synthetic-but-structurally-valid experiences + market outcomes. Export
    simulateMarket({nUsers, nVendors, rounds, seed}) -> { experiences, coalitions, outcomes, stats }.
    Reuse simHarness invariants. Add tests/marketGenerativeSim.test.js proving determinism under a fixed
    seed, no NaN/oob outputs, and monotonic stats (more buyers -> larger coalitions -> better clearing).
    Keep `npm test` green.

- id: sim-policy-trainer
  title: Train + validate negotiation/coalition policies entirely in the generative sim
  material: no
  model: opus
  depends: [market-generative-sim]
  proof: `node --test tests/simPolicyTrainer.test.js` exits 0
  prompt: |
    Add server/utils/simPolicyTrainer.js (pure): train a policy (negotiation strategy / coalition-join
    rule) against marketGenerativeSim by evaluating candidates over many seeded markets and keeping the
    best by expected member welfare; before "deploying", validate with offPolicyEval (when present) on the
    logged real experiences so a sim-overfit policy is caught. Export trainPolicy({generations, seed}) ->
    { policy, simScore, validation } and evaluatePolicy(policy, markets). Add tests/simPolicyTrainer.test.js
    proving the trained policy beats a random/naive baseline in-sim and that the validation hook runs.
    Keep `npm test` green.

- id: attested-outcomes
  title: Cryptographically attested outcomes (tamper-evident experiences)
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/attestedOutcome.test.js` exits 0
  prompt: |
    Kill poisoning at the source by making outcomes VERIFIABLE, not just reported. Add
    server/utils/attestedOutcome.js (pure): wrap an outcome artifact (quote, closed-deal record) with a
    signature/commitment (HMAC or hash-chain) so it is tamper-evident; on capture, route attested outcomes
    into a TRUSTED tier and unattested ones into an untrusted/quarantine tier that robustAggregation
    down-weights. Export attestOutcome(artifact, secret), verifyOutcome(attested, secret), and
    trustTier(attested). Wire trustTier into the experience-capture helper so the graph records
    provenance. Add tests/attestedOutcome.test.js proving tamper detection and correct tiering. Material:
    root-of-trust for the corpus. Keep `npm test` green.

- id: zk-eligibility
  title: Zero-knowledge-style eligibility/demand proofs (prove qualification without revealing data)
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/zkEligibility.test.js` exits 0
  prompt: |
    Let a user prove they qualify (income band, demand WTP threshold) WITHOUT revealing the underlying
    figure. Add server/utils/zkEligibility.js (pure, dependency-free): a commitment + threshold-proof
    scheme — Pedersen-style or hash-commitment with a disjunctive range proof over coarse bands — that
    proves "value >= threshold" or "value in band B" given a commitment, without disclosing the value.
    Export commit(value, salt), proveThreshold(value, salt, threshold), verifyThreshold(commitment, proof,
    threshold). Be explicit in comments about the cryptographic assumptions/limits of the dependency-free
    construction. Add tests/zkEligibility.test.js proving a true statement verifies, a false one fails, and
    the proof transcript does not contain the raw value. Material: privacy + eligibility gating. Keep
    `npm test` green.

- id: collective-action
  title: Collective action beyond price — non-purchase coalitions (appeals, bargaining, shared services, time-banking)
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/collectiveAction.test.js` exits 0
  prompt: |
    Widen the mesh from group-buying to collective agency. Add server/utils/collectiveAction.js (pure):
    model new coalition goalTypes that don't clear on price — group fee-dispute/appeal pools (value =
    expected recovery × success prob ÷ effort), collective bargaining blocks (insurer/employer), shared
    single-provider hire (split one tax pro across N), and reciprocal time/skill banking (a balanced
    favor-exchange ledger). Export valueOfCollectiveAction(kind, params), formActionCoalition(requests),
    and timeBankSettle(ledger) (balances reciprocal credits). Reuse coordinationMesh quorum/compat ideas.
    Add tests/collectiveAction.test.js proving appeal-pool value scales with success prob, bargaining
    power rises with members, and time-bank settlement conserves credits. Keep `npm test` green.

- id: roadmap-synthesizer
  title: Self-extending roadmap engine — auto-draft the next orchestrator task block from value gaps
  material: no
  model: sonnet
  depends: []
  proof: `node --test tests/roadmapSynthesizer.test.js` exits 0
  prompt: |
    Close the loop on the platform itself. Add server/utils/roadmapSynthesizer.js (pure): read summaries
    of the experience/value graphs (coverage by goalType, playbook confidence, where realized value is
    leaking vs potential) and emit a RANKED list of the highest-leverage missing capabilities, each
    rendered as a well-formed orchestrator intake task block (id/title/material/model/depends/proof/prompt
    — the exact schema in this file). It PROPOSES only; humans still gate material work. Export
    detectGaps(graphStats) and renderIntakeBlock(gap). Add tests/roadmapSynthesizer.test.js proving the
    highest-leverage gap ranks first and that renderIntakeBlock emits a parseable block with every required
    field. Keep `npm test` green.

OPERATOR:
  - Tomorrow market-making over demand credentials: Tomorrow must accept/verify demandCredential tokens and make markets in demand-forwards — cross-repo (tomorrow), needs the shared signing key + a Tomorrow-side endpoint. Cannot be a single pareto task.
  - Publish the A2A protocol as an interop SPEC (versioned, public) so vendor agents can implement it — a standards/docs effort + a vendor sandbox; coordinate cross-repo via the Darwin kernel.
  - Cross-product passport consumption: Smarter/Apparently/Tomorrow must verify experiencePassport claims (shared key + sibling endpoints, HMAC per the _s2s helpers) — cross-repo enablement.
  - Demand exchange + A2A settlement touch real money movement and a new vendor-facing surface — counsel review on demand aggregation, vendor ToS, and the non-custodial/agent-of-user posture before wide rollout; vendor API-key issuance + secrets are operator-managed.
  - ZK/attestation as a production root-of-trust: have the dependency-free constructions reviewed (and likely replaced with a vetted crypto lib) by security before they gate trust at scale.
