PROJECT: smarter

- id: smarter-fix-htui-path
  title: Fix hardcoded @ht/ui absolute path (CI blocker)
  material: no
  model: haiku
  depends: []
  proof: npm run build succeeds; grep shows no absolute /Users/.../ht-ui alias in config
  prompt: |
    @ht/ui is aliased to an absolute path (/Users/.../apparently/packages/ht-ui), which breaks
    CI and other machines. Convert it to a workspace dependency or published package reference.
    Remove the absolute-path alias from config. See HANDOFF_MASTER / Darwin C2.

- id: smarter-shell-3surface
  title: 3-surface shell — Now / Ask / Approve as primary chrome
  material: no
  model: sonnet
  depends: [smarter-get-green]
  proof: npx vue-tsc --noEmit exits 0; default route renders Now/Ask/Approve; seed demo intact
  prompt: |
    HANDOFF_MASTER Workstream 2. Make Now/Ask/Approve the primary chrome (default route /),
    demote the 33-item nav to an "All tools" drawer, keep the Live HUD overlay. Wire Ask to
    askRouter and Approve to approvals (TrustBadge + sources on every card). First-run: "I found
    N things I can handle and M that need you." Mock-degradable. Do NOT rebuild engines.

- id: smarter-surface-engines
  title: Surface newest engines as pages (Campaigns, Filing, Compliance Cal, Legacy, Capture)
  material: no
  model: sonnet
  depends: [smarter-get-green]
  proof: npx vue-tsc --noEmit exits 0; new pages render on seed; nav entries added
  prompt: |
    HANDOFF_MASTER Workstream 3. Build pages: /campaigns (live grid over getCampaigns/tick/rollup),
    /filing (requirements->validate report + submission-policy toggle, human-tap default),
    compliance calendar surfaced in Now/Approve, legacy artifacts per matter, manual capture +
    contact directory. Mobile-first. Mastery dashboard is a SEPARATE owner-auth task — exclude here.

- id: smarter-owner-auth-privacy
  title: Owner-auth gate for Mastery + anonymization pass
  material: yes
  model: sonnet
  depends: [smarter-get-green]
  proof: tests pass; /api/admin/mastery* returns 401/403 without ops auth
  prompt: |
    HANDOFF_MASTER Workstream 4 (CRITICAL/privacy). Gate /api/admin/mastery* and the mastery UI
    behind ops/admin auth — never exposed to firm users. Add an anonymization/strip pass so ONLY
    process knowledge (no client identities/privileged content) enters the Mastery Library +
    contact directory. MATERIAL — requires human approval before merge.

- id: smarter-integration-glue
  title: Wire engines into the loops (matter close, manual step, campaigns, legacy DoD)
  material: no
  model: sonnet
  depends: [smarter-surface-engines]
  proof: npx vue-tsc --noEmit exits 0; integration tests/seed flows pass
  prompt: |
    HANDOFF_MASTER Workstream 5. On matter close -> dealBible + masteryLibrary.recordRecipe +
    resume ingestAssignment + worth refresh. After a manual step -> manualCapture.recordManualStep
    then offer swarm takeover when ready. Campaign tick + compliance renewals + status rollups via
    scheduled tasks surfaced in Now/Approve. Legacy requiredLegacyArtifacts as matter-runner DoD.

- id: smarter-distillation-enforce
  title: Route all learning through the distillation gate + enforce prepareForModel
  material: yes
  model: sonnet
  depends: [smarter-get-green]
  proof: tests pass; a test asserts assertNoClientData() blocks client identifiers pre-persist
  prompt: |
    HANDOFF_MASTER addendum 1-2 (privilege-safe learning). Route every path that persists into the
    learning store / Mastery Library / hivemind (manualCapture, swarm playbook authoring, dealBible,
    postMortem, contact directory) through distill()/redact(); call assertNoClientData() as a
    pre-persist guard and log "0 client identifiers persisted" to trust receipts. Enforce
    dataPosture.prepareForModel() at the LLM boundary so personal-portal mode strips identifiers
    before any external model call. Do NOT rebuild the engines — wire + enforce. MATERIAL.

- id: smarter-email-as-rail
  title: Retire the inbox UI — email becomes transmission rail only
  material: no
  model: sonnet
  depends: [smarter-shell-3surface]
  proof: npx vue-tsc --noEmit exits 0; no inbox to-do/filing UI remains; Now/Approve is the surface
  prompt: |
    HANDOFF_MASTER addendum 5. Eliminate the inbox as an admin surface. Inbound mail is ingested ->
    triaged -> drafted -> resolved by the swarm and surfaced (only if needed) as a Now/Approve item;
    outbound goes silently. Remove/retire any "inbox" UI used as a to-do/filing/status tool.

OPERATOR:
  - Live provider keys: ANTHROPIC_API_KEY (for in-app AI), Gmail/Graph OAuth, DOCUSIGN_*, eBilling/DMS creds
  - Desktop-control auto-filer (NMLS/state portals) — per-portal authorization + human-tap on submit
  - Real-time WebSocket host; PWA/mobile shell; SOC2 + in-tenant/BYO-model; real NER redactor behind distillation
