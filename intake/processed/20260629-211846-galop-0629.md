PROJECT: galop

- id: broadcast-odds-ticker
  title: Animate the broadcast odds board on live price moves (steamer/drifter)
  material: no
  model: sonnet
  depends: []
  proof: `cd racefeed && npx tsc --noEmit` exits 0
  prompt: |
    The galop git repo is the Expo/RN app under racefeed/ (it has its own .git). A prior session
    added racefeed/components/feed/BroadcastOverlay.tsx exporting LiveBug, LowerThird, and
    BroadcastStandings (an on-air odds board ranked by de-vigged market probability), token-driven
    (constants/colors.ts + constants/typography.ts, <AppText>, <AppIcon>), reduced-motion via the
    repo's useReducedMotion hook.
    Deliverable: make BroadcastStandings react to live odds deltas — when a runner's price shortens
    (steamer) or drifts, show a small up/down arrow + a brief flash on the changed row, driven off
    the existing market-move data the feed already carries (grep for market_move / odds fields in
    the live feed hook). Gate the flash/animation on useReducedMotion (no motion → static arrow
    only). Keep it a reusable prop-driven component; no new palette (Colors.* / silkColor only).
    Verify the racefeed app typechecks clean.

- id: broadcast-replay-parity
  title: Reuse the broadcast layer on the race result/replay screen
  material: no
  model: sonnet
  depends: []
  proof: `cd racefeed && npx tsc --noEmit` exits 0
  prompt: |
    Drop the existing LowerThird + BroadcastStandings (from
    racefeed/components/feed/BroadcastOverlay.tsx — already export-ready) into the race
    result/replay screen at racefeed/app/race/[id].tsx so the replay reads with the same
    broadcast/TV-graphics language as the live feed. Reuse the screen's existing race + field data;
    do not refetch differently. Token-driven, <AppText> variants, reduced-motion safe, a11y labels on
    the standings rows. Verify racefeed typechecks clean.

- id: photo-finish-results-bug
  title: Animated winner "results bug" lower-third at race resolution
  material: no
  model: sonnet
  depends: [broadcast-replay-parity]
  proof: `cd racefeed && npx tsc --noEmit` exits 0
  prompt: |
    Add a winner "results bug" — a lower-third that animates in when a race resolves, announcing the
    winner (and a one-line result), reusing the timing/idiom of the existing WinCelebration /
    RaceReveal components and the LowerThird styling from BroadcastOverlay.tsx. Gate the entrance
    animation on useReducedMotion (reduced motion → appears statically). Token-driven, a11y
    (accessibilityLiveRegion/label on the result). Wire it into the resolution path on the
    live/replay surface. Verify racefeed typechecks clean.

OPERATOR:
  - (none — RN type-gated; any app-store build/submit is a separate release step.)
