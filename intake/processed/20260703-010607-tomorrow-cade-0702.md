PROJECT: tomorrow

# CADE war-room position + recipient-alignment engine. Distinct from the ECP
# credentialing intake (tomorrow-0702.md) — different subsystem. Reference:
# apparently CLAUDE.md "Position engine v1–v4" + apparently canonical modules.

- id: cade-restore-warroom-position-engine
  title: Restore + verify the war-room position + recipient-alignment engine
  material: yes
  model: sonnet
  depends: []
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    The war-room position engine + recipient-alignment layer were implemented but
    this worktree periodically wipes BOTH tracked and untracked files (an auto-commit
    bot had committed `server/utils/warRoom/positionEngine.ts` in a broken 194-line
    block-only state). Recreate the two modules FULL + correct, using the apparently
    canonical files as reference, adapting model calls to war-room `aiCall`:
      * `server/utils/warRoom/positionEngine.ts` — port from
        `apparently/server/engines/legal-bots/position-engine.ts`, keeping the war-room
        top half (mandateFromDraftPosture, draftSignalsToLedger, makeAiCallAdversaries
        over `~/server/utils/aiConfig` aiCall, assessClausePosition) PLUS the identical
        SHARED POSITION CORE (computeWinRateKPI, auditOverrides, solveMultiParty,
        buildFallbackLadder, routeByTenability). Byte-identical shared-core outputs
        (golden-vector lockstep vs apparently/smarter).
      * `server/utils/warRoom/recipientAlignment.ts` — port
        `apparently/server/engines/legal-bots/recipient-alignment.ts` verbatim (pure
        core: RECIPIENT_REGISTRY, resolveRecipient, scoreAlignment, runAlignmentPass,
        buildRecipientContext, computeAlignmentKpi, heuristicLearnProfile,
        learnRecipientProfile) + add `makeAiCallAligner()` over aiCall (reviewer +
        reviser, parseJson).
    Verify strict typecheck, then COMMIT (git push now unblocked in
    `.claude/settings.local.json`) so the reset can no longer wipe them.

- id: cade-warroom-clause-wiring
  title: Wire position/alignment through clauseDrafter + clause-drafts endpoint
  material: yes
  model: sonnet
  depends: [cade-restore-warroom-position-engine]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    In `server/utils/warRoom/clauseDrafter.ts`: import from `./positionEngine` and
    `./recipientAlignment`; add `ClauseDraftOptions` (mandate | positionInstruction,
    mode, humanCounselOverride, maxRounds, recipientId, recipientPerson); add a 4th
    `positionOpts?` param to `generateClauseDrafts`; extend `DraftResult` with
    `position` (assessClausePosition of the recommended draft; Red grounded in the
    already-retrieved `precedents` via precedentContext) and `alignment`
    (alignForRecipient with makeAiCallAligner). In
    `server/api/firm/rooms/[id]/war-room/clause-drafts.post.ts` thread the body fields
    into `positionOpts` and return `positionBlocked`. Fail-soft, backwards-compatible.

- id: cade-warroom-clause-panel-ui
  title: Surface position/alignment in ClauseDraftPanel + war-room page
  material: no
  model: sonnet
  depends: [cade-warroom-clause-wiring]
  proof: `npx vue-tsc --noEmit` exits 0
  prompt: |
    Add `components/warRoom/PositionPanel.vue` (port the self-contained scoped-CSS
    component from `apparently/app/components/legal-opinion/PositionPanel.vue`:
    tenability band/meter, self-service gate banner + counsel-override form, weakness
    ledger, and the recipient-concurrence section with anticipated RFIs). Add optional
    `position`/`alignment` props + a `positionOverride` emit to
    `components/warRoom/ClauseDraftPanel.vue` and render `<PositionPanel>` at the top.
    In `pages/firm/negotiations/[id]/war-room.vue`, bind `:position`/`:alignment` from
    each clause's DraftResult and handle `@positionOverride`.

- id: cade-warroom-position-tests
  title: Vitest for the war-room position + alignment engine
  material: no
  model: haiku
  depends: [cade-restore-warroom-position-engine]
  proof: `npx vitest run server/utils/warRoom/__tests__/positionEngine.test.ts` exits 0
  prompt: |
    Recreate `server/utils/warRoom/__tests__/positionEngine.test.ts`: mandate
    resolution; draftSignalsToLedger → materially-weak → gate blocked; override clears
    / attorney_supervised allows; injected Red/Blue loop converge + irreducible-critical
    → blocked; shared core (computeWinRateKPI lift, solveMultiParty weakest party,
    buildFallbackLadder recommended rung, routeByTenability). Injected — no live model.

OPERATOR:
  - The tomorrow worktree reverts uncommitted TRACKED files to HEAD and an auto-commit bot committed positionEngine.ts broken; a human must confirm the corrected files are committed to HEAD (git push now allowed) or restores keep reverting.
