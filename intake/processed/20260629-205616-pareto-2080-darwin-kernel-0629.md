PROJECT: pareto-2080

- id: darwin-agentledger-govern
  title: Wire @darwin/kernel govern() into the Tier-A/B/C agent spine + persist receipts
  material: yes
  model: sonnet
  depends: []
  proof: `node --experimental-strip-types --test server/utils/darwin/__tests__/govern.test.ts` exits 0
  prompt: |
    The kernel is already vendored at vendor/darwin-kernel and an adapter exists at
    server/utils/darwin/govern.ts (exports govern(action, prevReceipt)). Activate it.
    Steps (additive — do NOT change existing tier logic):
    1. In server/utils/agentLedger.js, before an action transitions to
       awaiting_approval/approved/executing, call govern({type, actor, userId,
       amountUsd, metadata}) and capture {decision, receipt}. Tier-C money moves
       already escalate via the kernel §1a override; keep the existing signed Tier-C
       token flow intact.
    2. Persist the returned receipt to a new darwin_receipts row (append-only) via the
       Supabase client. If the table is absent, fail-soft (log + continue) so the path
       never crashes — the migration is an OPERATOR step.
    3. Add test server/utils/darwin/__tests__/govern.test.ts that constructs a sample
       Tier-B action, runs govern(), and asserts verifyReceipt(receipt) === true and
       decision is one of allow|escalate|deny.
    See DARWIN_KERNEL_ADOPTION.md (Wire 1) in this repo for the exact recipe.

- id: darwin-capabilities-seed
  title: Publish Pareto engine capabilities at startup (Wire 2)
  material: no
  model: sonnet
  depends: []
  proof: `node --experimental-strip-types scripts/darwin-metering-proof.ts` prints "OK: real Pareto engine ran through the metered ledger end-to-end"
  prompt: |
    server/utils/darwin/capabilities.ts already builds the Pareto capability specs and a
    MeteredRegistry whose handlers call the REAL engines (simulateRetirement etc.), and
    scripts/darwin-metering-proof.ts already proves it.
    Steps:
    1. Add a Nitro server plugin server/plugins/darwinCapabilities.ts that, on startup,
       calls publishAndMeter('pareto') (or publishes paretoCapabilitySpecs to the shared
       registry transport) so monte_carlo/allocator/deduction_optimizer/geo_arbitrage are
       discoverable. Guard so it runs once and never throws on boot (fail-soft).
    2. Extend paretoEngineHandlers() in capabilities.ts to wire the allocator,
       deduction_optimizer, and geo_arbitrage capability ids to their real engine
       functions (server/utils/{allocator,deductionOptimizer,geoArbitrage}.js), mapping
       inputs the same way monte_carlo is mapped.
    3. Confirm scripts/darwin-metering-proof.ts still runs green.

- id: darwin-subtree-vendor
  title: Replace the copy-vendored kernel with a real git subtree
  material: yes
  model: sonnet
  depends: [darwin-capabilities-seed]
  proof: `git log --oneline -- vendor/darwin-kernel | head -1` is non-empty AND `node --experimental-strip-types scripts/darwin-metering-proof.ts` exits 0
  prompt: |
    Today vendor/darwin-kernel is a plain copy. Make it a canonical git subtree so the
    kernel can be re-synced with history.
    Steps:
    1. From the orchestrator repo (env ORCHESTRATOR_REPO, default ../beethoven/claude-orchestrator):
       `git subtree split -P packages/darwin-kernel -b _kernel_split` then in THIS repo
       `git subtree add --prefix vendor/darwin-kernel <orchestrator-path> _kernel_split --squash`.
       If no git remote/ref is available, KEEP the copy-vendor and instead add
       scripts/vendor-darwin-kernel.sh to a CI step — do NOT block on the subtree.
    2. Ensure package.json keeps `"@darwin/kernel": "file:vendor/darwin-kernel"`.
    3. Re-run the proof script to confirm nothing broke.

OPERATOR:
  - Apply vendor/darwin-kernel/sql/0001_darwin_kernel.sql to the Pareto Supabase project (creates darwin_receipts and the other additive darwin_* tables, RLS-enabled). Name-check first.
  - Set DARWIN_SIGNING_PRIVATE_KEY_PEM in the Pareto host env (a stable Ed25519 PKCS8 PEM) so receipts share the portfolio trust anchor.
