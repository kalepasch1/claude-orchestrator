PROJECT: smarter

# Elevate Smarter's EXISTING comms intelligence to its own full autonomy bar — do NOT rebuild what exists.
# Ground truth (read these first): server/utils/obligations.ts (promise/obligation extract + reconcile,
# Obligation = {owner:'me'|'them', dueDate, status:'open'|'fulfilled'|'waived', sourceQuote, sourceMessageId}),
# server/utils/governance.ts (trust dial counsel_only|co_pilot|auto_pilot, streak auto-promote AUTO_PROMOTE_THRESHOLD=20,
# autonomy cockpit receipts), server/utils/policy.ts (evaluateAction -> PolicyDecision+PolicyReceipt;
# KILL_SWITCH_GATED=['auto_send','external_share','sign_document']; UPL boundary), server/utils/llm.ts
# (getAnthropicClient + MODELS{classify:haiku,draft/summarize/warroom:sonnet}), matters = ProjectItem in
# types/index.ts with obligations[]+tasks[]+threadIds, stores/app.ts (monolithic Pinia), @ht/ui design tokens,
# Supabase (NOT Prisma), server/api/{domain}/{action}.{method}.ts. Stack: Nuxt3+TS+Pinia. compromise NLP lib available.
# IMPORTANT: there is currently NO test runner — task 1 adds one; every later proof depends on it.

- id: smarter-test-harness
  title: Add a vitest harness + first contract tests for the pure utils (currently none)
  material: no
  model: sonnet
  depends: []
  proof: `npx vitest run` exits 0
  prompt: |
    Smarter has no test runner. Add vitest (+ @vue/test-utils if needed), a vitest.config, and a
    `"test": "vitest run"` script in package.json. Write contract-shape smoke tests for the pure,
    side-effect-free parts of: server/utils/obligations.ts (extract + reconcile shapes), server/utils/
    policy.ts (evaluateAction returns PolicyDecision+receipt; KILL_SWITCH_GATED gating), server/utils/
    governance.ts (trust-dial resolution + streak auto-promote). Mock the Anthropic client; do NOT call the
    network. This unblocks proofs for every other task in this file. Keep it green.

- id: obligations-llm-extraction
  title: LLM-backed obligation extraction (regex stays as fallback), behind model policy
  material: no
  model: sonnet
  depends: [smarter-test-harness]
  proof: `npx vitest run test/obligations.spec.ts` exits 0
  prompt: |
    Upgrade extractObligations in server/utils/obligations.ts from heuristic PROMISE_PATTERNS/DUE_HINTS to
    an LLM-backed extractor via server/utils/llm.ts. Add a MODELS.extract key (haiku) rather than hardcoding.
    Extract bidirectional commitments (owner 'me'|'them', dueDate, sourceQuote, sourceMessageId) from a thread.
    KEEP the existing regex path as a deterministic fallback when: no LLM key, or workspace storagePolicy ==
    'strict' (no email-body to model). Do NOT change the Obligation type contract consumed by matters/ProjectItem.
    Tests: labeled fixtures (promises both directions, due-date phrasings, negatives); precision-oriented (avoid
    false promises); assert fallback path triggers under strict storage.

- id: obligations-autonudge-autofulfill
  title: Auto-nudge / auto-fulfill obligations, gated by trust dial + Policy Constitution + kill-switch
  material: yes
  model: opus
  depends: [obligations-llm-extraction]
  proof: `npx vitest run test/obligationActions.spec.ts` exits 0
  prompt: |
    Add server/utils/obligationActions.ts: for each open obligation, choose an action — nudge counterparty
    (their overdue promise), draft fulfillment (my promise), or escalate — and route EVERY action through the
    existing governance + policy stack. Do not invent a parallel autonomy system.
    - governance.ts trust level decides execution: counsel_only => propose only; co_pilot => draft + awaiting_approval
      receipt; auto_pilot => execute within policy.
    - policy.ts evaluateAction must pass; 'auto_send'/'external_share' are KILL_SWITCH_GATED — honor the global
      kill-switch and UPL boundary (never auto-send legal advice).
    - Emit autonomy-cockpit receipts (auto_applied / awaiting_approval / proposed) consistent with getCockpit().
    Tests: full matrix per trust level x action type, kill-switch ON blocks all auto_send, UPL-class content forced
    to awaiting_approval even at auto_pilot. No real network sends in tests (mock).

- id: obligation-5-95-surface
  title: Wrap the obligations/matter surface in the 5/95 DecisionBudget pattern (+ pin the contract)
  material: no
  model: sonnet
  depends: [obligations-autonudge-autofulfill]
  proof: `npx vitest run test/decisionBudget.spec.ts` exits 0 AND `npm run build` exits 0
  prompt: |
    Introduce the 5/95 primitive in Smarter (currently absent; inbound concept from Tomorrow). Add
    types/decisionBudget.ts pinning the contract { Outcome, oneKnob (Recommended default), proof() } and a
    server/utils/fiveNinetyFive.ts wrapper. Apply it to the obligations/matter surface so each item is presented
    as a 95% resolved Outcome (fact) + one Recommended discretionary knob + on-demand proof (sourceQuote + the
    policy receipt). Logic + types must be tested; wiring it into a .vue surface is allowed but the proof is the
    unit test + a clean `npm run build`. Keep types portable for the future shared autonomy-budget package.

- id: obligation-xappsignal
  title: Emit overdue/at-risk obligation risk as xappSignal (PII-barrier clean)
  material: yes
  model: sonnet
  depends: [obligations-llm-extraction]
  proof: `npx vitest run test/xappSignal.spec.ts` exits 0
  prompt: |
    Add types/xappSignal.ts with the portfolio payload { ts, eventType, severity, orgId, affectedEntity, metadata }
    and emit a signal from reconcileObligations() when an obligation goes overdue or at-risk. ENFORCE the PII barrier:
    metadata carries ids/enums only — never sourceQuote, email bodies, counterparty names, or addresses. Tests assert
    payload shape AND that no PII fields leak into metadata. This is the cross-app operational-risk feed other apps
    (Tomorrow) consume; do not open a live S2S connection here (that needs a secret — see OPERATOR).

OPERATOR:
  - Outbound send credentials for auto-fulfill (Gmail / Microsoft Graph send scopes already used for ingest) — confirm send scopes + per-workspace consent before any auto_pilot external send.
  - Legal/counsel sign-off on the UPL boundary before auto_pilot is allowed to send external legal communications; keep auto_send kill-switch-gated until signed off.
  - Cross-app S2S to actually deliver xappSignal to Tomorrow needs TOMORROW_WARROOM_API_URL/KEY (already referenced) + the operational-risk-signal secret; wire the live emitter only after the contract is pinned and secrets are set.
  - Shared autonomy-budget package extraction (Smarter trust dial + Tomorrow DecisionBudget + Apparently materiality gate = one primitive) is a cross-repo refactor — coordinate, don't fork silently.
