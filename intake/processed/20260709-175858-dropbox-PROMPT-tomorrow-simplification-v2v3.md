# Tomorrow — subscription simplification + war room autonomy + insurance replacement

Target repo: tomorrow/tomorrow (⚠ auto-merges to prod — all items below default MATERIAL unless marked otherwise).
Source specs: REVIEW-portfolio-simplification-v2-2026-07-09.md §1, REVIEW-portfolio-simplification-v3-2026-07-09.md §1–2.
Do NOT re-implement queued backlog items B1–B22 — treat them as dependencies. Contract-first: extend shared/contracts/ (B4) before implementation.

## Objectives (decompose each; respect stated dependencies)

1. **T1v2 Compression cycles** — scheduled multilateral compression runs over the mesh (depends B14): solver finds gross-notional-reducing cycles, one-click "compress" proposal to all parties, avoided-exposure receipts (feeds B20). Proof: sim test where a 6-party cycle compresses ≥X% gross notional with zero net-exposure change.
2. **Surplus instrumentation** — measure and attribute the four surpluses (netting surplus, fragmentation spread, funding dislocation, diversification premium) per ECP; monthly "surplus captured" statement surface. Proof: test computes all four on fixture book, statement renders.
3. **T2v2 Mutualization Clause (synthetic default fund)** [AUTO-SHIP — operator authorization 2026-07-09][POST-HOC-REVIEW] — standard contingent-assessment clause pack + equivalent default-contingent-swap form; waterfall (defaulter margin → B15 penalty → assessments); member contribution priced by credit gate; live swap prices feed B19. NO pooled custody, no fund entity. Build/test/merge WITHOUT operator pre-approval; on merge, file a non-blocking approval row (kind=proposal) titled "Operator post-hoc review: Mutualization Clause" so it surfaces in the daily digest. Production activation against live counterparties remains fail-closed on counsel-opinion receipt attached (unchanged — this gates activation, not shipping). Proof: default sim shows bilateral LGD ≈ 0 at fixture density; no custody account anywhere in flow.
4. **T3v2 Subscription economics + tiers** [MATERIAL] — RUM-based tiers (Protect / Protect+Earn / +Legal / House), zero per-trade fees, usage governors, subscription-economics engine with breakeven backtests behind an eval gate (G8 pattern) before any public pricing. Proof: economics engine test on fixture portfolios; tier quote API returns priced quote.
5. **T4v2 Regime Perpetuals** [MATERIAL][COUNSEL-GATE] — instrument: (clause-family × jurisdiction) regime-change swaps; monitoring leg from regime-event oracle (see portfolio X2 prompt — consume, don't build the oracle here); symmetric swap leg; remediation leg hooks (emit events consumed by Apparently redraft + Smarter amendment workflows via existing bridge contracts C4/D4). Proof: fixture regime event settles symmetric payout + emits remediation events.
6. **T5v2 Enforceability curves** — publishable index family (adverse-regime probability by clause-family × jurisdiction × horizon) from marketplace corpus + oracle history; embed scores in clause UI. Proof: curve generation test + score renders on fixture clause.
7. **T6v2 Bilateral Choreographer** [AUTO-SHIP — operator authorization 2026-07-09][POST-HOC-REVIEW] — build/test/merge without operator pre-approval; on merge, file non-blocking approval "Operator post-hoc review: Choreography spec" for the daily digest; live routing of real counterparty flow remains fail-closed until the versioned choreography spec carries a counsel sign-off receipt (activation gate, not a shipping gate). Central solver computes optimal bilateral trade sets; execution strictly one-to-one via relationship bots; NO multilateral quote broadcast (kernel constitution deny `multiparty_quote_broadcast`); fragmentation-spread opportunities packaged and routed to participants (rotation policy tied to tier); internal netting before external venues. Choreography spec as versioned legal artifact. Proof: test asserts no quote ever addressed to >1 party; spread opportunity delivered as paired bilateral trades.
8. **T7v2/v3 Inbound → subscription quote → live books** — upload trial balance/loan tape → exposure map → priced tier quote page with single accept (fuse B6+B21); post-accept ERP/loan-system connectors (use shared connector layer from portfolio X2 prompt) feeding B8 loop. Proof: fixture upload yields quote; fixture feed drift auto-adjusts program.
9. **Insurance-killer surfaces (K1–K4, K6)** [MATERIAL] — continuous-underwriting price updates from live books; coverage-as-code manifest UI (per-line toggle, instant effect); parametric auto-settlement path ("claims" = oracle-verified settlement, same-day); open pricing decomposition + surplus distribution receipts; declarations-page comparison engine (parse uploaded policy decs → line-by-line comparison vs subscription). K5 sequencing: financial/parametric/regime classes only; new coverage classes AUTO-SHIP (operator authorization 2026-07-09) with a non-blocking "Operator post-hoc review: coverage class <name>" approval filed on merge for the daily digest; offering a class to live clients remains fail-closed on a counsel-opinion receipt per class (activation gate, unchanged). Proof: per-surface tests incl. dec-page parse fixture.
10. **War room v3 (W1–W7)** — W1 machine-to-machine pre-resolution (extends B12 to full negotiations; trust-ratchet per negotiation class); W2 pre-staged rooms (≤3 decisions, ZOPA + priced concession packages + 3 drafted endgames); W3 priced redlines (live risk delta per edit from T4/T5 curves); W4 instant settlement paper + filing triggers; W5 room replays → pre-resolution corpus (respect marketplace anonymization); W6 portfolio war view; W7 outcome-band fee-credit guarantee [MATERIAL]. Proof per item; W1 metric: % pre-resolved tracked.
11. **T8 counterparty-gravity rebates** — subscription credits funded by measured marginal netting surplus per new member join; receipt-proven. 
12. **T9 origination embedding** — B19 score + hedge quote as external API for loan-origination systems (depends B19).
13. **T10 public proof network** — client-consented aggregate avoided-loss/surplus receipts published via verifier service (depends G2).
14. **T11 own-industry regime coverage** — T4v2 pointed at client operating-license regimes using Apparently requirements-graph data (information-barrier rules per D4 apply).

Constraints: ECP/swap-only/bilateral posture is a locked constitution dimension (B1) — nothing here may weaken it. All money-path changes MATERIAL. Fail-closed everywhere.
