PROJECT: smarter

# Omnichannel social marketer — remaining build. The core (channels, schemes, CADE,
# 8 growth loops, flywheel, corpus grounding, media briefs, deliverability, Chrome exec
# path + LinkedIn live adapter) is already BUILT + DEPLOYED to the orchestrator Supabase
# (project eatfwdzfurujcuwlhdgj) and verified — see smarter/HANDOFF_SOCIAL_OMNICHANNEL.md
# (v24/v25). These tasks are the genuinely-remaining deliverables. Dependencies are within
# this file only. Live API posting + secrets are OPERATOR items, never queued.
# NOTE: the social data plane lives in the ORCHESTRATOR Supabase (eatfwdzfurujcuwlhdgj),
# accessed from smarter via server/utils/orch.ts (orchClient). New orch schema is applied
# there; smarter's own DB is olaxnyrzoptjcntrrjgn.

- id: metrics-ingest
  title: Metrics ingestion endpoint feeding the flywheel
  material: no
  model: sonnet
  depends: []
  proof: `cd smarter && npx nuxi typecheck` exits 0 AND POSTing a sample metrics payload to /api/growth/metrics-ingest updates the target growth_social_post.metrics on eatfwdzfurujcuwlhdgj (verify via SQL) and, when the post has a variant_id, bumps growth_social_variant.trials
  prompt: |
    The flywheel (compute_scheme_outcomes/pick_variant) already exists but nothing FEEDS
    engagement metrics in. Build `server/api/growth/metrics-ingest.post.ts` that accepts
    `{ items: [{ postId, metrics:{impressions,likes,comments,shares,clicks} }] }` and calls
    the existing `record_post_metrics(p_post_id, p_metrics)` RPC (via orchClient) for each.
    Also accept `{ actionId, ok, signal }` items → `record_action_health`. Add a thin
    Chrome-scrape SEAM helper in `server/utils/adapters/analytics.ts` (empty registry keyed
    by platform, like socialTick.ts publishers) that the future browser worker can call to
    pull per-post analytics; document the shape. Mock-degradable, orchClient-guarded.
    Reference: server/utils/social.ts, server/utils/socialTick.ts, HANDOFF_SOCIAL_OMNICHANNEL.md.

- id: loops-ui
  title: Growth Loops dashboard page + nav
  material: no
  model: sonnet
  depends: []
  proof: `cd smarter && npx nuxi typecheck` exits 0 AND `@vue/compiler-sfc` parse+compile of pages/growth-loops.vue succeeds
  prompt: |
    Surface the already-built loop features (no UI yet). Build `pages/growth-loops.vue`
    reading GET /api/growth/loops (returns attribution, signals, health, marketplace,
    crossApp). Show: cross-app rollup table (accounts/connected/posted/engaged/clicks per
    app), attribution by platform, account-health cards (status healthy/watch/paused with
    signals), listening signals feed, and the scheme marketplace ranked by outcome_score
    with a one-tap "apply" that POSTs loops.post rankMarketplace→social applyScheme. Add a
    "Growth Loops" link to the Growth nav group in components/AppShell.vue. Match the styling
    of pages/social.vue and pages/channels.vue. Reference: server/api/growth/loops.get.ts.

- id: voice-learner
  title: Train per-account voice profile from best past posts
  material: no
  model: sonnet
  depends: []
  proof: `cd smarter && npx nuxi typecheck` exits 0 AND calling the new endpoint for an account writes a row to growth_voice_profile on eatfwdzfurujcuwlhdgj (verify via SQL)
  prompt: |
    set_voice_profile exists but nothing populates it. Add a `learnVoice` action to
    `server/api/growth/loops.post.ts` (or a new endpoint) that takes `{ accountId, samples:[] }`
    (best past posts; if omitted, pull the account's posted growth_social_post bodies), runs
    an LLM pass (server/utils/llm.ts getAnthropicClient) to extract tone + 3 imitation
    examples + dos/donts, and calls the `set_voice_profile` RPC. Fail-soft heuristic when no
    API key. The generation path already reads voice via getVoiceString (server/utils/social.ts).

- id: x-adapter
  title: X (Twitter) live publisher + OAuth
  material: yes
  model: sonnet
  depends: []
  proof: `cd smarter && npx nuxi typecheck` exits 0 (adapter + start/callback routes compile; publisher registered in socialTick.ts publishers['x'])
  prompt: |
    Mirror the LinkedIn adapter for X. Add `server/api/auth/x/start.get.ts` +
    `callback.get.ts` (OAuth2 PKCE, scopes tweet.read tweet.write users.read offline.access;
    carry app/owner in state; store token as a growth_channel_credential vault row like the
    LinkedIn callback). Add `server/utils/adapters/x.ts` `publishXPost(item)` (POST
    https://api.twitter.com/2/tweets; threads = chained replies via in_reply_to_tweet_id).
    Register `x: publishXPost` in server/utils/socialTick.ts publishers. OAuth helpers go in
    server/utils/oauth.ts (mirror getLinkedInAuthUrl/exchange/profile). Reference:
    server/utils/adapters/linkedin.ts, server/api/auth/linkedin/*.

- id: meta-adapter
  title: Meta (Facebook Page / Instagram / Threads) live publisher + OAuth
  material: yes
  model: sonnet
  depends: []
  proof: `cd smarter && npx nuxi typecheck` exits 0 (adapter + Meta OAuth routes compile; publishers registered for facebook + instagram + threads in socialTick.ts)
  prompt: |
    Add Meta Graph publishing. `server/api/auth/meta/start.get.ts` + `callback.get.ts`
    (Facebook Login; request pages_manage_posts, instagram_content_publish, plus Threads
    scopes; store token per account). `server/utils/adapters/meta.ts` with publishFacebook
    (POST /{page-id}/feed), publishInstagram (2-step media container → publish), publishThreads
    (Threads API). Register facebook/instagram/threads in socialTick.ts publishers. Note:
    Instagram requires a media URL — read post.media[0]; if none, mark failed with a clear
    reason. Reference: server/utils/adapters/linkedin.ts.

- id: reddit-adapter
  title: Reddit live publisher + OAuth
  material: yes
  model: sonnet
  depends: []
  proof: `cd smarter && npx nuxi typecheck` exits 0 (adapter + Reddit OAuth routes compile; publisher registered for reddit)
  prompt: |
    Add Reddit posting. `server/api/auth/reddit/start.get.ts` + `callback.get.ts` (OAuth2,
    scope submit identity; store token). `server/utils/adapters/reddit.ts` publishRedditPost
    (POST /api/submit with sr from post.meta.subreddit, title, text). Respect per-subreddit
    self-promo rules — the recipe already warns. Register reddit in socialTick.ts publishers.
    Reference: server/utils/adapters/linkedin.ts, growth_browser_recipe reddit rows.

- id: medium-adapter
  title: Medium live publisher (integration token)
  material: yes
  model: haiku
  depends: []
  proof: `cd smarter && npx nuxi typecheck` exits 0 (adapter compiles; registered for medium in socialTick.ts publishers)
  prompt: |
    Medium uses a per-user integration token (no OAuth). Add `server/utils/adapters/medium.ts`
    publishMediumArticle(item): read the token from the account's growth_channel_credential
    (meta.access_token), GET /v1/me for authorId, POST /v1/users/{authorId}/posts with title
    + contentFormat markdown + publishStatus draft (SAFE default: draft, not public). Register
    medium in socialTick.ts publishers. The Channels page already captures the token via the
    api_key connect flow. Reference: server/utils/adapters/linkedin.ts.

- id: predictive-timing
  title: Best-time-to-post scheduling from historical engagement
  material: yes
  model: sonnet
  depends: [metrics-ingest]
  proof: live SQL on eatfwdzfurujcuwlhdgj shows a `best_post_slot(p_account_id, p_platform)` function returning a future timestamptz derived from the account's historical metrics, AND `cd smarter && npx nuxi typecheck` exits 0
  prompt: |
    Add a migration to eatfwdzfurujcuwlhdgj: `best_post_slot(p_account_id, p_platform)` that
    picks the next posting window from the hour-of-day/day-of-week buckets with the highest
    historical engagement (from growth_social_post.metrics of posted rows for that account;
    fall back to a sensible default when <10 samples). Wire it into apply_scheme / the
    generate→queue path so auto posts get scheduled_at = best_post_slot(...) instead of now().
    Keep the existing jitter for actions (next_slot). Idempotent; RLS default-deny.

- id: revenue-attribution
  title: Conversion/revenue attribution onto tracked links
  material: yes
  model: sonnet
  depends: []
  proof: live SQL on eatfwdzfurujcuwlhdgj shows a `record_link_conversion(p_slug, p_revenue)` function that increments growth_social_link.conversions + revenue, AND social_attribution reflects revenue; `cd smarter && npx nuxi typecheck` exits 0
  prompt: |
    Close the loop to money. Add migration to eatfwdzfurujcuwlhdgj:
    `record_link_conversion(p_slug text, p_revenue numeric default 0)` updating
    growth_social_link.conversions + revenue. Add `server/api/growth/track/conversion.post.ts`
    ({ slug, revenue }) calling it — this is the webhook each app hits on signup/purchase.
    social_attribution already sums revenue by platform. Document in HANDOFF how each app
    (apparently/tomorrow/darwn) posts its signup event here. No money MOVEMENT — attribution
    counters only.

- id: brand-safety-gate
  title: Per-account brand-safety guardrails + pre-publish gate
  material: yes
  model: opus
  depends: []
  proof: live SQL on eatfwdzfurujcuwlhdgj shows a growth_brand_guardrail table + a `check_publish_allowed(p_post_id)` function that blocks a post whose body matches a banned term; `cd smarter && npx nuxi typecheck` exits 0
  prompt: |
    Before scaling autonomy, add a safety layer. Migration to eatfwdzfurujcuwlhdgj:
    `growth_brand_guardrail(account_id, banned_terms text[], required_disclosure boolean,
    max_aggressiveness text, notes)` (RLS default-deny) + `check_publish_allowed(p_post_id)`
    returning {allowed, reasons[]} that blocks on banned-term match or missing #ad disclosure
    when meta.promotional. Wire the gate into social_due_now/social_due_chrome so blocked
    posts are skipped with a reason (do NOT publish). Also expose it in social-tick before the
    publisher call. Reference: HANDOFF_SOCIAL_OMNICHANNEL.md (autonomy + CADE sections).

- id: conversation-inbox
  title: Inbound comment/DM triage + CADE-drafted replies
  material: no
  model: opus
  depends: []
  proof: `cd smarter && npx nuxi typecheck` exits 0 AND a sample inbound payload to the new endpoint creates growth_social_signal rows (kind='inbound') on eatfwdzfurujcuwlhdgj with a drafted reply in payload, status left for human approval
  prompt: |
    Highest-value untapped surface: respond to inbound. Add migration for
    `growth_inbound(id, app, account_id, platform, kind(comment|dm|reply|mention), author,
    text, source_ref, lead_score numeric, draft jsonb, status, created_at)` on
    eatfwdzfurujcuwlhdgj. Add `server/api/growth/inbox.post.ts` (ingest inbound items; the
    Chrome listening worker feeds it) that CADE-drafts a reply (server/utils/cade.ts +
    generateEngagement) and lead-scores each (LLM + heuristics). Replies are DRAFTS —
    status='needs_review'; sending is a human-approved queued action (reuse enqueue_social_action).
    Hot leads (score>0.7) also enqueue a Tier-A note to the existing BD/outreach path.

- id: campaign-from-goal
  title: One-click campaign from a natural-language goal
  material: no
  model: sonnet
  depends: [loops-ui]
  proof: `cd smarter && npx nuxi typecheck` exits 0 AND POSTing a goal to the endpoint returns a plan (chosen scheme + assigned accounts + generated first wave) and stages drafts on eatfwdzfurujcuwlhdgj
  prompt: |
    Turn a sentence into a program. Add `server/api/growth/campaign-from-goal.post.ts`
    ({ goal, app }) that: (1) classifies the goal → objective, (2) calls rank_marketplace to
    pick the best-fit scheme, (3) selects connected accounts for the implied platforms,
    (4) calls apply_scheme, (5) triggers fillDrafts (grounded generation) for the first wave,
    (6) returns a plan summary with projected reach. Add a goal box to pages/growth-loops.vue.
    Reference: server/utils/social.ts, server/api/growth/social.post.ts, schemes.

OPERATOR:
  - Register developer/OAuth apps + set secrets (never queue): LinkedIn (LINKEDIN_CLIENT_ID/SECRET), X (X_CLIENT_ID/SECRET), Meta (META_APP_ID/SECRET), Reddit (REDDIT_CLIENT_ID/SECRET), TikTok, YouTube (Google scope). Redirect URIs → https://smarter-nine.vercel.app/api/auth/<platform>/callback. See smarter/.env.growth.
  - Set CORPUS_SUPABASE_SERVICE_KEY (apparently project oosolxvlfyifkhjohdzq service_role) in smarter env to activate corpus grounding.
  - Set CRON_SECRET in Vercel project env (guards /api/cron/social-tick + /api/cron/social-optimize) and confirm the two Vercel crons are enabled (vercel.json).
  - Set SOCIAL_LIVE=1 in Vercel to enable real publishing once ≥1 adapter is verified; leave 0 to keep simulate mode.
  - Provide an image/video generation API key if media briefs should render actual assets (media-render is a future task; briefs already generate).
  - Per-account go-live: flip the detected accounts (LinkedIn personal + 3 pages, Medium, Reddit, X) from autonomy='approval' to 'auto' when ready; optionally set growth_settings social_auto_amplify=true to enable swarm amplification.
  - Pre-approve the 3 Cowork scheduled tasks (social-browser-worker, social-growth-digest, social-listening) by running each once so tool permissions are stored.
