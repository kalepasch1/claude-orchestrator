PROJECT: apparently

# Frontier-2 (APPARENTLY) — self-growing primitive library, contract ratings, embed-everywhere, autonomous entity lifecycle.
# Builds on the CREG/CEPL intakes (apparently-0629-creg.md et al.) + the Hive. Referenced deps are by prose where they
# cross files. Conventions: typed SupabaseClient<Database>; callClaudeStructured + selectModel; idempotent name-checked
# migrations; pure cores unit-testable; parametric bright line + legal steps human-gated.

- id: cepl-auto-primitive-spawn
  title: Self-growing CEPL — auto-propose a new primitive when residual basis recurs
  material: yes
  model: opus
  depends: []
  proof: "`npx vitest run tests/engines/cepl/auto-spawn.test.ts` exits 0 (when the CREG→CEPL mapper repeatedly leaves a high-basis residual for the same exposure across >=N clients, proposePrimitive drafts a new cepl_primitive spec; below threshold it does not; drafted primitives are queued, not auto-activated)"
  prompt: |
    Directly shrink residual basis over time. Reuse the vertical auto-spawn pattern on the CEPL: engine
    server/engines/cepl/auto-spawn.ts records residual-basis signals from mapCregToCepl, and when the SAME poorly-mapped
    exposure recurs across >=N clients with verified backing, proposePrimitive drafts a new standardized primitive spec
    (trigger over reg_facts + payoff unit + basis grade) and queues it (status 'proposed') for human graduation into
    cepl_primitives. The library grows toward what the market actually carries → residual → 0 autonomously. Pure
    recurrence/threshold core unit-tested. MATERIAL: it expands the tradeable universe — graduation is human-gated.

- id: contract-ratings
  title: Contract-as-ratable-commodity — rate contract types from their CREG/CEPL basket
  material: no
  model: sonnet
  depends: []
  proof: "`npx vitest run tests/engines/cepl/contract-ratings.test.ts` exits 0 (rateContract returns a letter grade + adverse-regime probability + hedge-cost band for a contract type from its CEPL basket + calibrated probabilities; identical baskets rate identically)"
  prompt: |
    A Moody's-for-contracts. Engine server/engines/cepl/contract-ratings.ts: rateContract(cregOrBasket) → {grade A-D,
    adverseRegimeProbability, hedgeCostBandBp, drivers} computed from the CEPL basket + calibrated per-primitive
    probabilities. Benchmark contract TYPES (e.g. 'CA SaaS MSA') by anonymized aggregation. Pure rating core
    unit-tested (deterministic, monotone in basket risk). Feeds negotiation + pricing; publish surface is separate.

- id: embed-opinion-hedge-checkout
  title: Embeddable "generate opinion + CEPL hedge for this contract" checkout step
  material: yes
  model: opus
  depends: []
  proof: "`npx vitest run tests/api/embed/opinion-hedge.test.ts` exits 0 (a scoped embed token issues; the determine+quote endpoint returns an opinion summary + a CEPL hedge-basket quote for a posted contract; un-allowlisted origin rejected; compute-only, never binds)"
  prompt: |
    Distribution → everywhere. Extend the compliance-api embed kit: server/api/embed/[token]/opinion-hedge.post.ts —
    given a contract, return an opinion summary (CREG) + a CEPL hedge-basket quote so a marketplace/lender/payments
    platform can add "opinion + hedge" as a checkout step. Public, CORS-validated, token-scoped, rate-limited,
    COMPUTE-ONLY (quote + draft; issuance/binding stays gated). Unit-test token/scope/quote/origin. MATERIAL: public
    API surface + auth.

- id: autonomous-entity-lifecycle
  title: Compose formation → licensed → contracted → hedged → migrated/wound-down as one flow
  material: yes
  model: opus
  depends: []
  proof: "`npx vitest run tests/engines/lifecycle/entity-lifecycle.test.ts` exits 0 (runLifecycle sequences the stages using existing engines — instant-institution, vertical registry, CREG opinion, CEPL hedge, monitoring, arbitrage-driven migration — each stage gated by its own approval; a regime-kill event routes to migrate/wind-down)"
  prompt: |
    The operating system for a regulated entity. Orchestrator server/engines/lifecycle/entity-lifecycle.ts:
    runLifecycle(entity) sequences formation (instant-institution) → licensing across jurisdictions (vertical registry +
    shared-artifact reuse) → contract opinions (CREG) → hedge (CEPL basket) → perpetual monitoring (memo + Hive) →
    on a regime-kill trigger, migrate (open/close rebalance) or wind-down. Each stage is a COMPOSITION of already-built
    engines behind its own approval gate — do NOT rebuild them; call them. Pure sequencer core unit-tested with mocked
    stage engines (correct ordering, gating, regime-kill branch). MATERIAL: it drives filings/hedges/money-adjacent
    steps — every stage human-gated per its existing rules.

OPERATOR:
  - Counsel: CEPL auto-spawned primitives must be reviewed as parametric (not indemnity) before graduation; contract ratings + published benchmarks need a disclaimer/UPL review; the embed checkout's opinion output needs the same legal-advice determination as the core opinion product.
  - Deploy any new migrations (proposed-primitive status, ratings cache) after name-check.
  - Provision embed API ToS + rate-limit/abuse review before external go-live.
