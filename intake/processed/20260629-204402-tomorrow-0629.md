PROJECT: tomorrow

- id: contracts-tomorrow
  title: Pin cross-cutting shared interfaces (contract-first)
  material: no
  model: sonnet
  depends: []
  proof: `npm run typecheck` exits 0
  prompt: |
    Create shared/contracts/ with TS interface files only (no behavior), so every
    dependent branch agrees on boundaries: decisionBudget.ts (surface-id registry +
    Outcome/One-knob/Proof wrapper props, building on server/utils/ux/decisionBudget.ts),
    xappSignal.ts ({ts,eventType,severity,orgId,affectedEntity,metadata} operational-risk
    signal; PII-barrier note), warRoomSync.ts (WarRoomUpdate/WarRoomBridgeEvent matching
    smarter/server/api/warroom/bridge.post.ts), perpLifecycle.ts (drift->propose->re-strike
    ->re-price->re-prove events), contingentIdentity.ts (default-OFF flag + contingent-reveal
    state machine types). Export all; add a trivial type-only test. See HANDOFF-cowork-master-build.md §5.

- id: p0-decision-budget-wrapper
  title: 5/95 wrapper + wire DecisionBudget to telemetry + make lint blocking
  material: no
  model: sonnet
  depends: [contracts-tomorrow]
  proof: `node scripts/lint-decision-budgets.mjs && npx vitest run server/utils/ux/__tests__/decisionBudget.test.ts` exits 0
  prompt: |
    Add components/ui/FiveNinetyFive.vue (Outcome 95% as fact / single Recommended knob /
    disclosure / mandatory GlassBox proof) per shared/contracts/decisionBudget.ts. Wire
    server/utils/ux/decisionBudget.ts to existing telemetry (server/utils/ux/magicMomentTracker.ts,
    uxExperimentRunner.ts). Fix the two declared violations: pages/app/onboarding/institution.vue
    (6->3) and the war-room surface (8->5) by collapsing advanced controls behind disclosure.
    Flip scripts/lint-decision-budgets.mjs footer to process.exit so declared-surface violations
    fail the build (keep undeclared advisory).

- id: p0-mandate-collapse
  title: Replace 105-control mandate page with inferred single-confirm
  material: yes
  model: sonnet
  depends: [p0-decision-budget-wrapper]
  proof: `node scripts/lint-decision-budgets.mjs` exits 0
  prompt: |
    pages/app/mandate.vue renders ~105 controls (worst 5/95 violation). Pre-fill ALL fields
    from peer-consensus (server/utils/autonomy/constitutionCompiler.ts) + the institution's own
    public-data history; present ONE confirm ('looks right / adjust'); move the full field set
    behind a 'More controls' disclosure. Preserve the server/utils/hive/mandatePolicy.ts round-trip
    (no field lost, just defaulted+hidden). Declare app/mandate budget=3 in the lint registry.
    Material: mandate governs autonomous trading limits.

- id: trust-ratchet
  title: Self-shrinking per-user autonomy ratchet
  material: yes
  model: sonnet
  depends: [p0-decision-budget-wrapper]
  proof: `npx vitest run server/utils/ux/__tests__/trustRatchet.test.ts` exits 0
  prompt: |
    server/utils/ux/trustRatchet.ts (pure) generalizing server/utils/hive/approvalQueue.ts
    learning-mode: a decision a user ratified N times graduates to autonomous; each surface
    recomputes presented decisions to the user's current trust frontier (5/95 -> 2/98 -> 1/99).
    Test proves graduation-after-N and per-user frontier recompute. Material: changes what
    auto-executes without human approval.

- id: perp-lifecycle-loop
  title: Close the perpetual lifecycle loop (cron)
  material: yes
  model: opus
  depends: [contracts-tomorrow]
  proof: `npx vitest run server/utils/otc/eventOptions/__tests__/lifecycleManager.test.ts` exits 0
  prompt: |
    server/utils/otc/eventOptions/lifecycleManager.ts + server/api/cron/perp-lifecycle.post.ts:
    read open perpetual positions, compute drift via mmHedgeOrchestrator.detectNakedInventory,
    call proposeHedge, apply perpetualLegs.reStrikePerpetualLeg, re-price, mint a fresh C1 proof.
    Honor the distanceToResolution<5% resolution-zone guard (no auto re-strike in-zone; escalate).
    Test: a drift event drives end-to-end re-strike+re-price+proof, and the guard blocks in-zone.

- id: composite-payoff-compiler
  title: Risk-spec -> payoff optimizer + perpetual-vs-discrete auto-choice
  material: yes
  model: opus
  depends: [perp-lifecycle-loop]
  proof: `npx vitest run server/utils/otc/__tests__/compositePayoffCompiler.test.ts` exits 0
  prompt: |
    server/utils/otc/compositePayoffCompiler.ts over server/utils/otc/payoffDSL.ts. Input: a risk
    spec (NL or risk-vector). Output: an allowlist-valid instrument (gradient ramp / call-spread-
    on-leg / reinstatement) via composeProgram + assertComposable, choosing perpetual vs discrete
    by funding-carry-vs-one-off-cost over the horizon, fail-closed against instrumentAllowlist +
    a structural backtest. Test asserts: valid instrument + backtest + rationale; discrete chosen
    for a one-off with stated cost delta; un-backtested composite rejected.

- id: funding-equilibrium
  title: Cross-book funding equilibrium engine
  material: yes
  model: sonnet
  depends: [perp-lifecycle-loop]
  proof: `npx vitest run server/utils/otc/__tests__/fundingEquilibrium.test.ts` exits 0
  prompt: |
    server/utils/otc/fundingEquilibrium.ts (pure): given netted system state across all open
    perpetual positions, set funding rates that clear aggregate risk so each user's re-strike
    references the network state (marginal hedge cost falls as the book grows). Test: adding
    offsetting demand lowers computed marginal funding cost; per-position guards conserved.

- id: instrument-discovery-loop
  title: Autonomous instrument-variant discovery + Pareto promotion
  material: yes
  model: sonnet
  depends: [composite-payoff-compiler]
  proof: `npx vitest run server/utils/otc/__tests__/instrumentDiscovery.test.ts` exits 0
  prompt: |
    server/utils/otc/instrumentDiscovery.ts: every autonomously-generated underlying (generatorV3)
    spawns its perpetual-hedge, embedded-option, and gradient-payout variants; backtest each;
    promote Pareto-winners into the catalog; fail-closed on un-backtested composites. Test:
    new underlying -> backtested variants; a failing variant is never promoted.

- id: warroom-autoskip
  title: War-room auto-skip gate + learning frontier
  material: yes
  model: sonnet
  depends: [contracts-tomorrow]
  proof: `npx vitest run server/utils/warRoom/__tests__/autoSkipGate.test.ts` exits 0
  prompt: |
    server/utils/warRoom/autoSkipGate.ts + learningFrontier.ts. Route clauses where novelty<30 &&
    risk<30 && notional<cap && commonlyApprovedByThisUser to the email/1-button path
    (server/utils/coordination/approvalNotify.ts + /api/approvals/inline/[token]) using
    server/utils/warRoom/noveltySentinel.ts scores. Wire the stubbed hivemind learning-mode into a
    per-user, per-clause-type auto-approve frontier that widens on each approve/reject; add a
    counsel-needed signal. Test: gated clause skips room; clause failing any condition opens it;
    frontier widens after repeated approvals. Material: routes around human approval on contracts.

- id: warroom-attorney-cockpit
  title: 5/95 attorney cockpit + ZOPA + one-button close
  material: yes
  model: sonnet
  depends: [warroom-autoskip]
  proof: `npx vitest run server/utils/warRoom/__tests__/attorneyCockpit.test.ts` exits 0
  prompt: |
    Badge 95% of clauses 'at-market, auto-accepted' (market-standard percentile) so only out-of-
    market clauses are action items; pre-compute each open clause's ZOPA + a market-median midpoint;
    generate a pre-call briefing pack; add a post-counsel one-button close that fires the existing
    closingWorkflow + server/utils/docusign.ts. Test on a sample deal: only out-of-market clauses
    surface, each with a midpoint, and one-button close advances to send_for_signature. Material:
    executes contracts via DocuSign.

- id: mesh-of-rings
  title: Hierarchical mesh-of-rings netting (1000-party, posture-clean)
  material: yes
  model: opus
  depends: [contracts-tomorrow]
  proof: `npx vitest run server/utils/otc/rings/__tests__/meshOfRings.test.ts` exits 0
  prompt: |
    server/utils/otc/rings/meshOfRings.ts: compose many small-N rings (capacityFormation,
    multilateralNetting, selfHealingRing) into a mesh giving 100s-1000s-party dispersion WITHOUT a
    single large ring, preserving N6 non-CCP/bilateral posture. Emit a breach-resilience metric
    (max single-party default impact as % of each party's net). Test: 1000-party sim disperses;
    metric <1% per party; existing failingInvariants still hold.

- id: credit-hard-gate
  title: Hard, market-priced credit gate + debt-ceiling + hedgeable passport
  material: yes
  model: sonnet
  depends: [contracts-tomorrow]
  proof: `npx vitest run server/utils/otc/rings/__tests__/creditReliability.test.ts` exits 0
  prompt: |
    Upgrade server/utils/otc/rings/creditReliability.ts + creditStore.ts: min-tier admission (user
    AND Tomorrow set); a debt-ceiling absorbing penalty state so serial defaulters can't fully
    recover reputation (portable in creditPassport); risk-priced participation (wider spread from
    predictDeterioration severity); hedgeable passport (a perp on a counterparty's passport). Test:
    sub-tier party denied or priced wider; serial defaulter recovery capped.

- id: breach-remediation
  title: Three-pronged instant breach remediation
  material: yes
  model: sonnet
  depends: [mesh-of-rings, credit-hard-gate]
  proof: `npx vitest run server/utils/otc/rings/__tests__/defaultManagement.test.ts` exits 0
  prompt: |
    Wire server/utils/otc/rings/defaultManagement.ts: on breach (1) auto-fire selfHealingRing +
    discoverPairings to source a replacement party/parcels so honest parties resume immediately,
    (2) open a pre-agreed shared-cost remediation matter via the Smarter bridge (warRoomSync
    contract; Smarter task smarter-warroom-bridge-activate provides the hook), (3) apply the credit
    penalty on reveal. Test: sim breach -> replacement + remediation-matter payload + credit penalty
    + contract resume in one flow.

- id: per-contract-hedging
  title: Per-contract (not per-bucket) hedge derivation
  material: yes
  model: sonnet
  depends: [contracts-tomorrow]
  proof: `npx vitest run server/utils/otc/__tests__/perContractHedger.test.ts` exits 0
  prompt: |
    server/utils/otc/perContractHedger.ts: extract each loan/policy/customer-contract risk vector
    via server/utils/extraction/verticalExtractors.ts + documentProcessor.ts and derive a hedge
    bound to that contract id (not a portfolio bucket). Test: ingest a small loan tape -> one hedge
    per contract with extraction provenance, measurably tighter than the bucket baseline. Material:
    consumes sensitive customer contract data.

- id: lifecycle-adaptive-hedging
  title: Hedges follow contract lifecycle (default/prepay/cancel/degrade)
  material: yes
  model: sonnet
  depends: [per-contract-hedging]
  proof: `npx vitest run server/utils/otc/__tests__/lifecycleAdaptiveHedging.test.ts` exits 0
  prompt: |
    Consume Apparently loan-state signals (shared/contracts/xappSignal.ts) at
    server/api/apparently/bank/exposure-sync and adjust the per-contract hedge via
    perpetualLegs.reStrikePerpetualLeg (stop on removal, increase on degrade). Depends on Apparently
    tasks apparently-tomorrow-client + apparently-signal-emitters for the live feed. Test: sim
    default stops the hedge; degradation increases it; each emits a fresh proof.

- id: underwriting-score
  title: Cross-portfolio risk score + origination-embedded hedge quote
  material: yes
  model: opus
  depends: [per-contract-hedging]
  proof: `npx vitest run server/utils/risk/__tests__/underwritingScore.test.ts` exits 0
  prompt: |
    server/utils/risk/underwritingScore.ts: proprietary score trained on loan/policy outcomes across
    all users (firm/geo/segment/global, building on selfImprovingEngine); expose origination-embedded
    hedge quoting ('hedge residual to X for Y bps') via the Apparently bank bridge + a peer benchmark
    percentile. Test: candidate loan -> score + hedge-to-X + bps + percentile; recommendation
    backtests against realized outcomes. Material: fair-lending / SR 11-7 model-governance surface.

- id: benefit-receipts
  title: Benefit cascade + event-time avoided-loss + real-time compliance-doc regen
  material: yes
  model: sonnet
  depends: [contracts-tomorrow]
  proof: `npx vitest run server/utils/hive/__tests__/hedgeAttributionEngine.test.ts` exits 0
  prompt: |
    Extend server/utils/hive/hedgeAttributionEngine.ts: full benefit cascade (capital freed,
    earnings-volatility reduced, earnings uplift) + event-time avoided-loss receipts + emergent-
    benefit discovery; regenerate the compliance doc set (SRT/ASC 815/RBC/constitution) chained to
    C1 proofs on every re-optimization. Test: sim market event -> defensible avoided-$ receipt with a
    proof trail; a re-optimization regenerates the chained compliance docs. Material: regenerates
    regulatory filings.

- id: outbound-onboarding
  title: Outbound pre-computed onboarding + shadow-mode pre-commitment receipt
  material: yes
  model: sonnet
  depends: [contracts-tomorrow]
  proof: `npx vitest run server/utils/autonomy/__tests__/outboundOnboarding.test.ts` exits 0
  prompt: |
    Pre-compute the OnboardingPackage (server/utils/autonomy/instantInstitution.ts) for ingested
    FDIC entities and mint personalized 'click-to-arm' URLs (trapped $ + drafted constitution +
    first hedges); add a shadow-mode 60-90d pre-commitment receipt backtesting the autonomous book
    against their real public positions. Test: one identifier -> full value surface + shadow receipt,
    no further input. Material: generates outbound comms to real institutions (sending requires the
    OPERATOR domain/approval below).

- id: contingent-identity
  title: Contingent-ECP-identity feature (DEFAULT-OFF)
  material: yes
  model: opus
  depends: [contracts-tomorrow, legal-identity-docs]
  proof: `npx vitest run server/utils/otc/__tests__/contingentIdentity.test.ts` exits 0
  prompt: |
    Implement the contingent-reveal engine per shared/contracts/contingentIdentity.ts: counterparty
    identity escrowed and withheld between parties (never from platform/regulator), released only on
    a Reveal Event (breach/default or event+unanimous consent); prove ECP status/credit tier via the
    C1 verifiableProof layer WITHOUT revealing identity; terms enforceable regardless of reveal state.
    Ship behind a DEFAULT-OFF feature flag. Test asserts: flag defaults off; a blind party's tier is
    provable without identity; voluntary reveal requires event+consent; default reveal is not
    consent-gated. The flag stays OFF in prod until OPERATOR counsel sign-off. See legal/ docs.

- id: legal-identity-docs
  title: Expand contingent-identity legal document set (drafts)
  material: yes
  model: opus
  depends: [contracts-tomorrow]
  proof: `test -f legal/ISDA_CONTINGENT_IDENTITY_INTEGRATION.md && test -f legal/IDENTITY_CUSTODIAN_AGREEMENT.md && test -f legal/IOI_ANONYMITY_DISCLOSURE_SCHEDULE.md && test -f legal/BREACH_REMEDIATION_ENGAGEMENT_LETTER.md` exits 0
  prompt: |
    Expand the contingent-ECP-identity set under legal/ (rider + comparables memo already exist):
    ISDA Schedule/Confirmation integration language (escrowed identity + reveal-on-trigger), IOI
    Anonymity Disclosure Schedule, Identity Custodian / Escrow-Agent Agreement, unanimously-pre-
    agreed Breach-Remediation Engagement Letter; finalize the comparables memo. ALL marked DRAFT —
    FOR EXECUTION BY COUNSEL, default-OFF, ECP-only; cross-reference shared/contracts/contingentIdentity.ts.
    Material: legal documents (execution gated to OPERATOR counsel).

- id: drift-guard-lint
  title: CI lint that fails on shared-contract drift
  material: no
  model: haiku
  depends: [contracts-tomorrow]
  proof: `node scripts/lint-shared-contracts.mjs` exits 0
  prompt: |
    scripts/lint-shared-contracts.mjs: assert each shared/contracts/*.ts interface matches the
    canonical definition consumed cross-repo (compare exported member names/shapes against a
    checked-in snapshot). Exit non-zero on drift. This is the guard that keeps tomorrow/smarter/
    apparently from silently diverging as the swarm edits them in parallel.

OPERATOR:
  - Counsel sign-off (CFTC/derivatives + AML) against legal/CONTINGENT_ECP_IDENTITY_COMPARABLES_MEMO.md §4 BEFORE the contingent-identity feature flag is enabled for any live transaction. Code ships flag OFF.
  - Execute the legal/ contingent-identity documents (rider, ISDA integration, custodian agreement, IOI schedule, remediation engagement letter) — drafts only until signed.
  - outbound-onboarding sending: approve the outbound campaign + configure a verified sending domain before any click-to-arm URLs are emailed to real institutions.
  - Any Supabase migration/prod deploy produced by material tasks: human-approve the merge (orchestrator routes these to approval automatically).
