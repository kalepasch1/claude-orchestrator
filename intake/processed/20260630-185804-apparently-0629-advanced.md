PROJECT: apparently

# Wave 2 — advanced retrieval / trust / product layers on the curated corpus.
# Handoffs: corpus/FLYWHEEL_STRATEGY.md, corpus/IMPROVEMENTS.md.
# Deployed (Supabase oosolxvlfyifkhjohdzq) — do NOT redo: corpus_documents/clauses/clause_embeddings/
#   citations/query_log/gaps/source_state; fns search_corpus_authority, corpus_coverage, log_corpus_query,
#   corpus_completeness, increment_corpus_fetch_count; POST /api/research/query + GET /api/research/coverage;
#   server/utils/corpus-precedent.ts; corpus/scripts/{harvesters,extract-citations,backfill-bodies}.mjs + eval/.
# These depend on Wave-1 (apparently-0629.md): doc-embeddings-migration, hybrid-rrf-search, citation-graph-expansion.

- id: passage-index
  title: Passage-level chunking + index (precise pinpoint retrieval)
  material: yes
  model: opus
  depends: []
  proof: `npx vitest run server/utils/__tests__/chunk.test.ts` exits 0
  prompt: |
    Add a passage layer so retrieval returns the exact section, not a 200-page blob. Migration: table
    corpus_passages(passage_id text pk, doc_id text fk, ordinal int, heading text, text text, char_start int,
    char_end int, fts tsvector generated, embedding vector(1024) null) + GIN + HNSW + service_role RLS. Add a
    pure helper server/utils/chunk.ts `chunkDocument(text, {targetTokens})` that splits on section/numbering
    boundaries (reuse corpus/scripts/segment-clauses.mjs splitter logic) with char offsets. Add a passage build
    step to backfill-bodies.mjs (after body fetch). Unit-test chunk.ts: stable offsets, no overlap, boundary
    splitting. Human applies the migration (material).

- id: passage-search-fn
  title: search_corpus_passages RPC + pinpoint citations in the endpoint
  material: yes
  model: sonnet
  depends: [passage-index]
  proof: `npx nuxi typecheck` exits 0
  prompt: |
    Add SQL fn search_corpus_passages(query_text, p_doc_ids text[] default null, p_limit) ranking passages by
    FTS (and vector when embeddings present). In /api/research/query, when `pinpoint:true`, return the top
    passage per hit doc with {heading, char_start, char_end, snippet} so citations point to the exact section.
    Read path; the migration adding the fn is the material part.

- id: query-decomposition
  title: LLM query decomposition for multi-element legal questions
  material: no
  model: opus
  depends: []
  proof: `npx vitest run server/utils/__tests__/decompose.test.ts` exits 0 && `npx nuxi typecheck` exits 0
  prompt: |
    Real questions have multiple legal elements. Add server/utils/query-plan.ts `planQuery(question)` that uses
    aiCall to emit a structured plan: sub-queries each with {concept, issuer?, doc_type?, jurisdiction?,
    date_window?}. Add POST /api/research/ask that runs each sub-query through search_corpus_authority, merges
    (dedupe by doc_id), and returns grouped-by-sub-question cited results + an overall coverage block. Unit-test
    planQuery with a mocked aiCall (assert it returns >=1 well-formed sub-query and falls back to the raw
    question on parse failure). Read-only.

- id: coverage-agent
  title: "100%-then-answer" agentic loop with self gap-fill
  material: yes
  model: opus
  depends: [query-decomposition]
  proof: `npx vitest run server/utils/__tests__/research-agent.test.ts` exits 0
  prompt: |
    Wrap planQuery + retrieval in server/utils/research-agent.ts `runResearch(question, {maxRounds})`:
    plan -> retrieve -> if any sub-query's best rank < threshold, log a corpus_gaps row (status open) AND, when
    autofill enabled, trigger the gap-harvest worker, then re-retrieve once -> synthesize a cited answer with a
    coverage statement. Unit-test the control flow with mocked retrieval (gap detected -> harvest called ->
    re-retrieve -> answer). The harvest trigger writes/queues -> material. Pure orchestration is the proof.

- id: point-in-time
  title: As-of-date (point-in-time) retrieval
  material: no
  model: sonnet
  depends: []
  proof: `npx vitest run server/utils/__tests__/as-of.test.ts` exits 0
  prompt: |
    Add an `asOf` (YYYY-MM-DD) param to /api/research/query: filter to docs effective on/before asOf and prefer
    the operative version at that date using effective_date + the supersedes chain (a doc superseded AFTER asOf
    is still operative AS OF asOf). Put the operative-version selection in a pure helper
    server/utils/as-of.ts `operativeAsOf(docs, asOf)` and unit-test the supersession-timeline logic. Read-only.

- id: hyde-retrieval
  title: HyDE (hypothetical-document embedding) retrieval option
  material: no
  model: sonnet
  depends: [doc-embeddings-migration]
  proof: `npx vitest run server/utils/__tests__/hyde.test.ts` exits 0
  prompt: |
    For jargon/sparse queries, generate a short hypothetical ideal answer (aiCall), embed THAT (Voyage), and
    vector-search by it. Add server/utils/hyde.ts `hydeQuery(question)` -> text; wire as a `hyde:true` option in
    the vector path of the research endpoint, fused via rrf alongside FTS. Unit-test hyde.ts with mocked aiCall
    + graceful fallback to the literal query. Read-only.

- id: span-grounding-verifier
  title: Span-level grounding + verbatim quote-faithfulness gate
  material: no
  model: sonnet
  depends: [passage-index]
  proof: `npx vitest run server/utils/__tests__/quote-verify.test.ts` exits 0
  prompt: |
    Anti-hallucination. Add server/utils/quote-verify.ts: `verifyQuote(quote, docText)` (normalized
    whitespace/case substring check) and `groundSentence(sentence, candidatePassages)` returning the best
    supporting passage + char span or null. Expose POST /api/research/verify-citation { quote, docId } that
    loads the corpus body and returns {faithful:boolean, span?}. Unit-test verbatim match, near-miss rejection,
    whitespace tolerance. Read-only; this is the structural guard against fabricated/misquoted cites.

- id: shepardize-on-write
  title: Citation health at draft time (negative-treatment warnings)
  material: no
  model: sonnet
  depends: []
  proof: `npx nuxi typecheck` exits 0
  prompt: |
    Add server/utils/cite-health.ts `checkCitations(citations[])` that, for each cited authority, looks it up in
    corpus_documents/corpus_citations + citation_freshness_scores and returns status (ok | superseded | stale |
    negative_treatment | not_in_corpus). Surface it in the opinion/drafting path (advisory, fail-soft) so a draft
    that cites a vacated rule is flagged inline. typecheck 0 errors.

- id: adverse-authority
  title: Adverse-authority / red-team retrieval
  material: no
  model: sonnet
  depends: []
  proof: `npx nuxi typecheck` exits 0
  prompt: |
    Add POST /api/research/adverse { question, conclusion }: use aiCall to phrase the OPPOSITE proposition, run
    it through search_corpus_authority, and return the strongest contrary authorities. Wire an advisory call
    into the RLO opinion path so each opinion can auto-populate a "strongest argument against" section. This is
    what makes the opinion "legally reasoned." Read-only; fail-soft.

- id: research-provenance-proof
  title: Verifiable research-provenance bundle per answer
  material: yes
  model: sonnet
  depends: []
  proof: `npx vitest run server/utils/__tests__/research-proof.test.ts` exits 0
  prompt: |
    Mint a content-addressed, signed manifest per answer: {question, corpus_version, doc_ids+hashes, coverage%,
    timestamp}. Add server/utils/research-proof.ts `mintResearchProof(...)` + `verifyResearchProof(...)` (SHA-256
    content hash + HMAC or Ed25519; reuse any existing proof/receipt util in the repo). Store in a
    research_proofs table (migration). Unit-test: mint -> verify true; tamper -> false. Material (new table +
    signing). Examiner/court-defensible "show your work".

- id: entity-knowledge-graph
  title: Entity extraction → structured analytics over law
  material: yes
  model: opus
  depends: []
  proof: `npx vitest run server/utils/__tests__/entities.test.ts` exits 0
  prompt: |
    Extract entities (parties, products, regulators, judges/ALJs, dollar amounts, dates) from doc bodies into a
    corpus_entities table (migration: entity_id, doc_id fk, type, value, normalized, span). Add a pure extractor
    server/utils/entities.ts (regex for $amounts/dates + aiCall for parties/products, fail-soft) and an analytics
    fn corpus_entity_search. Enables "every CFTC spoofing enforcement over $1M since 2015". Unit-test the
    deterministic extractors. Human applies the migration (material).

- id: cross-jurisdiction-matrix
  title: One-query cross-jurisdiction comparison matrix
  material: no
  model: sonnet
  depends: []
  proof: `npx nuxi typecheck` exits 0
  prompt: |
    Add POST /api/research/matrix { question, jurisdictions[] }: run the question per jurisdiction (filter by
    corpus_documents jurisdiction/source) and return a structured table {jurisdiction -> {stance, top_authority,
    cites}}. Render-ready JSON for a comparison grid. Read-only.

- id: synthetic-qgen-eval
  title: Self-growing eval via synthetic question generation
  material: no
  model: sonnet
  depends: []
  proof: `node --check corpus/eval/gen-golden.mjs && npx vitest run corpus/eval/__tests__/gen.test.ts`
  prompt: |
    The golden set is 10 hand-written cases; it must scale with the corpus. Write corpus/eval/gen-golden.mjs:
    for a sample of docs, aiCall to generate a realistic question whose answer is THAT doc, append to
    eval/golden-set.json (dedupe). Add a pure validator (well-formed case shape) + unit test. Keeps the
    regression gate honest as coverage 100×s. Repo-only writes.

- id: brief-citation-mining
  title: Authority weighting from real practice (brief citation mining)
  material: no
  model: sonnet
  depends: []
  proof: `node --check corpus/scripts/mine-brief-citations.mjs`
  prompt: |
    Winning briefs reveal which authorities matter. Write corpus/scripts/mine-brief-citations.mjs: pull briefs
    via CourtListener (reuse harvest-courtlistener.mjs patterns + token), run extractCitations over them, and
    upsert a practice_weight per normalized citation (how often the bar actually cites it). Feed this into the
    authority-weighting signal (Wave-1). Data/harvester task; live run is operator-gated. Proof = node --check.

- id: corpus-metered-api
  title: Metered external API for the cited corpus (productization)
  material: yes
  model: sonnet
  depends: []
  proof: `npx nuxi typecheck` exits 0
  prompt: |
    Expose the cited query layer as a metered external API: POST /api/public/corpus/query gated by an API-key
    table (api_keys: key_hash, owner, plan, monthly_quota) with per-key rate/quota metering (reuse any existing
    metering util). Returns cited results + coverage; NEVER emits reference_only bodies verbatim. Touches
    auth + (future) billing -> material. typecheck 0 errors; quota logic unit-tested if a helper is added.

OPERATOR:
  - Approve prod apply of migrations: passage-index, research_proofs, corpus_entities, api_keys.
  - Confirm commercial/legal posture before enabling corpus-metered-api publicly (data is public record; the product is the curation — confirm ToS + that reference_only material is never served).
  - VOYAGE_API_KEY required for passage/HyDE vector paths; COURTLISTENER_API_TOKEN for brief-citation-mining.
