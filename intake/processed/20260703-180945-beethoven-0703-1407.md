PROJECT: beethoven

# Orchestrator self-improvement: a fleet-wide CADE scorecard so the orchestrator can
# route the NEXT CADE improvement to whichever app is currently weakest. beethoven
# self-improvement runs FIRST. Runner is Python + pytest (runner/tests/).

- id: cade-fleet-scorecard
  title: Fleet CADE scorecard + route-to-weakest-app hint
  material: yes
  model: sonnet
  depends: []
  proof: `pytest runner/tests/test_cade_scorecard.py -q` exits 0
  prompt: |
    Add `runner/cade_scorecard.py`: a pure aggregator that takes per-app CADE telemetry
    (win-rate lift, calibration gap, alignment recall/surprise, override failure-rate —
    the shapes returned by apparently's `GET /api/firm-api/positions/kpi` and
    `/alignment-kpi`) and produces (a) a normalized fleet scorecard and (b) a
    `weakest_app` + `recommended_next_capability` hint the scheduler can use to prioritize
    the next CADE task. Pure functions only (inputs passed in; no live HTTP in the unit
    test). Add `runner/tests/test_cade_scorecard.py` covering normalization, weakest-app
    selection, tie-breaks, and empty input. Do NOT wire it into the live scheduler in this
    task (that is a follow-up, human-approved) — just land the tested pure module so the
    build stays green. Reuse the runner's existing scoring/serialization patterns
    (see runner/tests/test_model_routing.py, test_ev_scheduler.py). This closes the loop:
    the fleet gets one CADE north-star and self-directs improvement to the weakest app.

OPERATOR:
  - Wiring the scorecard into the live EV scheduler (so it actually reprioritizes) is a separate human-approved change — not queued here.
  - Each app must expose its CADE KPI endpoints (apparently already does; smarter/tomorrow via their CADE intakes) for live telemetry.
