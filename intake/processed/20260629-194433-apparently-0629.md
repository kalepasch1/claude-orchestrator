PROJECT: apparently

- id: applier-apply-path-fixes
  title: Fix self-improvement applier — prompt-refinement dedup + rlo_drafting_templates column drift
  material: yes
  model: sonnet
  depends: []
  proof: `npx vitest run server/engines/self-assessment/__tests__/applier-fixes.test.ts` exits 0
  prompt: |
    File: server/engines/self-assessment/improvement-applier.ts

    Two real bugs to fix in this one file (a validated reference patch exists at the
    Cowork output `apparently-apply-pipeline-fixes.patch`; reproduce its intent):

    1) applyPromptRefinement appends a "## Correction-Based Refinements" block every
       cycle with no dedup, so the bot system_prompt grows unbounded (the live
       citation-verifier prompt had 4–5 byte-near-identical blocks). Add a pure helper
       `dedupRefinementBlocks(prompt)` that: splits on /(?=#{1,3}\s*Correction-Based Refinements)/,
       preserves any base text before the first block, dedupes blocks by normalized
       content (strip dates `(20\d\d-\d\d-\d\d)`, collapse whitespace, lowercase), and
       keeps only the 2 most-recent unique blocks. Call it at the top of
       applyPromptRefinement: `change = { ...change, new_prompt: dedupRefinementBlocks(change.new_prompt) }`.

    2) applyTemplateUpdate writes columns that DO NOT EXIST. The rlo_drafting_templates
       table columns are: vertical, template_name, template_body, template_type, template_key
       (NOT gaming_vertical, NOT content). Fix create_template to insert
       { template_key: `${vertical}_default`, vertical, template_name: `Default ${vertical} Opinion Template`,
       template_type: 'standard', template_body: change.template_body || change.content || '{}' }
       where `const vertical = change.vertical || change.gaming_vertical`. Make it idempotent:
       first SELECT id where vertical=vertical limit 1 maybeSingle(); if it exists, return
       { action:'create_template', skipped:true, reason:'template already exists', template_id, vertical }.
       Fix update_template to read/write template_body (not content).

    Add a vitest test at server/engines/self-assessment/__tests__/applier-fixes.test.ts that
    (a) asserts dedupRefinementBlocks collapses 4 duplicate blocks to <=2 and is idempotent,
    and (b) asserts applyTemplateUpdate builds the insert with vertical/template_body/template_name
    (mock the supabase client). Do not change any other apply path. Keep TypeScript strict-clean.

- id: applier-loud-failures
  title: Surface apply failures instead of leaving proposals silently approved-but-unapplied
  material: yes
  model: sonnet
  depends: [applier-apply-path-fixes]
  proof: `npx vitest run server/engines/self-assessment/__tests__/apply-failure.test.ts` exits 0
  prompt: |
    Today when applyProposal returns { success:false } the row sits status='approved',
    applied_at=null forever and the hourly approval-scanner retries it silently (3 gaming
    template_updates + 1 prompt_refinement were stuck for days with no signal).

    In server/api/cron/approval-scanner.get.ts: track an attempt counter per proposal
    (store in rollback_data.apply_attempts or a dedicated column added via migration —
    prefer rollback_data to avoid a migration). When applyProposal fails AND
    (attempts >= 3 OR created_at older than 6h), set the proposal status='apply_failed',
    write the error into rejection_reason, and postAlert() to #apparently-alerts with the
    proposal id + error. Never leave an item retrying silently. Add a vitest test
    (server/engines/self-assessment/__tests__/apply-failure.test.ts) that simulates 3 failed
    applies and asserts the status flips to 'apply_failed' and an alert is emitted (mock postAlert).
    Keep the happy path (success) unchanged.

- id: proposal-generation-dedup
  title: Hash-dedup refinement/proposal generation so duplicate guidance is never created
  material: no
  model: sonnet
  depends: [applier-apply-path-fixes]
  proof: `npx vitest run server/engines/self-assessment/__tests__/generation-dedup.test.ts` exits 0
  prompt: |
    Root-cause for the prompt bloat + duplicate proposals: the generator appends/creates
    a new refinement even when equivalent guidance already exists. In the prompt-refinement
    generator (generatePromptFromCorrections and/or wherever improvement_proposals of
    category 'prompt_refinement' are created), before creating/appending: normalize the
    candidate block (strip dates, collapse whitespace, lowercase) and compute a hash; skip
    creation if (a) an equivalent normalized block already exists in the target bot's live
    system_prompt, or (b) an open improvement_proposals row for the same bot_key carries an
    equivalent block. Return the existing reference instead of creating a duplicate. Add a
    vitest test (…/__tests__/generation-dedup.test.ts) proving a second identical candidate
    yields no new proposal. Pure/unit-testable; mock supabase.

OPERATOR:
  - Remove a stale git lock left by a sandbox tool (one-time): `rm -f /Users/kpasch/Documents/apparently/.git/index.lock`
  - After CI passes on auto/self-improvement, approve the prod merge of applier-apply-path-fixes (material — touches the self-improvement core that edits legal-bot prompts).
  - To enable native Slack one-click approvals (currently fails closed): set SLACK_SIGNING_SECRET in prod env + configure the Slack app's Event Subscriptions / Interactivity request URLs.
  - Note: the live citation-verifier prompt was already de-bloated in the DB (4 blocks -> 1, logged in apparently.cowork_change_log for rollback); these tasks fix the CODE so it can't recur.
