PROJECT: smarter

# Self-driving legal opinion — SMARTER side (opinion generation + CREG extraction + hedge-priced negotiation).
# Part of a coordinated 3-repo build: smarter (opinion/CREG), apparently (reg_facts grounding + CEPL + perpetual memo),
# tomorrow (standardized CEPL hedge market). Builds on the PLOEH intake pair (apparently-0629-ploeh.md,
# tomorrow-0629-ploeh.md) already queued. Shared, versioned schema lives in shared/schemas/creg.ts + cepl.ts and MUST
# match across all three repos (a CREG node = one exposure; a contract = a weighted CEPL basket + basis scores +
# thin bespoke residual). Joined by the signed S2S seam (PLOEH_S2S_SECRET). Reuse Smarter's existing opinion drafting,
# QC committee (qc.ts), citation verification (citations.ts), war-room dealIntel/negotiation, omni-channel ingest
# (ingest.ts + connectors), swarm/masteryLibrary, apparentlyExport.ts. FIRST STEP: grep for existing CREG/opinion-hedge
# scaffolding and extend it. Conventions: TS strict; provider seam (llm.ts) for AI; pure cores unit-testable.

- id: creg-schema
  title: Shared CREG + CEPL basket schema (the spine all three repos agree on)
  material: no
  model: sonnet
  depends: []
  proof: "`npx vitest run tests/creg/schema.test.ts` exits 0 (Zod schemas parse a CREG with exposure nodes each carrying {riskType, triggerCondition, severity, counterparty, jurisdiction, dependsOnClause, dependsOnAuthority} and a CEPL basket = weighted primitive legs + basisScore + residualFlag; round-trips)"
  prompt: |
    Define the versioned shared spine in shared/schemas/{creg,cepl}.ts (Zod): CregNode (one exposure) and Creg
    (a contract's exposure graph), plus CeplBasket (weighted standardized primitive legs + per-leg basisScore +
    a bespoke-residual descriptor). Version the schema (schemaVersion field). This is imported verbatim by the
    apparently + tomorrow CREG intakes — keep it minimal, serializable, and S2S-payload-ready. Unit-test parse +
    round-trip + version.

- id: creg-extraction
  title: Extract a CREG from a contract/opinion (clauses → exposure nodes)
  material: no
  model: opus
  depends: [creg-schema]
  proof: "`npx vitest run tests/creg/extraction.test.ts` exits 0 (extractCreg over a fixture contract returns exposure nodes for indemnity/termination/regulatory-dependency/rep-warranty clauses, each linked to its clause + the authority it relies on)"
  prompt: |
    Turn Smarter's opinion analysis into structured exposure. Engine server/utils/creg/extraction.ts:
    extractCreg(contract, opinion) uses the existing clause analysis + callClaudeStructured to emit CregNode[] —
    one node per material exposure (indemnity cap, termination trigger, regulatory dependency, rep/warranty,
    covenant, counterparty-credit), each tagged with the clause it comes from and the legal authority it depends on
    (so a reg_fact change can later invalidate it). Pure extraction core testable with a fixture contract + mocked
    LLM. This CREG is the machine-readable backbone the prose opinion is generated FROM.

- id: opinion-as-code
  title: Opinion-as-code — render prose FROM the CREG; recompute conclusions on change
  material: no
  model: opus
  depends: [creg-extraction]
  proof: "`npx vitest run tests/creg/opinion-as-code.test.ts` exits 0 (renderOpinion(creg) produces prose; changing a fact/clause input recomputes the affected node's conclusion deterministically and regenerates only the affected prose)"
  prompt: |
    Make the opinion a continuously-evaluated object, not a static document. Engine
    server/utils/creg/opinion-as-code.ts: each CregNode conclusion is a function of {verified facts, clause text};
    renderOpinion(creg) generates prose from the graph; recomputeNode(node, inputs) re-derives a conclusion when an
    input changes; regenerating prose only for changed nodes. So the opinion never silently goes stale and never
    needs a human re-read. Pure core unit-tested (deterministic recompute + selective regen). Escalate genuinely
    novel conclusions to counsel via the existing Novelty Sentinel (UPL-safe).

- id: clause-hedge-pricing-feedback
  title: Feed each clause's CEPL hedge price into the negotiation optimizer
  material: no
  model: sonnet
  depends: [creg-extraction]
  proof: "`npx vitest run tests/creg/clause-pricing.test.ts` exits 0 (given CEPL basket prices per clause from the S2S feed, the negotiation optimizer minimizes concessionCost + hedgeCost and ranks concessions by net economic value)"
  prompt: |
    Make negotiation economic. Each CregNode maps to a CEPL basket whose price comes from Tomorrow over the S2S
    feed. Engine server/utils/creg/clause-pricing.ts: annotate each clause with its residual-risk hedge price and
    extend the war-room concession optimizer (dealIntel/negotiation) to minimize concessionCost + hedgeCost — "this
    indemnity is 12bp/yr to hedge; concede or price it in." Pure optimizer core unit-tested. No firm can quote this
    because they can't price the residual.

- id: self-originating-opinions
  title: Auto-generate opinion + CREG + hedge offer on any signed/changed contract
  material: no
  model: sonnet
  depends: [creg-extraction]
  proof: "`npx vitest run tests/creg/self-originating.test.ts` exits 0 (a new/changed contract arriving via ingest triggers extractCreg + an opinion draft + a hedge-offer payload; a spam/non-contract artifact does not)"
  prompt: |
    Zero-intake. Wire Smarter's omni-channel ingestion (ingest.ts + email/Slack/Teams/DocuSign connectors) so that
    when a contract is signed or amended, the system auto-runs extractCreg → opinion draft → emits a hedge-offer
    payload (CEPL basket) to the client. The opinion writes itself when the contract appears. Reuse the existing
    classifier to skip non-contracts. Unit-test the trigger path (mocked ingest). Human approval still gates issuance.

- id: trust-bundle-assembly
  title: Attach proof + warranty + hedge references to every opinion (the trust bundle)
  material: yes
  model: sonnet
  depends: [opinion-as-code]
  proof: "`npx vitest run tests/creg/trust-bundle.test.ts` exits 0 (assembleTrustBundle attaches a proof-carrying attestation, a warranty-offer ref, and a CEPL hedge-basket ref to an opinion; a bundle missing any component is flagged incomplete)"
  prompt: |
    The categorical differentiator vs a firm. Engine server/utils/creg/trust-bundle.ts: assembleTrustBundle(opinion)
    attaches (1) a proof-carrying attestation (from apparently's attestation-signing-core / proof-carrying work),
    (2) a warranty-offer reference (the filing/opinion warranty), (3) a CEPL hedge-basket reference (Tomorrow).
    Opinion + proof + warranty + hedge is a different product than prose + disclaimer. MATERIAL: it represents
    coverage/guarantee — the bundle is assembled + human-gated before it's presented as binding; do not auto-bind.

- id: creg-s2s-emit
  title: Emit CREG + CEPL basket + basis scores to apparently/tomorrow over signed S2S
  material: yes
  model: sonnet
  depends: [creg-schema, creg-extraction]
  proof: "`npx vitest run tests/creg/s2s-emit.test.ts` exits 0 (signs a CREG/CEPL payload; a tampered/expired envelope is rejected on the receiving-schema validation)"
  prompt: |
    Build Smarter's side of the seam (HMAC, PLOEH_S2S_SECRET, mirror the established S2S pattern). Emit the CREG +
    its CEPL basket + basis scores to apparently (for reg_facts grounding) and tomorrow (for pricing/hedging).
    Validate against shared/schemas. Unit-test sign/verify/expiry/tamper. MATERIAL: cross-system trust boundary;
    risk data only, no funds/instructions.

- id: creg-ops-surface
  title: Opinion + CREG + live hedge-price UI
  material: no
  model: sonnet
  depends: [opinion-as-code, clause-hedge-pricing-feedback]
  proof: "`npx vue-tsc --noEmit` exits 0 AND the surface renders the CREG exposure nodes, the generated opinion prose, per-clause hedge prices, and the trust-bundle status against real data"
  prompt: |
    Surface it: a per-contract view showing the CREG exposure nodes, the opinion-as-code prose, each clause's live
    CEPL hedge price, and the trust-bundle (proof/warranty/hedge) status. Read-mostly; reuse existing scaffolding.
    Prove with vue-tsc clean.

OPERATOR:
  - Counsel/UPL review: whether an auto-generated, self-updating opinion constitutes legal advice and how the Novelty Sentinel escalation + human sign-off must gate issuance. Load-bearing before any opinion is presented as binding.
  - Provision PLOEH_S2S_SECRET (shared across smarter/apparently/tomorrow) + the attestation signing key.
  - Approve the shared shared/schemas/{creg,cepl}.ts version with the apparently + tomorrow teams before dependent tasks build (one schema, three repos).
