PROJECT: beethoven

# Orchestrator-internal CADE/deliberation improvements. These are ADDITIVE and must not conflict with the
# already-queued serialized committees.py chain (cade-contracts → … → cade-counter-bandit-causal →
# cade-determination-rlhf) or my live modules (committees.py certify()/deliberation_onepager(),
# determinations table, outcome_instrument.py, constitution.py, causal_attribution.py, determination_actions
# trigger). To stay conflict-free: everything below except the harness lives in NEW files (runner/cx_*.py)
# and is discovered dynamically, so it never edits committees.py/periodic.py/runner.py and can run in
# parallel. Only the harness touches the shared scheduler files, so nothing else here does. All of it reads
# tables that already exist (determinations, determination_outcomes, committee_scoreboard, constitution_checks,
# committee_experiments, owner_overrides, approvals). Reuse existing helpers in committees.py (db.select/insert,
# review(), _consensus, _cal_weight) — do not reimplement.

- id: cade-extras-harness
  title: Dynamic extension harness — discover + run runner/cx_*.py on a schedule
  material: no
  model: sonnet
  depends: [cade-certificate-proof-constitution]
  proof: `python3 -m py_compile runner/cade_extras.py runner/periodic.py runner/runner.py` exits 0 (runner-only change; frontend `npm run build` stays green)
  prompt: |
    Add runner/cade_extras.py exposing run(): it globs runner/cx_*.py, imports each, and calls its run()
    inside try/except (one bad module must never break the loop; log failures). Wire ONE dispatch entry
    "cadeextras" in periodic.py's job map + a daily schedule row in runner.py's _SCHEDULE (reuse the existing
    committee* entries as the pattern). This is the ONLY task here that edits periodic.py/runner.py — all other
    cx_* tasks add new files the harness auto-runs, so they never touch shared files. Add a tiny test that a
    dummy cx_ module's run() is invoked.

- id: cx-reviewer-queue
  title: Reviewer-time optimization — rank the human queue by value-of-review
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_reviewer_queue.py runner/cade_extras.py` exits 0
  prompt: |
    Add runner/cx_reviewer_queue.py with run(): score every pending human item (approvals status=pending +
    improvement_proposals status=for_review) by expected value-of-review = materiality × contention
    (1−consensus_pct from its determination) × reversibility × the reviewer's historical override rate
    (owner_overrides). Write the ranked list into inbox (kind='review_queue') so the owner's scarce attention
    lands on the highest-leverage decisions first. Read-only except the inbox digest; no schema change.

- id: cx-outcome-horizons
  title: Multi-horizon outcome labels (7/30/90-day) for durable calibration
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_outcome_horizons.py` exits 0
  prompt: |
    Add runner/cx_outcome_horizons.py with run(): for determinations old enough, compute the realized
    revenue/usage delta at 7/30/90-day horizons (reuse outcome_instrument's signal sources: merge_revenue,
    app_revenue, committee_rollouts) and store per-horizon rows in determination_outcomes (metric like
    'revenue_30d'). Flag decays (positive at 7d, negative at 30/90d). Never guess when no signal exists. This
    lets calibration weight durability over first impressions. No schema change (reuse determination_outcomes).

- id: cx-voi-router
  title: Value-of-information experiment routing (explore where learning pays most)
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_voi_router.py` exits 0
  prompt: |
    Add runner/cx_voi_router.py with run(): find the expert domain with the fewest graded calls / worst Brier
    in committee_scoreboard (highest uncertainty), and — bounded to 1 per run — queue a small A/B experiment
    proposal targeting that domain (insert into improvement_proposals status='proposed', surface='experiment',
    divergent=false) so the system deliberately reduces its own uncertainty fastest. Reuse the hypothesis shape
    from committees.mine_hypotheses. No schema change.

- id: cx-constitution-learn
  title: Self-hardening constitution — propose new predicates from near-misses
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_constitution_learn.py` exits 0
  prompt: |
    Add runner/cx_constitution_learn.py with run(): scan constitution_checks failures + committee_rollouts
    auto-rollbacks + dissent_vindicated determinations for recurring patterns a rule would have caught, and
    PROPOSE new constitution predicates as owner-ratifiable items (insert into approvals kind='material',
    status='pending', with a clear why) — never auto-activate a rule. This tightens the guardrails over time
    under human ratification. Reuse constitution.py's predicate vocabulary. No schema change.

- id: cx-shadow-cade
  title: Shadow determinations — compare CADE vs. decisions humans already made
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_shadow_cade.py` exits 0
  prompt: |
    Add runner/cx_shadow_cade.py with run(): for recently human-decided approvals (approved/denied), run
    committees.review() in SHADOW (bounded, a few per run) on the same subject and record where CADE and the
    human diverged (insert a determination_outcomes row source='shadow' + an inbox digest of divergences).
    This builds a trust/accuracy signal and free ground-truth before granting more autonomy. Read-only except
    the shadow rows + digest. Reuse committees.review; do not edit committees.py.

- id: cx-provider-divergence
  title: Cross-provider disagreement flags on high-materiality determinations
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_provider_divergence.py` exits 0
  prompt: |
    Add runner/cx_provider_divergence.py with run(): for the most recent high-materiality determinations
    (bounded to ~2 per run to stay cost-safe on subscription models), re-ask the chair-synthesis question
    across two different providers via model_policy/model_gateway and flag when their verdicts diverge (insert
    an inbox note kind='provider_divergence' naming the split). Surfaces model-specific blind spots before they
    ship. Reuse model_gateway.complete; no schema change.

- id: cx-postmortem
  title: Auto post-mortems on vindicated dissent + new red-team patterns
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_postmortem.py` exits 0
  prompt: |
    Add runner/cx_postmortem.py with run(): for determinations where dissent_vindicated became true, draft a
    short post-mortem (what the majority missed, which domain was right) and store it (inbox kind='postmortem')
    plus append a reusable red-team pattern (a one-line heuristic) to a patterns list the pre-mortem step can
    load, so the same class of miss can't recur. Reuse committees dissent data. No schema change.

- id: cx-determination-slo
  title: Determination cost/latency SLOs + quality-per-dollar auto-tune signal
  material: no
  model: sonnet
  depends: [cade-extras-harness]
  proof: `python3 -m py_compile runner/cx_determination_slo.py` exits 0
  prompt: |
    Add runner/cx_determination_slo.py with run(): aggregate determinations by materiality band and compute
    an SLO view (count, avg confidence, avg consensus, and — if available from cost_ledger — $ per
    determination) written to inbox/board_minutes, and emit a recommended panel-size / model-tier nudge per
    band toward a target quality-per-dollar (advisory only; do not change committees.py). Reuse cost_ledger if
    present; degrade gracefully if not. No schema change.

OPERATOR:
  - None required. All tasks are runner-only, additive, and read tables that already exist; no secrets, deploys, schema migrations, or legal sign-off. (The pre-existing cade-determination-federation task remains the one item that spans repos / needs the Ed25519 signing key.)
