PROJECT: smarter

- id: contracts-smarter
  title: Add vitest harness + pin integration types
  material: no
  model: sonnet
  depends: []
  proof: `npm test` exits 0
  prompt: |
    Smarter has no test runner. Install vitest, add a "test" script (vitest run) to package.json,
    create tests/ with one smoke test. Add types/integration.ts mirroring Tomorrow's
    shared/contracts/warRoomSync.ts (WarRoomUpdate, WarRoomBridgeEvent) and xappSignal.ts so the
    Smarter side of the bridge cannot drift. Add a contract-shape test asserting those types.
    See HANDOFF-cowork-master-build.md §5.

- id: smarter-warroom-bridge-activate
  title: Activate the Tomorrow war-room bridge live path
  material: yes
  model: sonnet
  depends: [contracts-smarter]
  proof: `npx vitest run tests/warroom-bridge.test.ts` exits 0
  prompt: |
    Complete the live path of server/api/warroom/bridge.post.ts against the pinned warRoomSync
    contract: real push/pull using TOMORROW_WARROOM_API_URL/KEY (OPERATOR sets these), set
    ProjectItem.tomorrowConnected on connect, and add the pre-agreed remediation-firm matter hook
    that Tomorrow's breach-remediation task calls. Test: push+pull round-trips against a contract
    mock; a breach payload creates a remediation matter. Material: cross-app negotiation + legal.

- id: smarter-opsignal-feed
  title: Emit degradation signals to Tomorrow (PII-barrier clean)
  material: yes
  model: sonnet
  depends: [contracts-smarter]
  proof: `npx vitest run tests/opsignal.test.ts` exits 0
  prompt: |
    From server/utils/standing.ts + reliabilityScore.ts + the IssueAgent, emit an xappSignal to
    Tomorrow when a matter/relationship degrades (degrading relationship, missed response, stalled
    negotiation) so Tomorrow's risk scorer can hedge the growing risk. Enforce the information
    barrier: no client PII / matter content / amounts leave Smarter. Test: a degradation event
    produces a well-formed signal excluding PII. Material: cross-app data egress.

- id: smarter-model-policy
  title: Centralize hardcoded model strings
  material: no
  model: haiku
  depends: [contracts-smarter]
  proof: `! grep -rEn "claude-(haiku|sonnet|opus)" server --include=*.ts | grep -v model-policy.ts` exits 0
  prompt: |
    Centralize the hardcoded MODELS map in server/utils/llm.ts into a new server/utils/model-policy.ts
    exposing selectModel(taskType) (mirror Apparently's pattern). Replace every literal claude-* model
    string outside model-policy.ts with the policy lookup. Proof grep returns no matches.

- id: smarter-5-95
  title: Apply 5/95 decision-budget lint over the existing trust dial
  material: no
  model: sonnet
  depends: [contracts-smarter]
  proof: `node scripts/lint-decision-budgets.mjs` exits 0
  prompt: |
    Reuse Smarter's existing trust dial (counsel_only/co_pilot/auto_pilot in server/utils/governance.ts)
    as the autonomy substrate. Port a decision-budget lint script (adapt tomorrow/scripts/
    lint-decision-budgets.mjs) for Smarter's pages; declare tight budgets for the war-room, matter-
    detail, and now/approve surfaces and collapse advanced controls behind disclosure so attorneys
    see only the 5% that matters. Proof: the lint passes for declared surfaces.

OPERATOR:
  - Set TOMORROW_WARROOM_API_URL and TOMORROW_WARROOM_API_KEY in Smarter's env before the war-room bridge can run live (tasks build/test against a mock without them).
  - Confirm the cross-app S2S secret used for opsignal egress matches Tomorrow's expected secret.
