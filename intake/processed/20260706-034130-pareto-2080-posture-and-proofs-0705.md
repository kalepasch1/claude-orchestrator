PROJECT: pareto-2080

# REVISED deltas only — most prior comms/portfolio suggestions already landed or are in-flight (posture suites exist
# for tomorrow+apparently but NOT pareto; no proof-carrying-negotiation; no counterfactual->autonomy proposer).
# House rules unchanged: invariants free/non-discretionary/non-custodial/agent-of-user/writeback; pure engines in
# server/utils + tests/*.test.js (node:test, `npm test`); requireAuth+requireOwnership; parseInt guards; ADDITIVE ONLY;
# prepare-not-execute (propose; human commits). `npm test` must not regress beyond the 2 known failures. Inspect the
# repo before adding — if an equivalent exists, EXTEND it, do not duplicate.

- id: pareto-posture-compliance-suite
  title: Encode Pareto's posture as an executable compliance-as-tests suite + merge-gate manifest
  material: yes
  model: opus
  depends: []
  proof: `node --test tests/posture.compliance.test.js` exits 0 AND `test -f server/utils/compliance/compliance.manifest.json`
  prompt: |
    Mirror tomorrow's posture-compliance-suite pattern for Pareto's posture. Create
    server/utils/compliance/postureInvariants.js (pure predicates) + tests/posture.compliance.test.js, each invariant
    its own test, wired to the REAL guard functions (not string checks where possible):
      - free: no fee/affiliate/lead-gen/data-sale code path (assert via the existing scanner pattern).
      - non-discretionary: sessionRail.commitStagedAction rejects actor!='human' (a fixture agent-actor commit throws).
      - non-custodial: no path holds/moves funds (staged actions stop at Submit).
      - agent-of-user: every server/api/finance/* handler calls requireAuth + requireOwnership.
      - writeback: every `// @writeback-required` module actually calls requireWriteback (reuse
        scripts/check-writeback-invariant.mjs).
    Add server/utils/compliance/compliance.manifest.json = { "suite": "tests/posture.compliance.test.js",
    "command": "node --test tests/posture.compliance.test.js" } so the orchestrator compliance-gate auto-discovers it
    and BLOCKS any merge that regresses posture. Do NOT change behavior — only lock it under test. Additive.

- id: pareto-proof-carrying-negotiation
  title: Signed, offline-verifiable proof pack attached to each completed negotiation
  material: yes
  model: opus
  depends: []
  proof: `node --check server/utils/negotiationProof.js` exits 0 AND `node --test tests/negotiationProof.test.js` exits 0
  prompt: |
    Add server/utils/negotiationProof.js — a PURE assembler that, for a completed negotiation outcome, builds a
    proof pack binding: (a) the counterparty-verification result (reuse adversary-twin / counterparty verification if
    present; else a pluggable port), (b) a within-authority check against the user's staged-action caps/authority,
    (c) the CADE determination certificate when available — consume @darwin/kernel/cade packageForReviewer/signing;
    do NOT reimplement signing. verifyNegotiationProof(pack) verifies OFFLINE with no platform secret (public-key);
    tampering with the rate, counterparty, or authority invalidates it. node:test uses an EPHEMERAL test keypair for the
    sign->verify round-trip + tamper-detection + secret-free verify (production signing key is operator-set). Non-custodial,
    agent-of-user, prepare-only. Inspect first; if a negotiation-proof exists, extend it rather than duplicate.

- id: pareto-counterfactual-autonomy-proposer
  title: Propose (never auto-apply) autonomy-cap widenings from the counterfactual "cost of holding back"
  material: yes
  model: sonnet
  depends: []
  proof: `node --check server/utils/autonomyProposer.js` exits 0 AND `node --test tests/autonomyProposer.test.js` exits 0
  prompt: |
    Add server/utils/autonomyProposer.js — a PURE engine reading counterfactualLedger rollup (forgone value where the
    user held an action they would have approved) that PROPOSES per-category autonomy-cap adjustments within each
    category's floor/ceiling. It ONLY proposes (returns a proposal surfaced in approvals / the autonomy-simulate view);
    the existing trust-autotune + a human arm it — it NEVER auto-widens autonomy (invariant 2). Bounds: never exceed
    ceiling, never below floor, and require a minimum evidence threshold (n observations) before proposing. node:test:
    forgone-cost above threshold => a bounded widen proposal; insufficient evidence => none; a proposal never
    auto-applies. Feed the proposal into the existing autonomy-simulate surface as a suggestion only.

OPERATOR:
  - DARWIN_SIGNING_PRIVATE_KEY_PEM in prod for real negotiation-proof signing (tests use an ephemeral keypair).
  - Counsel confirms the posture-invariant list is complete BEFORE the compliance suite becomes a hard merge gate.
  - `npx prisma migrate deploy` is operator-only; no fee/affiliate/data-sale path may be introduced (invariant 1).
