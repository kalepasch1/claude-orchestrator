# Test/tooling dependencies. Runtime deps come from requirements.txt.
#
# The merge gate runs:
#   python3 -m pytest runner/tests tests -q -p no:randomly --timeout=60
# which needs pytest-timeout; it was never declared anywhere, so the gate failed
# with "unrecognized arguments: --timeout" on any machine built from the manifest.

-r requirements.txt

pytest>=8.0
pytest-timeout>=2.2

# runner/tests/test_session_proof_of_work_config.py imports pytz for the
# America/New_York deadline assertion. The import is inside a try, so the test
# silently skipped its own assertion on any machine built from the manifest.
pytz>=2024.1
