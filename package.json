{
  "name": "claude-orchestrator",
  "private": true,
  "scripts": {
    "test": "python3 -m pytest runner/tests -q --tb=short && npm --prefix web test -- --run",
    "test:python": "python3 -m pytest runner/tests -q --tb=short",
    "test:web": "npm --prefix web test -- --run",
    "build": "npm --prefix web run build"
  }
}
