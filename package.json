{
  "name": "claude-orchestrator",
  "private": true,
  "version": "0.1.0",
  "description": "Claude Orchestrator — multi-agent fleet coordination",
  "scripts": {
    "test": "python3 -m pytest runner/tests -x -q",
    "test:verbose": "python3 -m pytest runner/tests -v",
    "test:kernel": "cd packages/darwin-kernel && npm test"
  }
}
