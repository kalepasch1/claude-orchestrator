/**
 * Domain entities of the compounding code-generation platform spine.
 *
 * Import operations from `shared/contracts`; import the records those
 * operations act on from here. Declaration-only — no runtime code.
 */
export type * from "./compound.ts";
