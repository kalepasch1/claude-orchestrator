# Legacy short-form migrations

These migrations predate the canonical timestamped Supabase history fetched from
the linked production project. They are preserved for audit and schema archaeology,
but intentionally live outside `supabase/migrations` so the CLI sees one unique,
deterministic version sequence.

Do not move these files back into the active migration directory. New migrations
must use timestamp versions generated by `supabase migration new`.
