# Runtime dependencies for the orchestrator (`runner` package).
#
# This file previously listed only requests/python-dotenv/prometheus-client, so a
# fresh clone that ran `pip install -r requirements.txt` still could not import
# large parts of `runner/` — the missing packages below are imported by modules
# that ship in this repo. Lower bounds only (no upper pins) to avoid the version
# conflicts that made the original install fail; they match the versions the
# suite is currently green against.
#
# Verify with:  python3 scripts/verify_deps.py

requests>=2.28
python-dotenv>=1.0
prometheus-client>=0.17

# Config / serialization
PyYAML>=6.0
python-dateutil>=2.8

# Process + host introspection (capacity pacer, lane medic, zombie reaper)
psutil>=5.9

# Network clients
aiohttp>=3.9
websockets>=12.0

# Model providers
anthropic>=0.40

# SQL parsing / static analysis used by the gates
sqlglot>=25.0
cryptography>=42.0
pyflakes>=3.0

# Git operations used by scripts/delete_remote_branches.py — imported at
# runtime (guarded by try/except ImportError with an install hint), so it was
# easy to miss here even though the script hard-fails without it.
GitPython>=3.1
