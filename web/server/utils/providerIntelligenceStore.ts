import {serviceClient} from './fleetSupabase'
import {applyFieldMappings,assignHoldout,estimateCausalEffects,inferFieldMappings,intelligenceDigest,normalizeFinalityEvent,rankProviderAccounts,simulateProviderMutation} from './providerIntelligence'

const baselineHours:Record<string,number>={send_payment:.5,issue_credit:.4,prepare_invoice:.35,issue_contract:1.5,sign_contract:1,send_renewal_notice:.75,prepare_employee_record:1.25,prepare_payroll_run:3,submit_payroll:1.5,prepare_tax_return:4,reconcile_tax_base:2}

export async function assignProviderExperiment(organizationId:string,sagaId:string,experimentKey='provider-intelligence-v1'){const sb=serviceClient(),assignmentUnit=`${organizationId}:${sagaId}:${experimentKey}`,assignment=assignHoldout(assignmentUnit,.1);const {data,error}=await sb.from('provider_experiment_assignments').upsert({organization_id:organizationId,saga_id:sagaId,experiment_key:experimentKey,variant:assignment.variant,assignment_unit:assignmentUnit,assignment_digest:assignment.assignment_digest},{onConflict:'saga_id,experiment_key'}).select().single();if(error)throw new Error('provider_experiment_assignment_failed');return data}

export async function prepareProviderExecution(saga:any,step:any,accounts:any[]){const sb=serviceClient();let {data:assignment}=await sb.from('provider_experiment_assignments').select('*').eq('saga_id',saga.id).eq('experiment_key','provider-intelligence-v1').maybeSingle();if(!assignment)assignment=await assignProviderExperiment(saga.organization_id,saga.id);const treatment=assignment.variant==='treatment';let effectiveInput=step.input||{},applied:string[]=[];if(treatment){const {data:mappings}=await sb.from('provider_field_mappings').select('*').eq('organization_id',saga.organization_id).in('provider',accounts.map(account=>account.provider)).eq('operation',step.operation).gte('confidence',.9).gte('support',3);const mapped=applyFieldMappings(effectiveInput,mappings||[]);effectiveInput=mapped.input;applied=mapped.applied}let ranked=accounts.map((account,index)=>({account,score:1-index*.001,profile:null}));if(treatment&&accounts.length){const jurisdiction=String(effectiveInput.jurisdiction||'*'),{data:profiles}=await sb.from('provider_route_profiles').select('*').eq('organization_id',saga.organization_id).eq('operation',step.operation).in('connector_account_id',accounts.map(account=>account.id)).in('jurisdiction',[jurisdiction,'*']);ranked=rankProviderAccounts(accounts,profiles||[],step.operation,jurisdiction)}const selected=ranked[0];if(!selected)throw new Error('connected_provider_required');const twin=simulateProviderMutation(selected.account.provider,step.operation,effectiveInput),requestDigest=intelligenceDigest({provider:selected.account.provider,operation:step.operation,input:effectiveInput,idempotency_key:step.idempotency_key});const {data:twinRun,error}=await sb.from('provider_digital_twin_runs').upsert({organization_id:saga.organization_id,saga_id:saga.id,step_id:step.id,provider:selected.account.provider,operation:step.operation,request_digest:requestDigest,scenario_digest:twin.scenario_digest,verdict:twin.verdict,checks:twin.checks,predicted:{...twin.predicted,route_score:selected.score,field_mappings_applied:applied}},{onConflict:'step_id,request_digest'}).select().single();if(error)throw new Error('provider_digital_twin_persistence_failed');const failed=twin.checks.filter(check=>!check.passed).map(check=>check.key);if(twin.verdict==='block')throw new Error(`digital_twin_block:${failed.join(',')}`);if(String(effectiveInput.environment||'sandbox')==='production'&&twin.verdict==='review')throw new Error(`missing_provider_input:${failed.join(',')}`);return{account:selected.account,input:effectiveInput,assignment,twinRun,routeScore:selected.score,fieldMappingsApplied:applied}}

async function updateRouteProfile(execution:any,success:boolean,latencyMs:number){const sb=serviceClient(),jurisdiction=String(execution.response_meta?.jurisdiction||'*');const {data:current}=await sb.from('provider_route_profiles').select('*').eq('connector_account_id',execution.connector_account_id).eq('operation',execution.operation).eq('jurisdiction',jurisdiction).maybeSingle();const n=Number(current?.sample_size||0),sample=n+1,failureRate=((Number(current?.failure_rate||0)*n)+(success?0:1))/sample,p50=n?Math.round((Number(current?.p50_settlement_ms||0)*.8)+(latencyMs*.2)):latencyMs;await sb.from('provider_route_profiles').upsert({organization_id:execution.organization_id,connector_account_id:execution.connector_account_id,provider:execution.provider,operation:execution.operation,jurisdiction,expected_fee_bps:Number(current?.expected_fee_bps||0),p50_settlement_ms:p50,failure_rate:failureRate,risk_score:Number(current?.risk_score||0),sample_size:sample,updated_at:new Date().toISOString()},{onConflict:'connector_account_id,operation,jurisdiction'})}

export async function learnFromVerifiedExecution(execution:any){const sb=serviceClient(),now=Date.now(),started=new Date(execution.provider_started_at||execution.created_at).getTime(),latency=Math.max(0,now-started);const {data:step}=await sb.from('agentic_business_saga_steps').select('input').eq('id',execution.step_id).maybeSingle();for(const mapping of inferFieldMappings(execution.provider,execution.operation,step?.input||{})){const {data:current}=await sb.from('provider_field_mappings').select('*').eq('organization_id',execution.organization_id).eq('provider',execution.provider).eq('operation',execution.operation).eq('source_path',mapping.source_path).eq('target_path',mapping.target_path).maybeSingle();const support=Number(current?.support||0)+1,successes=Number(current?.successes||0)+1;await sb.from('provider_field_mappings').upsert({organization_id:execution.organization_id,provider:execution.provider,operation:execution.operation,...mapping,support,successes,confidence:successes/support,last_verified_at:new Date().toISOString()},{onConflict:'organization_id,provider,operation,source_path,target_path'})}const {data:assignment}=await sb.from('provider_experiment_assignments').select('*').eq('saga_id',execution.saga_id).eq('experiment_key','provider-intelligence-v1').maybeSingle(),input=step?.input||{},providedBaseline=Number(input.baseline_hours),baseline=Math.max(0,Number.isFinite(providedBaseline)&&providedBaseline>0?providedBaseline:baselineHours[execution.operation]??.5),automated=latency/3600000,realized=Number(input.realized_financial_value),value=Number.isFinite(realized)?realized:0;await sb.from('provider_outcome_measurements').upsert({organization_id:execution.organization_id,saga_id:execution.saga_id,assignment_id:assignment?.id||null,provider:execution.provider,operation:execution.operation,variant:assignment?.variant||'control',automated_hours:automated,baseline_hours:baseline,hours_saved:Math.max(0,baseline-automated),error_count:0,settlement_latency_ms:latency,financial_value:value,currency:String(input.currency||'USD').slice(0,8),non_regressed:true,evidence:{execution_id:execution.id,receipt_digest:execution.receipt_digest,measurement_contract:'provider-outcome/v1',hours_baseline_source:Number.isFinite(providedBaseline)&&providedBaseline>0?'observed_prior_process':'versioned_benchmark',financial_value_source:Number.isFinite(realized)?'realized_input':'unavailable'}},{onConflict:'saga_id,operation'});await updateRouteProfile(execution,true,latency)}

export async function learnFromFailedExecution(execution:any,errorCode:string){const sb=serviceClient(),now=Date.now(),started=new Date(execution.provider_started_at||execution.created_at).getTime(),latency=Math.max(0,now-started),{data:assignment}=await sb.from('provider_experiment_assignments').select('*').eq('saga_id',execution.saga_id).eq('experiment_key','provider-intelligence-v1').maybeSingle();await sb.from('provider_outcome_measurements').upsert({organization_id:execution.organization_id,saga_id:execution.saga_id,assignment_id:assignment?.id||null,provider:execution.provider,operation:execution.operation,variant:assignment?.variant||'control',automated_hours:latency/3600000,baseline_hours:baselineHours[execution.operation]||.5,hours_saved:0,error_count:1,settlement_latency_ms:latency,financial_value:0,non_regressed:false,evidence:{execution_id:execution.id,error_digest:intelligenceDigest(errorCode),measurement_contract:'provider-outcome/v1'}},{onConflict:'saga_id,operation'});await updateRouteProfile(execution,false,latency)}

export async function recordFinalityEvent(execution:any,event:any,source:'webhook'|'poll'|'read_after_write'='webhook'){const sb=serviceClient(),normalized=normalizeFinalityEvent(execution.provider,event,source);const {data,error}=await sb.from('business_provider_finality_events').upsert({organization_id:execution.organization_id,connector_account_id:execution.connector_account_id,execution_id:execution.id,...normalized,verified:true},{onConflict:'connector_account_id,provider_event_id'}).select().single();if(error)throw new Error('normalized_finality_persistence_failed');return data}

export async function providerIntelligenceDashboard(organizationId:string){const sb=serviceClient(),q=(table:string,limit=100)=>sb.from(table).select('*').eq('organization_id',organizationId).order('created_at',{ascending:false}).limit(limit);const [mappings,outcomes,twins,adapters,finality,routes,leases]=await Promise.all([q('provider_field_mappings',100),q('provider_outcome_measurements',500),q('provider_digital_twin_runs',100),q('provider_adapter_manifests',50),q('business_provider_finality_events',100),sb.from('provider_route_profiles').select('*').eq('organization_id',organizationId).order('updated_at',{ascending:false}).limit(100),q('provider_credential_grants',50)]);const rows=(result:any)=>result.error?[]:result.data||[],outcomeRows=rows(outcomes);return{mappings:rows(mappings),outcomes:outcomeRows,twins:rows(twins),adapters:rows(adapters),finality:rows(finality),routes:rows(routes),leases:rows(leases),causal:estimateCausalEffects(outcomeRows),summary:{learned_mappings:rows(mappings).filter((item:any)=>item.support>=3&&item.confidence>=.9).length,twin_blocks:rows(twins).filter((item:any)=>item.verdict==='block').length,active_adapters:rows(adapters).filter((item:any)=>item.status==='active').length,verified_finality:rows(finality).filter((item:any)=>item.verified).length,active_leases:rows(leases).filter((item:any)=>item.status==='active'&&new Date(item.expires_at)>new Date()).length}}}
