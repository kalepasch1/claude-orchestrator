import {describe,expect,it} from 'vitest'
import {sanitizePortfolioSimulation,simulateSafePortfolio} from './portfolioSimulation'
const venture={archetype:'saas',stage:'growth',team_size:'2_5',ai_spend:'1000_5000',objectives:['reduce_cost'],capabilities:['billing'],tools:['openai'],constraints:['privacy_first']}
describe('privacy-safe public portfolio simulation',()=>{it('never echoes sensitive freeform fields',()=>{const result=simulateSafePortfolio({ventures:[{...venture,company_name:'SecretCo',repository:'github.com/private',customer_data:'secret'}]});expect(JSON.stringify(result)).not.toContain('SecretCo');expect(JSON.stringify(result)).not.toContain('github.com/private');expect(result.privacy_receipt.stored).toBe(false)});it('rejects unbounded categories and oversized portfolios',()=>{expect(()=>sanitizePortfolioSimulation({ventures:[{...venture,archetype:'SecretCo'}]})).toThrow('INVALID_CATEGORY');expect(()=>sanitizePortfolioSimulation({ventures:Array(26).fill(venture)})).toThrow('PORTFOLIO_SIZE')})})
