import {describe,expect,it} from 'vitest'
import {assessSettlement,evaluateDisclosure} from './hivemindAdvanced'
describe('advanced hivemind safeguards',()=>{it('never approves proprietary disclosure fields',()=>{const x=evaluateDisclosure({fields:['pattern_summary','source_code','customer_identity'],audience:'member_network'},0,100);expect(x.approved_fields).not.toContain('source_code');expect(x.rejected_fields).toEqual(expect.arrayContaining(['source_code','customer_identity']))});it('blocks exhausted privacy budgets',()=>{expect(evaluateDisclosure({fields:['pattern_summary','aggregate_outcomes'],audience:'public'},95,100).decision).toBe('redact')});it('settles only adequately sampled causal value',()=>{const x=assessSettlement({baseline_value_cents:10000,treatment_value_cents:14000,baseline_samples:100,treatment_samples:100,confidence:.9});expect(x.decision).toBe('settle');expect(x.incremental).toBe(4000);expect(x.rebate).toBeGreaterThan(0)});it('blocks implausible or undersampled claims',()=>{const x=assessSettlement({baseline_value_cents:100,treatment_value_cents:5000,baseline_samples:5,treatment_samples:5,confidence:.99});expect(x.decision).toBe('blocked');expect(x.rebate).toBe(0)})})
