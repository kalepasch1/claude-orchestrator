import{organizationContext,requireOrgAdmin}from'../../../utils/adaptiveFabric'
import{requireConnectorUser}from'../../../utils/connectorFabric'
import{resolveCredential}from'../../../utils/businessProviderFabric'
import{runSandboxConformance}from'../../../utils/providerIntelligence'
import{serviceClient}from'../../../utils/fleetSupabase'
function authHeaders(manifest:any,credential:any){const headers:Record<string,string>={};for(const auth of manifest.auth||[]){if(auth.type==='http'&&auth.scheme==='bearer'){const token=credential.access_token||credential.api_key;if(token)headers.authorization=`Bearer ${token}`}else if(auth.type==='http'&&auth.scheme==='basic'){const username=credential.username||credential.account_id,password=credential.password||credential.license_key;if(username&&password)headers.authorization=`Basic ${Buffer.from(`${username}:${password}`).toString('base64')}`}else if(auth.type==='apiKey'){const value=credential[auth.key]||credential.api_key;if(value)headers[auth.name]=String(value)}}return headers}
export default defineEventHandler(async event=>{const user=await requireConnectorUser(event),context=await organizationContext(user);requireOrgAdmin(context);const body=await readBody<any>(event),sb=serviceClient(),org=context.membership.organization_id,{data:adapter}=await sb.from('provider_adapter_manifests').select('*').eq('id',String(body.adapter_id||'')).eq('organization_id',org).eq('status','conformance_pending').maybeSingle();if(!adapter)throw createError({statusCode:404,message:'pending_adapter_not_found'});const{data:account}=await sb.from('connector_accounts').select('*').eq('organization_id',org).eq('provider',adapter.provider).eq('environment','sandbox').eq('status','connected').limit(1).maybeSingle();if(!account)throw createError({statusCode:409,message:'connected_sandbox_provider_required'});const credential=await resolveCredential(account,org),auth=authHeaders(adapter.manifest,credential),result=await runSandboxConformance(adapter.manifest,Array.isArray(body.tests)?body.tests:[],async request=>{const response:any=await $fetch.raw(request.url,{method:request.method,headers:{...request.headers,...auth},body:request.body,timeout:30000,ignoreResponseError:true});return{status:response.status}});const{data:run,error}=await sb.from('provider_adapter_conformance_runs').insert({organization_id:org,adapter_id:adapter.id,environment:'sandbox',tests:result.tests,passed:result.passed,failed:result.failed,status:result.status,evidence_digest:result.evidence_digest}).select().single();if(error)throw createError({statusCode:500,message:'conformance_result_persistence_failed'});await sb.from('provider_adapter_manifests').update({status:result.status==='passed'?'active':'rejected',activated_at:result.status==='passed'?new Date().toISOString():null}).eq('id',adapter.id);return{run,adapter_status:result.status==='passed'?'active':'rejected'}})
