{
  "git": {
    "deploymentEnabled": {
      "*": false,
      "master": true
    }
  },
  "installCommand": "npm ci --ignore-scripts --prefer-offline --no-audit --no-fund",
  "ignoreCommand": "bash -c 'git diff --quiet ${VERCEL_GIT_PREVIOUS_SHA:-HEAD^} HEAD -- . ../packages/darwin-kernel'",
  "crons": [
    {
      "path": "/api/fleet/analytics?rangeDays=1&sync=true",
      "schedule": "0 6 * * *"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Frame-Options",
          "value": "SAMEORIGIN"
        },
        {
          "key": "Content-Security-Policy",
          "value": "frame-ancestors 'self' https://*.vercel.app"
        }
      ]
    }
  ]
}
